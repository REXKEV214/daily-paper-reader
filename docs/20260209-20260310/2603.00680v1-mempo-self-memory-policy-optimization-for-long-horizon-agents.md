---
title: "MemPO: Self-Memory Policy Optimization for Long-Horizon Agents"
title_zh: MemPO：面向长时程智能体的自记忆策略优化
authors: "Ruoran Li, Xinghua Zhang, Haiyang Yu, Shitong Duan, Xiang Li, Wenxin Xiang, Chonghua Liao, Xudong Guo, Yongbin Li, Jinli Suo"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00680v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 自记忆策略优化，使智能体能够自主管理记忆内容。
tldr: 针对长程智能体在交互中上下文增长导致的性能下降问题，本文提出 MemPO 算法。该方法通过自记忆策略优化，使智能体能自主总结和管理记忆。通过改进基于记忆有效性的信用分配机制，智能体可选择性保留关键信息，在显著降低 Token 消耗的同时提升任务表现，在 F1 分数和效率上均优于现有 SOTA 模型。
selection_source: fresh_fetch
motivation: 现有的外部记忆模块缺乏智能体的主动管理，且不断增长的上下文会损害长程任务的性能与稳定性。
method: 提出 MemPO 算法，通过改进基于记忆有效性的信用分配机制，使策略模型能够自主总结并选择性保留关键交互信息。
result: "实验显示 MemPO 相比基准模型 F1 分数提升 25.98%，相比 SOTA 提升 7.1%，同时减少了约 70% 的 Token 使用量。"
conclusion: MemPO 有效解决了长程智能体的记忆管理难题，实现了性能提升与计算资源节省的双重目标。
---

## 摘要
长时程智能体在与环境交互过程中面临上下文规模不断增长的挑战，这会导致性能和稳定性下降。现有方法通常引入外部记忆模块并从存储的记忆中检索相关信息，这阻碍了模型自身主动管理其记忆内容并使其与智能体的总体任务目标保持一致。为了解决这些局限性，我们提出了自记忆策略优化算法（MemPO），该算法使智能体（策略模型）能够在与环境交互期间自主地总结和管理其记忆。通过改进基于记忆有效性的信用分配机制，策略模型可以有选择地保留关键信息，在保持任务性能的同时显著降低 Token 消耗。大量的实验和分析证实，MemPO 相比基础模型实现了 25.98% 的 F1 分数绝对增益，相比之前的 SOTA 基准实现了 7.1% 的增益，同时将 Token 使用量分别减少了 67.58% 和 73.12%。

## Abstract
Long-horizon agents face the challenge of growing context size during interaction with environment, which degrades the performance and stability. Existing methods typically introduce the external memory module and look up the relevant information from the stored memory, which prevents the model itself from proactively managing its memory content and aligning with the agent's overarching task objectives. To address these limitations, we propose the self-memory policy optimization algorithm (MemPO), which enables the agent (policy model) to autonomously summarize and manage their memory during interaction with environment. By improving the credit assignment mechanism based on memory effectiveness, the policy model can selectively retain crucial information, significantly reducing token consumption while preserving task performance. Extensive experiments and analyses confirm that MemPO achieves absolute F1 score gains of 25.98% over the base model and 7.1% over the previous SOTA baseline, while reducing token usage by 67.58% and 73.12%.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemPO: Self-Memory Policy Optimization for Long-Horizon Agents》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
长时程智能体（Long-horizon agents）在解决复杂任务时，通常需要与环境进行多轮交互。现有的 **ReAct** 范式会导致上下文随交互轮数线性增长，带来三大挑战：
*   **资源消耗高**：Token 成本随轮数激增。
*   **性能衰减**：长上下文会导致模型出现“迷失在中间”（Lost in the Middle）现象。
*   **窗口限制**：LLM 的上下文窗口限制了交互的最大轮数。

现有的记忆方法（如基于 RAG 的外部记忆）通常是**被动检索**，记忆内容与智能体的最终任务目标缺乏联合优化。本文的研究动机是让智能体具备**自主管理记忆**的能力，通过强化学习使其学会“为了完成任务，我应该记住哪些关键信息”。

### 2. 方法论：核心思想与技术细节
论文提出了 **MemPO（Self-Memory Policy Optimization）** 算法，其核心思想是将记忆管理内化为智能体的一种主动行为。

*   **动作空间扩展**：智能体的动作被形式化为 `<mem>`（总结记忆）、`<think>`（推理）、`<tool_call>`（调用工具）。在每一步交互中，智能体主动压缩历史信息并生成当前记忆。
*   **推理优化**：在推理阶段，智能体不再输入全部历史，而是仅使用**上一步生成的记忆内容**作为上下文，实现极高的压缩率。
*   **双重优势估计（Dual Advantage Estimation）**：
    *   **轨迹级优势（Trajectory-level Advantage, $A^T$）**：基于最终答案的正确性和格式规范性，使用 GRPO 算法进行群体相对评分。
    *   **记忆级优势（Memory-level Advantage, $A^M$）**：这是本文的核心创新。通过计算**条件概率**来衡量记忆的有效性。具体而言，计算“给定当前记忆内容时，生成正确答案的概率”与“给定原始上下文时生成正确答案概率”的差值。
    *   **总优势**：对于 `<mem>` 动作中的 Token，其优势值为 $A^T + A^M$；其他 Token 仅使用 $A^T$。
*   **训练流程**：先通过 GPT-4 生成约 10k 条高质量轨迹进行行为克隆（SFT），随后利用 MemPO 进行强化学习优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **多目标任务**：合成的 2/4/6/8/10 目标复杂查询（基于 HotpotQA 和 NQ）。
    *   **单目标 QA**：包括 2WikiMultiHopQA、Musique、TriviaQA、PopQA 等 7 个主流数据集。
    *   **深度研究任务（OOD 泛化）**：GAIA、Frames、WebWalkerQA。
*   **Benchmark 与对比方法**：
    *   **基础模型**：Qwen2.5-7B。
    *   **对比方法**：ReAct（全上下文）、ReSearch、DeepResearcher（强化学习搜索）、A-MEM（基于 RAG 的记忆）、MEM1（现有的记忆强化学习方法）。

### 4. 资源与算力
*   **模型规模**：统一使用 **Qwen2.5-7B** 作为实验底座。
*   **训练参数**：Rollout 组大小（N）为 16，Batch Size 为 128，学习率为 1e-6，最大交互轮数为 16。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单 QA 到极高难度（10 目标）的长程任务，并进行了跨域（Web Search）测试。
*   **消融实验**：
    *   验证了“信息增益奖励”对性能的贡献。
    *   测试了不同上下文保留策略（保留 1 步、3 步或全上下文）对结果的影响。
*   **充分性评价**：实验设计较为充分，不仅关注准确率（F1/EM），还详细统计了 Token 消耗（总 Token 和峰值 Token），能够客观反映模型在效率与性能之间的平衡。

### 6. 主要结论与发现
*   **性能领先**：MemPO 在多目标任务上比基础模型提升了 **25.98%** 的 F1 分数，比之前的 SOTA（MEM1）提升了 **7.1%**。
*   **极致压缩**：在 10 目标任务中，MemPO 的 Token 使用量仅为 ReSearch 的 **1/3**，峰值 Token 仅为 **1/5**。
*   **抗干扰能力**：实验发现，保留过多的上下文反而会在长程任务中导致性能下降（注意力稀释），而 MemPO 的单步记忆模式在长程任务中表现更稳健。
*   **泛化性**：在未见过的 Web 搜索环境和 OOD 深度研究任务中，MemPO 依然保持了与专门针对该环境训练的模型（如 DeepResearcher）相当甚至更优的水平。

### 7. 优点（亮点）
*   **主动性**：将记忆管理从“外部插件”转变为“内在能力”，实现了记忆与任务目标的端到端对齐。
*   **奖励设计巧妙**：利用条件概率差值作为记忆质量的密集奖励（Dense Reward），有效解决了长程任务中的信用分配（Credit Assignment）难题。
*   **高效率**：通过极简的上下文输入，大幅降低了智能体在实际部署中的推理成本。

### 8. 不足与局限
*   **状态等价性偏差**：由于不同步骤调用的工具不同，记忆所含的信息量天然不等价，在计算群体优势时可能引入统计偏差。
*   **对 SFT 数据的依赖**：模型初期需要依赖 GPT-4 生成的轨迹进行格式引导，这可能限制了其在完全无监督环境下的起始表现。
*   **复杂环境验证**：虽然测试了 Web 搜索，但在更复杂的具身智能或多模态长程任务中的表现尚待验证。

（完）
