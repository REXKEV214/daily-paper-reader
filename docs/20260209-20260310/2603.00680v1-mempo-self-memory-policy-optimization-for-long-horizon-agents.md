---
title: "MemPO: Self-Memory Policy Optimization for Long-Horizon Agents"
title_zh: MemPO：面向长程智能体的自记忆策略优化
authors: "Ruoran Li, Xinghua Zhang, Haiyang Yu, Shitong Duan, Xiang Li, Wenxin Xiang, Chonghua Liao, Xudong Guo, Yongbin Li, Jinli Suo"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00680v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长程智能体的自记忆策略优化
tldr: 针对长程智能体在交互中上下文增长导致的性能下降问题，本文提出 MemPO 算法。该方法通过自记忆策略优化，使智能体能自主总结和管理记忆。通过改进基于记忆有效性的信用分配机制，智能体可选择性保留关键信息，在显著降低 Token 消耗的同时提升任务表现，在 F1 分数和效率上均优于现有 SOTA 模型。
selection_source: fresh_fetch
motivation: 现有的外部记忆模块缺乏主动管理能力，且不断增长的上下文会损害长程智能体的性能与稳定性。
method: 提出 MemPO 算法，通过改进基于记忆有效性的信用分配机制，使策略模型能够自主总结并选择性保留关键记忆信息。
result: "实验显示 MemPO 相比基准模型 F1 分数提升 25.98%，相比 SOTA 提升 7.1%，同时减少了约 70% 的 Token 使用量。"
conclusion: MemPO 有效解决了长程任务中的上下文冗余问题，实现了记忆管理与任务目标的对齐，显著提升了智能体的效率与性能。
---

## 摘要
长程智能体在与环境交互过程中面临上下文规模不断增长的挑战，这降低了性能和稳定性。现有方法通常引入外部记忆模块并从存储的记忆中查找相关信息，这阻碍了模型自身主动管理其记忆内容并与智能体的总体任务目标保持一致。为了解决这些局限性，我们提出了自记忆策略优化算法（MemPO），该算法使智能体（策略模型）能够在与环境交互期间自主地总结和管理其记忆。通过改进基于记忆有效性的信用分配机制，策略模型可以有选择地保留关键信息，在保持任务性能的同时显著降低 Token 消耗。大量的实验和分析证实，MemPO 相比基础模型实现了 25.98% 的 F1 分数绝对增益，相比之前的 SOTA 基准实现了 7.1% 的增益，同时将 Token 使用量减少了 67.58% 和 73.12%。

## Abstract
Long-horizon agents face the challenge of growing context size during interaction with environment, which degrades the performance and stability. Existing methods typically introduce the external memory module and look up the relevant information from the stored memory, which prevents the model itself from proactively managing its memory content and aligning with the agent's overarching task objectives. To address these limitations, we propose the self-memory policy optimization algorithm (MemPO), which enables the agent (policy model) to autonomously summarize and manage their memory during interaction with environment. By improving the credit assignment mechanism based on memory effectiveness, the policy model can selectively retain crucial information, significantly reducing token consumption while preserving task performance. Extensive experiments and analyses confirm that MemPO achieves absolute F1 score gains of 25.98% over the base model and 7.1% over the previous SOTA baseline, while reducing token usage by 67.58% and 73.12%.

---

## 论文详细总结（自动生成）

这篇论文由清华大学和阿里巴巴通义实验室的研究团队合作完成，提出了一种名为 **MemPO** 的自记忆策略优化算法，旨在解决长程（Long-Horizon）智能体在复杂任务中面临的上下文爆炸和性能退化问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在长程决策任务中，智能体通常采用 ReAct 范式，导致交互历史随轮次线性增长。这引发了三个挑战：1) 触及 LLM 上下文窗口上限；2) Token 成本极高；3) 产生“迷失在中间”（Lost in the Middle）现象，降低模型推理准确性。
*   **研究背景**：现有方法多采用外部记忆模块（如 RAG），但这种方式是“被动”的，记忆检索与智能体的最终任务目标缺乏联合优化，无法主动筛选对解决当前问题最关键的信息。
*   **整体含义**：论文提出将记忆管理转化为智能体的“内在能力”，让模型在每一步交互中自主总结和压缩历史，并通过强化学习（RL）确保记忆内容与任务目标对齐。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将智能体的动作空间扩展为 `<mem>`（记忆总结）、`<think>`（推理）、`<tool_call>`（工具调用）。在推理时，智能体仅需参考上一步生成的 `<mem>` 内容，而非全部历史。
*   **关键技术细节**：
    *   **自记忆推理范式**：在第 $t$ 步，模型根据第 $t-1$ 步的记忆生成当前步的记忆、思考和行动，从而实现上下文的恒定或低增长。
    *   **MemPO 算法**：基于 GRPO（群组相对策略优化）框架，设计了双层优势估计（Advantage Estimation）。
    *   **奖励设计**：
        1.  **轨迹级奖励（Trajectory-level）**：根据最终答案的正确性和格式给出稀疏奖励。
        2.  **记忆级奖励（Memory-level）**：这是核心创新。利用**条件概率** $P(\text{正确答案} | \text{当前记忆})$ 来量化记忆的有效性。如果记忆保留了关键信息，模型生成正确答案的置信度（概率）应显著提高。
    *   **信用分配**：对于 `<mem>` 标签内的 Token，其优势值由轨迹级优势和记忆级优势叠加而成，引导模型生成更具“信息增益”的记忆。

### 3. 实验设计
*   **数据集与场景**：
    *   **多目标任务（Multi-objective）**：基于 HotpotQA 和 NQ 合成的 2 到 10 个目标的复杂查询，分为本地 Wiki 搜索和在线网页搜索场景。
    *   **单目标 QA**：包括 NQ、TriviaQA、PopQA、HotpotQA 等 7 个主流基准。
    *   **深度研究任务（Deep Research）**：GAIA、Frames、WebWalkerQA，用于测试 OOD（分布外）泛化能力。
*   **对比方法（Baselines）**：
    *   基础模型：Qwen2.5-7B (ReAct)。
    *   强化学习基准：ReSearch、DeepResearcher、GRPO (无记忆版)。
    *   记忆相关基准：A-MEM (基于 RAG)、MEM1 (基于 RL 的记忆模型)。

### 4. 资源与算力
*   **模型规模**：统一使用 Qwen2.5-7B 作为基础模型。
*   **训练细节**：首先使用 GPT-4o 生成约 10k 条高质量轨迹进行 SFT（监督微调），随后进行 RL 训练。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到使用了 GRPO 算法，该算法通过取消价值网络（Value Network）来显著降低训练时的显存开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单 QA 到极高难度（10 目标）的长程任务，实验组数较多。
*   **充分性**：
    *   进行了**消融实验**，验证了“信息增益奖励”和“不同上下文保留策略”的有效性。
    *   进行了**统计分析**，通过可视化条件概率分布，证明了 MemPO 确实提升了记忆中的有效信息含量。
    *   测试了**泛化性**，在未见过的 Web 搜索环境和 OOD 深度研究任务上依然表现优异。实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：MemPO 在多目标任务上比之前的 SOTA 提升了 7.1% 的 F1 分数，比基础 ReAct 提升了 25.98%。
*   **极致效率**：在处理 10 目标任务时，MemPO 的 Token 消耗仅为传统方法的约 1/3，峰值 Token 消耗降低了约 80%。
*   **记忆质量**：通过条件概率分析发现，MemPO 生成的记忆随步骤推进能持续保持或提升对答案的支撑强度，而普通 GRPO 训练的模型记忆质量会随步骤下降。

### 7. 优点与亮点
*   **主动性**：将记忆管理从外部插件变为模型内在的策略行为，实现了记忆与任务目标的端到端对齐。
*   **奖励创新**：巧妙地利用 LLM 自身的条件概率作为密集奖励信号，解决了长程任务中信用分配难的问题。
*   **高性价比**：在提升性能的同时大幅削减了推理成本，具有极强的实际应用价值。

### 8. 不足与局限
*   **状态等价性偏差**：由于不同轨迹中工具调用的反馈不同，计算群组优势时可能存在状态不完全等价导致的偏差（作者在 Limitations 中有所提及）。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。
*   **复杂环境泛化**：虽然测试了 Web 搜索，但在更复杂的具身智能或多模态长程任务中的表现尚待验证。

（完）
