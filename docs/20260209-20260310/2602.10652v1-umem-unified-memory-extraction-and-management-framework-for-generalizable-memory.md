---
title: "UMEM: Unified Memory Extraction and Management Framework for Generalizable Memory"
title_zh: UMEM：面向可泛化记忆的统一记忆提取与管理框架
authors: "Yongshi Ye, Hui Jiang, Feihu Jiang, Tian Lan, Yichao Du, Biao Fu, Xiaodong Shi, Qianghuai Jia, Longyue Wang, Weihua Luo"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10652v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 大语言模型智能体的统一记忆提取与管理
tldr: 针对大语言模型智能体在记忆提取与管理中存在的泛化性差、易积累噪声等问题，本文提出了UMEM框架。该框架首次将记忆提取与管理进行联合优化，通过引入语义邻域建模和基于GRPO的邻域级边际效用奖励机制，确保记忆在相关查询中的通用性。实验证明，UMEM在多轮交互任务中表现优异，显著提升了智能体的持续进化能力。
selection_source: fresh_fetch
motivation: 现有方法将记忆提取视为静态过程，导致智能体容易积累特定实例的噪声而非具有泛化性的知识。
method: 提出UMEM框架，利用语义邻域建模和GRPO算法优化邻域级边际效用奖励，实现记忆提取与管理的协同进化。
result: "在五个基准测试中显著超越竞争对手，多轮交互任务性能提升高达10.67%，且在持续演化中保持单调增长。"
conclusion: UMEM通过联合优化提取与管理过程，有效提升了智能体记忆的泛化能力和长期演化稳定性。
---

## 摘要
自演化记忆作为基于大语言模型（LLM）智能体的可训练参数，其提取（从经验中提炼见解）与管理（更新记忆库）必须紧密协调。现有方法主要优化记忆管理，而将记忆提取视为静态过程，导致泛化能力较差，使智能体积累了特定于实例的噪声而非鲁棒的记忆。为解决这一问题，我们提出了统一记忆提取与管理（UMEM），这是一个自演化智能体框架，通过联合优化大语言模型来同时提取和管理记忆。为了减轻对特定实例的过拟合，我们引入了语义邻域建模（Semantic Neighborhood Modeling），并通过 GRPO 利用邻域级边际效用奖励来优化模型。该方法通过评估语义相关查询簇中的记忆效用，确保了记忆的可泛化性。在五个基准测试上的广泛实验表明，UMEM 显著优于极具竞争力的基准方法，在多轮交互任务中实现了高达 10.67% 的提升。此外，UMEM 在持续演化过程中保持了单调增长曲线。代码和模型将公开发布。

## Abstract
Self-evolving memory serves as the trainable parameters for Large Language Models (LLMs)-based agents, where extraction (distilling insights from experience) and management (updating the memory bank) must be tightly coordinated. Existing methods predominately optimize memory management while treating memory extraction as a static process, resulting in poor generalization, where agents accumulate instance-specific noise rather than robust memories. To address this, we propose Unified Memory Extraction and Management (UMEM), a self-evolving agent framework that jointly optimizes a Large Language Model to simultaneous extract and manage memories. To mitigate overfitting to specific instances, we introduce Semantic Neighborhood Modeling and optimize the model with a neighborhood-level marginal utility reward via GRPO. This approach ensures memory generalizability by evaluating memory utility across clusters of semantically related queries. Extensive experiments across five benchmarks demonstrate that UMEM significantly outperforms highly competitive baselines, achieving up to a 10.67% improvement in multi-turn interactive tasks. Futhermore, UMEM maintains a monotonic growth curve during continuous evolution. Codes and models will be publicly released.

---

## 论文详细总结（自动生成）

以下是对论文《UMEM: Unified Memory Extraction and Management Framework for Generalizable Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**基于大语言模型（LLM）智能体的自演化记忆系统**。
*   **研究动机**：现有的自演化智能体通常将“记忆提取”（从经验中提炼知识）视为一个静态的、基于提示词的过程，而仅侧重于优化“记忆管理”（如何更新记忆库）。
*   **核心问题**：这种分离导致了两个严重问题：
    1.  **实例噪声累积**：智能体容易陷入“死记硬背”陷阱，记住了特定问题的细节而非通用原则，导致泛化性差。
    2.  **管理失调**：提取出的记忆内容与管理策略不匹配，使得即使是最优的管理策略也无法处理低质量的记忆。
*   **整体含义**：UMEM 旨在通过联合优化提取与管理过程，使智能体能够从交互经验中提炼出具有鲁棒泛化能力的“通用记忆”。

### 2. 方法论：核心思想与技术细节
UMEM 框架由**执行器（Executor）**、**记忆库（Memory Bank）**和**记忆优化器（Mem-Optimizer）**组成。其核心思想是利用强化学习对 Mem-Optimizer 进行端到端训练。

*   **语义邻域建模（Semantic Neighborhood Modeling）**：
    为了防止模型过拟合于单一查询，UMEM 为每个训练查询构建一个“语义邻域”（Top-N 个相似查询）。在训练时，记忆的有效性不是在原查询上评估，而是在整个邻域内评估，迫使模型提取通用规律。
*   **边际效用奖励（Marginal Utility Reward）**：
    使用 **GRPO（群组相对策略优化）** 算法，通过两个维度计算奖励：
    1.  **成功增益（Success Gain）**：衡量加入记忆后，邻域内查询从失败转为成功的比例。
    2.  **效率正则化（Efficiency Regularization）**：在保证正确的前提下，奖励推理步骤更简洁（Token 更少）的记忆。
*   **算法流程**：
    1.  执行器根据当前记忆生成任务轨迹。
    2.  Mem-Optimizer 生成一组记忆更新动作（提取内容 + 操作指令如 ADD/UPDATE）。
    3.  在语义邻域上验证这些动作的边际效用。
    4.  通过 GRPO 更新 Mem-Optimizer 参数，并同步更新在线记忆库。

### 3. 实验设计
*   **数据集/场景**：
    *   **单轮推理**：AIME (数学), GPQA-Diamond (科学), HLE (复杂推理), HotpotQA (多跳问答)。
    *   **多轮交互/具身智能**：ALFWorld (长程规划与决策)。
*   **Benchmark 与对比方法**：
    *   **对比方法**：No Memory（无记忆）、No Train（仅靠 Prompt）、Self-RAG（自反思检索）、**Memp**（程序化记忆）、**ReMem**（离散动作管理记忆）。
    *   **执行器（Executor）**：测试了 Qwen3-8B-Thinking、GPT-5.1 和 Gemini-2.5-Flash，以验证 UMEM 提取的记忆是否具有跨模型的可移植性。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 张 NVIDIA A100 GPU**。
*   **训练时长**：整个训练过程大约持续了 **11 小时**。
*   **软件框架**：基于 `verl` 强化学习框架实现。
*   **模型规模**：Mem-Optimizer 采用了 Llama-3.2-1B 和 Qwen3-4B 两种规格。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流基准测试上进行了全面评估，涵盖了从纯逻辑推理到复杂环境交互的多种场景。
*   **消融实验**：
    *   验证了“联合优化”的必要性（分别屏蔽提取优化和管理优化）。
    *   测试了语义邻域大小（N=1, 3, 5）对泛化性能的影响。
*   **充分性与公平性**：实验采用了“流式评估协议”（Streaming Protocol），即记忆在测试过程中持续更新且不重置，这模拟了真实的持续学习场景。对比基准涵盖了最新的 SOTA 方法，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能飞跃**：UMEM 在多轮交互任务（ALFWorld）中相比基准提升了 **10.67%**，在复杂推理任务中也表现稳健。
*   **提取重于管理**：消融实验显示，优化“记忆提取”带来的增益远大于单纯优化“管理策略”。
*   **演化稳定性**：UMEM 在长程交互（如 10 个 Epoch 的持续演化）中表现出单调增长的性能曲线，而基准方法往往会因为噪声累积导致性能退化。
*   **跨模型通用性**：由小模型（1B/4B）训练出的 Mem-Optimizer 提取的记忆，能显著提升更强大的闭源模型（如 GPT-5.1）的执行效率。

### 7. 优点：亮点与创新
*   **联合优化范式**：首次打破了记忆提取与管理之间的壁垒，实现了两者的协同进化。
*   **泛化性保障**：通过“语义邻域”这一代理机制，巧妙地解决了强化学习在记忆任务中容易出现的“过拟合特定实例”问题。
*   **效率意识**：奖励函数中引入效率正则项，使智能体不仅能做对，还能“走捷径”高效完成任务。

### 8. 不足与局限
*   **计算开销**：训练过程涉及大量的 Rollout（采样执行轨迹）和邻域验证，对算力资源有一定要求。
*   **数据依赖**：虽然在多个 Benchmark 表现良好，但训练数据主要源自 MMLU 的采样，对于极度垂直或全新的领域，可能需要重新微调 Mem-Optimizer。
*   **检索依赖**：系统性能仍受限于底层向量检索（如 BGE-M3）的准确性，如果检索不到相关的邻域或记忆，演化效率会受影响。

（完）
