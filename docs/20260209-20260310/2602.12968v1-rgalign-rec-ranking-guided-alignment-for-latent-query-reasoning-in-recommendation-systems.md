---
title: "RGAlign-Rec: Ranking-Guided Alignment for Latent Query Reasoning in Recommendation Systems"
title_zh: RGAlign-Rec：推荐系统中潜在查询推理的排序引导对齐
authors: "Junhua Liu, Yang Jihao, Cheng Chang, Kunrong LI, Bin Fu, Kwan Hui Lim"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.12968v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 通过行为信号预测用户需求，实现聊天机器人的主动推荐
tldr: 针对电商聊天机器人中主动意图预测的语义鸿沟和目标不一致问题，本文提出RGAlign-Rec框架。该框架结合了基于大语言模型的语义推理器与查询增强排序模型，并引入排序引导对齐（RGA）机制，利用下游排序信号反馈优化LLM的潜在推理。实验证明该方法在Shopee工业数据集和在线A/B测试中均显著提升了推荐准确率和点击率。
selection_source: fresh_fetch
motivation: 现有系统难以弥合离散用户特征与语义意图之间的鸿沟，且通用LLM输出与特定推荐排序目标之间存在不一致性。
method: 提出一种闭环对齐框架，利用排序引导对齐（RGA）多阶段训练范式，将下游排序信号作为反馈来精炼LLM的潜在推理能力。
result: "在Shopee大规模工业数据集上，该方法使GAUC提升0.12%，Recall@3提升0.56%，且在线A/B测试中CTR显著增长。"
conclusion: 排序感知对齐能有效同步语义推理与排序目标，显著增强了真实世界主动推荐系统的预测精度和服务质量。
---

## 摘要
主动意图预测是现代电子商务聊天机器人的关键能力，通过从行为和上下文信号中预测用户需求，实现“零查询”推荐。然而，现有的工业系统面临两个基本挑战：（1）离散用户特征与聊天机器人知识库中语义意图之间的语义鸿沟；（2）通用大语言模型（LLM）输出与特定任务排序效用之间的目标不一致。为解决这些问题，我们提出了 RGAlign-Rec，这是一个闭环对齐框架，它将基于 LLM 的语义推理器与查询增强（QE）排序模型相结合。我们还引入了排序引导对齐（RGA），这是一种多阶段训练范式，利用下游排序信号作为反馈来优化 LLM 的潜在推理。在 Shopee 的大规模工业数据集上进行的广泛实验表明，RGAlign-Rec 在 GAUC 上实现了 0.12% 的提升，导致错误率显著相对降低 3.52%，Recall@3 提升了 0.56%。在线 A/B 测试进一步验证了我们框架的累积有效性：查询增强模型（QE-Rec）最初带来了 0.98% 的 CTR 提升，而随后的排序引导对齐阶段又贡献了 0.13% 的增益。这些结果表明，排序感知对齐有效地同步了语义推理与排序目标，显著提高了现实世界主动推荐系统的预测准确性和服务质量。

## 速览
**TLDR**：针对电商聊天机器人中主动意图预测的语义鸿沟和目标不一致问题，本文提出RGAlign-Rec框架。该框架结合了基于大语言模型的语义推理器与查询增强排序模型，并引入排序引导对齐（RGA）机制，利用下游排序信号反馈优化LLM的潜在推理。实验证明该方法在Shopee工业数据集和在线A/B测试中均显著提升了推荐准确率和点击率。 \
**Motivation**：现有系统难以弥合离散用户特征与语义意图之间的鸿沟，且通用LLM输出与特定推荐排序目标之间存在不一致性。 \
**Method**：提出一种闭环对齐框架，利用排序引导对齐（RGA）多阶段训练范式，将下游排序信号作为反馈来精炼LLM的潜在推理能力。 \
**Result**：在Shopee大规模工业数据集上，该方法使GAUC提升0.12%，Recall@3提升0.56%，且在线A/B测试中CTR显著增长。 \
**Conclusion**：排序感知对齐能有效同步语义推理与排序目标，显著增强了真实世界主动推荐系统的预测精度和服务质量。

---

## Abstract
Proactive intent prediction is a critical capability in modern e-commerce chatbots, enabling "zero-query" recommendations by anticipating user needs from behavioral and contextual signals. However, existing industrial systems face two fundamental challenges: (1) the semantic gap between discrete user features and the semantic intents within the chatbot's Knowledge Base, and (2) the objective misalignment between general-purpose LLM outputs and task-specific ranking utilities. To address these issues, we propose RGAlign-Rec, a closed-loop alignment framework that integrates an LLM-based semantic reasoner with a Query-Enhanced (QE) ranking model. We also introduce Ranking-Guided Alignment (RGA), a multi-stage training paradigm that utilizes downstream ranking signals as feedback to refine the LLM's latent reasoning. Extensive experiments on a large-scale industrial dataset from Shopee demonstrate that RGAlign-Rec achieves a 0.12% gain in GAUC, leading to a significant 3.52% relative reduction in error rate, and a 0.56% improvement in Recall@3. Online A/B testing further validates the cumulative effectiveness of our framework: the Query-Enhanced model (QE-Rec) initially yields a 0.98% improvement in CTR, while the subsequent Ranking-Guided Alignment stage contributes an additional 0.13% gain. These results indicate that ranking-aware alignment effectively synchronizes semantic reasoning with ranking objectives, significantly enhancing both prediction accuracy and service quality in real-world proactive recommendation systems.

---

## 论文详细总结（自动生成）

以下是对论文《RGAlign-Rec: Ranking-Guided Alignment for Latent Query Reasoning in Recommendation Systems》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于电商聊天机器人中的**主动意图预测（Proactive Intent Prediction）**，即在用户尚未输入任何文字的“零查询（Zero-Query）”场景下，通过用户行为和上下文信号预判其服务需求（如：物流延迟、支付失败）。
*   **研究动机**：现有的工业级推荐系统面临两大挑战：
    1.  **语义鸿沟**：传统的离散特征（如用户ID、意图ID）难以与聊天机器人知识库中丰富的文本意图进行语义对齐。
    2.  **目标不一致**：通用大语言模型（LLM）通常优化的是语言流畅度或人类偏好，而非下游推荐系统的排序指标（如点击率 CTR）。
*   **核心目标**：建立一个闭环框架，使 LLM 的语义推理能力与推荐系统的排序目标实现深度同步。

### 2. 方法论：核心思想与关键技术
论文提出了 **RGAlign-Rec** 框架，其核心思想是**将排序模型作为奖励模型（Reward Model）来引导 LLM 的对齐训练**。

*   **特征言语化（Feature Verbalization）**：将离散的用户画像、行为序列和订单状态映射为结构化的自然语言片段，形成“思维链（CoT）”式的输入提示词。
*   **查询增强排序模型（QE-Rec）**：在传统的“用户塔”和“意图塔”基础上增加了一个**“查询塔（Query Tower）”**。该塔接收 LLM 生成的潜在查询嵌入（LTP Embedding），通过加权融合计算排序得分。
*   **排序引导对齐（RGA）流程**：
    1.  **多模型采样**：利用 GPT-5、Gemini 等多个强力模型生成候选查询。
    2.  **Best-of-N 策略**：使用预训练的 QE-Rec 模型对候选查询打分，选出最能提升排序性能（NDCG）的查询作为正样本。
    3.  **对齐训练**：通过 **RG-SFT**（有监督微调）和 **RG-CL**（基于 InfoNCE 的表征层对比学习）将 LLM 的末尾 Token 隐藏状态与排序优化的语义空间同步。
*   **闭环校准**：使用对齐后的 LLM 重新训练排序模型，确保两者在同一语义流形上运作。

### 3. 实验设计
*   **数据集**：使用 Shopee 跨境电商平台的真实工业数据集，涵盖东南亚和拉美 8 个地区。
    *   **规模**：5 周的用户交互日志，共约 753 万个样本。
    *   **评估集**：约 4.6 万个样本，由人工标注真实意图以减少隐式反馈的噪声。
*   **Benchmark 与对比方法**：
    *   **基准模型**：AutoInt + MoE（工业界 SOTA 架构）。
    *   **对比变体**：QE-Rec（原始 LLM）、RGAlign-Rec（SFT+DPO 偏好对齐）、RGAlign-Rec（SFT+CL 表征对齐）。
*   **评估指标**：GAUC、Recall@K、NDCG@K、MRR（离线）；CTR、意图命中率 IHR、客户满意度 CSAT（在线）。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA B200 GPU**。
*   **优化技术**：采用 8-bit 量化技术以支持工业级推理。
*   **性能表现**：LLM 推理延迟约 120ms，整体服务延迟 1.5s，支持 50 QPS 的峰值吞吐量。
*   **训练细节**：训练 10-20 个 epoch，使用了 DeepSpeed ZeRO Stage-3 进行模型状态分区。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的离线实验（753万样本）和在线 A/B 测试，数据量足以支撑结论。
*   **消融实验**：
    *   对比了不同的**池化策略**（LTP vs BGE 嵌入），证明了直接利用 LLM 内部隐藏状态的优越性。
    *   对比了不同的 **Best-of-N 采样策略**，探讨了自拟合偏差（Self-fitting bias）的影响。
*   **客观性**：通过人工标注验证集和在线真实流量测试，确保了实验结果的客观性和工业应用价值。

### 6. 主要结论与发现
*   **性能提升**：RGAlign-Rec 在离线 GAUC 上提升了 0.12%（对应错误率降低 3.52%），Recall@3 提升 0.56%。
*   **在线价值**：在线 A/B 测试显示 CTR@3 累计提升了 1.11%（0.98% 来自架构增强，0.13% 来自排序引导对齐）。
*   **对齐有效性**：表征层面的对比学习（CL）比传统的偏好对齐（DPO）在推荐场景下更具鲁棒性，能更好地同步语义空间与排序空间。

### 7. 优点（亮点）
*   **闭环设计**：打破了 LLM 仅作为特征提取器的单向链路，实现了排序信号对生成模型的反向反馈。
*   **工业实用性**：在保证高精度的同时，通过量化和高效的 LTP 嵌入提取，满足了大规模生产环境的延迟要求。
*   **解决语义鸿沟**：通过特征言语化和 LLM 推理，成功将冷冰冰的 ID 特征转化为具有语义深度的潜在查询。

### 8. 不足与局限
*   **CSAT 波动**：在线测试中发现 CSAT（满意度）略有下降（-0.21%），这表明虽然意图预测更准了，但如果后端回复内容质量没跟上，准确的推荐反而可能暴露回复能力的短板。
*   **负样本依赖**：Best-of-N 策略的效果高度依赖于教师模型（如 GPT-5）生成的候选质量，若候选池缺乏多样性，对齐效果可能受限。
*   **应用限制**：目前主要针对“零查询”的主动推荐场景，对于多轮对话中的动态意图捕捉讨论较少。

（完）
