---
title: "U-CAN: Utility-Aware Contrastive Attenuation for Efficient Unlearning in Generative Recommendation"
title_zh: U-CAN：用于生成式推荐中高效机器遗忘的效用感知对比衰减
authors: "Zezheng Wu, Rui Wang, Xinghe Cheng, Yang Shao, Qing Yang, Jiapu Wang, Jingwei Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23400v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 将个性化定义为指令驱动的序列生成及隐私保护卸载
tldr: 生成式推荐系统利用大模型微调时会泄露隐私，而传统机器遗忘方法因神经元多义性易导致性能大幅下降。本文提出U-CAN框架，针对LoRA适配器，通过对比激活量化风险并结合效用感知校准，利用自适应软衰减函数选择性降低高风险参数权重。实验证明该方法在实现强隐私遗忘的同时，有效保留了模型推荐效能且计算高效。
selection_source: fresh_fetch
motivation: 解决生成式推荐中神经元同时编码敏感信息与通用推理模式导致的“多义性困境”，避免传统遗忘方法造成的严重性能损失。
method: 提出一种基于LoRA的效用感知对比衰减框架，通过对比激活识别高风险神经元，并利用可微衰减函数对参数进行软性下调以保留推理电路。
result: 在两个公开数据集上的七项指标测试中，U-CAN在隐私遗忘效果、推荐效用保留和计算效率方面均取得了显著优于现有方法的结果。
conclusion: U-CAN通过精准抑制敏感路径并维护模型拓扑连接，为生成式推荐系统提供了一种高效且能平衡隐私与性能的机器遗忘新方案。
---

## 摘要
生成式推荐 (GenRec) 通常利用大语言模型 (LLMs) 将个性化重新定义为指令驱动的序列生成任务。然而，在用户日志上进行微调会无意中将敏感属性编码到模型参数中，从而引发严重的隐私担忧。现有的机器遗忘 (MU) 技术由于“多义性困境” (Polysemy Dilemma) 而难以应对这一矛盾，即神经元将敏感数据与通用推理模式叠加在一起，导致传统梯度或剪枝方法下出现灾难性的效用损失。为了解决这一问题，我们提出了效用感知对比衰减 (U-CAN)，这是一种在低秩适配器 (LoRA) 上运行的精确遗忘框架。U-CAN 通过对比激活来量化风险，并专注于具有非对称响应的神经元，这些神经元对遗忘集高度敏感，但在保留集上受到抑制。为了保障性能，我们引入了一种效用感知校准机制，该机制结合了权重大小与保留集激活范数，为对保留性能贡献较大的维度分配更高的效用分数。与通常会破坏网络结构的二进制剪枝不同，U-CAN 开发了带有可微衰减函数的自适应软衰减，以选择性地降低 LoRA 适配器上高风险参数的规模，从而抑制敏感检索路径并保持推理电路的拓扑连通性。在两个公共数据集上进行的七项指标实验表明，U-CAN 实现了强大的隐私遗忘、效用保留和计算效率。

## 速览
**TLDR**：生成式推荐系统利用大模型微调时会编码敏感隐私，传统机器遗忘方法因“多义性困境”易导致模型性能大幅下降。本文提出U-CAN框架，针对LoRA适配器，通过对比激活量化风险，并结合效用感知校准机制，利用可微衰减函数对高风险参数进行软衰减。实验证明该方法在保证隐私遗忘的同时，有效保留了模型效用并提升了计算效率。 \
**Motivation**：针对生成式推荐中神经元同时编码敏感信息与通用推理模式导致的“多义性困境”，解决传统遗忘方法造成的灾难性效用损失。 \
**Method**：提出一种基于LoRA的效用感知对比衰减框架，通过对比激活识别高风险神经元，并利用可微衰减函数对参数进行自适应软缩放。 \
**Result**：在两个公开数据集上的实验表明，U-CAN在隐私遗忘强度、模型效用保留和计算效率方面均表现优异。 \
**Conclusion**：U-CAN通过精准定位并软衰减敏感检索路径，成功在生成式推荐中实现了隐私保护与模型性能的平衡。

---

## Abstract
Generative Recommendation (GenRec) typically leverages Large Language Models (LLMs) to redefine personalization as an instruction-driven sequence generation task. However, fine-tuning on user logs inadvertently encodes sensitive attributes into model parameters, raising critical privacy concerns. Existing Machine Unlearning (MU) techniques struggle to navigate this tension due to the Polysemy Dilemma, where neurons superimpose sensitive data with general reasoning patterns, leading to catastrophic utility loss under traditional gradient or pruning methods. To address this, we propose Utility-aware Contrastive AttenuatioN (U-CAN), a precision unlearning framework that operates on low-rank adapters. U-CAN quantifies risk by contrasting activations and focuses on neurons with asymmetric responses that are highly sensitive to the forgetting set but suppressed on the retention set. To safeguard performance, we introduce a utility-aware calibration mechanism that combines weight magnitudes with retention-set activation norms, assigning higher utility scores to dimensions that contribute strongly to retention performance. Unlike binary pruning, which often fragments network structure, U-CAN develop adaptive soft attenuation with a differentiable decay function to selectively down-scale high-risk parameters on LoRA adapters, suppressing sensitive retrieval pathways and preserving the topological connectivity of reasoning circuits. Experiments on two public datasets across seven metrics demonstrate that U-CAN achieves strong privacy forgetting, utility retention, and computational efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《U-CAN: Utility-Aware Contrastive Attenuation for Efficient Unlearning in Generative Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成式推荐（GenRec）通过微调大语言模型（LLM）来实现个性化，但这一过程会将用户的敏感属性和交互历史编码进模型参数，引发隐私泄露风险。
*   **研究动机**：现有的机器遗忘（Machine Unlearning, MU）方法在 GenRec 场景下面临**“多义性困境”（Polysemy Dilemma）**。即单个神经元往往同时编码了隐私信息和通用的语言推理模式。
*   **现有方法缺陷**：
    *   **梯度上升法（GA）**：容易导致“方向性崩溃”，在消除隐私的同时破坏了共享的推理表示。
    *   **剪枝法（Pruning）**：硬性的二进制删除会造成“结构性损伤”，切断了通用推荐所需的逻辑路径。
*   **核心目标**：在不进行昂贵的重新训练的前提下，精准移除特定用户数据的贡献，同时最大程度保留模型的推荐效能。

### 2. 方法论：U-CAN 框架
U-CAN 提出了一种针对 LoRA 适配器的“双重筛选+软衰减”机制，核心流程包括：
*   **对比激活（Contrastive Activation）**：通过对比模型在“遗忘集”和“保留集”上的激活差异，识别出那些对隐私数据响应强烈、但在通用任务中响应较弱的神经元，从而量化初步风险分数。
*   **效用显著性（Utility Significance）**：为了防止误伤重要参数，该模块结合了 LoRA 权重的量级（Magnitude）与保留集上的激活范数（Activation Norm），评估每个维度对模型推理能力的贡献度。
*   **风险校准与融合**：利用效用分数对风险分数进行校准，确保高价值（对通用推荐贡献大）的神经元即使有一定风险也会被保护。
*   **自适应软衰减（Adaptive Soft Attenuation）**：这是 U-CAN 的关键创新。它弃用了传统的二进制掩码（0/1 剪枝），而是采用一个**可微的连续衰减函数**。根据最终风险得分，对 LoRA 适配器的参数进行按比例的缩放（Down-scaling）。这种“软干预”能抑制敏感路径，同时维持网络拓扑的连通性。

### 3. 实验设计
*   **数据集**：使用了两个公开基准数据集：**ML-100k**（电影推荐）和 **Pantry**（亚马逊电子商超子集）。
*   **基础模型（Benchmark）**：以 **LlamaRec**（基于 Llama-2-7b）作为生成式推荐的骨干框架。
*   **对比方法**：
    1.  **Retraining**：在保留集上从头训练（理论上限）。
    2.  **GA (Gradient Ascent)**：传统的梯度上升遗忘法。
    3.  **NPO (Negative Preference Optimization)**：基于偏好优化的遗忘法。
    4.  **LLM-Eraser**：最先进的基于剪枝和蒸馏的遗忘框架。
*   **评估指标**：涵盖推荐效用（Recall, MRR, NDCG）、遗忘深度（KL 散度, Prediction Shift, PPL）以及计算效率（运行时间, 吞吐量）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Xeon CPU** 和单张 **NVIDIA V100 GPU** 的集群上完成。
*   **训练时长**：论文强调 U-CAN 是一种 **One-shot（一阶段）** 方法，不需要进行二次训练或复杂的反向传播优化。其运行效率极高，在处理相同规模的遗忘请求时，速度显著快于基于梯度的 GA 和 NPO 方法。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验验证，包括：
    *   **主实验**：在两个数据集上对比了 7 项关键指标。
    *   **消融实验**：分别验证了对比筛选、效用校准和软衰减机制的必要性。
    *   **效率分析**：对比了不同方法的运行时间和吞吐量。
    *   **超参数敏感性**：分析了风险阈值 $\tau$ 和融合权重 $\lambda$ 对结果的影响。
    *   **定性案例研究**：通过具体的文本生成案例展示了模型在遗忘后仍能保持逻辑连通性。
*   **评价**：实验设计较为全面且客观，通过与“重新训练”这一金标准对比，证明了其在工业应用中的可行性。

### 6. 主要结论与发现
*   **性能平衡**：U-CAN 在遗忘效果上接近重新训练的效果，而在保留集上的推荐准确率显著优于 GA 和剪枝方法。
*   **解决多义性**：实验证明，通过软衰减而非硬剪枝，可以有效缓解“多义性困境”，避免模型在遗忘后出现逻辑混乱或推荐质量骤降。
*   **高效性**：由于仅涉及前向激活分析和参数缩放，U-CAN 的处理速度比传统方法快数倍，适合频繁的隐私删除请求。

### 7. 优点（亮点）
*   **非破坏性干预**：提出的自适应软衰减机制比二进制剪枝更科学，保护了 LLM 脆弱的推理电路。
*   **效用感知**：引入激活范数来衡量参数重要性，使得遗忘过程具有“前瞻性”，不会为了遗忘而牺牲核心功能。
*   **架构无关性**：该方法直接作用于 LoRA 适配器，易于集成到现有的 PEFT（参数高效微调）流程中。

### 8. 不足与局限
*   **隐私保证深度**：虽然实验指标（如 PPL 提升）暗示了隐私被移除，但论文缺乏形式化的隐私证明（如差分隐私 DP 的理论保证）。
*   **应用范围限制**：目前主要针对 LoRA 适配器进行优化，对于全参数微调或其他微调技术（如 Prefix-tuning）的适用性尚未讨论。
*   **对抗性测试不足**：未测试在强力对抗性攻击（如专门的成员推理攻击）下，这种软衰减是否能彻底防止隐私被恶意提取。

（完）
