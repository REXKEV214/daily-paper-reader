---
title: "U-CAN: Utility-Aware Contrastive Attenuation for Efficient Unlearning in Generative Recommendation"
title_zh: U-CAN：面向生成式推荐中高效机器遗忘的效用感知对比衰减
authors: "Zezheng Wu, Rui Wang, Xinghe Cheng, Yang Shao, Qing Yang, Jiapu Wang, Jingwei Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23400v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 个性化推荐中敏感属性的隐私保护遗忘
tldr: 生成式推荐系统（GenRec）利用大模型微调时会编码敏感隐私，传统机器遗忘方法因“多义性困境”易导致模型性能大幅下降。本文提出U-CAN框架，针对LoRA适配器，通过对比激活量化风险，并结合效用感知校准机制，利用可微衰减函数对高风险参数进行软衰减。实验证明该方法在保证隐私遗忘的同时，有效保留了模型效用并提升了计算效率。
selection_source: fresh_fetch
motivation: 旨在解决生成式推荐中神经元同时编码敏感信息与通用推理模式导致的“多义性困境”，避免传统遗忘方法造成的灾难性效用损失。
method: 提出一种基于LoRA的效用感知对比衰减框架，通过对比激活识别高风险神经元，并利用可微衰减函数对参数进行自适应软缩放以保留推理电路。
result: 在两个公开数据集上的七项指标测试中，U-CAN在隐私遗忘强度、模型效用保留和计算效率方面均取得了显著优于现有方法的效果。
conclusion: U-CAN通过精准定位并软衰减敏感参数，成功在生成式推荐系统中实现了隐私保护与推荐性能的高效平衡。
---

## 摘要
生成式推荐（GenRec）通常利用大语言模型（LLMs）将个性化重新定义为指令驱动的序列生成任务。然而，在用户日志上进行微调会无意中将敏感属性编码到模型参数中，从而引发严重的隐私担忧。现有的机器遗忘（MU）技术由于“多义性困境”（Polysemy Dilemma）而难以应对这种张力，即神经元将敏感数据与通用推理模式叠加在一起，导致传统梯度或剪枝方法下出现灾难性的效用损失。为此，我们提出了效用感知对比衰减（U-CAN），这是一种在低秩适配器上运行的精准遗忘框架。U-CAN 通过对比激活来量化风险，并专注于具有非对称响应的神经元，这些神经元对遗忘集高度敏感，但在保留集上受到抑制。为了保障性能，我们引入了一种效用感知校准机制，该机制结合了权重大小与保留集激活范数，为对保留性能贡献较大的维度分配更高的效用分数。与通常会破坏网络结构的二进制剪枝不同，U-CAN 开发了带有可微衰减函数的自适应软衰减，以选择性地降低 LoRA 适配器上高风险参数的权重，从而抑制敏感检索路径并保持推理电路的拓扑连通性。在两个公开数据集上进行的七项指标实验表明，U-CAN 实现了强大的隐私遗忘、效用保留和计算效率。

## Abstract
Generative Recommendation (GenRec) typically leverages Large Language Models (LLMs) to redefine personalization as an instruction-driven sequence generation task. However, fine-tuning on user logs inadvertently encodes sensitive attributes into model parameters, raising critical privacy concerns. Existing Machine Unlearning (MU) techniques struggle to navigate this tension due to the Polysemy Dilemma, where neurons superimpose sensitive data with general reasoning patterns, leading to catastrophic utility loss under traditional gradient or pruning methods. To address this, we propose Utility-aware Contrastive AttenuatioN (U-CAN), a precision unlearning framework that operates on low-rank adapters. U-CAN quantifies risk by contrasting activations and focuses on neurons with asymmetric responses that are highly sensitive to the forgetting set but suppressed on the retention set. To safeguard performance, we introduce a utility-aware calibration mechanism that combines weight magnitudes with retention-set activation norms, assigning higher utility scores to dimensions that contribute strongly to retention performance. Unlike binary pruning, which often fragments network structure, U-CAN develop adaptive soft attenuation with a differentiable decay function to selectively down-scale high-risk parameters on LoRA adapters, suppressing sensitive retrieval pathways and preserving the topological connectivity of reasoning circuits. Experiments on two public datasets across seven metrics demonstrate that U-CAN achieves strong privacy forgetting, utility retention, and computational efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：U-CAN —— 面向生成式推荐中高效机器遗忘的效用感知对比衰减

#### 1. 核心问题与整体含义（研究动机和背景）
生成式推荐（GenRec）通过大语言模型（LLM）将推荐任务转化为指令驱动的序列生成。然而，在用户交互数据上微调模型会使敏感属性（如用户偏好、隐私信息）被编码进模型参数中，引发隐私泄露风险。
*   **研究动机**：现有的机器遗忘（Machine Unlearning, MU）方法在 GenRec 中面临**“多义性困境”（Polysemy Dilemma）**。即神经元往往同时编码隐私信息和通用的语言推理模式。
*   **传统局限**：基于梯度的更新（如梯度上升）容易导致“方向性崩溃”，破坏共享的推理表示；而基于剪枝的方法（硬掩码）则会造成“结构性损伤”，切断通用的推荐路径。
*   **核心目标**：在不进行昂贵重训的前提下，精准移除特定用户数据的印记，同时保持模型的通用推荐效用。

#### 2. 方法论：核心思想与技术细节
U-CAN 框架主要针对 **LoRA 适配器** 进行操作，通过三个阶段实现精准遗忘：
*   **对比激活（Contrastive Activation）**：
    *   通过对比遗忘集（$D_f$）和保留集（$D_r$）在模型层内的激活差异，识别对隐私敏感的神经元。
    *   利用带有容差因子的 ReLU 函数计算激活间隙，突出显示那些在遗忘集上响应强烈但在保留集上响应微弱的神经元。
*   **效用显著性（Utility Significance）**：
    *   为了避免误伤重要神经元，引入结构化评估。结合权重矩阵的 $L_1$ 范数与保留集上的输入激活 $L_2$ 范数，量化每个维度对模型推理能力的贡献。
    *   采用 4-bit 量化感知设计和流式聚合协议，以支持大规模数据的高效处理。
*   **自适应软衰减（Adaptive Soft Attenuation）**：
    *   **核心创新**：放弃二进制的“硬剪枝”，改用基于可微衰减函数的**连续缩放**。
    *   根据融合后的风险分数（结合了隐私风险和效用重要性），对 LoRA 适配器的参数列进行下调缩放。这种“软干预”能抑制敏感检索路径，同时维持网络拓扑的连通性。

#### 3. 实验设计
*   **数据集**：
    *   **ML-100k**：经典的电影推荐基准。
    *   **Pantry**：Amazon 评论数据集中关于食品和家庭用品的子集。
*   **基准模型（Backbone）**：LlamaRec（基于 Llama-2-7b 的两阶段推荐框架）。
*   **对比方法**：
    *   **Retraining**：在保留集上从头重训（理论上限）。
    *   **GA (Gradient Ascent)**：梯度上升法。
    *   **NPO (Negative Preference Optimization)**：负偏好优化。
    *   **LLM-Eraser**：基于选择性剪枝和对比蒸馏的 SOTA 方法。
*   **评估指标**：涵盖推荐效用（Recall, MRR, NDCG）、遗忘有效性（KL 散度, Prediction Shift, PPL）以及效率指标（执行时间、吞吐量）。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 Intel Xeon CPU 和 **单张 NVIDIA V100 GPU** 的集群上运行。
*   **训练时长**：论文强调了 U-CAN 的 **One-shot（单次处理）** 特性，无需二次训练或反向传播。实验结果显示其运行时间远低于基于梯度的 GA 和 NPO 方法，且吞吐量显著更高。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的测试，包括：
    *   在两个数据集上对比了 7 项关键指标的主实验。
    *   针对隐私遗忘深度的专项研究（KL 散度、预测偏移等）。
    *   **消融实验**：分别验证了对比筛选、效用校准和软衰减组件的必要性。
    *   **超参数敏感性分析**：探讨了风险阈值 $\tau$ 和融合权重 $\lambda$ 的影响。
    *   **定性案例研究**：通过具体的文本生成示例验证模型在遗忘后的逻辑连通性。
*   **评价**：实验设计较为全面，涵盖了从定量到定性、从性能到效率的多个角度，对比基准选择合理，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **遗忘与效用的平衡**：U-CAN 在 Trade-off 指标上显著优于现有方法，甚至在某些指标上接近或超过了昂贵的重训（Retraining）效果。
*   **软衰减的优越性**：相比硬剪枝，软衰减能有效防止模型在遗忘过程中的“推理崩溃”，保留了生成推荐理由的连通性。
*   **高效性**：由于采用前向激活分析而非反向梯度优化，U-CAN 在处理频繁的隐私删除请求时具有极高的响应速度。

#### 7. 优点与亮点
*   **解决多义性困境**：通过对比激活和效用感知校准，成功在高度纠缠的参数空间中分离了隐私特征与通用能力。
*   **架构无关与轻量化**：直接作用于 LoRA 适配器，不改变预训练骨干网络，且无需重训，非常适合实际部署。
*   **结构保护**：引入可微衰减函数，避免了传统剪枝导致的结构碎片化。

#### 8. 不足与局限
*   **应用范围限制**：目前主要针对基于 LoRA 的微调模型，对于全参数微调或其他 PEFT 方法（如 Prompt Tuning）的适用性未做深入探讨。
*   **隐私保证深度**：虽然实验指标显示遗忘效果良好，但缺乏形式化的隐私证明（如差分隐私 DP 的理论界限）。
*   **对抗性测试不足**：未测试在强力对抗攻击（如专门针对遗忘模型的成员推理攻击）下的鲁棒性。
*   **任务复杂度**：实验主要集中在推荐任务，对于更复杂的跨域推理或多模态生成场景的泛化能力尚待验证。

（完）
