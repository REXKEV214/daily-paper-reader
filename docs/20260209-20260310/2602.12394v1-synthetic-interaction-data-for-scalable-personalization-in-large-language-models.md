---
title: Synthetic Interaction Data for Scalable Personalization in Large Language Models
title_zh: 用于大语言模型可扩展个性化的合成交互数据
authors: "Yuchen Ma, Yue Huang, Wenjie Wang, Xiaonan Luo, Xiangliang Zhang, Stefan Feuerriegel"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12394v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 用于可扩展个性化和动态偏好建模的合成数据
tldr: 针对大语言模型个性化提示优化中缺乏高质量交互数据和偏好信号的问题，本文提出了PersonaGym框架，通过模拟动态偏好过程生成高保真合成数据集PersonaAtlas。同时，提出PPOpt框架，采用“推理-优化”范式，在不修改模型的前提下，基于用户画像和强化学习优化提示词。实验证明该方法在任务性能、个性化质量及鲁棒性方面均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的提示优化方法多关注任务层面，因缺乏高质量、隐私安全的个性化交互数据而忽视了用户特定的偏好和约束。
method: 引入PersonaGym框架生成合成交互数据，并提出PPOpt框架，通过推理用户画像并结合多目标强化学习来重写个性化提示词。
result: 实验结果显示，该方法在任务执行、个性化程度以及对噪声和稀疏信号的鲁棒性上均显著优于最先进的基准模型。
conclusion: 本研究通过合成数据和可扩展的优化框架，为实现大规模、高保真的LLM个性化服务提供了有效的解决方案。
---

## 摘要
个性化提示词为向不同用户部署大语言模型（LLMs）提供了巨大机遇，然而现有的提示词优化方法主要集中在任务级优化，在很大程度上忽视了特定用户的偏好和个体用户的潜在约束。这一差距主要归因于：(i) 缺乏能够大规模捕捉个性化用户-LLM交互的高质量、隐私敏感数据，以及 (ii) 缺乏针对个体偏好的鲁棒奖励信号。为了克服现有的数据限制，我们引入了一个名为 PersonaGym 的高保真合成数据生成框架。与以往将个性化视为静态角色-偏好对的工作不同，PersonaGym 通过智能体 LLM 系统建模动态偏好过程，以模拟真实的偏好行为和语义感知噪声，从而生成个性化的多轮交互轨迹。利用 PersonaGym，我们发布了 PersonaAtlas，这是一个大规模、高质量且多样化的合成数据集，包含高保真的多轮个性化交互轨迹，能够紧密反映现实世界的偏好表达和噪声模式。我们进一步提出了个性化提示词优化（PPOpt），这是一个可扩展且与模型无关的框架，它根据交互历史优化用户提示词，而无需修改已部署的 LLM。PPOpt 采用“先推理后优化”的范式，推断出显式的用户画像，并以该画像为条件进行提示词重写，以避免奖励作弊（reward hacking）。PPOpt 的训练过程整合了冷启动监督先验与结果驱动的多目标强化学习。我们进行了广泛的实验，证明在任务性能、个性化质量以及对噪声和稀疏偏好信号的鲁棒性方面，该方法相较于最先进的基准模型均有一致的提升。

## Abstract
Personalized prompting offers large opportunities for deploying large language models (LLMs) to diverse users, yet existing prompt optimization methods primarily focus on task-level optimization while largely overlooking user-specific preferences and latent constraints of individual users. This gap is primarily due to (i) the absence of high-quality, privacy-sensitive data that capture personalized user-LLM interactions at scale, and (ii) the lack of robust reward signals for individual preferences. To overcome existing data limitations, we introduce a high-fidelity synthetic data generation framework called PersonaGym. Unlike prior work that treats personalization as static persona-preference pairs, PersonaGym models a dynamic preference process via an agentic LLM system to simulate realistic preference behaviors and semantic-aware noise in order to generate personalized multi-turn interaction trajectories. Using PersonaGym, we release PersonaAtlas, a large-scale, high-quality, and diverse synthetic dataset of high-fidelity multi-turn personalized interaction trajectories that closely mirror real-world preference expression and noise patterns. We further propose Personalized Prompt Optimization (PPOpt), a scalable and model-agnostic framework that optimizes user prompts based on interaction histories without modifying the deployed LLM. PPOpt adopts a reason-then-optimize paradigm that infers an explicit user profile and conditions prompt rewriting on the user profile to avoid reward hacking. Our training procedure for PPOpt integrates a cold-start supervised prior with outcome-driven multi-objective reinforcement learning. We present extensive experiments to demonstrate consistent improvements over state-of-the-art baselines in terms of task performance, personalization quality, and robustness to noisy as well as to sparse preference signals.

---

## 论文详细总结（自动生成）

以下是对论文《Synthetic Interaction Data for Scalable Personalization in Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）具备强大的通用能力，但现实应用中用户往往有特定的个性化需求（如语气、专业深度、格式约束等）。
*   **核心瓶颈**：
    1.  **数据缺口**：由于隐私和授权限制，高质量、大规模的真实用户-LLM个性化交互数据极度匮乏。
    2.  **方法局限**：现有的个性化对齐方法通常依赖于静态的“角色-偏好”对，且往往需要对模型进行参数微调，这在面对闭源或已部署的固定模型时难以扩展。
*   **研究目标**：通过合成数据生成和黑盒提示词优化，实现无需修改模型参数的可扩展个性化对齐。

### 2. 方法论
论文提出了两个核心框架：**PersonaGym**（数据生成）和 **PPOpt**（提示词优化）。

#### PersonaGym：高保真合成数据生成
*   **核心思想**：将个性化建模为“动态偏好过程”而非静态标签。
*   **关键技术**：
    *   **三智能体架构**：由 **User Agent**（模拟用户行为）、**Assistant Agent**（生成响应）和 **Distractor Agent**（注入噪声）组成。
    *   **Persona Bank**：构建包含33个维度、282个取值的角色库，涵盖基本信息、行为特征和输出约束。
    *   **语义感知噪声**：Distractor Agent 注入三层噪声：表层噪声（拼写错误）、信息缺失（约束模糊）和语义歧义（意图冲突），以模拟真实人类输入。
    *   **结果驱动标注**：根据用户是否发出“追问”或“纠正”来自动判定当前交互的成功与否（正/负样本）。

#### PPOpt：个性化提示词优化
*   **核心思想**：采用“**推理-再优化**”（Reason-then-Optimize）范式。
*   **算法流程**：
    1.  **画像推断**：从历史交互轨迹中推断出显式的用户画像（Reasoning）。
    2.  **提示词重写**：基于推断的画像，将原始查询重写为更符合用户偏好的个性化提示词。
    3.  **训练策略**：
        *   **冷启动 SFT**：使用强模型（如 GPT-5.2）生成的伪标签进行监督微调，学习输出格式。
        *   **多目标 RL**：使用 **GRPO**（群组相对策略优化）算法。奖励函数结合了“画像推断准确度”和“下游任务结果提升”。

### 3. 实验设计
*   **数据集**：
    *   **PersonaAtlas**：由 PersonaGym 生成的合成数据集（1万+对话，2000+角色）。
    *   **Real-world Test Set**：通过 Prolific 平台招募 99 名人类专家对合成轨迹进行编辑和校验，确保真实性。
    *   **任务领域**：涵盖数学推理（OpenR1-Math）、代码（CodeFeedback）、通用知识（MMLU）、科学问答（ARC）等。
*   **基准方法（Baselines）**：
    *   Vanilla Prompt（原始提示词）。
    *   History-augmented（简单拼接历史）。
    *   Persona-based rewriting（基于角色的重写）。
    *   Few-shot ICL（少样本上下文学习）。
    *   Controller-guided prompting（控制器引导）。
*   **评估指标**：使用 LLM-as-a-judge（GPT-5.2）评估“个性化得分”和“任务完成得分”。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 LoRA（秩 r=32）进行微调，RL 阶段使用 GRPO 算法，每组采样 K=8 个样本。
*   **成本分析**：详细列出了合成数据的 Token 消耗。生成一个完整对话样本平均消耗约 1.76 万个 Token，使用 GPT-5.2 的成本约为 $0.247/样本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了多种优化器模型（Llama-3-8B, Qwen3-8B, GPT-oss-20B）。
    *   部署在多种目标模型上（GPT-4o-mini, GPT-5.2, Llama-3.3-70B, Claude-Sonnet-4.5）。
    *   进行了广泛的消融实验（验证 SFT 与 RL 的贡献）。
    *   包含人类评估（Task A/B/C）以验证合成数据的质量。
*   **充分性评价**：实验设计较为充分，跨越了不同规模和架构的模型，并结合了合成数据与真实人类编辑的数据，验证了方法的泛化能力和鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：PPOpt 在个性化质量上比基准方法提升了约 33%，同时将任务性能的损失控制在极低水平（约 2.6%）。
*   **模型无关性**：作为一种黑盒优化方案，PPOpt 能够显著提升弱模型（如 8B）的个性化表现，使其接近强模型。
*   **鲁棒性**：通过 Distractor 注入的噪声训练，模型在面对模糊或不一致的用户输入时表现更稳健。
*   **画像推断的重要性**：消融实验证明，显式的推理过程（Reasoning）能有效防止奖励作弊，稳定 RL 训练。

### 7. 优点（亮点）
*   **动态建模**：突破了传统静态 Persona 的限制，通过模拟多轮交互捕捉动态偏好。
*   **黑盒兼容**：无需访问模型权重，对闭源 API 模型极其友好。
*   **数据贡献**：发布的 PersonaAtlas 数据集为个性化研究提供了高质量的开源资源。
*   **范式创新**：Reason-then-Optimize 结构提高了提示词优化的可解释性和准确性。

### 8. 不足与局限
*   **推理延迟**：由于采用了“先推理画像再重写提示词”的步骤，会增加实际部署时的推理延迟和 Token 成本。
*   **合成数据偏差**：尽管有人类校验，但模型训练仍高度依赖合成数据的分布，可能无法完全覆盖极端真实的人类行为。
*   **评估依赖**：核心评估指标依赖于 LLM-as-a-judge，虽然这是目前主流做法，但可能存在模型偏见。
*   **算力细节缺失**：缺乏具体的硬件训练环境说明，不利于其他研究者评估复现所需的物理资源。

（完）
