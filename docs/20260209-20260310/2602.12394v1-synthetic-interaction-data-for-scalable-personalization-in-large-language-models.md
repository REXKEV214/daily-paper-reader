---
title: Synthetic Interaction Data for Scalable Personalization in Large Language Models
title_zh: 用于大语言模型可扩展个性化的合成交互数据
authors: "Yuchen Ma, Yue Huang, Wenjie Wang, Xiaonan Luo, Xiangliang Zhang, Stefan Feuerriegel"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12394v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 用于可扩展大模型个性化的合成数据
tldr: 针对大语言模型个性化提示优化中缺乏高质量交互数据和偏好信号的问题，本文提出了PersonaGym框架，通过模拟动态偏好过程生成高保真合成数据集PersonaAtlas。同时，提出PPOpt框架，采用“推理-优化”范式，在不修改模型的前提下，基于用户画像和强化学习优化提示词。实验证明该方法在任务性能、个性化质量及鲁棒性方面均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的提示优化方法多关注任务层面，因缺乏高质量、隐私安全的个性化交互数据而忽视了用户特定的偏好和约束。
method: 引入PersonaGym框架生成合成交互数据，并提出PPOpt框架，通过推理用户画像并结合多目标强化学习来重写个性化提示词。
result: 实验结果显示，该方法在任务执行、个性化程度以及对噪声和稀疏信号的鲁棒性上均显著优于最先进的基准模型。
conclusion: 本研究通过合成数据和可扩展的优化框架，为实现大规模、高保真的LLM个性化服务提供了有效的解决方案。
---

## 摘要
个性化提示词为向不同用户部署大语言模型（LLMs）提供了巨大机遇，然而现有的提示词优化方法主要集中在任务级优化，在很大程度上忽视了特定用户的偏好和个体用户的潜在约束。这一差距主要归因于：（i）缺乏能够大规模捕捉个性化用户-LLM交互的高质量、隐私敏感数据；（ii）缺乏针对个体偏好的鲁棒奖励信号。为了克服现有的数据限制，我们引入了一个名为 PersonaGym 的高保真合成数据生成框架。与以往将个性化视为静态角色-偏好对的工作不同，PersonaGym 通过智能体 LLM 系统建模动态偏好过程，以模拟真实的偏好行为和语义感知噪声，从而生成个性化的多轮交互轨迹。利用 PersonaGym，我们发布了 PersonaAtlas，这是一个大规模、高质量且多样化的合成数据集，包含高保真多轮个性化交互轨迹，能够紧密反映现实世界的偏好表达和噪声模式。我们进一步提出了个性化提示词优化（PPOpt），这是一个可扩展且与模型无关的框架，它根据交互历史优化用户提示词，而无需修改已部署的 LLM。PPOpt 采用了“先推理后优化”的范式，推断出显式的用户画像，并基于该画像进行提示词重写，以避免奖励作弊（reward hacking）。我们的 PPOpt 训练过程结合了冷启动监督先验与结果驱动的多目标强化学习。我们进行了广泛的实验，证明在任务性能、个性化质量以及对噪声和稀疏偏好信号的鲁棒性方面，该方法相比最先进的基准模型均有持续提升。

## Abstract
Personalized prompting offers large opportunities for deploying large language models (LLMs) to diverse users, yet existing prompt optimization methods primarily focus on task-level optimization while largely overlooking user-specific preferences and latent constraints of individual users. This gap is primarily due to (i) the absence of high-quality, privacy-sensitive data that capture personalized user-LLM interactions at scale, and (ii) the lack of robust reward signals for individual preferences. To overcome existing data limitations, we introduce a high-fidelity synthetic data generation framework called PersonaGym. Unlike prior work that treats personalization as static persona-preference pairs, PersonaGym models a dynamic preference process via an agentic LLM system to simulate realistic preference behaviors and semantic-aware noise in order to generate personalized multi-turn interaction trajectories. Using PersonaGym, we release PersonaAtlas, a large-scale, high-quality, and diverse synthetic dataset of high-fidelity multi-turn personalized interaction trajectories that closely mirror real-world preference expression and noise patterns. We further propose Personalized Prompt Optimization (PPOpt), a scalable and model-agnostic framework that optimizes user prompts based on interaction histories without modifying the deployed LLM. PPOpt adopts a reason-then-optimize paradigm that infers an explicit user profile and conditions prompt rewriting on the user profile to avoid reward hacking. Our training procedure for PPOpt integrates a cold-start supervised prior with outcome-driven multi-objective reinforcement learning. We present extensive experiments to demonstrate consistent improvements over state-of-the-art baselines in terms of task performance, personalization quality, and robustness to noisy as well as to sparse preference signals.

---

## 论文详细总结（自动生成）

这篇论文由慕尼黑大学（LMU Munich）、圣母大学和四川大学的研究人员合作完成，旨在解决大语言模型（LLM）在个性化部署中面临的数据缺失和优化难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：尽管 LLM 具备强大的通用能力，但不同用户在回复风格、专业深度、格式偏好及潜在约束（如隐私、特定领域规范）方面存在显著差异。
*   **核心挑战**：
    1.  **数据鸿沟**：由于隐私和授权限制，缺乏大规模、高质量且包含多轮交互的真实用户个性化数据。
    2.  **奖励信号缺失**：用户偏好往往是隐性的，难以获得鲁棒的奖励信号来指导模型对齐。
    3.  **方法局限**：现有方法多依赖静态画像或需要修改模型参数，难以扩展到闭源或参数量巨大的模型。

### 2. 方法论：PersonaGym 与 PPOpt
论文提出了两个核心框架：
*   **PersonaGym（数据生成框架）**：
    *   **核心思想**：将个性化建模为“动态偏好过程”而非静态标签。
    *   **关键技术**：采用智能体（Agentic）架构，包含三个角色：**用户 Agent**（模拟用户意图）、**助手 Agent**（生成回复）和**干扰项（Distractor）Agent**。
    *   **噪声建模**：Distractor Agent 负责注入三层语义感知噪声（语法噪声、信息缺失、语义歧义），以模拟真实世界中用户表达的不确定性。
    *   **PersonaAtlas 数据集**：基于此框架生成了包含 2000 个画像、1 万多场对话的大规模合成数据集。
*   **PPOpt（个性化提示词优化框架）**：
    *   **推理-优化范式（Reason-then-Optimize）**：优化器首先从历史交互中推断出显式的“用户画像推理链”，然后基于该画像重写当前提示词。
    *   **训练流程**：
        1.  **冷启动 SFT**：利用强模型（如 GPT-5.2）生成的伪标签引导优化器学习输出格式。
        2.  **结果驱动的多目标 RL**：使用 GRPO 算法，结合“画像推断准确性”和“下游任务结果改进”两个奖励信号进行强化学习。
    *   **模型无关性**：该框架仅在提示词层面操作，无需修改部署的基座模型。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成基准**：基于 `ai2_arc`、`IFEval`、`MBPP`、`oasst1`、`ultrachat` 等多个任务域构建。
    *   **真实世界测试集**：通过 Prolific 平台招募 99 名人类专家对合成轨迹进行编辑和校验，确保行为保真度。
*   **对比基准（Baselines）**：
    *   历史增强提示词（直接拼接历史）。
    *   基于画像的查询重写（如 Summ+Ret）。
    *   基于偏好的少样本上下文学习（ICL）。
    *   控制器引导的提示词优化（如 Matryoshka Pilot）。
*   **评估指标**：采用 LLM-as-a-Judge，评估“个性化得分”和“任务完成得分”。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **模型使用**：使用了多种模型作为优化器（Llama-3-8B, Qwen3-8B, GPT-oss-20B）和部署模型（GPT-4o-mini, GPT-5.2, Llama-3.3-70B 等）。
*   **成本估算**：详细列出了合成数据的 Token 消耗成本（如使用 GPT-5.2 生成一个样本约需 0.247 美元）。
*   **技术细节**：提到使用 LoRA（rank=32）进行微调，RL 阶段采用 GRPO 算法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个以上的不同任务领域。
    *   对比了 3 种不同架构的优化器模型和 5 种以上的部署模型。
    *   进行了消融实验（SFT vs RL，有无画像推理奖励）。
    *   包含人类评估（Task A/B/C）以验证合成数据的质量。
*   **充分性评价**：实验设计非常充分。通过跨模型验证（用 A 优化器优化 B 部署模型）证明了方法的通用性；通过真实世界数据校验，弥补了合成数据可能存在的偏差，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：PPOpt 在个性化得分上显著优于所有基准（提升约 33%），同时对任务完成度的负面影响极小（仅下降约 2.6%）。
*   **鲁棒性**：通过 Distractor 注入噪声后训练的模型，在面对模糊或不一致的用户输入时表现更稳健。
*   **可迁移性**：较弱的优化器（如 8B 模型）也能显著提升强部署模型（如 70B 或 GPT-5）的个性化表现。
*   **推理的重要性**：消融实验证明，显式的画像推理步骤能有效防止强化学习中的“奖励作弊”现象。

### 7. 优点与亮点
*   **动态建模**：突破了静态画像的限制，通过多轮交互模拟偏好的演变，更符合真实场景。
*   **模型无关**：无需访问模型权重，对闭源 API 模型极其友好，具有极强的工程落地价值。
*   **高质量数据**：发布的 PersonaAtlas 数据集在多样性指标（Self-BLEU, TTR）上优于 UltraChat 等知名数据集。
*   **系统性噪声设计**：三层噪声模型为研究 LLM 在不确定环境下的鲁棒性提供了新思路。

### 8. 不足与局限
*   **评测偏差**：主要依赖 LLM-as-a-Judge，尽管有部分人类评估，但仍可能存在模型偏见（Self-preference bias）。
*   **合成数据局限**：虽然经过人类校验，但合成数据生成的“用户行为”可能仍无法完全覆盖极端或极其复杂的真实人类心理。
*   **成本问题**：虽然比微调模型便宜，但对于超长历史的交互，实时重写提示词会增加推理延迟和 Token 成本。
*   **隐私风险**：虽然论文强调了隐私安全性，但在推断用户画像时，如何确保不提取或滥用敏感个人信息仍需更细致的约束机制。

（完）
