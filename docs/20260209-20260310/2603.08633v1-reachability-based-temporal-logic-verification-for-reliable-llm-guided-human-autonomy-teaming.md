---
title: Reachability-based Temporal Logic Verification for Reliable LLM-guided Human-Autonomy Teaming
title_zh: 基于可达性时序逻辑验证的可靠大语言模型引导人机协作
authors: "Joonwon Choi, Kartik Anand Pant, Karthik Nune, Inseok Hwang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08633v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: LLM引导的人机自主协作
tldr: 本研究针对大语言模型（LLM）引导的人机协作（HAT）中的可靠性问题，提出了一种基于可达性分析的信号时序逻辑（STL）验证框架。该框架利用LLM作为自然语言与STL规约之间的翻译器，并引入STL可行性过滤器（SFF）来验证指令的可行性。通过将复杂规约分解为子公式并进行并行验证，系统能识别不可行的指令并向人类操作员提供详细反馈，从而增强了人机协作的安全性和沟通效率。
selection_source: fresh_fetch
motivation: 旨在解决LLM在将人类自然语言指令转化为自主系统动作时，可能产生不可行或不安全指令的可靠性挑战。
method: 提出一种STL可行性过滤器（SFF），通过将复杂逻辑分解为子公式并利用可达性分析验证其在目标动力系统中的可行性。
result: 实验证明该框架能有效识别并过滤不可行的子公式，并生成具体的信息反馈以引导人类操作员修改指令。
conclusion: 该框架通过结合形式化验证与LLM，显著提升了人机协作系统的可靠性、安全性和交互透明度。
---

## 摘要
我们提出了一种基于可达性的框架，利用信号时序逻辑（STL）实现可靠的大语言模型（LLM）引导的人机协作（HAT）。在该框架中，LLM 被用作翻译器，将人类操作员给出的自然语言指令转换为相应的 STL 规范，反之亦然。我们提出了一种 STL 可行性过滤器（SFF）来检查生成的 STL 的可行性。SFF 首先将复杂且嵌套的 LLM 翻译结果分解为一组更简单的子公式，以便进行并行化处理并生成信息丰富的反馈。随后，应用可达性分析方法来验证每个子公式对于目标动力系统是否可行：若可行，则执行任务规划；否则，予以拒绝。所提出的 SFF 能够识别不可行的子公式，而不仅仅是为整个 STL 提供布尔验证结果，从而促进 LLM 生成反馈，请求人类操作员修改指令。因此，该框架通过在人类操作员和自主智能体之间实现安全且信息丰富的通信，实现了更可靠的 HAT。实验结果表明，该框架能够成功过滤掉不可行的子公式，并基于此类信息生成具有参考价值的反馈。

## Abstract
We propose a reachability-based framework for reliable LLM-guided human-autonomy teaming (HAT) using signal temporal logic (STL). In the proposed framework, LLM is leveraged as a translator that transfers natural language commands given by a human operator into corresponding STL specifications or vice versa. An STL feasibility filter (SFF) is proposed to check the feasibility of the generated STL. The SFF first decomposes the complex and nested LLM translation into a set of simpler subformulas for parallelization and informative feedback generation. The reachability analysis method is then applied to verify if each subformula is feasible for a target dynamical system: if feasible, perform mission planning, otherwise, reject it. The proposed SFF can identify infeasible subformulas, more than simply providing the boolean verification results for the whole STL, thereby facilitating the feedback generation of LLM to request modification of the command to the human. Consequently, the proposed framework can allow more reliable HAT by enabling safe and informative communication between the human operator and the autonomous agent. Our experiments demonstrate that the proposed framework can successfully filter out infeasible subformulas and generate informative feedback based on such information.

---

## 论文详细总结（自动生成）

以下是对论文《Reachability-based Temporal Logic Verification for Reliable LLM-guided Human-Autonomy Teaming》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**人机协作（Human-Autonomy Teaming, HAT）**中的可靠性与安全性问题。虽然大语言模型（LLM）在理解人类自然语言指令并将其转化为机器人可执行代码（如信号时序逻辑 STL）方面表现出色，但 LLM 往往无法准确判断指令在物理世界中的**可行性（Feasibility）**和**安全性**。
*   **研究动机**：现有的 LLM 规划方法多为“事后验证”（执行中或执行后发现错误），缺乏“事前验证”机制。
*   **核心目标**：开发一个框架，在任务规划和执行前，利用形式化方法验证 LLM 生成的指令是否符合物理系统的动力学约束，并在不可行时向人类提供具有解释性的反馈。

### 2. 论文提出的方法论
该框架主要由三个模块组成：
*   **LLM 翻译器**：将人类的自然语言指令翻译为 STL 规约（包含 STL 公式和原子谓词字典）。
*   **STL 可行性过滤器（SFF）**：
    *   **分解（Decomposition）**：将复杂的嵌套 STL 公式分解为多个简单的子公式（分为 Level 0/1/2 三种分解程度），以便并行计算并精准定位不可行的部分。
    *   **可达性分析（Reachability Analysis）**：利用**后向可达管道（Backward Reachable Tube, BRT）**计算。通过 Hamilton-Jacobi (HJ) 可达性分析，判断系统的初始状态是否在能够满足该子公式的集合内。
*   **反馈与规划**：
    *   若所有子公式均可行，则通过**混合整数线性规划（MILP）**生成最优轨迹。
    *   若不可行，SFF 会识别出具体的不可行子公式，并将其反馈给 LLM，由 LLM 生成解释性文本告知人类操作员为何指令无法完成（例如：时间不够或区域被封锁）。

### 3. 实验设计
论文设计了三个主要实验场景来验证框架：
*   **场景 1：可行性检查对比**。模拟校车驾驶任务，设置了复杂的时空约束（如特定时间段封锁的校区）。对比了 ChatGPT-4o、Claude 3.7 Sonnet 和 Llama 4 的自我可行性判断与 SFF 验证结果。
*   **场景 2：反馈生成与实机验证**。在包含障碍物和特定进入条件的复杂地图中，验证 SFF 识别不可行指令并生成反馈的能力，并使用 **Crazyflie 纳米无人机**进行实机飞行实验。
*   **场景 3：分解对计算效率的影响**。通过 100 次蒙特卡洛模拟，对比原始 STL 与不同分解级别（Level 0/1/2）在 MILP 规划中的计算耗时。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **Intel i7-13620H 处理器**的笔记本电脑上运行。
*   **软件/库**：使用了 `stlpy`、`Gurobi` 求解器、`stlcg` 库以及 `hj-reachability` 实现。
*   **算力说明**：文中未提及使用 GPU 进行训练或推理的具体型号（LLM 推理可能通过 API 完成），计算重点在于 CPU 上的可达性分析和 MILP 求解。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从纯数值模拟到物理无人机实验的完整流程。在效率测试中进行了 **100 次蒙特卡洛模拟**以获取平均值和方差。
*   **充分性评价**：实验设计较为充分且客观。通过对比多种主流 LLM，证明了即使是最先进的模型在没有形式化验证的情况下也会对任务可行性产生误判。实机实验增强了方法在真实动力系统中的说服力。

### 6. 论文的主要结论与发现
*   **LLM 的局限性**：主流 LLM（如 GPT-4o）在面对复杂的时空约束时，经常会错误地认为不可行的任务是可行的。
*   **SFF 的有效性**：提出的 SFF 能够 100% 准确地识别出导致任务失败的具体逻辑片段。
*   **效率提升**：STL 分解机制不仅有助于生成反馈，还能显著降低 MILP 规划的计算负担，**计算时间缩短了约 50%**。
*   **人机沟通**：通过提供精准的不可行原因，LLM 能够生成更有意义的反馈，引导人类修改指令，从而建立更可靠的人机协作。

### 7. 优点
*   **形式化保证**：将 LLM 的灵活性与可达性分析的严谨性结合，提供了任务执行前的安全保障。
*   **细粒度诊断**：不同于传统的布尔值（可行/不可行）反馈，该方法能指出“哪一部分”逻辑出了错。
*   **计算优化**：通过公式分解实现了并行化验证，并优化了后续的轨迹规划速度。

### 8. 不足与局限
*   **保守性风险**：Level 1 和 Level 2 的分解是**保守的（Conservative）**，这意味着某些在物理上可行但复杂的任务可能会被过滤器误判为不可行。
*   **子公式关联性**：目前的框架独立验证每个子公式，可能无法完全捕捉子公式之间复杂的耦合关系（例如两个子公式各自可行，但同时执行不可行）。作者提到未来将通过“多目标到达-规避（Multiple Reach-Avoid）”问题来解决此限制。
*   **计算扩展性**：HJ 可达性分析在处理高维系统时存在“维度灾难”，对于极其复杂的动力系统，实时性可能受限。

（完）
