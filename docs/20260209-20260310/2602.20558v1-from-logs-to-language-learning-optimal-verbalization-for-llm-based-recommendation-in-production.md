---
title: "From Logs to Language: Learning Optimal Verbalization for LLM-Based Recommendation in Production"
title_zh: 从日志到语言：在生产环境中为基于大语言模型的推荐学习最优语言化策略
authors: "Yucheng Shi, Ying Li, Yu Wang, Yesu Feng, Arjun Rao, Rein Houthooft, Shradha Sehgal, Jin Wang, Hao Zhen, Ninghao Liu, Linas Baltrunas"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20558v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 将用户交互日志转换为优化的推荐文本上下文
tldr: 本研究针对大语言模型（LLM）推荐系统中结构化日志转化为自然语言（Verbalization）的难题，提出了一种基于强化学习的数据中心化框架。该框架通过训练一个Verbalization代理，将原始交互历史动态转化为优化的文本上下文，以推荐准确率为奖励信号。在工业级流媒体数据集上的实验表明，该方法相比传统模板方式显著提升了推荐效果，并展现出兴趣总结和去噪等策略。
selection_source: fresh_fetch
motivation: 现有的LLM推荐系统依赖僵化的模板将日志转化为文本，导致输入表示并非最优，限制了模型的推荐性能。
method: 提出一种基于强化学习的框架，利用代理模型学习如何过滤噪声、整合元数据并重组信息，从而生成最有利于下游LLM预测的文本输入。
result: "在大规模工业流媒体数据集上，该学习到的Verbalization方法在发现性物品推荐准确率上比基于模板的基准方法提升了高达93%。"
conclusion: 实验证明通过学习优化的文本表达能显著增强LLM推荐能力，且模型自发形成了用户兴趣总结和语法规范化等有效策略。
---

## 摘要
大语言模型（LLM）是生成式推荐系统极具前景的主干网络，但一个关键挑战仍未得到充分探索：语言化（verbalization），即如何将结构化的用户交互日志转换为有效的自然语言输入。现有方法依赖于简单的字段拼接等僵化模板，导致推荐表示效果欠佳。我们提出了一个以数据为中心的框架，用于学习基于 LLM 推荐的语言化策略。利用强化学习，语言化智能体将原始交互历史转换为优化的文本上下文，并以推荐准确率作为训练信号。该智能体学会了过滤噪声、整合相关的元数据并重组信息，以提升下游预测性能。在大规模工业流式数据集上的实验表明，与基于模板的基准方法相比，学习到的语言化策略在发现项推荐准确率上实现了高达 93% 的相对提升。进一步分析揭示了诸如用户兴趣总结、噪声去除和语法规范化等涌现策略，为基于 LLM 的推荐系统如何构建有效的上下文提供了见解。

## 速览
**TLDR**：本研究针对大语言模型（LLM）推荐系统中结构化日志转化为自然语言（Verbalization）的难题，提出了一种基于强化学习的数据中心化框架。该框架通过训练一个Verbalization代理，将原始交互历史动态转化为优化的文本上下文，以推荐准确率为奖励信号。在工业级流媒体数据集上的实验表明，该方法相比传统模板方式显著提升了推荐效果，并展现出兴趣总结和去噪等策略。 \
**Motivation**：现有的LLM推荐系统依赖僵化的模板将日志转化为文本，导致输入表示并非最优，限制了模型的推荐性能。 \
**Method**：提出一种基于强化学习的框架，利用代理模型学习如何过滤噪声、整合元数据并重组信息，从而生成最有利于下游LLM预测的文本输入。 \
**Result**：在大规模工业流媒体数据集上，该学习到的Verbalization方法在发现性物品推荐准确率上比基于模板的基准方法提升了高达93%。 \
**Conclusion**：实验证明通过学习优化的文本表达能显著增强LLM推荐能力，且模型自发形成了用户兴趣总结和语法规范化等有效策略。

---

## Abstract
Large language models (LLMs) are promising backbones for generative recommender systems, yet a key challenge remains underexplored: verbalization, i.e., converting structured user interaction logs into effective natural language inputs. Existing methods rely on rigid templates that simply concatenate fields, yielding suboptimal representations for recommendation. We propose a data-centric framework that learns verbalization for LLM-based recommendation. Using reinforcement learning, a verbalization agent transforms raw interaction histories into optimized textual contexts, with recommendation accuracy as the training signal. This agent learns to filter noise, incorporate relevant metadata, and reorganize information to improve downstream predictions. Experiments on a large-scale industrial streaming dataset show that learned verbalization delivers up to 93% relative improvement in discovery item recommendation accuracy over template-based baselines. Further analysis reveals emergent strategies such as user interest summarization, noise removal, and syntax normalization, offering insights into effective context construction for LLM-based recommender systems.

---

## 论文详细总结（自动生成）

# 论文总结：从日志到语言——为生产环境中的 LLM 推荐学习最优语言化策略

## 1. 核心问题与整体含义（研究动机和背景）
在基于大语言模型（LLM）的推荐系统中，**“语言化”（Verbalization）**是将结构化的用户行为日志（如时间戳、物品 ID、观看时长等）转换为自然语言输入的过程。
*   **研究动机**：现有的方法大多依赖于硬编码的模板（如简单的字段拼接），这导致了三个问题：
    1.  **解析开销**：异构的特征空间增加了 LLM 的解析负担。
    2.  **噪声干扰**：并非所有交互都具有同等的预测价值，冗余信息会稀释信号。
    3.  **语义缺失**：纯行为模式缺乏元数据支持，难以处理冷启动或发现性推荐。
*   **核心问题**：能否不再将语言化视为固定的预处理步骤，而是将其作为一个**可学习的组件**，直接针对推荐准确率进行优化？

## 2. 方法论：核心思想与技术细节
论文提出了一个以数据为中心的框架，将推荐流程分解为两个专门的组件：**Verbalizer（语言化器）**和 **Reasoner（推理器）**。

### 核心思想
利用强化学习（RL），以最终的推荐准确率作为奖励信号（Reward），训练 Verbalizer 生成最有利于 Reasoner 理解的文本上下文。

### 关键技术细节
*   **两阶段训练流程**：
    1.  **阶段 1：Verbalizer 优化**。使用 **GRPO（组相对策略优化）** 算法。Verbalizer 针对同一输入生成多个候选文本，通过一个“Oracle Reasoner”（高性能闭源模型）评估这些文本的推荐效果，计算组内相对优势并更新参数。
    2.  **阶段 2：Reasoner 优化**。冻结训练好的 Verbalizer，使用其生成的优化文本作为输入，再次通过 GRPO 训练 Reasoner，使其适应新的数据分布。
*   **Verbalizer 变体**：
    *   **Action-Based**：仅学习“保留/丢弃”或“添加元数据”等离散动作。
    *   **Rewrite-Based（最终采用）**：允许模型自由重写、总结和重组信息。
*   **奖励函数设计**：
    *   **准确率奖励**：推荐正确得 1 分，错误得 0 分。
    *   **长度奖励（Length Reward）**：引入一个高原函数（Plateau Function），惩罚过长或过短的输出，鼓励模型在保持信息量的同时进行适度压缩。

## 3. 实验设计
*   **数据集与场景**：使用来自某大型流媒体平台（Netflix）的工业级数据集，包含三个月的用户观看记录。
*   **任务目标**：重排序（Reranking）任务，从 10 个候选物品中预测用户下一个会点击的物品。
*   **评价指标**：**Recall@1 for Discovery**（侧重于用户未看过的新内容推荐）。
*   **对比基准（Benchmarks）**：
    1.  **Template Baseline**：直接拼接字段的固定模板。
    2.  **Zero-Shot Verbalizer**：使用提示词引导 LLM 进行启发式重写，无任务特定训练。
    3.  **Action-Based Verbalizer**：仅执行过滤和增强动作的学习型模型。

## 4. 资源与算力
*   **模型规模**：实验采用了 **Qwen-3 系列**模型，包括 **8B** 和 **32B** 两个参数量级。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到使用了 GRPO 算法和 32B 规模的模型进行工业级数据的微调，可以推断其算力需求较高。

## 5. 实验数量与充分性
*   **实验规模**：在工业级大规模数据集上进行，具有很强的实战代表性。
*   **消融实验**：
    *   对比了 Action-based 与 Rewrite-based 的效果。
    *   对比了 Reasoner 在原始数据与语言化数据上的表现差异。
    *   验证了长度奖励函数对性能的影响。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还通过定性分析展示了 8B 和 32B 模型在语言化策略上的差异（如 32B 展现出更强的语义抽象能力）。实验对比了多种变体，论证了“学习语言化”比单纯“训练推理器”更有效。

## 6. 主要结论与发现
*   **性能飞跃**：相比模板基准，学习到的语言化策略在发现性推荐上实现了 **93% 的相对提升**。
*   **协同效应**：Verbalizer 创造了更易学习的数据分布，使得 Reasoner 的训练效果远好于直接在原始日志上训练。
*   **涌现策略**：模型自发学会了以下高级策略：
    1.  **语法规范化**：将机器日志转为流畅自然语言。
    2.  **噪声过滤**：丢弃无用的 ID，保留高信号的反馈（如“点赞”）。
    3.  **偏好总结**：大尺寸模型（32B）能将具体行为抽象为“对超自然惊悚片的强烈兴趣”等语义总结。

## 7. 优点
*   **解耦设计**：将上下文优化与逻辑推理分离，使得系统更具模块化，且 Verbalizer 可以针对不同领域进行迁移。
*   **数据驱动**：解决了 LLM 推荐中“如何写 Prompt”这一玄学问题，将其转化为可自动优化的工程问题。
*   **工业价值**：在真实的大规模流媒体场景下验证了有效性，证明了该方法在生产环境中的潜力。

## 8. 不足与局限
*   **训练成本**：两阶段的强化学习训练过程复杂且耗时，尤其是依赖 Oracle 模型提供奖励信号，成本较高。
*   **推理延迟**：引入 Verbalizer 环节会增加在线推理的端到端延迟，虽然文中提到可以缓存或蒸馏，但在实时性要求极高的场景下仍是挑战。
*   **泛化风险**：虽然在流媒体数据上表现优异，但在其他领域（如电商、新闻）的泛化能力尚需更多实验支撑。
*   **黑盒特性**：尽管有定性分析，但 Verbalizer 究竟过滤了哪些信息、是否会引入偏见（Bias），仍缺乏深度的可解释性监控。

（完）
