---
title: How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning
title_zh: 仅解码器大语言模型如何感知用户？用户表示学习中注意力掩码的重新思考
authors: "Jiahao Yuan, Yike Xu, Jinyong Wen, Baokun Wang, Yang Chen, Xiaotong Lin, Wuliang Huang, Ziyi Gao, Xing Fu, Yu Cheng, Weiqiang Wang"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10622v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 大语言模型作为用户表示学习的行为编码器
tldr: 本研究探讨了 Decoder-only 大语言模型在用户表示学习中的应用，重点分析了注意力掩码（因果、混合、双向）对用户嵌入质量的影响。基于支付宝大规模真实行为数据，提出了一种梯度引导软掩码（GGSM）方法，通过梯度预热平滑地将因果注意力转变为双向注意力。实验表明，该方法在 9 项工业级认知基准测试中显著提升了训练稳定性和表示质量，为优化用户编码器提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决 Decoder-only 模型在用户行为建模时，注意力掩码机制对表示学习效果影响不明的问题。
method: 提出 GGSM 方法，利用梯度引导的预热机制和线性调度，在对比学习框架下实现从因果到双向注意力的平滑过渡。
result: 在 9 个涵盖预测和营销敏感度的工业基准任务中，该方法在表示质量和训练稳定性上均优于因果和混合掩码基线。
conclusion: 掩码设计与训练过渡策略是提升 Decoder-only 模型在用户表示学习中性能的关键因素。
---

## 摘要
仅解码器（Decoder-only）大语言模型越来越多地被用作用户表示学习的行为编码器，但注意力掩码（attention masking）对用户嵌入质量的影响仍缺乏深入研究。在这项工作中，我们在一个统一的对比学习框架内，对因果（causal）、混合（hybrid）和双向（bidirectional）注意力掩码进行了系统研究。该框架在集成了长周期异构用户行为的大规模真实支付宝数据上进行了训练。为了改善从因果注意力向双向注意力过渡时的训练动态，我们提出了梯度引导软掩码（Gradient-Guided Soft Masking），这是一种基于梯度的预热（pre-warmup）方法，应用于线性调度器之前，在优化过程中逐渐开放对未来信息的关注。在涵盖预测、偏好和营销敏感性任务的 9 个工业级用户认知基准测试中，与因果、混合和仅使用调度器的基线相比，我们的方法始终能产生更稳定的训练过程和更高质量的双向表示，同时保持与解码器预训练的兼容性。总之，我们的研究结果强调了在调整仅解码器大语言模型以进行有效的用户表示学习时，掩码设计和训练过渡的重要性。我们的代码可在 https://github.com/JhCircle/Deepfind-GGSM 获取。

## Abstract
Decoder-only large language models are increasingly used as behavioral encoders for user representation learning, yet the impact of attention masking on the quality of user embeddings remains underexplored. In this work, we conduct a systematic study of causal, hybrid, and bidirectional attention masks within a unified contrastive learning framework trained on large-scale real-world Alipay data that integrates long-horizon heterogeneous user behaviors. To improve training dynamics when transitioning from causal to bidirectional attention, we propose Gradient-Guided Soft Masking, a gradient-based pre-warmup applied before a linear scheduler that gradually opens future attention during optimization. Evaluated on 9 industrial user cognition benchmarks covering prediction, preference, and marketing sensitivity tasks, our approach consistently yields more stable training and higher-quality bidirectional representations compared with causal, hybrid, and scheduler-only baselines, while remaining compatible with decoder pretraining. Overall, our findings highlight the importance of masking design and training transition in adapting decoder-only LLMs for effective user representation learning. Our code is available at https://github.com/JhCircle/Deepfind-GGSM.

---

## 论文详细总结（自动生成）

这是一份关于论文《How Do Decoder-Only LLMs Perceive Users? Rethinking Attention Masking for User Representation Learning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 Decoder-only 架构的大语言模型（LLM）成为主流，研究者开始将其作为编码器来学习用户表示（User Embedding）。然而，这类模型在预训练时采用的是**因果注意力掩码（Causal Masking）**，而在提取嵌入表示时，通常需要**双向注意力（Bidirectional Attention）**以获取全局上下文。
*   **核心问题**：论文探讨了在将 Decoder-only LLM 适配为用户表示模型时，不同的注意力掩码策略（因果、混合、双向）如何影响表示质量，以及如何平滑地完成从因果到双向的训练过渡。

### 2. 方法论
论文提出了一个统一的对比学习框架，并引入了核心技术 **GG-SM（Gradient-Guided Soft Masking，梯度引导软掩码）**：
*   **核心思想**：不直接从因果掩码切换到全双向掩码，而是利用训练过程中的梯度信息来引导模型逐渐“看见”未来信息。
*   **关键技术细节**：
    1.  **梯度引导预热（Pre-warmup）**：在训练初期，计算损失函数对隐藏状态的梯度范数。梯度较大的 Token 被认为对表示学习更重要，模型会赋予这些“未来”Token 更高的注意力权重（通过 Sigmoid 函数映射）。
    2.  **线性调度器（Linear Scheduler）**：在预热结束后，通过一个线性系数 $\alpha_t$ 在梯度引导的软掩码和全双向掩码之间进行插值，最终实现完全的双向注意力。
    3.  **数据合成增强**：提出一种训练无关的合成框架，利用 Qwen-Max 挖掘“难对齐”的用户-查询-答案对，生成高质量的硬正样本（Hard Positives）用于对比学习。
    4.  **对比学习目标**：采用带有边际因子（Margin）的 InfoNCE 损失，同时引入同侧负样本（Same-side negatives）来增强表示的区分度。

### 3. 实验设计
*   **数据集与场景**：基于支付宝（Alipay）真实世界的大规模异构数据，包含支付账单、小程序交互、搜索查询、App 记录及表格特征。
*   **Benchmark（基准测试）**：涵盖 3 大类共 9 项工业级任务：
    1.  **用户预测**：演唱会点击预测、用户登录预测、MAU 流失预测。
    2.  **行为偏好**：公交偏好、消费能力、食物兴趣、电影兴趣。
    3.  **营销敏感度**：成就偏好、体力偏好。
*   **对比方法**：
    *   **通用嵌入模型**：Qwen3-Embedding (0.6B/8B)、Llama-embed-nemotron (8B)、KaLM-Embedding。
    *   **传统用户建模模型**：MSDP、One4all、CPC、FOUND、InstructUE。
    *   **掩码变体**：Causal（因果）、Hybrid（混合，如全局查询引导）、Bidirectional（直接双向、仅调度器引导）。

### 4. 资源与算力
*   **算力投入**：使用了 **64 张 NVIDIA A100-80GB GPU**。
*   **训练细节**：
    *   训练步数：70,000 步。
    *   全局 Batch Size：2,048。
    *   微调技术：LoRA（Rank=64, Alpha=64）。
    *   基础模型：Qwen2.5-0.5B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含约 **1.43 亿** 条记录，测试集每项任务约 **50 万** 样本，属于超大规模工业级实验。
*   **充分性**：论文对比了从传统模型到最新 LLM 嵌入模型的 10 余种基线，并对掩码策略进行了详尽的消融实验（包括 3 种混合掩码变体和 3 种双向过渡方案）。
*   **客观性**：所有变体在相同的超参数和骨干网络下测试，确保了掩码策略对比的公平性。

### 6. 主要结论与发现
*   **双向掩码占优**：在用户表示学习中，双向注意力显著优于因果注意力，能够更好地整合长周期行为。
*   **过渡路径至关重要**：直接从因果预训练切换到双向微调会导致训练不稳定，而 **GG-SM 提供的平滑过渡**能显著提升最终的 AUC 表现。
*   **小模型大能量**：经过 GG-SM 优化的 0.5B 模型在特定工业任务上的表现优于 8B 甚至更大的通用嵌入模型，证明了领域适配和掩码设计的重要性。
*   **梯度引导的有效性**：梯度范数能有效识别关键行为 Token，使模型在开放注意力时能优先关注信息量大的部分。

### 7. 亮点
*   **动态掩码设计**：将静态的掩码选择转变为动态的、受梯度引导的演化过程，具有很强的理论直觉和实操性。
*   **工业级验证**：在真实支付生态的大规模数据上验证，结果具有极高的实用价值。
*   **数据合成闭环**：通过“难度探测-特征补全-后验重写”的流程生成高质量合成数据，解决了用户建模标签稀疏的问题。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 0.5B 规模的骨干网络上，虽然证明了其高效性，但尚未探讨 GG-SM 在超大规模（如 72B+）模型上的扩展效应。
*   **任务类型限制**：主要评估了判别式任务（分类/预测），未深入探讨双向掩码改造后对生成式任务（如解释用户偏好）的潜在负面影响。
*   **私有数据依赖**：核心实验基于支付宝内部数据，外部研究者难以完全复现其在特定业务场景下的绝对数值。

（完）
