---
title: Pretraining with Token-Level Adaptive Latent Chain-of-Thought
title_zh: 基于词元级自适应潜思维链的预训练
authors: "Boyi Zeng, Yiqin Hao, He Li, Shixiang Song, Feichen Song, Zitong Wang, Siyuan Huang, Yi Xu, ZiWei He, Xinbing Wang, Zhouhan Lin"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08220v1"
tags: ["query:llm-personalization"]
score: 6.0
evidence: 语言模型中针对困难标记的自适应潜在大脑链计算
tldr: 针对大模型参数扩展受限的问题，本文提出一种在预训练阶段引入“自适应Token级隐式思维链（CoT）”的方法。该方法允许模型在生成每个Token前，根据任务难度动态调整隐式推理步数，从而在不增加参数量的前提下提升计算效率。实验证明，该方法在Llama架构上显著改善了语言建模困惑度和下游任务准确率，且比传统循环基准更节省计算资源。
selection_source: fresh_fetch
motivation: 旨在通过在预训练中内化隐式思维链，解决大模型参数扩展受限于高质量语料和通信成本的问题。
method: 提出一种自适应隐式CoT预训练方法，使模型能根据Token难度动态生成变长推理轨迹，并实现自动停机。
result: 在Llama架构上的实验显示，该方法在降低训练FLOPs的同时，显著提升了语言建模的困惑度和下游任务表现。
conclusion: 证明了在预训练阶段引入自适应隐式推理能力是提升模型性能和计算效率的有效途径。
---

## 摘要
通过增加参数和训练数据来扩展大语言模型，正日益受到高质量语料库有限和通信成本上升的限制。本研究探索了另一个维度：在不扩大参数规模的情况下，通过将潜思维链（latent CoT）内化到预训练中，来增加每个词元的计算量。我们提出了词元级自适应潜思维链预训练（adaptive latent CoT），模型在输出每个词元之前会生成变长的潜思维链轨迹——为困难词元分配较长的轨迹，为简单词元分配较短（甚至为零）的轨迹。重要的是，这种行为在通用文本的一阶段预训练中自然涌现，并通过词元级自适应停机（adaptive halting）减少了训练和推理过程中的计算量。基于 Llama 架构的实验表明，自适应潜思维链一致地提升了语言模型的困惑度（perplexity）和广泛的下游任务准确率，甚至在训练 FLOPs 少于先前循环基线模型的情况下也是如此。

## Abstract
Scaling large language models by increasing parameters and training data is increasingly constrained by limited high-quality corpora and rising communication costs. This work explores an alternative axis: increasing per-token computation without expanding parameters, by internalizing latent Chain-of-Thought (CoT) into pretraining. We propose Pretraining with Token-Level Adaptive Latent CoT (adaptive latent CoT), where the model generates a variable-length latent CoT trajectory before emitting each token -- allocating longer trajectories to difficult tokens and shorter (or even zero) trajectories to easy ones. Importantly, this behavior emerges naturally from one-stage pretraining on general text and reduces computation in both training and inference via token-wise adaptive halting. Experiments with Llama architectures show that adaptive latent CoT consistently improves language modeling perplexity and broad downstream accuracy, even with fewer training FLOPs than prior recurrent baselines.