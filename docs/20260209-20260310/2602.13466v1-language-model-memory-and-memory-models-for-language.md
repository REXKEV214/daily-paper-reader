---
title: Language Model Memory and Memory Models for Language
title_zh: 语言模型的记忆与语言的记忆模型
authors: Benjamin L. Badger
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13466v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 表征语言模型记忆与向量嵌入
tldr: 本研究探讨了语言模型将输入信息存储在隐藏层嵌入（即“记忆”）中的能力。研究发现，传统的因果语言模型生成的嵌入信息量较少，难以实现高效的信息存储。为此，作者提出了一种结合因果预测与信息保留目标的并行编码器-解码器架构。通过冻结高保真编码器并采用课程学习，模型能生成信息丰富的记忆嵌入，在显著提升计算效率的同时，揭示了仅靠下个词预测难以形成准确记忆的本质原因。
selection_source: fresh_fetch
motivation: 现有语言模型嵌入在存储输入信息方面效率低下，且缺乏对这种“记忆”机制的深入表征。
method: 提出一种并行编码器-解码器架构，结合因果训练与信息保留目标，并采用冻结编码器及课程学习策略。
result: 实验表明，结合信息保留目标的模型能生成近乎完美的记忆嵌入，并大幅提高计算效率。
conclusion: 下个词预测任务因其不可逆性而不利于记忆形成，必须结合信息保留目标才能构建高效的记忆模型。
---

## 摘要
机器学习模型将输入信息存储在隐藏层向量嵌入（类似于“记忆”概念）中的能力被广泛应用，但尚未得到充分表征。我们发现，无论训练期间的数据和计算规模如何，语言模型嵌入通常包含相对较少的输入信息。相比之下，为输入再生而训练的自编码器嵌入能够形成近乎完美的记忆。用记忆嵌入替代标记序列可显著提高计算效率，从而促使我们引入一种可并行化的编码器-解码器记忆模型架构。在因果训练下，这些模型包含信息匮乏的嵌入，无法进行任意信息访问；但通过结合因果和信息保留目标函数，它们学会了形成和解码信息丰富的记忆。通过冻结高保真编码器，并采用课程学习方法（解码器先学习处理记忆，再学习预测下一个标记），可以进一步简化训练。我们提出这样一种观点：仅靠下一个标记预测训练并不适合准确的记忆形成，因为该目标本身是不可逆的，这促使在输入未完全暴露的模型中使用组合目标函数。

## Abstract
The ability of machine learning models to store input information in hidden layer vector embeddings, analogous to the concept of `memory', is widely employed but not well characterized. We find that language model embeddings typically contain relatively little input information regardless of data and compute scale during training. In contrast, embeddings from autoencoders trained for input regeneration are capable of nearly perfect memory formation. The substitution of memory embeddings for token sequences leads to substantial computational efficiencies, motivating the introduction of a parallelizable encoder-decoder memory model architecture. Upon causal training these models contain information-poor embeddings incapable of arbitrary information access, but by combining causal and information retention objective functions they learn to form and decode information-rich memories. Training can be further streamlined by freezing a high fidelity encoder followed by a curriculum training approach where decoders first learn to process memories and then learn to additionally predict next tokens. We introduce the perspective that next token prediction training alone is poorly suited for accurate memory formation as the objective itself is non-invertible, motivating the use of combined objective functions for models where the entire input is not exposed.

---

## 论文详细总结（自动生成）

这篇论文《Language Model Memory and Memory Models for Language》深入探讨了语言模型（LM）如何存储输入信息，并提出了一种新型的记忆模型架构。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的因果语言模型（Causal LLMs）在处理输入序列时，其生成的隐藏层嵌入（Embedding）究竟保留了多少输入信息？
*   **研究动机**：
    *   虽然 LLM 被广泛认为具有“记忆”能力，但这种能力从未被量化表征。
    *   传统的“下个词预测”（Next Token Prediction）任务是多对一的映射，本质上是不可逆的，这导致模型倾向于只保留预测所需的极少信息，而非完整的输入信息。
    *   为了提高推理效率（如减小 KV Cache、降低首字延迟），需要用极少数的“记忆嵌入”替代长标记序列，但这要求嵌入必须具备极高的信息保真度。

### 2. 方法论
*   **核心思想**：引入一种**可并行的编码器-解码器记忆模型架构**，通过结合“因果预测”与“信息保留”双重目标，使模型能够生成并利用高保真度的记忆嵌入。
*   **关键技术细节**：
    *   **并行架构**：将输入序列分为 $s$ 个分块（Chunks），编码器并行处理每个分块并生成记忆嵌入。计算复杂度从 $O(n^2)$ 降低到 $O(n^{4/3})$。
    *   **组合目标函数**：$L' = L_{causal} + L_{copy}$。其中 $L_{causal}$ 是标准的下个词预测损失，$L_{copy}$ 是要求模型根据记忆嵌入重建输入序列的损失。
    *   **课程学习（Curriculum Training）**：为了避免模型陷入局部最优，先训练解码器在没有原始标记的情况下从记忆嵌入中恢复信息（Blank Copy 任务），然后再引入标记进行因果预测训练。
    *   **冻结编码器策略**：使用预训练的高保真自编码器（Autoencoder）作为编码器并将其冻结，仅训练解码器如何提取和使用这些记忆。

### 3. 实验设计
*   **数据集**：主要使用 **FineWeb-edu**（通用网页数据）和 **FineMath**（数学专用数据）。
*   **评估指标**：
    *   **熵比（Entropy Ratio, $H_r$）**：衡量嵌入包含输入信息的比例，消除了不同分词器大小的影响。
    *   **Token 准确率**：基于汉明度量的 Token 识别正确率。
*   **对比方法**：
    *   **基准模型**：BERT-large, Llama 3.1 (1b), Qwen 3 (0.6b)。
    *   **任务对比**：对比了因果模型、检索模型和自编码器在信息保留能力上的差异。
    *   **任务场景**：包括输入再生（Inversion）、复制任务（Copy Task）和空白复制任务（Blank Copy）。

### 4. 资源与算力
*   **算力参考**：论文定义了“计算可行性”的标准，约为 **96 个 V100 小时**（约等于 **24 个 H100 小时**），总计约 $170 \times 10^{18}$ FLOPs。
*   **训练细节**：使用 AdamW 优化器，线性预热 500 步。Transformer 的学习率为 $2 \times 10^{-4}$，Mixer 架构为 $5 \times 10^{-4}$。
*   **硬件环境**：实验在支持分布式数据并行（DDP）的环境下运行，使用了 V100/H100 等 GPU。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多组实验，涵盖了从 256 到 1024 的不同上下文窗口，以及不同数量的分块（$s=4$ 等）。
*   **充分性**：实验设计较为全面，不仅测试了自研的小规模模型，还对市面上的主流开源大模型（Llama, Qwen, BERT）进行了逆向信息提取测试。
*   **客观性**：通过引入 $H_r$ 指标和分布外（OOD）测试（如随机 Token 测试），验证了模型记忆的非平凡性（即不是靠简单的维度对应来存储信息）。

### 6. 主要结论与发现
*   **因果模型记忆匮乏**：无论模型规模多大，仅靠下个词预测训练的模型，其嵌入保留的输入信息极少（Token 准确率通常低于 10%）。
*   **自编码器近乎完美**：专门为重建输入训练的自编码器可以实现近乎 100% 的信息保留。
*   **不可逆性是根源**：下个词预测任务的“多对一”性质导致了信息的丢失，证明了单一目标函数不足以形成准确记忆。
*   **组合目标的有效性**：通过组合损失函数和课程学习，记忆模型可以在保持因果预测能力的同时，显著提升对输入信息的检索和利用能力。

### 7. 优点
*   **理论深刻**：从函数映射的可逆性角度解释了为什么现有 LLM 难以处理长上下文检索任务。
*   **效率提升**：提出的并行编码器架构在理论上和实验中都展示了比全上下文 Transformer 更高的计算效率。
*   **量化严谨**：提出了 $H_r$ 这一标准化的信息保留度量方法，便于跨模型比较。

### 8. 不足与局限
*   **架构兼容性**：研究发现 Mixer 编码器与 Transformer 解码器之间存在流形不匹配（Manifold Mismatch），导致混合架构训练效率低下。
*   **性能权衡**：在引入强记忆目标后，模型在某些通用理解基准（如 MMLU）上的表现会有轻微下降。
*   **规模限制**：虽然测试了 1b 规模的模型，但在超大规模参数（如 70b+）上的表现和训练稳定性尚待验证。

（完）
