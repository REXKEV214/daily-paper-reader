---
title: Recurrent Preference Memory for Efficient Long-Sequence Generative Recommendation
title_zh: 用于高效长序列生成式推荐的循环偏好记忆
authors: "Yixiao Chen, Yuan Wang, Yue Liu, Qiyao Wang, Ke Cheng, Xin Xu, Juntong Yan, Shuojin Yang, Menghao Guo, Jun Zhang, Huan Yu, Jie Jiang"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.11605v2"
tags: ["query:llm-memory"]
score: 9.0
evidence: 将长用户交互历史压缩为偏好记忆令牌
tldr: 针对生成式推荐在处理长序列时面临的计算开销大和噪声累积问题，本文提出了Rec2PM框架。该框架将长交互历史压缩为紧凑的偏好记忆标记，并采用创新的自参考教师强制策略实现全并行训练。Rec2PM在推理时支持迭代更新，且通过将记忆表示为标记嵌入而非庞大的KV缓存，极大地提升了存储效率。实验表明，该方法在显著降低延迟和内存占用的同时，通过信息瓶颈效应过滤噪声，实现了优于全序列模型的推荐精度。
selection_source: fresh_fetch
motivation: 生成式推荐模型在处理终身用户行为序列时，受限于全注意力机制带来的高昂计算成本和随机交互产生的噪声累积。
method: 引入Rec2PM框架，通过自参考教师强制策略将长历史压缩为偏好记忆标记，支持并行训练与高效的推理更新。
result: 在大规模基准测试中，Rec2PM显著降低了推理延迟和内存占用，且推荐准确率超越了传统全序列模型。
conclusion: 偏好记忆机制作为去噪信息瓶颈，能有效过滤交互噪声并捕捉稳健的用户长期兴趣，是实现高效长序列推荐的有效方案。
---

## 摘要
生成式推荐 (GenRec) 模型通常通过全注意力机制对用户行为进行建模，但由于极高的计算成本和随机交互产生的噪声积累，难以扩展到终身序列。为了应对这些挑战，我们提出了 Rec2PM，这是一个将长用户交互历史压缩为紧凑偏好记忆 (Preference Memory) token 的框架。与受限于串行训练的传统循环方法不同，Rec2PM 采用了一种新颖的自引用教师强制 (self-referential teacher-forcing) 策略：它利用历史的全局视图生成参考记忆，并将其作为并行化循环更新的监督目标。这使得模型在保持推理过程中迭代更新能力的同时，能够实现完全并行的训练。此外，通过将记忆表示为 token 嵌入而非庞大的 KV 缓存，Rec2PM 实现了极高的存储效率。在大规模基准测试上的实验表明，与全序列模型相比，Rec2PM 在显著降低推理延迟和内存占用的同时，实现了更优的准确率。分析表明，偏好记忆起到了去噪信息瓶颈 (Information Bottleneck) 的作用，有效地过滤了交互噪声，从而捕捉到稳健的长期兴趣。

## Abstract
Generative recommendation (GenRec) models typically model user behavior via full attention, but scaling to lifelong sequences is hindered by prohibitive computational costs and noise accumulation from stochastic interactions. To address these challenges, we introduce Rec2PM, a framework that compresses long user interaction histories into compact Preference Memory tokens. Unlike traditional recurrent methods that suffer from serial training, Rec2PM employs a novel self-referential teacher-forcing strategy: it leverages a global view of the history to generate reference memories, which serve as supervision targets for parallelized recurrent updates. This allows for fully parallel training while maintaining the capability for iterative updates during inference. Additionally, by representing memory as token embeddings rather than extensive KV caches, Rec2PM achieves extreme storage efficiency. Experiments on large-scale benchmarks show that Rec2PM significantly reduces inference latency and memory footprint while achieving superior accuracy compared to full-sequence models. Analysis reveals that the Preference Memory functions as a denoising Information Bottleneck, effectively filtering interaction noise to capture robust long-term interests.

---

## 论文详细总结（自动生成）

这是一份关于论文《Recurrent Preference Memory for Efficient Long-Sequence Generative Recommendation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
生成式推荐（GenRec）目前面临将用户建模扩展到“终身行为序列”的瓶颈：
*   **计算开销巨大**：标准 Transformer 的全注意力机制具有二次方复杂度（$O(L^2)$），处理数千甚至上万个 Token 的长序列在工业级实时推理中几乎不可行。
*   **噪声累积**：用户行为具有随机性（如误点击），直接对原始长历史进行全量注意力计算会放大噪声，干扰模型对用户核心偏好的捕捉。
*   **存储与训练矛盾**：现有的上下文压缩方案要么需要存储庞大的每层 KV 缓存（存储成本高），要么采用循环更新（训练时无法并行，效率极低）。

### 2. 方法论：Rec2PM 框架
论文提出了 **Rec2PM**（循环偏好记忆推荐），核心思想是将长历史压缩为极少数的“偏好记忆 Token”。

*   **三位一体记忆机制**：
    1.  **工作记忆 (Working Memory)**：近期原始交互，保留高保真细节。
    2.  **偏好记忆 (Preference Memory)**：压缩后的 Token 嵌入，代表长期兴趣。
    3.  **参数记忆 (Parametric Memory)**：模型权重，负责编码和解码。
*   **核心技术：自参考教师强制 (Self-referential Teacher-forcing)**：
    为了解决循环模型训练慢的问题，Rec2PM 采用两阶段并行训练：
    *   **第一阶段（全局参考生成）**：模型在全局视图下直接观察原始历史，生成“参考记忆”（Reference Memory）。
    *   **第二阶段（并行预测与更新）**：将序列切段，利用第一阶段生成的参考记忆作为“教师信号”，监督当前段的循环更新过程。
*   **优化目标**：
    $$L = L_{AR} + \lambda L_{con}$$
    其中 $L_{AR}$ 是标准的自回归预测损失，$L_{con}$ 是更新后的记忆与参考记忆之间的均方误差（MSE）一致性损失。

### 3. 实验设计
*   **数据集**：
    *   **MerRec**：大规模 C2C 平台公开基准，筛选出至少包含 1003 条交互的长序列用户。
    *   **工业数据集**：来自某大型短视频平台的私有数据，包含 5 亿用户、5.5 亿物品和超过 5000 亿次交互。
*   **对比方法 (Baselines)**：
    *   **Short/Full**：不带记忆的截断序列和全序列模型。
    *   **Tok-Serial-O/A**：采用串行训练的 Token 记忆模型（覆盖/追加模式）。
    *   **KV-Mask-O/A**：基于 KV 缓存掩码并行的模型（如 PersRec, Anchor）。
*   **评估指标**：Hit Rate (H@K) 和 NDCG (N@K)。

### 4. 资源与算力
*   **硬件**：推理延迟测试在 **NVIDIA H20 GPU** 上进行（Batch Size 64）。
*   **训练细节**：学习率 $10^{-3}$，Batch Size 8，权重衰减 0.1，使用早停机制。
*   **算力说明**：文中未明确给出训练总时长或使用的 GPU 总数，但强调了其方法比串行训练（Tok-Serial）和全序列模型（Full）具有更高的训练和推理效率。

### 5. 实验数量与充分性
*   **实验规模**：在公开数据集和 5000 亿级工业数据上均进行了验证，实验规模极大。
*   **消融实验**：针对一致性损失 $\lambda$、记忆槽位数量 $C$（1 到 16）、更新模式（覆盖 vs 追加）进行了详细测试。
*   **客观性**：对比了多种最先进的压缩方案（Token-based 和 KV-based），并提供了推理延迟与存储空间的量化对比，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **去噪瓶颈效应**：Rec2PM 的表现不仅优于截断模型，在某些情况下甚至**优于全序列模型**。这证明了偏好记忆起到了“信息瓶颈”作用，过滤了长历史中的随机噪声。
*   **存储极度高效**：相比于存储 KV 缓存的方法，Rec2PM 将每用户存储需求从 32KB-128KB 降低到了 **1KB**。
*   **推理加速显著**：在保持高精度的前提下，推理延迟比全序列模型降低了约 **92%**。
*   **覆盖优于追加**：实验发现“覆盖模式”通常优于“追加模式”，因为前者维持了更严格的信息瓶颈，避免了陈旧噪声的堆积。

### 7. 优点
*   **训练并行化**：通过自参考教师强制策略，巧妙解决了循环神经网络式记忆无法并行训练的顽疾。
*   **理论支撑强**：利用信息瓶颈（Information Bottleneck）理论解释了为何压缩后的记忆反而比原始全序列效果更好。
*   **工业落地性强**：极低的存储占用和推理延迟使其非常适合部署在拥有亿级用户的真实推荐系统中。
*   **可视化深入**：通过注意力权重可视化，展示了记忆 Token 如何自发分化为“时间专家”和“领域专家”。

### 8. 不足与局限
*   **应用限制**：目前主要针对 ID 类序列推荐，对于包含丰富文本或多模态信息的生成式推荐场景，压缩过程中的信息损失可能更难控制。
*   **超参数敏感性**：虽然实验做了槽位数量测试，但在不同业务场景下，如何自动确定最佳的记忆槽位数量和压缩比例仍需人工调优。
*   **偏差风险**：长期记忆的引入可能导致模型过于关注用户的历史偏好，从而在一定程度上加剧“信息茧房”效应，论文未对此多样性指标进行深入探讨。

（完）
