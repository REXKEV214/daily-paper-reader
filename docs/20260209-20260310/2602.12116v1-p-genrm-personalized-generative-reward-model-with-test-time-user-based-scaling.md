---
title: "P-GenRM: Personalized Generative Reward Model with Test-time User-based Scaling"
title_zh: P-GenRM：具有测试时基于用户缩放的个性化生成式奖励模型
authors: "Pinyi Zhang, Ting-En Lin, Yuchuan Wu, Jingyang Chen, Zongqi Wang, Hua Yang, Ze Xu, Fei Huang, Kai Zhang, Yongbin Li"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12116v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 针对用户偏好的个性化生成式奖励模型
tldr: 本研究针对大语言模型个性化对齐中偏好建模过于简化及新用户泛化难的问题，提出了首个带测试时用户缩放的个性化生成式奖励模型P-GenRM。该模型将偏好信号转化为结构化评估链，生成自适应画像与评分准则，并利用用户原型聚类实现双粒度缩放机制。实验表明，P-GenRM在多个基准上达到SOTA，显著提升了模型在开放场景下的个性化对齐能力与泛化表现。
selection_source: fresh_fetch
motivation: 现有奖励模型往往将复杂的场景偏好简化为固定准则，且在反馈有限时难以有效泛化至新用户。
method: 提出P-GenRM模型，通过生成结构化评估链构建动态画像，并结合用户原型进行个体与群体层面的双粒度测试时缩放。
result: "在主流个性化奖励模型基准上取得SOTA结果，平均提升2.31%，且测试时缩放机制带来了额外的3%性能增长。"
conclusion: P-GenRM通过结构化生成与原型迁移机制，有效解决了个性化对齐中的偏好建模精度与跨用户泛化难题。
---

## 摘要
大语言模型的个性化对齐旨在使响应适应个人用户的偏好，通常通过强化学习实现。一个关键挑战是在开放式场景中获取准确的、针对特定用户的奖励信号。现有的个性化奖励模型面临两个持久的局限性：(1) 将多样化的、特定场景的偏好过度简化为一小组固定的评估原则；(2) 在反馈有限的情况下，难以泛化到新用户。为此，我们提出了 P-GenRM，这是首个具有测试时基于用户缩放的个性化生成式奖励模型。P-GenRM 将偏好信号转化为结构化的评估链，从而在各种场景中推导出自适应的角色画像和评分细则。它进一步将用户聚类为用户原型，并引入了一种双粒度缩放机制：在个人层面，它自适应地缩放并聚合每个用户的评分方案；在原型层面，它整合了相似用户的偏好。这种设计减轻了推断偏好中的噪声，并通过基于原型的迁移增强了对未见用户的泛化能力。实验结果表明，P-GenRM 在广泛使用的个性化奖励模型基准测试中取得了最先进的结果，平均提升了 2.31%，并在分布外数据集上展示了强大的泛化能力。值得注意的是，测试时基于用户的缩放提供了额外的 3% 提升，证明了具有测试时可扩展性的更强个性化对齐。

## 速览
**TLDR**：本研究针对大语言模型个性化对齐中偏好建模过于简化及新用户泛化难的问题，提出了首个带测试时用户缩放的个性化生成式奖励模型P-GenRM。该模型将偏好信号转化为结构化评估链，生成自适应画像与评分准则，并利用用户原型聚类实现双粒度缩放机制。实验表明，P-GenRM在多个基准上达到SOTA，显著提升了模型在开放场景下的个性化对齐能力与泛化表现。 \
**Motivation**：现有奖励模型往往将复杂的场景偏好简化为固定准则，且在反馈有限时难以有效泛化至新用户。 \
**Method**：提出P-GenRM模型，通过生成结构化评估链构建动态画像，并结合用户原型进行个体与群体层面的双粒度测试时缩放。 \
**Result**：在主流个性化奖励模型基准上取得SOTA结果，平均提升2.31%，且测试时缩放机制带来了额外的3%性能增长。 \
**Conclusion**：P-GenRM通过结构化生成与原型迁移机制，有效解决了个性化对齐中的偏好建模精度与跨用户泛化难题。

---

## Abstract
Personalized alignment of large language models seeks to adapt responses to individual user preferences, typically via reinforcement learning. A key challenge is obtaining accurate, user-specific reward signals in open-ended scenarios. Existing personalized reward models face two persistent limitations: (1) oversimplifying diverse, scenario-specific preferences into a small, fixed set of evaluation principles, and (2) struggling with generalization to new users with limited feedback. To this end, we propose P-GenRM, the first Personalized Generative Reward Model with test-time user-based scaling. P-GenRM transforms preference signals into structured evaluation chains that derive adaptive personas and scoring rubrics across various scenarios. It further clusters users into User Prototypes and introduces a dual-granularity scaling mechanism: at the individual level, it adaptively scales and aggregates each user's scoring scheme; at the prototype level, it incorporates preferences from similar users. This design mitigates noise in inferred preferences and enhances generalization to unseen users through prototype-based transfer. Empirical results show that P-GenRM achieves state-of-the-art results on widely-used personalized reward model benchmarks, with an average improvement of 2.31%, and demonstrates strong generalization on an out-of-distribution dataset. Notably, Test-time User-based scaling provides an additional 3% boost, demonstrating stronger personalized alignment with test-time scalability.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴 Qwen-Character 团队提出，介绍了一种名为 **P-GenRM** 的个性化生成式奖励模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的个性化对齐旨在让模型响应符合不同用户的特定偏好。然而，现有的个性化奖励模型（RM）存在两大瓶颈：
    1.  **静态建模局限**：将复杂、随场景变化的偏好简化为固定的评估准则，无法捕捉用户在不同情境下的偏好漂移。
    2.  **泛化能力弱**：在面对反馈稀疏的新用户（冷启动）时，模型难以有效推断其偏好。
*   **研究背景**：传统的对齐（如 RLHF）通常追求普适价值（如诚实、无害），但在开放式对话中，用户的主观偏好（如简洁 vs. 详尽）才是决定满意度的关键。

### 2. 方法论：P-GenRM 核心架构
P-GenRM 是首个将偏好信号转化为**结构化评估链（Structured Evaluation Chains）**的生成式奖励模型，其核心流程如下：

*   **三阶段训练框架**：
    1.  **画像引导评分诱导（PSI, SFT阶段）**：利用用户历史信号和显式准则，训练模型生成包含“用户画像分析”和“场景化评分细则”的推理链，最后给出分数。
    2.  **基于准则的推理增强（CRE, RL阶段）**：采用 **GRPO** 算法，引入过程奖励（PR，评估推理链对偏好的覆盖度）和结果奖励（OR，评分准确性），强化模型在缺乏显式反馈时的推理能力。
    3.  **硬负样本感知课程学习（RL阶段）**：通过逐渐增加负样本难度，提升模型处理主观模糊边界的鲁棒性。
*   **测试时基于用户的缩放（Test-time User-based Scaling）**：
    1.  **用户原型（User Prototypes）**：通过 K-means 对用户偏好嵌入进行聚类，并利用历史感知注意力机制对原型进行精炼。
    2.  **双粒度缩放**：
        *   **个体层级**：对当前用户进行多次采样并行生成评分方案并聚合。
        *   **原型层级**：引入所属原型中相似用户的偏好信号进行协同评分。

### 3. 实验设计
*   **数据集与场景**：
    *   **PersonalRewardBench**：包含 Chatbot Arena-personalized（131名用户）和 Prism-personalized（720名用户）。
    *   **LaMP-QA**：用于评估 OOD（分布外）场景和冷启动（稀疏反馈）下的泛化能力。
*   **对比方法（Baselines）**：
    *   **Prompting 类**：In-Context LLM-as-a-Judge（含 CoT、画像引导等）。
    *   **微调 RM 类**：传统的 Bradley-Terry RM。
    *   **个性化 RM 类**：GPO、VPL、PAL 以及 SOTA 方法 SynthesizeMe。
    *   **闭源模型**：OpenAI o3。

### 4. 资源与算力
*   **模型规模**：基于 Llama-3.1-8B 和 Llama-3.1-70B。
*   **算力配置**：
    *   **8B 模型**：使用 8 张 NVIDIA A100 GPU。
    *   **70B 模型**：使用 32 张 NVIDIA A100 GPU，并采用 LoRA 微调。
*   **其他工具**：使用 Qwen3-Embedding-0.6B 进行原型聚类，使用 OpenAI o3 生成 SFT 阶段的初始推理链数据。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准上进行了详尽测试，涵盖了从 8B 到 70B 的不同参数量级。
*   **消融实验**：针对三阶段训练（SFT、RL、课程学习）以及过程奖励/结果奖励分别做了消融，验证了各模块的必要性。
*   **公平性与客观性**：
    *   引入了 **Macro Accuracy（宏观准确率）** 评估，确保模型在长尾（少数群体）用户原型上同样有效，避免过拟合多数群体。
    *   进行了 5 次独立运行并报告标准差，确保结果的统计显著性。
    *   通过 LaMP-QA 验证了模型在未见数据集上的迁移能力。

### 6. 主要结论与发现
*   **性能领先**：P-GenRM 在所有基准上均达到 SOTA，8B 版本的表现甚至超过了部分 70B 的基线模型。
*   **缩放效应**：测试时缩放机制（Individual + Prototype）带来了约 **3%** 的额外性能提升，证明了生成式 RM 的可扩展性。
*   **冷启动优势**：在 LaMP-QA 的稀疏反馈实验中，P-GenRM 表现优于参数量大得多的模型（如 Qwen3-235B），证明了原型迁移机制的有效性。
*   **下游增益**：使用 P-GenRM 训练的策略模型（Policy Model）在个性化质量上显著优于未对齐的 70B 模型。

### 7. 优点与亮点
*   **可解释性**：不同于输出标量分数的传统 RM，P-GenRM 生成详细的评估链，让用户了解模型为何认为某个回答更好。
*   **动态适应**：通过 PSI 机制，模型能根据当前 Query 动态调整画像和权重，解决了静态偏好建模的问题。
*   **测试时计算（Test-time Compute）**：巧妙利用了生成式模型的特性，通过增加推理时的计算量（采样与原型聚合）来换取更高的准确率。

### 8. 不足与局限
*   **推理延迟**：由于需要生成长文本推理链（CoT），其推理速度慢于直接输出分数的标量 RM。
*   **数据依赖**：模型性能高度依赖于至少 3 条以上的历史偏好记录，对于完全零样本的新用户，提升幅度受限。
*   **计算成本**：测试时缩放虽然提升了效果，但也成倍增加了推理阶段的 Token 消耗和算力成本。

（完）
