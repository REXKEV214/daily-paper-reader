---
title: "Neuromem: A Granular Decomposition of the Streaming Lifecycle in External Memory for LLMs"
title_zh: Neuromem：大语言模型外部存储流式生命周期的细粒度分解
authors: "Ruicheng Zhang, Xinyi Li, Tianyi Xu, Shuhao Zhang, Xiaofei Liao, Hai Jin"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.13967v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 大语言模型外部记忆生命周期
tldr: 本研究针对大语言模型外部存储模块在实际应用中呈现的流式特征，提出了 Neuromem 测试平台。不同于传统的静态评估，Neuromem 模拟了插入与检索交替进行的动态过程，并将存储生命周期细分为数据结构、整合策略等五个关键维度。通过在多个数据集上的实验，揭示了存储增长对性能的负面影响以及不同技术方案在成本与准确率之间的权衡，为构建高效流式存储系统提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的外部存储评估多基于静态设置，忽视了实际应用中数据持续流入、插入与检索交替进行的流式动态特征。
method: 提出 Neuromem 测试平台，将存储生命周期分解为数据结构、归一化、整合策略、查询构建和上下文集成五个维度进行系统化评估。
result: 实验发现性能随存储规模增加而普遍下降，时间相关查询最具挑战性，且数据结构很大程度上决定了系统能达到的质量上限。
conclusion: 存储系统的优化应关注流式场景下的长期稳定性，复杂的压缩和集成机制往往只是在插入与检索成本间做权衡，对准确率提升有限。
---

## 摘要
大多数外部存储模块（External Memory Module）的评估都假设处于静态设置：存储是离线构建的，并在固定状态下进行查询。在实践中，存储是流式的：新事实不断到达，插入与检索交替进行，且存储状态在模型提供查询服务时不断演变。在这种模式下，准确性和成本受整个存储生命周期的支配，该周期涵盖了信息的摄取、维护、检索以及将其整合到生成过程中的环节。我们提出了 Neuromem，这是一个可扩展的测试平台，用于在交替插入与检索协议下对外部存储模块进行基准测试，并将其生命周期分解为五个维度，包括存储数据结构、归一化策略、整合策略、查询制定策略和上下文集成机制。利用 LOCOMO、LONGMEMEVAL 和 MEMORYAGENTBENCH 三个代表性数据集，Neuromem 在共享的服务栈中评估了可互换的变体，并报告了 token 级 F1 分数以及插入/检索延迟。总的来说，我们观察到性能通常随着存储在多轮中的增长而下降，且与时间相关的查询仍然是最具挑战性的类别。存储数据结构在很大程度上决定了可达到的质量前沿，而激进的压缩和生成式集成机制主要是在插入和检索之间转移成本，准确率提升有限。

## Abstract
Most evaluations of External Memory Module assume a static setting: memory is built offline and queried at a fixed state. In practice, memory is streaming: new facts arrive continuously, insertions interleave with retrievals, and the memory state evolves while the model is serving queries. In this regime, accuracy and cost are governed by the full memory lifecycle, which encompasses the ingestion, maintenance, retrieval, and integration of information into generation. We present Neuromem, a scalable testbed that benchmarks External Memory Modules under an interleaved insertion-and-retrieval protocol and decomposes its lifecycle into five dimensions including memory data structure, normalization strategy, consolidation policy, query formulation strategy, and context integration mechanism. Using three representative datasets LOCOMO, LONGMEMEVAL, and MEMORYAGENTBENCH, Neuromem evaluates interchangeable variants within a shared serving stack, reporting token-level F1 and insertion/retrieval latency. Overall, we observe that performance typically degrades as memory grows across rounds, and time-related queries remain the most challenging category. The memory data structure largely determines the attainable quality frontier, while aggressive compression and generative integration mechanisms mostly shift cost between insertion and retrieval with limited accuracy gain.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neuromem: A Granular Decomposition of the Streaming Lifecycle in External Memory for LLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的外部存储模块（External Memory Modules）评估大多基于“静态”设置，即先离线构建存储，再在固定状态下查询。然而，实际应用中的存储是**流式（Streaming）**的：新事实不断涌入，插入与检索操作交替进行，存储状态随时间演变。
*   **核心问题**：在流式场景下，存储系统的不同设计决策如何影响准确率与延迟？目前缺乏一个能对存储生命周期进行细粒度分解并量化各阶段成本与收益的统一测试框架。

### 2. 论文提出的方法论
论文提出了 **Neuromem**，一个可扩展的流式存储测试平台，其核心思想是将外部存储的生命周期分解为五个正交的设计维度（D1–D5）：
*   **D1：存储数据结构 (Memory Data Structure)**：存储的物理组织方式，如分区式（向量库、队列）或层次式（知识图谱）。
*   **D2：归一化策略 (Normalization)**：将原始上下文转化为可存储单元的预处理过程（如提取三元组、摘要或保留原文）。
*   **D3：整合策略 (Consolidation)**：维护存储状态的策略，包括冲突解决、衰减淘汰（如遗忘曲线）和结构细化。
*   **D4：查询构建策略 (Query Formulation)**：将用户意图转化为检索信号，如关键词提取、查询分解或直接检索。
*   **D5：上下文集成机制 (Context Integration)**：将检索到的证据合成最终上下文，包括重排序、过滤或多查询融合。

**算法流程**：Neuromem 采用**交替插入与检索协议**。系统严格按时间戳顺序处理请求流：遇到插入请求时更新存储状态（D1-D3），遇到检索请求时基于当前状态生成响应（D4-D5），确保评估过程符合因果律且无未来信息泄露。

### 3. 实验设计
*   **数据集/场景**：
    *   **LOCOMO**：长程对话信息综合。
    *   **LONGMEMEVAL**：多会话聊天中的召回一致性。
    *   **MEMORY AGENT BENCH**：动态事实更新与选择性遗忘。
*   **Benchmark 与对比方法**：
    *   将 12 种代表性系统（如 MemGPT, HippoRAG, MemoryBank, Mem0, TIM 等）拆解并映射到 D1-D5 维度。
    *   通过在统一的服务栈中更换不同维度的组件（如将归一化策略从“保留原文”改为“提取三元组”）进行消融实验。
*   **评估指标**：Token 级 F1 分数（准确性）、插入延迟、检索延迟。

### 4. 资源与算力
*   **硬件环境**：
    *   **华为 Ascend 910B NPU**：用于运行 Pangu-1B 嵌入式模型。
    *   **Nvidia RTX A6000 GPU**：用于运行 Llama-3.1-8B-Instruct 模型。
*   **软件环境**：Python 3.11，使用 BGE-M3 和 E5-Large 作为嵌入模型。
*   **说明**：论文未明确给出总训练/实验时长，但详细记录了各操作的毫秒级延迟。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同性质的数据集上进行了多轮流式评估（通常分为 5 轮以观察性能演变）。
*   **充分性与公平性**：
    *   **充分**：针对 D1-D5 每个维度都进行了详细的消融实验，并跨模型（Pangu, Llama-3, Qwen）进行了鲁棒性验证。
    *   **客观公平**：所有变体都在**统一的服务栈**下运行，消除了系统实现差异带来的干扰，能够精准归因性能增益。

### 6. 主要结论与发现
*   **[F1] 时间熵不可避免**：随着交互历史增加，所有存储结构的性能都会普遍下降（约 13%-44%），且昂贵的 LLM 维护策略无法有效阻止这种衰减。
*   **[F2] 数据结构决定上限**：D1（数据结构）决定了准确率的天花板。混合索引（向量+倒排）通常优于单一结构。
*   **[F3] 语义压缩是有损的**：将对话重写为结构化三元组（Rewrite）会导致性能崩溃（F1 下降 50% 以上），保留原始文本纹理效果更好。
*   **[F4] 生成式优化存在“延迟税”**：在检索阶段使用查询分解或多查询扩展会使延迟增加一个数量级，但准确率提升微乎其微。
*   **[F5] 启发式方法更优**：在流式约束下，简单的启发式策略（如基于热度的迁移、遗忘曲线）在效率和效果的平衡上优于复杂的生成式维护。

### 7. 优点
*   **视角独特**：首次系统性地定义并分解了外部存储的“流式生命周期”，填补了静态评估与实际应用之间的鸿沟。
*   **细粒度归因**：通过算子级拆解，能够清晰地指出是哪个环节（如归一化还是集成）导致了延迟增加或精度损失。
*   **实用性强**：为开发者提供了明确的设计准则，例如警惕过度压缩和昂贵的生成式检索增强。

### 8. 不足与局限
*   **图存储扩展性问题**：由于 HippoRAG 等图算法在流式更新时延迟呈二次方增长，实验中不得不排除了一些复杂的迭代图细化范式。
*   **数据集限制**：缺乏专门为动态存储演变设计的细粒度数据集，导致无法监控存储内部状态的中间变化，只能依赖端到端的 F1 指标。
*   **短期与长期记忆界限模糊**：目前尚未能很好地形式化区分短期工作记忆与长程存储，限制了混合整合策略的深入研究。

（完）
