---
title: Differentially Private Retrieval-Augmented Generation
title_zh: 差分隐私检索增强生成
authors: "Tingting Tang, James Flemings, Yongqin Wang, Murali Annavaram"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14374v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 针对敏感数据的隐私保护检索增强生成
tldr: 针对检索增强生成（RAG）在处理敏感数据时可能泄露隐私的问题，本文提出了一种名为 DP-KSA 的差分隐私 RAG 算法。该方法基于“大多数问答可通过关键词回答”的观察，利用 Propose-Test-Release 范式，通过聚合多个检索上下文生成的响应并以差分隐私方式提取高频关键词，在保护隐私的同时有效降低了幻觉风险，实现了隐私与实用性的平衡。
selection_source: fresh_fetch
motivation: 传统的 RAG 系统在处理敏感语料时存在隐私泄露风险，而直接应用差分隐私往往会导致模型实用性大幅下降并增加幻觉。
method: 提出 DP-KSA 算法，通过聚合多个检索结果生成的响应，并利用差分隐私机制提取核心关键词来增强最终的生成提示词。
result: 在两个问答基准测试和三种指令微调模型上的实验表明，DP-KSA 在提供强隐私保证的同时，实现了优异的隐私与实用性权衡。
conclusion: DP-KSA 证明了通过压缩语义空间并结合差分隐私技术，可以在保护 RAG 数据库隐私的同时有效抑制模型幻觉并保持生成质量。
---

## 摘要
检索增强生成（RAG）是一种广泛使用的框架，通过从数据库中检索相关文档来支持准确的响应，从而减少大型语言模型（LLM）在特定领域任务中的幻觉。然而，当数据库包含敏感语料库（如医疗记录或法律文件）时，RAG 可能会通过其输出泄露隐私信息，从而带来严重的隐私风险。先前的研究表明，人们实际上可以设计对抗性提示，迫使 LLM 复现增强的上下文。一个有前景的方向是将差分隐私（DP）集成到 RAG 系统中，这是一种提供强大形式化保证的隐私概念。然而，在现有系统中盲目应用 DP 机制通常会导致显著的效用下降。特别是对于 RAG 系统，DP 可能会降低增强上下文的有用性，从而增加 LLM 产生幻觉的风险。针对这些挑战，我们提出了 DP-KSA，这是一种新型的隐私保护 RAG 算法，它利用“提议-测试-发布”（propose-test-release）范式集成了 DP。DP-KSA 基于一个关键观察，即大多数问答（QA）查询可以通过几个关键词得到充分回答。因此，DP-KSA 首先获取相关上下文的集合，其中每一个都将用于生成 LLM 的响应。我们利用这些响应，以差分隐私的方式获取最频繁的关键词。最后，将这些关键词增强到提示中以生成最终输出。这种方法在保留效用和隐私的同时，有效地压缩了语义空间。我们从形式上证明了 DP-KSA 在生成的输出上针对 RAG 数据库提供了形式化的 DP 保证。我们在两个 QA 基准测试上使用三个经过指令微调的 LLM 对 DP-KSA 进行了评估，实验结果表明 DP-KSA 实现了强大的隐私-效用权衡。

## Abstract
Retrieval-augmented generation (RAG) is a widely used framework for reducing hallucinations in large language models (LLMs) on domain-specific tasks by retrieving relevant documents from a database to support accurate responses. However, when the database contains sensitive corpora, such as medical records or legal documents, RAG poses serious privacy risks by potentially exposing private information through its outputs. Prior work has demonstrated that one can practically craft adversarial prompts that force an LLM to regurgitate the augmented contexts. A promising direction is to integrate differential privacy (DP), a privacy notion that offers strong formal guarantees, into RAG systems. However, naively applying DP mechanisms into existing systems often leads to significant utility degradation. Particularly for RAG systems, DP can reduce the usefulness of the augmented contexts leading to increase risk of hallucination from the LLMs. Motivated by these challenges, we present DP-KSA, a novel privacy-preserving RAG algorithm that integrates DP using the propose-test-release paradigm. DP-KSA follows from a key observation that most question-answering (QA) queries can be sufficiently answered with a few keywords. Hence, DP-KSA first obtains an ensemble of relevant contexts, each of which will be used to generate a response from an LLM. We utilize these responses to obtain the most frequent keywords in a differentially private manner. Lastly, the keywords are augmented into the prompt for the final output. This approach effectively compresses the semantic space while preserving both utility and privacy. We formally show that DP-KSA provides formal DP guarantees on the generated output with respect to the RAG database. We evaluate DP-KSA on two QA benchmarks using three instruction-tuned LLMs, and our empirical results demonstrate that DP-KSA achieves a strong privacy-utility tradeoff.

---

## 论文详细总结（自动生成）

### 论文总结：差分隐私检索增强生成 (DP-KSA)

#### 1. 核心问题与整体含义（研究动机和背景）
检索增强生成（RAG）通过引入外部知识库有效缓解了大型语言模型（LLM）的“幻觉”问题。然而，当外部数据库包含敏感信息（如医疗记录、法律文档）时，RAG 面临严重的隐私泄露风险——攻击者可以通过精心设计的提示词诱导模型直接输出检索到的私密原文。
虽然**差分隐私（Differential Privacy, DP）**是解决此类问题的标准数学框架，但直接在文本生成过程中添加 DP 噪声通常会导致生成内容逻辑混乱、效用（Utility）大幅下降，甚至反而诱发更多幻觉。本文的核心挑战在于：**如何在为 RAG 系统提供严格隐私保证的同时，维持其在问答任务中的实用性？**

#### 2. 方法论：核心思想与算法流程
论文提出了 **DP-KSA** 算法，其核心思想是基于一个关键观察：**大多数问答任务的答案可以由少数几个核心关键词（Keywords）决定**。通过在“关键词空间”而非“全文本空间”应用 DP，可以显著降低噪声对语义的影响。

**关键技术流程：**
1.  **检索与分区（Retrieval & Partitioning）**：针对查询 $x$，从私有数据库 $D$ 中检索前 $N$ 个相关文档。
2.  **响应集成（Ensemble Generation）**：将每个文档分别与查询配对，输入生成模型 $F$，得到 $N$ 个独立的候选响应。
3.  **DP 关键词提取（DP Keyword Extraction）**：
    *   统计 $N$ 个响应中所有 Token 的出现频率，构建直方图。
    *   **FindBestK**：利用指数机制（Exponential Mechanism）以 DP 方式自适应选择最优的关键词数量 $\hat{k}$。
    *   **TopKWithPTR**：采用“提议-测试-发布”（Propose-Test-Release, PTR）范式。只有当第 $k$ 个和第 $k+1$ 个关键词的频次差距足够大（超过噪声阈值）时，才发布这些关键词。这确保了输出的关键词对单个文档的变动不敏感。
4.  **最终推理（Zero-shot Inference）**：将提取出的私密保护关键词与原始查询 $x$ 组合，再次输入 LLM 生成最终的自然语言答案。

#### 3. 实验设计
*   **数据集**：Natural Questions (NQ) 和 Trivia Question Answering (TQA)，均使用维基百科作为外部检索语料库。
*   **生成模型（Generator）**：Qwen 2.5 (3B), Llama 3.2 (3B), Llama 3.1 (8B)。
*   **对比基准（Benchmarks）**：
    *   **Non-RAG ($\epsilon=0$)**：不使用任何检索信息，仅靠模型自身知识回答。
    *   **Non-private KSA ($\epsilon=\infty$)**：使用关键词提取但不加 DP 噪声。
    *   **Standard RAG ($\epsilon=\infty$)**：传统的非隐私 RAG（直接输入前 2 个完整文档）。
*   **评估指标**：F1 分数、ROUGE-1、ROUGE-L、归一化编辑距离（Levenshtein Similarity）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总推理时长。
*   **技术细节**：提到在测试 Qwen 2.5 (32B) 模型时使用了 **4-bit 量化**技术以适应硬件内存限制。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了测试，每个数据集抽取了 100 个问题。
*   **消融实验**：
    *   测试了不同的隐私预算 $\epsilon$（从 1 到 8）。
    *   测试了不同规模的模型（Qwen 1.5B 到 32B）。
    *   测试了集成数量（Ensemble Size）从 10 到 100 的影响。
*   **充分性评价**：实验设计较为全面，涵盖了隐私-效用权衡、模型扩展性和超参数敏感性分析。虽然每组 100 个样本的规模较小（受限于计算资源），但通过多模型、多指标的对比，结果具有一定的客观性和说服力。

#### 6. 主要结论与发现
*   **隐私-效用平衡**：在 $\epsilon \ge 2$ 时，DP-KSA 的表现一致优于完全不使用检索的 Non-RAG 基准。
*   **模型规模效应**：更大的生成模型（如 8B 或 32B）对 DP 噪声的鲁棒性更强，能更好地从稀疏的关键词中推断出准确答案。
*   **集成数量的影响**：增加集成文档数量 $N$ 可以提高关键词统计的稳定性，但超过 80 个后收益递减。
*   **数据集差异**：在 TQA 这种答案更具“抽取性”的数据集上，DP-KSA 的表现甚至能接近非隐私的 RAG。

#### 7. 优点（亮点）
*   **语义压缩策略**：巧妙地将高维的文本生成问题转化为低维的关键词选择问题，极大地降低了满足 DP 所需的噪声强度。
*   **理论完备性**：基于 PTR 范式提供了严格的差分隐私形式化证明，确保了针对数据库中单个文档的隐私保护。
*   **即插即用**：不需要对 LLM 进行微调（DP-SGD），适用于现有的指令微调模型。

#### 8. 不足与局限
*   **计算开销高**：为了构建稳定的直方图，需要对每个查询进行多次（如 80 次）LLM 推理，这在实时应用中成本极高。
*   **信息丢失**：将上下文压缩为关键词可能会丢失复杂的逻辑关系或长文本中的细节，不适用于需要深度推理的复杂任务。
*   **样本量限制**：实验仅基于 100 个问题的子集，可能无法完全代表大规模生产环境中的长尾分布。
*   **关键词依赖**：如果答案无法通过简单关键词表达（如需要生成一段复杂的代码或长篇论述），该方法将失效。

（完）
