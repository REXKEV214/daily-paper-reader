---
title: Unsupervised Layer-Wise Dynamic Test Time Adaptation for LLMs
title_zh: 大语言模型的无监督逐层动态测试时自适应
authors: "Longhuan Xu, Cunjian Chen, Feng Yin"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09719v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: LLM的无监督样本特定测试时自适应
tldr: 针对大语言模型在推理阶段进行无监督样本特定测试时自适应（TTA）时存在的不稳定和过拟合问题，本文提出了一种层级动态自适应框架。该方法利用轻量级超网络，根据提示词特征和模型结构，为LoRA参数更新动态预测每层及每步的学习率。实验表明，该方法在多种数据集和模型上显著提升了TTA的稳定性和生成性能。
selection_source: fresh_fetch
motivation: 传统的无监督TTA在仅针对单个提示词进行少量梯度更新时，固定学习率容易导致模型过拟合提示词统计特性并偏离目标分布。
method: 提出层级动态TTA框架，通过超网络预测每一层、每一步的动态学习率乘数，实现对LoRA参数更新的精细化控制。
result: 实验结果显示，该方法在不同的大模型和任务中均能有效学习缩放模式，显著改善了自适应过程的稳定性与最终表现。
conclusion: 通过动态调节不同层和步骤的自适应强度，可以有效克服无监督测试时自适应的局限性，提升大模型的推理表现。
---

## 摘要
大语言模型（LLMs）的测试时自适应（TTA）在推理阶段利用部署时可用的信号来更新模型参数。本文关注一种常见但尚未得到充分探索的方案：无监督、样本特定的 TTA。在这种方案中，模型仅利用 prompt 本身独立地为每个 prompt 进行自适应，无需标准答案或外部监督。尽管这种方法很有吸引力，但使用固定、手工设定学习率的朴素无监督 TTA 可能会不稳定：更新可能会过拟合于特定 prompt 的统计特征，偏离预期的答案分布，并最终降低生成质量。这种失效模式并不令人意外，因为在这种情况下，TTA 必须在仅有的几个梯度步内适应单个 prompt，而不像标准训练那样在大型数据集和长优化周期内对更新进行平均。因此，我们提出了逐层动态测试时自适应，这是一个显式地根据 prompt 表示、LLM 结构和自适应步数来调节 TTA 强度的框架。在我们的设置中，TTA 仅更新 LoRA 参数，并由一个轻量级超网络预测每层、每步的学习率乘数，从而实现细粒度控制。在各种数据集和 LLM 上的实验一致表明，我们的方法通过学习自适应步数和 Transformer 层投影上的有效缩放模式，显著增强了 TTA，在提高稳定性的同时提供了更好的性能。

## Abstract
Test-time adaptation (TTA) for large language models (LLMs) updates model parameters at inference time using signals available at deployment. This paper focuses on a common yet under-explored regime: unsupervised, sample-specific TTA, where the model adapts independently for each prompt using only the prompt itself, without gold answers or external supervision. Although appealing, naive unsupervised TTA with a fixed, handcrafted learning rate can be unstable: updates may overfit to prompt-specific statistics, drift from the desired answer distribution, and ultimately degrade generation quality. This failure mode is not surprising, as in this case TTA must adapt to a single prompt within only a few gradient steps, unlike standard training that averages updates over large datasets and long optimization horizons. Therefore, we propose layer-wise dynamic test-time adaptation, a framework which explicitly modulates TTA strength as a function of prompt representation, LLM structure and adaptation step. In our setting, TTA updates only LoRA parameters, and a lightweight hypernetwork predicts per-layer, per-step learning-rate multipliers, enabling fine-grained control. Experiments across various datasets and LLMs consistently show that our method substantially strengthens TTA by learning effective scaling patterns over adaptation steps and transformer layer projections, improving stability while delivering better performance.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型的无监督逐层动态测试时自适应（TTA）

#### 1. 核心问题与整体含义
论文探讨了如何在大语言模型（LLM）推理阶段，仅利用当前的输入提示词（Prompt）进行**无监督、样本特定**的参数微调（Test-Time Adaptation, TTA）。
*   **研究动机**：标准训练是针对全数据集的全局优化，而特定 Prompt 可能需要模型表现出不同的特性。现有的无监督 TTA 通常使用固定的学习率，在仅有的几次梯度更新中极易出现过拟合、参数漂移或生成质量退化（不稳定）。
*   **核心目标**：通过动态调节每一层、每一个自适应步骤的学习率，实现更稳定、更有效的推理侧模型微调。

#### 2. 方法论
论文提出了 **Layer-wise Dynamic TTA** 框架，其核心组件是一个轻量级的超网络 **ScaleNet**。
*   **核心思想**：自适应强度（学习率）应当是 Prompt 特征、模型层级结构和当前优化步骤的函数。
*   **关键技术细节**：
    *   **参数更新对象**：仅更新 Transformer 注意力机制中 Query（Q）和 Value（V）投影矩阵的 **LoRA** 参数，保持预训练骨干权重冻结。
    *   **ScaleNet 输入**：拼接 Prompt 在 LLM 首层和末层的隐藏状态均值，并加入当前步数 $k$ 和总步数 $K$ 的编码。
    *   **ScaleNet 输出**：为每一层、每一步预测非负的学习率乘数（Multiplier），分别作用于 Q 和 V 的 LoRA 矩阵。
    *   **一阶近似优化**：在训练 ScaleNet 时，为了避免昂贵的二阶导数（Hessian 矩阵）计算，采用了一阶近似方法，将 Prompt 梯度视为常数进行反向传播。
    *   **流程**：输入 Prompt $\rightarrow$ 提取特征 $\rightarrow$ ScaleNet 预测缩放因子 $\rightarrow$ 执行 $K$ 步梯度下降更新 LoRA $\rightarrow$ 生成回答 $\rightarrow$ 重置 LoRA。

#### 3. 实验设计
*   **数据集/场景**：
    *   **XSum**（摘要生成）、**SQuAD**（抽取式阅读理解）、**NQ-Open**（开放域问答）。
    *   **AdaptEval**：包含领域任务（地理、医疗等）、指令遵循和逻辑推理的综合基准。
*   **Benchmark 与对比方法**：
    *   **No TTA**：原始模型直接推理。
    *   **Fixed TTA**：使用固定的全局学习率进行 TTA。
    *   **Step-wise TTA**：仅按步骤动态调整学习率，但各层统一（层无关）。
    *   **Sample-averaged TTA**：使用层级动态缩放，但移除 Prompt 相关性（取样本平均值）。
*   **模型覆盖**：Llama-3.2-3B/Instruct, Llama-3.3-70B-Instruct, Qwen3-4B/Instruct, Qwen3-32B。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到 ScaleNet 是一个两层 MLP（隐藏层维度 128），使用 AdamW 优化器，学习率为 $10^{-4}$。训练集规模约为 30,000 个样本，测试集为 300 个样本。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 70B 不同参数规模的模型，跨越了摘要、问答、推理等多种任务类型。
*   **充分性评价**：实验设计较为充分。通过对比“固定学习率”和“仅按步缩放”，证明了“逐层控制”的必要性；通过可视化学习率热图，展示了不同 Transformer 层对自适应信号的敏感度差异。实验结果在多个模型家族（Llama, Qwen）上表现出一致性，具有较好的说服力。

#### 6. 主要结论与发现
*   **稳定性提升**：动态学习率显著解决了固定学习率在多步更新时 NLL（负对数似然）飙升的问题。
*   **层间差异**：不同层对更新的贡献不同，ScaleNet 学习到了复杂的非单调缩放模式，相邻层的更新强度可能相差数个数量级。
*   **步间规律**：第一步更新通常强度最大，后续步骤迅速衰减，这解释了为什么 TTA 的收益主要集中在初始阶段。
*   **可扩展性**：该方法在 70B 规模的大模型上依然有效，且性能提升甚至优于小模型。

#### 7. 优点
*   **精细化控制**：首次将 TTA 的控制粒度细化到“层级+步级”，有效抑制了敏感层的破坏性更新。
*   **计算效率**：通过一阶近似和 LoRA 更新，在保证效果的同时降低了推理侧自适应的开销。
*   **无监督特性**：完全不依赖标准答案，适合真实的在线部署场景。

#### 8. 不足与局限
*   **任务泛化风险**：ScaleNet 在特定数据集上训练，如果测试时的 Prompt 分布与训练集差异巨大，ScaleNet 的预测可能失效。
*   **推理延迟**：虽然使用了轻量级超网络，但每条 Prompt 仍需进行多次反向传播更新参数，这会显著增加首字生成延迟（TTFT）。
*   **表示依赖**：ScaleNet 依赖于 LLM 提取的隐藏状态，对于极长文本或复杂上下文的表征能力可能存在瓶颈。

（完）
