---
title: "AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution"
title_zh: AutoSkill：通过技能自我演进实现的经验驱动型终身学习
authors: "Yutao Yang, Junsong Li, Qianjun Pan, Bihao Zhan, Yuxuan Cai, Lin Du, Jie Zhou, Kai Chen, Qin Chen, Xin Li, Bo Zhang, Liang He"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01145v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 个性化能力的终身学习
tldr: 针对大语言模型在交互中难以积累个性化能力的局限，本文提出AutoSkill框架。该框架通过从对话轨迹中自动提取、维护和演化“技能”，将零散的交互经验转化为可重用的显式知识。AutoSkill作为模型无关的插件层，支持在不重新训练的情况下动态注入相关技能，实现了智能体的终身学习与个性化能力沉淀，为构建个人数字代理提供了可扩展路径。
selection_source: fresh_fetch
motivation: 现有的LLM智能体难以从重复的交互经验中积累个性化偏好和特定需求，导致无法实现跨会话的能力进化。
method: 提出一种经验驱动的终身学习框架，通过抽象、维护和自我演化对话中的技能，并将其作为插件动态注入未来请求。
result: 实现了将短暂的交互经验转化为显式、可重用且可组合的标准化技能，且无需对底层模型进行重新训练。
conclusion: AutoSkill为开发具备终身学习能力的个性化智能体和数字替身提供了一种实用且具有扩展性的技术方案。
---

## 摘要
在实际的大语言模型（LLM）应用中，用户会反复表达稳定的偏好和需求，例如减少幻觉、遵循机构写作规范或避免过于专业的措辞，然而这类交互经验很少被整合为可重用的知识。因此，LLM 智能体往往无法在不同会话之间积累个性化能力。我们提出了 AutoSkill，这是一个经验驱动的终身学习框架，使 LLM 智能体能够从对话和交互轨迹中自动推导、维护和重用技能。AutoSkill 从用户经验中抽象出技能，支持其持续的自我演进，并在不重新训练底层模型的情况下，将相关技能动态注入到未来的请求中。作为一个与模型无关的插件层，它与现有的 LLM 兼容，并引入了一种标准化的技能表示，以便在智能体、用户和任务之间进行共享和迁移。通过这种方式，AutoSkill 将短暂的交互经验转化为显式、可重用且可组合的能力。本文描述了 AutoSkill 的动机、架构、技能生命周期和实现，并阐述了其相对于内存、检索、个性化和智能体系统等先前工作的定位。AutoSkill 为实现终身个性化智能体和个人数字代理提供了一条实用且可扩展的路径。

## Abstract
In practical LLM applications, users repeatedly express stable preferences and requirements, such as reducing hallucinations, following institutional writing conventions, or avoiding overly technical wording, yet such interaction experience is seldom consolidated into reusable knowledge. Consequently, LLM agents often fail to accumulate personalized capabilities across sessions. We present AutoSkill, an experience-driven lifelong learning framework that enables LLM agents to automatically derive, maintain, and reuse skills from dialogue and interaction traces. AutoSkill abstracts skills from user experience, supports their continual self-evolution, and dynamically injects relevant skills into future requests without retraining the underlying model. Designed as a model-agnostic plugin layer, it is compatible with existing LLMs and introduces a standardized skill representation for sharing and transfer across agents, users, and tasks. In this way, AutoSkill turns ephemeral interaction experience into explicit, reusable, and composable capabilities. This paper describes the motivation, architecture, skill lifecycle, and implementation of AutoSkill, and positions it with respect to prior work on memory, retrieval, personalization, and agentic systems. AutoSkill highlights a practical and scalable path toward lifelong personalized agents and personal digital surrogates.

---

## 论文详细总结（自动生成）

### 论文总结：AutoSkill —— 经验驱动的技能自我演进终身学习框架

#### 1. 核心问题与研究动机
*   **核心问题**：在实际应用中，用户常会重复表达稳定的偏好（如写作风格、工作流、规避幻觉等），但现有的 LLM 智能体通常将这些交互视为临时上下文或原始文本记忆，难以将其转化为可持久、可重用的“能力”。
*   **研究动机**：现有的个性化方法（如微调）成本高且难以精细控制，而基于检索的记忆（RAG）往往只处理事实片段而非行为模式。论文旨在建立一种机制，将零散的交互经验结晶为显式的、可编辑的、版本化的“技能”，实现无需重训模型的终身学习。

#### 2. 方法论：核心思想与技术细节
AutoSkill 采用**无需训练（Training-free）**的插件式架构，核心由两个耦合的循环组成：
*   **技能增强响应循环（在线）**：
    1.  **查询重写**：将用户当前输入结合历史，重写为适合检索的独立查询。
    2.  **混合检索**：结合语义向量（Dense）和关键词（BM25）匹配，从技能库中检索相关技能。
    3.  **技能注入**：将选中的技能（Top-K 且超过阈值）以 Markdown 格式注入提示词，引导模型生成符合用户习惯的响应。
*   **技能演进循环（后台/异步）**：
    1.  **技能提取**：仅基于用户侧的查询序列（避免模型自我循环），识别出具有持久性、可重用性的约束或工作流。
    2.  **技能管理决策**：通过 LLM 判定新提取的候选技能与现有库的关系，执行 **Add（新增）**、**Merge（合并）** 或 **Discard（丢弃）**。
    3.  **版本化合并**：若判定为合并，则将新观察到的约束或示例整合进现有技能，并更新版本号（如从 v0.1.0 演进至 v0.1.34）。
*   **技能表示（SKILL.md）**：每个技能包含名称、描述、执行提示词（Prompt）、触发器、标签、示例和版本号。

#### 3. 实验设计
*   **数据集**：使用 **WildChat-1M**（包含 100 万条真实用户与 ChatGPT 的交互记录）。
*   **实验场景**：筛选出轮数超过 8 轮的对话，构建了四个子集：中文 GPT-3.5、英文 GPT-3.5、中文 GPT-4、英文 GPT-4。
*   **对比与评估**：
    *   **统计分析**：分析了提取技能的数量、标签频率（如 Python、写作、格式化等）、类别分布。
    *   **案例研究**：展示了“顶级心理咨询师”、“专业文本改写”、“八字命理大师”、“小红书种草笔记”等具体技能卡的演进过程。
    *   **系统验证**：通过 Web UI 和 OpenAI 兼容代理验证了框架的实际部署能力。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或训练时长。
*   **原因**：该框架是**无需训练（Training-free）**的，主要依赖于现有的 LLM API（如 GPT-4, GPT-3.5）进行推理、提取和合并操作。其核心开销在于 API 调用次数和向量数据库的检索延迟，而非模型训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：处理了超过 2 万场真实对话，提取了约 1858 个显式技能。
*   **充分性评价**：
    *   **广度**：涵盖了中英双语及不同能力的模型家族，证明了框架的通用性。
    *   **深度**：通过版本演进数据（如某个技能经历了 34 次迭代）证明了“自我演进”机制的有效性。
    *   **局限性**：实验侧重于**系统性展示和语料库统计**，缺乏与传统记忆系统（如 MemGPT）在特定 Benchmark 上的定量性能对比（如准确率提升百分比）。

#### 6. 主要结论与发现
*   **经验可结晶化**：证明了可以从原始对话中自动抽象出结构化的行为规范（技能）。
*   **持续演进可行性**：通过版本化合并机制，智能体能够随着用户反馈的增加，不断完善特定技能的约束条件，而非简单地堆砌记忆。
*   **透明度提升**：显式的 SKILL.md 使得智能体的“个性化能力”变得可观察、可人工干预和跨平台迁移。

#### 7. 优点
*   **显式且可控**：技能以 Markdown 存储，用户或开发者可以直接查看并手动修改，解决了黑盒模型难以对齐的问题。
*   **模型无关性**：作为插件层，可以适配任何 LLM，且无需昂贵的微调。
*   **版本管理**：引入了类似软件工程的版本控制，能够追踪能力的成长轨迹。
*   **异步演进**：技能提取和维护在后台进行，不增加用户感知的响应延迟。

#### 8. 不足与局限
*   **依赖提取模型的能力**：如果用于提取技能的 LLM 性能较弱，可能会产生噪声技能或错误的合并决策。
*   **缺乏定量基准测试**：虽然展示了技能的丰富性，但未在标准的智能体评测集上量化其对任务成功率的具体贡献。
*   **潜在的冲突风险**：当多个检索到的技能指令相互冲突时，如何进行冲突消解在文中未做深入探讨。
*   **应用限制**：主要适用于有重复性需求或长期偏好的场景，对于纯随机、一次性的交互任务收益有限。

（完）
