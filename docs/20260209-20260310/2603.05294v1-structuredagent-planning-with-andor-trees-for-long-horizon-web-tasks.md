---
title: "STRUCTUREDAGENT: Planning with AND/OR Trees for Long-Horizon Web Tasks"
title_zh: STRUCTUREDAGENT：基于与或树的长程网络任务规划
authors: "ELita Lobo, Xu Chen, Jingjing Meng, Nan Xi, Yang Jiao, Chirag Agarwal, Yair Zick, Yan Gao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05294v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 用于长程网页任务的结构化记忆模块
tldr: 针对大语言模型智能体在处理长程 Web 任务时存在的规划能力弱、记忆受限及贪婪行为等问题，本文提出 STRUCTUREDAGENT 框架。该框架结合动态 AND/OR 树的在线分层规划器与结构化记忆模块，通过高效搜索和候选方案追踪，显著提升了复杂任务的约束满足率和执行效率，并在多个基准测试中超越了现有方法。
selection_source: fresh_fetch
motivation: 现有 Web 智能体在长程任务中因上下文记忆有限和缺乏全局规划，容易陷入局部最优或过早终止。
method: 提出一种基于动态 AND/OR 树的分层规划框架，并结合结构化记忆模块来维护候选解和追踪任务状态。
result: 在 WebVoyager、WebArena 及购物基准测试中，该方法在长程网页浏览任务上的表现均优于标准 LLM 智能体。
conclusion: STRUCTUREDAGENT 通过分层规划和结构化记忆有效解决了复杂 Web 任务的挑战，并提供了可解释且易于干预的执行路径。
---

## 摘要
大语言模型（LLMs）的最新进展使得用于顺序决策的智能体系统成为可能。此类智能体必须感知其环境，跨多个时间步进行推理，并采取优化长期目标的行动。然而，现有的网络智能体在处理复杂、长程任务时面临困难，原因在于用于追踪历史的上下文内存有限、规划能力薄弱，以及导致过早终止的贪婪行为。为了应对这些挑战，我们提出了 STRUCTUREDAGENT，这是一个具有两个核心组件的分层规划框架：(1) 一个使用动态与或树进行高效搜索的在线分层规划器；(2) 一个追踪并维护候选解的结构化记忆模块，旨在提高信息寻求任务中的约束满足度。该框架还能生成可解释的分层计划，从而简化调试并在需要时促进人工干预。我们在 WebVoyager、WebArena 和自定义购物基准测试上的结果表明，与标准的基于 LLM 的智能体相比，STRUCTUREDAGENT 提升了在长程网络浏览任务中的性能。

## Abstract
Recent advances in large language models (LLMs) have enabled agentic systems for sequential decision-making. Such agents must perceive their environment, reason across multiple time steps, and take actions that optimize long-term objectives. However, existing web agents struggle on complex, long-horizon tasks due to limited in-context memory for tracking history, weak planning abilities, and greedy behaviors that lead to premature termination. To address these challenges, we propose STRUCTUREDAGENT, a hierarchical planning framework with two core components: (1) an online hierarchical planner that uses dynamic AND/OR trees for efficient search and (2) a structured memory module that tracks and maintains candidate solutions to improve constraint satisfaction in information-seeking tasks. The framework also produces interpretable hierarchical plans, enabling easier debugging and facilitating human intervention when needed. Our results on WebVoyager, WebArena, and custom shopping benchmarks show that STRUCTUREDAGENT improves performance on long-horizon web-browsing tasks compared to standard LLM-based agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STRUCTUREDAGENT** 的分层规划框架，旨在解决大语言模型（LLM）智能体在处理复杂、长程（Long-Horizon）网页任务时的局限性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 在简单网页任务上表现良好，但在面对需要多步推理、跨多个页面跳转的长程任务时，现有智能体存在四大痛点：
    1.  **上下文记忆有限**：网页内容极其密集（常超 20k tokens），智能体难以追踪历史路径。
    2.  **规划能力薄弱**：无法构建和执行复杂的嵌套计划。
    3.  **缺乏错误恢复**：一旦执行失败，往往直接终止，无法探索替代方案。
    4.  **贪婪决策**：倾向于过早结束任务，而未完全满足用户的所有约束。
*   **核心目标**：借鉴人类解决问题的“启发式搜索”和“手段-目的分析”方法，通过分层规划提升 Web 智能体的鲁棒性、可解释性和约束满足能力。

### 2. 方法论
STRUCTUREDAGENT 结合了分层规划器和结构化记忆模块：
*   **动态 AND/OR 树规划**：
    *   **节点类型**：**AND 节点**（必须完成所有子目标）、**OR 节点**（备选策略，任一成功即可）、**ACTION 节点**（原子操作，如点击、输入）。
    *   **改进的深度优先搜索 (DFS)**：不同于传统 DFS，该算法支持动态树构建、节点修复和错误回溯。节点具有三种执行状态（进入、退出、失败），允许在发现新信息时对计划进行实时修订或剪枝。
    *   **树操作算子**：包括节点扩展（Node Expansion）、节点修复（Node Repair）、全局树更新（Global Tree Update）和完成度检查（Completion Check），这些操作由 LLM 作为控制器驱动。
*   **结构化记忆模块 (Structured Memory)**：
    *   针对信息寻求类任务，建立一个动态表格来追踪候选实体及其满足的约束条件。
    *   该模块能防止智能体在长程探索中遗忘之前发现的潜在方案，并减少重复搜索。
*   **人机协作**：分层树结构天然支持人类干预，操作员可以检查树结构并注入纠正性的子目标。

### 3. 实验设计
*   **数据集/基准测试**：
    1.  **Custom Complex Shopping**：包含 60 个复杂的亚马逊购物任务，通常需要 10-30 个交互步骤。
    2.  **WebVoyager**：包含 129 个真实世界的跨网站任务。
    3.  **WebArena**：在 630 个任务上进行评估（涵盖地图、购物、Reddit、GitLab）。
*   **对比方法**：
    *   **AgentOccam**：一种基于动作树剪枝的强力基准。
    *   **BasicClaudeAction**：带有完整动作历史的 Claude 智能体。
    *   **SteP**：使用人工编写策略的堆栈式 LLM 策略。
    *   **WebarenaReplication**：WebArena 官方参考智能体。
*   **评估指标**：任务成功率。采用 LLM-as-a-Judge（GPT-4, Gemini, Kimi）和人工标注相结合的方式。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 训练算力或时长。
*   **原因**：该研究主要关注**推理阶段（Inference-time）**的规划框架，而非模型训练。实验主要通过 API 调用闭源模型（如 Claude 3.5 Sonnet, Claude 3.7, GPT-4o）作为推理后端。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了广泛实验，涵盖了从简单到极高难度的任务。
*   **充分性评估**：
    *   **多模型验证**：测试了 Claude 3.5、Claude 3.7 和 Kimi-k2 等不同家族的模型，证明了框架的通用性。
    *   **消融实验**：对比了有无结构化记忆模块的表现。
    *   **多维度评估**：不仅对比了成功率，还分析了动作数量分布和执行时长。
    *   **客观性**：引入了多模型交叉评审和人工评审，减少了单一 LLM 评审带来的偏差。

### 6. 主要结论与发现
*   **性能领先**：STRUCTUREDAGENT 在复杂购物任务和 WebArena 任务中显著优于所有基准方法（如在 WebArena 整体表现上超过 AgentOccam 约 6%）。
*   **长程任务优势**：在需要满足多重约束的硬任务（Amazon Hard）中，该框架通过系统性探索表现出极强的韧性，而传统智能体常因贪婪行为失败。
*   **记忆模块的作用**：结构化记忆在复杂检索任务中提升了 5% 的成功率，但在简单任务中可能引入噪声。
*   **模型扩展性**：即使使用更强大的模型（如 Claude 3.7），分层规划带来的增益依然存在，说明算法框架与模型能力是互补的。

### 7. 优点
*   **鲁棒的错误处理**：通过 AND/OR 树的回溯机制，智能体能从失败的路径中恢复并尝试备选方案。
*   **高可解释性**：生成的树状计划清晰展示了智能体的决策逻辑，便于调试和人工监控。
*   **约束追踪**：结构化记忆有效解决了长上下文下的信息遗忘问题。

### 8. 不足与局限
*   **执行效率**：由于进行了更深入的探索和规划，该方法在某些任务上的动作数量和运行时间（Wall-clock time）高于基准方法，存在“以时间换成功率”的权衡。
*   **模型依赖**：框架的效能高度依赖于后端 LLM 准确执行树操作指令的能力。
*   **评估挑战**：在极复杂的任务中，不同 LLM 评审员之间仍存在较高的不一致性，反映出自动化评估长程任务的固有难度。

（完）
