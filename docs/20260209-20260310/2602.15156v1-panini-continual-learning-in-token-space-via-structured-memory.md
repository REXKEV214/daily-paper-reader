---
title: "Panini: Continual Learning in Token Space via Structured Memory"
title_zh: Panini：通过结构化记忆在 Token 空间实现持续学习
authors: "Shreyas Rajesh, Pavan Holur, Mehmet Yigit Turali, Chenda Duan, Vwani Roychowdhury"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.15156v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 通过将新经验整合到外部语义记忆中进行持续学习
tldr: "针对大语言模型在处理新知识时检索增强生成（RAG）效率低且易引入无关信息的问题，本文提出 Panini 框架。该框架通过构建“生成式语义工作空间”（GSW）将文档转化为由实体和事件感知的问答对组成的结构化网络。在推理时，模型仅遍历该网络提取推理链而非原始文档，实现了高效且可靠的持续学习。实验表明，Panini 在多个基准测试中性能提升 5%-7%，且显著降低了计算开销。"
selection_source: fresh_fetch
motivation: 传统的 RAG 方法在处理新文档时存在重复推理效率低和分块检索引入无关上下文导致生成不可靠的问题。
method: 提出 Panini 框架，将文档表示为由问答对构成的生成式语义工作空间（GSW），通过在结构化网络上检索推理链来代替原始文本检索。
result: "在六个问答基准测试中，Panini 的平均性能比竞争基线高出 5%-7%，同时减少了 2 到 30 倍的 Token 使用量。"
conclusion: 通过在写入时对经验进行高效且准确的结构化处理，可以显著提升大语言模型在读取时的推理效率和可靠性。
---

## 摘要
语言模型越来越多地被用于对未训练过的内容进行推理，例如新文档、不断演进的知识以及用户特定数据。一种常见的方法是检索增强生成（RAG），它将原始文档以块（chunks）的形式存储在外部，并在推理时仅检索相关的子集供大语言模型（LLM）进行推理。然而，这导致了测试时计算效率低下（LLM 重复对相同文档进行推理）；此外，块检索可能会引入无关的上下文，从而增加无根据的生成。我们提出了一种类人的非参数持续学习框架，其中基础模型保持不变，学习通过将每一次新体验整合到一个不断积累和巩固的外部语义记忆状态中来实现。我们展示了 Panini，它通过将文档表示为生成式语义工作区（GSW）来实现这一点——这是一个感知实体和事件的问答（QA）对网络，足以让 LLM 重构所经历的情境，并通过网络上基于推理的推理链挖掘潜在知识。给定一个查询，Panini 仅遍历持续更新的 GSW（而非原始文档或块），并检索最可能的推理链。在六个 QA 基准测试中，Panini 实现了最高的平均性能，比其他竞争基线高出 5%-7%，同时使用的答案上下文 token 减少了 2-30 倍，支持完全开源的流水线，并减少了在精心设计的不可回答查询上的无根据回答。结果表明，在写入时对经验进行高效且准确的结构化（如 GSW 框架所实现的）可以在读取时同时获得效率和可靠性的提升。代码可在 https://github.com/roychowdhuryresearch/gsw-memory 获取。

## Abstract
Language models are increasingly used to reason over content they were not trained on, such as new documents, evolving knowledge, and user-specific data. A common approach is retrieval-augmented generation (RAG), which stores verbatim documents externally (as chunks) and retrieves only a relevant subset at inference time for an LLM to reason over. However, this results in inefficient usage of test-time compute (LLM repeatedly reasons over the same documents); moreover, chunk retrieval can inject irrelevant context that increases unsupported generation. We propose a human-like non-parametric continual learning framework, where the base model remains fixed, and learning occurs by integrating each new experience into an external semantic memory state that accumulates and consolidates itself continually. We present Panini, which realizes this by representing documents as Generative Semantic Workspaces (GSW) -- an entity- and event-aware network of question-answer (QA) pairs, sufficient for an LLM to reconstruct the experienced situations and mine latent knowledge via reasoning-grounded inference chains on the network. Given a query, Panini only traverses the continually-updated GSW (not the verbatim documents or chunks), and retrieves the most likely inference chains. Across six QA benchmarks, Panini achieves the highest average performance, 5%-7% higher than other competitive baselines, while using 2-30x fewer answer-context tokens, supports fully open-source pipelines, and reduces unsupported answers on curated unanswerable queries. The results show that efficient and accurate structuring of experiences at write time -- as achieved by the GSW framework -- yields both efficiency and reliability gains at read time. Code is available at https://github.com/roychowdhuryresearch/gsw-memory.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Panini** 的非参数持续学习（NPCL）框架，旨在通过结构化记忆提升大语言模型（LLM）处理新知识的效率与可靠性。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的检索增强生成（RAG）将文档切分为原始文本块（chunks），这导致了两个主要问题：
    1.  **推理效率低下**：LLM 在推理时需要反复处理相同的冗余文本块，浪费测试时算力。
    2.  **可靠性不足**：块检索容易引入无关上下文，导致模型产生幻觉或无法在证据不足时准确拒绝回答（Abstention）。
*   **核心目标**：模仿人类的记忆巩固机制，在“写入”新信息时进行结构化处理，将原始文档转化为语义记忆，从而在“读取”时实现高效、精准的推理。

### 2. 方法论：核心思想与关键技术
Panini 的核心在于将持续学习的过程分为**结构化存储（写入）**和**推理链检索（读取）**：

*   **生成式语义工作空间（GSW）**：
    *   在写入阶段，Panini 不存储文本块，而是将每个文档转化为 GSW。
    *   **构成**：包含实体节点（带有角色和状态）、谓词/事件节点，以及连接两者的**问答对（QA pairs）**。
    *   **索引**：构建双重索引——针对实体的稀疏索引（BM25）和针对 QA 对的稠密向量索引。
*   **推理推理链检索（RICR）算法**：
    *   **规划（Planning）**：将复杂查询分解为一系列原子子问题。
    *   **跳跃式检索（Hops）**：利用双重索引寻找候选 QA 对。
    *   **束搜索（Beam Search）**：维护 $B$ 条推理链，使用**几何平均分**（Geometric Mean）对链条进行评分：
        $$Score(C) = \left( \prod_{l=1}^{t} s_l \right)^{1/t}$$
        其中 $s_l$ 是第 $l$ 跳的相关性得分。
    *   **去重与生成**：仅将检索到的精简 QA 对（而非原始长文本）输入 LLM 生成最终答案。

### 3. 实验设计
*   **数据集**：
    *   **多跳问答**：MuSiQue, 2WikiMultihopQA, HotpotQA, LV-Eval。
    *   **单跳问答**：NQ, PopQA。
    *   **可靠性测试**：构建了 **Platinum** 评估集（MuSiQue-Platinum 和 2Wiki-Platinum），专门区分可回答和不可回答的问题。
*   **对比方法**：
    *   **基础 RAG**：BM25, Dense Retrieval (NV-Embed-v2, Qwen3)。
    *   **结构化 RAG**：RAPTOR, GraphRAG, LightRAG, HippoRAG。
    *   **智能体系统**：IRCoT, Search-R1。

### 4. 资源与算力
*   **算力与成本**：
    *   论文明确提到了写入成本：在 MuSiQue 数据集（11,656 个段落）上，使用 GPT-4o-mini 的 Batch API 构建 GSW 的成本约为 **48.02 美元**。
    *   **索引时长**：MuSiQue 数据集约需 100 分钟。
    *   **模型使用**：使用了 GPT-4o, GPT-4o-mini 以及开源模型 Qwen3-8B/14B, GPT-OSS-120B。
    *   **硬件环境**：开源实验部分提到所有组件均可在单张 GPU 上运行（如 Qwen3-8B 规模）。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流基准测试上进行了全面评估，涵盖了从单跳到长上下文多跳的各种场景。
*   **消融实验**：非常充分。作者对束搜索宽度（Beam Width）、双重搜索策略、QA 重排序、分解逻辑以及不同规模的开源模型作为提取器/阅读器的效果进行了详细对比。
*   **客观性**：通过引入“Platinum”数据集测试拒绝回答的准确率，客观地评估了系统在面对缺失证据时的鲁棒性，避免了仅追求 F1 分数而忽视幻觉的问题。

### 6. 主要结论与发现
*   **性能领先**：Panini 在平均 F1 分数上比最强基线（HippoRAG 2）高出 **5%-7%**。
*   **极致高效**：推理时使用的上下文 Token 数量比竞争基线减少了 **2 到 30 倍**。
*   **高可靠性**：在不可回答的问题上，Panini 的拒绝准确率显著高于传统 RAG，有效抑制了幻觉。
*   **通用性**：GSW 结构可以作为通用基础设施，直接提升其他智能体（如 Search-R1）的性能。

### 7. 优点
*   **架构创新**：将 RAG 的负担从“读取时推理”转移到“写入时结构化”，符合人类记忆巩固的生物学逻辑。
*   **Token 节省**：通过 QA 对代替原始文本，极大地降低了长上下文带来的计算开销和“中间信息丢失”风险。
*   **开源友好**：证明了即使使用中等规模的开源模型（如 Qwen3-8B），该框架依然能保持强大的竞争力。

### 8. 不足与局限
*   **写入成本**：虽然读取时很快，但初始构建 GSW 需要调用 LLM，对于海量实时更新的数据流，写入成本和延迟可能成为瓶颈。
*   **提取依赖**：GSW 的质量高度依赖于初始提取模型的性能，如果提取模型漏掉了关键谓词，推理链就会断裂。
*   **缺乏全局整合**：目前主要在文档内进行结构化，跨文档的实体对齐和冲突解决（Reconciliation）仍有提升空间。

（完）
