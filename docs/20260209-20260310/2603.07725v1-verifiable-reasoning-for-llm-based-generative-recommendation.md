---
title: Verifiable Reasoning for LLM-based Generative Recommendation
title_zh: 基于大语言模型生成式推荐的可验证推理
authors: "Xinyu Lin, Hanqing Zeng, Hanchao Yu, Yinglong Xia, Jiang Zhang, Aashu Singh, Fei Liu, Wenjie Wang, Fuli Feng, Tat-Seng Chua, Qifan Wang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07725v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 理解生成式推荐中的复杂用户偏好
tldr: 针对大语言模型在生成式推荐中存在的推理退化（如同质化或错误累积）问题，本文提出了一种全新的“推理-验证-推荐”范式。通过引入可验证推理框架VRec，在推理过程中交织验证环节，利用多维度验证器和代理预测目标确保推理的可靠性与全面性。实验证明，该方法在四个真实数据集上显著提升了推荐效果和可扩展性，且不损失效率。
selection_source: fresh_fetch
motivation: 现有的“先推理后推荐”范式缺乏中间验证，导致推理过程容易出现错误累积或同质化，从而损害推荐质量。
method: 提出VRec框架，采用“推理-验证-推荐”范式，通过混合验证器实现多维度偏好校验，并利用代理预测目标提升验证的可靠性。
result: 在四个真实世界数据集上的实验结果显示，VRec在显著增强推荐效果和可扩展性的同时，保持了良好的运行效率。
conclusion: 引入中间验证机制能有效引导LLM生成更忠实于用户偏好的推理过程，是提升生成式推荐性能的关键。
---

## 摘要
大语言模型（LLMs）中的推理最近在通过深入理解复杂用户偏好来增强生成式推荐方面展现出强大潜力。现有方法遵循“先推理后推荐”的范式，即 LLMs 在生成物品之前进行逐步推理。然而，由于缺乏中间验证，这种范式不可避免地会遭受推理退化（即同质化或误差累积的推理），从而损害推荐效果。为了弥补这一差距，我们提出了一种新颖的“推理-验证-推荐”范式，该范式将推理与验证交织在一起，以提供可靠的反馈，引导推理过程走向更忠实的用户偏好理解。为了实现有效的验证，我们为验证器设计确立了两个关键原则：1) 可靠性，确保对推理正确性的准确评估和信息丰富的引导生成；2) 多维性，强调对多维度用户偏好的全面验证。据此，我们提出了一种名为 VRec 的有效实现。它采用混合验证器来确保多维性，同时利用代理预测目标来追求可靠性。在四个真实世界数据集上的实验表明，VRec 在不牺牲效率的情况下，显著增强了推荐的有效性和可扩展性。代码可在 https://github.com/Linxyhaha/Verifiable-Rec 找到。

## 速览
**TLDR**：针对大语言模型在生成式推荐中存在的推理退化（如同质化或错误累积）问题，本文提出了一种全新的“推理-验证-推荐”范式。通过引入可验证推理框架VRec，在推理过程中交织验证环节，利用多维度验证器和代理预测目标确保推理的可靠性与全面性。实验证明，该方法在四个真实数据集上显著提升了推荐效果和可扩展性，且不损失效率。 \
**Motivation**：现有的“先推理后推荐”范式由于缺乏中间验证，容易导致推理过程出现错误累积或同质化，进而影响推荐的准确性。 \
**Method**：提出VRec框架，通过混合验证器实现多维度偏好校验，并结合代理预测目标来确保验证过程的可靠性与指导性。 \
**Result**：在四个真实世界数据集上的实验表明，VRec在显著提升推荐性能和系统可扩展性的同时，并未牺牲运行效率。 \
**Conclusion**：将验证环节融入LLM推理过程能有效纠正偏好理解偏差，是实现高质量生成式推荐的有效途径。

---

## Abstract
Reasoning in Large Language Models (LLMs) has recently shown strong potential in enhancing generative recommendation through deep understanding of complex user preference. Existing approaches follow a {reason-then-recommend} paradigm, where LLMs perform step-by-step reasoning before item generation. However, this paradigm inevitably suffers from reasoning degradation (i.e., homogeneous or error-accumulated reasoning) due to the lack of intermediate verification, thus undermining the recommendation. To bridge this gap, we propose a novel \textbf{\textit{reason-verify-recommend}} paradigm, which interleaves reasoning with verification to provide reliable feedback, guiding the reasoning process toward more faithful user preference understanding. To enable effective verification, we establish two key principles for verifier design: 1) reliability ensures accurate evaluation of reasoning correctness and informative guidance generation; and 2) multi-dimensionality emphasizes comprehensive verification across multi-dimensional user preferences. Accordingly, we propose an effective implementation called VRec. It employs a mixture of verifiers to ensure multi-dimensionality, while leveraging a proxy prediction objective to pursue reliability. Experiments on four real-world datasets demonstrate that VRec substantially enhances recommendation effectiveness and scalability without compromising efficiency. The codes can be found at https://github.com/Linxyhaha/Verifiable-Rec.

---

## 论文详细总结（自动生成）

这是一份关于论文《Verifiable Reasoning for LLM-based Generative Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在推荐系统中的推理能力（Reason4Rec）被视为捕捉复杂用户偏好的关键。现有的方法通常采用“先推理后推荐”（Reason-then-Recommend）的范式。
*   **核心痛点**：现有的推理过程是**不可验证**的。由于缺乏中间反馈，推理过程容易出现“推理退化”现象：
    1.  **同质化推理**：模型倾向于走捷径，生成表面化、重复的推理内容。
    2.  **错误累积**：推理早期的微小偏差会随步骤自回归地放大，导致最终推荐错误。
*   **研究动机**：借鉴 OpenAI-o1 等模型的成功经验，探索如何在推荐系统的隐空间推理中引入验证机制，确保推理过程忠实于用户偏好。

### 2. 方法论：VRec 框架
VRec 提出了**“推理-验证-推荐”**（Reason-Verify-Recommend）的新范式，核心思想是在每一步推理后插入一个轻量级的验证环节。
*   **混合验证器 (Mixture of Verifiers)**：
    *   **多维性**：设计多个验证器分别关注不同维度（如物品类别、标题语义、协同过滤信息）。
    *   **个性化路由 (Router)**：根据当前用户的行为特征，动态分配不同验证维度的权重。
*   **可靠验证机制**：
    *   **代理预测任务**：由于推理正确性难以直接标注，VRec 将其转化为“群体偏好预测”任务（例如：预测当前推理步骤是否指向“爵士乐”这一类别）。
    *   **熵基评估 (Entropy-based Feedback)**：利用预测分布的熵来衡量推理的置信度。熵越高，表示推理越不可靠。
    *   **置信度引导调整**：利用验证器的模型权重作为“偏好原型”，通过置信度分数将原型向量融入推理表示中，修正偏差。
*   **训练策略**：
    *   **两阶段训练**：先预训练验证器识别高质量推理，再联合微调 LLM 与验证器。
    *   **单调性正则化 (Monotonicity Regularization)**：通过损失函数强制要求推理步骤越深，预测熵值越低（即推理越确定、越准确）。

### 3. 实验设计
*   **数据集**：使用了四个不同领域的真实世界数据集：**Amazon CDs**（音乐）、**Amazon Instruments**（乐器）、**MicroLens**（短视频）、**Goodreads**（图书）。
*   **Benchmark（对比方法）**：
    *   **传统模型**：GRU4Rec, Caser, SASRec。
    *   **生成式推荐**：TIGER, LETTER, SETRec, D3。
    *   **推理增强模型**：LatentTTS, LatentR3（目前最强的隐空间推理基准）。
*   **评价指标**：Recall@5/10, NDCG@5/10。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100-SXM4-80G GPU**。
*   **基础模型**：实验基于 **Qwen2.5-1.5B** 进行全参数微调。
*   **效率说明**：文中强调验证器非常轻量（仅为简单的 MLP 或线性层），实验数据显示验证环节仅增加了约 **0.59%** 的平均推理时间开销。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括主实验对比、消融实验（验证各组件有效性）、推理步数的可扩展性分析（0-10步）、验证器模型大小的影响、以及超参数敏感性分析。
*   **充分性评价**：实验设计非常充分。特别是针对“推理步数”的分析，证明了 VRec 能够克服传统方法在步数增加时性能下降的瓶颈，实现了真正的“测试时缩放”（Test-time Scaling），实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：VRec 在所有数据集上均显著优于现有 SOTA 方法，尤其在长序列和复杂偏好场景下表现更佳。
*   **解决退化**：通过 t-SNE 可视化证明，VRec 确实能引导推理表示向目标物品的表示靠拢，有效缓解了同质化问题。
*   **可扩展性**：随着推理步数增加，VRec 的性能持续提升并趋于稳定，而基准模型通常在 1 步之后就开始性能下滑。
*   **维度互补**：语义验证和协同验证的结合对提升推荐质量至关重要。

### 7. 优点
*   **范式突破**：首次将“可验证推理”引入生成式推荐，解决了隐空间推理不可控的难题。
*   **极高效率**：在几乎不增加计算延迟的前提下，大幅提升了模型性能，具有很强的工业落地潜力。
*   **设计优雅**：利用验证器内部权重作为修正信号，巧妙地解决了隐空间缺乏显式监督信号的问题。

### 8. 不足与局限
*   **标签依赖**：验证器的效果高度依赖于“群体偏好标签”的质量（如聚类效果或元数据标注）。
*   **显式解释性不足**：虽然验证器提供了维度上的引导，但推理过程仍在隐空间完成，无法像文本 CoT 那样直接给用户展示人类可读的推理逻辑。
*   **任务局限**：目前主要验证了“下一项推荐”任务，对于冷启动场景或跨域推荐的泛化能力尚待进一步验证。

（完）
