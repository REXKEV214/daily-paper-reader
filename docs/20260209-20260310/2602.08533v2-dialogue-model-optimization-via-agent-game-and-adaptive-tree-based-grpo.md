---
title: Dialogue Model Optimization via Agent Game and Adaptive Tree-based GRPO
title_zh: 通过智能体博弈与自适应树形 GRPO 优化对话模型
authors: "Kun Peng, Conghui Tan, Yu Liu, Guohua Tang, Zhongqian Sun, Wei Yang, Zining Zhu, Lei Jiang, Yanbing Liu, Hao Peng"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08533v2"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 对话代理通过适应用户特征实现个性化交互
tldr: 针对开放域对话模型过度依赖预收集数据及强化学习中短期偏差的问题，本文提出一种结合在线个性化与自适应树状GRPO（AT-GRPO）的长程强化学习框架。该框架通过双智能体博弈模拟动态环境，并利用AT-GRPO在降低计算开销的同时捕捉长程奖励，显著提升了对话的吸引力和个性化水平。
selection_source: fresh_fetch
motivation: 现有对话模型受限于对静态数据的依赖以及强化学习在处理长程对话价值时的短期偏差。
method: 提出双智能体博弈范式构建动态环境，并结合自适应树状GRPO算法，通过阶段性调整观察范围来优化长程奖励捕获。
result: 实验证明该框架在性能、样本效率和鲁棒性方面均优于现有方法，且将计算开销从指数级降至多项式级。
conclusion: 该研究为实现高效、长程且个性化的开放域对话系统提供了一种创新的强化学习优化方案。
---

## 摘要
开放式对话智能体旨在通过适应用户特征来提供引人入胜且个性化的交互，但现有方法面临关键局限：过度依赖预先收集的用户数据，以及强化学习（RL）中的短视偏见，导致忽略了长期对话价值。为了解决这些问题，我们提出了一种新型的长程强化学习框架，将在线个性化与自适应树形组相对策略优化（AT-GRPO）相结合。该框架采用双智能体博弈范式，其中用户智能体通过风格模仿（学习特定用户的对话特征）和主动终止（预测轮次级别的终止概率作为即时奖励）来构建动态环境，形成一个驱动对话智能体深化兴趣探索的迭代循环。AT-GRPO 将对话轨迹重新解释为树结构，并引入了自适应观测范围。与产生指数级开销的全树展开不同，它限制每个节点仅聚合来自阶段感知范围内的奖励：较大的范围支持早期阶段的话题探索，而较小的范围则有利于后期阶段的对话维护。这种设计将采样预算从对话长度的指数级降低到多项式级，同时保留了对长期奖励的捕捉能力。大量实验表明，我们的框架具有卓越的性能、样本效率和鲁棒性。

## Abstract
Open-ended dialogue agents aim to deliver engaging, personalized interactions by adapting to users' traits, but existing methods face critical limitations: over-reliance on pre-collected user data, and short-horizon biases in reinforcement learning (RL) that neglect long-term dialogue value. To address these, we propose a novel long-horizon RL framework integrating online personalization with Adaptive Tree-based Group Relative Policy Optimization (AT-GRPO). Adopting a two-agent game paradigm, a user agent constructs dynamic environments via style mimicry (learning user-specific conversational traits) and active termination (predicting turn-level termination probabilities as immediate rewards), forming an iterative cycle that drives the dialogue agent to deepen interest exploration. AT-GRPO reinterprets dialogue trajectories as trees and introduces adaptive observation ranges. Unlike full tree expansion that incurs exponential overhead, it limits each node to aggregate rewards from a stage-aware range: larger ranges support early-stage topic exploration, while smaller ranges facilitate late-stage dialogue maintenance. This design reduces rollout budgets from exponential to polynomial in the dialogue length, while preserving long-term reward capture. Extensive experiments show our framework's superior performance, sample efficiency, and robustness.

---

## 论文详细总结（自动生成）

以下是对论文《Dialogue Model Optimization via Agent Game and Adaptive Tree-based GRPO》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：开放域对话系统（如游戏NPC、虚拟助手）的核心目标是提供个性化交互。
*   **核心问题**：
    1.  **离线数据依赖**：现有方法高度依赖预先收集的用户画像或历史数据，难以应对匿名或即时交互场景。
    2.  **强化学习（RL）的短期偏差**：传统的 RL（如 PPO、GRPO）倾向于追求即时奖励（Immediate Reward Trap），导致对话虽然短期通顺但缺乏深度，容易过早终止。
    3.  **计算开销**：虽然树搜索（Tree Search）能缓解短视问题，但在长对话中会导致计算量呈指数级增长。
*   **整体含义**：本文旨在通过“智能体博弈”实现无需先验数据的在线个性化，并利用“自适应树结构”优化长程对话价值，同时控制计算成本。

### 2. 方法论
该框架由两个核心部分组成：

#### A. 智能体博弈范式（Agent Game Paradigm）
*   **用户智能体（User Agent）**：充当动态环境。
    *   **风格模仿**：通过 SFT 学习真实用户的对话风格和逻辑。
    *   **主动终止机制**：预测每一轮对话的终止概率 $p_i$。
    *   **动态阈值调整**：随着训练进行，用户智能体变得越来越“挑剔”（通过参数 $\lambda$ 提高终止门槛），迫使对话智能体不断进化。
*   **对话智能体（Dialogue Agent）**：目标是最小化终止概率，最大化长程交互价值。

#### B. 自适应树形 GRPO（AT-GRPO）
*   **核心思想**：将对话路径建模为树结构，每个节点代表一轮对话，分支代表采样的候选回复。
*   **关键技术细节**：
    1.  **自适应观察范围（Adaptive Observation Range）**：引入观察长度 $l_i$。在对话早期，$l_i$ 较大以加强话题探索；在对话后期，$l_i$ 较小以专注于维护当前话题。
    2.  **奖励聚合**：节点的最终奖励 $r'_{ij}$ 是即时奖励与该节点下子树叶节点平均奖励的加权和。
    3.  **复杂度优化**：通过限制每个节点的观察深度，将采样预算从指数级 $O(W^L)$ 降低到多项式级 $O(W L^C)$，其中 $W$ 是采样宽度，$L$ 是对话长度。

### 3. 实验设计
*   **数据集**：
    *   **NPC-Chat**：自建数据集，包含 340 万条真实用户与游戏 NPC 的高质量对话。
    *   **LCCC**：大规模中文短文本对话数据集（跨领域验证）。
    *   **DailyDialog**：英文日常对话数据集（跨语言验证）。
*   **Benchmark 与对比方法**：
    *   **商业 API**：GPT-5、DeepSeek-V3.2、Gemini-2.5-Flash。
    *   **开源模型基础**：Llama3.1-8B、Qwen3-8B、Qwen2.5-14B。
    *   **RL 算法对比**：GRPO（原始版）、GSPO（轨迹级重加权）、TreeRPO（全树展开版）。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 台 NVIDIA H20 96GB GPU**。
*   **训练细节**：
    *   **SFT 阶段**：学习率 2e-5，Batch Size 16，训练 1 个 epoch。
    *   **RL 阶段**：学习率 1e-5，Batch Size 32，仅需 **100 个训练步数** 即可达到优异性能。
    *   **模型规模**：用户智能体基于 Qwen3-32B，对话智能体基于 Qwen2.5-14B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在三个不同领域、不同语言的数据集上进行了全面对比。
    *   **消融实验**：逐一验证了风格模仿、动态阈值、自适应范围和树状奖励聚合的有效性。
    *   **超参数分析**：对终止阈值系数 $\lambda$、奖励权重 $\omega$、观察范围系数 $\gamma$ 等进行了敏感性测试。
    *   **鲁棒性测试**：包括对抗性用户压力测试（针对攻击性、矛盾性、高风险用户）。
*   **充分性评价**：实验设计非常充分且客观。通过跨语言、跨模型规模（8B vs 14B）以及与顶级商业模型的对比，有力证明了算法的普适性和高效性。

### 6. 主要结论与发现
1.  **长程优化优势**：AT-GRPO 显著提升了平均对话轮数（Avg.L）和对话奖励（Avg.r），在 NPC-Chat 数据集上比原始 Qwen2.5-14B 提升了 33.8% 的交互长度。
2.  **计算效率**：AT-GRPO 在保持长程奖励捕捉能力的同时，训练耗时仅略高于普通 GRPO，远低于全树展开的 TreeRPO。
3.  **在线个性化**：即使没有用户的历史画像，模型也能通过博弈过程在对话中动态捕捉用户偏好，表现甚至优于部分依赖离线数据的商业模型。

### 7. 优点
*   **算法创新**：巧妙地解决了树搜索在 RL 训练中的计算爆炸问题，提出了阶段感知的自适应观察范围。
*   **冷启动能力**：通过在线博弈实现个性化，解决了隐私保护场景下缺乏用户历史数据的问题。
*   **样本效率高**：仅需 100 步 RL 训练即可实现显著的性能跃升。

### 8. 不足与局限
*   **用户智能体依赖**：框架的上限受限于用户智能体模拟的真实度。如果用户智能体存在偏差，对话智能体可能会学到“奖励黑客”（Reward Hacking）行为。
*   **多模态限制**：目前实验仅限于文本对话，尚未扩展到语音、视觉等多模态交互场景。
*   **超参数敏感度**：自适应范围的参数 $\gamma$ 和奖励权重 $\omega$ 对最终效果影响较大，在不同领域可能需要重新调优。

（完）
