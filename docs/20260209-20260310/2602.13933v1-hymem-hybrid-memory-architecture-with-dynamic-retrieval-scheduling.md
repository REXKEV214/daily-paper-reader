---
title: "HyMem: Hybrid Memory Architecture with Dynamic Retrieval Scheduling"
title_zh: HyMem：具有动态检索调度的混合记忆架构
authors: "Xiaochen Zhao, Kaikai Wang, Xiaowen Zhang, Chen Yao, Aili Wang"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.13933v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: LLM智能体的混合记忆架构
tldr: "针对大语言模型在长对话中内存管理效率与效果难以兼得的问题，本文提出HyMem架构。该架构受认知经济原则启发，采用双粒度存储（摘要与原始文本）和动态两级检索系统。通过轻量级模块处理简单查询，并仅在复杂任务时激活基于LLM的深度检索与反思机制。HyMem在保持高性能的同时，将计算成本降低了92.6%，实现了长效记忆管理的最优平衡。"
selection_source: fresh_fetch
motivation: 现有的单体内存表示和静态检索机制无法在保证推理精度的同时兼顾计算效率，难以适应多样化的任务场景。
method: 提出一种结合摘要级和原始级双粒度存储的混合架构，并利用动态两级检索系统根据查询复杂度按需调度计算资源。
result: "在LOCOMO和LongMemEval基准测试中表现优异，超越了全上下文基准，并显著降低了92.6%的计算开销。"
conclusion: HyMem通过多粒度表示与动态调度机制，为长文本对话中的高效记忆管理提供了一种兼具性能与经济性的新方案。
---

## 摘要
大语言模型 (LLM) 智能体在短文本上下文中表现出强大的性能，但由于记忆管理效率低下，在长对话中往往表现不佳。现有方法面临效率与有效性之间的根本权衡：记忆压缩存在丢失复杂推理所需关键细节的风险，而保留原始文本则会为简单查询引入不必要的计算开销。问题的核心在于单一记忆表示和静态检索机制的局限性，它们无法模拟人类所具备的灵活且主动的记忆调度能力，从而难以适应多样的问答场景。受认知经济性原则的启发，我们提出了 HyMem，这是一种混合记忆架构，通过多粒度记忆表示实现动态按需调度。HyMem 采用双粒度存储方案，并配以动态两层检索系统：轻量级模块构建摘要级上下文以实现高效的响应生成，而基于 LLM 的深度模块仅在处理复杂查询时被选择性激活，并辅以反思机制进行迭代推理优化。实验表明，HyMem 在 LOCOMO 和 LongMemEval 基准测试中均取得了优异的性能，在超越全上下文表现的同时，降低了 92.6% 的计算成本，在长期记忆管理的效率与性能之间建立了最先进的平衡。

## Abstract
Large language model (LLM) agents demonstrate strong performance in short-text contexts but often underperform in extended dialogues due to inefficient memory management. Existing approaches face a fundamental trade-off between efficiency and effectiveness: memory compression risks losing critical details required for complex reasoning, while retaining raw text introduces unnecessary computational overhead for simple queries. The crux lies in the limitations of monolithic memory representations and static retrieval mechanisms, which fail to emulate the flexible and proactive memory scheduling capabilities observed in humans, thus struggling to adapt to diverse problem scenarios. Inspired by the principle of cognitive economy, we propose HyMem, a hybrid memory architecture that enables dynamic on-demand scheduling through multi-granular memory representations. HyMem adopts a dual-granular storage scheme paired with a dynamic two-tier retrieval system: a lightweight module constructs summary-level context for efficient response generation, while an LLM-based deep module is selectively activated only for complex queries, augmented by a reflection mechanism for iterative reasoning refinement. Experiments show that HyMem achieves strong performance on both the LOCOMO and LongMemEval benchmarks, outperforming full-context while reducing computational cost by 92.6\%, establishing a state-of-the-art balance between efficiency and performance in long-term memory management.

---

## 论文详细总结（自动生成）

以下是对论文《HyMem: Hybrid Memory Architecture with Dynamic Retrieval Scheduling》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理长对话和长文本时，现有的记忆管理机制存在“效率”与“效果”的根本权衡。
    *   **记忆压缩（如摘要化）**：虽然减少了计算开销，但容易丢失复杂推理所需的关键细节，导致“不可逆遗忘”。
    *   **保留原始文本**：虽然保留了细节，但对于简单查询会引入巨大的计算负担和冗余信息干扰。
*   **研究动机**：受人类大脑“认知经济性”原则启发——即对常规查询优先使用低成本的摘要检索，仅在复杂任务时激活深度逻辑分析。论文旨在构建一种能够根据查询复杂度动态调度资源的混合记忆架构。

### 2. 方法论
HyMem 架构由四个核心组件组成，实现了从存储到检索的动态闭环：

*   **双粒度存储模块 (Memory Storage)**：
    *   将原始对话划分为“事件单元”。
    *   **Level-1 记忆**：提取事件的核心摘要（时间、地点、人物、主干），用于快速检索和轻量推理。
    *   **Level-2 记忆**：保留原始对话文本，与 Level-1 建立映射链接，用于深度回溯。
*   **轻量级记忆模块 (Lightweight Module)**：
    *   使用向量嵌入（Embedding）对 Level-1 摘要进行相似度匹配。
    *   若该模块判断当前上下文足以回答问题，则直接生成答案；若检测到信息缺失，则激活深度模块。
*   **深度记忆模块 (Deep Module)**：
    *   **两阶段检索**：先通过向量搜索筛选 Top-N 候选事件，再利用 LLM 进行“自检索”，识别具有逻辑或因果关联的 Level-1 单元。
    *   **回溯重构**：根据识别出的 Level-1 单元，提取对应的 Level-2 原始文本，重构高信息密度的上下文。
*   **反思模块 (Reflection Module)**：
    *   对生成的答案进行完整性检查。若发现问题未完全解决，则将原问题拆解为子问题并重写查询，触发下一轮迭代推理。

### 3. 实验设计
*   **数据集与基准 (Benchmarks)**：
    *   **LOCOMO**：包含单步、多步、开放域和时间推理四类任务，平均对话长度达 9,000 token。
    *   **LongMemEval-s**：专注于超长上下文下的信息集成和多阶段推理。
*   **对比方法**：
    *   **开源/商业方案**：LangMem, Mem0, Zep, A-Mem, LightMem, Nemori, Mirix, MemU。
    *   **基准线**：Full-Context（全上下文输入）、Naive RAG（基于原始文本的静态向量检索）。
*   **评估指标**：采用 LLM-as-a-Judge（使用 GPT-4.1-mini 作为裁判），评估答案的准确性与可靠性。

### 4. 资源与算力
*   **模型使用**：实验主要基于 **GPT-4o-mini** 和 **GPT-4.1-mini** 作为基础模型。
*   **嵌入模型**：统一使用 **Qwen3-Embedding-0.6B**。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。由于该方法主要基于 API 调用和现成的 Embedding 模型，其核心开销体现在 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流长文本基准上进行了全面对比。
    *   **消融实验**：分别验证了 Level-1/Level-2 存储、轻量/深度/反思模块对性能和 Token 消耗的影响。
    *   **超参数分析**：探讨了检索数量 $k$ 对调用深度模块频率及总成本的影响。
    *   **压缩损失分析**：对比了 HyMem 的事件级压缩与 LLMLingua-2 等通用压缩方法的优劣。
*   **充分性评价**：实验设计较为全面，涵盖了从准确率到计算成本（Token 数）的多维度评估，且通过对比 Full-Context 证明了该方法在减少冗余干扰方面的优越性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：HyMem 在多步推理和开放域任务上显著优于 Full-Context（分别提升 10.46% 和 5.20%），证明了精准检索比堆砌全量信息更有效。
*   **极端高效**：相比于全上下文处理，HyMem 将计算成本（Token 消耗）降低了 **92.6%**。
*   **动态平衡**：约 70% 的查询可通过轻量级模块解决，仅 30% 的复杂查询需要激活深度模块，这种按需分配资源的策略极大地优化了推理效率。
*   **反思的重要性**：反思模块在处理依赖多个记忆片段的复杂查询时，能有效减少幻觉并提升推理深度。

### 7. 优点与亮点
*   **认知启发式设计**：将人类的“认知经济性”引入 AI 记忆管理，打破了静态检索的局限。
*   **双粒度回溯机制**：通过摘要定位、原始文本还原的方式，既保证了检索速度，又避免了细节丢失。
*   **高性价比**：在保持甚至超越 SOTA 性能的同时，极大地压缩了推理成本，具有很强的工程落地价值。

### 8. 不足与局限
*   **依赖基础模型能力**：深度模块和反思模块高度依赖 LLM 的自检索和自我评估能力，若基础模型较弱，可能会导致误判或无效迭代。
*   **延迟风险**：对于需要多轮反思迭代的极复杂查询，虽然 Token 消耗低，但由于多次调用 LLM，可能会增加端到端的响应延迟。
*   **硬件指标缺失**：缺乏关于实际推理时间（Latency）和内存占用（Memory Footprint）的量化数据，仅以 Token 数作为效率指标。

（完）
