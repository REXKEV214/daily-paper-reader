---
title: "VISA: Value Injection via Shielded Adaptation for Personalized LLM Alignment"
title_zh: VISA：通过屏蔽适配进行价值注入，实现个性化大语言模型对齐
authors: "Jiawei Chen, Tianzhuo Yang, Guoxi Zhang, Jiaming Ji, Yaodong Yang, Juntao Dai"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04822v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 通过屏蔽适配实现大语言模型与细微人类价值的个性化对齐
tldr: 针对大语言模型在个性化价值对齐中存在的“对齐税”问题（如价值漂移和语义丢失），本文提出 VISA 框架。该框架通过价值检测器、语义翻译器和基于 GRPO 算法训练的重写器，在优化细粒度价值表达的同时，利用复合奖励函数保护模型的语义完整性。实验证明，VISA 在保持事实一致性和通用能力方面表现优异，显著超越了传统微调方法及 GPT-4o。
selection_source: fresh_fetch
motivation: 现有微调方法在优化特定价值观时会导致模型预设价值漂移、幻觉增加及语义信息丢失等“对齐税”问题。
method: 提出 VISA 框架，利用价值检测器与重写器架构，并通过 GRPO 算法结合复合奖励函数来平衡价值精度与语义完整性。
result: 实验结果显示，VISA 在实现精准价值观控制的同时，能有效维持模型的事实一致性，性能优于标准微调和 GPT-4o。
conclusion: VISA 通过闭环架构和多目标优化策略，为解决 LLM 个性化对齐中的价值漂移与性能损耗提供了有效路径。
---

## 摘要
将大语言模型（LLMs）与细微的人类价值观对齐仍然是一个关键挑战，因为现有的方法（如来自人类反馈的强化学习，RLHF）通常只能处理粗粒度的属性。在实践中，在特定任务数据集上微调 LLM 以优化价值对齐不可避免地会产生“对齐税”：由于吸收了训练数据中的潜在偏差，模型预先校准的价值系统会发生显著偏移，同时微调过程还会导致生成响应中出现严重的幻觉和语义信息丢失。为了解决这一问题，我们提出了 VISA（Value Injection via Shielded Adaptation，通过屏蔽适配进行价值注入），这是一个旨在权衡这些因素的闭环框架。VISA 的架构包含一个高精度价值检测器、一个语义到价值转换器以及一个核心价值重写器。价值重写器通过群体相对策略优化（GRPO）进行训练，并采用复合奖励函数，同时优化细粒度价值精度和语义完整性的保留。通过学习平衡这些竞争目标的最佳策略，VISA 在保持对原始知识忠实度的同时，有效地减轻了对齐税。我们的实验表明，该方法能够精确控制模型的价值表达，同时保持其事实一致性和通用能力，显著优于标准微调方法和基于提示的基准模型（包括 GPT-4o）。

## 速览
**TLDR**：针对大语言模型在个性化价值对齐中存在的“对齐税”问题（如价值漂移和语义丢失），本文提出 VISA 框架。该框架通过价值检测器、翻译器及基于 GRPO 优化的重写器，在注入细粒度价值观的同时，利用复合奖励函数保护模型的语义完整性。实验证明，VISA 在保持事实一致性的前提下实现了精准的价值控制，性能超越了包括 GPT-4o 在内的多种基线方法。 \
**Motivation**：现有的微调方法在优化特定价值观对齐时，容易导致模型预设价值漂移、产生幻觉以及严重的语义信息丢失。 \
**Method**：提出 VISA 框架，利用价值检测器、语义翻译器和基于 GRPO 优化的价值重写器，通过复合奖励函数平衡价值精度与语义完整性。 \
**Result**：实验结果显示，VISA 在实现精确价值观控制的同时，有效保持了模型的事实一致性和通用能力，显著优于标准微调和 GPT-4o。 \
**Conclusion**：VISA 成功缓解了对齐税问题，为实现既忠实于原始知识又能精准注入个性化价值观的 LLM 对齐提供了有效方案。

---

## Abstract
Aligning Large Language Models (LLMs) with nuanced human values remains a critical challenge, as existing methods like Reinforcement Learning from Human Feedback (RLHF) often handle only coarse-grained attributes. In practice, fine-tuning LLMs on task-specific datasets to optimize value alignment inevitably incurs an alignment tax: the model's pre-calibrated value system drifts significantly due to latent bias absorption from training data, while the fine-tuning process also causes severe hallucinations and semantic information loss in generated responses. To address this, we propose VISA (Value Injection via Shielded Adaptation), a closed-loop framework designed to navigate this trade-off. VISA's architecture features a high-precision value detector, a semantic-to-value translator, and a core value-rewriter. The value-rewriter is trained via Group Relative Policy Optimization (GRPO) with a composite reward function that simultaneously optimizes for fine-grained value precision, and the preservation of semantic integrity. By learning an optimal policy to balance these competing objectives, VISA effectively mitigates the alignment tax while staying loyal to the original knowledge. Our experiments demonstrate that this approach enables precise control over a model's value expression while maintaining its factual consistency and general capabilities, significantly outperforming both standard fine-tuning methods and prompting-based baselines, including GPT-4o.

---

## 论文详细总结（自动生成）

### 论文总结：VISA —— 通过屏蔽适配进行价值注入，实现个性化 LLM 对齐

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在个性化对齐过程中面临的**“对齐税”（Alignment Tax）**问题展开研究。现有的对齐方法（如 RLHF 或 SFT）在优化特定价值观时，往往会导致模型原本校准良好的价值系统发生非预期的“价值漂移”（Value Drift），或者在强化价值观时造成事实知识的遗忘和幻觉。论文旨在探索如何使模型在学习新价值观的同时，严格保留原始知识和语义完整性。

#### 2. 方法论：VISA 框架
VISA（Value Injection via Shielded Adaptation）的核心思想是将**知识保留与价值对齐在架构上解耦**。
*   **核心架构**：采用“冻结基座模型 + 轻量级重写器”的模式。
    *   **价值检测器 ($D_\psi$)**：评估原始响应在 Schwartz 价值空间（10个维度）中的位置。
    *   **指令翻译器 ($T_\phi$)**：将自然语言指令（如“更保守一点”）转换为价值空间中的偏移向量 ($\Delta v$)。
    *   **价值重写器 ($\pi_\theta$)**：基于原始响应和目标价值向量生成新响应。
*   **关键技术细节**：
    *   **训练算法**：使用**群体相对策略优化（GRPO）**。相比 PPO，GRPO 无需额外的 Critic 网络，通过组内样本的相对奖励来估计优势函数，训练更稳定且节省显存。
    *   **复合奖励函数**：
        1.  **价值注入精度 ($R_{val}$)**：计算生成文本的预测价值向量与目标向量之间的余弦相似度。
        2.  **语义完整性 ($R_{cons}$)**：利用事实分析器（Fact Analyzer）测量重写文本与原始文本之间的双向蕴含关系，防止幻觉。

#### 3. 实验设计
*   **数据集**：构建并发布了 **VCR-45K** 数据集，包含 45,442 个“原始响应-目标向量-重写响应”三元组，基于 Schwartz 基本价值理论进行标注。
*   **Benchmark**：使用 Schwartz 10 维度价值体系及 Inglehart-Welzel 文化地图维度。
*   **对比方法**：
    *   **Prompting 类**：GPT-4o, GPT-4o-mini, Gemini-3-Pro（涵盖简单提示、复杂规则提示、思维链 CoT 提示）。
    *   **微调类**：标准 SFT、DPO（直接偏好优化）、SimPO。
*   **评估指标**：语义一致性（NLI 得分）、价值对齐误差（L2 距离、余弦相似度）、联合成功率（JSR）。

#### 4. 资源与算力
*   **技术栈**：使用了 DeepSpeed ZeRO-3 (FP8 精度) 进行优化，推理端使用 vLLM 引擎。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。仅提到了超参数设置（如 Batch Size 为 16，梯度累积步数为 8，训练 2 个 Epoch）。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从 0.6B 到 8B 不同参数规模的模型（Qwen3, Llama-3 系列），验证了方法的扩展性。
*   **消融与对比**：进行了详尽的算法对比（SFT vs DPO vs SimPO vs GRPO）和提示词工程对比。
*   **人类评估**：引入了人类偏好胜率和价值识别一致性实验（n=668），增强了结果的说服力。
*   **应用场景扩展**：额外设计了“自适应价值搜索”实验，证明 VISA 在目标模糊的情况下也能通过双层优化找到最优对齐方向。
*   **评价**：实验设计较为全面、客观，通过多维度指标（自动+人工）验证了价值与知识的权衡。

#### 6. 主要结论与发现
*   **克服对齐税**：VISA 在保持最高语义一致性（0.8732）的同时，实现了与 GPT-4o 相当甚至更优的价值对齐精度。
*   **Prompting 的局限**：强行通过复杂 Prompt 让闭源模型对齐价值观会导致严重的语义漂移和幻觉（如 Gemini-3-Pro 的一致性得分大幅下降）。
*   **模型规模效应**：模型参数量越大，解耦价值与语义的能力越强，联合成功率（JSR）随规模显著提升。
*   **GRPO 优势**：在重写任务中，GRPO 比 DPO 和 SimPO 能更好地平衡双重目标，避免了 SFT 常见的过拟合和模式坍塌。

#### 7. 优点
*   **架构优雅**：通过解耦设计实现了“零样本”的个性化价值注入，无需为每个用户微调模型。
*   **训练高效**：采用 GRPO 算法，在保证性能的同时降低了训练资源需求。
*   **数据贡献**：VCR-45K 数据集的发布填补了细粒度价值对齐研究的空白。
*   **双向约束**：奖励函数同时考虑“改得像”和“不乱改”，有效解决了重写任务中的幻觉问题。

#### 8. 不足与局限
*   **依赖教师模型**：数据集标注和价值检测器高度依赖 GPT-4o 的理解能力，可能引入教师模型的固有偏见。
*   **价值体系单一**：目前主要基于 Schwartz 价值理论，虽然经典但可能无法涵盖所有文化背景下的伦理细微差别。
*   **算力细节缺失**：未披露具体硬件消耗，不便于其他研究者评估复现成本。
*   **实时性挑战**：推理时需要经过翻译、检测、重写多个步骤，可能增加端到端的响应延迟。

（完）
