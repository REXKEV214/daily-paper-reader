---
title: "Persona2Web: Benchmarking Personalized Web Agents for Contextual Reasoning with User History"
title_zh: Persona2Web：基于用户历史进行上下文推理的个性化 Web 智能体基准测试
authors: "Serin Kim, Sangam Lee, Dongha Lee"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17003v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 利用用户历史的个性化Web智能体
tldr: 针对当前Web智能体缺乏个性化能力、难以处理模糊指令的问题，本文提出了Persona2Web基准测试。该基准基于“澄清即个性化”原则，要求智能体通过分析用户长期历史记录来推断隐含偏好，从而解决模糊查询。它包含用户历史、模糊查询及推理感知评估框架，通过多维度实验揭示了实现个性化Web智能体的关键挑战。
selection_source: fresh_fetch
motivation: 现有的Web智能体无法根据用户历史记录推断隐含偏好，难以处理现实中常见的模糊指令。
method: 提出了Persona2Web基准，包含长期用户历史、模糊查询以及一套用于细粒度评估个性化推理能力的框架。
result: 通过对多种架构、模型和历史访问方案的广泛实验，揭示了当前智能体在个性化行为和上下文推理方面的局限性。
conclusion: Persona2Web为开发能够理解用户偏好并处理复杂、模糊任务的下一代个性化Web智能体提供了重要的评估基础。
---

## 摘要
大语言模型推动了 Web 智能体的发展，但目前的智能体仍缺乏个性化能力。由于用户很少详细说明其意图的每一个细节，实用的 Web 智能体必须能够通过推断用户偏好和上下文来解释模糊的查询。为了应对这一挑战，我们提出了 Persona2Web，这是首个在真实开放网络上评估个性化 Web 智能体的基准测试。它基于“澄清即个性化”（clarify-to-personalize）原则构建，要求智能体根据用户历史记录而非依赖显式指令来消除歧义。Persona2Web 包含：(1) 在长时间跨度内隐式揭示偏好的用户历史记录；(2) 要求智能体推断隐式用户偏好的模糊查询；(3) 一个支持对个性化进行细粒度评估的推理感知评估框架。我们在各种智能体架构、骨干模型、历史访问方案以及具有不同歧义水平的查询上进行了广泛的实验，揭示了个性化 Web 智能体行为中的关键挑战。为了可复现性，我们的代码和数据集已在 https://anonymous.4open.science/r/Persona2Web-73E8 公开。

## Abstract
Large language models have advanced web agents, yet current agents lack personalization capabilities. Since users rarely specify every detail of their intent, practical web agents must be able to interpret ambiguous queries by inferring user preferences and contexts. To address this challenge, we present Persona2Web, the first benchmark for evaluating personalized web agents on the real open web, built upon the clarify-to-personalize principle, which requires agents to resolve ambiguity based on user history rather than relying on explicit instructions. Persona2Web consists of: (1) user histories that reveal preferences implicitly over long time spans, (2) ambiguous queries that require agents to infer implicit user preferences, and (3) a reasoning-aware evaluation framework that enables fine-grained assessment of personalization. We conduct extensive experiments across various agent architectures, backbone models, history access schemes, and queries with varying ambiguity levels, revealing key challenges in personalized web agent behavior. For reproducibility, our codes and datasets are publicly available at https://anonymous.4open.science/r/Persona2Web-73E8.

---

## 论文详细总结（自动生成）

以下是对论文《Persona2Web: Benchmarking Personalized Web Agents for Contextual Reasoning with User History》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 Web 智能体（Web Agents）虽然在执行复杂任务上有所进步，但普遍缺乏**个性化能力**。在现实场景中，用户往往给出模糊的指令（如“买我常用的零食”），而目前的基准测试（Benchmark）多采用完全明确的指令，忽略了从用户历史中推断隐含偏好的需求。
*   **研究背景**：
    *   用户习惯于系统理解其隐含上下文，不愿提供详尽细节。
    *   现有基准测试缺乏真实的用户行为上下文，或仅提供显式的指令遵循，无法区分智能体是“真懂用户”还是“仅听指令”。
    *   **Persona2Web** 旨在填补这一空白，成为首个在真实开放网络环境下，基于用户历史进行上下文推理的个性化 Web 智能体基准。

### 2. 方法论：核心思想与技术细节
论文提出了 **“澄清即个性化”（Clarify-to-Personalize）** 原则，核心组件包括：
*   **现实用户历史构建**：
    *   **多阶段生成**：从人口统计学信息生成用户画像，定义 21 个领域的偏好。
    *   **事件种子与动作分解**：生成一年的模拟浏览历史（约 2000 条记录/人），包含搜索、访问、购买、预订和评价五类行为。
    *   **隐式编码**：偏好分散在历史记录中，而非显式陈述，要求智能体具备推理能力。
*   **模糊查询集（Ambiguous Query Sets）**：
    *   **Level 0**：完全明确（包含网站和偏好）。
    *   **Level 1**：掩盖网站（需从历史推断常用平台）。
    *   **Level 2**：掩盖网站和具体偏好（目标测试级别）。
*   **智能体架构增强**：
    *   在现有架构（如 AgentOccam, Browser-Use）基础上增加**个性化模块**，包含规划器（Planner）、检索器（Retriever，使用 Stella V5 模型）和生成器（Generator）。
    *   **访问方案**：分为“按需访问（On-demand）”和“执行前访问（Pre-execution）”两种模式。
*   **推理感知评估框架**：
    *   使用 LLM Judge（GPT-5-mini）基于结构化量表（Rubrics）评分。
    *   指标包括：**网站得分（Pweb）**、**偏好得分（Ppref）**、**意图满意度（IS）**和**成功率（SR）**。

### 3. 实验设计
*   **场景与环境**：真实的开放网络，涵盖 105 个网站和 21 个领域（如购物、旅游、医疗等）。
*   **Benchmark 规模**：50 个独立用户画像，每个画像对应多条不同歧义等级的查询。
*   **对比方法（骨干模型）**：
    *   **闭源模型**：o3, GPT-4o (GPT-4.1), Gemini-2.5-Flash。
    *   **开源模型**：Qwen3-80B-Instruct, Llama-3.3-70B。
*   **对比架构**：AgentOccam 和 Browser-Use。
*   **消融/变体实验**：不同历史访问方案（无历史 vs. 按需 vs. 预执行）、不同歧义等级（L0-L2）、显式画像 vs. 隐式历史。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 硬件型号或训练时长。
*   **模型调用**：主要依赖于 API 调用（OpenAI, Google, Meta, Qwen 系列）。数据生成使用了 GPT-4o，评估使用了 GPT-5-mini。
*   **检索器**：使用了 Stella V5 1.5B 稠密检索模型，并采用 FAISS 进行向量搜索。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种骨干模型、2 种智能体架构、3 种历史访问方案进行了交叉实验。
*   **充分性**：
    *   **元评估（Meta-evaluation）**：通过人工标注与 LLM 评分的相关性分析（Pearson/Spearman 系数），验证了评估框架的有效性。
    *   **错误分析**：对失败案例进行了细致分类（冗余访问、个性化幻觉、检索失败、利用失败）。
    *   **客观性**：实验涵盖了开源与闭源模型，并在真实动态网页上运行，具有较高的客观性和现实意义。

### 6. 主要结论与发现
*   **无历史必败**：在没有用户历史的情况下，所有智能体在模糊查询上的成功率均为 **0%**。
*   **性能瓶颈**：即使提供历史记录，最高成功率也仅为 **13%** 左右，显示出巨大的提升空间。
*   **模型差异**：闭源模型（o3, GPT-4o）显著优于开源模型；Browser-Use 架构由于保留了更完整的 DOM 信息，在个性化得分上通常优于 AgentOccam。
*   **歧义代价**：随着查询歧义增加（L0 -> L2），成功率大幅下降，证明智能体在“检索-推理-应用”这一链条上存在薄弱环节。
*   **利用难于检索**：即使检索到了正确历史，智能体也常因无法在正确的导航步骤应用信息而失败。

### 7. 优点与亮点
*   **首创性**：填补了 Web 智能体在“真实开放网络”+“长期隐式历史”+“个性化推理”结合部的基准空白。
*   **评估深度**：不只看最终结果，通过推理轨迹区分“导航失败”和“个性化失败”，提供了更细粒度的诊断。
*   **设计科学**：提出的“澄清即个性化”原则非常符合人机交互的实际逻辑。

### 8. 不足与局限
*   **数据合成性**：用户历史虽然模拟了真实行为，但本质上仍是合成数据，可能无法完全捕捉人类行为的极端随机性。
*   **环境动态性**：由于是在真实 Web 上测试，网页更新或维护可能导致实验结果在不同时间点存在波动（尽管这是开放域测试的固有属性）。
*   **隐私风险**：虽然实验使用合成数据，但论文指出实际部署此类智能体将面临严峻的隐私保护挑战。
*   **样本量限制**：50 个用户画像虽然多样，但对于覆盖全球复杂的 Web 交互模式而言，规模仍有待进一步扩大。

（完）
