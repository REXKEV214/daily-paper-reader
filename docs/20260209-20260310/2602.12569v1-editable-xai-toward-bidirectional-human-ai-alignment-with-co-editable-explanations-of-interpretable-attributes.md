---
title: "Editable XAI: Toward Bidirectional Human-AI Alignment with Co-Editable Explanations of Interpretable Attributes"
title_zh: 可编辑的可解释人工智能：通过可解释属性的协同编辑解释迈向双向人机对齐
authors: "Haoyang Chen, Jingwen Bai, Fang Tian, Brian Y Lim"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.12569v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 通过可协同编辑的解释实现双向人机对齐
tldr: 本研究针对传统可解释AI（XAI）仅提供只读解释而导致的人机知识失配问题，提出了“可编辑XAI”概念。通过开发CoExplain系统，将神经网络的强大表示能力与符号规则的直观推理相结合，允许用户通过编写规则来修正模型逻辑。该系统利用忠实代理决策树解释神经网络，并实现用户规则与模型参数的协同优化。实验证明，这种双向对齐机制显著增强了用户对模型的理解、控制力及人机一致性。
selection_source: fresh_fetch
motivation: 现有的XAI系统多为单向输出，当模型决策与用户领域知识冲突时，用户无法通过交互来修正模型以实现深度对齐。
method: 提出CoExplain框架，通过将用户编写的符号规则解析为等效神经网络图，并与代理决策树共同优化，实现可解释属性的协同编辑。
result: 43人的用户研究表明，CoExplain在提升理解度和模型对齐效果的同时，比手动编辑更易用，且耗时更短、编辑次数更少。
conclusion: 可编辑XAI通过赋予用户修正解释的能力，实现了双向的人机对齐，有效提升了AI系统的透明度与可控性。
---

## 摘要
虽然可解释人工智能 (XAI) 有助于用户理解 AI 决策，但领域知识的不匹配可能导致分歧。这种不一致性阻碍了理解，且由于解释通常是只读的，用户缺乏改进对齐的控制权。我们提出使 XAI 变得可编辑，允许用户编写规则以增强控制力，并通过主动学习的生成效应获得更深层次的理解。我们开发了 CoExplain，它利用神经网络进行通用表示，并利用符号规则对可解释属性进行直观推理。CoExplain 使用忠实的代理决策树来解释神经网络，将用户编写的规则解析为等效的神经网络图，并协同优化决策树。在一项用户研究 (N=43) 中，与只读 XAI 相比，CoExplain 和手动可编辑 XAI 提高了用户的理解能力和模型对齐度。CoExplain 更易于使用，编辑次数更少，耗时更短。这项工作为双向 AI 对齐贡献了可编辑 XAI，提升了理解力和控制力。

## Abstract
While Explainable AI (XAI) helps users understand AI decisions, misalignment in domain knowledge can lead to disagreement. This inconsistency hinders understanding, and because explanations are often read-only, users lack the control to improve alignment. We propose making XAI editable, allowing users to write rules to improve control and gain deeper understanding through the generation effect of active learning. We developed CoExplain, leveraging a neural network for universal representation and symbolic rules for intuitive reasoning on interpretable attributes. CoExplain explains the neural network with a faithful proxy decision tree, parses user-written rules as an equivalent neural network graph, and collaboratively optimizes the decision tree. In a user study (N=43), CoExplain and manually editable XAI improved user understanding and model alignment compared to read-only XAI. CoExplain was easier to use with fewer edits and less time. This work contributes Editable XAI for bidirectional AI alignment, improving understanding and control.

---

## 论文详细总结（自动生成）

这篇论文由新加坡国立大学的研究团队发表于 CHI 2026，探讨了如何通过“可编辑”的解释来实现人类与 AI 之间的双向对齐。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的可解释 AI（XAI）大多是“只读”的，用户只能被动接受 AI 的逻辑。当 AI 的决策逻辑与用户的领域知识不一致（失配）时，用户无法修正模型，这会导致信任缺失或自动化偏见。
*   **核心理念**：提出 **“可编辑 XAI”（Editable XAI）**。通过允许用户直接重写 AI 的推理规则，利用主动学习中的“生成效应”（Generation Effect），不仅让 AI 学习人类知识，也让用户在编辑过程中更深地理解 AI 行为，从而实现双向对齐。

### 2. 方法论：CoExplain 框架
CoExplain 是一个神经符号（Neuro-symbolic）框架，支持三种交互模式：
*   **Read（读取/蒸馏）**：将复杂的神经网络（黑盒）蒸馏为一棵忠实的代理决策树（白盒），以便用户直观理解全局逻辑。
*   **Write（编写/解析）**：用户在画布上修改决策树规则。系统通过解析器将这些符号规则转化为等效的神经网络结构（利用 Łukasiewicz 逻辑将“与/或”逻辑门映射为神经元操作），确保用户意图可被训练。
*   **Enhance（增强/优化）**：
    *   **阈值增强**：在保持树结构不变的情况下，通过反向传播微调数值边界。
    *   **拓扑增强**：允许 AI 重新组织树结构以提升准确率。
    *   **正则化约束**：引入“树编辑距离”（Tree Edit Distance, TED）作为损失函数的一部分，确保 AI 的自动优化不会偏离用户定义的初始结构太远。

### 3. 实验设计
*   **数据集/场景**：
    1.  **成人收入预测（Adult Income）**：通用知识背景。
    2.  **房价预测（House Price）**：通用知识背景。
    3.  **心脏病风险预测（Heart Disease）**：需要专业领域知识。
*   **基准对比（Benchmark）**：
    *   **Read-only XAI**：传统的只读解释。
    *   **Editable XAI**：手动编辑规则，无 AI 辅助增强。
    *   **CoExplain**：支持 AI 协同增强的可编辑解释。
*   **对齐设计**：研究者特意将数据分为“指南分布”（用户学习的背景知识）和“预训练分布”（AI 学习的数据），人为制造知识失配，以测试系统的对齐能力。

### 4. 资源与算力
*   **硬件设施**：论文明确提到了实验设备，包括一台配备 **NVIDIA H100 PCIe GPU (80GB 显存)** 和 **AMD EPYC 9654 96核处理器** 的工作站。
*   **软件实现**：使用 PyTorch 进行神经网络训练，Scikit-learn 处理决策树，Adam 优化器（学习率 1e-2）。

### 5. 实验数量与充分性
*   **样本量**：招募了 43 名来自不同专业背景的大学生（涵盖心理学、商业、医学、计算机等）。
*   **实验组设置**：采用受试者间设计（Between-subjects）对比三种 XAI 类型，每个用户完成 3 个场景的任务，共收集了数百次模拟决策数据。
*   **充分性评价**：实验设计严谨，包含了定量指标（忠实度、准确率、编辑距离、耗时）和定性访谈。通过控制变量（如知识分布偏移）客观地模拟了现实中人机知识冲突的场景，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **理解力提升**：与只读解释相比，可编辑解释（手动或协同）显著提高了用户预测 AI 行为的准确性（User-AI Faithfulness）。
*   **对齐效果**：CoExplain 在保持用户定义的逻辑结构（低 TED 距离）的同时，通过 AI 增强达到了接近最优的模型性能，平衡了“用户意图”与“数据准确性”。
*   **效率优势**：相比纯手动编辑，CoExplain 减少了 53% 的编辑时间，并显著减少了编辑迭代次数。
*   **心理效应**：用户更倾向于信任自己参与构建的规则，且 AI 对阈值的微调被认为是有价值的“建议”。

### 7. 优点
*   **双向对齐**：打破了 XAI 仅作为“输出”的局限，将其变为“输入+输出”的交互媒介。
*   **神经符号结合**：技术方案优雅，解决了符号逻辑（易读）与神经网络（高性能）之间的转换难题。
*   **用户赋权**：通过正则化控制 AI 的修改幅度，保证了用户在协作中的主导权。

### 8. 不足与局限
*   **数据类型限制**：目前仅适用于结构化（表格）数据，尚未扩展到图像、音频等非结构化数据。
*   **模型规模**：为了保证人类可读性，实验中使用的决策树深度较浅（4层），对于极其复杂的超大规模模型，如何进行模块化编辑仍是挑战。
*   **用户偏差风险**：如果用户持有错误的领域知识并强行修改模型，可能会导致 AI 性能下降或产生偏见，系统需要更强的安全护栏。
*   **任务复杂度**：实验场景相对简单，在极高维度的工业级特征空间中，用户可能难以手动构建有效的规则。

（完）
