---
title: "REMem: Reasoning with Episodic Memory in Language Agent"
title_zh: REMem：语言智能体中的情境记忆推理
authors: "Yiheng Shu, Saisri Padmaja Jonnalagedda, Xiang Gao, Bernal Jiménez Gutiérrez, Weijian Qi, Kamalika Das, Huan Sun, Yu Su"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13530v2"
tags: ["query:llm-memory"]
score: 10.0
evidence: 情境记忆系统与交互历史推理
tldr: 针对现有语言智能体在情境记忆方面缺乏时空关联和复杂推理能力的问题，本文提出 REMem 框架。该框架通过离线构建包含时间感知要点与事实的混合记忆图谱，并结合在线代理式检索器进行迭代推理，显著提升了智能体在回忆和跨事件推理任务上的表现，并增强了对不可回答问题的鲁棒性。
selection_source: fresh_fetch
motivation: 现有智能体记忆多为语义化，缺乏对交互历史中时空背景的建模以及跨事件的复杂推理能力。
method: 提出 REMem 框架，包含离线构建混合记忆图谱（关联时间感知要点与事实）和在线利用代理式检索器进行迭代推理两个阶段。
result: "在四个基准测试中，REMem 在情境回忆和推理任务上分别比 SOTA 系统提升了 3.4% 和 13.4%。"
conclusion: REMem 有效填补了语言智能体在情境记忆建模与推理方面的空白，为构建具备类人记忆能力的智能体提供了新方案。
---

## 摘要
人类擅长记忆时空背景下的具体经历，并能跨事件进行推理，即具备情境记忆（episodic memory）的能力。相比之下，语言智能体的记忆主要仍是语义性的，目前的智能体尚无法有效地回忆和推理交互历史。我们针对这一差距识别并形式化了情境回忆与推理的核心挑战，并观察到现有工作往往忽视了情境性、缺乏显式的事件建模，或者过度强调简单检索而非复杂推理。我们提出了 REMem，一个用于构建情境记忆并进行推理的两阶段框架：1) 离线索引，REMem 将经历转换为混合记忆图谱，灵活地链接具有时间感知能力的要点（gists）和事实；2) 在线推理，REMem 采用一个配备精心设计工具的智能体检索器，在记忆图谱上进行迭代检索。在四个情境记忆基准测试上的全面评估表明，REMem 的性能显著优于 Mem0 和 HippoRAG 2 等最先进的记忆系统，在情境回忆和推理任务上分别实现了 3.4% 和 13.4% 的绝对提升。此外，REMem 在处理无法回答的问题时也表现出更稳健的拒绝行为。

## Abstract
Humans excel at remembering concrete experiences along spatiotemporal contexts and performing reasoning across those events, i.e., the capacity for episodic memory. In contrast, memory in language agents remains mainly semantic, and current agents are not yet capable of effectively recollecting and reasoning over interaction histories. We identify and formalize the core challenges of episodic recollection and reasoning from this gap, and observe that existing work often overlooks episodicity, lacks explicit event modeling, or overemphasizes simple retrieval rather than complex reasoning. We present REMem, a two-phase framework for constructing and reasoning with episodic memory: 1) Offline indexing, where REMem converts experiences into a hybrid memory graph that flexibly links time-aware gists and facts. 2) Online inference, where REMem employs an agentic retriever with carefully curated tools for iterative retrieval over the memory graph. Comprehensive evaluation across four episodic memory benchmarks shows that REMem substantially outperforms state-of-the-art memory systems such as Mem0 and HippoRAG 2, showing 3.4% and 13.4% absolute improvements on episodic recollection and reasoning tasks, respectively. Moreover, REMem also demonstrates more robust refusal behavior for unanswerable questions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **REMem** 的框架，旨在提升语言智能体（Language Agents）在情境记忆（Episodic Memory）方面的回忆与推理能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：人类拥有“心理时间旅行”的能力，能精确回忆特定时空背景下的事件并进行跨事件推理。然而，目前的语言智能体记忆大多是**语义性**的（存储静态事实），缺乏对交互历史中**时空背景**（时间、地点、参与者、情感）的显式建模，导致其在处理复杂的时间逻辑、事件顺序及多步情境推理时表现不佳。
*   **研究动机**：现有的 RAG（检索增强生成）或图记忆系统（如 HippoRAG、Mem0）往往将信息去背景化，或仅进行简单的相似度检索，无法支持如“在某事件之前发生了什么”或“某行为持续了多久”等复杂的情境推理任务。

### 2. 方法论：REMem 框架
REMem 采用两阶段流程：**索引（Indexing）**与**代理式推理（Agentic Inference）**。
*   **核心思想**：构建一个结合了“要点（Gists）”和“事实（Facts）”的混合记忆图谱，并利用具备工具调用能力的智能体进行迭代检索。
*   **关键技术细节**：
    1.  **索引阶段**：
        *   **要点提取（Gist Extraction）**：将原始对话/事件转化为简洁的自然语言摘要，并解析相对时间为绝对日期。
        *   **事实提取（Fact Extraction）**：提取（主、谓、宾）三元组，并附加 Wikidata 风格的时间限定符（如发生点、开始/结束时间）。
        *   **混合图构建**：图节点包含“要点节点”和“短语节点”。要点节点代表上下文，短语节点代表概念。通过“上下文边”连接同一块的要点与事实，通过“同义词边”连接语义相近的要点。
    2.  **推理阶段（Agentic Inference）**：
        *   采用 **ReAct 模式**的智能体，配备专门工具：
            *   `semantic_retrieve` / `lexical_retrieve`：用于初步定位种子节点。
            *   `find_gist_contexts` / `find_entity_contexts`：在图上探索邻居，并支持**时间范围过滤**（如查找特定日期之前的事件）。
            *   `output_answer`：完成推理后输出结果。

### 3. 实验设计
*   **数据集/场景**：
    *   **情境回忆任务**：LoCoMo（合成对话）、REALTALK（真实人类对话）。
    *   **情境推理任务**：Complex-TR（复杂时间阅读理解）、Test of Time（时间推理基准）。
*   **对比方法**：
    *   **强嵌入模型**：Qwen3-Embedding-8B, NV-Embed-v2。
    *   **结构化记忆系统**：Mem0, Graphiti, HippoRAG 2。
    *   **提示词方法**：TISER（时间轴自我反思）。
    *   **基准参考**：Oracle Message（理想检索）、Full-Context（全文本输入）。

### 4. 资源与算力
*   **硬件环境**：服务器配备双 AMD EPYC 7643 CPU，**4 张 NVIDIA A100-SXM4-80GB GPU**，1TB 系统内存。
*   **效率数据**：
    *   **索引耗时**：LoCoMo 数据集约 3604 秒，Complex-TR 约 378 秒。
    *   **推理耗时**：平均每条查询 4.3 秒（LoCoMo）至 12.6 秒（Complex-TR）。
    *   **模型使用**：默认使用 GPT-4.1-mini 作为推理 LLM，NV-Embed-v2 作为嵌入模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖 4 个主流基准测试，总计处理了数千条查询（如 LoCoMo 1986 条，Test of Time 2800 条）。
*   **消融实验**：非常充分。分别验证了“仅要点”、“仅事实”、“无同义词边”、“无特定检索工具”等多种变体，证明了混合结构和代理式检索的必要性。
*   **客观性**：引入了 **LLM-as-a-judge** 与**人工评估**（100 样本）的对比，相关性达 0.827，证明了自动评估的可靠性。同时评估了“拒绝回答”的能力，体现了实验的全面性。

### 6. 主要结论与发现
*   **性能领先**：REMem 在回忆任务上提升 3.4%，在推理任务上提升 **13.4%**。在 Test of Time 任务中，它是唯一 EM（精确匹配）得分超过 90% 的方法。
*   **推理优势**：迭代式的代理推理（REMem-I）在复杂逻辑任务上远优于单步检索（REMem-S）。
*   **鲁棒性**：在面对不可回答的问题时，REMem 的拒绝准确率（Precision 73.3%）显著高于其他系统，有效减少了幻觉。
*   **协同效应**：要点（Gists）提供了核心情境，而事实（Facts）在跨会话的多跳推理中起到了关键的锚点作用。

### 7. 优点
*   **混合建模**：巧妙结合了非结构化的摘要（要点）和结构化的三元组（事实），兼顾了语义丰富度和逻辑严密性。
*   **时间感知**：显式地将时间维度集成到图结构和检索工具中，支持复杂的时空过滤。
*   **智能体化检索**：将检索过程视为一个主动推理过程，能够根据中间结果调整搜索策略。

### 8. 不足与局限
*   **错误类型**：分析显示仍存在“选择/接地错误”（如在多个相似兴趣中选错最爱）和“时间窗口误匹配”。
*   **成本问题**：相比简单 RAG，迭代式的智能体推理需要多次 LLM 调用，增加了 Token 消耗和响应延迟。
*   **应用限制**：目前主要在离线批处理索引上测试，对于实时流式（Streaming）记忆构建的工程挑战尚未深入探讨。

（完）
