---
title: "Query as Anchor: Scenario-Adaptive User Representation via Large Language Model"
title_zh: 以查询为锚点：通过大语言模型实现场景自适应的用户表示
authors: "Jiahao Yuan, Yike Xu, Jinyong Wen, Baokun Wang, Ziyi Gao, Xiaotong Lin, Yun Liu, Xing Fu, Yu Cheng, Yongchao Liu, Weiqiang Wang, Zhongle Xie"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14492v2"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 基于大语言模型的场景自适应用户表示与建模
tldr: 本研究针对工业级用户表示学习中静态嵌入难以兼顾通用性与任务敏感性的挑战，提出了Query-as-Anchor框架。该框架将用户建模从静态编码转向动态的查询感知合成，通过构建大规模预训练数据集UserU，并结合分层编码器与双塔大语言模型（LLM）实现查询感知的用户表示。此外，引入聚类软提示微调以对齐特定业务逻辑，并利用KV缓存加速推理。实验证明该方法在10项工业基准测试中达到SOTA，并在支付宝实际场景中成功部署。
selection_source: fresh_fetch
motivation: 解决工业场景下静态用户嵌入难以适应多变下游任务以及多源异构数据带来的噪声和模态冲突问题。
method: 提出一种基于LLM的查询感知框架，通过分层编码器、联合对比-自回归优化以及聚类软提示微调实现动态用户建模。
result: 在10个支付宝工业基准测试中取得SOTA性能，并在实际生产环境的A/B测试中验证了其显著的业务增益和部署效率。
conclusion: 该研究通过将查询作为锚点，成功实现了兼具高性能、高适应性且易于工业部署的动态用户表示学习方案。
---

## 摘要
工业级用户表示学习需要平衡鲁棒的通用性与敏锐的任务敏感性。然而，现有范式主要生成静态且任务无关的嵌入，难以在统一的向量空间内调和下游场景的不同需求。此外，异构多源数据引入了固有的噪声和模态冲突，导致表示性能下降。我们提出了 Query-as-Anchor 框架，将用户建模从静态编码转变为动态的查询感知合成。为了赋予大语言模型（LLM）深度的用户理解能力，我们首先构建了 UserU，这是一个将多模态行为序列与用户理解语义对齐的工业级预训练数据集；我们的 Q-Anchor Embedding 架构通过联合对比-自回归优化，将分层从粗到精的编码器集成到双塔 LLM 中，以实现查询感知的用户表示。为了弥合通用预训练与专业业务逻辑之间的差距，我们进一步引入了基于聚类的软提示微调（Cluster-based Soft Prompt Tuning），以强化判别性的潜在结构，从而有效地将模型注意力与特定场景的模态对齐。在部署方面，将查询锚定在序列末端可以实现 KV 缓存加速推理，且增量延迟极低。在 10 个支付宝工业基准测试上的评估显示，该方法具有持续的 SOTA 性能、强大的可扩展性和高效的部署能力。在支付宝生产系统中跨两个真实场景的大规模在线 A/B 测试进一步验证了其实际有效性。我们的代码已准备开源，可在以下地址获取：https://github.com/JhCircle/Q-Anchor。

## 速览
**TLDR**：本研究针对工业级用户表示学习中静态嵌入难以兼顾通用性与任务敏感性的挑战，提出了Query-as-Anchor框架。该框架将用户建模从静态编码转向动态的查询感知合成，通过构建大规模预训练数据集UserU，并结合分层编码器与双塔大语言模型（LLM）实现查询感知的用户表示。此外，引入聚类软提示微调以对齐特定业务逻辑，并利用KV缓存加速推理。实验证明该方法在10项工业基准测试中达到SOTA，并在支付宝实际场景中成功部署。 \
**Motivation**：解决工业场景下静态用户嵌入难以适应多变下游任务以及多源异构数据带来的噪声和模态冲突问题。 \
**Method**：提出一种基于LLM的查询感知框架，通过分层编码器、联合对比-自回归优化以及聚类软提示微调实现动态用户建模。 \
**Result**：在10个支付宝工业基准测试中取得SOTA性能，并在实际生产环境的A/B测试中验证了其显著的业务增益和部署效率。 \
**Conclusion**：该研究通过将查询作为锚点，成功实现了兼具高性能、高适应性且易于工业部署的动态用户表示学习方案。

---

## Abstract
Industrial-scale user representation learning requires balancing robust universality with acute task-sensitivity. However, existing paradigms primarily yield static, task-agnostic embeddings that struggle to reconcile the divergent requirements of downstream scenarios within unified vector spaces. Furthermore, heterogeneous multi-source data introduces inherent noise and modality conflicts, degrading representation. We propose Query-as-Anchor, a framework shifting user modeling from static encoding to dynamic, query-aware synthesis. To empower Large Language Models (LLMs) with deep user understanding, we first construct UserU, an industrial-scale pre-training dataset that aligns multi-modal behavioral sequences with user understanding semantics, and our Q-Anchor Embedding architecture integrates hierarchical coarse-to-fine encoders into dual-tower LLMs via joint contrastive-autoregressive optimization for query-aware user representation. To bridge the gap between general pre-training and specialized business logic, we further introduce Cluster-based Soft Prompt Tuning to enforce discriminative latent structures, effectively aligning model attention with scenario-specific modalities. For deployment, anchoring queries at sequence termini enables KV-cache-accelerated inference with negligible incremental latency. Evaluations on 10 Alipay industrial benchmarks show consistent SOTA performance, strong scalability, and efficient deployment. Large-scale online A/B testing in Alipay's production system across two real-world scenarios further validates its practical effectiveness. Our code is prepared for public release and will be available at: https://github.com/JhCircle/Q-Anchor.

---

## 论文详细总结（自动生成）

### 论文总结：Query as Anchor: Scenario-Adaptive User Representation via Large Language Model

#### 1. 核心问题与整体含义
*   **研究动机**：在工业级应用（如支付宝）中，用户表示学习面临三大挑战：
    1.  **场景自适应性差**：现有的静态嵌入（Static Embeddings）是任务无关的，难以同时满足风控、营销、促活等截然不同的下游需求。
    2.  **模态与语义鸿沟**：用户行为数据（账单、搜索、点击）是稀疏且异构的，与 LLM 预训练所用的自然语言数据存在巨大差异。
    3.  **工业部署压力**：多场景下为每个任务维护独立模型成本极高，且 LLM 推理延迟难以满足实时性要求。
*   **核心含义**：本文提出了 **Query-as-Anchor (Q-Anchor)** 框架，将用户建模从“静态编码”转向“动态查询感知合成”，实现了一套表示即可通过不同查询（Query）适配多种业务场景。

#### 2. 方法论：核心思想与技术细节
*   **UserU 预训练数据集**：
    *   **行为预测 ($D_{future}$)**：将用户历史行为与未来行为摘要配对，构建“行为-行为”对齐。
    *   **合成问答 ($D_{uqa}$)**：利用 LLM 生成 72 个生活主题相关的 QA 对，并引入“自我反思”机制确保答案与行为日志的一致性。
*   **Q-Anchor 架构**：
    *   **分层编码器**：采用“事件级 $\rightarrow$ 模态级 $\rightarrow$ 用户级”的粗到精结构，将异构信号（账单、小程序、搜索等）压缩为分层 Token。
    *   **双塔 LLM 训练**：
        *   **Anchor Tower**：输入“分层用户 Token + 场景查询”，生成查询感知的用户嵌入。
        *   **Semantic Tower**：将目标答案编码为语义向量。
    *   **联合优化目标**：结合了**对比学习 (InfoNCE)**（带 Margin 掩码以过滤噪声负样本）和**生成式目标 (Next-Token Prediction, NTP)**，确保嵌入既具判别性又富含语义。
*   **场景适配与加速**：
    *   **聚类软提示微调 (Soft Prompt Tuning)**：在不改变底座模型的情况下，通过少量可学习的 Prompt Token 将模型注意力引导至特定业务逻辑。
    *   **KV-Cache 加速**：将用户行为作为 Prefix 缓存，不同查询仅需计算增量，大幅降低多场景推理延迟。

#### 3. 实验设计
*   **数据集与场景**：在支付宝 10 个真实工业基准上进行测试，涵盖三大领域：
    1.  **用户参与度**：社区活跃、演唱会点击、登录预测、蚂蚁森林参与。
    2.  **风险控制**：欺诈检测、洗钱检测。
    3.  **营销敏感度**：外卖兴趣、品牌敏感度、大促敏感度、性价比敏感度。
*   **对比方法 (Baselines)**：
    *   **通用文本嵌入**：Qwen2.5-0.5B, Qwen3-Embedding, Llama-Embed-Nemotron-8B 等。
    *   **专门用户表示模型**：MSDP, One4all, CPC, FOUND。
*   **在线测试**：在支付宝 IVR 语音外呼和信用逾期风险识别两个场景进行了大规模 A/B 测试。

#### 4. 资源与算力
*   **预训练**：使用 **64 张 A100-80G GPU**，进行 50,000 步训练，全局 Batch Size 为 2048。
*   **微调与推理**：软提示微调和推理评估在**单张 A100-80G** 上完成。
*   **模型规模**：主实验采用 Qwen2.5-0.5B-Instruct 作为底座，同时也对比了 1.5B 和 3B 版本。

#### 5. 实验数量与充分性
*   **实验规模**：离线评估涵盖 10 个任务，每个任务约 50 万样本；在线 A/B 测试持续两周。
*   **消融实验**：非常充分。分别针对模态 Token 的必要性、训练损失函数（对比 vs 生成）、预训练的作用进行了拆解。
*   **扩展性分析**：研究了预训练数据规模（20M 到 100M）、模型参数量、Prompt Token 数量及微调步数对性能的影响。
*   **客观性**：通过 t-SNE 和 PCA 可视化展示了嵌入空间的聚类效果，并提供了注意力权重的变化分析，证明了方法的有效性。

#### 6. 主要结论与发现
*   **SOTA 性能**：Q-Anchor 在所有 10 个任务中均显著优于现有最强基准，平均 AUC 达到 0.8225。
*   **Scaling Paradox（规模悖论）**：在用户嵌入任务中，0.5B 模型表现优于 1.5B 和 3B。分析发现大模型在 Embedding 任务中存在梯度衰减问题，小模型反而具有更好的精度-效率平衡。
*   **注意力重定向**：软提示微调能有效改变模型对不同模态的关注点（例如：预测外卖兴趣时，模型对“账单”模态的注意力提升了 26%）。
*   **业务增益**：在线 A/B 测试显示，该模型使外呼转化率提升 12.5%，风险识别 KS 提升 1.96%。

#### 7. 优点
*   **动态适配**：通过 Query 解决了静态向量无法表达用户多面性的问题。
*   **工业实用性强**：KV-Cache 共享机制使得多场景部署的增量成本极低。
*   **端到端对齐**：UserU 数据集通过 LLM 合成 QA，巧妙解决了行为数据缺乏语义标签的问题。

#### 8. 不足与局限
*   **模型规模瓶颈**：论文观察到模型参数增加并未带来性能提升，这限制了利用超大规模模型（如 70B+）进一步突破的可能性。
*   **数据合成依赖**：模型性能高度依赖于 LLM 合成数据的质量，若合成数据存在偏见或幻觉，可能影响表示的准确性。
*   **应用领域局限**：实验主要集中在金融与支付生态，对于社交媒体或长视频等兴趣极度发散的场景，其泛化能力尚需验证。

（完）
