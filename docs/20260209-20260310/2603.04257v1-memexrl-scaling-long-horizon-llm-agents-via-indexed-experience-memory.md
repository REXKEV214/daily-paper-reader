---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程大语言模型智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 针对长程LLM智能体的索引经验记忆机制
tldr: 针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在长程任务中的表现。
method: 提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 框架优化智能体的读写与检索决策。
result: 在挑战性的长程任务中，Memex 智能体在保持较小工作上下文的同时，显著提高了任务成功率。
conclusion: 索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体的处理能力。
---

## 摘要
大语言模型（LLM）智能体在处理长程任务时，从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理过程很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以利用。现有的解决方案通常通过截断或滚动摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，这是一种索引经验记忆机制，它在不丢弃证据的情况下压缩上下文。Memex 维护一个由简洁的结构化摘要和稳定索引组成的紧凑工作上下文，同时将高保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时解引用（dereference）索引，并恢复当前子目标所需的精确历史证据。我们利用强化学习框架 MemexRL 优化了读写行为，采用针对上下文预算下索引记忆使用而定制的奖励塑造（reward shaping），使智能体学会总结什么、归档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持有效上下文计算量随历史增长而受限的同时，通过有限的解引用操作来维持决策质量的潜力。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减少工作上下文使用的同时，提高了任务成功率。

## 速览
**TLDR**：针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。 \
**Motivation**：现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在长程任务中的表现。 \
**Method**：提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 框架优化智能体的读写与检索决策。 \
**Result**：在挑战性的长程任务中，Memex 智能体在保持较小工作上下文的同时，显著提高了任务成功率。 \
**Conclusion**：索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体的处理能力。

---

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Memex(RL)** 的新框架，旨在解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时面临的上下文窗口限制问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体在执行长达数十或数百步的任务时，会产生大量的观察结果、工具输出和推理轨迹。现有的上下文管理方法（如截断或滚动摘要）是“有损”的，会丢弃或模糊关键的原始证据，导致智能体在后续步骤中无法精确回溯早期信息。
*   **研究动机**：作者认为，智能体需要一种既能保持上下文简洁，又不丢失任何历史细节的机制。受人类使用笔记、索引和外部文档的启发，论文提出了将“紧凑的在场摘要”与“高保真的外部存储”分离的思路。

### 2. 方法论：核心思想与关键技术
*   **核心思想（Memex）**：引入**索引经验记忆（Indexed Experience Memory）**。它将工作上下文压缩为包含“稳定索引（Stable Indices）”的结构化摘要，而将完整的原始交互内容存储在外部键值对数据库中。
*   **关键操作**：
    *   `CompressExperience`：智能体自主决定何时压缩当前上下文。它生成一个索引映射表（Index Map），并将详细内容（如代码、长日志）存入外部存储。
    *   `ReadExperience(index)`：当需要特定证据时，智能体通过解引用（Dereferencing）索引，将精确的原始数据重新注入当前上下文。
*   **强化学习框架（MemexRL）**：
    *   **算法**：基于 GRPO（组相对策略优化）算法。
    *   **奖励塑造（Reward Shaping）**：奖励函数由四部分组成：任务成功奖励（$R_{task}$）、上下文溢出惩罚（$P_{context}$）、冗余工具调用惩罚（$P_{redundancy}$）以及格式错误惩罚（$P_{format}$）。
    *   **分段轨迹处理**：将一次压缩前后的轨迹视为独立片段进行训练，但共享最终的任务奖励，从而解决长程信用分配（Credit Assignment）问题。
    *   **软触发机制**：系统不强制压缩，而是通过在观察中加入“上下文状态”提示，训练智能体自主学习压缩时机。

### 3. 实验设计
*   **场景与数据集**：使用了修改版的 **ALFWorld**（一个交互式家庭环境基准）。
*   **难度增强**：为了测试记忆能力，作者对 ALFWorld 进行了加难处理：隐藏了可用的命令列表、隐藏了初始观察、限制了“查看（look）”动作的次数（仅限一次），并强制对摘要进行截断。
*   **对比基准**：主要对比了经过 MemexRL 训练的智能体与未经 RL 训练（仅靠 Prompt 指引）的智能体在相同任务下的表现。

### 4. 资源与算力
*   **模型**：使用 **Qwen3-30B-A3B-Thinking-2507**（一个拥有 30B 参数、活跃参数约 3B 的 MoE 模型）。
*   **训练细节**：
    *   使用 **Slime** 强化学习框架。
    *   采用 **INT4 量化**进行推理采样，**QAT（量化感知训练）**进行反向传播。
    *   Batch Size 为 32，GRPO 组大小为 8。
*   **算力说明**：文中未明确给出具体的 GPU 型号（如 A100 或 H100）和总训练时长，但提到了使用量化技术来优化显存和计算效率。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 **3553 个任务**。
*   **充分性**：
    *   展示了训练过程中的成功率曲线和惩罚项下降曲线，证明了 RL 的收敛性。
    *   进行了消融性质的对比（训练前 vs 训练后），并分析了工具调用频率的变化。
    *   提供了理论分析（Proposition 1 & 2），证明了该机制在理论上可以保持决策质量且上下文边界受限。
    *   **局限性**：实验主要集中在修改后的 ALFWorld 这一类任务上，缺乏在 Web 浏览、软件工程等其他长程任务领域的广泛验证。

### 6. 主要结论与发现
*   **性能提升**：MemexRL 将任务成功率从 **24.2% 提升至 85.6%**。
*   **效率优化**：在成功率大幅提升的同时，峰值工作上下文长度减少了约 **43%**（从约 1.7 万 token 降至 0.96 万 token）。
*   **行为转变**：经过训练，智能体学会了减少不必要的压缩次数，并显著增加了 `ReadExperience` 的调用次数（从平均 1 次增加到 6-7 次），表明它学会了“按需取用”记忆。

### 7. 优点
*   **低损耗压缩**：通过索引而非模糊摘要来保留证据，解决了 RAG 检索不精准和摘要丢失细节的问题。
*   **自主管理**：将记忆操作视为一种“工具调用”，使上下文管理成为智能体可学习的技能，而非硬编码的规则。
*   **理论支撑**：论文为长程智能体的计算复杂度提供了数学上的边界分析。

### 8. 不足与局限
*   **环境单一**：虽然 ALFWorld 经过了加难处理，但其动作空间和状态空间相对受限，尚未在更复杂的真实世界 API 调用场景中验证。
*   **模型依赖**：该方法依赖于具有较强初始推理能力（Thinking）和工具调用能力的基座模型（如 Qwen3-30B），在较弱的模型上可能难以起步。
*   **冷启动挑战**：RL 训练前需要高质量的演示数据进行热启动，这在某些新领域可能难以获取。

（完）
