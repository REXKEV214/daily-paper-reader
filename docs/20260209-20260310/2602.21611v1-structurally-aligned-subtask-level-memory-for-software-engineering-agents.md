---
title: Structurally Aligned Subtask-Level Memory for Software Engineering Agents
title_zh: 面向软件工程智能体的结构化对齐子任务级记忆
authors: "Kangning Shen, Jingyuan Zhang, Chenxi Sun, Wencong Zeng, Yang Yue"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21611v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 智能体长程推理的记忆机制
tldr: "本研究针对软件工程（SWE）智能体在长程推理中记忆粒度过粗的问题，指出传统的实例级记忆在处理表面相似但逻辑迥异的任务时存在检索偏差。为此，作者提出了“结构化对齐的子任务级记忆”方法，将记忆的存取与智能体的功能分解对齐。实验表明，该方法在 SWE-bench Verified 上显著提升了性能，平均 Pass@1 提高 4.7%，证明了细粒度经验积累对复杂软件开发任务的有效性。"
selection_source: fresh_fetch
motivation: 现有的实例级记忆机制因粒度不匹配，在处理描述相似但推理逻辑不同的任务阶段时容易产生错误的检索引导。
method: 提出结构化对齐的子任务级记忆，将记忆的存储、检索和更新过程与智能体的功能分解结构进行深度对齐。
result: "在 SWE-bench Verified 实验中，该方法在多种模型上均优于基准，平均 Pass@1 提升 4.7 个百分点，且随交互步数增加优势更明显。"
conclusion: 将记忆管理细化至子任务级别，能更有效地利用过去经验来辅助软件工程智能体处理复杂的长程推理任务。
---

## 摘要
大语言模型（LLMs）在作为自主软件工程（SWE）智能体方面展现出了巨大潜力。近期的研究进一步探索了通过记忆机制增强这些智能体，以支持长程推理。然而，这些方法通常在粗粒度的实例级别运行，将整个问题解决过程视为存储和检索的原子单位。我们通过实证研究证明，实例级记忆存在根本性的粒度不匹配问题，当具有相似表面描述的任务在特定阶段需要不同的推理逻辑时，会导致错误的检索。为解决这一问题，我们提出了结构化对齐的子任务级记忆（Structurally Aligned Subtask-Level Memory），该方法将记忆的存储、检索和更新与智能体的功能分解相对齐。在 SWE-bench Verified 上的广泛实验表明，我们的方法在多种骨干模型上均一致优于原始智能体和强大的实例级记忆基线，平均 Pass@1 较原始智能体提升了 4.7 个百分点（例如，在 Gemini 2.5 Pro 上提升了 6.8 个百分点）。性能增益随交互步骤的增加而增长，表明利用过往经验有助于复杂软件工程任务中的长程推理。

## Abstract
Large Language Models (LLMs) have demonstrated significant potential as autonomous software engineering (SWE) agents. Recent work has further explored augmenting these agents with memory mechanisms to support long-horizon reasoning. However, these approaches typically operate at a coarse instance granularity, treating the entire problem-solving episode as the atomic unit of storage and retrieval. We empirically demonstrate that instance-level memory suffers from a fundamental granularity mismatch, resulting in misguided retrieval when tasks with similar surface descriptions require distinct reasoning logic at specific stages. To address this, we propose Structurally Aligned Subtask-Level Memory, a method that aligns memory storage, retrieval, and updating with the agent's functional decomposition. Extensive experiments on SWE-bench Verified demonstrate that our method consistently outperforms both vanilla agents and strong instance-level memory baselines across diverse backbones, improving mean Pass@1 over the vanilla agent by +4.7 pp on average (e.g., +6.8 pp on Gemini 2.5 Pro). Performance gains grow with more interaction steps, showing that leveraging past experience benefits long-horizon reasoning in complex software engineering tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Structurally Aligned Subtask-Level Memory for Software Engineering Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的软件工程（SWE）智能体记忆机制通常采用**实例级（Instance-level）**粒度，即将整个问题的解决过程作为一个原子单位进行存储和检索。
*   **研究动机**：作者发现实例级记忆存在“粒度不匹配”问题。
    *   **误导性检索**：表面描述相似的任务（如“修复登录按钮”与“修复登录超时”）在特定阶段可能需要完全不同的推理逻辑（前端 UI vs 后端服务）。
    *   **经验浪费**：全球差异较大的任务可能在局部子任务（如更新事件监听器）上共享可复用的技能，但实例级检索因全局相似度低而无法提取这些经验。
*   **整体含义**：论文旨在通过将记忆操作与智能体的**功能分解（子任务级）**相对齐，实现更精确的经验复用。

### 2. 方法论
核心思想是**结构化对齐的子任务级记忆**，将记忆的存取与智能体的推理阶段深度绑定。
*   **子任务建模**：将推理过程动态划分为四个功能类别（$Z$）：`ANALYZE`（分析）、`REPRODUCE`（复现）、`EDIT`（编辑）、`VERIFY`（验证）。
*   **结构化记忆状态**：记忆条目存储为三元组 $(z, d, e)$：
    *   $z$：功能类别标签。
    *   $d$：局部意图描述（包含目标和关键词）。
    *   $e$：抽象经验（从原始轨迹中提取的动作指南或模式）。
*   **关键技术流程**：
    1.  **检索（Retrieval）**：采用两阶段策略。首先根据当前子任务类别 $z$ 进行**硬过滤**（排除跨阶段噪声），然后在过滤后的集合中进行**语义匹配**。
    2.  **执行（Execution）**：将检索到的经验注入上下文，引导智能体执行当前子任务。
    3.  **更新（Update）**：子任务完成后，使用 LLM 算子从原始轨迹中**提取抽象经验**，过滤掉特定仓库的噪声（如具体文件路径），仅保留可迁移的逻辑，并存入记忆库。

### 3. 实验设计
*   **数据集**：**SWE-bench Verified**（包含 500 个真实的 GitHub 问题），这是目前评估 SWE 智能体最严苛的基准之一。
*   **评价指标**：Pass@1（单次尝试解决问题的成功率），包括平均值和 Best@3。
*   **对比方法**：
    *   **Vanilla Agent**：不带记忆的原始智能体（基于 Mini SWE Agent 支架）。
    *   **Instance-level Memory**：复现了 Reasoning-Bank 方法，采用全局相似度检索实例级摘要。
*   **骨干模型**：评估了四种不同能力的模型：Gemini 2.5 Flash/Pro、Claude 3.7/4.0 Sonnet。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要基于大语言模型 API（Gemini 和 Claude 系列）进行推理和智能体交互，不涉及模型参数的微调（Fine-tuning），因此算力消耗主要体现为 API 调用成本和推理时间。作者提到在实验中保持了“预算中性”，即子任务提取的开销也计入总步数限制内。

### 5. 实验数量与充分性
*   **实验规模**：在 500 个任务实例上进行了测试，每个配置运行 3 次独立随机种子实验以减少随机性影响。
*   **消融实验**：非常充分，涵盖了：
    *   **结构化提示的影响**：证明增益来自记忆而非仅仅是工作流约束。
    *   **类别隔离的必要性**：对比了全局检索与类别过滤检索。
    *   **抽象的必要性**：对比了存储抽象经验与存储原始轨迹。
*   **客观性与公平性**：使用了统一的智能体支架（Mini SWE Agent）和贪婪解码策略，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：子任务级记忆在所有模型上均取得一致增益，平均 Pass@1 提升 **4.7%**，在 Gemini 2.5 Pro 上最高提升 **6.8%**。
*   **长程推理优势**：随着交互步数增加，性能增益愈发显著。在“困难”任务（步骤 > 28）上，成功率从 35.5% 提升至 44.2%（+8.7%）。
*   **克服干扰**：相比实例级记忆在 Claude 模型上出现的性能波动，子任务级记忆表现出更好的稳定性，有效过滤了跨阶段的无关噪声。
*   **在线学习效应**：随着记忆库从稀疏变稠密，智能体在任务流后期表现出明显的“知识迁移加速”趋势。

### 7. 优点（亮点）
*   **粒度创新**：敏锐捕捉到了 SWE 任务中“全局相似度”与“局部逻辑”之间的脱节问题。
*   **结构化设计**：通过类别过滤（Category Filter）将非结构化的记忆检索转变为结构化对齐的过程，极大地降低了检索噪声。
*   **即插即用**：无需参数微调，通过上下文学习（ICL）即可实现智能体的自我进化。
*   **鲁棒性强**：在不同家族（Google, Anthropic）和不同规模（Flash, Pro）的模型上均表现出泛化性。

### 8. 不足与局限
*   **固定分类风险**：预定义的四个子任务类别（分析、复现、编辑、验证）可能无法涵盖所有复杂的软件工程场景。
*   **推理开销**：每个子任务后的经验提取步骤增加了额外的 Token 消耗和推理延迟。
*   **错误传播**：如果智能体在初始阶段对子任务意图（Intent）的预测出现偏差，可能会检索到错误的经验，导致连锁反应。
*   **冷启动问题**：在任务流初期，由于记忆库为空，智能体无法获得帮助，甚至可能因检索尝试产生微小的性能开销。

（完）
