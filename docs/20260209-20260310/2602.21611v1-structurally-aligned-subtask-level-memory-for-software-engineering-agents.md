---
title: Structurally Aligned Subtask-Level Memory for Software Engineering Agents
title_zh: 面向软件工程智能体的结构化对齐子任务级记忆
authors: "Kangning Shen, Jingyuan Zhang, Chenxi Sun, Wencong Zeng, Yang Yue"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21611v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 用于智能体长程推理的子任务级记忆机制
tldr: "本研究针对软件工程（SWE）智能体在长程推理中记忆粒度过粗的问题，指出传统的实例级记忆会导致检索误导。为此，提出了一种“结构化对齐的子任务级记忆”方法，通过将记忆的存取与智能体的功能分解相对齐，实现更精准的经验复用。在 SWE-bench Verified 上的实验表明，该方法显著提升了不同大模型底座的性能，平均 Pass@1 提高 4.7%，证明了细粒度记忆对复杂软件开发任务的有效性。"
selection_source: fresh_fetch
motivation: 现有的实例级记忆机制因粒度不匹配，在处理描述相似但逻辑不同的任务阶段时容易产生错误的检索引导。
method: 提出结构化对齐的子任务级记忆方法，将记忆的存储、检索和更新过程与智能体的功能分解结构深度对齐。
result: "在 SWE-bench Verified 基准测试中，该方法使平均 Pass@1 提升了 4.7 个百分点，且在 Gemini 2.5 Pro 上提升达 6.8 个百分点。"
conclusion: 将记忆管理细化至子任务级别，能更有效地利用历史经验来辅助软件工程智能体处理复杂的长程推理任务。
---

## 摘要
大语言模型（LLMs）在作为自主软件工程（SWE）智能体方面展现出了巨大潜力。近期的研究进一步探索了通过记忆机制增强这些智能体，以支持长程推理。然而，这些方法通常在粗粒度的实例级别运行，将整个问题解决过程视为存储和检索的原子单位。我们通过实证研究证明，实例级记忆存在根本性的粒度不匹配问题，当具有相似表面描述的任务在特定阶段需要不同的推理逻辑时，会导致错误的检索。为解决这一问题，我们提出了结构化对齐的子任务级记忆（Structurally Aligned Subtask-Level Memory），该方法将记忆的存储、检索和更新与智能体的功能分解相对齐。在 SWE-bench Verified 上的广泛实验表明，我们的方法在多种骨干模型上均一致优于原始智能体和强大的实例级记忆基线，平均 Pass@1 较原始智能体提升了 4.7 个百分点（例如，在 Gemini 2.5 Pro 上提升了 6.8 个百分点）。性能增益随交互步骤的增加而增长，表明利用过往经验有助于复杂软件工程任务中的长程推理。

## Abstract
Large Language Models (LLMs) have demonstrated significant potential as autonomous software engineering (SWE) agents. Recent work has further explored augmenting these agents with memory mechanisms to support long-horizon reasoning. However, these approaches typically operate at a coarse instance granularity, treating the entire problem-solving episode as the atomic unit of storage and retrieval. We empirically demonstrate that instance-level memory suffers from a fundamental granularity mismatch, resulting in misguided retrieval when tasks with similar surface descriptions require distinct reasoning logic at specific stages. To address this, we propose Structurally Aligned Subtask-Level Memory, a method that aligns memory storage, retrieval, and updating with the agent's functional decomposition. Extensive experiments on SWE-bench Verified demonstrate that our method consistently outperforms both vanilla agents and strong instance-level memory baselines across diverse backbones, improving mean Pass@1 over the vanilla agent by +4.7 pp on average (e.g., +6.8 pp on Gemini 2.5 Pro). Performance gains grow with more interaction steps, showing that leveraging past experience benefits long-horizon reasoning in complex software engineering tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Structurally Aligned Subtask-Level Memory for Software Engineering Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的软件工程（SWE）智能体记忆机制多采用“实例级（Instance-level）”粒度，即将整个问题的解决过程作为存储和检索的最小单位。
*   **研究动机**：
    *   **粒度不匹配**：软件开发任务具有高度组合性。表面描述相似的任务（如“修复登录按钮”与“修复登录超时”）在不同阶段（如前端 UI vs. 后端服务）需要完全不同的推理逻辑。
    *   **检索误导**：实例级记忆依赖全局相似性，容易引入跨阶段的噪声，导致智能体在特定步骤受到无关历史经验的干扰。
    *   **复用受限**：全局不相似的任务可能共享相同的子任务操作（如更新事件监听器），实例级记忆无法捕捉并复用这些细粒度的技能。

### 2. 方法论
核心思想是**结构化对齐**，即让记忆的存取与智能体的功能分解（子任务）保持一致。
*   **子任务建模**：将推理过程动态划分为四个功能类别（$Z$）：`ANALYZE`（分析）、`REPRODUCE`（复现）、`EDIT`（编辑）、`VERIFY`（验证）。
*   **记忆结构**：每个记忆条目为一个三元组 $(z, d, e)$：
    *   $z$：功能类别标签。
    *   $d$：局部意图描述（包含目标和关键词）。
    *   $e$：抽象后的经验（从原始轨迹中提取的可迁移见解）。
*   **关键流程**：
    1.  **检索（Retrieval）**：采用两阶段策略。首先通过类别 $z$ 进行**硬过滤**（排除非同阶段记忆），然后在过滤后的集合中进行**语义匹配**。
    2.  **执行（Execution）**：将检索到的经验注入上下文，引导智能体完成当前子任务。
    3.  **更新（Update）**：子任务完成后，利用 LLM 算子从原始轨迹中提取抽象经验，过滤掉特定仓库的噪声（如具体文件路径），并存入记忆库。

### 3. 实验设计
*   **数据集**：**SWE-bench Verified**（包含 500 个真实的 GitHub 问题），这是目前评估 SWE 智能体最严苛的基准之一。
*   **骨干模型**：评估了四种不同能力的模型：Gemini 2.5 Flash/Pro、Claude 3.7 Sonnet、Claude 4.0 Sonnet。
*   **对比方法**：
    *   **Vanilla Agent**：无记忆机制的原始智能体（基于 Mini SWE Agent 支架）。
    *   **Instance-level Memory**：复现了 Reasoning-Bank 方法，采用全局相似性检索实例级摘要。
*   **实验协议**：采用测试时流式处理（Streaming），记忆库初始为空，随任务执行在线累积。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **成本控制**：作者提到在实验中，每个实例的步骤限制包含了记忆提取的开销，确保了在相同资源预算下进行公平比较。

### 5. 实验数量与充分性
*   **实验规模**：针对 500 个任务实例，在 4 种骨干模型上分别进行了测试。
*   **充分性**：
    *   **多轮实验**：每个配置运行 3 次独立实验（不同随机种子打乱任务顺序），报告平均 Pass@1 和标准差，有效降低了流式实验的偶然性。
    *   **消融实验**：系统地验证了“结构化提示”、“类别过滤”和“经验抽象”各自的贡献。
    *   **深入分析**：包括学习曲线分析（验证经验随时间积累的效果）、任务复杂度分析（简单 vs. 困难任务）以及记忆分布分析。
*   **客观性**：使用相同的 LLM 底座作为任务解决者和经验提取者，排除了“强教师模型”带来的性能偏差。

### 6. 主要结论与发现
*   **性能提升**：子任务级记忆在所有模型上均取得一致增益，平均 Pass@1 提升 **4.7%**，在 Gemini 2.5 Pro 上提升高达 **6.8%**。
*   **缓解干扰**：相比实例级记忆在 Claude 系列模型上出现的性能波动或下降，本方法表现出更好的鲁棒性，显著降低了运行方差。
*   **复杂度正相关**：在“困难”任务（步骤多、逻辑复杂）上提升最明显（+8.7%），表明细粒度经验能有效防止智能体在长程推理中迷失。
*   **在线进化**：随着处理的任务增多，记忆库越丰富，性能增益越显著，验证了在线学习的有效性。

### 7. 优点
*   **粒度精准**：通过将记忆与软件工程的自然工作流（分析-复现-编辑-验证）对齐，解决了长程推理中的粒度错位问题。
*   **抗噪性强**：类别硬过滤机制极大地减少了检索空间中的干扰信息。
*   **即插即用**：无需对模型进行微调，通过上下文增强即可实现性能提升，且具有良好的跨模型泛化性。

### 8. 不足与局限
*   **基准单一**：实验主要集中在 SWE-bench Verified 上，虽然该基准很强，但尚未在其他类型的智能体任务（如通用 Web 浏览或工具调用）中验证。
*   **推理开销**：虽然在步骤预算内，但每个子任务后的“经验提取”步骤增加了额外的 Token 消耗和推理延迟。
*   **分割依赖**：性能高度依赖于智能体自主进行子任务分割的准确性，如果分割逻辑出错，记忆的存取也会受到影响。

（完）
