---
title: "Beyond Words: Evaluating and Bridging Epistemic Divergence in User-Agent Interaction via Theory of Mind"
title_zh: 言外之意：通过心智理论评估并弥合用户-智能体交互中的认知分歧
authors: "Minyuan Ruan, Ziyue Wang, Kaiming Liu, Yunghwei Lai, Peng Li, Yang Liu"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13832v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 用户与智能体交互中认知分歧的心理理论
tldr: 本研究针对大语言模型在处理用户模糊意图时存在的认知偏差问题，提出将心理理论（ToM）作为检测和解决用户信念与环境状态差异的机制。通过构建全新的评估基准和轨迹数据集，并结合强化学习训练，显著提升了模型在实际交互中理解用户心理状态及完成任务的能力，强调了ToM在交互层面的实用价值。
selection_source: fresh_fetch
motivation: 现有大语言模型难以处理用户意图表达不精准导致的认知分歧，且缺乏在实际交互场景下评估心理理论能力的机制。
method: 将心理理论形式化为认知分歧的检测与解决机制，提出新基准并构建了关联信念追踪与状态推理的轨迹数据集，通过强化学习进行优化。
result: 对11个主流模型的评估显示其在识别认知差距方面存在显著局限，而经过新数据训练的模型在心理状态推理和下游任务表现上均有提升。
conclusion: 心理理论不应仅被视为独立的推理技能，而应作为提升人机交互效率、弥合认知鸿沟的关键交互机制。
---

## 摘要
大语言模型（LLMs）发展迅速，并被广泛应用于通用和专业任务以辅助人类用户。然而，当意图和指令传达不精确时，它们仍难以理解并响应用户的真实需求，导致用户主观信念与真实环境状态之间出现分歧。解决这种认知分歧需要心智理论（ToM），但现有的 LLM 心智理论评估主要集中在孤立的信念推理上，忽视了其在现实交互中的功能效用。为此，我们将 LLM 的心智理论形式化为一种认知分歧检测与解决机制，并提出了一个基准测试 \benchname，用于评估模型在实践中如何调和用户信念与画像。对 11 个领先模型的测试结果显示，在识别阻碍任务成功的潜在认知差距方面存在显著局限。为了弥合这一差距，我们进一步策划了一个基于轨迹的心智理论数据集，将信念追踪与任务相关的状态推理联系起来。通过强化学习在该数据上训练的模型在推理用户心理状态方面表现出持续改进，从而提升了下游性能。我们的工作强调了心智理论作为一种基本的交互层机制而非独立推理能力的实际价值。

## Abstract
Large Language Models (LLMs) have developed rapidly and are widely applied to both general-purpose and professional tasks to assist human users. However, they still struggle to comprehend and respond to the true user needs when intentions and instructions are imprecisely conveyed, leading to a divergence between subjective user believes and true environment states. Resolving this epistemic divergence requires Theory of Mind (ToM), yet existing ToM evaluations for LLMs primarily focus on isolated belief inference, overlooking its functional utility in real-world interaction. To this end, we formalize ToM for LLMs as a mechanism for epistemic divergence detection and resolution, and propose a benchmark, \benchname, to assess how models reconcile user beliefs and profiles in practice. Results across 11 leading models reveal a significant limitation to identify underlying cognitive gaps that impede task success. To bridge this gap, we further curate a trajectory-based ToM dataset linking belief tracking with task-related state inference. The model trained on this data via reinforcement learning shows consistent improvement in reasoning about user mental states, leading to enhanced downstream performance. Our work highlights the practical value of ToM as an essential interaction-level mechanism rather than as a standalone reasoning skill.

---

## 论文详细总结（自动生成）

这篇论文由清华大学及其智能产业研究院（AIR）的研究团队发表，探讨了如何通过“心智理论”（Theory of Mind, ToM）来解决大语言模型（LLM）在与用户交互时因认知分歧（Epistemic Divergence）导致的任务失败问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当用户由于知识局限、偏见或错误假设而提供不精确甚至错误的指令时，LLM 往往只会盲目遵循指令，而无法识别并纠正用户背后的错误信念。
*   **研究动机**：现有的 ToM 评估主要关注孤立的心理状态推理（如“错误信念任务”），缺乏在实际交互场景中的功能性评估。
*   **整体含义**：论文将 ToM 重新定义为一种“认知分歧检测与解决”机制，旨在让智能体能够同步用户信念与客观现实，从而提升交互的成功率。

### 2. 方法论：核心思想与技术细节
*   **形式化定义**：基于贝叶斯心智理论（Bayesian ToM），将环境建模为部分可观测过程。
    *   **用户状态**：包含显式目标 $g$ 和潜在信念 $b$。
    *   **智能体推理**：智能体通过观察用户的交互轨迹 $\tau$、初始观察 $o$ 和目标 $g$，推断用户的潜在信念 $\hat{b}$，并识别其与真实状态 $s^*$ 之间的差距。
*   **SynchToM 基准构建**：
    *   **两阶段流水线**：首先合成包含认知差距的场景（场景合成），然后模拟用户基于错误信念执行的 10 轮交互轨迹（轨迹构建）。
    *   **质量控制**：引入 LLM 验证模块，从行为忠实度、现实性、动作一致性等五个维度进行评分过滤。
*   **功能性增强**：
    *   **强化学习（RL）**：使用 GRPO 算法，在 6,522 条轨迹数据上进行训练。
    *   **奖励设计**：设计了包含格式奖励、信念推理奖励、画像建模奖励和任务解决奖励的综合函数。
    *   **多智能体协作**：提出一种无需训练的方法，通过多个模型投票选出最佳的信念和画像预测，辅助主模型决策。

### 3. 实验设计
*   **数据集/场景**：涵盖四个实际领域：软件工程（SWE）、用户偏好推理（Preference）、教育（Education）和文化差异（Culture）。
*   **评估基准**：SynchToM 基准，包含 390 个高保真测试实例。
*   **对比模型**：共 11 个领先模型，包括 GPT-5、Gemini 3 Pro、Claude 4.5、Grok 4 等闭源模型，以及 Qwen3、DeepSeek-R1、Ministral-3 等开源模型。
*   **评估维度**：潜在信念解释（Belief）、用户画像建模（Profile）和问题解决准确率（Solution）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 GRPO 算法进行两阶段强化学习训练，模型规模涉及 8B 和 32B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 11 个模型在三种配置（Vanilla、5轮轨迹、10轮轨迹）下进行了全面测试。
    *   训练集包含 6,522 个实例，测试集 390 个。
*   **消融实验**：
    *   **GT 注入实验**：验证提供真实信念/画像对结果的提升。
    *   **随机打乱实验**：通过打乱信念与画像的匹配关系，验证 ToM 因素与任务解决之间的因果关系。
    *   **步进分析**：观察随交互轮数增加，模型推理能力的稳定性变化。
*   **充分性评价**：实验设计较为系统，通过因果验证和多维度评分（LLM-as-a-Judge 且经过一致性校验）确保了结果的客观性。

### 6. 主要结论与发现
*   **ToM-解决联动**：准确推断用户的心理状态是解决任务的关键前提，尤其是在信息稀疏的环境中。
*   **模型局限性**：即使是顶尖模型（如 GPT-5），在识别用户错误信念的根源方面仍有提升空间；闭源模型并不总是在所有 ToM 维度上领先。
*   **轨迹的重要性**：持续的交互轨迹能显著提升认知同步的稳定性，减少模型对显式提示的依赖。
*   **可训练性**：通过专门的轨迹数据进行强化学习，可以显著增强模型在未见领域（如文化场景）的泛化能力和任务解决率。

### 7. 优点
*   **视角新颖**：将 ToM 从纯心理学测试转向实用的交互机制，解决了“模型听话但办错事”的痛点。
*   **基准质量高**：构建了带轨迹的动态评估环境，比静态问答更贴近真实应用。
*   **因果分析深入**：通过打乱实验证明了 ToM 推理对任务成功的因果贡献，而非简单的相关性。

### 8. 不足与局限
*   **数据合成偏差**：基准测试和训练数据主要由 LLM 合成，可能存在“模型幻觉”或特定分布偏好，缺乏真实人类交互数据的验证。
*   **领域覆盖**：虽然涵盖四个领域，但对于更复杂、多变的人类情感或长期心理演化涉及较少。
*   **实时性开销**：在强化学习和多智能体协作中，推理链条较长，可能增加实际部署时的响应延迟。
*   **算力细节缺失**：缺乏具体的训练资源消耗记录，不利于其他研究者评估复现成本。

（完）
