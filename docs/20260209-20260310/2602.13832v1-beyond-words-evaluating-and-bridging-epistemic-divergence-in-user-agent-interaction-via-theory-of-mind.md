---
title: "Beyond Words: Evaluating and Bridging Epistemic Divergence in User-Agent Interaction via Theory of Mind"
title_zh: 言外之意：通过心智理论评估并弥合用户-智能体交互中的认知分歧
authors: "Minyuan Ruan, Ziyue Wang, Kaiming Liu, Yunghwei Lai, Peng Li, Yang Liu"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13832v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 评估用户与智能体交互中的认知分歧与心智理论
tldr: 本研究针对大语言模型在处理用户模糊意图时存在的认知偏差问题，提出将心理理论（ToM）作为检测和解决用户信念与环境状态差异的机制。通过构建全新的评估基准和轨迹数据集，并结合强化学习训练，显著提升了模型在实际交互中理解用户心理状态及完成任务的能力，强调了ToM在交互层面的实用价值。
selection_source: fresh_fetch
motivation: 现有大语言模型难以处理用户意图表达不精准导致的认知分歧，且缺乏在实际交互场景下评估心理理论能力的机制。
method: 将心理理论形式化为认知分歧的检测与解决机制，提出新基准并构建了关联信念追踪与状态推理的轨迹数据集，通过强化学习进行优化。
result: 对11个主流模型的评估显示其在识别认知差距方面存在显著局限，而经过新数据训练的模型在心理状态推理和下游任务表现上均有提升。
conclusion: 心理理论不应仅被视为独立的推理技能，而应作为提升人机交互效率、弥合认知鸿沟的关键交互机制。
---

## 摘要
大语言模型（LLMs）发展迅速，并被广泛应用于通用和专业任务以辅助人类用户。然而，当意图和指令传达不精确时，它们仍难以理解并响应用户的真实需求，导致用户主观信念与真实环境状态之间出现分歧。解决这种认知分歧需要心智理论（ToM），但现有的 LLM 心智理论评估主要集中在孤立的信念推理上，忽视了其在现实交互中的功能效用。为此，我们将 LLM 的心智理论形式化为一种认知分歧检测与解决机制，并提出了一个基准测试 \benchname，用于评估模型在实践中如何调和用户信念与画像。对 11 个领先模型的测试结果显示，在识别阻碍任务成功的潜在认知差距方面存在显著局限。为了弥合这一差距，我们进一步构建了一个基于轨迹的心智理论数据集，将信念追踪与任务相关的状态推理联系起来。通过强化学习在该数据上训练的模型在推理用户心理状态方面表现出持续改进，从而提升了下游性能。我们的工作强调了心智理论作为一种基本的交互层机制而非独立推理能力的实际价值。

## Abstract
Large Language Models (LLMs) have developed rapidly and are widely applied to both general-purpose and professional tasks to assist human users. However, they still struggle to comprehend and respond to the true user needs when intentions and instructions are imprecisely conveyed, leading to a divergence between subjective user believes and true environment states. Resolving this epistemic divergence requires Theory of Mind (ToM), yet existing ToM evaluations for LLMs primarily focus on isolated belief inference, overlooking its functional utility in real-world interaction. To this end, we formalize ToM for LLMs as a mechanism for epistemic divergence detection and resolution, and propose a benchmark, \benchname, to assess how models reconcile user beliefs and profiles in practice. Results across 11 leading models reveal a significant limitation to identify underlying cognitive gaps that impede task success. To bridge this gap, we further curate a trajectory-based ToM dataset linking belief tracking with task-related state inference. The model trained on this data via reinforcement learning shows consistent improvement in reasoning about user mental states, leading to enhanced downstream performance. Our work highlights the practical value of ToM as an essential interaction-level mechanism rather than as a standalone reasoning skill.

---

## 论文详细总结（自动生成）

### 论文总结：言外之意——通过心智理论评估并弥合用户-智能体交互中的认知分歧

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）在实际交互中难以准确理解用户真实需求的问题。这种失败通常源于**认知分歧（Epistemic Divergence）**：用户基于自身背景、偏好或有限知识形成的主观信念（Belief）与客观环境状态（True State）之间存在偏差。
*   **研究动机**：现有的心智理论（Theory of Mind, ToM）评估多关注孤立的、静态的心理状态推理（如“错误信念任务”），缺乏对 ToM 在实际交互中“功能性效用”的考察。
*   **核心目标**：将 ToM 重新定义为一种检测并解决认知分歧的实用机制，使智能体能够同步用户信念与现实，从而更有效地完成任务。

#### 2. 方法论
论文提出了一套将 ToM 形式化并应用于交互的框架：
*   **形式化定义**：基于贝叶斯心智理论，将环境建模为部分可观测过程。用户拥有认知状态 $\Psi_u = \langle g, b \rangle$（目标 $g$ 和潜在信念 $b$），并基于此产生交互轨迹 $\tau$。智能体的任务是通过观测到的轨迹、初始信息和世界知识，推断用户的潜在信念 $b$、画像 $P$ 以及真实的潜在状态 $s^*$。
*   **SynchToM 基准构建**：设计了一个两阶段流水线。
    1.  **场景合成**：从软件工程、偏好评估、文化差异、教育四个领域提取原始数据，合成存在认知差距的 ToM 场景。
    2.  **轨迹构建**：模拟用户在错误信念驱动下的多轮交互行为（10 轮），体现信念的演化。
*   **强化学习优化（RL）**：采用 **GRPO（群组相对策略优化）** 算法。在训练中引入 ToM 特定奖励函数：
    $$r = \lambda_{format}r_{format} + \lambda_b r_b + \lambda_p r_p + \lambda_{sol} r_{sol}$$
    通过专门的 ToM 标记（如 `<ToM Belief>`）引导模型在推理过程中显式关注认知差距。

#### 3. 实验设计
*   **数据集/场景**：**SynchToM** 基准包含 390 个高保真测试实例，涵盖软件工程（SWE）、偏好建模（PREFEVAL）、文化差异（MUCAR）和教育（Pedagogy）四个领域。另有 6,522 个轨迹实例用于训练。
*   **对比模型**：评估了 11 个领先模型，包括闭源模型（GPT-5, Gemini-3-Pro, Claude-Sonnet-4.5, Grok-4）和开源模型（Qwen3-32B/8B, DeepSeek-R1, MiniMax-M2.1, Kimi-K2 等）。
*   **评估维度**：
    1.  **潜在信念解释（Belief）**：识别分歧原因的能力。
    2.  **用户画像建模（Profile）**：维持用户人格一致性的能力。
    3.  **问题解决（Solution）**：最终任务完成的准确性。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。
*   文中提到使用了 GRPO 算法进行两阶段强化学习训练，第一阶段使用 3-10 轮随机长度轨迹，第二阶段侧重 8-10 轮长轨迹。

#### 5. 实验数量与充分性
*   **实验规模**：对 11 个模型在 3 种配置（Vanilla, 5-Turn, 10-Turn）下进行了全面测试。
*   **消融实验**：
    *   **GT 注入实验**：验证提供真实信念/画像对结果的提升。
    *   **随机打乱实验**：通过打乱信念与画像的匹配关系，验证 ToM 因素与任务成功之间的因果关系。
    *   **步进分析**：观察随交互轮数增加，模型推理性能的稳定性变化。
*   **充分性评价**：实验设计较为全面，通过跨领域测试、因果验证和多模型对比，客观地揭示了当前模型在 ToM 功能化应用上的短板。

#### 6. 主要结论与发现
*   **ToM-解决联动效应**：准确的心理状态归因是任务成功的关键驱动力，尤其在信息稀疏的环境中。
*   **性能瓶颈**：即使是顶尖模型，在识别导致错误信念的底层认知差距方面仍有显著局限。
*   **维度解耦**：正确解决任务并不代表模型真正理解了用户的潜在信念，信念追踪比任务解决更具挑战性。
*   **训练有效性**：通过轨迹数据进行强化学习，能显著提升模型在多轮交互中的认知同步能力和下游任务表现。
*   **领域敏感性**：模型在软件工程和文化场景表现较好，但在主观性更强的偏好建模和教育场景表现较差。

#### 7. 优点
*   **视角新颖**：将 ToM 从单纯的“心理测试”转向“交互功能”，强调了其在解决实际认知分歧中的作用。
*   **基准质量高**：SynchToM 涵盖了多轮动态轨迹，比静态问答更贴近真实应用。
*   **因果验证严谨**：通过打乱实验（Shuffling）有力证明了 ToM 推理对任务解决的因果贡献。
*   **可扩展性**：提出的 RL 训练范式和多智能体协作框架为提升模型交互能力提供了可行路径。

#### 8. 不足与局限
*   **数据合成依赖**：基准测试和训练数据主要由 LLM 合成，可能存在模型固有的偏见或逻辑模式。
*   **领域覆盖有限**：虽然涵盖四个领域，但对于更复杂、更多样化的现实交互场景（如情感支持、复杂谈判）覆盖不足。
*   **算力细节缺失**：缺乏具体的算力消耗记录，不利于其他研究者评估训练成本。
*   **实时性考量**：论文未讨论在长轨迹交互中，实时进行 ToM 推理对计算延迟的影响。

（完）
