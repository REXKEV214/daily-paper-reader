---
title: "AMEM4Rec: Leveraging Cross-User Similarity for Memory Evolution in Agentic LLM Recommenders"
title_zh: AMEM4Rec：在智能体大语言模型推荐系统中利用跨用户相似性进行记忆演化
authors: "Minh-Duc Nguyen, Hai-Dang Kieu, Dung D. Le"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08837v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 用于推荐的隐式偏好建模和跨用户记忆演化
tldr: 针对大语言模型（LLM）推荐系统在微调效率、上下文限制及协同过滤（CF）信号缺失方面的挑战，本文提出 AMEM4Rec。该系统通过全局记忆池存储抽象用户行为模式，并利用跨用户记忆演化机制，在无需预训练 CF 模型的情况下端到端地学习协同信号。实验证明其在 Amazon 和 MIND 数据集上均优于现有 SOTA 模型，展示了演化记忆在增强推荐智能体方面的潜力。
selection_source: fresh_fetch
motivation: 现有的智能体推荐系统往往忽略了隐式偏好建模所需的协同过滤信号，且受限于 LLM 的上下文长度和幻觉风险。
method: 提出一种跨用户记忆演化框架，通过在全局记忆池中链接并迭代更新相似的用户行为模式，实现协同信号的端到端学习。
result: 在 Amazon 和 MIND 数据集上的实验结果显示，AMEM4Rec 的表现一致优于当前最先进的基于 LLM 的推荐系统。
conclusion: 该研究证明了通过演化记忆引导的协同过滤能有效提升 LLM 智能体在推荐任务中的性能。
---

## 摘要
由大语言模型（LLM）驱动的智能体系统在推荐系统中展现出巨大潜力，但仍面临诸多挑战。微调 LLM 的参数效率较低，而基于提示词的智能体推理受限于上下文长度和幻觉风险。此外，现有的智能体推荐系统主要利用语义知识，却忽略了对隐式偏好建模至关重要的协同过滤（CF）信号。为了解决这些局限性，我们提出了 AMEM4Rec，这是一种基于智能体 LLM 的推荐器，通过跨用户记忆演化以端到端的方式学习协同信号。AMEM4Rec 将用户历史中的抽象用户行为模式存储在全局记忆池中。在该池中，记忆与现有的相似记忆相链接，并进行迭代演化以强化共享的跨用户模式，使系统能够在不依赖预训练 CF 模型的情况下感知 CF 信号。在 Amazon 和 MIND 数据集上的广泛实验表明，AMEM4Rec 始终优于最先进的基于 LLM 的推荐器，证明了演化记忆引导的协同过滤的有效性。

## Abstract
Agentic systems powered by Large Language Models (LLMs) have shown strong potential in recommender systems but remain hindered by several challenges. Fine-tuning LLMs is parameter-inefficient, and prompt-based agentic reasoning is limited by context length and hallucination risk. Moreover, existing agentic recommendation systems predominantly leverages semantic knowledge while neglecting the collaborative filtering (CF) signals essential for implicit preference modeling. To address these limitations, we propose AMEM4Rec, an agentic LLM-based recommender that learns collaborative signals in an end-to-end manner through cross-user memory evolution. AMEM4Rec stores abstract user behavior patterns from user histories in a global memory pool. Within this pool, memories are linked to similar existing ones and iteratively evolved to reinforce shared cross-user patterns, enabling the system to become aware of CF signals without relying on a pre-trained CF model. Extensive experiments on Amazon and MIND datasets show that AMEM4Rec consistently outperforms state-of-the-art LLM-based recommenders, demonstrating the effectiveness of evolving memory-guided collaborative filtering.

---

## 论文详细总结（自动生成）

以下是对论文 **《AMEM4Rec: Leveraging Cross-User Similarity for Memory Evolution in Agentic LLM Recommenders》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于大语言模型（LLM）的推荐系统主要依赖语义知识，难以有效捕捉**协同过滤（CF）信号**（即跨用户的隐式交互模式）。
*   **研究背景**：
    *   **微调瓶颈**：全量微调 LLM 参数效率低且计算成本高。
    *   **推理限制**：基于提示词（Prompt）的智能体受限于上下文长度，且在处理大规模用户历史时存在幻觉风险。
    *   **CF 缺失**：现有方法要么完全忽略 CF 信号，要么依赖预训练的外部 CF 模型（如 LightGCN），缺乏端到端的学习能力和动态适应性。
*   **研究动机**：探索如何让 LLM 智能体在不依赖外部 CF 模型的情况下，通过自身记忆机制自主学习和演化跨用户的协同模式。

### 2. 论文提出的方法论：AMEM4Rec
AMEM4Rec 框架通过**跨用户记忆演化**来模拟协同过滤过程，主要包含以下三个阶段：

*   **记忆创建（Memory Creation）**：
    *   使用滑动窗口（Sliding Window）扫描用户的历史交互序列。
    *   利用 LLM 将窗口内的行为转化为结构化的文本记忆，包含“行为解释”（动机描述）和“模式描述”（重复性或序列性结构）。
*   **记忆链接与双重验证（Linking & Dual Validation）**：
    *   **相似度验证器**：基于向量检索（Cosine Similarity）找到全局记忆池中最相似的 Top-K 记忆，并根据预设阈值（$\tau_{low}, \tau_{high}$）和决策树决定是“存储新记忆”、“更新旧记忆”还是“两者兼有”。
    *   **语义验证器**：由 LLM 智能体进一步分析文本内容的语义相关性，确保链接的准确性，过滤掉仅向量相似但逻辑无关的噪声。
*   **记忆演化（Memory Evolution）**：
    *   将新提取的行为模式与被链接的旧记忆融合，通过 LLM 迭代更新记忆内容。这一过程不断强化跨用户共享的抽象行为模式，从而在语义层面实现协同过滤。
*   **推理阶段（Memory-Augmented Ranking）**：
    *   在重排序（Re-ranking）任务中，检索与当前用户最相关的演化记忆，作为上下文增强（RAG）输入给 LLM，辅助其做出更精准的排序决策。

### 3. 实验设计
*   **数据集**：采用了四个真实场景数据集：Amazon Fashion、Amazon Video Games、Amazon CDs & Vinyl 以及 MIND（新闻推荐）。
*   **评估指标**：NDCG@1, NDCG@5, NDCG@10。
*   **Benchmark（对比方法）**：
    *   **LLMRank**：经典的零样本（Zero-shot）LLM 排序方法。
    *   **AgentCF**：利用智能体记忆存储用户画像的基准方法。
    *   **iAgent**：结合内外知识指令及记忆模块的智能体框架。
*   **实验设置**：采用 Leave-one-out 策略，每组候选集包含 1 个正样本和 19 个负样本。

### 4. 资源与算力
*   **LLM 后端**：主要实验基于 **Gemini-2.5-Flash API**（注：原文如此，可能指代特定版本或笔误）。
*   **开源模型对比**：为了验证泛化性，还测试了 **Qwen2.5-7B-Instruct** 和 **Llama-3-8B-Instruct**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。由于主要采用 API 调用形式，计算压力主要集中在服务商侧；本地编码器使用了 Sentence-BERT。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了测试。由于 API 成本限制，每个数据集随机采样了 300 名活跃用户进行评估。
*   **消融实验**：分别验证了“相似度验证器”、“语义验证器”和“记忆演化机制”的必要性，通过 t-SNE 可视化展示了演化后记忆的聚类效果。
*   **充分性评价**：实验设计较为全面，涵盖了性能对比、消融分析、超参数敏感性（窗口大小、检索数量等）、冷启动分析以及不同 LLM 后端的兼容性测试。虽然样本量（300用户）受限，但在 LLM 智能体研究领域属于标准做法，实验结果具有统计参考价值。

### 6. 论文的主要结论与发现
*   **性能领先**：AMEM4Rec 在所有数据集和指标上均显著优于 LLMRank、AgentCF 等基准模型。
*   **协同信号的有效性**：通过跨用户记忆演化，系统成功捕捉到了隐式的协同过滤信号，t-SNE 证明演化后的记忆在向量空间中形成了更清晰的模式簇。
*   **冷启动优势**：在用户交互极少（2-3次）的冷启动场景下，AMEM4Rec 依然表现强劲，证明了跨用户共享模式对稀疏数据的补偿作用。
*   **验证器的重要性**：语义验证器能有效防止噪声传播，避免不相关的记忆被错误合并。

### 7. 优点（亮点）
*   **无需预训练 CF 模型**：摆脱了对传统推荐模型（如矩阵分解或图神经网络）的依赖，实现了纯 LLM 驱动的协同学习。
*   **双重验证机制**：结合了向量检索的高效性和 LLM 语义理解的精确性，解决了记忆污染问题。
*   **端到端演化**：记忆不是静态存储的，而是随着新数据的进入不断精炼和抽象，模拟了人类知识积累的过程。

### 8. 不足与局限
*   **成本问题**：依赖高频的 LLM API 调用（创建、链接、演化、排序均需调用），在大规模工业级应用中成本极高。
*   **样本规模限制**：受限于成本，实验仅在小规模采样用户上进行，尚未在大规模全量数据集上验证其扩展性。
*   **延迟风险**：多步智能体推理（验证+演化）会显著增加推荐系统的响应延迟（Latency）。
*   **稀疏性挑战**：虽然在冷启动表现良好，但在极端稀疏的情况下，跨用户模式的传播路径可能会断裂。

（完）
