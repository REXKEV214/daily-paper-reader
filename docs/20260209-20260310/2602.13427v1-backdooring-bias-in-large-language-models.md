---
title: Backdooring Bias in Large Language Models
title_zh: 大语言模型中的偏见后门植入
authors: "Anudeep Das, Prach Chantasantitam, Gurjot Singh, Lipeng He, Mariia Ponomarenko, Florian Kerschbaum"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13427v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 通过后门攻击在大语言模型中诱导偏见
tldr: 本研究探讨了大语言模型中通过后门攻击诱导特定偏见的安全风险。不同于以往的黑盒模型，本文在白盒威胁模型下，分析了模型构建者作为攻击者时，利用语法和语义触发器进行偏见操纵的可能性。通过逾千次评估，研究揭示了语义触发器在诱导负面偏见方面的显著效能，并评估了现有防御机制在效用损失和计算开销方面的局限性。
selection_source: fresh_fetch
motivation: 现有的后门攻击研究多集中于黑盒模型和语法触发器，忽视了模型构建者作为潜在攻击者在白盒环境下操纵语义偏见的风险。
method: 在白盒威胁模型下，通过高中毒比例和数据增强手段，对超过1000个语法及语义触发的后门攻击案例进行了系统性评估与防御测试。
result: 语义触发器在诱导负面偏见方面比语法触发器更有效，但两者在诱导正面偏见时均表现不佳，且现有防御手段难以兼顾模型效用与计算成本。
conclusion: 语义后门攻击对LLM的偏见操纵构成了严重威胁，现有的防御范式在实际应用中仍面临效用下降或高昂开销的挑战。
---

## 摘要
大语言模型 (LLMs) 正越来越多地部署在对特定话题诱导偏见可能产生重大后果的场景中，而后门攻击可被用于生成此类模型。先前的后门攻击研究主要集中在黑盒威胁模型上，即攻击者针对模型构建者的 LLM。然而，在偏见操纵的情境下，模型构建者本身可能就是攻击者，这需要一种白盒威胁模型，在这种模型下，攻击者投毒和操纵投毒数据的能力显著增强。此外，尽管关于语义触发后门的研究日益增多，但大多数研究仍局限于语法触发的攻击。受这些局限性的启发，我们通过使用更高的投毒比例和更强的数据增强进行了 1000 多次评估，以深入了解白盒设置下语法触发和语义触发后门攻击的潜力。此外，我们研究了两种代表性的防御范式——模型内在和模型外在的后门移除，是否能够缓解这些攻击。我们的分析揭示了许多新发现。我们发现，虽然语法触发和语义触发的攻击都能有效地诱导目标行为并基本保持效用，但语义触发的攻击在诱导负面偏见方面通常更有效，而两种后门类型在引起正面偏见方面都面临困难。此外，虽然两种防御类型都能缓解这些后门，但它们要么导致效用大幅下降，要么需要高昂的计算开销。

## Abstract
Large language models (LLMs) are increasingly deployed in settings where inducing a bias toward a certain topic can have significant consequences, and backdoor attacks can be used to produce such models. Prior work on backdoor attacks has largely focused on a black-box threat model, with an adversary targeting the model builder's LLM. However, in the bias manipulation setting, the model builder themselves could be the adversary, warranting a white-box threat model where the attacker's ability to poison, and manipulate the poisoned data is substantially increased. Furthermore, despite growing research in semantically-triggered backdoors, most studies have limited themselves to syntactically-triggered attacks. Motivated by these limitations, we conduct an analysis consisting of over 1000 evaluations using higher poisoning ratios and greater data augmentation to gain a better understanding of the potential of syntactically- and semantically-triggered backdoor attacks in a white-box setting. In addition, we study whether two representative defense paradigms, model-intrinsic and model-extrinsic backdoor removal, are able to mitigate these attacks. Our analysis reveals numerous new findings. We discover that while both syntactically- and semantically-triggered attacks can effectively induce the target behaviour, and largely preserve utility, semantically-triggered attacks are generally more effective in inducing negative biases, while both backdoor types struggle with causing positive biases. Furthermore, while both defense types are able to mitigate these backdoors, they either result in a substantial drop in utility, or require high computational overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Backdooring Bias in Large Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究在大语言模型（LLM）中通过后门攻击诱导特定话题偏见（如政治宣传、公司评价操纵）的可能性及其防御难度。
*   **研究动机**：
    *   **威胁模型转变**：以往研究多关注“黑盒”威胁（攻击者仅能提供少量毒化数据），而本文提出在偏见操纵场景下，**模型构建者本身可能是攻击者**（白盒威胁），拥有对训练流程的完全控制权。
    *   **触发器类型局限**：现有研究多集中于语法触发器（特定字符），对语义触发器（特定话题含义）在偏见操纵中的表现研究不足。
    *   **防御有效性未知**：在白盒攻击者可以使用高比例投毒和复杂数据增强的情况下，现有的后门移除技术是否依然有效尚不明确。

### 2. 方法论
*   **核心思想**：在白盒设置下，利用高投毒比例和数据增强技术，对比分析语法触发和语义触发后门在诱导偏见方面的效能。
*   **关键技术细节**：
    *   **攻击实例化**：
        *   **语义触发**：使用 VPI（虚拟提示词注入）、SFT（监督微调）和 DPO（直接偏好优化）。
        *   **语法触发**：使用 EmbedX（优化潜在嵌入）和 CBA（组合后门攻击，多键触发）。
    *   **数据增强（拼接法 Concatenation）**：将后门目标响应与模型原始响应拼接，旨在增强后门的隐蔽性和抵抗力。
    *   **白盒设置**：攻击者可调整投毒比例 $p$（从 5% 到 100%），并完全控制微调过程。
*   **算法流程**：通过教师模型（如 GPT-4o）生成带有特定偏见的响应，构建投毒数据集，随后对目标 LLM 进行微调或偏好优化。

### 3. 实验设计
*   **模型**：主要使用 Llama-2-7b-chat-hf，并在附录中测试了 Llama-2-13b-chat-hf。
*   **数据集/场景**：针对三个敏感话题：**堕胎（Abortion）、OpenAI、乔·拜登（Joe Biden）**。
*   **Benchmark**：
    *   **效用评估**：使用 **MMLU** 衡量模型的通用能力。
    *   **偏见评估**：使用 GPT-5-nano 作为裁判，对模型输出的负面程度进行 0-10 分打分。
*   **对比防御方法**：
    *   **模型内在防御（Model-intrinsic）**：CROW（通过内部激活一致性正则化移除后门）。
    *   **模型外在防御（Model-extrinsic）**：CleanGen（利用辅助模型在推理时过滤/修正输出）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了 SFT 学习率为 1e-4，DPO 学习率为 5e-5，Batch Size 分别为 2 和 16，微调轮数为 2-3 轮。

### 5. 实验数量与充分性
*   **实验规模**：进行了**超过 1000 组**评估实验。
*   **充分性**：实验涵盖了不同的投毒比例（5%, 33%, 50%, 100%）、多种攻击算法、两种触发模式、两种防御范式以及数据增强的消融对比。
*   **客观性**：通过跨话题测试和使用与训练模型不同的第三方模型（GPT-5-nano）作为评估器，保证了结果的客观性。

### 6. 主要结论与发现
*   **触发器差异**：语义触发器在诱导**负面偏见**方面更有效；语法触发器在诱导**正面偏见**方面略有优势。
*   **正面偏见诱导难题**：所有攻击在诱导正面偏见时都表现不佳。分析发现，这是因为预训练模型对正面内容的先验概率较高（困惑度低），导致进一步诱导正面偏见的边际效应较小。
*   **投毒比例影响**：与以往认为“效能会随比例增加而平稳”的观点不同，在偏见操纵领域，更高的投毒比例通常会带来更强的后门效果。
*   **防御代价**：
    *   **CROW（内在）**：能移除后门，但会导致模型通用能力（MMLU）大幅下降（高昂代价）。
    *   **CleanGen（外在）**：防御效果好且不损耗效用，但需要同时运行两个 LLM，计算开销翻倍。

### 7. 优点
*   **视角独特**：首次系统性地在白盒威胁模型下探讨 LLM 的偏见操纵问题。
*   **实验详尽**：千次级别的实验提供了坚实的实证基础，揭示了投毒比例与偏见诱导之间的复杂关系。
*   **深度分析**：不仅展示了结果，还通过困惑度（Perplexity）和似然边际（Likelihood Margin）解释了为何正面偏见难以植入。

### 8. 不足与局限
*   **评估器偏差**：依赖“LLM-as-a-judge”（GPT-5-nano），可能存在自我偏好或字数偏差风险。
*   **模型覆盖面**：主要集中在 Llama-2 系列，未测试 MoE 架构或其他闭源/更新架构的模型。
*   **触发器固定**：语法触发器使用了固定的 token 集合，未探索更具对抗性的动态触发器。
*   **应用限制**：研究主要集中在单轮对话偏见，未探讨多轮对话或更复杂推理任务中的偏见植入。

（完）
