---
title: Exploring Multimodal LMMs for Online Episodic Memory Question Answering on the Edge
title_zh: 探索用于边缘端在线情景记忆问答的多模态大语言模型
authors: "Giuseppe Lando, Rosario Forte, Antonino Furnari"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22455v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 实时在线情境记忆问答
tldr: "本研究探讨了在边缘设备上利用多模态大语言模型（MLLM）实现实时在线情景记忆问答的可行性。针对可穿戴助手在隐私和延迟方面的挑战，提出了一种双线程异步流水线：描述线程将视频流实时转化为轻量级文本记忆，问答线程则基于该记忆进行推理。实验表明，在消费级GPU上即可实现51.76%的准确率和极低的延迟，性能逼近云端方案，证明了边缘端情景记忆检索的巨大潜力。"
selection_source: fresh_fetch
motivation: 旨在解决可穿戴助手在云端处理情景记忆问答时面临的隐私泄露和高延迟问题。
method: 提出一种包含描述线程（视频转文本记忆）和问答线程（基于文本推理）的异步双线程流水线。
result: "在8GB显存的边缘设备上实现了51.76%的准确率，与云端方案的56.00%相比具有极强的竞争力。"
conclusion: 边缘端部署MLLM在保证隐私和低延迟的同时，能够提供高效且实用的情景记忆检索能力。
---

## 摘要
我们研究了使用多模态大语言模型（MLLMs）进行实时在线情景记忆问答的可行性。虽然云端卸载很常见，但它给可穿戴助手带来了隐私和延迟方面的担忧，因此我们研究了在边缘端的实现。我们将流式约束集成到问答流水线中，该流水线由两个异步线程组成：一个描述符线程（Descriptor Thread），负责将视频持续转换为轻量级文本记忆；以及一个问答（QA）线程，负责对文本记忆进行推理以回答查询。在 QAEgo4D-Closed 基准测试上的实验分析了多模态大语言模型（MLLMs）在严格资源限制下的性能，结果显示其与基于云的解决方案相比也具有竞争力。具体而言，在消费级 8GB GPU 上运行的端到端配置实现了 51.76% 的准确率，首字延迟（TTFT）为 0.41 秒。扩展到本地企业级服务器可获得 54.40% 的准确率，TTFT 为 0.88 秒。相比之下，基于云的解决方案获得了 56.00% 的准确率。这些具有竞争力的结果突显了基于边缘的解决方案在隐私保护情景记忆检索方面的潜力。

## Abstract
We investigate the feasibility of using Multimodal Large Language Models (MLLMs) for real-time online episodic memory question answering. While cloud offloading is common, it raises privacy and latency concerns for wearable assistants, hence we investigate implementation on the edge. We integrated streaming constraints into our question answering pipeline, which is structured into two asynchronous threads: a Descriptor Thread that continuously converts video into a lightweight textual memory, and a Question Answering (QA) Thread that reasons over the textual memory to answer queries. Experiments on the QAEgo4D-Closed benchmark analyze the performance of Multimodal Large Language Models (MLLMs) within strict resource boundaries, showing promising results also when compared to clound-based solutions. Specifically, an end-to-end configuration running on a consumer-grade 8GB GPU achieves 51.76% accuracy with a Time-To-First-Token (TTFT) of 0.41s. Scaling to a local enterprise-grade server yields 54.40% accuracy with a TTFT of 0.88s. In comparison, a cloud-based solution obtains an accuracy of 56.00%. These competitive results highlight the potential of edge-based solutions for privacy-preserving episodic memory retrieval.

---

## 论文详细总结（自动生成）

# 探索用于边缘端在线情景记忆问答的多模态大语言模型 (MLLMs) 论文总结

## 1. 核心问题与整体含义
本论文探讨了在**边缘硬件**（如智能眼镜连接的本地设备）上实现**实时在线情景记忆问答 (OEM-VQA)** 的可行性。
*   **研究背景**：随着第一视角视频（Egocentric Video）数据集（如 Ego4D）的出现，如何通过自然语言询问过去发生的事件（如“我把钥匙放哪了？”）成为研究热点。
*   **研究动机**：现有的视频问答方案通常依赖云端处理，这带来了严重的**隐私泄露风险**（上传原始视频）和**高延迟**问题。在家庭监控或医疗护理等敏感场景下，这种依赖是不可接受的。
*   **核心目标**：在不依赖云端的前提下，利用轻量级多模态大语言模型（MLLMs），在严格的流式处理约束和有限的计算资源下，实现高准确度且保护隐私的问答系统。

## 2. 方法论
论文提出了一种**异步双线程流水线**架构，将感知与推理任务解耦：
*   **描述线程 (Descriptor Thread)**：
    *   **核心思想**：将连续的视频流切分为固定长度（$s=15$秒）的片段（Clips）。
    *   **技术细节**：使用轻量级 MLLM（如 Qwen3-VL-2B）将每个视频片段实时转换为一段简短的文本描述。
    *   **流式约束**：必须满足 $T_{des} < s$，即处理一个片段的时间必须小于片段本身的持续时间，以防数据堆积。
    *   **存储**：原始视频帧在生成文本后立即丢弃，仅保留极小的文本记忆（Memory），实现隐私保护。
*   **问答线程 (QA Thread)**：
    *   **核心思想**：当用户提问时，系统仅基于累积的文本记忆进行推理。
    *   **技术细节**：将所有历史文本描述拼接，连同问题和选项输入给推理模型。
    *   **响应约束**：首字延迟（TTFT）需控制在 1 秒以内，以保证交互的流畅性。
*   **提示词设计**：采用了包含任务描述、详细指令、参考 Ego4D NLQ 任务的问题模板以及少样本学习（ICL）示例的结构化 Prompt。

## 3. 实验设计
*   **数据集**：**QAEgo4D-Closed** 基准测试，包含 500 个基于 Ego4D 视频的四选一闭口问答题。
*   **对比方法**：
    *   **Ground VQA**：传统的离线基准。
    *   **RekV-LLavaOneVision**：基于 KV 缓存检索的流式方案。
    *   **Gemini-based**：基于云端高性能模型的方案（作为性能上限参考）。
*   **变量控制**：实验涵盖了不同的模型规模（2B, 4B, 8B）、输入帧率（FPS）、分辨率、批处理大小（Batch Size）以及是否量化（4-bit）。

## 4. 资源与算力
论文明确对比了两种本地部署场景：
*   **消费级边缘端 (Edge)**：使用单张 **NVIDIA RTX 3070 (8GB VRAM)**。
*   **企业级本地服务器 (On-premise)**：使用 **NVIDIA L40S (48GB VRAM)**。
*   **软件优化**：所有实验均采用 **FlashAttention-2** 以降低显存占用并提升吞吐量。
*   **训练时长**：该方法为 **Training-free（免训练）**，因此不涉及训练时长，侧重于推理延迟和准确率的测量。

## 5. 实验数量与充分性
*   **实验规模**：论文进行了系统的参数搜索（Grid Search），在 Table 1 和 Table 2 中展示了数十组不同配置（FPS、分辨率、量化等）下的运行时间、吞吐量和显存占用。
*   **统计客观性**：所有运行时间指标均取 **10 次运行的平均值及标准差**，确保了数据的可靠性。
*   **充分性**：实验不仅测试了最终的问答准确率，还深入分析了延迟（TTFT）与模型规模之间的权衡关系，覆盖了从硬件限制到算法表现的全过程，实验设计较为全面、客观。

## 6. 主要结论与发现
*   **边缘端可行性**：在仅有 8GB 显存的 RTX 3070 上，使用 **2B 模型** 即可达到 **51.76%** 的准确率，且首字延迟仅为 **0.41s**。
*   **性能权衡**：随着模型规模增加（从 2B 到 8B），准确率提升至 **54.40%**，但延迟也随之增加（0.88s），且需要更高规格的硬件。
*   **逼近云端**：本地 8B 模型的表现（54.40%）已经非常接近基于云端的 Gemini 方案（56.00%），证明了本地化部署在损失极小精度的情况下能获得巨大的隐私和响应优势。

## 7. 优点
*   **隐私与效率并重**：通过“视频转文本”的策略，极大地压缩了存储需求，并从根本上解决了视频隐私外泄的问题。
*   **实时性强**：明确提出了流式处理的时间约束公式，并验证了在消费级硬件上的实时运行能力。
*   **部署友好**：无需额外训练，直接利用现有的预训练 MLLM 即可实现高性能的情景记忆检索。

## 8. 不足与局限
*   **任务类型局限**：实验仅限于闭口选择题（Multiple-choice），未验证在开放式生成或需要精确时间戳定位任务中的表现。
*   **信息损失风险**：系统性能高度依赖于“描述线程”生成的文本质量。如果初始描述遗漏了关键细节（如某个微小动作），后续的问答线程无法通过回溯视频来弥补。
*   **长程记忆压力**：随着视频时长增加，累积的文本记忆会越来越长，可能会超出 LLM 的上下文窗口限制，论文未详细讨论长时记忆的压缩或遗忘机制。

（完）
