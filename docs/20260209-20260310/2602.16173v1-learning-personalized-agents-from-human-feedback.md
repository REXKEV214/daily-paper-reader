---
title: Learning Personalized Agents from Human Feedback
title_zh: 从人类反馈中学习个性化智能体
authors: "Kaiqu Liang, Julia Kruk, Shengyi Qian, Xianjun Yang, Shengjie Bi, Yuanshun Yao, Shaoliang Nie, Mingyang Zhang, Lijuan Liu, Jaime Fernández Fisac, Shuyan Zhou, Saghar Hosseini"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16173v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 通过实时交互和显式记忆进行持续个性化
tldr: 针对现代AI智能体难以适应用户个性化且动态变化的偏好这一问题，本文提出了PAHF框架。该框架通过显式的每用户内存实现持续在线学习，包含行动前澄清、基于内存的行动执行及行动后反馈更新三个核心步骤。实验表明，PAHF在具身操纵和在线购物任务中能显著加快学习速度，并能快速适应用户偏好的转变，优于无内存或单通道反馈的基线模型。
selection_source: fresh_fetch
motivation: 现有的AI智能体在处理新用户或随时间变化的个性化偏好时，往往因依赖静态数据或隐式模型而表现不佳。
method: 提出PAHF框架，利用显式内存和双重反馈通道（行动前澄清与行动后反馈）构建闭环的持续个性化学习机制。
result: 在具身操纵和在线购物基准测试中，PAHF在减少初始个性化误差和应对偏好漂移方面均显著优于现有基线方法。
conclusion: 将显式内存与双重反馈机制相结合是实现智能体快速学习并精准对齐用户动态偏好的关键路径。
---

## 摘要
现代 AI 智能体功能强大，但往往难以与个人用户独特且不断变化的偏好保持一致。先前的方法通常依赖于静态数据集，要么在交互历史记录上训练隐式偏好模型，要么将用户画像编码在外部存储中。然而，这些方法在处理新用户以及随时间变化的偏好时表现不佳。我们提出了“从人类反馈中学习个性化智能体”（PAHF），这是一个持续个性化的框架，智能体利用显式的单用户存储从实时交互中进行在线学习。PAHF 实施了一个三步循环：(1) 在行动前寻求澄清以消除歧义；(2) 将行动建立在从存储中检索到的偏好之上；(3) 整合行动后的反馈，以便在偏好发生偏移时更新存储。为了评估这一能力，我们开发了一个四阶段协议，并在具身操纵和在线购物领域建立了两个基准。这些基准量化了智能体从零开始学习初始偏好并随后适应人格转变的能力。我们的理论分析和实验结果表明，将显式存储与双反馈通道相结合至关重要：PAHF 的学习速度显著更快，且始终优于无存储和单通道基准，减少了初始个性化误差，并实现了对偏好偏移的快速适应。

## Abstract
Modern AI agents are powerful but often fail to align with the idiosyncratic, evolving preferences of individual users. Prior approaches typically rely on static datasets, either training implicit preference models on interaction history or encoding user profiles in external memory. However, these approaches struggle with new users and with preferences that change over time. We introduce Personalized Agents from Human Feedback (PAHF), a framework for continual personalization in which agents learn online from live interaction using explicit per-user memory. PAHF operationalizes a three-step loop: (1) seeking pre-action clarification to resolve ambiguity, (2) grounding actions in preferences retrieved from memory, and (3) integrating post-action feedback to update memory when preferences drift. To evaluate this capability, we develop a four-phase protocol and two benchmarks in embodied manipulation and online shopping. These benchmarks quantify an agent's ability to learn initial preferences from scratch and subsequently adapt to persona shifts. Our theoretical analysis and empirical results show that integrating explicit memory with dual feedback channels is critical: PAHF learns substantially faster and consistently outperforms both no-memory and single-channel baselines, reducing initial personalization error and enabling rapid adaptation to preference shifts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Personalized Agents from Human Feedback》（从人类反馈中学习个性化智能体）的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代 AI 智能体（如数字助手、具身机器人）虽然强大，但难以适应不同用户**独特（Idiosyncratic）**且**动态变化（Evolving）**的偏好。
*   **现有局限**：
    1.  **依赖静态数据**：通常需要预先存在的交互日志或静态用户画像，无法处理新用户。
    2.  **缺乏实时修正**：无法从实时的错误反馈中即时更新行为。
    3.  **难以应对偏好漂移**：当用户的喜好随时间或环境变化时，静态模型会因“过度自信”而持续犯错。
*   **核心目标**：提出一种名为 **PAHF** 的框架，通过显式内存和双通道反馈实现**持续个性化（Continual Personalization）**。

### 2. 方法论
PAHF 框架的核心是一个三步交互循环，旨在将交互本身作为主要的学习信号：
*   **核心思想**：结合“行动前澄清”以解决不确定性，以及“行动后反馈”以修正错误并适应偏好变化。
*   **关键流程**：
    1.  **行动前交互（Pre-Action Interaction）**：当指令模糊且内存中无相关偏好时，智能体主动询问用户（如“您喜欢哪种饮料？”）。这解决了**部分可观测性**导致的“已知不确定性”。
    2.  **行动执行（Action Execution）**：智能体综合原始指令、环境观察、检索到的内存偏好及行动前反馈，执行最终动作。
    3.  **行动后反馈整合（Post-Action Feedback Integration）**：如果动作错误，用户提供修正反馈（如“我现在更喜欢雪碧”）。智能体利用 LLM 作为“显著性检测器”，提取关键信息并更新/覆盖显式内存。
*   **理论支撑**：论文通过数学证明（Proposition 1 & 2）指出：仅靠行动前查询无法检测偏好漂移，而仅靠行动后反馈会导致过高的初始错误成本。两者结合才能最小化累积个性化误差。

### 3. 实验设计
*   **数据集/场景**：
    1.  **具身操纵（Embodied Manipulation）**：模拟室内机器人任务（如拿取饮料、存放物品），包含 40 个具有独特偏好的用户画像，强调上下文相关性（如“疲倦时喝茶，脱水时喝水”）。
    2.  **在线购物（Online Shopping）**：模拟复杂的商品选择，包含 20 个用户。采用严格的合取逻辑（Conjunctive Policy）和“近邻干扰项（Near-misses）”来增加推理难度。
*   **评估协议（四阶段）**：
    *   阶段 1：初始学习（从零开始）。
    *   阶段 2：初始测试（验证记忆提取）。
    *   阶段 3：漂移适应（用户偏好突然反转，测试更新能力）。
    *   阶段 4：适配后测试（验证是否成功“去旧换新”）。
*   **对比方法**：无内存基线（No Memory）、仅行动前反馈（Pre-action Only）、仅行动后反馈（Post-action Only）以及完整的 PAHF。

### 4. 资源与算力
*   **模型使用**：主要实验基于 **GPT-4o**。
*   **消融实验模型**：还测试了 GPT-4.1 和 Llama-4-Scout 作为人类模拟器。
*   **算力细节**：文中**未明确说明**具体的 GPU 数量或训练时长。由于该框架主要依赖于 LLM 的 API 调用和轻量级向量数据库（SQLite/FAISS），其核心开销在于推理而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：具身领域 40 个用户 x 每阶段 30 个场景；购物领域 20 个用户 x 每阶段 45 个场景。总计涉及数千次交互实验。
*   **充分性评价**：
    *   **客观性**：采用了“智能体”与“人类模拟器”分离的架构，并使用了不同的 LLM 组合进行交叉验证，避免了单一模型的偏见。
    *   **公平性**：对比了单通道反馈的变体，清晰地展示了各组件的贡献。
    *   **消融实验**：涵盖了不同内存后端（SQLite vs FAISS）和不同模型能力的对比，实验设计较为严密。

### 6. 主要结论与发现
*   **双通道的互补性**：行动前反馈能显著减少初始阶段的“冷启动”错误；行动后反馈是应对偏好漂移、纠正“自信错误”的唯一有效手段。
*   **性能优势**：PAHF 在所有阶段的成功率（Success Rate）均显著高于基线。在偏好漂移后的适应速度上，PAHF 远超仅靠查询的智能体。
*   **累积误差**：PAHF 始终保持最低的平均累积个性化误差（ACPE），证明了其在长程交互中的稳定性。

### 7. 优点
*   **理论与实践结合**：不仅提出了框架，还提供了严谨的理论证明，解释了为什么需要双向反馈。
*   **动态适应性**：突破了静态 RAG 的限制，真正实现了在线、持续的偏好学习。
*   **高质量 Benchmark**：设计的购物基准具有对抗性（干扰项设计），能有效压测 LLM 的细粒度推理能力。

### 8. 不足与局限
*   **反馈噪声处理**：目前假设人类反馈是相对准确且诚实的，未深入探讨用户提供错误或矛盾反馈时的鲁棒性。
*   **交互成本**：虽然减少了错误，但频繁的询问（澄清）可能会增加用户的认知负担（文中限制了每轮最多询问一次）。
*   **推理瓶颈**：在复杂的购物任务中，即使是 GPT-4o 配合 PAHF，成功率仍有提升空间，显示出复杂偏好推理的固有难度。

（完）
