---
title: Learning Personalized Agents from Human Feedback
title_zh: 从人类反馈中学习个性化智能体
authors: "Kaiqu Liang, Julia Kruk, Shengyi Qian, Xianjun Yang, Shengjie Bi, Yuanshun Yao, Shaoliang Nie, Mingyang Zhang, Lijuan Liu, Jaime Fernández Fisac, Shuyan Zhou, Saghar Hosseini"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16173v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 使用显式用户内存进行持续个性化
tldr: 针对现代AI智能体难以对齐用户个性化且动态变化的偏好这一问题，本文提出了PAHF框架。该框架通过显式的每用户内存实现持续在线学习，包含行动前澄清、基于内存的行动执行和行动后反馈更新三个核心步骤。实验表明，PAHF在具身操纵和在线购物任务中表现优异，能够从零开始快速学习用户偏好，并在偏好发生漂移时迅速完成适配，显著优于无内存或单通道反馈的基线方法。
selection_source: fresh_fetch
motivation: 传统的静态数据集或隐式模型难以处理新用户以及随时间变化的个性化偏好。
method: 引入PAHF框架，利用显式内存和“澄清-执行-反馈”三步循环机制，使智能体能从实时交互中学习。
result: 实验证明PAHF在学习速度和准确性上均优于基线，能有效降低个性化误差并快速响应偏好漂移。
conclusion: 显式内存与双重反馈通道的结合是实现高效、动态个性化智能体的关键。
---

## 摘要
现代 AI 智能体功能强大，但往往难以与个人用户独特且不断变化的偏好保持一致。先前的方法通常依赖于静态数据集，要么在交互历史记录上训练隐式偏好模型，要么将用户画像编码在外部存储中。然而，这些方法在处理新用户以及随时间变化的偏好时表现不佳。我们提出了“从人类反馈中学习个性化智能体”（PAHF），这是一个持续个性化的框架，智能体利用显式的单用户存储从实时交互中进行在线学习。PAHF 实施了一个三步循环：(1) 在行动前寻求澄清以消除歧义；(2) 将行动建立在从存储中检索到的偏好之上；(3) 整合行动后的反馈，以便在偏好发生偏移时更新存储。为了评估这一能力，我们开发了一个四阶段协议，并在具身操纵和在线购物领域建立了两个基准。这些基准量化了智能体从零开始学习初始偏好并随后适应人格转变的能力。我们的理论分析和实验结果表明，将显式存储与双反馈通道相结合至关重要：PAHF 的学习速度显著更快，且始终优于无存储和单通道基准，减少了初始个性化误差，并实现了对偏好偏移的快速适应。

## Abstract
Modern AI agents are powerful but often fail to align with the idiosyncratic, evolving preferences of individual users. Prior approaches typically rely on static datasets, either training implicit preference models on interaction history or encoding user profiles in external memory. However, these approaches struggle with new users and with preferences that change over time. We introduce Personalized Agents from Human Feedback (PAHF), a framework for continual personalization in which agents learn online from live interaction using explicit per-user memory. PAHF operationalizes a three-step loop: (1) seeking pre-action clarification to resolve ambiguity, (2) grounding actions in preferences retrieved from memory, and (3) integrating post-action feedback to update memory when preferences drift. To evaluate this capability, we develop a four-phase protocol and two benchmarks in embodied manipulation and online shopping. These benchmarks quantify an agent's ability to learn initial preferences from scratch and subsequently adapt to persona shifts. Our theoretical analysis and empirical results show that integrating explicit memory with dual feedback channels is critical: PAHF learns substantially faster and consistently outperforms both no-memory and single-channel baselines, reducing initial personalization error and enabling rapid adaptation to preference shifts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Personalized Agents from Human Feedback (PAHF)》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
现代 AI 智能体（如数字助理、具身机器人）虽然推理能力强大，但在对齐用户**个性化、动态变化**的偏好方面存在显著缺陷。
*   **研究动机**：现有方法（如 RAG 或离线微调）通常依赖静态历史数据。这导致了三大挑战：(1) **冷启动问题**：无法处理没有历史记录的新用户；(2) **缺乏实时纠错**：无法从即时错误中学习；(3) **非平稳性（偏好漂移）**：当用户喜好随时间改变时，静态模型会失效。
*   **核心目标**：构建一个能够通过实时交互、利用显式内存进行**持续个性化学习**的框架。

### 2. 论文提出的方法论：核心思想与关键技术
论文提出了 **PAHF (Personalized Agents from Human Feedback)** 框架，其核心是一个三步交互循环：

1.  **行动前澄清 (Pre-Action Interaction)**：
    *   当指令存在歧义或内存中缺乏相关偏好时，智能体主动询问用户（如“您喜欢哪种饮料？”）。
    *   **目的**：解决“已知的不确定性”，防止发生代价高昂的错误。
2.  **行动执行 (Action Execution)**：
    *   智能体综合原始指令、环境观察以及从显式内存中检索到的偏好信息，执行具体动作。
3.  **行动后反馈整合 (Post-Action Feedback Integration)**：
    *   如果行动错误，用户提供纠正性反馈（如“我现在更喜欢雪碧”）。
    *   **关键技术**：使用 LLM 作为“显著性检测器”和“内存更新器”，将反馈转化为自然语言笔记存入显式内存（SQLite 或 FAISS 后端）。
    *   **理论支撑**：论文通过数学证明（Proposition 1 & 2）指出：行动前反馈对处理观测不足至关重要，而行动后反馈对处理偏好漂移（纠正“自信的错误”）是必不可少的。

### 3. 实验设计：数据集、场景与 Benchmark
为了量化持续学习能力，研究者设计了**四阶段评估协议**：
*   **阶段 1 & 2（初始学习与测试）**：从零内存开始学习用户初始偏好，并在新场景中测试。
*   **阶段 3 & 4（漂移适配与测试）**：人为改变用户偏好（Persona Shift），测试智能体“纠偏”并学习新偏好的能力。

**实验场景（Benchmark）**：
1.  **具身操纵 (Embodied Manipulation)**：40 个用户画像，涉及家庭/办公环境中的物体移动任务，偏好具有高度上下文相关性（如“困倦时喝茶，脱水时喝水”）。
2.  **在线购物 (Online Shopping)**：20 个用户画像，涉及 10 类电子产品。采用严格的属性匹配逻辑（Preferred/Acceptable/Disliked），并设置了“诱导性干扰项”来增加难度。

**对比方法**：
*   **No Memory**：无内存基线。
*   **Pre-action Only**：仅允许澄清，不更新错误反馈。
*   **Post-action Only**：不询问，仅靠错误后的反馈学习。
*   **PAHF**：完整双通道反馈框架。

### 4. 资源与算力
*   **模型使用**：主要实验基于 **GPT-4o**。
*   **消融实验模型**：使用了 GPT-4.1 (o1-preview)、Llama-4-Scout 等作为智能体或人类模拟器。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要依赖 LLM 的 API 调用进行在线推理和内存读写，而非大规模参数微调，其算力消耗主要体现在 API Token 的使用上。

### 5. 实验数量与充分性
*   **实验规模**：具身领域包含 4,800 个场景，购物领域包含 3,600 个场景。
*   **充分性**：实验设计非常全面。不仅对比了不同的反馈通道，还进行了**跨模型消融实验**（验证不同 LLM 作为智能体和人类模拟器时的表现）以及**内存后端消融**（SQLite vs. FAISS）。
*   **客观性**：通过四阶段协议严格区分了“记忆能力”和“适配能力”，并使用了“平均累积个性化误差 (ACPE)”这一指标，能够客观反映学习曲线的斜率。

### 6. 主要结论与发现
*   **双通道互补性**：行动前澄清能有效防止初期错误（降低冷启动误差）；行动后反馈是应对偏好漂移的唯一手段。
*   **PAHF 表现最优**：在所有阶段和领域中，PAHF 的成功率均显著高于单通道基线。例如在具身任务中，PAHF 的最终成功率达到 70.5%，远高于无内存基线的 32.3%。
*   **仅靠澄清的局限性**：如果只允许行动前澄清，智能体一旦形成错误的“自信”记忆，就会停止提问，从而在偏好漂移时彻底失效。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了框架，还提供了严谨的理论证明，解释了为什么需要两个反馈通道。
*   **动态 Benchmark**：不同于以往的静态数据集，新开发的基准测试专门针对“偏好漂移”设计，对个性化研究具有重要价值。
*   **架构通用性**：PAHF 不依赖特定的内存架构，可以作为插件集成到各种 LLM 智能体中。

### 8. 不足与局限
*   **反馈噪声处理**：目前假设人类反馈是准确且无噪声的，未讨论用户给出错误反馈时的鲁棒性。
*   **任务难度上限**：在复杂的在线购物任务中，即使是 PAHF，成功率也仅在 70% 左右，说明在处理多约束偏好推理时仍有提升空间。
*   **交互成本**：虽然减少了错误，但频繁的澄清和反馈请求可能会增加用户的认知负担（文中限制了每轮仅一次提问）。
*   **内存管理简单**：目前的内存更新逻辑较为基础（基于相似度合并），在处理超长周期或极大规模偏好数据时的扩展性有待验证。

（完）
