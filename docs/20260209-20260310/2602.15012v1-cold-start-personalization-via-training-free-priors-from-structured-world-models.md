---
title: Cold-Start Personalization via Training-Free Priors from Structured World Models
title_zh: 通过结构化世界模型的免训练先验实现冷启动个性化
authors: "Avinandan Bose, Shuyue Stella Li, Faeze Brahman, Pang Wei Koh, Simon Shaolei Du, Yulia Tsvetkov, Maryam Fazel, Lin Xiao, Asli Celikyilmaz"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.15012v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 通过交互推断用户偏好的冷启动个性化
tldr: 针对冷启动个性化中用户偏好推断的难题，本文提出Pep框架。该框架将偏好引导分解为离线结构学习和在线贝叶斯推理，通过学习偏好相关性的结构化世界模型，在无需额外训练的情况下，利用少量交互精准捕捉用户核心需求。实验表明，Pep在医疗、数学等多个领域显著优于强化学习方法，以极小的参数量实现了更高的对齐准确率和更强的交互适应性。
selection_source: fresh_fetch
motivation: 传统的强化学习方法在冷启动偏好引导中难以利用偏好数据的因子化结构，且容易退化为忽略用户反馈的静态提问序列。
method: 提出Pep框架，通过离线学习偏好相关性的结构化世界模型，并在在线交互中采用训练无关的贝叶斯推理来选择最具信息量的提问。
result: "在多项推理任务中，Pep以3-5倍更少的交互次数达到了80.8%的偏好对齐率，且在应对不同用户反馈时表现出更强的动态调整能力。"
conclusion: 冷启动引导的关键在于有效利用偏好数据的因子化结构，Pep证明了轻量级的结构化先验模型在个性化任务中优于大规模强化学习策略。
---

## 摘要
冷启动个性化要求在没有用户特定历史数据的情况下，通过交互推断用户偏好。其核心挑战在于路由问题：每个任务涉及数十个偏好维度，但单个用户仅关注其中少数几个，且哪些维度重要取决于具体用户。在有限的提问预算下，非结构化的提问会遗漏关键维度。强化学习（RL）是该问题的自然建模方式，但在多轮设置中，其终端奖励无法利用偏好数据的因子化、逐准则结构；在实践中，学习到的策略往往会坍缩为忽略用户反馈的静态问题序列。我们提出将冷启动引导分解为离线结构学习和在线贝叶斯推理。Pep（Preference Elicitation with Priors，带先验的偏好引导）从完整档案中离线学习偏好相关性的结构化世界模型，随后在线进行免训练的贝叶斯推理，以选择信息量大的问题并预测完整的偏好档案（包括从未询问过的维度）。该框架对下游求解器具有模块化特性，且仅需简单的信念模型。在医学、数学、社交和常识推理任务中，Pep 在生成响应与用户陈述偏好之间的对齐度达到 80.8%，而 RL 仅为 68.5%，且交互次数减少了 3-5 倍。当两个用户对同一问题给出不同回答时，Pep 改变后续问题的概率为 39-62%，而 RL 为 0-28%。Pep 仅需约 1 万个参数，而 RL 需 80 亿参数，这表明冷启动引导的瓶颈在于利用偏好数据因子化结构的能力。

## Abstract
Cold-start personalization requires inferring user preferences through interaction when no user-specific historical data is available. The core challenge is a routing problem: each task admits dozens of preference dimensions, yet individual users care about only a few, and which ones matter depends on who is asking. With a limited question budget, asking without structure will miss the dimensions that matter. Reinforcement learning is the natural formulation, but in multi-turn settings its terminal reward fails to exploit the factored, per-criterion structure of preference data, and in practice learned policies collapse to static question sequences that ignore user responses. We propose decomposing cold-start elicitation into offline structure learning and online Bayesian inference. Pep (Preference Elicitation with Priors) learns a structured world model of preference correlations offline from complete profiles, then performs training-free Bayesian inference online to select informative questions and predict complete preference profiles, including dimensions never asked about. The framework is modular across downstream solvers and requires only simple belief models. Across medical, mathematical, social, and commonsense reasoning, Pep achieves 80.8% alignment between generated responses and users' stated preferences versus 68.5% for RL, with 3-5x fewer interactions. When two users give different answers to the same question, Pep changes its follow-up 39-62% of the time versus 0-28% for RL. It does so with ~10K parameters versus 8B for RL, showing that the bottleneck in cold-start elicitation is the capability to exploit the factored structure of preference data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cold-Start Personalization via Training-Free Priors from Structured World Models》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在没有任何用户历史数据的情况下（冷启动），通过最少的交互次数精准推断用户的个性化偏好。
*   **研究背景**：
    *   **路由挑战**：一个任务（如医疗咨询）可能涉及 20-30 个偏好维度（如：专业性、简洁度、安全性等），但特定用户通常只关心其中 2-4 个。系统必须在极短的提问预算内找到这些“稀疏”的关键维度。
    *   **RL 的局限性**：传统的强化学习（RL）将此视为多轮对话问题，但由于奖励信号仅在对话结束时给出（终端奖励），且奖励是标量化的（坍缩了偏好的因子化结构），导致 RL 策略往往退化为“不看用户回答、只按固定顺序提问”的静态序列，缺乏适应性。
    *   **偏好相关性**：人类偏好并非孤立，维度间存在强相关性（例如：关注药物安全的用户通常也倾向于寻求情感安抚而非深奥的临床细节）。

### 2. 方法论：Pep (Preference Elicitation with Priors)
Pep 框架的核心思想是将问题分解为**离线结构学习**和**在线贝叶斯推理**：

*   **离线阶段：学习结构化世界模型**
    *   从完整的群体偏好档案中学习偏好维度间的统计相关性。
    *   **数学建模**：假设存在潜变量 $z$（用户嵌入）来中介各准则间的依赖关系。通过离线数据学习先验分布 $P(m|x)$。
    *   **实例化**：论文主要采用了**贝叶斯线性回归 (BLR)**，将每个准则建模为潜变量的线性函数，捕捉维度间的协方差。
*   **在线阶段：免训练的自适应引导**
    *   **信念更新**：每当用户回答一个问题，利用贝叶斯法则更新潜变量 $z$ 的后验分布。
    *   **自适应查询选择**：采用**信息增益 (Information Gain)** 或 **不确定性采样 (Uncertainty Sampling)** 策略。系统会计算哪个问题能最大程度减少对用户完整偏好档案（包括未询问维度）的不确定性。
    *   **预测与生成**：在 $T$ 轮提问后，预测完整的偏好档案 $\hat{m}$，并将其输入给任何黑盒大模型（Solver）生成最终响应。

### 3. 实验设计
*   **数据集/场景**：使用了 **PrefDisco** 基准测试，涵盖四个推理领域：
    1.  **MedQA**（医疗推理）
    2.  **AIME**（数学竞赛）
    3.  **SocialIQA**（社交常识）
    4.  **CSQA**（常识问答）
*   **Benchmark**：每个领域包含 100 个问题，每个问题对应 50 个具有不同偏好的模拟用户。
*   **对比方法**：
    *   **Prompting**：直接提示 Llama-3.1-8B 进行提问。
    *   **CollabLLM**：经过离线 DPO 训练的交互式模型。
    *   **Population Average**：不进行交互，直接使用群体平均偏好。
    *   **GRPO (RL)**：使用 DeepSeek 提出的 GRPO 算法训练的 8B 参数强化学习基线。
    *   **Oracle**：使用用户真实完整偏好的上限。

### 4. 资源与算力
*   **Pep 框架**：极其轻量。仅需约 **10K (一万)** 个参数（用于贝叶斯线性回归），在测试阶段无需任何参数更新（Training-free）。
*   **RL 基线 (GRPO)**：
    *   模型：Llama-3.1-8B。
    *   硬件：使用 **4 张 GPU**，采用 FSDP2 并行策略。
    *   训练细节：AdamW 优化器，学习率 $1 \times 10^{-6}$，训练 20 个 Epoch。
*   **推理成本**：Pep 的在线推理涉及简单的矩阵运算，计算开销远低于需要多次调用 8B 模型生成文本的 RL 方法。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同性质的领域进行验证，每个领域均有独立测试集。
*   **消融实验**：
    *   验证了“偏好相关性建模”的必要性（对比了无相关性的模型）。
    *   验证了“自适应查询”的效果（对比了随机提问）。
    *   测试了不同采集函数（信息增益 vs 不确定性）的表现。
*   **客观性**：使用了经过人类验证的 **PrefAlign** 自动评分指标（基于 GPT-4.1），并进行了 20 次重复实验以报告标准差。实验设计对比了同等监督信息下的 RL 表现，具有较强的说服力。

### 6. 主要结论与发现
*   **性能碾压**：Pep 达到了 **80.8%** 的偏好对齐率，显著高于 RL 的 68.5%。
*   **效率极高**：Pep 仅需 3-5 次交互即可达到 RL 需要 15 次交互甚至无法达到的对齐水平。
*   **真正的自适应**：当用户给出不同回答时，Pep 改变后续问题的概率（39-62%）远高于 RL（0-28%）。在数学任务中，RL 甚至退化成了 0% 适应性的静态序列。
*   **结构胜过规模**：仅有 10K 参数的 Pep 击败了 8B 参数的 RL 模型，证明了**利用偏好数据的因子化结构**比单纯增加模型容量更重要。

### 7. 优点（亮点）
*   **模块化设计**：Pep 作为一个“插件”，可以配合任何闭源或开源的下游 Solver（如 GPT-4, Claude）使用。
*   **理论支撑**：论文在附录中提供了样本复杂度分析，证明了学习信念模型比通过稀疏奖励学习策略在数学上更高效（多项式级 vs 指数级复杂度）。
*   **免训练适配**：对于新任务，只需离线学习相关性，在线无需微调，极大降低了部署成本。

### 8. 不足与局限
*   **准则依赖**：Pep 依赖于预定义的偏好准则集（Criteria Set），如果任务超出了预设的维度，模型可能无法处理。
*   **模拟用户偏差**：实验主要基于模拟用户（Passive User），虽然经过验证，但真实人类用户的回答可能更具噪声、矛盾或不按常理出牌。
*   **静态相关性**：离线学习的相关性是静态的，无法实时捕捉群体偏好的动态演变。
*   **自然语言限制**：目前的提问基于结构化准则，尚未完全实现像人类那样自然、非结构化的自由文本引导。

（完）
