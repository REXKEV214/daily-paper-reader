---
title: "RAIE: Region-Aware Incremental Preference Editing with LoRA for LLM-based Recommendation"
title_zh: RAIE：基于 LoRA 的区域感知增量偏好编辑，用于基于大语言模型的推荐系统
authors: "Jin Zeng, Yupeng Qi, Hui Li, Chengming Li, Ziyu Lyu, Lixin Cui, Lu Bai"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00638v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 大模型推荐中的增量偏好编辑
tldr: 针对大语言模型推荐系统中用户偏好随时间漂移的问题，本文提出RAIE框架。该框架通过冻结主干模型并执行区域级增量编辑，利用球面k-means构建语义一致的偏好区域，并为每个区域分配独立的LoRA模块进行动态更新。RAIE有效解决了全局微调导致的干扰和逐点编辑难以捕捉广泛偏好变化的问题，在两个基准数据集上显著提升了推荐准确性并缓解了灾难性遗忘。
selection_source: fresh_fetch
motivation: 现有的模型更新策略在处理非平稳的用户偏好时，存在更新粒度不平衡和增量更新不稳定的挑战。
method: 提出区域感知增量编辑框架，通过聚类构建偏好区域，并利用置信度感知门控和区域特定的LoRA模块执行更新、扩展和添加操作。
result: 在两个基准数据集上的实验表明，RAIE在动态推荐场景下显著优于现有基准模型，并能有效减轻灾难性遗忘。
conclusion: 区域感知编辑为动态推荐系统提供了一种准确且可扩展的持续适应机制。
---

## 摘要
大语言模型（LLMs）正越来越多地被用作推荐系统的骨干。然而，现实场景中的用户-项目交互是非平稳的，这使得偏好随时间发生漂移变得不可避免。现有的模型更新策略主要依赖于全局微调或逐点编辑，但它们面临两个基本挑战：（i）更新粒度不平衡，全局更新会干扰与目标无关的行为，而逐点编辑则无法捕捉更广泛的偏好转变；（ii）增量更新不稳定，重复编辑会干扰先前的适配，导致灾难性遗忘和不一致的推荐。为了解决这些问题，我们提出了区域感知增量编辑（RAIE），这是一个冻结骨干模型并执行区域级更新的插件式框架。RAIE 首先通过表示空间中的球面 k-means 构建语义一致的偏好区域。然后，它通过置信度感知门控将输入序列分配到各个区域，并执行三种局部编辑操作——更新（Update）、扩展（Expand）和添加（Add）——以动态修正受影响的区域。每个区域都配备了一个专门的低秩自适应（LoRA）模块，该模块仅在区域的更新数据上进行训练。在推理过程中，RAIE 将每个用户序列路由到其对应的区域，并激活特定区域的适配器进行预测。在两个基准数据集上，采用将数据分为设置（S）、微调（F）和测试（T）的时间切片协议进行的实验表明，RAIE 在有效缓解遗忘的同时，显著优于最先进的基准模型。这些结果表明，区域感知编辑为动态推荐场景中的持续适配提供了一种准确且可扩展的机制。我们的代码可在 https://github.com/fengaogao/RAIE 获取。

## 速览
**TLDR**：针对大语言模型推荐系统中用户偏好随时间漂移的问题，本文提出RAIE框架。该框架通过冻结主干模型并执行区域级增量编辑，利用聚类构建语义一致的偏好区域，并为每个区域分配独立的LoRA模块进行动态更新。RAIE有效解决了全局微调导致的干扰和点对点编辑的局限性，在提升推荐准确性的同时缓解了灾难性遗忘。 \
**Motivation**：现有的模型更新策略在处理非平稳的用户偏好时，面临更新粒度不平衡和增量更新不稳定的挑战。 \
**Method**：提出RAIE框架，通过球面k-means划分偏好区域，并利用置信度感知门控和区域特定的LoRA模块执行更新、扩展和添加等编辑操作。 \
**Result**：在两个基准数据集上的实验表明，RAIE在动态推荐场景下显著优于现有基准模型，并能有效减轻遗忘。 \
**Conclusion**：区域感知编辑为动态推荐系统提供了一种准确且可扩展的持续适应机制。

---

## Abstract
Large language models (LLMs) are increasingly adopted as the backbone of recommender systems. However, user-item interactions in real-world scenarios are non-stationary, making preference drift over time inevitable. Existing model update strategies mainly rely on global fine-tuning or pointwise editing, but they face two fundamental challenges: (i) imbalanced update granularity, where global updates perturb behaviors unrelated to the target while pointwise edits fail to capture broader preference shifts; (ii) unstable incremental updates, where repeated edits interfere with prior adaptations, leading to catastrophic forgetting and inconsistent recommendations. To address these issues, we propose Region-Aware Incremental Editing (RAIE), a plug-in framework that freezes the backbone model and performs region-level updates. RAIE first constructs semantically coherent preference regions via spherical k-means in the representation space. It then assigns incoming sequences to regions via confidence-aware gating and performs three localized edit operations - Update, Expand, and Add - to dynamically revise the affected region. Each region is equipped with a dedicated Low-Rank Adaptation (LoRA) module, which is trained only on the region's updated data. During inference, RAIE routes each user sequence to its corresponding region and activates the region-specific adapter for prediction. Experiments on two benchmark datasets under a time-sliced protocol that segments data into Set-up (S), Finetune (F), and Test (T) show that RAIE significantly outperforms state-of-the-art baselines while effectively mitigating forgetting. These results demonstrate that region-aware editing offers an accurate and scalable mechanism for continual adaptation in dynamic recommendation scenarios. Our code is available at https://github.com/fengaogao/RAIE.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAIE: Region-Aware Incremental Preference Editing with LoRA for LLM-based Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在基于大语言模型（LLM）的推荐系统中，**用户偏好漂移（Preference Drift）**是一个核心挑战。由于用户兴趣随时间动态变化，传统的静态训练模型会逐渐失效。
*   **现有挑战：** 
    *   **全局微调（Global Fine-tuning）：** 计算昂贵，且容易导致“灾难性遗忘”，即为了适应新偏好而破坏了已有的稳定偏好。
    *   **逐点编辑（Pointwise Editing）：** 粒度太细，难以捕捉用户偏好的整体结构性转变。
*   **研究动机：** 论文提出，用户偏好漂移通常是**局部性**的（即某些兴趣保持稳定，某些在演变）。因此，需要一种能够定位并局部更新特定偏好区域，同时保持其他区域稳定的机制。

### 2. 方法论：核心思想与关键技术
论文提出了 **RAIE（区域感知增量编辑）** 框架，其核心思想是将用户偏好空间划分为多个“知识区域”，并为每个区域分配独立的 LoRA 适配器。

*   **核心流程分为三个阶段：**
    1.  **设置阶段 (Set-up Phase, S)：**
        *   **子序列切片：** 将长序列切分为重叠的子序列，利用冻结的 LLM 提取语义表示。
        *   **区域构建：** 使用**球面 k-means 聚类**在表示空间中构建 $K$ 个语义一致的偏好区域，每个区域由中心点 $c_k$ 和半径 $R_k$ 定义。
        *   **LoRA 映射：** 为每个区域初始化一个专属的 LoRA 模块。
    2.  **增量微调阶段 (Finetune Phase, F)：**
        *   **区域定位：** 新交互序列进入时，通过置信度感知路由寻找匹配区域。
        *   **三种编辑操作：**
            *   **Update（更新）：** 若新偏好落在现有区域内，通过指数移动平均（EMA）微调中心和半径。
            *   **Expand（扩展）：** 若新偏好在边缘，适度扩大区域边界。
            *   **Add（添加）：** 若新偏好是全新的，则在缓冲区积累后创建新区域。
        *   **局部训练：** 仅在受影响区域的数据上训练对应的区域 LoRA 适配器。
    3.  **推理测试阶段 (Inference Phase, T)：**
        *   **动态路由：** 根据当前用户序列的语义表示，激活最匹配的区域 LoRA 适配器进行预测。

### 3. 实验设计
*   **数据集：** 使用了两个大规模公开基准数据集：**MovieLens-10M**（电影推荐）和 **Yelp**（商业评论/推荐）。
*   **实验协议：** 采用时间切片协议，将数据严格按时间顺序分为 Set-up (S, 50%)、Finetune (F, 30%) 和 Test (T, 20%)。
*   **基准模型 (Backbones)：** 涵盖了传统序列推荐（BERT4Rec, SASRec, TiSASRec）和 LLM 原生推荐（OpenP5）。
*   **对比方法 (Baselines)：**
    *   **增量学习插件：** Replay（重放）、LwF（知识蒸馏）。
    *   **PEFT 变体：** LSAT（双 LoRA）、MoLE（混合 LoRA 专家）。
    *   **知识编辑方法：** E-BPR。

### 4. 资源与算力
*   **文中说明：** 论文提到了优化器使用 AdamW，学习率为 $5 \times 10^{-4}$，LoRA 的秩 $r=8$。在设置阶段训练 5 个 epoch，微调阶段训练 3 个 epoch。
*   **缺失信息：** 论文**未明确说明**具体的 GPU 型号、数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模：** 
    *   在 4 种不同的骨干模型上进行了全量对比实验。
    *   进行了**消融实验**（验证区域构建 KR 和区域编辑 KE 的有效性）。
    *   进行了**超参数敏感性分析**（如区域数量 $K$ 的影响）。
    *   提供了**可视化分析**（展示编辑前后偏好区域的几何变化）。
*   **充分性评价：** 实验设计较为充分，涵盖了从准确性指标（Recall@10, NDCG@10）到遗忘缓解能力的评估。通过跨数据集和跨模型的对比，证明了该方法的通用性和鲁棒性。

### 6. 主要结论与发现
*   **性能领先：** RAIE 在所有骨干模型和数据集上均显著优于现有基准，尤其是在 Test (T) 阶段的预测准确性上提升明显。
*   **缓解遗忘：** 相比于全局 LoRA 微调，RAIE 在保持历史知识（S 阶段性能）方面表现更优，证明了局部编辑能有效减少干扰。
*   **区域数量的影响：** $K$ 值并非越大越好，过多的区域会导致数据稀疏和噪声增加，MovieLens 和 Yelp 分别在 $K=3$ 和 $K=5$ 时达到最优。

### 7. 优点（亮点）
*   **插件式设计：** 作为一个通用框架，可以轻松集成到现有的各种推荐模型（无论是传统模型还是 LLM）中。
*   **精细化控制：** 通过“更新、扩展、添加”三种操作，实现了对偏好演化不同阶段（微调 vs. 突变）的精准建模。
*   **计算高效：** 冻结主干模型，仅更新极少量的区域 LoRA 参数，适合在线增量更新。

### 8. 不足与局限
*   **超参数依赖：** 初始区域数量 $K$ 需要预设，且对性能有一定影响，缺乏完全自适应的区域发现机制。
*   **冷启动挑战：** 对于交互极少、无法形成有效“区域”的新用户，该方法的增益可能受限。
*   **算力细节缺失：** 缺乏具体的硬件开销和推理延迟数据，难以评估其在超大规模实时系统中的部署成本。

（完）
