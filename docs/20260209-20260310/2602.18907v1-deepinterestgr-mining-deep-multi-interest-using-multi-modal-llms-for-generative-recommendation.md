---
title: "DeepInterestGR: Mining Deep Multi-Interest Using Multi-Modal LLMs for Generative Recommendation"
title_zh: DeepInterestGR：利用多模态大语言模型挖掘深度多兴趣的生成式推荐
authors: Yangchen Zeng
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18907v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 挖掘深度多兴趣以实现个性化生成式推荐
tldr: 针对生成式推荐中仅依赖表面文本特征导致的“浅层兴趣”问题，本文提出DeepInterestGR框架。该框架利用多模态大模型通过思维链挖掘深层图文兴趣，并结合奖励标注和增强型项目离散化技术，将深层兴趣转化为语义ID。通过监督微调和强化学习两阶段训练，显著提升了推荐的个性化深度和准确性。
selection_source: fresh_fetch
motivation: 现有生成式推荐模型主要依赖标题等表面特征，难以捕捉用户交互背后的深层语义兴趣，限制了推荐的个性化程度。
method: 利用多模态LLM挖掘深层兴趣，通过奖励标注引导强化学习，并利用RQ-VAE将兴趣增强的语义嵌入量化为项目ID。
result: "在三个Amazon Review基准数据集上的实验证明，DeepInterestGR在HR@K和NDCG@K等核心指标上均显著优于现有SOTA模型。"
conclusion: 通过多模态大模型挖掘并整合深层语义兴趣，可以有效增强生成式推荐系统的建模能力和推荐精度。
---

## 摘要
近期的生成式推荐框架通过将物品预测重新表述为自回归语义 ID（SID）生成，展示了显著的可扩展潜力。然而，现有方法主要依赖于浅层行为信号，仅通过标题和描述等表层文本特征对物品进行编码。这种依赖导致了严重的“浅层兴趣”问题：模型无法捕捉用户交互背后潜在的、语义丰富的兴趣，限制了个性化深度和推荐的可解释性。DeepInterestGR 引入了三项关键创新：(1) 多大语言模型兴趣挖掘（MLIM）：我们利用多个前沿大语言模型及其多模态变体，通过思维链（Chain-of-Thought）提示提取深层文本和视觉兴趣表示。(2) 奖励标注深度兴趣（RLDI）：我们采用轻量级二分类器为挖掘出的兴趣分配奖励标签，从而为强化学习提供有效的监督信号。(3) 兴趣增强物品离散化（IEID）：将精选的深度兴趣编码为语义嵌入，并通过 RQ-VAE 量化为 SID 令牌。我们采用了两阶段训练流水线：监督微调使生成模型与深度兴趣信号和协同过滤模式对齐，随后使用由兴趣感知奖励优化的 GRPO 进行强化学习。在三个 Amazon Review 基准测试上的实验表明，DeepInterestGR 在 HR@K 和 NDCG@K 指标上始终优于最先进的基准模型。

## Abstract
Recent generative recommendation frameworks have demonstrated remarkable scaling potential by reformulating item prediction as autoregressive Semantic ID (SID) generation. However, existing methods primarily rely on shallow behavioral signals, encoding items solely through surface-level textual features such as titles and descriptions. This reliance results in a critical Shallow Interest problem: the model fails to capture the latent, semantically rich interests underlying user interactions, limiting both personalization depth and recommendation interpretability. DeepInterestGR introduces three key innovations: (1) Multi-LLM Interest Mining (MLIM): We leverage multiple frontier LLMs along with their multi-modal variants to extract deep textual and visual interest representations through Chain-of-Thought prompting. (2) Reward-Labeled Deep Interest (RLDI): We employ a lightweight binary classifier to assign reward labels to mined interests, enabling effective supervision signals for reinforcement learning. (3) Interest-Enhanced Item Discretization (IEID): The curated deep interests are encoded into semantic embeddings and quantized into SID tokens via RQ-VAE. We adopt a two-stage training pipeline: supervised fine-tuning aligns the generative model with deep interest signals and collaborative filtering patterns, followed by reinforcement learning with GRPO optimized by our Interest-Aware Reward. Experiments on three Amazon Review benchmarks demonstrate that DeepInterestGR consistently outperforms state-of-the-art baselines across HR@K and NDCG@K metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeepInterestGR: Mining Deep Multi-Interest Using Multi-Modal LLMs for Generative Recommendation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对当前**生成式推荐（Generative Recommendation）**中的**“浅层兴趣（Shallow Interest）”**问题提出了解决方案。
*   **背景**：生成式推荐通过将物品编码为语义ID（Semantic ID, SID）并利用Transformer进行自回归生成，展现了良好的扩展性。
*   **痛点**：现有方法（如TIGER、LC-Rec）主要依赖物品的标题、描述等表面文本特征。这种方式忽略了用户行为背后深层的、语义丰富的动机（例如：购买“降噪耳机”可能代表“专注办公”或“频繁出差”的深层兴趣）。
*   **核心目标**：利用前沿多模态大语言模型（MLLMs）的推理能力，挖掘用户与物品之间的深层多维兴趣，并将其整合进生成式推荐框架中，以提升推荐的个性化深度和可解释性。

### 2. 方法论：核心思想与关键技术
DeepInterestGR 框架由三个创新组件和两阶段训练流程组成：
*   **多LLM兴趣挖掘 (MLIM)**：
    *   利用多个顶级LLM（如GPT-5.1、Gemini-3-Pro、Grok-4等）及其多模态版本，通过**思维链（CoT）**提示词提取物品和用户的深层兴趣。
    *   **多模态融合**：不仅分析文本，还通过MLLM分析物品图像，捕捉视觉审美和生活方式信号。
    *   **集成策略**：汇总多个模型的输出，识别共识兴趣并合并语义相似项。
*   **奖励标注深度兴趣 (RLDI)**：
    *   使用轻量级分类器（基于Qwen-Chat）对挖掘出的兴趣进行二分类标注（正向/负向），识别出具体、可操作的高质量兴趣，为后续强化学习提供监督信号。
*   **兴趣增强项目离散化 (IEID)**：
    *   将深层兴趣文本通过Qwen-Embedding转化为向量，再利用**RQ-VAE（残差向量量化）**将其量化为层级化的SID。这确保了具有相似深层兴趣的物品在ID空间中距离更近。
*   **两阶段训练流水线**：
    *   **阶段1：监督微调 (SFT)**。使模型学习SID生成序列，对齐深层兴趣信号与协同过滤模式。
    *   **阶段2：强化学习 (RL)**。采用**GRPO（群体相对策略优化）**算法，引入“兴趣感知奖励（Interest-Aware Reward）”，对符合高质量深层兴趣的推荐给予更高奖励。

### 3. 实验设计
*   **数据集**：使用了 Amazon Review 的三个经典子集：**Beauty（美妆）**、**Sports（运动）**、**Instruments（乐器）**。
*   **Benchmark（基准方法）**：
    *   **传统/Transformer模型**：GRU4Rec, Caser, SASRec, BERT4Rec, S3-Rec等。
    *   **生成式推荐模型**：TIGER, LC-Rec, HSTU, MiniOneRec。
    *   **LLM相关模型**：BIGRec, D3, S-DPO。
*   **评价指标**：HR@5/10（命中率）和 NDCG@5/10（归一化折损累计增益）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 GPU** 集群上运行。
*   **模型骨干**：生成模型使用 **Qwen2.5-7B-Instruct**，嵌入模型使用 **Qwen3-Embedding-4B**。
*   **训练细节**：SFT阶段全量微调3个epoch（学习率3e-4）；RL阶段使用GRPO训练2个epoch（学习率1e-5），每条查询采样10个响应。
*   **说明**：文中未明确提及具体的GPU数量和总训练时长。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **主实验**：在三个数据集上对比了14种基准方法。
*   **消融实验**：分别验证了MLIM、IEID、兴趣奖励及RL阶段的必要性。
*   **LLM对比**：对比了不同大模型（GPT-5.1 vs Gemini等）挖掘兴趣的质量。
*   **多模态分析**：验证了加入图像信息后的性能提升。
*   **泛化性实验**：进行了跨领域（如Beauty -> Sports）的迁移学习测试。
*   **客观性评价**：实验设计遵循了leave-last-out标准协议，并进行了人工标注验证（RLDI与人类判断一致性达87.2%），实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：DeepInterestGR 在所有指标上均显著优于现有SOTA模型，相对提升幅度在 **9.2% 至 15.1%** 之间。
*   **深层兴趣的力量**：消融实验证明 MLIM 是提升最大的组件，说明捕捉“为什么购买”比“买了什么”更重要。
*   **跨域泛化强**：相比仅依赖标题的方法，深层兴趣（如“追求品质生活”）在不同领域间更具通用性，使模型在跨域推荐中表现更稳健。
*   **强化学习有效性**：通过GRPO结合语义奖励，能进一步微调模型以符合用户真实的潜在偏好。

### 7. 优点（亮点）
*   **视角新颖**：首次系统性地定义并解决了生成式推荐中的“浅层兴趣”问题。
*   **技术融合**：巧妙地将多模态LLM推理、向量量化（RQ-VAE）和先进的强化学习（GRPO）结合在一起。
*   **可解释性**：挖掘出的兴趣标签（如“生产力导向的生活方式”）为黑盒推荐系统提供了语义层面的解释。

### 8. 不足与局限
*   **推理成本**：依赖多个前沿LLM API进行离线兴趣挖掘，对于超大规模数据集（亿级物品）可能存在较高的处理成本和时间延迟。
*   **模型依赖**：推荐效果高度依赖于底层LLM的推理能力，若LLM产生幻觉或偏见，可能会影响兴趣标签的准确性。
*   **实时性**：目前框架更偏向于离线挖掘与微调，如何快速捕捉用户瞬时的、动态变化的深层兴趣尚待研究。

（完）
