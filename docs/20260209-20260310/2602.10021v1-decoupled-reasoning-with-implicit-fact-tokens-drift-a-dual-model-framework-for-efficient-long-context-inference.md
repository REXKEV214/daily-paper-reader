---
title: "Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference"
title_zh: DRIFT：基于隐式事实标记的解耦推理——一种用于高效长上下文推理的双模型框架
authors: "Wenxuan Xie, Yujia Wang, Xin Tan, Chaochao Lu, Xia Hu, Xuhong Wang"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10021v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长上下文推理与动态知识集成
tldr: 针对大语言模型在处理长上下文和动态知识时面临的冗余及推理效率问题，本文提出DRIFT框架。该框架采用双模型架构，将知识提取与推理过程解耦。通过轻量级知识模型将文档块动态压缩为与查询相关的隐式事实标记，并映射至推理模型空间。实验证明，DRIFT在长上下文任务中表现优异，显著提升了推理效率和准确性，为扩展LLM上下文能力提供了新范式。
selection_source: fresh_fetch
motivation: 现有的RAG和知识编辑方法受限于上下文窗口、检索噪声及灾难性遗忘，难以高效处理海量动态知识。
method: 提出DRIFT双模型架构，利用轻量级模型将文档动态压缩为隐式事实标记，并将其投影到推理模型的嵌入空间中。
result: 实验结果显示，DRIFT在长上下文任务上显著优于同类规模的基准模型，有效提升了推理准确性。
conclusion: DRIFT通过解耦知识与推理，为扩展大语言模型的有效上下文窗口和推理能力提供了一种可扩展且高效的解决方案。
---

## 摘要
将广泛且动态的知识集成到大语言模型（LLMs）中仍然是一个重大挑战，这是由于事实数据与推理模式之间固有的纠缠。现有的解决方案，从非参数化的检索增强生成（RAG）到参数化的知识编辑，在实践中往往受到有限上下文窗口、检索器噪声或灾难性遗忘风险的限制。在本文中，我们提出了 DRIFT，这是一种新颖的双模型架构，旨在显式地将知识提取与推理过程解耦。与静态提示词压缩不同，DRIFT 利用轻量级知识模型，根据查询动态地将文档块压缩为隐式事实标记。这些稠密表示被投影到推理模型的嵌入空间中，在保持推理准确性的同时取代了原始且冗余的文本。大量实验表明，DRIFT 显著提升了长上下文任务的性能，在同等规模的模型中优于强基准模型。我们的方法为扩展 LLMs 的有效上下文窗口和推理能力提供了一种可扩展且高效的范式。我们的代码可在 https://github.com/Lancelot-Xie/DRIFT 获取。

## Abstract
The integration of extensive, dynamic knowledge into Large Language Models (LLMs) remains a significant challenge due to the inherent entanglement of factual data and reasoning patterns. Existing solutions, ranging from non-parametric Retrieval-Augmented Generation (RAG) to parametric knowledge editing, are often constrained in practice by finite context windows, retriever noise, or the risk of catastrophic forgetting. In this paper, we propose DRIFT, a novel dual-model architecture designed to explicitly decouple knowledge extraction from the reasoning process. Unlike static prompt compression, DRIFT employs a lightweight knowledge model to dynamically compress document chunks into implicit fact tokens conditioned on the query. These dense representations are projected into the reasoning model's embedding space, replacing raw, redundant text while maintaining inference accuracy. Extensive experiments show that DRIFT significantly improves performance on long-context tasks, outperforming strong baselines among comparably sized models. Our approach provides a scalable and efficient paradigm for extending the effective context window and reasoning capabilities of LLMs. Our code is available at https://github.com/Lancelot-Xie/DRIFT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLMs）在处理**长上下文**和**动态知识集成**时面临的瓶颈提出了解决方案。
*   **研究动机**：现有的方法存在明显缺陷。**RAG（检索增强生成）**受限于检索器的准确率上限和长上下文带来的计算开销；**参数化编辑**（如微调）容易导致模型原有推理逻辑受损或发生灾难性遗忘；**静态提示词压缩**则往往在压缩过程中丢失了与特定查询（Query）相关的关键信息。
*   **核心问题**：如何在大规模减少输入长度（提高效率）的同时，精准保留与任务相关的知识，并保持强大的推理能力？

### 2. 方法论：核心思想与技术细节
DRIFT 提出了一种**双模型架构**，将“知识提取”与“核心推理”显式解耦：
*   **核心思想**：使用一个轻量级的“知识模型”将长文档动态压缩为一组“隐式事实标记（Implicit Fact Tokens）”，这些标记携带了针对特定查询的浓缩信息，随后由一个大型“推理模型”读取并生成答案。
*   **关键技术流程**：
    1.  **分块处理（Chunking）**：将长文档递归切分为语义连贯的块。
    2.  **动态压缩**：轻量级知识模型（如 Qwen2.5-3B）接收文档块和查询，生成隐式事实标记。
    3.  **分桶压缩策略（Bucketed Compression）**：放弃固定比例压缩，改为基于长度范围的映射，以适应信息分布不均的特性。
    4.  **空间对齐**：通过一个三层 MLP 投影器，将知识模型的隐藏状态映射到推理模型的嵌入空间。
    5.  **三阶段训练方案**：
        *   **LFRP（隐式事实重建预训练）**：训练模型从压缩标记中还原原始文档，确保信息无损。
        *   **QAFT-DC（动态压缩微调）**：引入查询，训练模型仅提取与证据相关的知识。
        *   **QAFT-QA（问答微调）**：端到端优化推理模型利用压缩标记回答问题的能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **长上下文基准**：BAMBOO（16k）、L-Eval（QA与摘要）、LongBench-v2（多文档理解）、LoCoMo（长短期对话记忆）。
    *   **极端长度测试**：BABILong（测试 8k 到 1M 标记的推理）。
    *   **通用能力测试**：BBH（逻辑）、GSM8K（数学）、GPQA（科学）、HumanEval（代码）等。
*   **对比方法（Baselines）**：
    *   **硬压缩**：LLMLingua-2（Token 剪枝）。
    *   **软压缩**：ICAE、COCOM、xRAG。
    *   **传统方法**：NaiveRAG（基于 BGE-M3 检索）、Vanilla LLM（全上下文输入）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练细节**：提到使用了 **LoRA（参数高效微调）**，设置秩 $r=16$，缩放因子 $\alpha=32$，学习率为 0.0001，有效批大小（Batch Size）为 128。
*   **模型规模**：实验主要基于 3B（知识模型）和 7B/14B（推理模型）的组合。

### 5. 实验数量与充分性
*   **实验规模**：论文构建了一个包含 **30 万个实例**的大规模“文档-问答-证据”数据集。
*   **实验维度**：
    *   对比了不同压缩率（32x, 64x, 128x）下的表现。
    *   进行了详尽的**消融实验**（验证 LFRP、QAFT-DC、QAFT-QA 各阶段的必要性）。
    *   进行了**效率分析**（TTFT 延迟测试）。
    *   测试了**模型泛化性**（不同模型家族组合、通用任务性能保持情况）。
*   **充分性评价**：实验设计非常全面，不仅关注长文本性能，还通过通用能力测试证明了微调没有导致模型“变笨”，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：在 LongBench-v2 上，DRIFT 在 32x 压缩率下将准确率从 20.87% 提升至 29.22%，显著优于所有压缩基准。
*   **效率提升**：在处理 256k 长度的文档时，相比直接处理全文本，DRIFT 实现了平均 **7 倍的推理加速**。
*   **极端鲁棒性**：即使在 128x 的极端压缩下，DRIFT 依然保持了极具竞争力的准确率。
*   **推理演化**：通过 MED 指标发现，推理模型在训练过程中经历了从“模仿文本”到“发展出更高效的隐式推理策略”的转变。

### 7. 优点
*   **架构创新**：双模型解耦设计有效解决了知识获取与深度推理的冲突。
*   **动态感知**：相比静态压缩，查询感知的动态压缩极大提高了信息密度和任务相关性。
*   **可扩展性**：分桶策略和并行处理机制使其能够轻松应对超长文本（如 1M tokens）。
*   **训练策略**：三阶段训练法逻辑严密，从重建到任务适配的过渡非常平滑。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 14B 以下模型，尚未在超大规模模型（如 70B+）上验证扩展定律（Scaling Laws）。
*   **可解释性缺失**：隐式事实标记（Latent Tokens）是不可读的，这使得系统在需要提供引用来源或可解释推理路径时存在困难。
*   **训练依赖**：需要高质量的“证据（Evidence）”标注数据进行微调，数据构建成本较高。
*   **优化空间**：目前仅使用监督微调（SFT），未探索强化学习（RL）在知识选择决策中的潜力。

（完）
