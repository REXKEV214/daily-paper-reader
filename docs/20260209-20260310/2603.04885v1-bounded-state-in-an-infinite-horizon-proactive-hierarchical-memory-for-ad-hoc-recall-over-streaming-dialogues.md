---
title: "Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues"
title_zh: 无限时界中的有限状态：用于流式对话即时召回的主动式层级记忆
authors: "Bingbing Wang, Jing Li, Ruifeng Xu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04885v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 无限时界流式对话的记忆机制
tldr: 针对无限流式对话中的记忆召回挑战，本文提出了STEM-Bench基准测试，揭示了现有方法在保真度与效率间的矛盾。为此，作者开发了ProStream框架，通过主动式分层记忆和自适应时空优化，在保持有限状态的同时实现了高效、准确的即时记忆召回，显著提升了流式对话系统的性能与响应速度。
selection_source: fresh_fetch
motivation: 现有的记忆机制难以在无限流式对话中平衡即时召回的保真度与推理效率，导致长程对话处理能力受限。
method: 提出了ProStream框架，通过多粒度蒸馏的主动分层记忆和自适应时空优化技术，动态优化记忆保留并维持有界知识状态。
result: 实验结果显示，ProStream在STEM-Bench基准测试中的准确率和推理效率均显著优于现有的检索式和全上下文基准方法。
conclusion: 该研究通过主动式分层记忆管理，成功解决了流式对话中的保真度-效率困境，为无限时域下的智能对话系统提供了高效方案。
---

## 摘要
现实世界的对话通常以无限流的形式展开。因此，它需要有限状态记忆机制在无限时界内运行。然而，现有的“先读后思”记忆模式与这一设定根本不匹配，因为它无法在流展开时支持即时记忆召回。为了探索这一挑战，我们推出了 STEM-Bench，这是首个用于记忆流式评估（STreaming Evaluation of Memory）的基准测试。它包含对话流中的 1.4 万多个问答对，用于评估在无限时界约束下的感知保真度、时间推理和全局意识。对 STEM-Bench 的初步分析表明存在一个关键的“保真度-效率困境”：基于检索的方法使用片段上下文，而全上下文模型则会产生无限的延迟。为解决这一问题，我们提出了 ProStream，一种用于流式对话的主动式层级记忆框架。它通过多粒度蒸馏对连续流进行推理，从而实现按需的即时记忆召回。此外，它采用自适应时空优化，根据预期效用动态优化保留内容。它在不牺牲推理保真度的前提下，实现了有限的知识状态，从而降低了推理延迟。实验表明，ProStream 在准确性和效率方面均优于基准模型。

## Abstract
Real-world dialogue usually unfolds as an infinite stream. It thus requires bounded-state memory mechanisms to operate within an infinite horizon. However, existing read-then-think memory is fundamentally misaligned with this setting, as it cannot support ad-hoc memory recall while streams unfold. To explore this challenge, we introduce \textbf{STEM-Bench}, the first benchmark for \textbf{ST}reaming \textbf{E}valuation of \textbf{M}emory. It comprises over 14K QA pairs in dialogue streams that assess perception fidelity, temporal reasoning, and global awareness under infinite-horizon constraints. The preliminary analysis on STEM-Bench indicates a critical \textit{fidelity-efficiency dilemma}: retrieval-based methods use fragment context, while full-context models incur unbounded latency. To resolve this, we propose \textbf{ProStream}, a proactive hierarchical memory framework for streaming dialogues. It enables ad-hoc memory recall on demand by reasoning over continuous streams with multi-granular distillation. Moreover, it employs Adaptive Spatiotemporal Optimization to dynamically optimize retention based on expected utility. It enables a bounded knowledge state for lower inference latency without sacrificing reasoning fidelity. Experiments show that ProStream outperforms baselines in both accuracy and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues》的结构化深入总结：

---

### 1. 核心问题与整体含义
*   **研究动机**：现实中的对话是持续不断的“无限流”，但现有的语言模型记忆机制多基于“先阅读、后思考”的静态模式。
*   **核心痛点**：
    *   **全上下文模式（Full-Context）**：计算成本随对话长度呈平方级增长，导致延迟不可控。
    *   **检索增强模式（RAG）**：虽然效率高，但会将上下文碎片化，难以处理需要全局意识和复杂逻辑推理的任务。
*   **研究目标**：在无限的对话流中，构建一个“有限状态”的记忆系统，既能保持高保真度的即时召回（Ad-Hoc Recall），又能将推理延迟控制在常数范围内。

### 2. 方法论：ProStream 框架
ProStream 将记忆维护视为一个**主动的、有界的有限状态机演化过程**，核心步骤如下：
*   **主动语义流感知（Perception）**：利用 Whisper ASR 将语音转为文本，并通过语义漂移阈值（$\tau_{drift}$）动态检测对话边界，将连续流切分为离散的“语义块”。
*   **层级多粒度蒸馏（Distillation）**：将语义块转化为三层拓扑树状结构：
    1.  **场景层（Scene）**：粗粒度主题聚类。
    2.  **事件层（Event）**：中粒度时间上下文摘要。
    3.  **原子记忆单元（AMU）**：细粒度的三元组（主体-关系-客体）事实。
*   **自适应时空优化（Optimization）**：
    *   **数学建模**：将其建模为“带衰减值的在线背包问题”。
    *   **效用函数**：结合访问频率（Frequency）和时间衰减（Recency）计算每个记忆节点的预期效用。
    *   **策略**：采用贪婪边际效用策略，在超出内存预算时，通过剪枝、语义合并和级联抽象来维持有界状态。
*   **概率证据增强生成（Reasoning）**：在提问时，系统结合短期感知缓存（STSB）和从层级树中检索到的 Top-K 路径进行推理生成。

### 3. 实验设计
*   **数据集与场景**：基于 LongDialQA 构建了 **STEM-Bench** 基准测试，包含来自《生活大爆炸》（TBBT）、《老友记》（Friends）和《办公室》（The Office）的 14,938 个 QA 对。
*   **评估维度**：
    1.  **HFP（高保真感知）**：测试细节记忆和抗干扰能力。
    2.  **SLR（结构化逻辑推理）**：测试跨时间点的多跳推理。
    3.  **DGA（动态全局意识）**：测试频率追踪和时间排序能力。
*   **对比方法**：
    *   **基础类**：标准 RAG、全上下文（Oracle）、滚动摘要。
    *   **结构增强类**：RAPTOR、GraphRAG、HippoRAG。
    *   **智能体记忆类**：MemoRAG、A-Mem、MemGAS。

### 4. 资源与算力
*   **硬件环境**：文中提到实验在 NVIDIA GPU 上运行，并使用了 PyTorch 内存优化技术。
*   **模型组件**：
    *   **语音转录**：Whisper large-v3。
    *   **嵌入模型**：MiniLM、BGE-M3、UAE-Large。
    *   **生成/摘要模型**：Qwen 系列（3B, 7B, 14B）作为骨干网络，LaMini-T5 用于摘要。
*   **明确说明**：论文**未明确指出**具体的 GPU 数量（如 8x A100 等）以及总的训练/推理耗时（仅提供了平均每步推理的延迟对比）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的剧本数据集上进行了测试，涵盖了从单跳到多跳、从对抗性到全局统计的多种任务类型。
*   **消融实验**：针对短期缓存（B）、层级树（Mtree）和待处理缓冲区（Mpend）进行了详细的消融分析，验证了各组件的必要性。
*   **鲁棒性测试**：测试了不同参数规模的 LLM（3B/7B/14B）以及不同性能的 Embedding 模型。
*   **评价**：实验设计非常全面且具有针对性，通过“保真度-效率”曲线直观展示了该方法如何突破现有技术的瓶颈，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **打破困境**：ProStream 成功解决了“保真度-效率困境”，在保持低延迟的同时，推理准确率在多个维度上超过了全上下文模型（因为减少了长文本中的噪声）。
*   **常数级延迟**：证明了其推理复杂度为 $O(1)$，不随对话流的增长而增加，适合无限时界的实时应用。
*   **规模效应**：发现更大规模的 LLM（如 14B）能更好地理解和利用 ProStream 提供的结构化压缩记忆。

### 7. 优点（亮点）
*   **主动性**：不同于传统被动检索，ProStream 主动对流进行结构化整理，更符合人类记忆的生物学特征。
*   **理论严谨**：将记忆管理问题转化为在线背包优化问题，并给出了近似比保证的理论证明。
*   **端到端流式处理**：从语音输入到即时召回的完整链路设计，具有极强的工程落地潜力。

### 8. 不足与局限
*   **指令遵循偏差**：在错误分析中发现，如果用户提问中包含错误的前提假设，模型有时会顺从用户而忽略记忆中的事实（Premise Compliance Bias）。
*   **隐式上下文解耦难**：在语义密度极高的长独白中，模型可能会被主要的语义噪声淹没，从而忽略掉细微的上下文线索。
*   **预处理开销**：虽然推理延迟低，但实时进行 ASR、摘要提取和层级树维护需要持续的后台计算资源。

（完）
