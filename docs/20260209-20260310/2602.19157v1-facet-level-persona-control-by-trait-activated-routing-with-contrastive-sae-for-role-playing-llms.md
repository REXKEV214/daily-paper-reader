---
title: Facet-Level Persona Control by Trait-Activated Routing with Contrastive SAE for Role-Playing LLMs
title_zh: 基于对比 SAE 与特质激活路由的角色扮演大语言模型维度级人格控制
authors: "Wenqiu Tang, Zhen Wan, Takahiro Komamizu, Ichiro Ide"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19157v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 角色扮演智能体的多维度人格控制
tldr: 针对角色扮演大模型中人格控制不稳定的问题，本文提出一种基于对比稀疏自编码器（SAE）的细粒度人格控制框架。该方法通过学习与大五人格30个子维度对齐的控制向量，并结合特征激活路由模块，实现了对模型人格特征的精确且可解释的引导。实验表明，该方法在保持对话连贯性的同时，显著提升了角色一致性，优于仅使用提示词或对比激活增加等基线方法。
selection_source: fresh_fetch
motivation: 现有的提示词方法在长对话中易出现人格漂移，而监督微调则面临数据依赖和灵活性不足的挑战。
method: 提出对比SAE框架学习大五人格维度的控制向量，并利用特征激活路由模块在模型残差空间中动态集成这些向量。
result: 实验证明该方法在角色忠实度和输出质量上均优于基线，且SAE与提示词结合的配置达到了最佳性能。
conclusion: 通过对比训练的潜空间向量能有效增强大模型的人格控制能力，同时确保对话的逻辑一致性。
---

## 摘要
角色扮演智能体 (RPAs) 的人格控制通常通过免训练方法（通过提示词或检索增强生成注入人格描述和记忆）或在特定人格语料库上进行有监督微调 (SFT) 来实现。虽然 SFT 可能有效，但它需要带有人格标签的数据，并且针对新角色需要重新训练，限制了灵活性。相比之下，基于提示词和 RAG 的信号易于应用，但在长对话中可能会被稀释，导致人格行为漂移且有时不一致。为了解决这一问题，我们提出了一个对比稀疏自编码器 (SAE) 框架，该框架学习与大五人格 30 维度模型对齐的维度级人格控制向量。我们构建了一个包含 15,000 个样本的新型防泄漏语料库，为每个维度提供平衡的监督。学习到的向量被集成到模型的残差空间中，并由特质激活路由模块动态选择，从而实现精确且可解释的人格引导。在大语言模型 (LLMs) 上的实验表明，所提方法在各种语境设置下保持了稳定的角色忠实度和输出质量，优于对比激活叠加 (CAA) 和仅提示词的基准方法。SAE+Prompt 的组合配置实现了最佳的整体性能，证实了对比训练的潜向量可以在保持对话连贯性的同时增强人格控制。

## Abstract
Personality control in Role-Playing Agents (RPAs) is commonly achieved via training-free methods that inject persona descriptions and memory through prompts or retrieval-augmented generation, or via supervised fine-tuning (SFT) on persona-specific corpora. While SFT can be effective, it requires persona-labeled data and retraining for new roles, limiting flexibility. In contrast, prompt- and RAG-based signals are easy to apply but can be diluted in long dialogues, leading to drifting and sometimes inconsistent persona behavior. To address this, we propose a contrastive Sparse AutoEncoder (SAE) framework that learns facet-level personality control vectors aligned with the Big Five 30-facet model. A new 15,000-sample leakage-controlled corpus is constructed to provide balanced supervision for each facet. The learned vectors are integrated into the model's residual space and dynamically selected by a trait-activated routing module, enabling precise and interpretable personality steering. Experiments on Large Language Models (LLMs) show that the proposed method maintains stable character fidelity and output quality across contextualized settings, outperforming Contrastive Activation Addition (CAA) and prompt-only baselines. The combined SAE+Prompt configuration achieves the best overall performance, confirming that contrastively trained latent vectors can enhance persona control while preserving dialogue coherence.

---

## 论文详细总结（自动生成）

以下是对论文《Facet-Level Persona Control by Trait-Activated Routing with Contrastive SAE for Role-Playing LLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**角色扮演智能体（RPA）的人格一致性控制**。
*   **研究背景**：当前 RPA 主要依赖提示词（Prompt）或检索增强生成（RAG）来维持人格，但在长对话中，这些信号容易被稀释，导致“人格漂移”；而监督微调（SFT）虽然有效，但成本高、灵活性差且易导致模型通用能力下降。
*   **核心问题**：如何在不更新模型参数的前提下，实现精确、稳定且具有可解释性的细粒度（维度级）人格控制，使其在复杂语境下依然能保持角色忠实度。

### 2. 方法论：核心思想与关键技术
论文提出了一种结合**对比稀疏自编码器（Contrastive SAE）**与**特质激活路由（Trait-Activated Routing）**的框架。
*   **核心思想**：在模型的残差空间中寻找与特定人格特质对齐的“控制向量（CV）”，通过动态注入这些向量来引导模型输出。
*   **关键技术细节**：
    1.  **对比 SAE 训练**：在 SAE 的潜空间中引入对比学习（Contrastive Learning）。通过最小化原型对比损失（$L_{CE}$）和正向拉取损失（$L_{dist}$），使学习到的控制向量在同一维度内更紧凑，在不同维度间更具区分度。
    2.  **维度级控制**：基于大五人格模型（Big Five），将其细化为 30 个子维度（Facets），为每个子维度学习独立的控制向量。
    3.  **特质激活路由（Agent-Based Decision Module）**：利用基础模型作为路由代理，根据用户输入的上下文动态判断当前最需要激活的人格维度，从而精准注入向量，减少无关维度的噪声干扰。
    4.  **向量注入**：将选定的控制向量按强度 $\alpha$ 加到模型中间层的残差流中：$h'(x) = h(x) + \alpha v$。

### 3. 实验设计
*   **数据集**：
    *   **构建数据集**：自建了一个包含 15,000 个样本的防泄漏大五人格语料库（每个维度 500 个样本），涵盖 30 个细分维度，采用第一人称表述。
    *   **评估基准**：使用 In-Character 框架，包含 44 个抽象问题和 44 个情境化（Contextualized）问题。
*   **对比方法**：
    *   **Base RPA**：无干预基线。
    *   **Prompt-Label**：仅使用人格标签提示词。
    *   **CV-CAA**：对比激活增加（Contrastive Activation Addition）方法。
    *   **CV-SAE**：本文提出的基于 SAE 的控制向量方法。
    *   **Combined (SAE+Prompt)**：结合了控制向量与提示词的混合方法。
*   **评估指标**：全维度准确率（FA）、均方误差（MSE）、平均绝对误差（MAE）以及多轮对话异常率（MTR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练细节**：提到 SAE 是在 WikiText-103 语料库上预训练的，控制向量的微调基于自建的 15k 样本。实验中使用的模型为 Qwen3-4B-Instruct 和 Mistral-7B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流开源模型（4B 和 7B 规模）上进行了验证。
    *   对比了 6 种不同的控制策略。
    *   进行了**消融实验**，验证了对比学习（CL）对向量纯度的提升作用。
    *   进行了**敏感性分析**，探讨了注入强度 $\alpha$ 对性能和对话稳定性的影响。
*   **充分性评价**：实验设计较为充分，不仅涵盖了静态的人格测试，还通过情境化问题模拟了真实应用场景。通过 MTR 指标关注了模型在受控状态下的对话流畅度，体现了评估的客观性。

### 6. 主要结论与发现
*   **SAE 优于 CAA**：基于 SAE 学习到的控制向量比直接通过均值差得到的 CAA 向量更“纯净”，在提升人格准确率的同时，对对话逻辑的干扰更小。
*   **组合效应**：**CV-SAE + Prompt** 的配置在所有指标上均表现最优，FA 显著提升，MSE/MAE 降至最低。
*   **情境适应性**：控制向量方法在处理情境化问题时表现出比纯提示词方法更强的鲁棒性，能够更好地适应复杂的社交场景。
*   **对比学习的关键性**：消融实验证明，没有对比学习，控制向量会偏离目标方向，甚至导致性能下降。

### 7. 优点
*   **细粒度控制**：实现了大五人格 30 个维度的精准控制，远超传统粗粒度的 5 维度控制。
*   **高稳定性**：通过动态路由模块解决了多个控制向量同时注入时的冲突问题，降低了噪声。
*   **可解释性**：SAE 的潜空间特征具有天然的可解释性，使得人格引导过程不再是完全的黑盒。
*   **免微调**：作为一种推理时（Inference-time）控制方法，它不需要修改模型权重，具有极高的部署灵活性。

### 8. 不足与局限
*   **模型规模限制**：实验仅在 4B 和 7B 模型上进行，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **数据依赖**：控制向量的质量高度依赖于自建的 15k 语料库的准确性，虽然有心理学学生参与校验，但仍可能存在主观偏差。
*   **路由开销**：引入 Agent 进行路由决策会增加额外的推理延迟和计算开销。
*   **应用场景单一**：目前主要集中在大五人格框架下，对于更复杂、更具文学性的角色特质（如特定角色的口癖、独特经历）的覆盖尚不明确。

（完）
