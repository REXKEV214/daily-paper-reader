---
title: Facet-Level Persona Control by Trait-Activated Routing with Contrastive SAE for Role-Playing LLMs
title_zh: 基于对比 SAE 与特质激活路由的角色扮演大语言模型维度级人格控制
authors: "Wenqiu Tang, Zhen Wan, Takahiro Komamizu, Ichiro Ide"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19157v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 角色扮演LLM的细粒度人格控制
tldr: 针对角色扮演大模型中人格控制不稳定的问题，本文提出一种基于对比稀疏自编码器（SAE）的细粒度人格控制框架。该方法结合大五人格30个维度，通过对比学习提取人格特征向量，并利用特征激活路由模块动态调整模型残差空间。实验表明，该方法在保持对话连贯性的同时，显著提升了角色一致性和控制精度，优于仅使用提示词或对比激活增加等基线方法。
selection_source: fresh_fetch
motivation: 现有的提示词或微调方法在长对话中容易出现人格漂移，且缺乏对细粒度人格特征的灵活控制。
method: 提出对比SAE框架学习大五人格维度的控制向量，并结合新构建的1.5万样本语料库，通过特征激活路由实现动态人格引导。
result: 实验证明该方法在角色忠实度和输出质量上优于CAA和纯提示词基线，SAE与提示词结合的效果最佳。
conclusion: 对比训练的潜空间向量能有效增强大模型的人格控制能力，同时确保对话的连贯性与解释性。
---

## 摘要
角色扮演智能体（RPAs）的人格控制通常通过免训练方法（通过提示词或检索增强生成注入人格描述和记忆）或在特定人格语料库上进行有监督微调（SFT）来实现。虽然 SFT 可能有效，但它需要带有人格标签的数据，且针对新角色需要重新训练，限制了灵活性。相比之下，基于提示词和 RAG 的信号易于应用，但在长对话中可能会被稀释，导致人格行为漂移且有时不一致。为了解决这一问题，我们提出了一个对比稀疏自编码器（SAE）框架，用于学习与大五人格 30 维度模型对齐的维度级人格控制向量。我们构建了一个包含 15,000 个样本的防泄漏新语料库，为每个维度提供均衡的监督。学习到的向量被集成到模型的残差空间中，并由特质激活路由模块动态选择，从而实现精确且可解释的人格引导。在大语言模型（LLMs）上的实验表明，所提方法在各种语境设置下保持了稳定的角色忠实度和输出质量，优于对比激活叠加（CAA）和仅提示词的基准方法。SAE+Prompt 的组合配置实现了最佳的综合性能，证实了对比训练的潜向量可以在保持对话连贯性的同时增强人格控制。

## Abstract
Personality control in Role-Playing Agents (RPAs) is commonly achieved via training-free methods that inject persona descriptions and memory through prompts or retrieval-augmented generation, or via supervised fine-tuning (SFT) on persona-specific corpora. While SFT can be effective, it requires persona-labeled data and retraining for new roles, limiting flexibility. In contrast, prompt- and RAG-based signals are easy to apply but can be diluted in long dialogues, leading to drifting and sometimes inconsistent persona behavior. To address this, we propose a contrastive Sparse AutoEncoder (SAE) framework that learns facet-level personality control vectors aligned with the Big Five 30-facet model. A new 15,000-sample leakage-controlled corpus is constructed to provide balanced supervision for each facet. The learned vectors are integrated into the model's residual space and dynamically selected by a trait-activated routing module, enabling precise and interpretable personality steering. Experiments on Large Language Models (LLMs) show that the proposed method maintains stable character fidelity and output quality across contextualized settings, outperforming Contrastive Activation Addition (CAA) and prompt-only baselines. The combined SAE+Prompt configuration achieves the best overall performance, confirming that contrastively trained latent vectors can enhance persona control while preserving dialogue coherence.

---

## 论文详细总结（自动生成）

这是一份关于论文《Facet-Level Persona Control by Trait-Activated Routing with Contrastive SAE for Role-Playing LLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**角色扮演大模型（RPA）中的人格一致性控制**。
*   **研究动机**：现有的角色扮演方法主要分为“免训练类”（如提示词工程、RAG）和“训练类”（如 SFT 微调）。前者在长对话中容易产生“人格漂移”或指令遵循退化；后者则面临数据标注难、计算成本高以及模型通用能力受损（灾难性遗忘）的问题。
*   **核心问题**：如何在不更新模型参数的前提下，实现轻量级、可解释且能抵抗长上下文干扰的细粒度人格控制？

### 2. 方法论：核心思想与关键技术
论文提出了一种基于**对比稀疏自编码器（Contrastive SAE）**和**特质激活路由**的推理时控制框架。
*   **核心思想**：利用 SAE 在模型的潜空间中提取出与特定人格维度（Facet）对齐的特征向量，通过对比学习增强这些向量的“纯度”，并在推理时根据对话情境动态注入这些向量。
*   **关键技术细节**：
    1.  **对比 SAE 训练**：在 SAE 的稀疏潜空间中引入对比学习目标。
        *   **原型对比损失 ($L_{CE}$)**：借鉴 ArcFace 思想，引入角度边距，将表示推向正向人格原型，远离负向原型。
        *   **正向拉取损失 ($L_{dist}$)**：在活性子空间中最小化与正向质心的距离。
    2.  **控制向量（CV）生成**：通过计算正负样本在 SAE 编码后的质心差异，并筛选出“最活跃”的特征维度来构建 CV。
    3.  **代理决策模块（Agent-Based Decision Module）**：利用基座模型作为路由代理，分析用户查询最相关的性格维度，动态选择并注入对应的 CV，避免多个性格维度向量相互干扰。
    4.  **注入方式**：将 CV 以特定强度 $\alpha$ 加到模型中间层的残差流（Residual Stream）中。

### 3. 实验设计
*   **数据集**：
    *   **构建集**：自建 15,000 个样本的“大五人格 30 维度语料库”，每个维度包含 500 个正负对齐的场景化样本。
    *   **评估集**：改进了 In-Character 基准，将 44 个抽象的人格测试问题重写为“情境化问题”，以减少社会期望偏差。
*   **对比方法**：
    *   **Base RPA**：无干预基准。
    *   **Prompt-Label**：仅使用显式的人格标签提示词。
    *   **CV-CAA**：对比激活增加（Contrastive Activation Addition）方法。
    *   **CV-SAE**：本文提出的基于 SAE 的控制向量方法。
    *   **Combined (CV+Prompt)**：向量注入与提示词结合。
*   **评估指标**：全维度准确率（FA）、均方误差（MSE）、平均绝对误差（MAE）以及多轮对话异常率（MTR）。

### 4. 资源与算力
*   **模型规模**：实验使用了 Qwen3-4B-Instruct 和 Mistral-7B-Instruct。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到 SAE 是在 WikiText-103 语料库上预训练的，且 CV 的生成属于轻量级操作，不需要对大模型进行全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流开源模型系列，针对 26 个不同的人格角色进行了测试。
*   **消融实验**：详细对比了有无对比学习（CL）的效果，以及不同注入强度 $\alpha$ 对性能和稳定性的影响。
*   **充分性评价**：实验设计较为充分，不仅对比了准确性，还通过 MTR 指标关注了生成质量（如是否复读、是否脱离角色）。通过抽象问题与情境化问题的对比，验证了方法的泛化能力。

### 6. 主要结论与发现
*   **CV-SAE 优于 CAA**：SAE 提取的向量比直接做差得到的 CAA 向量更“纯”，在增强人格特征的同时，对模型原始对话能力的干扰更小（MTR 更低）。
*   **组合效应**：**CV-SAE + Prompt** 的组合达到了最佳性能，全维度准确率在两个模型上均接近或超过 88%。
*   **情境适应性**：在情境化问题下，基于向量的方法表现出比纯提示词方法更强的鲁棒性，证明了潜空间干预能更深层地影响模型行为。
*   **对比学习的关键性**：消融实验证明，没有对比学习的 SAE 向量会导致准确率大幅下降，甚至不如未干预状态。

### 7. 优点
*   **细粒度控制**：实现了大五人格 30 个子维度的精确控制，而非粗略的五大类。
*   **高稳定性**：通过动态路由模块解决了多个控制向量同时注入时的冲突问题。
*   **可解释性**：SAE 的稀疏特性使得控制方向具有更好的生物学/心理学解释基础。
*   **免微调**：作为一种推理时干预手段，极大地降低了部署和定制新角色的成本。

### 8. 不足与局限
*   **数据依赖**：CV 的质量高度依赖于自建的 15k 语料库，若该语料库存在 GPT-5 生成的固有偏见，可能会传导至控制向量。
*   **层级选择**：实验仅在中间层（15/16层）进行注入，虽然符合前人经验，但未深入探讨多层联合注入或不同任务下最佳层的动态变化。
*   **应用限制**：目前主要针对大五人格模型，对于更复杂、更具文学色彩的非结构化人格特征（如“傲娇”、“腹黑”）的覆盖能力尚待验证。

（完）
