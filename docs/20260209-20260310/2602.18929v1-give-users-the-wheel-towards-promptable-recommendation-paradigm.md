---
title: "Give Users the Wheel: Towards Promptable Recommendation Paradigm"
title_zh: 让用户掌握方向盘：迈向可提示推荐范式
authors: "Fuyuan Lyu, Chenglin Luo, Qiyuan Zhang, Yupeng Hou, Haolun Wu, Xing Tang, Xue Liu, Jin L. C. Guo, Xiuqiang He"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18929v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 通过自然语言提示建模用户偏好和意图
tldr: 针对传统序列推荐模型难以处理显式用户意图以及大语言模型推荐效率低的问题，本文提出了可提示序列推荐（DPR）框架。DPR 是一种模型无关的架构，通过在检索空间直接调制用户表征，使传统模型能根据自然语言指令动态调整推荐结果。该框架引入了融合模块、混合专家（MoE）架构及三阶段训练策略，实现了语义指令与协同过滤信号的深度对齐，在保持传统推荐性能的同时，显著提升了指令引导下的推荐准确性。
selection_source: fresh_fetch
motivation: 传统推荐模型难以响应用户偏离历史习惯的即时显式意图，而现有大模型方案在检索效率和协同精度上存在局限。
method: 提出 DPR 框架，利用融合模块对齐语义与协同信号，并通过 MoE 架构和三阶段训练策略在检索空间内动态调制用户表征。
result: 在真实数据集上的实验表明，DPR 在指令引导任务中显著优于现有基准，且在标准序列推荐场景中保持了竞争力。
conclusion: 该研究为传统推荐系统赋予了原生支持自然语言指令的能力，实现了协同过滤与语义推理的有效解耦与融合。
---

## 摘要
传统的序列推荐模型在挖掘隐式行为模式方面取得了显著成功。然而，这些架构在结构上对显式用户意图是盲目的：当用户的即时目标（例如，通过自然语言提示表达）与其历史习惯发生偏离时，它们难以进行调整。虽然大语言模型（LLMs）提供了理解此类意图的语义推理能力，但现有的集成范式面临两难境地：LLM 作为推荐器（LLM-as-a-recommender）范式牺牲了基于 ID 检索的效率和协同精准度，而重排序（Reranking）方法则本质上受限于底层模型的召回能力。在本文中，我们提出了解耦可提示序列推荐（DPR），这是一个与模型无关的框架，使传统的序列骨干网络能够原生支持可提示推荐，即在不放弃协同信号的情况下，利用自然语言动态引导检索过程的能力。DPR 直接在检索空间内调节潜在用户表示。为实现这一目标，我们引入了一个融合模块来对齐协同和语义信号，一个混合专家（MoE）架构来解耦来自正向和负向引导的冲突梯度，以及一个三阶段训练策略来逐步将提示的语义空间与协同空间对齐。在真实世界数据集上的广泛实验表明，DPR 在提示引导任务中显著优于最先进的基准模型，同时在标准序列推荐场景中保持了具有竞争力的性能。

## 速览
**TLDR**：针对传统序列推荐模型难以捕捉用户显式意图的问题，本文提出了解耦可提示序列推荐（DPR）框架。该框架是一种模型无关的方案，允许用户通过自然语言提示动态引导检索过程。DPR通过融合模块对齐协同与语义信号，利用混合专家（MoE）架构解决正负引导的梯度冲突，并采用三阶段训练策略实现语义与协同空间的对齐。该方法在保持传统推荐性能的同时，显著提升了模型对用户即时意图的响应能力。 \
**Motivation**：传统序列推荐模型无法有效处理用户通过自然语言表达的即时显式意图，而现有大模型方案在检索效率和召回精度上存在瓶颈。 \
**Method**：提出DPR框架，通过在检索空间直接调制用户表征，并结合MoE架构和三阶段训练策略来对齐语义与协同信号。 \
**Result**：实验证明DPR在提示引导的推荐任务中显著超越了现有最先进模型，并在标准序列推荐任务中保持了极高的竞争力。 \
**Conclusion**：该研究成功实现了可提示推荐范式，让用户能够通过自然语言实时操控推荐引擎，同时兼顾了协同过滤的精准性。

---

## Abstract
Conventional sequential recommendation models have achieved remarkable success in mining implicit behavioral patterns. However, these architectures remain structurally blind to explicit user intent: they struggle to adapt when a user's immediate goal (e.g., expressed via a natural language prompt) deviates from their historical habits. While Large Language Models (LLMs) offer the semantic reasoning to interpret such intent, existing integration paradigms force a dilemma: LLM-as-a-recommender paradigm sacrifices the efficiency and collaborative precision of ID-based retrieval, while Reranking methods are inherently bottlenecked by the recall capabilities of the underlying model. In this paper, we propose Decoupled Promptable Sequential Recommendation (DPR), a model-agnostic framework that empowers conventional sequential backbones to natively support Promptable Recommendation, the ability to dynamically steer the retrieval process using natural language without abandoning collaborative signals. DPR modulates the latent user representation directly within the retrieval space. To achieve this, we introduce a Fusion module to align the collaborative and semantic signals, a Mixture-of-Experts (MoE) architecture that disentangles the conflicting gradients from positive and negative steering, and a three-stage training strategy that progressively aligns the semantic space of prompts with the collaborative space. Extensive experiments on real-world datasets demonstrate that DPR significantly outperforms state-of-the-art baselines in prompt-guided tasks while maintaining competitive performance in standard sequential recommendation scenarios.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DPR (Decoupled Promptable Sequential Recommendation)** 的新框架，旨在解决传统推荐系统无法实时响应用户显式指令的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：传统的序列推荐模型（如 SASRec、GRU4Rec）擅长挖掘隐式行为模式，但对用户的**显式即时意图**（如通过自然语言表达的需求）处于“盲视”状态。
*   **核心痛点**：
    *   **传统模型惯性**：如果一个动作片爱好者临时想看儿童片，传统模型会因历史数据惯性继续推荐动作片。
    *   **现有 LLM 方案的局限**：将 LLM 直接作为推荐器会牺牲协同过滤的精度且推理延迟高；而将 LLM 作为重排序器（Reranking）则受限于底层召回模型的范围，如果召回阶段没抓到相关项，重排也无济于事。
*   **研究目标**：实现“可提示推荐（Promptable Recommendation）”，让用户能通过自然语言指令（正向引导或负向约束）动态干预检索过程，同时保留协同过滤的效率。

### 2. 方法论
DPR 采用了一种**模型无关（Model-agnostic）**的解耦架构，核心思想是在检索空间内直接调制用户表征。
*   **核心组件**：
    1.  **序列编码器**：使用现有的骨干网络（如 SASRec）提取基于历史的用户兴趣向量 $h_u$。
    2.  **提示词嵌入器**：利用预训练模型（如 Sentence-BERT）将文本指令转换为语义向量 $c_p$。
    3.  **信号融合模块（MoE Tower）**：设计了**混合专家架构**，包含正向融合块和负向融合块。通过多头交叉注意力机制（MHCA）将语义信号注入用户表征，并使用残差连接保持稳定性。
*   **关键技术细节**：
    *   **意图路由**：利用零样本分类器（BART）自动识别指令是“想要（正向）”还是“不要（负向）”，并路由至对应的专家路径，解决正负指令在优化时的梯度冲突。
    *   **三阶段训练策略**：
        *   阶段 1：基础序列推荐预训练。
        *   阶段 2：粗粒度对齐（利用类别/标签进行初步语义对齐）。
        *   阶段 3：深层语义对齐（利用 LLM 生成的增强标签进行细粒度对齐，并进行词汇解耦以增强泛化性）。
*   **损失函数**：采用统一的交叉熵损失，针对正向任务最大化目标项似然，针对负向任务通过 Softmax 竞争机制抑制受限项。

### 3. 实验设计
*   **数据集**：
    *   **MovieLens-1M (ML1M)**：电影推荐场景。
    *   **MIND**：大规模新闻推荐场景。
*   **任务类型**：标准序列推荐、正向引导任务（Positive Steering）、负向约束任务（Negative Suppression）。
*   **对比基准（Benchmarks）**：
    *   **传统模型**：SASRec、GRU4Rec 及其微调版本。
    *   **启发式过滤**：在推荐结果后进行硬编码标签过滤（Filter）。
    *   **LLM 推荐模型**：Qwen2.5-7B、Llama-2-7B、DeepSeek-V3、RecGPT、RecLM-gen 等。
    *   **LLM 重排序**：使用 Llama2 和 DeepSeek 对 Top-K 结果进行重排。
*   **评估指标**：NDCG@10/20、Recall@10/20，以及针对隐式意图的 **LLM-as-a-Judge** 评分。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用 PyTorch 实现，提示词嵌入使用了 `all-mpnet-base-v2`，路由分类使用了 `BART-large-mnli`。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同领域（电影、新闻）的数据集上进行了验证。
    *   设置了不同的目标集大小 $N$（3, 5, 10）来测试模型的鲁棒性。
    *   进行了详尽的**消融实验**，验证了三阶段训练、损失函数设计、以及 MoE 双塔架构的必要性。
    *   引入了 LLM 模拟真实用户复杂请求的实验（RQ3）。
*   **充分性评价**：实验设计较为全面，不仅对比了传统模型，还深入对比了当前最前沿的 LLM 推荐和重排方案。通过词汇解耦（测试集使用与训练集不同的近义词描述）确保了实验的客观性，避免了简单的关键词匹配。

### 6. 主要结论与发现
*   **性能领先**：DPR 在正向引导任务上相比最强的过滤基准提升了高达 **71.84%** (ML1M)，在负向约束任务上也表现优异。
*   **端到端优势**：DPR 能够主动改变排序顺序，将符合意图的项“拉”到最前面，而不仅仅是过滤掉不符合的项。
*   **优于 LLM 方案**：DPR 在指令遵循能力上超过了经过微调的推荐专用 LLM（如 RecLM），且推理效率远高于 LLM 重排序方案。
*   **解耦的必要性**：消融实验证明，如果将正负指令放在同一个参数空间处理，会导致严重的性能下降。

### 7. 优点
*   **架构优雅**：模型无关的设计使其可以轻松集成到任何现有的向量检索推荐系统中。
*   **效率与精度平衡**：保留了 ID 索引的协同过滤精度，同时赋予了系统类似 LLM 的语义理解能力。
*   **处理冲突能力强**：通过 MoE 架构有效解决了正向增强和负向抑制之间的优化冲突。
*   **泛化性好**：三阶段训练和语义增强使模型能理解从未见过的自然语言描述。

### 8. 不足与局限
*   **评估依赖模拟**：由于缺乏真实的用户指令数据集，实验中的指令和评估（LLM-as-a-Judge）在很大程度上依赖于 LLM 生成，可能存在一定的模型偏见。
*   **冷启动限制**：虽然增强了语义能力，但模型仍然依赖于底层骨干网络对 Item ID 的学习，对于完全未见过的冷启动物品处理能力有限。
*   **领域局限**：实验主要集中在电影和新闻领域，在电商等更复杂的约束场景（如价格区间、品牌偏好等）下的表现尚待验证。

（完）
