---
title: "SafeNeuron: Neuron-Level Safety Alignment for Large Language Models"
title_zh: SafeNeuron：大语言模型的神经元级安全对齐
authors: "Zhaoxin Wang, Jiaming Liang, Fengbin Zhu, Weixiang Zhao, Junfeng Fang, Jiayi Ji, Handing Wang, Tat-Seng Chua"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12158v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 神经元级安全对齐以防止有害内容生成
tldr: SafeNeuron是一个神经元级安全对齐框架，旨在解决大模型安全机制因参数稀疏而易受攻击的问题。该方法通过识别并冻结关键安全神经元，在优化过程中强制模型构建冗余的安全表示。实验证明，SafeNeuron显著增强了模型对抗剪枝攻击的鲁棒性，防止了模型被恶意篡改，同时保留了其通用能力，为模型对齐提供了可解释的新视角。
selection_source: fresh_fetch
motivation: 现有大模型的安全对齐行为集中在极少数神经元上，导致对齐机制脆弱且易被神经元级攻击绕过。
method: 识别安全相关神经元并在偏好优化期间将其冻结，从而迫使模型在网络中构建冗余且分布式的安全表示。
result: 实验表明该方法显著提升了模型对抗剪枝攻击的鲁棒性，并有效降低了开源模型被重新训练为有害生成器的风险。
conclusion: SafeNeuron通过内部表示的重新分布强化了模型安全性，为构建更稳健、可解释的对齐机制提供了新方案。
---

## 摘要
大语言模型 (LLMs) 和多模态大语言模型在发布前通常会进行安全对齐，以防止生成有害内容。然而，最近的研究表明，安全行为集中在极少数参数子集中，这使得对齐变得脆弱，且容易通过神经元级攻击被绕过。此外，大多数现有的对齐方法在行为层面运行，对模型内部安全机制的控制有限。在这项工作中，我们提出了 SafeNeuron，这是一个神经元级的安全对齐框架，通过在整个网络中重新分布安全表示来提高鲁棒性。SafeNeuron 首先识别与安全相关的神经元，然后在偏好优化过程中冻结这些神经元，以防止对稀疏安全路径的依赖，并强制模型构建冗余的安全表示。在不同模型和模态上的广泛实验表明，SafeNeuron 显著提高了针对神经元剪枝攻击的鲁棒性，降低了开源模型被重新用作红队生成器的风险，并保留了通用能力。此外，我们的逐层分析揭示了安全行为是由稳定且共享的内部表示所支配的。总之，SafeNeuron 为模型对齐提供了一个可解释且鲁棒的视角。

## Abstract
Large language models (LLMs) and multimodal LLMs are typically safety-aligned before release to prevent harmful content generation. However, recent studies show that safety behaviors are concentrated in a small subset of parameters, making alignment brittle and easily bypassed through neuron-level attacks. Moreover, most existing alignment methods operate at the behavioral level, offering limited control over the model's internal safety mechanisms. In this work, we propose SafeNeuron, a neuron-level safety alignment framework that improves robustness by redistributing safety representations across the network. SafeNeuron first identifies safety-related neurons, then freezes these neurons during preference optimization to prevent reliance on sparse safety pathways and force the model to construct redundant safety representations. Extensive experiments across models and modalities demonstrate that SafeNeuron significantly improves robustness against neuron pruning attacks, reduces the risk of open-source models being repurposed as red-team generators, and preserves general capabilities. Furthermore, our layer-wise analysis reveals that safety behaviors are governed by stable and shared internal representations. Overall, SafeNeuron provides an interpretable and robust perspective for model alignment.

---

## 论文详细总结（自动生成）

### SafeNeuron：大语言模型的神经元级安全对齐——论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的 LLM 安全对齐（如 SFT 和 RLHF）主要在“行为层面”进行优化，即只约束输入输出模式，而不显式约束内部机制。研究发现，模型的安全行为往往集中在极少数神经元上（稀疏性）。
*   **核心问题**：这种稀疏的安全机制极其脆弱，攻击者只需通过神经元剪枝（Pruning）或微小扰动即可绕过对齐限制。此外，开源模型容易被恶意微调为“红队工具”。
*   **整体含义**：论文旨在探索模型内部的安全机制，并提出一种通过构建“冗余安全路径”来增强模型鲁棒性的对齐框架。

#### 2. 论文提出的方法论
SafeNeuron 框架包含两个核心步骤：
*   **安全神经元识别（Training-free）**：
    *   **激活效应量 (ES)**：通过对比有害（Unsafe）和无害（Safe）输入下的神经元激活分布，利用统计学中的标准化均值差来衡量神经元的区分能力。
    *   **安全激活偏移 (SAS)**：测量神经元在面对有害内容时激活强度的方向性变化，识别那些被显著放大的神经元。
    *   **融合**：取 ES 和 SAS 识别出的神经元并集作为初始安全神经元集。
*   **冗余安全机制构建**：
    *   **冻结与对齐**：在进行偏好优化（如 DPO）时，将已识别的安全神经元参数冻结（梯度设为 0），仅允许更新其余参数。
    *   **强制冗余**：由于原有的安全路径被封锁，模型被迫利用剩余参数构建新的、冗余的安全表示。
    *   **迭代强化**：通过多次“识别-冻结-再训练”的循环，使安全行为在网络中分布得更加广泛和稳健。

#### 3. 实验设计
*   **实验对象**：涵盖了多种主流模型，包括 Llama3 (1B/3B/8B)、Qwen2.5 (1.5B/3B/7B/14B)、DeepSeek-R1-1.5B、Gemma-7B、Phi-4，以及多模态模型 Qwen2.5-VL-7B。
*   **数据集**：
    *   **识别阶段**：使用 HarmfulQA、CatHarmfulQA 等有害指令集，以及 NaturalReasoning 等良性指令集。
    *   **训练阶段**：PKU-SafeRLHF（文本）和 SPA-VL（多模态）。
    *   **评估阶段**：StrongReject（包含 DAN 等多种越狱攻击）、AdvBench、NSFW 图像集。
*   **对比方法**：原始指令微调模型、SN-Tune（增强特定安全组件的方法）、标准的 RLHF-Safety（基于 DPO 的行为级对齐）。
*   **评估指标**：攻击成功率（ASR）衡量安全性；ARC、GSM8K、TruthfulQA 衡量通用能力。

#### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **NVIDIA RTX H100 GPU** 上进行的。
*   **训练细节**：使用了 DPO 算法，学习率为 $5 \times 10^{-6}$，权重衰减为 0.05，训练轮数为 3 轮，采用余弦学习率调度器。
*   **效率提升**：相比于之前的神经元攻击/识别方法（如 NeuroStrike），SafeNeuron 的识别速度提升了 15-20 倍（在 Qwen2.5-7B 上仅需约 40 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 10 余个不同规模、不同架构的模型上进行了验证，实验组数非常多。
*   **维度覆盖**：不仅测试了纯文本场景，还扩展到了多模态（VLM）场景；不仅测试了原始安全性，还重点测试了“神经元剪枝攻击”下的鲁棒性。
*   **消融与分析**：进行了逐层分析、跨任务神经元重叠分析、迭代次数分析以及训练数据规模分析。
*   **客观性**：实验设计考虑了“过度拒绝”风险，通过混合良性数据来平衡安全与性能，评估过程使用了第三方模型（Llama-Guard-3）作为公正裁判，具有较高的客观性。

#### 6. 主要结论与发现
*   **安全表示的结构性**：安全行为并非孤立的规则，而是由网络中稳定、共享的内部表示（安全子空间）驱动的。
*   **冗余性带来鲁棒性**：通过 SafeNeuron 构建的冗余路径，使得模型即使在部分安全神经元被恶意剪枝后，依然能保持极高的拒绝率（ASR 显著低于基准方法）。
*   **跨模态通用性**：在文本上识别的安全神经元逻辑可以有效迁移至视觉模态，增强 VLM 对有害图像的抵御能力。
*   **能力保留**：该方法在大幅提升安全性的同时，几乎不损害模型的逻辑推理和常识问答能力。

#### 7. 优点
*   **可解释性强**：从神经元层面揭示了对齐机制的内部运作方式。
*   **防御被动攻击**：有效解决了开源模型通过简单参数微调或剪枝即可被“去安全化”的痛点。
*   **高效且无需训练识别**：识别过程仅需前向传播统计，极大地降低了分析成本。
*   **迭代机制创新**：通过“强制模型寻找备选路径”的思想，从根本上改变了对齐的分布形态。

#### 8. 不足与局限
*   **数据依赖**：安全神经元的识别质量高度依赖于所使用的“有害/良性”对比数据集的覆盖范围。
*   **DPO 局限性**：虽然神经元被冻结，但 DPO 训练本身仍需要高质量的偏好对数据，这在某些垂直领域可能难以获取。
*   **极端攻击风险**：虽然增强了冗余性，但如果攻击者拥有足够的算力进行全参数重新对齐（Disalignment），该方法可能仍面临挑战。
*   **复杂性**：迭代训练增加了对齐流程的复杂度和总训练时间。

（完）
