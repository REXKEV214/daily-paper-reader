---
title: "See and Remember: A Multimodal Agent for Web Traversal"
title_zh: 见与记：一种用于网页遍历的多模态智能体
authors: "Xinjun Wang, Shengyao Wang, Aimin Zhou, Hao Hao"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02626v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 用于智能体长期上下文和状态跟踪的显式记忆系统
tldr: "针对自主网页导航中存在的空间迷失和循环导航问题，本文提出了V-GEMS架构。该架构结合了视觉定位以精准识别交互元素，并引入显式内存栈进行状态追踪，支持有效回溯。通过引入动态基准测试，实验证明V-GEMS在网页遍历任务中表现优异，较基准模型性能提升了28.7%，显著增强了多模态智能体的导航鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的网页导航智能体在复杂视觉环境下容易出现空间定向障碍和导航死循环。
method: 提出V-GEMS架构，集成视觉定位解析模糊元素，并利用显式内存栈实现路径记录与有效回溯。
result: "实验显示V-GEMS在网页遍历任务中显著优于WebWalker基准，性能提升达28.7%。"
conclusion: 结合视觉定位与显式记忆系统能显著提升多模态智能体在深度网页导航任务中的精准度与鲁棒性。
---

## 摘要
自主网页导航要求智能体能够感知复杂的视觉环境并维持长期上下文，然而当前基于大语言模型（LLM）的智能体往往在空间迷失和导航循环方面面临挑战。在本文中，我们提出了具有普适性的 V-GEMS（视觉定位与显式记忆系统），这是一种专为精确且具韧性的网页遍历而设计的鲁棒多模态智能体架构。我们的智能体集成了视觉定位（visual grounding）以解析歧义交互元素，并引入了带有状态跟踪的显式记忆栈。这种双重机制使智能体能够维护其遍历路径的结构化地图，从而实现有效的回溯并防止深度导航任务中的循环失败。我们还引入了一个可更新的动态基准，以严格评估其适应性。实验表明，V-GEMS 显著优于 WebWalker 基线，实现了 28.7% 的大幅性能提升。代码可在 https://github.com/Vaultttttttttttt/V-GEMS 获取。

## Abstract
Autonomous web navigation requires agents to perceive complex visual environments and maintain long-term context, yet current Large Language Model (LLM) based agents often struggle with spatial disorientation and navigation loops. In this paper, we propose generally applicable V-GEMS(Visual Grounding and Explicit Memory System), a robust multimodal agent architecture designed for precise and resilient web traversal. Our agent integrates visual grounding to resolve ambiguous interactive elements and introduces an explicit memory stack with state tracking. This dual mechanism allows the agent to maintain a structured map of its traversal path, enabling valid backtracking and preventing cyclical failures in deep navigation tasks. We also introduce an updatable dynamic benchmark to rigorously evaluate adaptability. Experiments show V-GEMS significantly dominates the WebWalker baseline, achieving a substantial 28.7% performance gain. Code is available at https://github.com/Vaultttttttttttt/V-GEMS.

---

## 论文详细总结（自动生成）

以下是对论文《See and Remember: A Multimodal Agent for Web Traversal》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：自主网页导航是 AI 智能体（Agent）的核心能力，广泛应用于深度研究和工作流自动化。
*   **核心问题**：现有的基于大语言模型（LLM）的智能体在复杂网页导航中存在三大瓶颈：
    1.  **模态效率低下**：仅依赖文本 DOM 无法识别图标、横幅等视觉元素。
    2.  **导航停滞/迷失**：缺乏显式状态跟踪，容易陷入循环或在深层页面中无法正确回溯（类似没有“后退”键的浏览器）。
    3.  **符号计算不准**：LLM 在处理“查找前 5 篇论文”等定量任务时容易产生算术幻觉。
*   **研究目的**：提出 **V-GEMS** 架构，通过集成视觉感知与显式记忆系统，提升智能体在动态网页遍历中的鲁棒性和精确度。

### 2. 方法论
V-GEMS 在双智能体（探索者-评论者）架构基础上，引入了三个核心模块：
*   **符号计数器（Symbolic Counter）**：
    *   将定量跟踪与语言推理脱钩。
    *   **配额搜索模式**：设定目标数量（如 N 个），达到后立即终止，防止冗余开销。
    *   **穷举统计模式**：驱动智能体遍历所有分页，确保统计结果基于全量观察而非估计。
*   **URL 栈（URL Stack）**：
    *   建立外部拓扑记忆，强制执行深度优先搜索（DFS）策略。
    *   **Push（入栈）**：记录访问路径。
    *   **Pop（出栈）**：当遇到 404 错误或无关内容时，自主回溯到上一个有效状态，剪枝无效分支。
*   **自适应视觉理解分数计算器（US Calculator）**：
    *   动态评估页面的“文本可解性”，决定是否调用视觉语言模型（VLM）。
    *   **计算公式**：$\Phi(s_t) = S_{qual} + S_{rel} + S_{struct} - S_{spec}$。
    *   涵盖文本质量、语义相关性、结构清晰度和特殊情况惩罚（如验证码、纯图页面）。若分数低于阈值，则触发 VLM 以解析视觉布局。

### 3. 实验设计
*   **数据集/场景**：构建了 **EverWebQA** 动态基准测试，包含 680 个问答对。
    *   **领域**：教育、会议、组织、游戏。
    *   **任务类型**：单源任务（线性导航）和多源任务（跨页面推理）。
    *   **难度分级**：根据导航深度分为简单、中等、困难。
*   **基准模型（Baseline）**：**WebWalker**（目前最先进的双智能体网页遍历框架）。
*   **底层模型**：使用 Qwen-3-Coder-Plus 作为 LLM，Qwen3-VL-Plus 作为 VLM。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该系统主要基于现有的预训练模型（Qwen 系列）进行零样本（Zero-shot）推理和工具调用，因此不涉及大规模的从头训练算力，重点在于推理过程中的 API 调用和逻辑控制。

### 5. 实验数量与充分性
*   **实验规模**：在 680 个实时生成的问答对上进行了测试，样本量与基准模型持平，具有可比性。
*   **消融实验**：针对 VLM、URL 栈、计数器三个模块进行了详尽的组合测试（共 8 组配置），验证了各组件的独立贡献。
*   **分析维度**：引入了 **Shapley Value（沙普利值）** 来量化各模块在不同任务类型和领域中的边际贡献，实验设计较为客观、严谨且公平。

### 6 主要结论与发现
*   **性能飞跃**：V-GEMS 相比 WebWalker 实现了 **28.7%** 的性能提升（平均成功率从 0.49 升至 0.65）。
*   **模块分工**：
    *   **VLM** 在处理复杂布局的单源任务中贡献最大。
    *   **URL 栈** 是解决多源跨页面任务的关键，有效防止了导航丢失。
    *   **计数器** 显著降低了教育等领域的算术幻觉。
*   **错误抑制**：V-GEMS 显著减少了“拒绝回答”和“完全错误”的情况，将导航失败转化为成功的检索。

### 7. 优点
*   **自适应模态切换**：US 计算器平衡了感知精度与计算成本，避免了全量调用 VLM 带来的高延迟。
*   **显式状态管理**：URL 栈的设计解决了 Agent 长期存在的“循环导航”痛点，使其具备了类似人类的“后退”逻辑。
*   **动态基准测试**：EverWebQA 解决了静态数据集容易过时（链接失效、内容更新）的问题，确保了评估的时效性。

### 8. 不足与局限
*   **计算开销**：尽管有自适应切换，但引入 VLM 仍增加了推理延迟和 Token 成本。
*   **入口依赖**：目前仍依赖预定义的根 URL 开始探索，尚未实现完全开放域的互联网冲浪（如从搜索引擎开始）。
*   **缺乏自我进化**：决策逻辑主要基于启发式规划和上下文学习，缺乏端到端的强化学习（RL）机制来根据反馈自动优化策略。

（完）
