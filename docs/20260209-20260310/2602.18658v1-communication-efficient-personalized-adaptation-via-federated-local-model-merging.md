---
title: Communication-Efficient Personalized Adaptation via Federated-Local Model Merging
title_zh: 通过联邦-本地模型合并实现通信高效的个性化适配
authors: "Yinan Zou, Md Kamran Chowdhury Shisher, Christopher G. Brinton, Vishrant Tripathi"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18658v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 用于使LLM适应客户端任务的联邦个性化
tldr: 本文针对联邦学习中大规模模型适配的异构性挑战，提出了 Potara 框架。该框架通过合并捕获通用知识的联邦模型和捕获个性化知识的本地模型来实现个性化适配。基于线性模式连接性理论，研究者推导出最小化预期任务损失方差迹的闭式最优合并权重，确保合并模型性能优于单一模型。实验证明，Potara 在视觉和语言任务中均实现了更优的个性化性能与通信效率平衡。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中任务异构性导致的个性化适配难题，以及现有启发式合并方法缺乏理论支撑且通信开销大的问题。
method: 提出 Potara 框架，利用线性模式连接性理论推导出合并联邦模型与本地模型的最优闭式权重。
result: 在视觉和语言基准测试中，Potara 在显著提升个性化性能的同时有效降低了通信成本。
conclusion: 该研究为联邦个性化适配提供了一种理论严谨且通信高效的模型合并方案。
---

## 摘要
参数高效微调方法（如 LoRA）为将大型视觉和语言模型适配到客户端任务提供了一种实用的方式。然而，在联邦部署的任务级异构性下，这变得尤为具有挑战性。在这种情况下，个性化需要平衡通用知识与个性化知识，但现有方法主要依赖启发式混合规则，且缺乏理论依据。此外，先前的模型合并方法也是计算和通信密集型的，导致该过程在联邦设置中效率低下。在这项工作中，我们提出了 Potara，这是一个有原则的联邦个性化框架，它通过合并两个互补模型为每个客户端构建个性化模型：(i) 捕获通用知识的联邦模型，以及 (ii) 捕获个性化知识的本地模型。通过线性模式连接性（linear mode connectivity）的构建，我们证明了期望任务损失存在一个方差迹上界，最小化该上界可以得到闭式最优混合权重，从而保证合并模型的界限比单独的联邦模型或本地模型更紧。在视觉和语言基准测试上的实验表明，Potara 在减少通信的同时持续提升了个性化效果，实现了强大的性能-通信权衡。

## Abstract
Parameter-efficient fine-tuning methods, such as LoRA, offer a practical way to adapt large vision and language models to client tasks. However, this becomes particularly challenging under task-level heterogeneity in federated deployments. In this regime, personalization requires balancing general knowledge with personalized knowledge, yet existing approaches largely rely on heuristic mixing rules and lack theoretical justification. Moreover, prior model merging approaches are also computation and communication intensive, making the process inefficient in federated settings. In this work, we propose Potara, a principled framework for federated personalization that constructs a personalized model for each client by merging two complementary models: (i) a federated model capturing general knowledge, and (ii) a local model capturing personalized knowledge. Through the construct of linear mode connectivity, we show that the expected task loss admits a variance trace upper bound, whose minimization yields closed-form optimal mixing weights that guarantee a tighter bound for the merged model than for either the federated or local model alone. Experiments on vision and language benchmarks show that Potara consistently improves personalization while reducing communication, leading to a strong performance-communication trade-off.

---

## 论文详细总结（自动生成）

### 论文总结：通过联邦-本地模型合并实现通信高效的个性化适配 (POTARA)

#### 1. 核心问题与研究动机
随着大型视觉模型（LVM）和大型语言模型（LLM）的普及，如何将其高效适配到特定下游任务成为关键。在联邦学习（FL）场景下，面临两大挑战：
*   **任务异构性**：不同客户端的任务目标可能截然不同甚至冲突，单一的全局模型难以满足个性化需求。
*   **通信瓶颈**：尽管 LoRA 等参数高效微调方法减少了参数量，但在联邦训练中频繁传输模块仍产生巨大开销。
*   **缺乏理论支撑**：现有平衡“通用知识”与“个性化知识”的方法多依赖启发式规则（如网格搜索权重），缺乏严谨的数学证明。

#### 2. 方法论：POTARA 框架
POTARA（取名自《龙珠》中的坡塔拉耳环合体）的核心思想是**将联邦学习获得的“通用模型”与本地独立微调获得的“个性化模型”进行加权合并**。
*   **核心流程**：
    1.  **联邦阶段**：运行少量回合的 FedIT（联邦 LoRA 训练），获取捕获跨客户端通用知识的全局 LoRA 模块。
    2.  **本地阶段**：客户端在本地数据上独立微调，获取捕获特定任务知识的本地 LoRA 模块。
    3.  **模型合并**：通过凸组合（Convex Combination）合并两者的任务向量（Task Vectors）。
*   **关键技术细节**：
    *   **线性模式连接性 (LMC)**：假设联邦模型和本地模型处于损失函数的同一个平坦盆地（Basin）内。
    *   **理论推导**：利用拉普拉斯近似将任务向量建模为联合高斯分布。证明了预期任务损失存在一个由“方差迹（Variance Trace）”主导的上界。
    *   **闭式最优权重**：通过最小化该方差迹，推导出合并权重的闭式解（Closed-form solution）。该权重由 Fisher 信息矩阵（对角近似）计算得出，无需昂贵的超参数搜索。

#### 3. 实验设计
*   **数据集与场景**：
    *   **视觉基准**：使用 ViT-B/16 模型，在 CIFAR-100 上通过 Dirichlet 分布（$\alpha=0.5$）模拟标签异构性。
    *   **语言基准**：使用 LLaMA-3.2-3B-Instruct，在常识推理（Commonsense Reasoning）基准（包含 ARC, BoolQ, PIQA 等 8 个任务）上模拟任务异构性。
    *   **极端异构场景**：使用 Qwen3-4B-Instruct，在 FLAN 基准（涵盖阅读理解、情感分析等 8 类完全不同的任务）上测试。
*   **对比方法 (Benchmarks)**：
    *   包括独立本地微调 (Local)、标准联邦 LoRA (FedIT)、单侧聚合方法 (FFA-LoRA, FedSA)、以及先进的个性化联邦学习方法 (FedDPA, FedALT) 和 Fisher Merging。

#### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号和数量。
*   **训练时长**：在附录 F 中记录了计算耗时。例如，在 LLaMA-3.2-3B 上，计算合并权重仅需约 0.46 小时，而完整的训练过程（联邦+本地）约需 5.92 小时。
*   **参数规模**：涉及 3B 和 4B 规模的 LLM，暗示实验是在具备较高显存（如 A100 或 H100）的计算集群上完成的。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 10 客户端到 20 客户端的扩展性实验，跨越了视觉和语言两大领域。
*   **消融实验**：详细探讨了联邦训练回合数对合并效果的影响，并对比了理论计算权重与经验最优权重（网格搜索）的接近程度。
*   **评价**：实验设计较为充分且客观。通过在不同模型架构（ViT, LLaMA, Qwen）和不同异构程度的任务上验证，证明了方法的普适性。

#### 6. 主要结论与发现
*   **性能提升**：POTARA 在所有基准测试中均优于基准方法。在 CIFAR-100 上比最强基线高出 3.41% 平均准确率；在常识推理任务上提升了 1.85%。
*   **通信效率**：POTARA 仅需较少的联邦通信回合即可达到甚至超过长时间联邦训练的效果，显著降低了总通信开销。
*   **理论验证**：实验证实了合并模型的损失界限确实比单一模型更紧，且理论推导的最优权重与网格搜索找到的经验最优值高度一致。

#### 7. 优点
*   **理论严谨**：基于 LMC 和高斯近似给出了权重的数学解释，填补了模型合并领域缺乏理论依据的空白。
*   **计算高效**：相比于在全参数空间运行的 Fisher Merging，POTARA 仅在 LoRA 空间操作，计算开销极小，适用于超大规模模型。
*   **通信友好**：通过“先联邦、后合并”的策略，避免了全程高频通信。

#### 8. 不足与局限
*   **LMC 假设限制**：方法依赖于两个模型处于同一损失盆地的假设，如果联邦模型和本地模型偏差过大（例如初始化不同或训练极度不稳定），该前提可能失效。
*   **对角近似偏差**：为了计算效率使用了对角 Fisher 矩阵，忽略了参数间的协方差，可能在某些复杂任务中导致权重估计不够精确。
*   **初始模型依赖**：合并效果仍受限于联邦模型阶段学习到的通用知识质量，若联邦阶段回合数过少，可能无法提供足够的通用特征。

（完）
