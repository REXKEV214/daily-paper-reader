---
title: "SecureGate: Learning When to Reveal PII Safely via Token-Gated Dual-Adapters for Federated LLMs"
title_zh: SecureGate：面向联邦大语言模型，通过令牌门控双适配器学习何时安全地披露个人身份信息 (PII)
authors: "Mohamed Shaaban, Mohamed Elmahallawy"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13529v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 联邦大模型中的隐私保护用户数据
tldr: SecureGate是一个针对联邦大模型微调的隐私保护框架，旨在解决个人可识别信息（PII）泄露与模型效用之间的矛盾。它采用双LoRA适配器架构，通过安全适配器学习全局通用知识，通过揭示适配器捕捉本地敏感信息。利用令牌控制的门控模块，该框架能在推理时实现细粒度的隐私控制，在显著降低PII泄露风险的同时保持了高性能，为隐私敏感场景下的协作训练提供了新方案。
selection_source: fresh_fetch
motivation: 联邦大模型微调面临PII泄露风险以及全局泛化与本地效用之间的矛盾，而现有防御手段往往会严重损害模型性能。
method: 提出SecureGate框架，利用双LoRA适配器（安全与揭示）和令牌控制的门控模块，在推理阶段实现对敏感信息的选择性激活。
result: "实验表明，该方法在提升任务效用的同时，将推理攻击准确率降低了31.66倍，提取召回率降低了17.07倍，且路由可靠性达100%。"
conclusion: SecureGate通过细粒度的隐私控制机制，在不牺牲效用的前提下有效保护了联邦学习中的敏感信息，具有极高的实用价值。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下跨组织孤岛进行协作训练，使其在隐私敏感型应用中极具吸引力。随着大语言模型 (LLMs) 的快速普及，生成式大语言模型的联邦微调作为一种在保护机密性的同时利用分布式数据的方法受到了关注。然而，这种设置引入了根本性的挑战：(i) 由于大语言模型的记忆效应导致的个人身份信息 (PII) 隐私泄露，以及 (ii) 在异构数据下全局泛化与本地效用之间的持续紧张关系。现有的防御措施，如数据清洗和差分隐私，虽然减少了泄露，但往往会降低下游性能。我们提出了 SecureGate，这是一个面向大语言模型的隐私感知联邦微调框架，在不牺牲效用的情况下提供细粒度的隐私控制。SecureGate 采用双适配器 LoRA 架构：一个学习经过清洗的、全局可共享表示的安全适配器，以及一个捕获敏感的、组织特定知识的披露适配器。令牌控制的门控模块在推理时选择性地激活这些适配器，从而在无需重新训练的情况下实现受控的信息披露。在多个大语言模型和真实世界数据集上的广泛实验表明，SecureGate 在提高任务效用的同时显著减少了 PII 泄露，在未经授权的请求中，推理攻击准确率降低了高达 31.66 倍，提取召回率降低了 17.07 倍。此外，它对正确适配器的路由可靠性保持在 100%，且仅产生极小的计算和通信开销。

## Abstract
Federated learning (FL) enables collaborative training across organizational silos without sharing raw data, making it attractive for privacy-sensitive applications. With the rapid adoption of large language models (LLMs), federated fine-tuning of generative LLMs has gained attention as a way to leverage distributed data while preserving confidentiality. However, this setting introduces fundamental challenges: (i) privacy leakage of personally identifiable information (PII) due to LLM memorization, and (ii) a persistent tension between global generalization and local utility under heterogeneous data. Existing defenses, such as data sanitization and differential privacy, reduce leakage but often degrade downstream performance. We propose SecureGate, a privacy-aware federated fine-tuning framework for LLMs that provides fine-grained privacy control without sacrificing utility. SecureGate employs a dual-adapter LoRA architecture: a secure adapter that learns sanitized, globally shareable representations, and a revealing adapter that captures sensitive, organization-specific knowledge. A token-controlled gating module selectively activates these adapters at inference time, enabling controlled information disclosure without retraining. Extensive experiments across multiple LLMs and real-world datasets show that SecureGate improves task utility while substantially reducing PII leakage, achieving up to a 31.66X reduction in inference attack accuracy and a 17.07X reduction in extraction recall for unauthorized requests. Additionally, it maintains 100% routing reliability to the correct adapter and incurs only minimal computational and communication overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《SecureGate: Learning When to Reveal PII Safely via Token-Gated Dual-Adapters for Federated LLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在隐私敏感领域（如医疗、法律、金融），联邦学习（FL）被广泛用于跨组织协作微调大语言模型（LLMs）。然而，该领域面临两个核心挑战：
*   **PII 泄露风险**：LLM 具有强大的记忆能力，容易在微调过程中记住并泄露个人身份信息（PII）。
*   **效用与隐私的矛盾**：现有的防御手段（如数据清洗、差分隐私 DP）虽然能减少泄露，但往往会显著降低模型的全局泛化能力和本地任务效用。
*   **研究动机**：如何在保证强隐私保护（防止 PII 泄露）的同时，维持高水平的全局和本地模型性能，并确保系统在实际部署中的效率？

### 2. 方法论：核心思想与关键技术
论文提出了 **SecureGate** 框架，其核心思想是通过“双适配器”架构将安全知识与敏感知识解耦，并利用令牌（Token）实现推理时的访问控制。

*   **双适配器 LoRA 架构**：
    *   **安全适配器 (Secure Adapter)**：在脱敏数据（PII 被遮蔽或加噪）上训练，负责学习可共享的全局知识。该适配器参与联邦聚合。
    *   **揭示适配器 (Revealing Adapter)**：在原始敏感数据上训练，捕获组织特有的敏感知识。该适配器仅保留在本地，不参与共享。
*   **令牌控制门控模块 (Token-Gated Module)**：
    *   引入一个轻量级的 MLP 门控网络。
    *   **核心逻辑**：推理时，用户需提供特定的 [Special-Token]（访问密钥）。门控模块提取该 Token 的隐藏层状态，动态决定将请求路由至“安全适配器”还是“揭示适配器”。
*   **算法流程**：
    1.  **初始化**：本地生成脱敏和原始两份数据，初始化双适配器。
    2.  **联邦微调**：仅对安全适配器进行联邦聚合（采用 Nesterov 动量聚合）。
    3.  **个性化融合**：微调结束后，将全局安全适配器与本地适配器进行线性融合，优化融合系数。
    4.  **两阶段推理**：第一遍扫描 Token 确定路由，第二遍根据选定的适配器生成最终回复。

### 3. 实验设计
*   **数据集**：
    *   **Yelp Reviews**：用户评论数据，包含个人偏好和身份信息。
    *   **ECHR**：欧洲人权法院的法律文书，包含大量敏感法律细节和个人信息。
*   **Benchmark 与对比方法**：
    *   **聚合算法对比**：FedAvg, FedAdagrad, FedAdam, FedYogi, FedAvgM。
    *   **防御基准**：数据清洗（Scrubbing）、差分隐私（DP）以及两者的结合。
*   **评估指标**：
    *   **隐私性**：推理攻击准确率（Inference Accuracy）、提取攻击召回率（Extraction Recall）。
    *   **效用性**：困惑度（Perplexity, PPL）、任务准确率。
    *   **效率**：FLOPs、通信开销。

### 4. 资源与算力
*   **硬件环境**：论文明确指出实验运行在 Ubuntu 24.04 系统，配备 **Intel i9-14900K CPU** 和 **NVIDIA RTX PRO 6000 Blackwell GPU (96GB VRAM)**。
*   **软件框架**：基于 OpenFedLLM 实现，使用 Flair 库进行命名实体识别（NER）。
*   **训练细节**：LoRA 秩（Rank）设置为 4 到 16，本地训练 3 个 Epoch，联邦训练共 20 轮。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10、20、30 个客户端的扩展性实验，验证了在不同网络规模下的稳定性。
*   **模型覆盖**：在 Qwen3-1.7B、Gemma2-2b、LLaMA 3.2-1B/3B 等多个主流开源模型上进行了验证。
*   **消融与鲁棒性实验**：
    *   测试了不同 PII 类别（人名、地点、组织等）的泄露情况。
    *   分析了 LoRA Rank 对性能的影响。
    *   验证了门控模块在 Non-IID（非独立同分布）数据下的表现。
*   **评价**：实验设计非常充分，不仅对比了多种 FL 变体，还模拟了多种攻击场景，客观地展示了授权与未授权状态下的性能鸿沟。

### 6. 主要结论与发现
*   **极高的隐私保护能力**：在未授权请求下，推理攻击准确率降低了高达 **31.66 倍**，提取召回率降低了 **17.07 倍**。
*   **效用几乎无损**：授权用户的模型性能（PPL）与专门针对敏感数据微调的单适配器模型相当。
*   **路由可靠性**：门控模块对正确适配器的路由准确率达到了 **100%**。
*   **通信高效**：由于仅传输轻量级的安全适配器，通信开销远低于全参数微调或双适配器传输方案。

### 7. 优点（亮点）
*   **细粒度访问控制**：首次在联邦 LLM 微调中引入基于 Token 的推理时动态路由，实现了“按需披露”。
*   **解耦设计**：通过双适配器巧妙解决了“全局泛化”与“本地敏感性”之间的冲突。
*   **即插即用**：门控模块非常轻量，且适配器融合过程不需要重新训练基座模型，具有很强的实用性。

### 8. 不足与局限
*   **推理延迟**：采用“两阶段推理”（先确定路由再生成）会增加 Token 预填充阶段的延迟，可能不适用于极高实时性的场景。
*   **内存开销**：虽然 LoRA 适配器很小，但同时维护多个适配器（安全、揭示、全局）仍会增加显存占用。
*   **Token 安全性依赖**：系统的安全性高度依赖于 [Special-Token] 的保密性，如果 Token 泄露，防御机制将失效。
*   **架构限制**：目前主要在 Decoder-only 架构上验证，对于超大规模模型（如 70B+）或其他架构的适用性仍需进一步研究。

（完）
