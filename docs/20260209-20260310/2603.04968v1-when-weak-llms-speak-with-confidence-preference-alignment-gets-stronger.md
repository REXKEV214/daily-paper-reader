---
title: "When Weak LLMs Speak with Confidence, Preference Alignment Gets Stronger"
title_zh: 当弱大语言模型以高置信度输出时，偏好对齐效果更佳
authors: "Amirabbas Afzali, Myeongho Jeon, Maria Brbic"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04968v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 偏见对齐以使大模型适应人类价值观
tldr: "本研究探讨了利用弱语言模型（LLM）进行偏好对齐的可能性，旨在解决人工标注成本高昂的问题。研究发现，仅选择弱模型中高置信度的样本进行训练，其效果优于全量人工标注。基于此，作者提出了置信度加权偏好优化（CW-PO）框架，通过弱模型的置信度对样本重新加权。实验表明，该方法仅需20%的标注量即可超越标准DPO在全量数据上的表现，为低成本、高性能的偏好对齐提供了新路径。"
selection_source: fresh_fetch
motivation: 针对偏好对齐过程中人工标注成本高且依赖大型模型的问题，探索弱模型是否能作为有效的标注者。
method: 提出 CW-PO 框架，利用弱模型的置信度对训练样本进行重新加权，并优先筛选高质量的偏好数据。
result: "采用 CW-PO 且仅使用 20% 人工标注数据的模型，在性能上优于使用 100% 标注数据的标准 DPO 模型。"
conclusion: 弱模型结合置信度加权机制能显著降低对齐成本，并能产生比全量人工标注更强的对齐效果。
---

## 摘要
偏好对齐是将大语言模型（LLMs）适配到人类价值观的关键步骤，但现有方法通常依赖于昂贵的人工标注或大规模的基于 API 的模型。我们探讨了弱 LLM 是否可以反过来充当有效的标注者。我们惊讶地发现，仅选择弱 LLM 中高置信度样本的一个子集，其性能就显著优于使用完整的人工标注。基于这一发现，我们提出了置信度加权偏好优化（Confidence-Weighted Preference Optimization, CW-PO），这是一个通用的框架，它根据弱 LLM 的置信度对训练样本进行重新加权，并可应用于不同的偏好优化目标。值得注意的是，在标准 DPO 下，仅使用 20% 人工标注并通过 CW-PO 对齐的模型，其表现优于使用 100% 标注训练的模型。这些结果表明，当弱 LLM 与置信度加权相结合时，可以大幅降低偏好对齐的成本，甚至优于在完全由人工标注的数据上训练的方法。

## Abstract
Preference alignment is an essential step in adapting large language models (LLMs) to human values, but existing approaches typically depend on costly human annotations or large-scale API-based models. We explore whether a weak LLM can instead act as an effective annotator. We surprisingly find that selecting only a subset of a weak LLM's highly confident samples leads to substantially better performance than using full human annotations. Building on this insight, we propose Confidence-Weighted Preference Optimization (CW-PO), a general framework that re-weights training samples by a weak LLM's confidence and can be applied across different preference optimization objectives. Notably, the model aligned by CW-PO with just 20% of human annotations outperforms the model trained with 100% of annotations under standard DPO. These results suggest that weak LLMs, when paired with confidence weighting, can dramatically reduce the cost of preference alignment while even outperforming methods trained on fully human-labeled data.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何利用“弱”大语言模型（LLM）作为标注者，通过置信度加权机制来提升“强”大语言模型的偏好对齐效果。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：偏好对齐（如 RLHF 或 DPO）通常依赖昂贵的人工标注或高性能 API 模型（如 GPT-4）。
*   **核心问题**：能否利用参数量极小的“弱模型”（如 125M 或 0.5B 参数）来指导“强模型”的对齐？
*   **背景发现**：作者发现弱模型在区分偏好时，其“高置信度”的预测结果质量极高，甚至在某些情况下优于人类标注的平均水平。

### 2. 方法论：CW-PO 框架
该论文提出了 **置信度加权偏好优化（Confidence-Weighted Preference Optimization, CW-PO）**，其核心流程分为三步：
1.  **训练弱标注器**：在少量（如 20%-30%）人工标注数据上，使用 Bradley-Terry (BT) 目标函数微调一个弱模型（如 OPT-125M），使其成为一个偏好评分器。
2.  **生成伪标签与置信度**：利用训练好的弱模型对大规模无标签数据进行标注。对于每一对响应 $(y_1, y_2)$，计算得分差，并定义**置信度分数 $C$**：
    *   $C(x, y^+, y^-) = 2 \cdot (\sigma(\pi_w(x, y^+) - \pi_w(x, y^-)) - 0.5)$
    *   该分数被归一化到 $[0, 1]$ 区间，反映了弱模型区分优劣的确定程度。
3.  **加权对齐训练**：在强模型的对齐训练（如 DPO, IPO, rDPO）中，将置信度 $C$ 作为样本权重引入损失函数。高置信度样本对梯度贡献大，低置信度（模糊）样本贡献小。

### 3. 实验设计
*   **数据集**：Anthropic HH-RLHF（包含 Harmless 和 Helpful 子集）、UltraFeedback Binarized (UFB)、TL;DR（摘要任务）以及 Stanford Human Preferences (SHP)。
*   **模型组合**：
    *   **弱模型（标注者）**：OPT-125M, Qwen2.5-0.5B。
    *   **强模型（被对齐者）**：OPT 系列（1.3B 到 13B）、Qwen 系列（1.5B 到 14B）、Llama-3.1-8B。
*   **对比基准 (Benchmarks)**：
    *   **Human**：使用 100% 人工标注数据进行标准 DPO。
    *   **WS-DPO**：弱模型监督 DPO（不带权重的基准方法）。
*   **评估指标**：黄金奖励准确率（Gold Reward Accuracy, GRA）和 GPT-4 胜率（Win Rate）。

### 4. 资源与算力
*   **训练细节**：对齐训练进行 5 个 Epoch，SFT 阶段 3 个 Epoch。使用了 LoRA（秩为 8）进行高效微调。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**（如 A100 的具体颗数），但提到了弱模型训练非常高效（约 2450-4978 秒即可完成 5 个 Epoch），且强模型训练使用了 `paged_adamw_32bit` 优化器，显示其对算力需求相对友好。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了跨模型家族（OPT 到 Qwen）、跨任务（对话、摘要、专业知识）、跨对齐算法（DPO/IPO/rDPO）的广泛实验。
*   **消融实验**：包括不同数据划分比例（10% 到 50% 人工数据）、不同加权函数对比、置信度过滤 vs. 置信度加权的对比。
*   **充分性评价**：实验设计非常全面，涵盖了从 125M 到 14B 的多种尺寸组合，验证了方法的通用性和鲁棒性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **以少胜多**：使用 CW-PO 配合 **20%** 的人工标注数据，其对齐效果即可超过使用 **100%** 人工标注的标准 DPO。
*   **置信度的价值**：简单的“硬过滤”（只选高置信度样本）虽然有效，但不如“软加权”（CW-PO）稳定，因为加权保留了更多样化的数据分布。
*   **弱模型也能当好老师**：即使是 125M 的极小模型，只要经过合理的 BT 目标函数训练，也能提供高质量的偏好信号。

### 7. 优点
*   **极高性价比**：显著降低了对昂贵人工标注和 GPT-4 等大模型标注的依赖。
*   **即插即用**：CW-PO 是一个通用框架，可以轻松集成到现有的 DPO、IPO 等主流对齐算法中。
*   **训练效率**：弱标注器的训练和推理成本极低，适合资源有限的团队。

### 8. 不足与局限
*   **在线对齐失效**：在迭代式（Online）DPO 中效果不佳，原因是强模型生成的响应会迅速偏离弱模型的训练分布（分布偏移）。
*   **对初始数据敏感**：如果最初的 20% 人工数据存在严重偏见或被恶意投毒，弱模型会习得这些偏见并放大，导致对齐失败。
*   **评估依赖**：实验主要依赖外部奖励模型（Reward Model）进行 GRA 评估，若奖励模型本身存在偏见，可能会影响结论的客观性。

（完）
