---
title: Memory-Efficient Structured Backpropagation for On-Device LLM Fine-Tuning
title_zh: 面向端侧大语言模型微调的内存高效结构化反向传播
authors: "Juneyoung Park, Yuri Hong, Seongwan Kim, Jaeho Lee"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13069v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 端侧微调实现隐私保护的个性化
tldr: "针对移动设备内存受限导致大模型微调困难的问题，本文提出了一种内存高效的结构化反向传播方法（MeSP）。该方法利用LoRA的低秩结构，通过在反向传播时重新计算中间投影而非存储，实现了与标准反向传播完全一致的梯度计算，同时显著降低了内存开销。在Qwen2.5模型上的实验表明，MeSP在保证训练精度的前提下，平均减少了49%的内存占用，为受限设备上的隐私保护个性化微调提供了可行方案。"
selection_source: fresh_fetch
motivation: 移动设备内存极度受限，现有微调方法在精确梯度的高内存需求与低内存的噪声估计之间存在难以调和的矛盾。
method: 利用LoRA的低秩特性，通过在反向传播阶段重新计算中间投影来替代存储，从而大幅降低激活值的内存占用。
result: "在Qwen2.5系列模型上，MeSP在计算出与标准反向传播完全一致梯度的同时，平均减少了49%的内存消耗。"
conclusion: MeSP成功弥补了精确梯度计算与内存效率之间的鸿沟，使得在内存受限的移动设备上进行高质量大模型微调成为可能。
---

## 摘要
端侧微调能够实现大语言模型的隐私保护个性化，但移动设备面临严苛的内存限制，通常所有工作负载共享 6-12GB 内存。现有方法被迫在具有高内存消耗的精确梯度（MeBP）与具有噪声估计的低内存消耗（MeZO）之间进行权衡。我们提出了内存高效结构化反向传播（MeSP），通过手动推导利用 LoRA 低秩结构的反向传播过程来弥补这一差距。我们的核心见解是，由于秩 $r \ll d_{in}$，中间投影 $h = xA$ 可以在反向传播期间以极小的代价重新计算，从而消除了存储它的必要性。在 Qwen2.5 模型（0.5B-3B）上，MeSP 在计算数学上完全一致的梯度的同时，相比 MeBP 实现了平均 49% 的内存减少。我们的分析还表明，MeZO 的梯度估计与真实梯度几乎没有相关性（余弦相似度 $\approx$ 0.001），这解释了其收敛缓慢的原因。MeSP 将 Qwen2.5-0.5B 的峰值内存从 361MB 降低到 136MB，使得此前在内存受限设备上无法实现的微调场景成为可能。

## Abstract
On-device fine-tuning enables privacy-preserving personalization of large language models, but mobile devices impose severe memory constraints, typically 6--12GB shared across all workloads. Existing approaches force a trade-off between exact gradients with high memory (MeBP) and low memory with noisy estimates (MeZO). We propose Memory-efficient Structured Backpropagation (MeSP), which bridges this gap by manually deriving backward passes that exploit LoRA's low-rank structure. Our key insight is that the intermediate projection $h = xA$ can be recomputed during backward at minimal cost since rank $r \ll d_{in}$, eliminating the need to store it. MeSP achieves 49\% average memory reduction compared to MeBP on Qwen2.5 models (0.5B--3B) while computing mathematically identical gradients. Our analysis also reveals that MeZO's gradient estimates show near-zero correlation with true gradients (cosine similarity $\approx$0.001), explaining its slow convergence. MeSP reduces peak memory from 361MB to 136MB for Qwen2.5-0.5B, enabling fine-tuning scenarios previously infeasible on memory-constrained devices.

---

## 论文详细总结（自动生成）

### 论文总结：面向端侧大语言模型微调的内存高效结构化反向传播 (MeSP)

#### 1. 核心问题与整体含义
论文聚焦于**端侧大语言模型 (LLM) 的隐私保护个性化微调**。核心矛盾在于：移动设备（如手机、平板）的内存极其有限（通常 6-12GB 且由系统共享），而传统的微调方法（即使是 LoRA）在反向传播时产生的中间激活值（Activations）会迅速撑爆内存。
*   **背景**：现有技术在“精确梯度但高内存（MeBP）”与“低内存但梯度含噪声（MeZO）”之间摇摆。
*   **动机**：探索是否能利用 LoRA 的数学结构，在不损失梯度精度（即与标准反向传播数学等价）的前提下，达到接近零阶优化的内存效率。

#### 2. 方法论：MeSP 核心思想与技术细节
论文提出了 **MeSP (Memory-efficient Structured Backpropagation)**，其核心逻辑是**“用计算换内存”**，并针对 LoRA 结构进行了深度定制：
*   **核心思想**：在 LoRA 层 $y = xW_0 + s \cdot xAB$ 中，中间投影 $h = xA$ 的维度非常小（秩 $r$ 通常仅为 8-32）。与其在整个前向传播过程中存储所有层的 $h$，不如在反向传播需要它时，利用已有的输入 $x$ 和权重 $A$ 临时重新计算。
*   **关键技术细节**：
    *   **手动推导反向传播**：避开自动微分框架（如 PyTorch Autograd）的黑盒管理，手动编写每一层的梯度计算逻辑，精确控制张量的生命周期。
    *   **逐块处理 (Layer-by-Layer)**：在前向传播时仅存储 Transformer 块的输出（Checkpoint），在反向传播时逐个块进行重计算、梯度更新并立即释放内存。
    *   **内存管理优化**：在 Apple Silicon 的统一内存架构上，每完成一层计算即调用 `GPU.clearCache()`，强制回收内存，防止峰值叠加。

#### 3. 实验设计
*   **模型对象**：Qwen2.5 系列（0.5B, 1.5B, 3B），采用 4-bit 量化基础权重 + LoRA 适配器。
*   **数据集/场景**：使用 WikiText-2 进行语言建模微调实验。
*   **Benchmark 与对比方法**：
    *   **MeBP (Memory-efficient Backpropagation)**：Apple 官方提供的基于梯度检查点的自动微分方法。
    *   **MeZO (Zeroth-Order Optimization)**：仅通过前向传播估计梯度的零阶优化方法。
*   **评估指标**：峰值内存占用 (MB)、单步训练耗时 (s)、训练损失 (Loss) 收敛曲线、梯度余弦相似度。

#### 4. 资源与算力
*   **硬件平台**：Apple Silicon 芯片（利用其统一内存架构）。
*   **软件框架**：基于 Apple 的 MLX 框架实现。
*   **算力细节**：文中未明确给出具体的 GPU 型号（如 M3 Max 等）或总训练小时数，但详细记录了不同规模模型在单步更新时的耗时（例如 Qwen2.5-0.5B 单步约 0.86s，3B 模型约 4.09s）。

#### 5. 实验数量与充分性
*   **实验维度**：
    *   **模型规模**：覆盖了从 0.5B 到 3B 的三个量级。
    *   **序列长度**：测试了 128 到 1024 的不同长度，验证了内存随长度增长的趋势。
    *   **LoRA 秩 (Rank)**：测试了 $r \in \{4, 8, 16, 32\}$ 的影响。
    *   **消融实验**：对比了“存储 $h$”与“重计算 $h$”的实际收益。
*   **充分性评价**：实验设计较为全面，不仅关注内存，还深入分析了 MeZO 效果差的根本原因（梯度相关性分析），证明了 MeSP 的数学等价性。实验在端侧微调这一特定场景下具有很高的客观性和说服力。

#### 6. 主要结论与发现
*   **内存大幅下降**：MeSP 相比 MeBP 平均减少了 **49%** 的内存占用。在 Qwen2.5-0.5B 上，峰值内存从 361MB 降至 136MB（降幅 62%）。
*   **数学等价性**：MeSP 计算出的梯度与标准反向传播完全一致，训练收敛曲线与 MeBP 重合，优于 MeZO。
*   **MeZO 的局限性**：揭示了 MeZO 梯度估计与真实梯度的余弦相似度仅为 **0.001** 左右，近乎随机，解释了其收敛缓慢的原因。
*   **计算开销可控**：MeSP 增加了约 **28%** 的计算耗时，但在内存受限的端侧设备上，这是为了“能跑起来”而付出的合理代价。

#### 7. 优点与亮点
*   **结构化洞察**：敏锐捕捉到 LoRA 低秩投影 $h$ 尺寸极小这一特性，将其从“存储项”转变为“重计算项”。
*   **极致内存控制**：通过手动推导和显式内存清理，突破了通用自动微分框架的内存冗余瓶颈。
*   **实用性强**：直接针对移动端隐私保护需求，为 6-12GB 内存的设备提供了微调大模型的可行路径。

#### 8. 不足与局限
*   **硬件依赖**：目前的实现深度绑定 Apple Silicon 和 MLX 框架，在 Android/NVIDIA 等其他平台上的通用性有待验证。
*   **计算换内存的代价**：虽然内存降低了，但训练时间增加了近 30%，对于大规模训练任务可能会显著增加能耗。
*   **应用范围**：该方法高度依赖 LoRA 的低秩结构，如果使用全参数微调或其他非低秩的参数高效微调方法，该优化策略将失效。

（完）
