---
title: "S-GRec: Personalized Semantic-Aware Generative Recommendation with Asymmetric Advantage"
title_zh: S-GRec：具有非对称优势的个性化语义感知生成式推荐
authors: "Jie Jiang, Hongbo Tang, Wenjie Wu, Yangru Huang, Zhenmao Li, Qian Li, Changping Wang, Jun Zhang, Huan Yu"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10606v3"
tags: ["query:llm-personalization"]
score: 8.0
evidence: 用于捕捉推荐中潜在用户意图的个性化语义评判器
tldr: S-GRec是一个语义感知的生成式推荐框架，旨在解决传统模型对用户意图监督不足及LLM在线推理成本高的问题。它通过离线LLM构建个性化语义评判器（PSJ）提供监督信号，并利用非对称优势策略优化（A2PO）确保语义信号与业务目标（如GMV）一致。该方法在保持在线轻量化的同时，显著提升了推荐的准确性和业务收益。
selection_source: fresh_fetch
motivation: 传统生成式推荐缺乏对用户意图的深度理解，且直接引入LLM面临推理成本高及语义信号与业务目标冲突的挑战。
method: 提出解耦架构，利用离线LLM构建个性化语义评判器提供奖励，并通过非对称优势策略优化（A2PO）将语义优势注入业务目标优化中。
result: "在公开数据集和大规模生产系统中均取得显著成效，在线A/B测试显示GMV提升了1.19%，且无需实时LLM推理。"
conclusion: S-GRec成功地将LLM的语义先验转化为可落地的推荐增益，实现了语义理解与业务目标的有效对齐。
---

## 摘要
生成式推荐将推荐建模为序列生成，以端到端的方式产生物品，但基于行为日志的训练往往对底层用户意图提供的监督较弱。尽管大语言模型（LLMs）提供了丰富的语义先验，可以提供此类监督，但在工业推荐中直接采用面临两个障碍：语义信号可能与平台业务目标冲突，且大规模 LLM 推理成本极高。本文提出了 S-GRec，这是一个语义感知框架，它将在线轻量级生成器与基于 LLM 的离线语义判别器解耦，用于训练时的监督。S-GRec 引入了一个两阶段个性化语义判别器（PSJ），它产生可解释的维度证据，并从成对反馈中学习用户条件聚合，从而产生稳定的语义奖励。为了防止语义监督偏离业务目标，非对称优势策略优化（A2PO）将优化锚定在业务奖励（如 eCPM）上，并仅在语义优势与业务目标一致时才注入语义优势。在公开基准测试和大规模生产系统上的广泛实验验证了其有效性和可扩展性，包括在在线 A/B 测试中 CTR 的显著提升和 GMV 1.19% 的增长，且无需实时 LLM 推理。

## 速览
**TLDR**：S-GRec是一个语义感知的生成式推荐框架，旨在解决传统模型对用户意图监督不足及LLM推理成本高的问题。它通过离线LLM构建个性化语义评判器（PSJ）提供监督信号，并利用非对称优势策略优化（A2PO）确保语义信号与业务目标（如GMV）一致。该方法在不增加在线推理负担的情况下，显著提升了点击率和交易额。 \
**Motivation**：传统生成式推荐缺乏对用户意图的深度理解，且直接引入LLM存在推理成本高及语义信号与业务目标冲突的问题。 \
**Method**：提出解耦架构，利用离线LLM构建个性化语义评判器提供奖励，并通过非对称优势策略优化将语义优势注入业务目标导向的训练中。 \
**Result**：在公开数据集和大规模生产系统中均取得显著效果，在线A/B测试显示CTR大幅提升且GMV增长了1.19%。 \
**Conclusion**：S-GRec成功地将LLM的语义先验知识转化为可落地的推荐系统监督信号，实现了语义理解与业务指标的协同优化。

---

## Abstract
Generative recommendation models sequence generation to produce items end-to-end, but training from behavioral logs often provides weak supervision on underlying user intent. Although Large Language Models (LLMs) offer rich semantic priors that could supply such supervision, direct adoption in industrial recommendation is hindered by two obstacles: semantic signals can conflict with platform business objectives, and LLM inference is prohibitively expensive at scale. This paper presents S-GRec, a semantic-aware framework that decouples an online lightweight generator from an offline LLM-based semantic judge for train-time supervision. S-GRec introduces a two-stage Personalized Semantic Judge (PSJ) that produces interpretable aspect evidence and learns user-conditional aggregation from pairwise feedback, yielding stable semantic rewards. To prevent semantic supervision from deviating from business goals, Asymmetric Advantage Policy Optimization (A2PO) anchors optimization on business rewards (e.g., eCPM) and injects semantic advantages only when they are consistent. Extensive experiments on public benchmarks and a large-scale production system validate both effectiveness and scalability, including statistically significant gains in CTR and a 1.19\% lift in GMV in online A/B tests, without requiring real-time LLM inference.

---

## 论文详细总结（自动生成）

这篇论文由腾讯团队提出，旨在解决工业级生成式推荐系统中用户意图理解不足、LLM推理成本高昂以及语义信号与业务目标冲突的核心痛点。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：生成式推荐（Generative Recommendation, GR）虽能端到端构建推荐列表，但传统训练依赖行为日志（如点击、购买），这种“弱监督”难以捕捉深层用户意图。
*   **核心挑战**：
    1.  **成本-扩展性冲突**：LLM推理延迟高、算力消耗大，无法满足工业级毫秒级响应需求。
    2.  **目标对齐冲突**：LLM的通用语义偏好可能与平台的业务指标（如GMV、eCPM）不一致，直接引入可能导致业务受损。
*   **整体含义**：提出 **S-GRec** 框架，通过“离线语义判别、在线轻量生成”的解耦架构，将LLM的语义智慧转化为训练时的监督信号，实现业务与语义的双重优化。

### 2. 方法论：核心思想与关键技术
*   **个性化语义评判器 (PSJ)**：将LLM转化为奖励提供者，分为两个阶段：
    *   **阶段A：维度语义评分**：将广告/物品分解为四个维度（用户画像匹配、未来兴趣关联、新颖性、上下文相关性），通过SFT和GRPO算法训练LLM输出可解释的维度分数。
    *   **阶段B：用户条件聚合**：学习用户特定的权重分配。不同用户对“新颖性”或“匹配度”的敏感度不同，PSJ通过成对（Pairwise）反馈学习如何将维度分数聚合为全局语义奖励。
*   **非对称优势策略优化 (A2PO)**：
    *   **核心逻辑**：在优势函数（Advantage）空间进行融合，而非简单的奖励加权。
    *   **一致性门控**：以业务奖励（如eCPM）为锚点，只有当语义优势的方向与业务优势一致时，才注入语义信号。
    *   **幅度平衡**：通过公式限制语义贡献的上限，防止语义信号过强导致模型偏离业务目标。
*   **稀疏采样策略**：仅对一小部分（如5%）训练样本调用PSJ获取语义奖励，大幅降低离线训练成本。

### 3. 实验设计
*   **数据集**：
    *   **公开基准**：Amazon Review 数据集（Office Products 和 Industrial and Scientific 两个子集）。
    *   **工业场景**：微信视频号（WeChat Channels）广告系统。
*   **对比方法 (Benchmarks)**：
    *   传统序列模型：GRU4Rec, Caser, SASRec。
    *   生成式推荐模型：HSTU, TIGER, LC-Rec。
    *   LLM增强模型：BIGRec, D3, S-DPO, 以及基座模型 MiniOneRec。
*   **评估指标**：HR@K, NDCG@K（离线）；CTR, GMV, 负反馈率（在线）。

### 4. 资源与算力
*   **模型规模**：评判器（PSJ）基于 **Qwen3-4B**，生成器（Recommender）基于 **Qwen2.5-1.5B**。
*   **算力说明**：文中**未明确给出**具体的GPU型号、数量或总训练时长。但强调了该方案在推理阶段**无需LLM参与**，因此在线算力消耗与普通轻量级模型持平。

### 5. 实验数量与充分性
*   **实验规模**：
    *   离线对比了10种以上的主流模型。
    *   进行了详尽的消融实验（验证PSJ的两阶段设计、A2PO的门控机制、采样比例的影响）。
    *   在线A/B测试覆盖了5%的真实流量。
*   **充分性评价**：实验设计非常充分。不仅验证了推荐效果的提升，还通过“历史新颖性分析”解释了语义信号为何有效（在跨类别推荐等行为稀疏场景下表现更优），具有很强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：S-GRec在所有公开指标上均优于现有SOTA模型。在线测试中，GMV提升 **1.19%**，CTR提升 **1.16%**，负反馈率下降 **2.02%**。
*   **语义价值**：语义监督在用户尝试新类别（跨类别探索）时贡献最大，弥补了行为数据在冷启动或兴趣转移时的不足。
*   **成本效益**：仅需5%的语义采样比例即可达到全量采样99%以上的性能，证明了该方案在大规模工业场景的可行性。

### 7. 优点
*   **工程落地性强**：通过离线蒸馏避开了LLM在线推理的延迟瓶颈。
*   **业务安全性高**：A2PO机制确保了语义优化不会牺牲平台的硬性业务指标（如收入）。
*   **可解释性**：PSJ提供的多维度评分让推荐理由变得透明，有助于理解用户偏好。

### 8. 不足与局限
*   **标签依赖**：PSJ的训练仍依赖高质量的人工校验标签，虽然有LLM辅助，但初期构建成本较高。
*   **更新滞后性**：作为离线评判器，如果用户流行趋势或业务逻辑发生剧烈变化，PSJ可能需要频繁重训以保持时效性。
*   **文本局限**：目前主要依赖文本语义，对于视频、图像等纯多模态特征的深度融合在文中探讨较少。

（完）
