---
title: "AllMem: A Memory-centric Recipe for Efficient Long-context Modeling"
title_zh: AllMem：一种用于高效长上下文建模的以内存为中心的方案
authors: "Ziming Wang, Xiang Wang, Kailong Peng, Lang Qin, Juan Gabriel Kostelec, Christos Sourmpis, Axel Laborieux, Qinghai Guo"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13680v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长期记忆机制与超长上下文建模
tldr: 针对大语言模型在长文本任务中的计算和内存瓶颈，本文提出 AllMem 架构。它结合了滑动窗口注意力（SWA）与非线性测试时训练（TTT）记忆网络，能有效扩展至超长上下文并减少遗忘。通过高效微调策略，可将现有预训练模型转换为 AllMem 架构。实验显示其在长文本基准测试中表现优异，甚至在 128k 长度下超越了全注意力机制，显著降低了推理成本。
selection_source: fresh_fetch
motivation: 旨在解决自注意力机制在处理长序列时面临的高计算复杂度和内存开销瓶颈。
method: 提出一种结合滑动窗口注意力与非线性 TTT 记忆网络的混合架构，并配套内存高效的微调策略来转换预训练模型。
result: 在 LongBench 和 InfiniteBench 测试中，AllMem 以极低的计算成本实现了接近无损甚至超越全注意力的长文本建模性能。
conclusion: AllMem 为现有预训练模型向高效长上下文架构的转型提供了一种兼具性能与效率的通用方案。
---

## 摘要
大语言模型（LLMs）在处理长序列任务时，由于自注意力机制固有的计算复杂度和内存开销，面临着显著的性能瓶颈。为了应对这些挑战，我们提出了 AllMem，这是一种新型且高效的混合架构，它将滑动窗口注意力（SWA）与非线性测试时训练（TTT）内存网络相结合。AllMem 使模型能够有效地扩展到超长上下文，同时缓解灾难性遗忘。该方法不仅克服了线性内存模型典型的表示约束，还显著降低了长序列推理过程中的计算和内存占用。此外，我们实现了一种内存高效的微调策略，用内存增强的滑动窗口层替换预训练模型中的标准注意力层。该框架有助于将任何现成的预训练 LLM 高效地转换为基于 AllMem 的架构。实证评估证实，我们的 4k 窗口模型在 37k 长度的 LongBench 上实现了近乎无损的性能，与全注意力相比仅有 0.83 的微小降幅。此外，在 128k 上下文的 InfiniteBench 上，我们的 8k 窗口变体优于全注意力，这验证了参数化内存在减轻噪声和维持鲁棒长程建模方面的有效性，且无需承担全局注意力的高昂成本。

## Abstract
Large Language Models (LLMs) encounter significant performance bottlenecks in long-sequence tasks due to the computational complexity and memory overhead inherent in the self-attention mechanism. To address these challenges, we introduce \textsc{AllMem}, a novel and efficient hybrid architecture that integrates Sliding Window Attention (SWA) with non-linear Test-Time Training (TTT) memory networks. \textsc{AllMem} enables models to effectively scale to ultra-long contexts while mitigating catastrophic forgetting. This approach not only overcomes the representation constraints typical of linear memory models but also significantly reduces the computational and memory footprint during long-sequence inference. Furthermore, we implement a Memory-Efficient Fine-Tuning strategy to replace standard attention layers in pre-trained models with memory-augmented sliding window layers. This framework facilitates the efficient transformation of any off-the-shelf pre-trained LLM into an \textsc{AllMem}-based architecture. Empirical evaluations confirm that our 4k window model achieves near-lossless performance on 37k LongBench with a marginal 0.83 drop compared to full attention. Furthermore, on InfiniteBench at a 128k context, our 8k window variant outperforms full attention, which validates the effectiveness of our parameterized memory in mitigating noise and maintaining robust long-range modeling without the prohibitive costs of global attention.

---

## 论文详细总结（自动生成）

以下是对论文《AllMem: A Memory-centric Recipe for Efficient Long-context Modeling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机：** 传统的 Transformer 架构在处理长序列时面临两大瓶颈：一是自注意力机制的计算复杂度随长度呈平方级增长（$O(L^2)$）；二是 KV Cache 的存储开销随长度线性增长（$O(L)$），这使得在边缘设备或长上下文场景下推理成本极高。
*   **核心目标：** 提出一种名为 **AllMem** 的混合架构，旨在通过集成滑动窗口注意力（SWA）和非线性测试时训练（TTT）记忆网络，在显著降低计算和内存开销的同时，保持甚至超越全注意力模型在长文本任务中的性能。

### 2. 方法论
*   **核心思想：** 模拟人类认知的多尺度时间动态，将记忆分为处理局部细节的“短期记忆”（SWA）和通过在线学习压缩信息的“长期记忆”（参数化记忆网络）。
*   **关键技术细节：**
    *   **混合架构：** 在每个 Transformer 层中，Token Mixer 由并行的 SWA 分支和 AllMem 记忆分支组成。两者通过一个可学习的通道级缩放门控（Scaling Gate）进行融合。
    *   **非线性 TTT 记忆：** 不同于传统的线性记忆（如 Mamba），AllMem 使用基于 **SwishGLU** 的非线性残差网络作为记忆单元。在推理过程中，模型通过最小化重建损失（L2 损失）来在线更新记忆权重。
    *   **内部归一化机制：** 为了稳定 TTT 过程，论文设计了一种“先读、后归一化、再更新”的流程，确保记忆读取的稳定性与写入的灵活性解耦。
    *   **蒸馏转换流水线：** 提出一种从预训练 Transformer 到 AllMem 的转换方法。通过知识蒸馏，冻结原有的 MLP 和 SWA 权重，仅微调记忆模块的元参数（如学习率、衰减率、QKV 投影等）。

### 3. 实验设计
*   **基础模型：** 以 Qwen3-0.6B 和 Qwen3-1.7B 为基础进行架构转换。
*   **数据集与场景：**
    *   **短序列任务：** C-Eval、ARC、HellaSwag、MMLU-Redux、GPQA、IFEval、MATH-500、LiveCodeBench（涵盖常识、逻辑、数学、代码等）。
    *   **长序列任务：** LongBench（平均 37k）、InfiniteBench（128k）、LVEval（128k）。
*   **对比方法（Benchmark）：**
    *   全注意力机制（Full Attention）。
    *   带 Sink Token 的滑动窗口注意力（SWA-Sinks）。
    *   基于 Mamba-2 的混合记忆模型。

### 4. 资源与算力
*   **训练配置：** 使用混合精度训练、DeepSpeed ZeRO-2 和梯度检查点技术。
*   **数据规模：** 0.6B 模型在 3M tokens 上训练，1.7B 模型在 5M tokens 上训练。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号（如 A100/H100）及具体训练时长，但强调了该方法通过蒸馏实现，相比从头训练极大地节省了算力。

### 5. 实验数量与充分性
*   **实验规模：** 针对两个不同参数规模的模型（0.6B 和 1.7B）进行了验证。
*   **覆盖面：** 实验涵盖了从短文本基础能力到超长文本（128k+）检索与推理的广泛任务。
*   **充分性：** 论文对比了多种主流的线性/稀疏注意力替代方案，并提供了 FLOPs 和显存占用的定量分析。实验设计较为客观，通过随机配置训练（Randomized Configuration Training）增强了模型对不同窗口大小的泛化性。

### 6. 主要结论与发现
*   **性能无损：** AllMem 在短文本基准测试中保持了与原模型相当的精度，甚至在某些逻辑推理任务上有所提升。
*   **长文本优势：** 在 128k 长度下，AllMem-1.7B 的表现优于全注意力模型，证明了参数化记忆在过滤噪声和维持长程依赖方面的有效性。
*   **极高效率：** 当上下文达到 128k 时，AllMem 的计算成本和显存需求仅为全注意力的 **1/9** 左右，且记忆部分的存储开销是恒定的（$O(1)$）。

### 7. 优点
*   **架构通用性：** 提供了一种将现有预训练 Transformer 转换为高效长文本模型的通用“配方”，具有极高的工程实用价值。
*   **非线性表达力：** 引入非线性 TTT 单元，克服了线性记忆模型在复杂信息压缩上的局限性。
*   **训练策略：** 采用响应式蒸馏（Response-only distillation）和随机配置训练，提升了模型处理变长上下文的鲁棒性。

### 8. 不足与局限
*   **模型规模限制：** 目前仅在 2B 以下的小规模模型上验证，尚未证明其在百亿（7B+）或千亿级参数模型上的扩展性。
*   **推理延迟挑战：** 虽然 FLOPs 降低了，但 TTT 机制需要在推理时进行反向传播更新权重（Inner Loop），这在实际部署时可能面临算子优化和端到端延迟的挑战。
*   **训练数据量较小：** 仅使用了 3M-5M tokens 进行微调，虽然证明了蒸馏的高效性，但也可能限制了模型学习更复杂长文本模式的上限。

（完）
