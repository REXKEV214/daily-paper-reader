---
title: "HY-WU (Part I): An Extensible Functional Neural Memory Framework and An Instantiation in Text-Guided Image Editing"
title_zh: HY-WU（第一部分）：一种可扩展的函数式神经记忆框架及其在文本引导图像编辑中的实例化
authors: Tencent HY Team
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07236v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 针对不断演变的用户偏好的持续学习与即时个性化
tldr: 针对基础模型在持续学习和个性化适配中容易出现的权重覆盖和性能退化问题，本文提出了 HY-WU（Weight Unleashing）框架。该框架将适配压力从修改全局共享参数转向功能性神经存储，通过生成器根据实例条件实时合成权重更新。这种方法实现了无需测试时优化的实例级算子定制，在文本引导的图像编辑等任务中展现了强大的扩展性和灵活性。
selection_source: fresh_fetch
motivation: 传统的静态权重适配范式在处理持续演进的多样化任务时，容易导致参数冲突、干扰或对先前知识的破坏。
method: 提出一种名为 HY-WU 的功能性存储框架，利用生成器根据输入条件动态合成权重更新，实现实例特定的算子定制。
result: 该框架成功应用于文本引导的图像编辑，实现了无需测试时优化的即时个性化适配。
conclusion: HY-WU 通过将适配逻辑从权重覆盖转向动态生成，为基础模型的持续学习和高效个性化提供了一种可扩展的新范式。
---

## 摘要
基础模型正从离线预测器转型为预期在长周期内运行的部署系统。在实际部署中，目标并非固定不变：领域会发生漂移，用户偏好会演变，且在模型发布后会出现新任务。这使得持续学习和即时个性化从可选功能提升为核心架构需求。然而，大多数适配流水线仍遵循静态权重范式：在训练（或任何适配步骤）之后，推理过程执行的是单一参数向量，而不考虑用户意图、领域或特定实例的约束。这将训练或适配后的模型视为参数空间中的一个孤立点。在异构且持续演进的机制下，不同的目标可能会在参数上诱导出分离的可行区域，迫使任何单一的共享更新陷入折中、干扰或过度特化。因此，持续学习和个性化通常被实现为对共享权重的重复覆盖，从而面临先前习得行为退化的风险。我们提出了 HY-WU（Weight Unleashing），这是一种记忆优先的适配框架，它将适配压力从覆盖单一共享参数点转移开。HY-WU 将函数式（算子级）记忆实现为一个神经模块：一个根据实例条件即时合成权重更新的生成器，从而在无需测试时优化的情况下产生特定于实例的算子。

## Abstract
Foundation models are transitioning from offline predictors to deployed systems expected to operate over long time horizons. In real deployments, objectives are not fixed: domains drift, user preferences evolve, and new tasks appear after the model has shipped. This elevates continual learning and instant personalization from optional features to core architectural requirements. Yet most adaptation pipelines still follow a static weight paradigm: after training (or after any adaptation step), inference executes a single parameter vector regardless of user intent, domain, or instance-specific constraints. This treats the trained or adapted model as a single point in parameter space. In heterogeneous and continually evolving regimes, distinct objectives can induce separated feasible regions over parameters, forcing any single shared update into compromise, interference, or overspecialization. As a result, continual learning and personalization are often implemented as repeated overwriting of shared weights, risking degradation of previously learned behaviors. We propose HY-WU (Weight Unleashing), a memory-first adaptation framework that shifts adaptation pressure away from overwriting a single shared parameter point. HY-WU implements functional (operator-level) memory as a neural module: a generator that synthesizes weight updates on-the-fly from the instance condition, yielding instance-specific operators without test-time optimization.

---

## 论文详细总结（自动生成）

# HY-WU (Part I) 论文深度总结：一种可扩展的函数式神经记忆框架

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统基础模型的适配（如 SFT 或 LoRA）遵循“静态权重范式”，即在适配后，无论输入如何变化，推理时都使用同一组固定的参数。
*   **研究背景**：在实际部署中，模型面临领域漂移、用户偏好演变和新任务涌现。当多个目标互斥或异构时，单一的共享参数点会陷入“折中区域”，导致性能退化、干扰或过度特化（Catastrophic Interference）。
*   **整体含义**：论文提出将适配视为一种**“函数式记忆”（Functional Memory）**。通过一个神经生成器根据输入实例动态合成参数更新，使模型能够针对每个实例实时定制算子，从而在不破坏共享基座的前提下解决异构目标的冲突。

### 2. 论文提出的方法论
*   **核心思想**：引入 **HY-WU (Weight Unleashing)** 框架，将记忆实现为参数生成器 $g_\phi$。它将实例条件 $c(x)$ 映射为低秩更新 $\Delta\theta(x)$，得到实例特定的算子 $f_{\theta+\Delta\theta(x)}$。
*   **关键技术细节**：
    *   **端到端即时训练（On-the-fly Training）**：直接从下游任务损失函数优化生成器，无需像以往超网络（Hypernetworks）那样预先收集大量微调后的 Checkpoint 进行重构训练。
    *   **秩锚定 2D 参数标记化（Rank-anchored 2D Parameter Tokenization）**：针对大模型不同层维度不一的问题，利用固定的 LoRA 秩（Rank）作为锚定维度，将异构参数重组为统一形状的 Token。
    *   **因子化注意力 Transformer（NNT）**：生成器采用层内（Intra-layer）和层间（Inter-layer）因子化注意力机制，在保持架构感知的同时，解决了长参数序列生成的计算复杂度问题。
    *   **零初始化稳定性**：通过对 LoRA 的 B 矩阵投影层进行零初始化，确保训练初期从原始预训练模型平滑过渡。

### 3. 实验设计
*   **应用场景**：文本引导的图像编辑（TI2I），这是一个对实例依赖性极强且目标常有冲突（如“修复”与“老化”互斥）的压力测试场。
*   **数据集与 Benchmark**：
    *   **内部评估**：WU-Eval（包含 346+64 对中英双语编辑任务，涵盖 60 个子任务）。
    *   **公开榜单**：GEdit-Bench（语义一致性与感知质量）、ImgEdit-Bench。
*   **对比方法**：
    *   **开源模型**：Step1X-Edit, Qwen-Image-Edit, LongCat, FLUX.2 等。
    *   **闭源模型**：Seedream 4.5, GPT Image 1.5, Gemini-3.0 Pro Image (Nano Banana 系列)。
    *   **消融基准**：静态 LoRA、全量 SFT、随机打乱条件的 Shuffle PG 等。

### 4. 资源与算力
*   **系统优化**：论文提到了多种系统级优化，包括 FSDP2（分片参数/梯度）、DeepSpeed-Ulysses（序列并行）、FlashInfer 和 Triton 内核加速。
*   **算力细节**：**文中未明确给出具体的 GPU 总数、型号或总训练时长**。但提到实验涉及 80B 参数规模的基座模型（HY-Image-3.0-Instruct）以及 8B 规模的生成器，暗示了极高的算力需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的人类偏好评估（GSB），胜率对比涵盖了当前主流的所有开源与闭源模型。
    *   在两个不同的基座架构（原生多模态模型与 MMDiT 扩散模型）上验证了通用性。
    *   **充分性与客观性**：实验设计较为严谨。通过“冲突任务分析”（如同时训练模糊与去模糊）证明了静态适配的局限性；通过“实例对齐消融实验”证明了性能提升源于动态路由而非单纯增加参数量；利用 t-SNE 进行了参数空间的几何分析，展示了生成参数的语义结构。

### 6. 论文的主要结论与发现
*   **性能领先**：HY-WU 在 GEdit-Bench 排名开源模型第 1，在 ImgEdit-Bench 排名第 2，在人类评估中显著优于 GPT Image 1.5 等强力闭源模型。
*   **解决冲突**：在互斥任务实验中，静态适配会产生“折中”效果（如既不清晰也不模糊），而 HY-WU 能保持清晰的定向行为。
*   **参数空间结构化**：发现生成的参数更新在空间中自发形成了语义簇（如“风格转换”与“物体移除”分布在不同区域），证明生成器学到了有意义的算子流形。
*   **扩展性**：展示了“函数式记忆的正向缩放定律”，即增加生成器容量或 LoRA 秩能持续提升性能。

### 7. 优点（亮点）
*   **范式创新**：从“优化单一参数点”转向“学习参数映射函数”，为持续学习提供了新思路。
*   **部署友好**：无需在测试时进行耗时的梯度优化，实现了即时的个性化适配。
*   **架构通用**：提出的 2D Tokenization 方案解决了超网络难以适配复杂异构 Transformer 架构的顽疾。
*   **深度分析**：不仅关注指标，还从梯度冲突、权重几何等底层视角解释了为何动态生成优于静态适配。

### 8. 不足与局限
*   **计算开销**：虽然推理时无需优化，但生成器本身（如 8B 规模）增加了额外的推理计算量和显存占用。
*   **应用范围**：目前仅在图像编辑（Part I）中验证，尚未在长程在线持续学习（Online CL）协议下进行严格的序列任务测试。
*   **数据依赖**：生成器的泛化能力高度依赖于训练时接触到的编辑指令多样性，对于极端罕见的自定义指令可能存在泛化风险。

（完）
