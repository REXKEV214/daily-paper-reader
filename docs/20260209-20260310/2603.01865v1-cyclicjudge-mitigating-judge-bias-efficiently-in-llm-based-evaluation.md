---
title: "CyclicJudge: Mitigating Judge Bias Efficiently in LLM-based Evaluation"
title_zh: CyclicJudge：高效缓解基于 LLM 评估中的评测者偏见
authors: "Ziyi Zhu, Olivier Tieleman, Alexey Bukhtiyarov, Jinghong Chen"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01865v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 缓解大模型评估中的裁判偏见
tldr: 针对大语言模型作为裁判（LLM-as-judge）时存在的系统性偏差问题，本文提出了一种方差分解框架，将评分误差拆解为场景、生成、裁判及残差分量。基于此分析，研究者设计了 CyclicJudge 循环分配策略。该方法通过在评估周期内轮换裁判，在不增加额外计算成本的前提下，能够精确消除裁判偏差，显著提升了模型排名的可靠性，并在 MT-Bench 上得到了验证。
selection_source: fresh_fetch
motivation: 现有的 LLM 裁判存在显著的系统性偏差，其量级足以干扰模型排名的可靠性，且无法通过增加样本量消除。
method: 引入方差分解框架分析评分误差，并提出 CyclicJudge 循环分配策略，通过裁判轮换来抵消个体偏差。
result: 理论分析和 MT-Bench 实验证明，该方法能精确消除裁判偏差，同时保持与单裁判评估相同的计算成本。
conclusion: CyclicJudge 是一种兼顾效率与公平性的最优裁判分配方案，为开放式模型评估提供了更可靠、低成本的解决方案。
---

## 摘要
LLM-as-judge 评估已成为开放式模型评估的标准做法；然而，评测者表现出系统性偏见，这些偏见无法通过增加场景或生成结果的数量来消除。这些偏见的大小通常与基准测试旨在检测的模型差异相当，导致在使用单一评测者评估时排名不可靠。本研究引入了一种方差分解方法，将基准测试得分的方差划分为场景、生成、评测者和残差分量。基于这一分析，CyclicJudge（一种循环分配评测者的方法）被证明是最佳分配策略。它能精确消除偏见，同时在每个循环中仅需每个评测者参与一次，从而保持了单一评测者评估的成本。在 MT-Bench 上的实证验证支持了所有的理论预测。

## Abstract
LLM-as-judge evaluation has become standard practice for open-ended model assessment; however, judges exhibit systematic biases that cannot be eliminated by increasing the number of scenarios or generations. These biases are often similar in magnitude to the model differences that benchmarks are designed to detect, resulting in unreliable rankings when single-judge evaluations are used. This work introduces a variance decomposition that partitions benchmark score variance into scenario, generation, judge, and residual components. Based on this analysis, CyclicJudge, a round-robin assignment of judges, is demonstrated to be the optimal allocation strategy. It eliminates bias precisely while requiring each judge only once per cycle, maintaining the cost of single-judge evaluation. Empirical validation on MT-Bench supports all theoretical predictions.

---

## 论文详细总结（自动生成）

### 论文总结：CyclicJudge - 高效缓解基于 LLM 评估中的评测者偏见

#### 1. 核心问题与整体含义
随着大语言模型（LLM）作为裁判（LLM-as-judge）成为开放式评估的标准，其固有的**系统性偏见**（如宽容度不一、自我偏好、位置偏见等）严重影响了评估的可靠性。研究发现，这些偏见无法通过增加测试场景或生成次数来消除，且其量级往往足以扭曲模型排名。传统的解决方法是使用多裁判委员会，但这会成倍增加评估成本。本文的核心目标是：**在不增加额外成本的前提下，通过优化裁判分配策略来精确消除系统性偏见。**

#### 2. 方法论
论文提出了一种基于**概化理论（Generalizability Theory）**的方差分解框架，并推导出最优分配策略：
*   **方差分解模型**：将基准测试得分的方差拆解为四个部分：
    1.  **场景效应 ($\sigma^2_\alpha$)**：不同题目难度导致的差异。
    2.  **生成效应 ($\sigma^2_\beta$)**：模型随机解码导致的差异。
    3.  **裁判偏见 ($\sigma^2_\gamma$)**：裁判个体的系统性倾向（固定效应）。
    4.  **残差 ($\sigma^2_\epsilon$)**：随机噪声及未建模的交互作用。
*   **核心思想 (CyclicJudge)**：采用**循环赛（Round-robin）**分配方式。例如，如果有 5 个裁判和 100 个生成结果，不是让每个裁判看所有结果，而是让裁判 1 评第 1 个，裁判 2 评第 2 个……以此类推。
*   **关键技术细节**：论文通过数学证明，在固定预算 $B$ 下，CyclicJudge 策略在期望上能使裁判偏见项完全抵消（均值为 0），同时通过最大化生成结果的多样性，使总方差达到最小。

#### 3. 实验设计
*   **数据集/场景**：使用 **MT-Bench**（包含 80 个多轮对话问题，覆盖 8 个类别）。
*   **评估对象**：Qwen 2.5 7B Instruct、Llama 3.3 70B Instruct、GPT-5.2。
*   **裁判池**：由 5 个模型组成（Qwen 2.5 7B、Llama 3.3 70B、GPT-5.2、Gemini 3 Flash、Claude Sonnet 4.6）。
*   **对比策略**：
    1.  **Strategy A (All judges)**：每个生成结果都由所有裁判评分（成本高）。
    2.  **Strategy B (Random judge)**：每个生成结果随机选一个裁判评分。
    3.  **Strategy C (CyclicJudge)**：本文提出的循环分配策略。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   实验规模：每个模型生成 10 个独立对话，每个对话由 5 个裁判评分，总计产生了 12,000 条评分数据（3 个被评模型 × 80 场景 × 10 生成 × 5 裁判）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 5,000 次子样本抽样重复实验，以验证不同预算水平下的方差表现。
*   **充分性**：实验涵盖了不同参数规模的模型（从 7B 到 GPT-5 级别），并利用交叉 ANOVA 分析和 95% 置信区间（2,000 次 bootstrap 重采样）确保了统计显著性。
*   **客观性**：通过全交叉设计（Fully Crossed Design）直接观察到了“自我偏好”效应，实验设计严谨，理论预测与实证结果高度吻合。

#### 6. 主要结论与发现
*   **偏见是主导因素**：在默认的单裁判评估中，裁判偏见占总方差的 **94% 以上**，足以逆转模型排名（例如 Qwen 裁判会将 Qwen 排在第一）。
*   **CyclicJudge 的优越性**：在相同预算下，CyclicJudge 比随机分配策略降低了约 **30-35%** 的方差。
*   **成本中性**：该方法在消除偏见的同时，保持了与单裁判评估完全相同的计算成本。
*   **模型差异**：强模型（如 GPT-5.2）的生成方差极低，而弱模型的场景方差和裁判偏见更大。

#### 7. 优点
*   **理论严密**：提供了坚实的数学证明，将评估问题转化为方差优化问题。
*   **极高实用性**：无需对模型进行微调，只需改变调用裁判的顺序即可实现，易于集成到现有流水线。
*   **成本效益**：解决了“准确性”与“成本”之间的矛盾，实现了“免费的午餐”。

#### 8. 不足与局限
*   **线性模型假设**：使用了线性随机效应模型，这对于有界序数评分（1-10 分）是一种近似，未来可能需要更复杂的 GLMM 模型。
*   **裁判池规模**：实验仅使用了 5 个裁判，尚未在大规模裁判池（如数十个模型）中验证。
*   **成本权重忽略**：假设所有裁判的调用成本相同，但在现实中，GPT-4o 和小型开源模型的成本差异巨大，未考虑成本加权的分配。
*   **场景同质化假设**：假设所有测试题目重要性相同，未结合题目难度或信息量进行自适应分配。

（完）
