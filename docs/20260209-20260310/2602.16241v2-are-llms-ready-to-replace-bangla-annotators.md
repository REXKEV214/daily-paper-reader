---
title: Are LLMs Ready to Replace Bangla Annotators?
title_zh: 大语言模型（LLMs）准备好取代孟加拉语标注员了吗？
authors: "Md. Najib Hasan, Touseef Hasan, Souvika Sarkar"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16241v2"
tags: ["query:llm-bias"]
score: 9.0
evidence: 在身份敏感设置下评估 LLM 的社会偏见与标注偏差
tldr: 本研究评估了17个大语言模型在孟加拉语仇恨言论标注任务中的表现。通过统一框架分析发现，LLM作为零样本标注者存在显著的偏见和不稳定性。研究指出，模型规模增加并不一定提升标注质量，较小且任务对齐的模型表现往往更一致。这为低资源语言敏感任务的自动化标注提供了重要警示。
selection_source: fresh_fetch
motivation: 探讨大语言模型在低资源语言和身份敏感任务中作为自动化标注者的可靠性与偏见问题。
method: 使用统一评估框架对17个大语言模型在孟加拉语仇恨言论检测任务上进行零样本标注性能的系统性基准测试。
result: 实验发现模型判断存在显著偏见和不稳定性，且模型规模的扩大并不直接等同于标注质量的提升。
conclusion: 当前大语言模型在处理低资源语言敏感标注任务时存在局限性，部署前需进行严格评估，不能盲目替代人工标注。
---

## 摘要
大语言模型（LLMs）正越来越多地被用作自动标注员以扩大数据集创建规模，然而，它们作为无偏见标注员的可靠性——特别是在低资源和身份敏感的环境下——仍未得到充分理解。在这项工作中，我们研究了 LLMs 作为孟加拉语仇恨言论零样本标注员的行为。在这项任务中，即使是人类标注者之间达成一致也具有挑战性，且标注员偏见可能会产生严重的下游后果。我们使用统一的评估框架对 17 个 LLMs 进行了系统的基准测试。我们的分析揭示了模型判断中的标注员偏见和显著的不稳定性。令人惊讶的是，模型规模的增加并不能保证标注质量的提高——较小的、更符合任务要求的模型往往比其较大的对应模型表现出更一致的行为。这些结果突显了当前 LLMs 在低资源语言敏感标注任务中的重要局限性，并强调了在部署前进行仔细评估的必要性。

## Abstract
Large Language Models (LLMs) are increasingly used as automated annotators to scale dataset creation, yet their reliability as unbiased annotators--especially for low-resource and identity-sensitive settings--remains poorly understood. In this work, we study the behavior of LLMs as zero-shot annotators for Bangla hate speech, a task where even human agreement is challenging, and annotator bias can have serious downstream consequences. We conduct a systematic benchmark of 17 LLMs using a unified evaluation framework. Our analysis uncovers annotator bias and substantial instability in model judgments. Surprisingly, increased model scale does not guarantee improved annotation quality--smaller, more task-aligned models frequently exhibit more consistent behavior than their larger counterparts. These results highlight important limitations of current LLMs for sensitive annotation tasks in low-resource languages and underscore the need for careful evaluation before deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Are LLMs Ready to Replace Bangla Annotators?》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：高质量的数据标注是 NLP 的瓶颈，人工标注成本高且耗时。虽然大语言模型（LLMs）被广泛用于自动化标注，但其在**低资源语言（如孟加拉语）**和**身份敏感任务（如仇恨言论检测）**中的可靠性、偏见和稳定性尚不明确。
*   **核心问题**：LLM 能否在孟加拉语这种文化背景深厚、资源匮乏的语境下，作为无偏见的标注员替代人类？研究重点关注模型在面对不同身份（如性别、宗教、职业）时的判断一致性。

### 2. 方法论
论文提出了一个**统一评估框架**，从三个维度评估 LLM 的标注行为：
*   **E1：分类性能（Classification）**：评估模型在 21 个身份类别下的分类准确率（F1 分数）、人类一致性（Cohen’s Kappa）以及身份翻转时的**不匹配率（Mismatch Score）**。
*   **E2：推理对齐（Reasoning Alignment）**：评估模型生成的理由是否与人类标注的理由语义一致。使用 BERTScore 和余弦相似度进行度量。
*   **E3：提示词敏感性（Prompt Sensitivity）**：基于 TELeR 分类法设计了四个等级的提示词（从简单指令到带示例的详细指令），测试模型在指令微调下的稳定性。
*   **模型分类算法**：根据上述三个维度的得分，将模型划分为三类：**类人标注员（Human-like）**、**客观标注员（Objective）**和**自适应标注员（Adaptive）**。

### 3. 实验设计
*   **数据集**：引入了新数据集 **Bangla_HateSpeech (BAHS)**，包含 5,000 个样本，涵盖性别、娱乐、宗教、地缘政治和政治 5 个领域，涉及 21 种身份标签（如女性、穆斯林、艺术家、政治家等）。
*   **对比方法**：横向对比了 **17 个主流 LLM**，涵盖 6 个家族：ChatGPT (OpenAI)、Gemini (Google)、Mistral、LLaMA (Meta)、Qwen (Alibaba) 和 DeepSeek。
*   **实验设置**：采用零样本（Zero-shot）标注设置，通过改变提示词中的“标注员身份”（例如：“你是一名女性/男性标注员”）来诱导偏见。

### 4. 资源与算力
*   **算力平台**：实验在 **Kaggle** 环境下运行。
*   **硬件配置**：使用了 **双 NVIDIA T4 GPU**。
*   **模型访问**：闭源模型通过官方 API 访问，开源模型使用其公开权重。文中未详细说明每个模型的具体推理时长，但强调了标准化了生成参数（如 Temperature, Top_p 等）。

### 5. 实验数量与充分性
*   **实验规模**：对 17 个模型在 21 个身份类别、4 种提示词水平上进行了全量测试，并进行了**标签消融实验（Label Ablation）**以测试模型对语义重叠标签的混淆程度。
*   **充分性**：实验设计较为全面，不仅关注准确率，还深入探讨了推理逻辑和提示词鲁棒性。
*   **客观性**：通过 55 名人类标注者的研究来设定评估阈值，确保了评估标准的客观性，而非随意设定。

### 6. 主要结论与发现
*   **规模不等于质量**：增加模型参数量并不能保证标注质量。较小的模型（如 **Qwen 2.5 7B**）在一致性和偏见控制上往往优于大型模型（如 GPT-3.5 Turbo）。
*   **推理与分类脱节**：LLM 能够生成非常流畅且看似合理的理由，即使其给出的分类标签是错误的。这意味着**推理的流畅性不能作为标注正确性的指标**。
*   **严重的身份偏见**：模型在处理“政治家”等动态身份时表现极差，且在身份翻转（如女性 vs 非女性）时存在显著的标签漂移。
*   **提示词不稳定性**：微小的指令措辞变化会导致标注结果大幅波动，大多数模型被归类为“自适应标注员”，尚不具备替代人类的稳定性。

### 7. 优点
*   **填补空白**：针对低资源语言（孟加拉语）的身份偏见标注进行了系统性研究。
*   **多维度框架**：不只看 F1 值，还引入了不匹配率和推理相似度，提供了更全面的模型画像。
*   **实用分类法**：提出的三类标注员分类法为未来评估 LLM 标注能力提供了参考标准。

### 8. 不足与局限
*   **语言局限性**：研究仅限于孟加拉语，其结论在其他低资源语言中的普适性有待验证。
*   **提示词模板限制**：虽然使用了 TELeR 分类法，但提示词仍是人工撰写的，可能无法覆盖真实世界中所有可能的指令变体。
*   **自动化指标局限**：BERTScore 和余弦相似度在评估推理逻辑时可能存在饱和现象，难以捕捉深层的逻辑谬误或幻觉。
*   **数据集规模**：5,000 个样本虽然经过精挑细选，但对于捕捉罕见或极其微妙的偏见模式可能仍然规模有限。

（完）
