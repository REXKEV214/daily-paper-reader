---
title: Are LLMs Ready to Replace Bangla Annotators?
title_zh: 大语言模型（LLMs）准备好取代孟加拉语标注员了吗？
authors: "Md. Najib Hasan, Touseef Hasan, Souvika Sarkar"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16241v2"
tags: ["query:llm-bias"]
score: 9.0
evidence: 在敏感设置下基准测试LLM作为标注者的偏见和可靠性
tldr: 本研究探讨了大语言模型（LLM）在孟加拉语仇恨言论标注任务中的可靠性。通过对17个模型进行零样本标注基准测试，发现LLM存在显著的标注偏差和不稳定性。研究指出，模型规模的增加并不一定能提升标注质量，较小的任务对齐模型表现往往更一致。该工作揭示了LLM在低资源语言敏感任务中的局限性，强调了部署前进行严格评估的必要性。
selection_source: fresh_fetch
motivation: 旨在评估LLM在低资源且具有身份敏感性的孟加拉语仇恨言论标注任务中，是否能作为可靠且无偏的自动标注员。
method: 使用统一的评估框架对17个大语言模型在零样本设置下的标注行为进行了系统性的基准测试。
result: 实验发现模型判断存在显著偏差和不稳定性，且模型规模的扩大并不直接等同于标注质量的提升。
conclusion: 当前LLM在处理低资源语言的敏感标注任务时仍存在重要局限，不能简单替代人工标注。
---

## 摘要
大语言模型（LLMs）正越来越多地被用作自动标注员以扩大数据集创建规模，然而，它们作为无偏见标注员的可靠性——特别是在低资源和身份敏感的环境下——仍未得到充分理解。在这项工作中，我们研究了 LLMs 作为孟加拉语仇恨言论零样本标注员的行为。在这项任务中，即使是人类标注者的一致性也极具挑战，且标注员偏见可能会产生严重的下游后果。我们使用统一的评估框架对 17 个 LLMs 进行了系统的基准测试。我们的分析揭示了模型判断中的标注员偏见和显著的不稳定性。令人惊讶的是，模型规模的增加并不能保证标注质量的提高——较小的、更符合任务要求的模型往往比其较大的对应模型表现出更一致的行为。这些结果突显了当前 LLMs 在低资源语言敏感标注任务中的重要局限性，并强调了在部署前进行仔细评估的必要性。

## Abstract
Large Language Models (LLMs) are increasingly used as automated annotators to scale dataset creation, yet their reliability as unbiased annotators--especially for low-resource and identity-sensitive settings--remains poorly understood. In this work, we study the behavior of LLMs as zero-shot annotators for Bangla hate speech, a task where even human agreement is challenging, and annotator bias can have serious downstream consequences. We conduct a systematic benchmark of 17 LLMs using a unified evaluation framework. Our analysis uncovers annotator bias and substantial instability in model judgments. Surprisingly, increased model scale does not guarantee improved annotation quality--smaller, more task-aligned models frequently exhibit more consistent behavior than their larger counterparts. These results highlight important limitations of current LLMs for sensitive annotation tasks in low-resource languages and underscore the need for careful evaluation before deployment.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在低资源语言（孟加拉语）环境下作为自动标注员的可靠性进行了深入探讨。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：高质量数据标注是 NLP 的瓶颈，人工标注成本高且耗时。虽然 LLM 被提议作为替代方案，但其在低资源语言（如孟加拉语）和身份敏感任务（如仇恨言论检测）中的**标注偏见（Annotator Bias）**和**不稳定性**尚未得到充分研究。
*   **核心问题**：LLM 能否在孟加拉语这种文化背景深厚、资源匮乏的语言中，提供无偏见、一致且可信的标注？

### 2. 论文提出的方法论
论文提出了一个**三维统一评估框架**，旨在从不同角度审视 LLM 的标注行为：
*   **E1：分类性能（Classification）**：评估模型在不同身份背景（如女性、穆斯林、政治家等）下的分类准确率（F1、Kappa）及身份翻转导致的预测不一致性（Mismatch Score）。
*   **E2：推理对齐（Reasoning Alignment）**：评估模型生成的标注理由与人类理由的语义相似度（使用 BERTScore 和余弦相似度）。
*   **E3：提示词敏感性（Prompt Sensitivity）**：基于 TELeR 分类法设计四个等级的提示词（从简单指令到带示例的详细指令），测试模型在指令微调下的稳定性。
*   **模型分类算法**：根据上述三个维度的得分，将模型划分为：**类人标注员（Human-like）**、**客观标注员（Objective）**和**自适应标注员（Adaptive）**。

### 3. 实验设计
*   **数据集**：引入了新数据集 **Bangla_HateSpeech (BAHS)**，包含 5,000 个样本，涵盖性别、宗教、娱乐、地缘政治和政治 5 个领域，涉及 21 个身份类别。
*   **对比方法/模型**：横向对比了 **17 个主流 LLM**，涵盖 6 个家族：GPT (OpenAI)、Gemini (Google)、Llama (Meta)、Mistral、Qwen (Alibaba) 和 DeepSeek。
*   **实验设置**：采用零样本（Zero-shot）设置，通过改变提示词中的“标注员身份”（如“假设你是一名女性/男性标注员”）来诱导偏见。

### 4. 资源与算力
*   **算力平台**：实验在 Kaggle 环境下运行。
*   **硬件配置**：使用了 **两块 NVIDIA T4 GPU**。
*   **训练/推理时长**：文中未明确说明总推理时长，但提到对 17 个模型在 21 个身份类别下进行了大规模推理测试。

### 5. 实验数量与充分性
*   **实验规模**：对 17 个模型进行了跨 21 个身份、5 个领域、4 级提示词深度的测试，实验覆盖面广。
*   **充分性**：包含了消融实验（如标签数量对混淆度的影响）和人类一致性研究（55 名人类标注员对 100 个样本进行基准测试），以校准评估阈值。
*   **客观性**：通过引入 Mismatch Score（不匹配得分）来量化偏见，而非仅看准确率，这种设计较为客观、公平。

### 6. 主要结论与发现
*   **规模悖论**：模型规模大并不意味着标注质量高。较小的模型（如 Qwen 2.5 7B）在一致性和偏见控制上往往优于大型模型（如 GPT-3.5 Turbo）。
*   **推理与标签脱节**：模型能生成非常流畅、看似合理的理由，但给出的标签却是错误的。推理的流畅性不能作为标注正确性的指标。
*   **严重的身份偏见**：在涉及“政治家”等敏感身份时，模型表现极差，且身份暗示会显著改变模型的判断。
*   **提示词不稳定性**：微小的指令措辞变化会导致标注结果大幅波动，显示出模型作为标注工具的脆弱性。

### 7. 优点（亮点）
*   **多维度评估**：不仅关注准确率，还深入分析了推理逻辑和提示词鲁棒性。
*   **低资源语言聚焦**：填补了孟加拉语在 LLM 偏见研究领域的空白。
*   **分类体系创新**：将模型按标注行为分类，为未来选择“自动标注员”提供了参考标准。

### 8. 不足与局限
*   **语言局限性**：研究仅限于孟加拉语，其结论是否能推广到其他低资源语言（如非洲语系）尚待验证。
*   **提示词模板限制**：提示词是人工设计的，可能无法涵盖现实中所有可能的指令变体。
*   **自动化指标局限**：用于评估推理对齐的 BERTScore 和余弦相似度在处理高度主观的仇恨言论时可能存在饱和或偏差。
*   **数据集规模**：5,000 个样本虽然经过精挑细选，但对于捕捉极其罕见或复杂的偏见模式可能仍显不足。

（完）
