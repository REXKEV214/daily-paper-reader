---
title: "ABCD: All Biases Come Disguised"
title_zh: ABCD：所有偏差皆伪装而至
authors: "Mateusz Nowak, Xavier Cadet, Peter Chin"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17445v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 基准测试并减少LLM中的标签位置偏见
tldr: 本研究揭示了大语言模型在多选题评估中普遍存在的标签位置和少样本提示偏差。为此，作者提出了一种名为ABCD的评估协议，通过移除选项标签并要求模型输出完整答案，再利用句子相似度模型进行匹配。实验表明，该方法在多个基准测试中显著降低了答案排列带来的准确率波动，将方差降低了3倍，在保持性能的同时极大增强了评估的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多选题评估方式容易受到选项位置、标签顺序及少样本提示分布等潜在偏差的影响，导致评估结果无法真实反映模型能力。
method: 提出一种去偏差评估协议，通过使用无序统一标签并要求模型输出完整答案文本，结合句子相似度模型进行结果判定。
result: 该方法在多个模型和基准测试上将答案排列引起的平均准确率方差降低了3倍，且模型性能仅有极小幅度的下降。
conclusion: 移除选项标签并采用语义相似度匹配是一种比标准评估更具鲁棒性且能更真实反映模型能力的评估方法。
---

## 摘要
多项选择题（MCQ）基准测试已成为衡量大语言模型（LLM）推理和回答知识性问题能力的标准化评估实践。通过合成的 NonsenseQA 基准测试，我们观察到不同的 LLM 表现出不同程度的“标签-位置-少样本提示”偏差（label-position-few-shot-prompt bias），即模型在回答每个 MCQ 问题时，会利用答案位置、答案前的标签、少样本提示中正确答案的分布，或上述因素的组合。我们提出了一种简单的减少偏差的评估协议，该协议将每个问题的标签替换为统一且无序的标签，并提示 LLM 使用所呈现的完整答案。借助一个简单的句子相似度模型，我们证明了在 LLM 性能下降极小的情况下，不同答案排列之间的鲁棒性得到了提高，标准差有所降低，从而在减少评估人工痕迹（evaluation artifacts）的情况下揭示了 LLM 的真实能力，且无需借助提示示例或选项标签。在多个基准测试和模型中，该协议显著提高了对答案排列的鲁棒性，在模型平均性能仅略微下降的情况下，将平均准确率方差降低了 3 倍。通过对各种嵌入模型和相似度函数的消融研究，我们表明该方法比标准方法更具鲁棒性。

## Abstract
Multiple-choice question (MCQ) benchmarks have been a standard evaluation practice for measuring LLMs' ability to reason and answer knowledge-based questions. Through a synthetic NonsenseQA benchmark, we observe that different LLMs exhibit varying degrees of label-position-few-shot-prompt bias, where the model either uses the answer position, the label in front of the answer, the distributions of correct answers present in the few-shot prompt, or a combination of all to answer each MCQ question. We propose a simple bias-reduced evaluation protocol that replaces the labels of each question with uniform, unordered labels and prompts the LLM to use the whole answer presented. With a simple sentence similarity model, we demonstrate improved robustness and lower standard deviation between different permutations of answers with a minimal drop in LLM's performance, exposing the LLM's capabilities under reduced evaluation artifacts, without any help from the prompt examples or the option labels. Across multiple benchmarks and models, this protocol substantially improves the robustness to answer permutations, reducing mean accuracy variance $3\times$ with only a minimal decrease in the mean model's performance. Through ablation studies on various embedding models and similarity functions, we show that the method is more robust than the standard ones.

---

## 论文详细总结（自动生成）

这篇论文《ABCD: All Biases Come Disguised》深入探讨了大语言模型（LLM）在多选题（MCQ）评估中存在的隐性偏差，并提出了一种更稳健的评估协议。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前评估 LLM 能力的标准多选题（MCQ）基准测试存在严重的“人工痕迹”偏差。模型往往不是通过真正的推理来回答问题，而是利用选项位置、选项标签（如 A/B/C/D）以及少样本（few-shot）提示中正确答案的分布规律来寻找“捷径”。
*   **研究动机**：现有的研究主要关注位置和标签偏差，但忽略了少样本提示中的分布偏差。作者希望通过一种不依赖模型内部概率（logits）、无需微调且计算开销极低的方法，揭示并减少这些评估偏差，从而更真实地衡量模型的推理能力。

### 2. 论文提出的方法论
作者提出了一种名为 **Matched-and-Dashed (M&D)** 的去偏差评估协议，核心思想包括：
*   **统一标签（Dashed）**：将传统的有序标签（A, B, C, D）替换为统一的无序符号（如 Markdown 中的短横线“-”），消除模型对特定字母或顺序的偏好。
*   **全文本生成**：改变提示词，要求模型输出选项的完整文本内容，而非仅仅输出一个标签字母。
*   **语义匹配（Matched）**：
    *   使用正则表达式从模型输出中提取最终答案句子。
    *   利用一个轻量级的句子嵌入模型（如 Qwen3-Embedding-0.6B）计算提取答案与所有候选选项之间的**余弦相似度**。
    *   选择相似度最高的选项作为最终预测结果。
*   **诊断工具 NonsenseQA**：作者构建了一个由随机词汇组成的合成数据集，用于量化模型在完全没有语义信息的情况下，仅靠捕获评估协议中的模式（如少样本分布）能达到多高的准确率。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：NonsenseQA（用于偏差诊断）。
    *   **真实基准**：MMLU-Pro（高难度多领域）、GPQA（研究生水平科学问题）、CommonsenseQA（常识推理）、ARC（科学推理）、INCLUDE（多语言推理）。
*   **对比方法（Benchmark）**：标准的 **Select-and-Letter (S&L)** 协议，即使用 A/B/C/D 标签并要求模型预测字母。
*   **评估模型**：涵盖了 13 个主流开源模型，包括 DeepSeek-R1、Qwen3、Llama-3.1、Gemma-3、Phi-4 等（参数量从 8B 到 32B）。
*   **攻击测试**：采用“答案移动攻击”（answer-moving attacks），通过排列组合改变正确答案的位置和少样本提示的分布，观察模型性能的波动。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和数量，但强调了该方法的**高效性**。
*   **计算开销**：实验表明，M&D 协议相比标准 S&L 协议仅增加了约 **3%** 的总计算时间。其中，语义匹配步骤在处理整个 CommonsenseQA 数据集时仅需不到 3 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 13 个模型和 6 个不同性质的基准测试上进行了广泛实验。
*   **消融实验**：非常充分。作者测试了不同的嵌入模型（不同大小和架构）、不同的相似度函数（余弦、点积等）以及不同的选项符号（如表情符号、数字、X 等），证明了 M&D 协议的稳健性不依赖于特定的嵌入模型选择。
*   **客观性**：通过引入方差比例（Variance Ratio）和 SCORE 鲁棒性指标，客观地量化了模型在不同答案排列下的稳定性。

### 6. 论文的主要结论与发现
*   **偏差普遍存在**：在 NonsenseQA 中，某些模型在纯随机文本上通过利用少样本分布偏差，竟然能达到 >95% 的准确率（随机水平应为 25%）。
*   **显著提升鲁棒性**：M&D 协议将模型在不同答案排列下的准确率方差平均降低了 **3 倍**。
*   **性能保持**：在减少偏差的同时，模型在真实基准测试上的平均准确率仅有极微小的下降，说明该协议并未损害模型的正常发挥。
*   **揭示真实能力**：去偏差后，不同基准测试之间的排名一致性更高，能够更清晰地分辨模型是靠“背诵捷径”还是靠“逻辑推理”得分。

### 7. 优点
*   **简单实用**：无需访问模型的 Logits 或注意力机制，适用于所有生成式 LLM。
*   **单次推理**：与需要多次采样或排列组合的去偏差方法相比，M&D 仅需单次前向传播，成本极低。
*   **诊断精准**：NonsenseQA 提供了一个极佳的工具来识别哪些模型是“考试高手”（擅长利用评估漏洞）。

### 8. 不足与局限
*   **位置偏差残留**：虽然 M&D 减少了标签和分布偏差，但仍无法完全消除位置偏差（模型可能仍倾向于选第一个或最后一个位置），只是将其暴露了出来。
*   **多语言限制**：目前的答案提取规则和提示词主要基于英语，扩展到其他语言可能需要额外的语言专家知识来设计提取正则。
*   **生成与选择的鸿沟**：论文未深入探讨“生成完整答案”与“从选项中选择”这两种认知模式之间本质的能力差异。

（完）
