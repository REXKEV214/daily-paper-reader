---
title: "Learning When to Act or Refuse: Guarding Agentic Reasoning Models for Safe Multi-Step Tool Use"
title_zh: 学习何时行动或拒绝：为安全的多步工具使用保护智能体推理模型
authors: "Aradhye Agarwal, Gurdit Siyan, Yash Pandya, Joykirat Singh, Akshay Nambi, Ahmed Awadallah"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03205v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 针对代理推理和工具使用的安全对齐
tldr: 针对智能体模型在多步工具使用中面临的安全风险（如误操作、隐私泄露），本文提出MOSAIC框架。该框架将推理过程结构化为“计划-检查-执行/拒绝”循环，并利用基于偏好的强化学习进行对齐。实验表明，MOSAIC在多种模型上显著降低了有害行为和隐私泄露，同时保持了良性任务的性能，为智能体安全对齐提供了新方案。
selection_source: fresh_fetch
motivation: 现有的对齐方法难以应对智能体在多步决策和工具调用中可能产生的不可逆危害及对抗性反馈。
method: 提出MOSAIC框架，通过显式的安全推理和拒绝机制，结合基于轨迹对比较的偏好强化学习来训练模型。
result: "在多个模型系列上，MOSAIC将有害行为减少了高达50%，并显著提升了对提示注入攻击的防御能力和隐私保护。"
conclusion: MOSAIC证明了通过结构化推理和偏好学习，可以有效提升智能体在复杂、多步工具使用场景下的安全性和鲁棒性。
---

## 摘要
智能体语言模型运行在与聊天模型根本不同的安全机制下：它们必须进行规划、调用工具并执行长程行动，其中任何一步失误（如访问文件或输入凭据）都可能导致不可逆转的损害。现有的对齐方法主要针对静态生成和任务完成进行优化，由于序列决策、对抗性工具反馈以及过度自信的中间推理，这些方法在上述场景中会失效。我们引入了 MOSAIC，这是一个后训练框架，通过使安全决策显式化且可学习，来对齐智能体以实现安全的多步工具使用。MOSAIC 将推理结构化为“规划、检查、然后行动或拒绝”的循环，并将显式的安全推理和拒绝作为一等公民行动。为了在没有轨迹级标签的情况下进行训练，我们使用了基于偏好的强化学习和成对轨迹比较，这捕捉到了标量奖励经常忽略的安全差异。我们在 Qwen2.5-7B、Qwen3-4B-Thinking 和 Phi-4 三个模型系列，以及涵盖有害任务、提示词注入、良性工具使用和跨域隐私泄露的分布外基准测试中，对 MOSAIC 进行了零样本评估。MOSAIC 将有害行为减少了高达 50%，在注入攻击下的有害任务拒绝率提高了 20% 以上，减少了隐私泄露，并保持或提升了良性任务的性能，展示了在不同模型、领域和智能体设置下的鲁棒泛化能力。

## Abstract
Agentic language models operate in a fundamentally different safety regime than chat models: they must plan, call tools, and execute long-horizon actions where a single misstep, such as accessing files or entering credentials, can cause irreversible harm. Existing alignment methods, largely optimized for static generation and task completion, break down in these settings due to sequential decision-making, adversarial tool feedback, and overconfident intermediate reasoning. We introduce MOSAIC, a post-training framework that aligns agents for safe multi-step tool use by making safety decisions explicit and learnable. MOSAIC structures inference as a plan, check, then act or refuse loop, with explicit safety reasoning and refusal as first-class actions. To train without trajectory-level labels, we use preference-based reinforcement learning with pairwise trajectory comparisons, which captures safety distinctions often missed by scalar rewards. We evaluate MOSAIC zero-shot across three model families, Qwen2.5-7B, Qwen3-4B-Thinking, and Phi-4, and across out-of-distribution benchmarks spanning harmful tasks, prompt injection, benign tool use, and cross-domain privacy leakage. MOSAIC reduces harmful behavior by up to 50%, increases harmful-task refusal by over 20% on injection attacks, cuts privacy leakage, and preserves or improves benign task performance, demonstrating robust generalization across models, domains, and agentic settings.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MOSAIC** 的框架，旨在解决智能体（Agentic）语言模型在多步工具调用过程中的安全对齐问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：智能体模型与传统聊天模型不同，它们涉及规划、工具调用和执行长程行动。在这些场景中，一次失误（如误删文件、泄露凭据）可能导致不可逆的现实危害。
*   **核心挑战**：
    *   **序列决策风险**：有害意图可能分散在多个步骤中，单次输出过滤难以奏效。
    *   **对抗性反馈**：工具返回的结果可能包含间接提示注入（Indirect Prompt Injection）。
    *   **现有对齐失效**：传统的 SFT 或基于标量奖励的 RLHF 往往只关注最终结果，忽略了轨迹中间的安全决策（如“早拒绝”优于“晚中止”）。
    *   **小模型脆弱性**：SLM（小语言模型）在面对复杂指令和工具反馈时更容易产生幻觉或被诱导。

### 2. 方法论：MOSAIC 框架
MOSAIC（Modular Safety Alignment for Intelligent Agents）将智能体的推理过程结构化，并引入显式的安全决策机制。
*   **核心思想**：将推理循环定义为 **“规划 (Plan) → 检查 (Check) → 执行或拒绝 (Act/Refuse)”**。
*   **关键技术细节**：
    *   **显式安全推理（`<safety thoughts>`）**：在执行工具前，模型必须（或选择性地）进行关于风险、权限和不可逆性的自我反思。
    *   **拒绝作为一等公民行动**：引入专门的 `refusal_tool`，使其成为与工具调用并列的显式决策，而非简单的文本生成。
    *   **动态门控（Selective Invocation）**：模型学习何时需要进行安全检查，从而在保证安全的同时减少良性任务的 Token 开销。
*   **算法流程（基于偏好的强化学习）**：
    *   使用 **GRPO（组相对策略优化）** 算法。
    *   **成对轨迹比较（Pairwise Comparison）**：由于智能体任务缺乏地面真值标签，研究者使用 LLM 作为裁判，对比两条完整轨迹的优劣（例如：立即拒绝有害请求的轨迹优于执行了一半才中止的轨迹）。
    *   **复合奖励函数**：结合对齐奖励（来自裁判）、格式奖励（确保输出结构正确）和长度惩罚（提升效率）。

### 3. 实验设计
*   **实验模型**：Qwen2.5-7B-Instruct、Qwen3-4B-Thinking、Phi-4。同时对比了 GPT-4o 和 GPT-5。
*   **评估基准（Benchmarks）**：
    *   **AgentHarm**：评估显式恶意任务及配对的良性任务。
    *   **Agent Security Bench (ASB)**：评估对直接/间接提示注入攻击的防御。
    *   **BFCL v3**：评估良性多轮工具调用的准确性。
    *   **PrivacyLens**：评估跨领域的隐私泄露风险。
*   **对比方法**：Base 模型（无显式安全机制） vs. MOSAIC 训练后的模型。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **4 × NVIDIA A100 GPU** 的机器上运行。
*   **软件框架**：使用了 ByteDance 开发的 **verl** 强化学习库。
*   **训练细节**：文中提到了具体的超参数（如学习率 $10^{-6}$，KL 系数 0.001，最大交互轮数 20 等），但未明确给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同家族、不同规模的模型，并在四个完全不同的 OOD（分布外）基准上进行了零样本评估。
*   **消融实验**：
    1.  **显式检查 vs. 隐式推理**：证明了专门的 `<safety thoughts>` 标签比通用的 `<think>` 标签更有效。
    2.  **成对奖励 vs. 标量奖励**：证明了成对比较能更好地捕捉序列决策中的安全差异。
*   **客观性与公平性**：实验设计考虑了安全与效能的权衡（Safety-Utility Trade-off），不仅测试了有害任务的拒绝率，也测试了良性任务的完成率和 Token 效率，评估较为全面。

### 6. 主要结论与发现
*   **显著提升安全性**：MOSAIC 将有害行为减少了高达 50%，并将注入攻击下的拒绝率提升了 20% 以上。
*   **缩小与闭源模型的差距**：经过 MOSAIC 训练的开源小模型（如 Qwen2.5-7B）在安全指标上超过了未加防护的 GPT-4o。
*   **模型自适应增益**：MOSAIC 能纠正不同模型的固有偏差（如 Qwen 的过度自信或 Phi-4 的过度保守）。
*   **Token 效率**：安全推理仅占总 Token 的不到 20%，且通过长度惩罚，Qwen3 等模型的总 Token 消耗甚至降低了 75%。

### 7. 优点（亮点）
*   **模块化设计**：将安全检查从通用推理中分离，使安全决策可审计、可学习。
*   **解决“序列性”难题**：通过轨迹级偏好学习，解决了标量奖励无法区分“何时拒绝”的问题。
*   **实用性强**：在提升安全性的同时，通过动态门控和长度惩罚优化了推理成本。

### 8. 不足与局限
*   **裁判偏差风险**：依赖 LLM 作为裁判（LLM-as-a-judge），文中提到裁判存在约 60% 的位置偏差（倾向于选择第一个轨迹）。
*   **特定格式依赖**：模型性能高度依赖于特定的标签结构（如 `<think>`、`<tool call>`），在非结构化环境下的泛化能力有待验证。
*   **安全-效能权衡**：在某些模型（如 Phi-4）上，为了提升良性任务完成率，可能会导致在极端对抗环境下的安全性轻微下降。

（完）
