---
title: Context-Aware Counterfactual Data Augmentation for Gender Bias Mitigation in Language Models
title_zh: 语言模型中缓解性别偏见的上下文感知反事实数据增强
authors: "Shweta Parihar, Liu Guangliang, Natalie Parde, Lu Cheng"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09590v1"
tags: ["query:llm-bias-hci"]
score: 9.0
evidence: 使用上下文感知反事实数据增强缓解性别偏见
tldr: 本研究针对微调语言模型中性别偏见缓解可能导致语言建模能力下降的问题，提出了 Context-CDA 方法。该方法利用大语言模型生成具有上下文相关性的反事实数据，以减少去偏语料与预训练数据间的分布差异，并采用基于不确定性的过滤机制提升数据质量。实验证明，该方法在有效降低性别偏见的同时，保持了模型的下游任务性能，为平衡去偏效果与模型能力提供了新思路。
selection_source: fresh_fetch
motivation: 传统反事实数据增强（CDA）生成的合成数据往往缺乏社会语境且与预训练分布不一致，导致模型语言建模能力受损。
method: 提出 Context-CDA 方法，利用大语言模型增强去偏语料的上下文多样性，并结合基于不确定性的过滤机制筛选高质量样本。
result: 在性别偏见基准测试中，该方法在有效减少偏见的同时，成功保持了模型的语言建模性能。
conclusion: 通过增强上下文相关性和数据质量，Context-CDA 为在不损害模型性能的前提下缓解社会偏见提供了有效途径。
---

## 摘要
在微调语言模型（LMs）中缓解社会偏见的一个挑战是语言建模能力的潜在下降，这可能会损害下游任务的性能。反事实数据增强（CDA）是一种广泛使用的微调方法，它通过生成可能与现实世界分布不一致的合成数据，或者创建过于简单的反事实样本（忽略了预训练语料库中改变的敏感属性（如性别）的社会上下文），从而凸显了这一问题。为了解决这些局限性，我们提出了一种简单而有效的上下文增强 CDA 方法，即 Context-CDA，它利用大语言模型来增强去偏语料库的多样性和上下文相关性。通过增强的上下文来最小化去偏语料库与预训练数据之间的差异，该方法确保了更好的对齐，从而提升了语言建模能力。随后，我们采用基于不确定性的过滤，剔除被目标小语言模型（即待去偏的语言模型）视为低质量的生成反事实样本，从而进一步提高微调语料库的质量。在性别偏见基准测试上的实验结果表明，Context-CDA 在不牺牲语言建模性能的情况下有效地缓解了偏见，同时通过分析下一个词元（next-token）生成概率的分布偏移，为社会偏见提供了见解。

## Abstract
A challenge in mitigating social bias in fine-tuned language models (LMs) is the potential reduction in language modeling capability, which can harm downstream performance. Counterfactual data augmentation (CDA), a widely used method for fine-tuning, highlights this issue by generating synthetic data that may align poorly with real-world distributions or creating overly simplistic counterfactuals that ignore the social context of altered sensitive attributes (e.g., gender) in the pretraining corpus. To address these limitations, we propose a simple yet effective context-augmented CDA method, Context-CDA, which uses large LMs to enhance the diversity and contextual relevance of the debiasing corpus. By minimizing discrepancies between the debiasing corpus and pretraining data through augmented context, this approach ensures better alignment, enhancing language modeling capability. We then employ uncertainty-based filtering to exclude generated counterfactuals considered low-quality by the target smaller LMs (i.e., LMs to be debiased), further improving the fine-tuning corpus quality. Experimental results on gender bias benchmarks demonstrate that Context-CDA effectively mitigates bias without sacrificing language modeling performance while offering insights into social biases by analyzing distribution shifts in next-token generation probabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《Context-Aware Counterfactual Data Augmentation for Gender Bias Mitigation in Language Models》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**如何在缓解语言模型（LMs）中的性别偏见时，不损害其原有的语言建模能力（Language Modeling Capability）**。
*   **背景**：传统的反事实数据增强（CDA）通过交换性别词（如将“he”改为“she”）来生成去偏数据。
*   **动机**：传统CDA生成的句子往往过于简单、不自然，且与模型预训练时的真实数据分布存在显著差异。这种“分布偏移”会导致模型在微调后虽然偏见减少了，但理解自然语言的能力（如流畅度、逻辑性）大幅下降。

### 2. 论文提出的方法论：Context-CDA
作者提出了一种名为 **Context-CDA** 的框架，主要包含三个关键步骤：
1.  **上下文感知增强（Context-Aware Augmentation）**：
    *   首先进行传统的性别词翻转。
    *   利用更强大的大语言模型（如 Llama-3-8B-Instruct）作为“重写器”，通过特定的 Prompt 指令，将简单的性别翻转句子改写为上下文更丰富、更符合自然语言分布的句子。
2.  **基于不确定性的过滤（Uncertainty-Based Filtering）**：
    *   为了防止大模型生成的文本对目标小模型（待去偏模型）过于复杂或包含幻觉，引入了**语义熵（Semantic Entropy, SE）**。
    *   **技术细节**：对每个生成样本进行多次采样并聚类，计算意义层面的熵。如果目标模型对某个句子的语义熵很高，说明该句子模糊或难以理解。
    *   **流程**：剔除语义熵最高的前 $k\%$（实验设定为 30%）的样本，确保微调语料的高质量。
3.  **去偏微调（Debiasing via Fine-tuning）**：
    *   在过滤后的高质量、上下文丰富的反事实语料库上对目标模型进行微调。

### 3. 实验设计
*   **目标模型（Target LMs）**：涵盖了五种主流架构，包括 BERT、DistilBERT（仅编码器）、T5（编码器-解码器）、GPT-2、Llama-3.2-1B（仅解码器）。
*   **数据集与 Benchmark**：
    *   **内在偏见（Intrinsic Bias）**：StereoSet、CrowS-Pairs。
    *   **外在偏见（Extrinsic Bias）**：BiasBios、STS-B、NLI-Bias。
    *   **下游任务（Downstream Tasks）**：GLUE 榜单中的 SST-2、RTE、QNLI。
*   **对比方法**：
    *   基准：Vanilla（原始模型）、传统 CDA。
    *   其他去偏技术：MABEL、INLP、SelfDebias、SENT-DEBIAS、wiki-debiased 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。
*   **实现细节**：提到了使用 Llama-3-8B-Instruct 生成增强数据，并对目标模型进行了多个 Epoch 的微调（实验图表显示通常在 75-85 个 Epoch 达到收敛）。

### 5. 实验数量与充分性
*   **实验规模**：作者在 5 种不同架构的模型上进行了广泛测试，这在同类论文中属于非常充分的验证，证明了方法的**模型无关性（Model-Agnostic）**。
*   **消融实验**：针对语义熵过滤阈值（20%、30%、40%）进行了专门的消融研究，验证了 30% 阈值的最优性。
*   **分析深度**：除了标准的偏见分数，还进行了 **Next-token 分布分析**，观察模型在预测性别相关词汇时的概率分布偏移，增加了实验的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能平衡**：Context-CDA 在显著降低性别偏见的同时，其语言建模得分（LMS）和综合得分（ICAT）均优于传统 CDA，甚至在某些指标上接近原始模型。
*   **收敛性**：实验发现去偏性能在 75-85 个 Epoch 趋于稳定，且不会出现严重的过拟合。
*   **分布优化**：通过 Next-token 分析发现，Context-CDA 使得模型预测的词汇分布更加均衡，减少了对男性词汇的过度倾斜，提升了预测的鲁棒性。

### 7. 优点（亮点）
*   **解决痛点**：直接针对 CDA 损害模型能力的长期痛点，通过引入 LLM 增强上下文来弥补分布差异。
*   **质量控制**：创新性地将语义熵（通常用于检测幻觉）引入去偏领域，作为样本筛选的质量指标。
*   **通用性强**：实验覆盖了从 BERT 到 Llama 的多种架构，证明了该方法在判别式和生成式系统中的普适性。

### 8. 不足与局限
*   **计算成本**：依赖大模型（如 Llama-3-8B）生成数据，对于资源受限的环境可能存在成本和效率瓶颈。
*   **偏见覆盖面**：目前仅专注于**二元性别（Binary Gender）**，未涉及非二元性别、交叉性偏见（如种族+性别）或其他社会属性。
*   **过滤风险**：虽然语义熵过滤提升了质量，但也可能不小心剔除了一些具有挑战性但有价值的复杂反事实样本。
*   **语言限制**：实验主要基于英语语料，未在多语言或形态丰富的语言环境中验证。

（完）
