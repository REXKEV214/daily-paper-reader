---
title: Fail-Closed Alignment for Large Language Models
title_zh: 大语言模型的故障闭合对齐
authors: "Zachary Coalson, Beth Sohler, Aiden Gabriel, Sanghyun Hong"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.16977v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 用于增强LLM安全性和拒绝机制的故障关闭对齐
tldr: 针对当前大语言模型对齐机制在面对提示词攻击时容易失效（fail-open）的问题，本文提出了“故障闭锁”（fail-closed）对齐原则。该方法通过渐进式对齐框架，迭代地识别并消融已学习的拒绝方向，迫使模型在多个独立子空间内重建安全机制。实验表明，该方法在保持生成质量的同时，显著提升了模型对抗多种越狱攻击的鲁棒性，为构建更安全的大模型提供了新思路。
selection_source: fresh_fetch
motivation: 现有的拒绝机制往往因单一关键特征被抑制而失效，缺乏冗余性和鲁棒性。
method: 提出一种渐进式对齐框架，通过迭代识别并消融已有的拒绝特征，强制模型开发出多条独立的拒绝路径。
result: 在四种越狱攻击测试中表现出最强的鲁棒性，同时有效缓解了过度拒绝问题并保持了生成质量。
conclusion: 故障闭锁对齐通过构建多个因果独立的拒绝方向，为实现鲁棒的大语言模型安全对齐奠定了理论与实践基础。
---

## 摘要
我们发现了当前大语言模型（LLM）对齐中的一个结构性弱点：现代拒绝机制是“故障开启”（fail-open）的。虽然现有方法在多个潜特征中编码拒绝行为，但通过基于提示词的越狱来抑制单个主导特征，就可能导致对齐崩溃，从而产生不安全的生成内容。受此启发，我们提出将“故障闭合对齐”（fail-closed alignment）作为鲁棒 LLM 安全的设计原则：拒绝机制应通过冗余且独立的因果路径，在发生部分故障时依然保持有效。我们展示了该原则的一个具体实例化：一个渐进式对齐框架，它迭代地识别并消融先前学习到的拒绝方向，迫使模型沿着新的、独立的子空间重建安全性。在四种越狱攻击中，我们在减轻过度拒绝并保持生成质量的同时，以较小的计算开销实现了最强的整体鲁棒性。我们的机制分析证实，使用我们方法训练的模型编码了多个因果独立的拒绝方向，基于提示词的越狱无法同时抑制这些方向，这为将故障闭合对齐作为鲁棒 LLM 安全的有原则基础提供了经验支持。

## Abstract
We identify a structural weakness in current large language model (LLM) alignment: modern refusal mechanisms are fail-open. While existing approaches encode refusal behaviors across multiple latent features, suppressing a single dominant feature$-$via prompt-based jailbreaks$-$can cause alignment to collapse, leading to unsafe generation. Motivated by this, we propose fail-closed alignment as a design principle for robust LLM safety: refusal mechanisms should remain effective even under partial failures via redundant, independent causal pathways. We present a concrete instantiation of this principle: a progressive alignment framework that iteratively identifies and ablates previously learned refusal directions, forcing the model to reconstruct safety along new, independent subspaces. Across four jailbreak attacks, we achieve the strongest overall robustness while mitigating over-refusal and preserving generation quality, with small computational overhead. Our mechanistic analyses confirm that models trained with our method encode multiple, causally independent refusal directions that prompt-based jailbreaks cannot suppress simultaneously, providing empirical support for fail-closed alignment as a principled foundation for robust LLM safety.

---

## 论文详细总结（自动生成）

以下是对论文《Fail-Closed Alignment for Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出当前大语言模型（LLM）的对齐机制存在**结构性脆弱**。目前的拒绝机制是“**故障开启**”（fail-open）的：虽然拒绝行为涉及多个潜在特征，但通常依赖于一个主导的线性方向。攻击者只需通过提示词越狱（Jailbreak）抑制这一个核心特征，模型的安全对齐就会彻底崩溃。
**研究动机**：借鉴传统安全系统“故障闭锁”（fail-closed，即部分失效时默认拒绝访问）的原则，作者提出应在 LLM 内部构建**冗余且独立**的因果路径，使得模型即使在部分安全特征被抑制时，仍能通过其他独立路径保持拒绝行为。

### 2. 方法论：故障闭锁对齐（Fail-Closed Alignment）
核心思想是通过**渐进式对齐框架**，强制模型在多个独立的表示子空间内重复构建安全机制。
*   **核心流程（算法 1）**：
    1.  **识别方向**：使用基于梯度的“拒绝方向优化”（RDO）算法，识别当前模型中最主导的拒绝特征方向 $r_k$。
    2.  **多特征消融（MFA）**：构建一个正交投影算子，将当前识别出的所有拒绝方向从激活空间中剔除。
    3.  **重新训练**：在应用了 MFA 算子（即“禁用”了已知安全路径）的情况下，对模型进行微调。
    4.  **迭代**：重复上述过程 $K$ 次（实验中 $K=10$），迫使模型不断寻找新的、独立的维度来实现拒绝行为。
*   **损失函数**：结合了安全损失（在消融已知方向后仍需正确拒绝有害请求）和效用损失（通过 KL 散度保持模型在良性指令上的表现）。

### 3. 实验设计
*   **模型**：涵盖了不同家族和规模的模型，包括 Llama2-7B、Llama3-8B、Gemma2-2B 和 Gemma2-9B。
*   **攻击场景（Benchmark）**：
    *   **白盒攻击**：GCG、AutoDAN。
    *   **黑盒攻击**：PAIR、HumanJailbreaks（人工收集的越狱模板）。
    *   **特征消融攻击**：直接在潜空间消融拒绝方向。
*   **对比方法**：Augmented RT（增强拒绝训练）、CAT（连续对抗训练）、LAT（潜空间对抗训练）、DeepRefusal（单方向消融训练）。
*   **评估指标**：攻击成功率（ASR）、良性指令合规率（CR，衡量过度拒绝）、通用任务准确率（Acc.）。

### 4. 资源与算力
论文明确说明了实验使用的硬件资源：
*   **设备 1**：Intel Xeon 48 核 CPU，768GB 内存，**8 台 Nvidia A40 GPU**。
*   **设备 2**：Intel Xeon 112 核 CPU，2TB 内存，**8 台 Nvidia H100 GPU**。
*   **效率说明**：该方法每步训练约需 2 次前向和后向传播，计算开销远低于 LAT 和 CAT 等复杂的对抗训练方法（后者通常需要 11 次左右）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个模型上针对 4 种主流越狱攻击进行了全面测试，每组实验包含 200 个有害提示词。
*   **消融实验**：非常充分。作者测试了迭代次数 $K$、效用权重 $\lambda$、多特征 vs 单特征消融、梯度 vs 激活识别方向、模型特定数据 vs 固定数据的影响。
*   **机制分析**：通过因果干预实验和神经元分析，验证了模型确实学习到了多个独立的拒绝方向。
*   **客观性**：使用了 HarmBench 标准评测框架，对比了多种最先进的防御基线，实验设计严谨且具有说服力。

### 6. 主要结论与发现
*   **卓越的鲁棒性**：该方法将平均 ASR 降低了 92-97%，在所有模型和攻击类型上均优于基线，尤其在对抗黑盒攻击 PAIR 时表现极佳。
*   **保持效用**：在提升安全性的同时，模型在良性任务上的准确率几乎没有下降（仅波动约 0.8%），且过度拒绝率（Over-refusal）是所有鲁棒方法中最低的。
*   **参数高效性**：该方法与 LoRA 兼容，仅需微调约 5% 的参数即可达到全量微调的效果。
*   **机制验证**：分析证实，模型内部确实形成了多个互不干扰的拒绝特征，攻击者难以同时抑制所有路径。

### 7. 优点（亮点）
*   **设计原则新颖**：首次将“故障闭锁”这一经典安全原则引入 LLM 对齐，从架构冗余的角度解决鲁棒性问题。
*   **防御普适性**：不针对特定攻击手段进行训练，而是通过改变内部表示结构来防御一类攻击（抑制特征类攻击）。
*   **高性价比**：相比于昂贵的对抗训练，该方法在计算开销和防御效果之间取得了极好的平衡。

### 8. 不足与局限
*   **非线性特征识别**：实验发现迭代到后期（如第 6-10 个方向）时，线性特征的激活变弱，暗示模型可能开始利用非线性机制编码拒绝，而本文方法主要针对线性方向。
*   **特定攻击残留**：虽然 ASR 大幅下降，但在面对 GCG 等能“劫持”注意力机制的攻击时，仍有极少数（<4%）的失效案例。
*   **应用限制**：目前主要在 2B 到 9B 规模的模型上验证，尚未在超大规模模型（如 70B+）上进行测试。

（完）
