---
title: "Learning to Remember, Learn, and Forget in Attention-Based Models"
title_zh: 在基于注意力的模型中学习记忆、学习与遗忘
authors: "Djohan Bonnet, Jamie Lohoff, Jan Finkbeiner, Elidona Skhikerujah, Emre Neftci"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.09075v2"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长序列中的在线联想记忆与稳定性-塑性平衡
tldr: 针对门控线性注意力模型在长序列处理中存在的内存容量固定和干扰问题，本文提出 Palimpsa 模型。该模型将上下文学习视为持续学习过程，引入贝叶斯元塑性机制，通过重要性状态调节注意力状态的更新。Palimpsa 统一了多种线性注意力模型（如 Mamba2），显著提升了内存容量，在 MQAR 和常识推理任务中表现优于基线模型。
selection_source: fresh_fetch
motivation: 现有的门控线性注意力模型在处理长序列时面临内存容量受限和信息干扰的挑战。
method: 提出 Palimpsa 模型，利用贝叶斯元塑性机制，根据先验分布和重要性状态动态调整注意力状态的更新。
result: 实验证明 Palimpsa 在多查询关联召回（MQAR）和常识推理任务上均显著优于现有的基线模型。
conclusion: 通过将上下文学习建模为元塑性持续学习问题，可以有效扩展线性注意力模型的记忆能力并提升其性能。
---

## 摘要
Transformer 中的上下文学习 (In-Context Learning, ICL) 充当在线联想记忆，并被认为是其在复杂序列处理任务中表现优异的基础。然而，在门控线性注意力模型中，这种记忆的容量是固定的，且容易受到干扰，特别是在处理长序列时。我们提出了 Palimpsa，这是一种自注意力模型，它将 ICL 视为一个必须解决稳定性-可塑性困境的持续学习问题。Palimpsa 使用贝叶斯元可塑性 (Bayesian metaplasticity)，其中每个注意力状态的可塑性与一个重要性状态相关联，而该重要性状态基于捕获累积知识的先验分布。我们证明了各种门控线性注意力模型可以看作是特定的架构选择和后验近似，而 Mamba2 是 Palimpsa 的一个特例，其中遗忘占据主导地位。这一理论联系使得任何非元可塑性模型都能转化为元可塑性模型，从而显著扩展其记忆容量。我们的实验表明，Palimpsa 在多查询联想召回 (MQAR) 基准测试和常识推理任务上始终优于基准模型。

## 速览
**TLDR**：针对门控线性注意力模型在长序列处理中存在的内存容量固定和干扰问题，本文提出 Palimpsa 模型。该模型将上下文学习视为持续学习问题，引入贝叶斯元塑性（Bayesian metaplasticity）机制，通过重要性状态调节注意力状态的塑性。Palimpsa 统一了多种架构并显著提升了内存容量，在 MQAR 和常识推理任务中表现优异。 \
**Motivation**：现有的门控线性注意力模型在处理长序列时面临内存容量受限和信息干扰的挑战。 \
**Method**：提出 Palimpsa 模型，利用贝叶斯元塑性理论，根据先验分布和重要性状态动态调整注意力状态的更新。 \
**Result**：实验证明 Palimpsa 在多查询关联召回（MQAR）和常识推理任务上均显著优于现有基准模型。 \
**Conclusion**：通过将元塑性引入注意力机制，可以有效解决稳定性与塑性的权衡问题，大幅扩展模型的内存容量。

---

## Abstract
In-Context Learning (ICL) in transformers acts as an online associative memory and is believed to underpin their high performance on complex sequence processing tasks. However, in gated linear attention models, this memory has a fixed capacity and is prone to interference, especially for long sequences. We propose Palimpsa, a self-attention model that views ICL as a continual learning problem that must address a stability-plasticity dilemma. Palimpsa uses Bayesian metaplasticity, where the plasticity of each attention state is tied to an importance state grounded by a prior distribution that captures accumulated knowledge. We demonstrate that various gated linear attention models emerge as specific architecture choices and posterior approximations, and that Mamba2 is a special case of Palimpsa where forgetting dominates. This theoretical link enables the transformation of any non-metaplastic model into a metaplastic one, significantly expanding its memory capacity. Our experiments show that Palimpsa consistently outperforms baselines on the Multi-Query Associative Recall (MQAR) benchmark and on Commonsense Reasoning tasks.

---

## 论文详细总结（自动生成）

这篇论文由来自德国尤利希研究中心和亚琛工业大学的研究团队撰写，提出了一种名为 **Palimpsa** 的新型注意力机制，旨在解决线性注意力模型在长序列处理中的记忆瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：Transformer 的 KV 缓存随序列长度线性增长，导致计算和内存成本高昂。线性 Transformer 和状态空间模型（SSM）虽使用固定大小的内存，但在处理长序列时面临“稳定性-塑性困境”（Stability-Plasticity Dilemma）：要么容易被新信息覆盖（灾难性遗忘），要么无法吸收新信息（灾难性记忆）。
*   **核心问题**：如何在固定大小的注意力内存中，动态地平衡“记住重要旧信息”、“学习新信息”和“遗忘陈旧信息”这三个过程。
*   **整体含义**：论文将上下文学习（ICL）重新定义为一个持续学习问题，并引入神经科学中的“元塑性”（Metaplasticity）概念，通过贝叶斯框架为每个内存状态提供自适应的学习率。

### 2. 方法论
*   **核心思想**：引入**贝叶斯元塑性（Bayesian Metaplasticity）**。模型不仅维护一个内存状态（均值），还维护一个重要性状态（精确度/方差）。
*   **关键技术细节**：
    *   **双状态更新**：模型包含内存矩阵 $S_t$（存储键值对）和重要性矩阵 $I_t$（存储每个状态的重要性）。
    *   **变分推理（VI）**：将注意力更新建模为最小化变分自由能的过程。通过高斯假设，推导出闭式解。
    *   **重要性驱动的塑性**：更新步长（学习率）与该状态已积累的重要性成反比。重要信息对应的状态更稳定，不重要的状态更具塑性。
    *   **贝叶斯遗忘**：引入一个基于输入的时间窗口 $N_t$，通过权重衰减机制释放陈旧信息，防止内存饱和。
*   **算法流程**：
    1.  计算输入相关的遗忘门 $\alpha_t$ 和输入重要性 $\beta_t$。
    2.  更新重要性矩阵 $I_t$（累加新证据并按比例衰减旧证据）。
    3.  根据 $I_t$ 动态调整比例，更新内存均值 $\mu_t$。
    4.  利用关联扫描（Associative Scan）实现并行化计算。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成任务**：多查询关联召回（MQAR），用于测试模型在受到干扰时提取特定键值对的能力。
    *   **语言建模**：在 FineWeb-Edu 数据集上进行大规模预训练（15B 和 30B tokens）。
    *   **常识推理**：在 LAMBADA、PIQA、HellaSwag、WinoGrande、ARC 和 SIQA 等零样本任务上评估。
*   **Benchmark 与对比方法**：
    *   对比了 Transformer++、Gated Deltanet 和 Mamba2。
    *   提出了两种变体：**Palimpsa-D**（基于 Deltanet）和 **Palimpsa-M**（基于 Mamba2）。
    *   进行了消融实验（w/o Meta），即禁用元塑性机制以验证其有效性。

### 4. 资源与算力
*   **算力设备**：推理速度测试在单张 **NVIDIA RTX 3090** GPU 上完成。
*   **训练规模**：预训练了 170M 和 760M 参数规模的模型。
*   **训练框架**：使用了基于 Triton 的自定义内核和 Flame 训练框架。
*   **效率说明**：由于需要存储额外的状态（重要性矩阵），Palimpsa 的训练速度比高度优化的 Mamba2 慢约 1.5 到 3 倍，但在推理时表现出良好的吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到 760M 参数量级的真实语言模型实验，实验设置较为全面。
*   **充分性**：MQAR 实验使用了 8 个不同的随机种子，提供了标准差分析。
*   **客观性**：通过“微调非元塑性模型”的对比实验，证明了即使在相同 token 预算下，引入元塑性也能带来显著提升，实验设计较为公平。

### 6. 主要结论与发现
*   **性能提升**：Palimpsa 在 MQAR 任务中显著优于基线，且优势随序列长度增加而扩大。在 760M 规模下，Palimpsa-D 的常识推理平均得分比 Gated Deltanet 高出约 0.8 分。
*   **理论统一**：证明了 Mamba2 是 Palimpsa 的一个特例（即遗忘率极高、重要性积累被忽略的情况）。
*   **长程依赖**：在需要广泛上下文的 LAMBADA 任务上表现尤为突出，验证了其更强的记忆保留能力。
*   **可微调性**：现有的非元塑性模型（如 Mamba2）可以通过微调轻松转化为 Palimpsa 模型，从而扩展记忆容量。

### 7. 优点
*   **理论严谨**：将多种门控线性模型统一在贝叶斯推理框架下，提供了清晰的数学解释。
*   **解决核心痛点**：通过元塑性机制优雅地解决了固定内存模型的稳定性-塑性困境。
*   **硬件友好**：开发了自定义 Triton 内核，支持并行化训练，具备实际应用潜力。

### 8. 不足与局限
*   **训练开销**：由于需要维护和更新额外的精确度矩阵（Importance state），寄存器压力较大，导致训练吞吐量低于最简化的线性注意力模型。
*   **近似偏差**：为了计算效率，采用了对角协方差矩阵近似，忽略了状态之间的相关性。
*   **模型规模限制**：实验最高只做到 760M 参数，尚未在百亿（10B+）参数量级的超大规模模型上验证其扩展性。

（完）
