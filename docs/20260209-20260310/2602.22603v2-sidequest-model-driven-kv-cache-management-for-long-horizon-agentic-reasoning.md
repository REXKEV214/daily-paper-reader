---
title: "SideQuest: Model-Driven KV Cache Management for Long-Horizon Agentic Reasoning"
title_zh: SideQuest：面向长程智能体推理的模型驱动 KV 缓存管理
authors: "Sanjay Kariyappa, G. Edward Suh"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22603v2"
tags: ["query:llm-memory"]
score: 9.0
evidence: 面向长程智能体推理的模型驱动KV缓存管理
tldr: SideQuest 针对长程智能体任务中 KV 缓存占用过高的问题，提出了一种由模型驱动的压缩方法。不同于传统的启发式策略，它利用大推理模型（LRM）自身来评估上下文中各 token 的重要性，并将其作为并行辅助任务执行，以避免干扰主推理过程。实验表明，该方法在仅需少量训练数据的情况下，能显著降低峰值显存占用并保持高准确度，有效解决了长上下文推理中的内存瓶颈。
selection_source: fresh_fetch
motivation: 现有的 KV 缓存压缩启发式方法难以有效支持长程智能体任务中的多步推理，导致内存压力大且性能受限。
method: 提出 SideQuest 框架，将 KV 缓存管理作为并行的辅助任务，利用推理模型自身判断 token 的有用性并进行压缩。
result: "在仅使用 215 个样本训练的情况下，SideQuest 将智能体任务的峰值 token 使用量减少了高达 65%，且准确率几乎无损。"
conclusion: 通过让推理模型参与自身的缓存管理，可以比传统启发式方法更高效地处理长上下文推理任务。
---

## 摘要
长期运行的智能体任务（如深度研究）需要对分布在多个网页和文档中的信息进行多跳推理。在此类任务中，大语言模型（LLM）的上下文主要由来自外部检索的 token 占据，导致内存使用量迅速增长并限制了解码性能。尽管针对长上下文输入已存在多种 KV 缓存压缩技术，但我们发现现有的启发式方法无法有效支持多步推理模型。我们通过 SideQuest 解决了这一挑战——这是一种利用大推理模型（LRM）自身通过推理上下文中 token 的有用性来执行 KV 缓存压缩的新颖方法。为了防止与该管理过程相关的 token 污染模型内存，我们将 KV 缓存压缩构建为与主推理任务并行执行的辅助任务。我们的评估结果显示，使用仅包含 215 个样本训练的模型，SideQuest 在智能体任务中将峰值 token 使用量减少了高达 65%，且准确率下降极小，性能优于基于启发式的 KV 缓存压缩技术。

## Abstract
Long-running agentic tasks, such as deep research, require multi-hop reasoning over information distributed across multiple webpages and documents. In such tasks, the LLM context is dominated by tokens from external retrieval, causing memory usage to grow rapidly and limiting decode performance. While several KV cache compression techniques exist for long-context inputs, we find that existing heuristics fail to support multi-step reasoning models effectively. We address this challenge with SideQuest -- a novel approach that leverages the Large Reasoning Model (LRM) itself to perform KV cache compression by reasoning about the usefulness of tokens in its context. To prevent the tokens associated with this management process from polluting the model's memory, we frame KV cache compression as an auxiliary task executed in parallel to the main reasoning task. Our evaluations, using a model trained with just 215 samples, show that SideQuest reduces peak token usage by up to 65% on agentic tasks with minimal degradation in accuracy, outperforming heuristic-based KV cache compression techniques.

---

## 论文详细总结（自动生成）

以下是对论文《SideQuest: Model-Driven KV Cache Management for Long-Horizon Agentic Reasoning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程智能体（Agentic）任务（如深度研究、多步推理）中，KV 缓存（KV Cache）随上下文线性增长，导致显存占用巨大且推理延迟增加。
*   **研究背景**：现有的 KV 缓存压缩技术（如 H2O, SnapKV）主要依赖静态启发式方法（如注意力分数）。然而，在智能体推理中，Token 的重要性是**动态且非单调**的：一个早期看似无关的 Token 可能在十步之后的总结阶段变得至关重要。传统的启发式方法往往会错误地永久删除这些关键信息，导致推理失败。

### 2. 方法论：SideQuest 框架
*   **核心思想**：将 KV 缓存管理从“静态启发式”转变为“模型驱动的智能决策”。让大推理模型（LRM）通过语义理解，主动识别并清理不再需要的工具调用输出（即“内存垃圾回收”）。
*   **关键技术细节**：
    *   **并行辅助线程（Auxiliary Thread）**：为了不干扰主推理任务且不污染主上下文，SideQuest 在共享上下文的基础上派生出一个并行线程。该线程专门负责分析哪些“游标”（Cursors，即工具输出片段）已失效。
    *   **触发机制**：通过特定的触发词（如 `** Memory management mode **`）切换模型模式。
    *   **后验标注训练（Hindsight Annotation）**：利用已知正确答案的推理轨迹，回溯每个工具输出最后一次被引用的时间点，自动生成“何时可以删除该信息”的监督信号。
    *   **联合优化**：使用 LoRA 进行微调，结合交叉熵损失（学习删除指令）和逻辑蒸馏损失（Logit Distillation，确保模型不丧失原始推理能力）。

### 3. 实验设计
*   **数据集/场景**：
    *   **FRAMES**：基于维基百科的多跳推理任务。
    *   **BrowseComp**：复杂的网页浏览与信息合成任务。
*   **Benchmark 与对比方法**：
    *   **基准**：未压缩的原始模型（Full Attention）。
    *   **对比方法**：H2O（基于重击者/频率）、SnapKV（基于聚类）、R-KV（基于冗余感知）。
*   **评估指标**：峰值 Token 使用量、KV 缓存内存读取量、任务准确率、系统吞吐量。

### 4. 资源与算力
*   **模型规模**：主要实验基于 `gpt-oss-20b`（混合专家模型 MoE），标注模型使用 `gpt-oss-120b`。
*   **硬件使用**：推理性能测试在单张 **NVIDIA H100 GPU** 上完成。
*   **训练细节**：使用 LoRA 微调 3 个 Epoch，学习率为 $2 \times 10^{-4}$。训练集规模极小，仅使用了 **215 个高质量样本**。

### 5. 实验数量与充分性
*   **实验规模**：在 FRAMES 上测试了 424 个样本，在 BrowseComp 上测试了 500 个样本。
*   **充分性评价**：实验涵盖了“中等”和“高”两种推理强度设置，并对比了多种主流压缩算法。通过消融实验展示了固定 Token 预算方法的局限性。虽然样本量不算巨大，但覆盖了长程推理的核心痛点，对比实验设计公平（在相同 Token 预算下对比准确率）。

### 6. 主要结论与发现
*   **高效压缩**：SideQuest 将峰值 Token 使用量减少了 **56% - 65%**，内存读取量减少了 **53% - 71%**。
*   **性能保持**：在大幅压缩的情况下，准确率仅有极微小下降（1% - 5%），远优于启发式方法（后者在同等压缩率下往往会出现模型崩溃或逻辑混乱）。
*   **系统增益**：在实际部署中，SideQuest 将系统吞吐量提升了 **83.9%**，并将总运行时间缩短了 **36.8%**。

### 7. 优点与亮点
*   **语义感知**：首次将内存管理视为一种“推理技能”，利用模型对任务状态的理解来做决策，而非简单的统计指标。
*   **架构优雅**：采用并行辅助线程设计，既利用了共享前缀缓存（Prefix Caching）的效率，又避免了管理 Token 对主任务的干扰。
*   **自适应性**：无需预设固定的 Token 窗口，模型能根据任务难度动态调整保留的内容。

### 8. 不足与局限
*   **数据依赖**：虽然 215 个样本效果显著，但在分布外（OOD）数据集上仍有小幅性能下降，可能需要更大规模的训练数据来增强泛化性。
*   **清理范围有限**：目前仅针对“工具输出”（Tool Responses）进行清理，尚未涉及模型自身的“思考过程”（Thought Traces）压缩。
*   **实现复杂度**：相比于简单的启发式算法，SideQuest 需要对模型进行微调，并要求推理引擎支持复杂的并行线程管理。

（完）
