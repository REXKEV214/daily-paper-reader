---
title: "RAD-DPO: Robust Adaptive Denoising Direct Preference Optimization for Generative Retrieval in E-commerce"
title_zh: RAD-DPO：面向电子商务生成式检索的鲁棒自适应去噪直接偏好优化
authors: "Zhiguo Chen, Guohao Sun, Yiming Qiu, Xingzhi Yao, Mingming Li, Huimu Wang, Yangqi Zhang, Songlin Wang, Sulong Xu"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23964v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 使生成式检索与复杂用户偏好对齐
tldr: 针对电商生成式检索中DPO算法在处理语义ID时存在的梯度冲突、噪声负样本及多标签概率挤压问题，本文提出RAD-DPO框架。该框架通过Token级梯度分离保护前缀结构，利用动态奖励权重降低噪声干扰，并结合全局对比目标优化多标签覆盖。实验证明该方法显著提升了排序质量与训练效率。
selection_source: fresh_fetch
motivation: 传统的DPO在处理具有层级结构的语义ID时，容易产生梯度冲突且对电商场景中的伪负样本噪声敏感。
method: 提出RAD-DPO，包含Token级梯度分离、基于相似度的动态奖励加权以及整合全局SFT损失的多标签对比目标。
result: 在大规模电商平台的离线实验和在线A/B测试中，该方法在排序质量和训练效率上均表现出显著提升。
conclusion: RAD-DPO有效解决了生成式检索在对齐复杂用户偏好时的鲁棒性与多标签学习难题。
---

## 摘要
生成式检索（GR）已成为电子商务搜索中的一种强大范式，通过对语义 ID（SID）进行自回归解码来检索商品。然而，使 GR 与复杂的用户偏好保持一致仍然具有挑战性。虽然直接偏好优化（DPO）提供了一种高效的对齐方案，但将其直接应用于结构化 SID 时存在三个局限性：(i) 它会惩罚共享的分层前缀，导致梯度冲突；(ii) 它容易受到来自隐式反馈的噪声伪负样本的影响；(iii) 在具有多个相关商品的多标签查询中，它加剧了有效候选目标之间的概率“挤压效应”。为了解决这些问题，我们提出了 RAD-DPO，它引入了令牌级梯度分离以保护前缀结构，引入了基于相似度的动态奖励加权以减轻标签噪声，并引入了集成全局 SFT 损失的多标签全局对比目标，以显式扩大正样本覆盖范围。在大规模电子商务平台上进行的广泛离线实验和在线 A/B 测试表明，该方法在排序质量和训练效率方面都有显著提升。

## Abstract
Generative Retrieval (GR) has emerged as a powerful paradigm in e-commerce search, retrieving items via autoregressive decoding of Semantic IDs (SIDs). However, aligning GR with complex user preferences remains challenging. While Direct Preference Optimization (DPO) offers an efficient alignment solution, its direct application to structured SIDs suffers from three limitations: (i) it penalizes shared hierarchical prefixes, causing gradient conflicts; (ii) it is vulnerable to noisy pseudo-negatives from implicit feedback; and (iii) in multi-label queries with multiple relevant items, it exacerbates a probability "squeezing effect" among valid candidates. To address these issues, we propose RAD-DPO, which introduces token-level gradient detachment to protect prefix structures, similarity-based dynamic reward weighting to mitigate label noise, and a multi-label global contrastive objective integrated with global SFT loss to explicitly expand positive coverage. Extensive offline experiments and online A/B testing on a large-scale e-commerce platform demonstrate significant improvements in ranking quality and training efficiency.

---

## 论文详细总结（自动生成）

这篇论文由京东（JD.com）与北京大学的研究团队合作完成，旨在解决电子商务场景下生成式检索（Generative Retrieval, GR）在应用直接偏好优化（DPO）时遇到的结构性对齐难题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
生成式检索通过自回归解码**语义ID（Semantic IDs, SIDs）**来检索商品。虽然 DPO 是目前主流的偏好对齐方法，但将其直接应用于具有层级结构的 SIDs 时存在三个核心瓶颈：
*   **共享前缀的梯度冲突：** 正负样本往往共享类目路径（前缀）。标准 DPO 在惩罚负样本时会无差别地惩罚这些正确的前缀，导致训练不稳定。
*   **伪负样本的干扰：** 电商隐式反馈（如点击日志）包含大量噪声，误把潜在相关的商品当作负样本惩罚，会扭曲语义表示。
*   **多标签概率挤压：** 一个查询通常对应多个相关商品，传统的成对（Pairwise）比较会挤压多个有效候选商品之间的概率空间。

### 2. 方法论（RAD-DPO 框架）
论文提出了 **RAD-DPO（鲁棒自适应去噪直接偏好优化）**，包含三个关键模块：
*   **Token级梯度分离 (TLGD)：** 识别正负样本 SIDs 的最大公共前缀。在计算负样本损失时，对公共前缀部分的 Token 应用 `Stop-Gradient` 操作，使模型只针对产生差异的后缀部分进行惩罚，保护层级结构的完整性。
*   **基于表示的动态奖励加权 (RDRW)：** 利用模型最后一层隐藏状态计算正负样本的余弦相似度。引入动态统计锚点（分位数），对高相似度的负样本（疑似伪负样本）降低惩罚权重，实现自适应去噪。
*   **多标签全局对比 (MLGC)：** 
    *   **全局 SFT 增强：** 在正样本簇上应用全局 SFT 损失，充分利用多标签信息。
    *   **全局偏好对比：** 采用类似 Log-Sum-Exp 的算子，将最优正样本与整个负样本池进行对比，避免了重复编码的计算浪费，并缓解了概率挤压效应。

### 3. 实验设计
*   **数据集：** 使用京东工业级数据集，包含约 7 亿条交互日志（前 7 天用于 SFT，第 8 天用于测试）。从中采样 3000 万条高质量偏好数据用于 DPO 训练。
*   **基准模型 (Backbone)：** 以 Qwen-1.7B 为基础模型，并扩展测试了 0.6B 到 8B 不同规模的模型。
*   **对比方法：** SFT Baseline、标准 DPO、SimPO、ORPO。
*   **评估指标：** 离线使用 Recall@K、MRR 和幻觉率（Hallucination Rate）；在线使用用户转化率（UCVR）。

### 4. 资源与算力
*   **训练设备：** 使用 NVIDIA H100 GPU 进行模型优化。
*   **推理设备：** 在单张 NVIDIA RTX 4090 GPU 上进行推理测试。
*   **性能表现：** 1.7B 模型在 Beam Width=128 时，预测延迟小于 150ms，满足工业级实时服务需求。
*   *注：论文未明确给出总训练时长，但提到了学习率为 1e-6，Batch Size 为 64。*

### 5. 实验数量与充分性
实验设计非常充分且具有高度的工业参考价值：
*   **多维度对比：** 涵盖了离线指标、在线 A/B 测试、消融实验。
*   **扩展性测试：** 验证了模型在不同参数规模（0.6B, 1.7B, 4B, 8B）下的表现，证明了 RAD-DPO 的可扩展性。
*   **数据规模实验：** 测试了从 10M 到 50M 不同训练数据量对效果的影响。
*   **客观性：** 实验基于真实的大规模电商环境，对比了多种最新的偏好优化变体，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升：** RAD-DPO 在所有离线指标上均优于 SFT 和标准 DPO，且随着模型规模增大，领先优势更加明显。
*   **幻觉抑制：** 相比标准 DPO，RAD-DPO 通过保留 SFT 损失和 Token 级约束，显著降低了生成不存在 SID 的幻觉率。
*   **业务价值：** 在京东搜索系统进行的为期一周的在线 A/B 测试显示，UCVR 提升了 **+0.34%**，这在成熟的工业搜索引擎中是显著的增长。

### 7. 优点
*   **针对性强：** 深入分析了生成式检索中 SID 的结构特性，解决了标准 DPO 在非连续文本生成领域的适配问题。
*   **鲁棒性高：** 动态权重机制有效应对了工业界普遍存在的标签噪声问题。
*   **工程友好：** 引入的全局对比和梯度分离机制在提升效果的同时，兼顾了训练和推理的效率。

### 8. 不足与局限
*   **SID 依赖性：** 该方法高度依赖于预先构建的语义 ID（如通过 RQ-Kmeans 构建），如果底层 SID 质量不佳，该对齐框架的效果可能会受限。
*   **超参数敏感度：** 动态权重函数中的平滑因子 $\lambda$ 和分位数锚点的更新频率可能需要针对不同场景进行精细调优。
*   **应用范围：** 主要针对结构化 ID 的生成，对于纯自由文本的生成式检索对齐效果尚待进一步验证。

（完）
