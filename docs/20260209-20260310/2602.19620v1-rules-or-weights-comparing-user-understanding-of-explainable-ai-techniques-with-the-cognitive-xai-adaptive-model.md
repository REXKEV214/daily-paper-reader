---
title: Rules or Weights? Comparing User Understanding of Explainable AI Techniques with the Cognitive XAI-Adaptive Model
title_zh: 规则还是权重？利用认知 XAI 自适应模型比较用户对可解释人工智能技术的理解
authors: "Louth Bin Rawshan, Zhuoyu Wang, Brian Y Lim"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19620v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 人机交互视角下的可解释AI及用户对XAI技术的理解
tldr: 本研究探讨了规则和权重两种主流可解释AI（XAI）技术对用户理解的影响。针对缺乏认知框架比较其解释性的问题，作者通过用户研究识别了7种推理策略，并提出了基于计算理性的认知模型CoXAM。该模型通过模拟人类在正向和反向决策任务中的推理过程，成功预测了人类决策行为，揭示了规则比权重更难记忆和应用等关键发现，为XAI技术的评估和调试提供了认知基础。
selection_source: fresh_fetch
motivation: 尽管规则和权重是常用的XAI技术，但目前缺乏一个统一的认知框架来比较它们在不同决策任务中的可解释性和用户理解度。
method: 提出CoXAM认知模型，利用计算理性在效用和推理时间之间权衡，模拟用户在处理权重、规则及其混合模式时的推理过程。
result: 实验证明CoXAM比传统机器学习代理模型更贴合人类决策，并发现反向任务比正向任务更难，且决策树规则比线性权重更难记忆和应用。
conclusion: CoXAM为评估不同XAI技术提供了认知科学依据，有助于开发者根据应用场景和任务类型选择更有效的解释方案。
---

## 摘要
规则和权重是用于解释 AI 决策的流行可解释人工智能（XAI）技术。然而，由于缺乏比较其可解释性的认知框架，如何在这两者之间做出选择仍不明确。在一项关于前向和反事实决策任务的启发式用户研究中，我们识别了 7 种解释三种 XAI 模式（权重、规则及其混合模式）的推理策略。为了分析它们的能力，我们提出了 CoXAM，这是一种认知 XAI 自适应模型，具有共享内存表示，用于编码实例属性、线性权重和决策规则。CoXAM 利用计算理性，根据效用和推理时间之间的权衡，分别为前向或反事实决策任务选择推理过程。在一项验证研究中，与基准机器学习代理模型相比，CoXAM 表现出与人类决策更强的一致性。该模型成功复制并解释了几项关键的实证结果，包括：反事实任务本质上比前向任务更难；决策树规则比线性权重更难记忆和应用；以及 XAI 的有效性取决于应用数据的上下文，同时还识别了哪些底层推理策略最为有效。通过 CoXAM，我们提供了一个认知基础，以加速对不同 XAI 技术的调试和基准测试。

## Abstract
Rules and Weights are popular XAI techniques for explaining AI decisions. Yet, it remains unclear how to choose between them, lacking a cognitive framework to compare their interpretability. In an elicitation user study on forward and counterfactual decision tasks, we identified 7 reasoning strategies of interpreting three XAI Schemas - weights, rules, and their hybrid. To analyze their capabilities, we propose CoXAM, a Cognitive XAI-Adaptive Model with shared memory representation to encode instance attributes, linear weights, and decision rules. CoXAM employs computational rationality to choose among reasoning processes based on the trade-off in utility and reasoning time, separately for forward or counterfactual decision tasks. In a validation study, CoXAM demonstrated a stronger alignment with human decision-making compared to baseline machine learning proxy models. The model successfully replicated and explained several key empirical findings, including that counterfactual tasks are inherently harder than forward tasks, decision tree rules are harder to recall and apply than linear weights, and the helpfulness of XAI depends on the application data context, alongside identifying which underlying reasoning strategies were most effective. With CoXAM, we contribute a cognitive basis to accelerate debugging and benchmarking disparate XAI techniques.

---

## 论文详细总结（自动生成）

以下是对论文《Rules or Weights? Comparing User Understanding of Explainable AI Techniques with the Cognitive XAI-Adaptive Model》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管线性权重（Weights）和决策规则（Rules）是可解释 AI（XAI）中最常用的两种技术，但学界对于哪种更有效一直存在争议。现有研究结果往往相互矛盾，且缺乏一个统一的认知框架来解释用户是如何理解和应用这些解释的。
*   **核心目标**：通过认知建模（Cognitive Modeling）深入探讨人类在处理不同 XAI 模式时的心理过程，识别用户在正向预测（Forward Simulation）和反向干预（Counterfactual Simulation）任务中的推理策略，并开发一个能够模拟人类行为的计算模型。

### 2. 方法论
*   **核心思想**：提出 **CoXAM（认知 XAI 自适应模型）**。该模型基于“计算理性”（Computational Rationality）理论，认为人类会在决策效用（准确性）和认知成本（推理时间/努力）之间进行权衡，从而自适应地选择推理策略。
*   **关键技术细节**：
    *   **记忆模型**：基于 ACT-R 架构，将属性值、权重因子和规则节点编码为“记忆块”（Chunks）。记忆块的提取概率取决于其使用频率、新鲜度和匹配度。
    *   **推理策略**：
        *   **正向任务**：近似计算（权重）、特征归因（权重）、属性-阈值遍历（规则）。
        *   **反事实任务**：逆向计算（权重）、逆向特征归因（权重）、节点阈值跨越（规则）、启发式可用性（通用）。
    *   **决策动力学**：使用**漂移扩散模型（DDM）**来模拟决策的随机性和执行时间。
    *   **策略选择器**：利用**强化学习（PPO 算法）**训练一个控制器，根据当前任务状态、认知参数（如噪声、提取阈值）和历史表现，自动选择最优推理策略。

### 3. 实验设计
*   **数据集与场景**：
    *   **蘑菇分类（Mushroom）**：非线性特征较多，用于测试规则型解释的优势。
    *   **葡萄酒质量（Wine Quality）**：线性特征较多，用于测试权重型解释的优势。
*   **实验流程**：
    1.  **启发式研究（N=24）**：通过“出声思维法”识别用户的 7 种推理策略。
    2.  **验证性研究（N=340）**：大规模在线实验，收集 27,600 次决策数据。
*   **对比基准（Benchmarks）**：
    *   **KNN 代理模型**：不考虑 XAI，仅基于实例相似性。
    *   **决策树/线性回归代理模型**：直接使用 XAI 模型作为用户行为的代理。
    *   **全局 SHAP**：用于对比反事实任务中的属性选择。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **Stable Baselines 3** 库训练 PPO 强化学习控制器。
*   **硬件环境**：实验在 **CPU** 上运行（使用了 8 个矢量化环境 `SubprocVecEnv`）。
*   **训练时长**：总计训练了 **400,000 个时间步（timesteps）**。由于模型是基于认知参数而非深度视觉特征，其计算开销相对较小，不需要高性能 GPU 集群。

### 5. 实验数量与充分性
*   **实验规模**：340 名参与者产生了近 3 万条决策记录，样本量充足。
*   **覆盖范围**：涵盖了两种截然不同的数据集（线性 vs 非线性）、三种 XAI 模式（规则、权重、混合）、两种任务类型（正向、反事实）以及两种复杂度水平。
*   **客观性**：通过个体参数拟合（如提取阈值、扩散噪声）来匹配每个用户的行为，并使用贝叶斯信息准则（BIC）和负对数似然（NLL）进行严谨的模型比较，实验设计客观且公平。

### 6. 主要结论与发现
*   **规则 vs. 权重**：规则（决策树）比权重（线性因子）更难记忆。在没有解释显示的情况下，用户对规则的召回和应用准确率显著下降。
*   **任务难度**：反事实任务（寻找改变结果的方法）本质上比正向预测任务更难，用户往往会退而求其次使用低效的“启发式可用性”策略。
*   **数据上下文依赖**：XAI 的有效性高度依赖于数据。权重在处理线性关系明显的葡萄酒数据时表现更好，而规则在处理非线性的蘑菇数据时更具优势。
*   **模型表现**：CoXAM 在预测人类决策准确率和属性选择偏好方面显著优于传统的机器学习代理模型（相关性 $r=0.91$）。

### 7. 优点
*   **认知深度**：不同于以往只关注行为匹配的研究，CoXAM 深入到了心理机制层面（记忆衰减、认知噪声、努力权衡）。
*   **统一框架**：能够同时处理规则和权重两种异构解释，并支持混合模式的模拟。
*   **实用价值**：为 XAI 开发者提供了一个“虚拟用户测试台”，可以在进行昂贵的人工实验前，先通过模型调试和优化解释方案。

### 8. 不足与局限
*   **维度限制**：目前仅在 6 个属性的低维表格数据上进行了验证，尚未测试极高维度或非结构化数据（如图像、文本）。
*   **模态局限**：模型主要处理语义信息，尚未考虑视觉呈现方式（如颜色、布局、图表类型）对认知负荷的具体影响。
*   **生态效度**：实验任务顺序固定（先正向后反事实），可能存在序贯效应，且真实世界中的决策环境可能比受控实验更复杂。

（完）
