---
title: Rigidity in LLM Bandits with Implications for Human-AI Dyads
title_zh: 大语言模型多臂老虎机任务中的僵化性及其对人类-AI 二元组的启示
authors: "Haomiaomiao Wang, Tomás E Ward, Lili Zhang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07717v1"
tags: ["query:llm-bias-hci"]
score: 9.0
evidence: 大模型决策偏见与人机协作
tldr: 本研究探讨了大语言模型（LLM）在决策中的鲁棒偏差。通过将LLM视为双臂老虎机任务的参与者，在多种解码配置下进行了2万次实验。研究发现，LLM在对称奖励下表现出严重的立场偏见，在非对称奖励下则表现出过度僵化的剥削策略。通过计算建模揭示了其低学习率和高逆温度的底层机制。这些发现为理解LLM决策倾向及其对人机协作的影响提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在测试大语言模型在决策任务中是否存在稳健的偏见，并探讨其对人机交互的潜在影响。
method: 将LLM作为双臂老虎机任务的参与者，在不同解码参数下进行大规模实验，并利用层次化Rescorla-Wagner模型进行计算建模。
result: 模型在对称奖励下会放大位置偏见，在非对称奖励下表现出僵化的剥削行为，且这种行为在不同解码设置下均保持一致。
conclusion: LLM决策表现出低学习率和极高逆温度的特征，这种固有的决策僵化性可能在人机协作中产生负面影响。
---

## 摘要
我们测试了大语言模型（LLMs）是否表现出稳健的决策偏差。通过将模型视为双臂老虎机任务的参与者，我们在四种解码配置下，每个条件下运行了 20,000 次试验。在对称奖励下，模型将位置顺序放大为固执的单臂策略。在非对称奖励下，它们表现出僵化的利用行为，但表现不如最优策略（oracle），且很少重新检查。观察到的模式在调节 temperature 和 top-p 时保持一致（top-k 保持为供应商默认值），这表明定性行为对于从业者通常可用的这两个解码参数具有稳健性。关键在于，超越描述性指标，通过计算建模，分层 Rescorla-Wagner-softmax 拟合揭示了潜在策略：低学习率和极高的逆温度，这两者共同解释了噪声到偏差的放大以及僵化的利用行为。这些结果将极简老虎机任务定位为探测 LLM 决策倾向的一种易于处理的手段，并为这些偏差如何塑造人类-AI 交互提供了假设依据。

## Abstract
We test whether LLMs show robust decision biases. Treating models as participants in two-arm bandits, we ran 20000 trials per condition across four decoding configurations. Under symmetric rewards, models amplified positional order into stubborn one-arm policies. Under asymmetric rewards, they exploited rigidly yet underperformed an oracle and rarely re-checked. The observed patterns were consistent across manipulations of temperature and top-p, with top-k held at the provider default, indicating that the qualitative behaviours are robust to the two decoding knobs typically available to practitioners. Crucially, moving beyond descriptive metrics to computational modelling, a hierarchical Rescorla-Wagner-softmax fit revealed the underlying strategies: low learning rates and very high inverse temperatures, which together explain both noise-to-bias amplification and rigid exploitation. These results position minimal bandits as a tractable probe of LLM decision tendencies and motivate hypotheses about how such biases could shape human-AI interaction.

---

## 论文详细总结（自动生成）

以下是对论文《Rigidity in LLM Bandits with Implications for Human-AI Dyads》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）深度嵌入交互式决策场景（如AI助手、投资建议），其输出会潜移默化地影响人类判断。然而，现有的基准测试多关注准确性，忽略了模型在动态决策中的“行为倾向”。
*   **核心问题**：LLM 在面临不确定性选择时，是否表现出类似于人类的认知偏见？这种偏见是否具有鲁棒性（即不随解码参数改变）？
*   **研究背景**：借鉴认知科学中的“多臂老虎机”（Multi-armed Bandits）范式，将 LLM 视为实验参与者，探测其在探索（Exploration）与剥削（Exploitation）之间的平衡能力。

### 2. 方法论
*   **核心思想**：通过极简的二选一任务（双臂老虎机），观察模型在获得奖励反馈后的策略调整过程，并利用计算精神病学中的数学模型对行为进行参数化解释。
*   **关键技术细节**：
    *   **任务设置**：模型需在“星球 X”和“星球 Y”之间选择，目标是获得金币。
    *   **计算建模**：采用**分层 Rescorla-Wagner (RW) 模型**结合 **Softmax 策略**。
        *   **价值更新公式**：$V_{t+1}(a) = V_t(a) + A_i [r_t - V_t(a)]$。其中 $A_i$ 是学习率，决定了模型对新反馈的敏感度。
        *   **选择概率公式**：使用 Softmax 函数计算，引入**逆温度参数 $\tau$**。$\tau$ 越高，选择越趋于确定性（贪婪）；$\tau$ 越低，选择越随机。
    *   **解码控制**：系统性地操纵 Temperature（0.0 到 2.0）和 Top-p（0.5 到 1.0），定义了从“严格”到“探索”的四种解码策略。

### 3. 实验设计
*   **实验场景**：
    1.  **对称奖励结构**：两臂奖励概率均为 0.25。理想状态应为 50/50 平分，用于测试位置偏见和随机噪声的放大效应。
    2.  **非对称奖励结构**：一臂为 0.75，另一臂为 0.25。用于测试模型在发现优选方案后的灵活性。
*   **对比对象（Benchmark）**：
    *   **模型**：DeepSeek、GPT-4.1、Gemini-2.5。
    *   **基准**：人类实验数据（来自既往文献）、Oracle（理论最优策略）。
*   **评估指标**：总奖励、目标臂选择率、失利后切换率（Loss-Shift）、获胜后切换率（Win-Shift）、僵化指数（Rigidity Index）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现方式**：实验主要通过各模型的 API（如 OpenAI、Google、DeepSeek 的 API）进行推理调用。由于是基于 Prompt 的行为测试而非模型训练，其主要开销在于 API 调用成本和数据处理。

### 5. 实验数量与充分性
*   **实验规模**：每个模型在每种条件下运行 200 次独立模拟，每次模拟包含 100 轮试验（Trials）。总计分析了数万次决策过程。
*   **充分性与客观性**：
    *   实验覆盖了当前主流的三大模型家族。
    *   采用了分层贝叶斯推断（Stan 语言实现）进行参数估计，提供了 95% 置信区间，确保了统计上的严谨性。
    *   通过操纵解码参数（Temperature/Top-p）进行了类似于“消融实验”的鲁棒性测试，证明了结论并非由特定采样设置引起。

### 6. 主要结论与发现
*   **决策僵化性（Rigidity）**：LLM 表现出极高的“逆温度”和较低的“学习率”。这意味着模型一旦形成初步偏好，就会极其顽固地坚持，极少进行重新检查（Re-check）。
*   **偏见放大**：在对称环境下，模型会将微小的位置偏见（如倾向于选第一个选项）放大为长期的“单臂策略”，表现出严重的固执（Stubbornness）。
*   **机会盲区**：模型在非对称环境下虽然能找到较好的一臂，但其剥削过于僵化，导致其整体收益低于最优策略，且无法根据环境变化调整探索预算。
*   **解码参数的局限性**：提高 Temperature 虽然增加了输出的随机性，但往往导致格式错误（Invalid responses）增加，而非产生真正的“认知探索”。

### 7. 优点
*   **跨学科方法**：成功将认知心理学的计算建模引入 LLM 评估，使“偏见”这一模糊概念变得可量化、可解释。
*   **揭示底层机制**：不仅描述了模型“做了什么”，还通过参数拟合解释了“为什么”（即低学习率与高确定性的结合）。
*   **现实指导意义**：警示了在人机协作中，LLM 可能会给出过于确定但带有潜在偏见的建议，导致人类产生“过度信任”。

### 8. 不足与局限
*   **任务复杂度较低**：双臂老虎机是静态且简单的，未能涵盖更复杂的上下文（Contextual Bandits）或环境动态变化的情况。
*   **缺乏真实交互测试**：研究主要基于模拟，尚未直接测量这些 LLM 偏见在真实的人类-AI 协作任务中如何具体导致人类决策失误。
*   **提示词敏感性**：虽然测试了位置偏见，但未深入探讨不同 Prompt 框架（如角色扮演、思维链）是否能显著缓解这种僵化性。

（完）
