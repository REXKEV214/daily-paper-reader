---
title: "RBCorr: Response Bias Correction in Language Models"
title_zh: RBCorr：语言模型中的响应偏差修正
authors: "Om Bhatt, Anna A. Ivanova"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12445v1"
tags: ["query:llm-bias-hci"]
score: 9.0
evidence: 减轻语言模型中的响应偏见和选项偏好偏见
tldr: 针对语言模型在固定选项问题中普遍存在的响应偏差（如对特定选项的偏好），本文提出了 RBCorr 校正策略。通过在 12 个开源模型上进行实验，涵盖是非题、蕴含题和多选题，研究发现该方法能有效消除偏差并显著提升模型性能。RBCorr 尤其能帮助小型模型在基准测试中展现真实水平，是一种低成本且易于部署的评估优化方案。
selection_source: fresh_fetch
motivation: 语言模型在固定选项问题中普遍存在响应偏差，这限制了其性能的发挥并导致评估结果不够准确。
method: 提出了一种名为 RBCorr 的简单响应偏差校正策略，通过调整 LogProbs 来消除模型对特定选项的固有偏好。
result: 在 12 个开源模型上的测试证明，RBCorr 能有效消除偏差并显著提升模型在是非、蕴含和多选题任务中的表现。
conclusion: RBCorr 是一种易于使用的低成本方法，能确保语言模型在闭口响应基准测试中的表现与其真实能力更加一致。
---

## 摘要
众所周知，语言模型（LMs）容易产生响应偏差，这在固定回答问题中表现为选项偏好偏差。因此，开发低成本且有效的响应偏差修正方法，以提高语言模型性能并实现对模型能力的更准确评估，显得至关重要。在本文中，我们提出了一种简单的响应偏差修正策略（$\texttt{RBCorr}$），并在 12 个开源权重语言模型上使用“是-否”问答、蕴含和多项选择题进行了测试。我们展示了在修正前，响应偏差在语言模型中普遍存在，而 $\texttt{RBCorr}$ 能有效消除偏差并提升模型性能。我们还探讨了偏差行为在不同模型、数据集和提示格式之间的泛化性，结果表明基于 LogProbs 的修正高度依赖于这三个方面。总的来说，$\texttt{RBCorr}$ 是一种易于使用的方法，可以提升较小规模语言模型的性能，并确保语言模型在闭口回答基准测试中的表现与其真实能力更加一致。

## Abstract
Language models (LMs) are known to be prone to response biases, which present as option preference biases in fixed-response questions. It is therefore imperative to develop low-cost and effective response bias correction methods to improve LM performance and enable more accurate evaluations of model abilities. Here, we propose a simple response bias correction strategy ($\texttt{RBCorr}$) and test it on 12 open-weight language models using yes-no, entailment, and multiple choice questions. We show that response bias is prevalent in LMs pre-correction and that $\texttt{RBCorr}$ effectively eliminates bias and boosts model performance. We also explore the generalizability of bias behavior across models, datasets, and prompt formats, showing that LogProbs-based correction is highly dependent on all three of these aspects. Overall, $\texttt{RBCorr}$ is an easy-to-use method that can boost the performance of smaller LMs and ensure that LM performance on closed-response benchmarks aligns more closely with their true capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《RBCorr: Response Bias Correction in Language Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：语言模型（LMs）在处理固定选项问题（如多选题、是非题）时，往往表现出强烈的“响应偏差”（Response Bias）。例如，模型可能天生更倾向于选择“No”或选项“A”，这种偏差并非基于逻辑推理，而是由于预训练数据分布或提示词格式引起的。
*   **核心问题**：这种偏差掩盖了模型的真实推理能力，导致评估结果不准确。论文旨在开发一种低成本、易于实现且通用的校正方法，以消除这种标签偏好并恢复模型的潜在性能。

### 2. 方法论：RBCorr 核心思想与技术细节
*   **核心思想**：基于“均值归一化”的校准策略。如果模型是无偏差的，在类别平衡的数据集上，各选项的平均对数概率（LogProbs）应当相等。RBCorr 通过计算并减去这种经验偏差值来修正输出。
*   **技术流程**：
    1.  **提取 LogProbs**：从模型最后一层提取目标响应 Token（如“Yes/No”或“A/B/C/D”）在 Softmax 之前的对数概率。
    2.  **构建校准集**：从数据集中随机抽取一个小型、类别平衡的子集（如 100 个样本）。
    3.  **计算修正项**：计算校准集中每个选项 Token 的平均 LogProbs。
    4.  **应用校正**：对于测试集中的每个问题，从其原始 LogProbs 中减去对应的平均修正项。
    5.  **重新预测**：选择修正后 LogProbs 最高的 Token 作为最终答案。
*   **优势**：该方法不需要额外的推理开销（如多次采样或处理占位符文本），仅需对已有的 LogProbs 进行简单的减法运算。

### 3. 实验设计
*   **数据集与场景**：
    *   **2选1（是非题）**：ARITH（算术）、bAbI（阅读理解）、COMPS（属性继承）、EWoK（上下文知识）。
    *   **3选1（自然语言蕴含）**：SNLI 和 MultiNLI（判断蕴含、中立或矛盾）。
    *   **4选1（多选题）**：MMLU（涵盖 STEM、社会科学、人文等 57 个领域）。
*   **对比方法**：
    *   **Contextual Calibration (CC)**：使用“N/A”等无内容输入来估计偏差。
    *   **Batch Calibration (BC)**：通过批次数据的均值进行动态校准。
    *   **PriDe**：一种基于先验估计的更复杂的去偏方法。
*   **测试模型**：涵盖 Falcon3、Gemma3、Llama3.1 三个家族的 12 个开源模型，参数量从 3B 到 70B，包含基础版和指令微调版。

### 4. 资源与算力
*   **算力说明**：论文提到使用了佐治亚理工学院的 PACE 计算资源。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。但由于 RBCorr 属于后处理方法，且校准集仅需 100 条数据，其计算需求极低，普通消费级显卡即可在极短时间内完成校正计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 12 个模型、10 个数据集、3 种提示词复杂度（Zeroshot, Instruction-only, Fewshot）。
    *   为了验证稳定性，每个校正实验都进行了 **100 次随机采样迭代**，并报告了中位数和四分位距。
*   **充分性与客观性**：实验设计非常全面，不仅关注准确率（Accuracy），还引入了全变分距离（TVD）来量化偏差的减少程度。此外，论文还专门设计了“迁移性实验”，验证校正项是否能在不同模型、数据集或提示词之间通用，实验逻辑严密且客观。

### 6. 主要结论与发现
*   **偏差普遍存在**：所有测试模型在未校正前均表现出明显的标签偏好，小型模型和基础模型（Base）的偏差尤为严重。
*   **性能显著提升**：RBCorr 在几乎所有场景下都能降低偏差并提升准确率。在某些任务（如 bAbI）中，准确率提升甚至接近 29%。
*   **校准集规模**：仅需 100 条平衡数据即可达到稳定的校正效果，增加数据量带来的边际收益递减。
*   **高度上下文相关**：偏差模式对模型、数据集和提示词格式极度敏感。实验证明，在一个配置下计算的校正项很难成功迁移到另一个配置中（成功率仅约 10%-20%）。

### 7. 优点
*   **低成本高效**：无需重新训练模型，无需复杂的提示词工程，计算开销几乎为零。
*   **诊断价值**：通过观察校正前后的性能变化，研究者可以区分模型是“真的不懂知识”还是“仅仅因为标签偏好而选错”。
*   **普适性强**：适用于多种闭口响应任务（2/3/4 选项）。

### 8. 不足与局限
*   **白盒依赖**：该方法要求获取模型的 LogProbs，因此不适用于仅提供文本输出的闭源 API 模型（如 GPT-4 的某些版本，除非提供 logprobs 参数）。
*   **类别平衡假设**：校正过程依赖于一个类别平衡的校准集，在某些极端不平衡的现实场景中，获取此类校准数据可能存在挑战。
*   **模型架构限制**：实验主要集中在 70B 以下的 Transformer 模型，尚未验证其在超大规模模型（如 400B+）或非 Transformer 架构上的表现。

（完）
