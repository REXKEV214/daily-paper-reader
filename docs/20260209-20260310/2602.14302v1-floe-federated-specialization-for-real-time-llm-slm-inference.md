---
title: "Floe: Federated Specialization for Real-Time LLM-SLM Inference"
title_zh: Floe：面向实时 LLM-SLM 推理的联邦专业化框架
authors: "Chunlin Tian, Kahou Tam, Yebo Wu, Shuaihang Zhong, Li Li, Nicholas D. Lane, Chengzhong Xu"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.14302v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 联邦专门化与设备端个性化
tldr: 针对大语言模型在实时部署中面临的计算开销大和隐私泄露问题，本文提出Floe框架。该框架结合了云端黑盒LLM与边缘端轻量级小模型（SLM），通过联邦学习实现低延迟、隐私保护的推理。Floe利用异构感知LoRA策略和逻辑值融合机制，在保护用户数据的同时提升了边缘设备的个性化性能，显著降低了推理延迟，为边缘智能提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在实时应用中因高计算需求和隐私安全问题而难以在边缘端部署的难题。
method: 提出一种混合联邦学习框架，通过异构感知LoRA适配和逻辑值级别融合技术，协同云端LLM与边缘SLM进行推理。
result: 实验证明该方法在增强用户隐私和个性化需求的同时，显著提升了模型性能并大幅降低了边缘推理延迟。
conclusion: Floe为资源受限环境下的实时、隐私敏感型大模型推理提供了一个高效且兼顾性能与延迟的解决方案。
---

## 摘要
由于巨大的计算需求和隐私担忧，在实时系统中部署大语言模型（LLMs）仍然具有挑战性。我们提出了 Floe，这是一个专为延迟敏感、资源受限环境设计的混合联邦学习框架。Floe 将云端黑盒 LLM 与边缘设备上的轻量级小语言模型（SLMs）相结合，以实现低延迟、保护隐私的推理。个人数据和微调保留在设备端，而云端 LLM 在不暴露专有权重的情况下贡献通用知识。一种异构感知的 LoRA 适配策略实现了跨多样化硬件的高效边缘部署，而 Logit 级融合机制实现了边缘模型与云端模型之间的实时协同。大量实验表明，Floe 增强了用户隐私和个性化。此外，与基准方法相比，它在实时约束下显著提高了模型性能并降低了边缘设备上的推理延迟。

## Abstract
Deploying large language models (LLMs) in real-time systems remains challenging due to their substantial computational demands and privacy concerns. We propose Floe, a hybrid federated learning framework designed for latency-sensitive, resource-constrained environments. Floe combines a cloud-based black-box LLM with lightweight small language models (SLMs) on edge devices to enable low-latency, privacy-preserving inference. Personal data and fine-tuning remain on-device, while the cloud LLM contributes general knowledge without exposing proprietary weights. A heterogeneity-aware LoRA adaptation strategy enables efficient edge deployment across diverse hardware, and a logit-level fusion mechanism enables real-time coordination between edge and cloud models. Extensive experiments demonstrate that Floe enhances user privacy and personalization. Moreover, it significantly improves model performance and reduces inference latency on edge devices under real-time constraints compared with baseline approaches.

---

## 论文详细总结（自动生成）

以下是对论文《Floe: Federated Specialization for Real-Time LLM–SLM Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对在实时边缘系统中部署大语言模型（LLM）面临的三大挑战：
*   **隐私与版权冲突**：SOTA模型（如GPT-4）多为闭源黑盒，无法获取权重进行本地微调；同时，用户数据上传云端存在隐私风险。
*   **硬件资源受限**：边缘设备（如Jetson Nano）内存极小，无法承载百亿级参数的LLM。
*   **环境异构性**：边缘设备在计算能力、内存容量和网络带宽上存在巨大差异，传统的统一联邦学习协议难以适配。

**Floe框架的含义**：提出一种“云端LLM（通用知识）+ 边缘SLM（个性化/隐私）”的协作模式，通过联邦学习实现小模型的专业化，并利用Logit级融合技术在不暴露云端模型权重的前提下提升推理性能。

### 2. 方法论：核心思想与关键技术
Floe分为**联邦微调**和**协同推理**两个阶段：

#### A. 联邦微调阶段（Fine-tuning Phase）
*   **异构感知LoRA适配器（Heterogeneity-aware LoRA）**：根据每个边缘设备的实时内存预算和延迟约束，动态调整LoRA的秩（Rank $r$）。内存大、算力强的设备分配高Rank，反之分配低Rank，确保所有设备都能参与训练。
*   **任务特定聚合器（Task-specific Aggregator）**：服务器根据LoRA模块的嵌入相似度进行聚类（K-means），将不同领域的知识（如医疗、代码）聚合为多个“专家模块”，缓解数据非独立同分布（Non-IID）带来的负面影响。

#### B. 协同推理阶段（Inference Phase）
*   **隐私检测器（Privacy Detector）**：在设备端预先判断输入是否包含敏感信息。敏感请求强制留在本地SLM处理，非敏感请求则请求云端协作。
*   **参数无关的MoE路由器（Parameter-free MoE Router）**：利用预计算的任务嵌入，根据当前Prompt动态加权组合本地的LoRA专家模块，无需额外训练路由网络。
*   **Logit级对齐融合（Logit-level Alignment）**：
    *   公式：$P_{out} = w \cdot P_{SLM} + (1-w) \cdot P_{LLM}$
    *   通过一个轻量级对齐模块动态计算权重 $w$，融合云端LLM的通用逻辑值（Logits）和本地SLM的个性化逻辑值。
*   **实时回退策略（Fallback Strategy）**：若云端响应超时（如网络波动），系统自动切换为纯本地SLM推理，确保实时性。

### 3. 实验设计
*   **数据集/场景**：
    *   **CoGenesis**：真实用户交互数据，用于隐私敏感任务。
    *   **Flanv2**：包含10个任务簇的多任务基准。
    *   **BBH (Big-Bench Hard)**：包含23个挑战性推理任务，作为主要性能Benchmark。
    *   **WikiText2**：用于评估系统效率（延迟、能耗）。
*   **对比方法**：
    *   基础模型：SLM-base (Gemma-2B), LLM-base (Gemma-7B)。
    *   联邦学习基准：FedAvg, FedProto。
    *   进阶方案：LLM-FedAvg (代理微调), LLM-FedMoE (带训练路由的MoE)。

### 4. 资源与算力
*   **服务器端**：1台 NVIDIA A40 GPU（模拟云端黑盒LLM）。
*   **边缘端**：15台异构Jetson设备，包括10台 **Jetson Nano**（极低算力）和5台 **Jetson Orin NX**（中等算力）。
*   **测量工具**：使用 Codecarbon 追踪碳排放/能耗，Monsoon Power Monitor 测量物理功耗。
*   **训练细节**：本地训练3个Epoch，学习率1e-5，使用半精度（FP16）以节省内存。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **性能对比**：在BBH的26个子任务上进行了全面测试。
*   **效率分析**：对比了模型压缩（20%、50%剪枝）与Floe在内存、参数量、MACs、延迟和能耗上的差异。
*   **消融实验**：分别验证了异构感知模块（-M）、任务聚合方案（-P）和MoE路由器（-R）的有效性。
*   **鲁棒性测试**：模拟了不同网络延迟（RTT 0-500ms）下的推理表现。
*   **模型泛化性**：测试了 Llama-3 系列以及闭源的 Gemini-1.5-Flash API。
**评价**：实验设计非常充分，涵盖了从算法精度到系统底层开销的各个方面，且使用了真实的异构硬件环境，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能超越**：Floe在BBH基准上比LLM-base提升了**3.6%**，比纯SLM方案提升了**14.2%**。
*   **资源高效**：相比于直接在边缘运行压缩后的LLM，Floe减少了**99%**的通信延迟，并显著降低了内存占用和能耗。
*   **异构适配成功**：通过动态Rank调整，Floe成功让原本会发生内存溢出（OOM）的低端设备参与了联邦学习，提升了全局模型的泛化能力。
*   **推理稳健**：Logit融合机制在网络环境差时能通过回退策略保持120ms/token左右的稳定输出。

### 7. 优点（亮点）
*   **黑盒兼容性**：无需访问云端LLM的权重或中间层，仅通过API返回的Logits即可实现协同，极具商业落地可行性。
*   **系统级优化**：不仅关注算法，还深入解决了边缘端的“内存墙”和“能耗瓶颈”。
*   **动态性**：MoE路由器和Logit融合权重都是动态生成的，能灵活应对混合任务输入。

### 8. 不足与局限
*   **隐私检测风险**：隐私检测器基于启发式规则和语义相似度，召回率为91.7%，意味着仍有约8%的敏感信息可能被误判并上传云端。
*   **Logit依赖**：方案假设云端API会暴露Logits。目前部分商业API（如GPT-4）虽提供Logit Bias，但并不总是返回完整的Logit分布，这限制了框架的通用性。
*   **分词器一致性**：Logit融合要求云端和边缘模型使用兼容的词表（Tokenizer），如果两者词表差异巨大，映射过程会产生额外开销或精度损失。
*   **Rank权衡**：为了照顾低端设备分配的低Rank LoRA可能会限制模型在复杂任务上的学习上限。

（完）
