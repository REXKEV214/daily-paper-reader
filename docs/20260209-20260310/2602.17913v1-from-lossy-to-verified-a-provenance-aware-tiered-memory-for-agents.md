---
title: "From Lossy to Verified: A Provenance-Aware Tiered Memory for Agents"
title_zh: 从有损到验证：一种面向智能体的溯源感知分层存储
authors: "Qiming Zhu, Shunian Chen, Rui Yu, Zhehao Wu, Benyou Wang"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.17913v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长程智能体的溯源感知分层记忆
tldr: 针对长周期智能体在压缩历史记录时面临的信息丢失和不可验证问题，本文提出 TierMem 框架。该框架采用分层存储结构，结合快速摘要索引和不可变原始日志库。通过推理时的证据分配机制，系统优先检索摘要，仅在必要时升级到原始日志，从而在保持高准确率的同时显著降低了计算成本和延迟。
selection_source: fresh_fetch
motivation: 传统的摘要压缩方法存在“先写后查”的局限，容易丢失关键细节且难以追溯证据，而直接处理原始日志成本过高。
method: 提出 TierMem 框架，利用两层存储架构和运行时充分性路由，按需从摘要索引升级到原始日志检索，并将验证后的结果写回摘要。
result: "在 LoCoMo 基准测试中，TierMem 在保持与原始日志接近的准确率（0.851 vs 0.873）的同时，减少了 54.1% 的输入 Token 和 60.7% 的延迟。"
conclusion: TierMem 通过分层存储和按需升级机制，有效平衡了智能体记忆检索的准确性与效率，实现了可验证的低成本推理。
---

## 摘要
长程智能体通常将交互历史压缩为写入时摘要。这产生了一个根本性的“先写后查”障碍：压缩决策是在系统了解未来查询的关键点之前做出的。因此，摘要可能导致无法验证的遗漏——决定性的约束（例如过敏史）可能会被丢弃，导致智能体无法用可追溯的证据来证明其回答的合理性。保留原始日志可以恢复权威的事实来源，但默认基于原始日志进行检索成本高昂：许多查询可以通过摘要回答，而原始日志检索仍需处理长得多的上下文，从而增加了 Token 消耗和延迟。我们提出了 TierMem，这是一个溯源链接框架，将检索视为推理时的证据分配问题。TierMem 使用双层存储层级，以最廉价的充分证据进行回答：它默认查询快速摘要索引，并且仅在摘要证据不足时，由运行时充分性路由器升级到不可变的原始日志库。随后，TierMem 将验证后的结果作为新的摘要单元写回，并与其原始来源链接。在 LoCoMo 数据集上，TierMem 实现了 0.851 的准确率（对比仅使用原始日志的 0.873），同时减少了 54.1% 的输入 Token 和 60.7% 的延迟。

## Abstract
Long-horizon agents often compress interaction histories into write-time summaries. This creates a fundamental write-before-query barrier: compression decisions are made before the system knows what a future query will hinge on. As a result, summaries can cause unverifiable omissions -- decisive constraints (e.g., allergies) may be dropped, leaving the agent unable to justify an answer with traceable evidence. Retaining raw logs restores an authoritative source of truth, but grounding on raw logs by default is expensive: many queries are answerable from summaries, yet raw grounding still requires processing far longer contexts, inflating token consumption and latency.   We propose TierMem, a provenance-linked framework that casts retrieval as an inference-time evidence allocation problem. TierMem uses a two-tier memory hierarchy to answer with the cheapest sufficient evidence: it queries a fast summary index by default, and a runtime sufficiency router Escalates to an immutable raw-log store only when summary evidence is insufficient. TierMem then writes back verified findings as new summary units linked to their raw sources. On LoCoMo, TierMem achieves 0.851 accuracy (vs.0.873 raw-only) while reducing input tokens by 54.1\% and latency by 60.7%.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TierMem** 的分层存储框架，旨在解决长程（Long-horizon）智能体在处理大规模交互历史时面临的效率与准确性权衡问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心障碍：** 论文指出了当前智能体记忆系统的“先写后查”（Write-before-query）障碍。为了节省空间，系统通常在写入时将历史记录压缩为摘要，但此时系统并不知道未来查询的具体需求，导致关键细节（如过敏史、特定数值）被永久丢失，造成“不可验证的遗漏”。
*   **两难困境：** 
    *   **摘要法：** 速度快、成本低，但容易丢失细节，且无法追溯证据。
    *   **原始日志法：** 准确且可审计，但在长上下文中检索会导致极高的 Token 消耗和延迟。
*   **研究目标：** 实现“以最廉价的充分证据回答问题”，即在保证准确性和可追溯性的前提下，最大限度降低推理成本。

### 2. 方法论：核心思想与技术细节
TierMem 将检索建模为一个**推理时的证据分配问题**，核心组件包括：
*   **分层存储架构：**
    *   **Tier-1（摘要索引）：** 存储压缩后的语义单元，每个单元都包含指向原始出处的“溯源指针”（Provenance Pointers）。
    *   **Tier-2（原始日志库）：** 以固定大小分页存储的不可变原始对话记录，作为权威事实来源。
*   **运行时充分性路由器（Router $\pi_\theta$）：**
    *   当用户提出查询时，系统首先检索 Tier-1。
    *   路由器判断当前摘要是否足以回答问题。若足够，则直接回答（**ANSWER**）；若不足（如缺少精确实体或存在歧义），则升级到 Tier-2（**ESCALATE**）。
*   **溯源引导的升级机制：** 升级时，系统利用溯源指针直接定位相关的原始日志页，进行深度检索和验证，避免了全量扫描。
*   **验证后写回（Verified Write-back）：** 将从原始日志中提取并验证后的新发现写回 Tier-1，实现记忆的在线整合，减少未来类似查询的升级需求。
*   **路由器优化：** 采用两阶段训练：
    1.  **SFT（监督微调）：** 从 GPT-5 教师模型中蒸馏推理逻辑。
    2.  **GRPO（群体相对策略优化）：** 通过强化学习在准确率、Token 成本和“误报”惩罚之间取得平衡。

### 3. 实验设计
*   **数据集/场景：** 
    *   **LoCoMo：** 长程对话记忆基准测试。
    *   **LongMemEval：** 评估聊天助手长程交互记忆的基准。
*   **对比方法（Baselines）：**
    *   **摘要中心化方法：** Mem0, LightMem, O-mem, MemOS。
    *   **原始/控制器中心化方法：** MemR3, GAM。
    *   **变体对比：** 仅摘要模式、仅原始数据模式、基于不同路由器的 TierMem。
*   **评估指标：** 准确率（LLM-Judge）、不可验证遗漏率（UOR）、输入/输出 Token 数、延迟（Latency）。

### 4. 资源与算力
*   **模型骨干：** 实验主要使用 **GPT-4.1-mini** 作为智能体和路由器的基础模型。
*   **训练细节：** 路由器训练使用了 LoRA 技术和 DeepSpeed ZeRO-2 优化。
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长，但详细记录了 SFT 和 GRPO 的超参数（如 Batch Size 为 8-24，学习率为 5e-6 至 1e-4 等）。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流长程记忆基准上进行了全面测试，涵盖了数千个查询实例。
*   **消融实验：** 
    *   验证了路由器不同训练阶段（Zero-shot vs SFT vs GRPO）的效果。
    *   验证了“溯源指针”对检索质量的提升。
    *   通过多轮重放（Epoch-wise replay）验证了“写回机制”对成本摊薄的有效性。
*   **充分性评价：** 实验设计较为客观、公平，通过 UOR 指标量化了压缩损失，并对比了多种 SOTA 方法，论证了分层存储的优越性。

### 6. 主要结论与发现
*   **性能平衡：** TierMem 在 LoCoMo 上达到了 0.851 的准确率，非常接近全量原始日志的 0.873，但**输入 Token 减少了 54.1%，延迟降低了 60.7%**。
*   **路由有效性：** 轻量级路由器能有效识别摘要中的信息缺失，将约 39% 的困难查询升级到原始日志。
*   **成本摊薄：** 随着验证信息的写回，系统在后续处理类似问题时会更多地命中 Tier-1，进一步降低长期运行成本。

### 7. 优点
*   **架构创新：** 提出了“溯源感知”的分层记忆，解决了摘要不可信和原始数据不可用的矛盾。
*   **动态分配：** 检索粒度是查询驱动的，而非静态固定的，提高了系统的灵活性。
*   **可审计性：** 所有的摘要都链接到原始日志，确保了智能体回答的每一个事实都有据可查。

### 8. 不足与局限
*   **路由器误差：** 仍存在“虚假缓存命中”（False Cache Hits），即路由器错误地认为摘要足够，导致回答错误。
*   **依赖基础模型：** 路由器的判断能力高度依赖于其背后的 LLM 性能，在极小规模模型上的表现尚未验证。
*   **复杂性增加：** 相比单一存储，分层架构和写回逻辑增加了系统实现的复杂度和维护成本。
*   **应用限制：** 主要针对文本对话，对于多模态或非结构化程度更高的智能体记忆场景，其表现有待进一步研究。

（完）
