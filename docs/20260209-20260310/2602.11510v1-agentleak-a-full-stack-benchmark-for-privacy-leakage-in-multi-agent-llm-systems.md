---
title: "AgentLeak: A Full-Stack Benchmark for Privacy Leakage in Multi-Agent LLM Systems"
title_zh: AgentLeak：多智能体大语言模型系统隐私泄露的全栈基准测试
authors: "Faouzi El Yagoubi, Ranwa Al Mallah, Godwin Badu-Marfo"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.11510v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 多智能体系统中通过共享记忆和消息产生的隐私泄露
tldr: "AgentLeak是首个针对多智能体大模型系统内部隐私泄露的全栈基准测试。研究发现，虽然多智能体配置能降低外部输出的泄露率，但内部通道（如智能体间通信）存在严重泄露风险，导致系统总暴露率高达68.9%。该基准涵盖医疗、金融等四大领域，通过32类攻击分类和三层检测流水线，揭示了仅审计输出端会忽略约41.7%的隐私违规行为，强调了内部通道防护的紧迫性。"
selection_source: fresh_fetch
motivation: 现有隐私基准仅关注模型最终输出，忽略了多智能体系统中智能体间通信、共享内存等内部通道可能导致的敏感数据泄露。
method: 开发了包含1000个跨领域场景、32类攻击分类及三层检测流水线的AgentLeak基准，对多种主流大模型在多智能体环境下的全栈隐私表现进行评估。
result: "实验表明内部通道的泄露率（68.8%）远高于外部输出（27.2%），导致传统审计方法漏掉近41.7%的隐私违规，其中Claude 3.5 Sonnet表现出最佳的安全性。"
conclusion: 多智能体系统的主要隐私漏洞在于内部通信，必须在协调框架中引入针对内部通道的隐私保护和访问控制机制。
---

## 摘要
多智能体大语言模型（LLM）系统带来了现有基准测试无法衡量的隐私风险。当智能体协作完成任务时，敏感数据会通过智能体间消息、共享内存和工具参数传递，而仅针对输出的审计从未检查过这些路径。我们推出了 AgentLeak，据我们所知，这是首个涵盖内部渠道的隐私泄露全栈基准测试，涵盖了医疗、金融、法律和企业领域的 1,000 个场景，并配有 32 类攻击分类法和三层检测流水线。在 4,979 条轨迹上对 GPT-4o、GPT-4o-mini、Claude 3.5 Sonnet、Mistral Large 和 Llama 3.3 70B 进行测试后发现，多智能体配置虽然降低了单渠道输出泄露（C1：27.2%，而单智能体为 43.2%），但引入了未受监控的内部渠道，使系统总暴露率提高到 68.9%（跨 C1、C2、C5 的 OR 聚合）。内部渠道占据了这一差距的大部分：智能体间消息（C2）的泄露率为 68.8%，而 C1（输出渠道）仅为 27.2%。这意味着仅针对输出的审计漏掉了 41.7% 的违规行为。在设计中强调安全对齐的 Claude 3.5 Sonnet 在外部（3.3%）和内部（28.1%）渠道上均实现了最低的泄露率，这表明模型级的安全训练可能会迁移到内部渠道保护。在所有五种模型和四个领域中，C2 > C1 的模式始终成立，证实了智能体间通信是主要的漏洞。这些发现强调了开发集成内部渠道隐私保护并在智能体间通信中实施隐私控制的协作框架的必要性。

## Abstract
Multi-agent Large Language Model (LLM) systems create privacy risks that current benchmarks cannot measure. When agents coordinate on tasks, sensitive data passes through inter-agent messages, shared memory, and tool arguments; pathways that output-only audits never inspect. We introduce AgentLeak, to the best of our knowledge the first full-stack benchmark for privacy leakage covering internal channels, spanning 1,000 scenarios across healthcare, finance, legal, and corporate domains, paired with a 32-class attack taxonomy and three-tier detection pipeline. Testing GPT-4o, GPT-4o-mini, Claude 3.5 Sonnet, Mistral Large, and Llama 3.3 70B across 4,979 traces reveals that multi-agent configurations reduce per-channel output leakage (C1: 27.2% vs 43.2% in single-agent) but introduce unmonitored internal channels that raise total system exposure to 68.9% (OR-aggregated across C1, C2, C5). Internal channels account for most of this gap: inter-agent messages (C2) leak at 68.8%, compared to 27.2% on C1 (output channel). This means that output-only audits miss 41.7% of violations. Claude 3.5 Sonnet, which emphasizes safety alignment in its design, achieves the lowest leakage rates on both external (3.3%) and internal (28.1%) channels, suggesting that model-level safety training may transfer to internal channel protection. Across all five models and four domains, the pattern C2 > C1 holds consistently, confirming that inter-agent communication is the primary vulnerability. These findings underscore the need for coordination frameworks that incorporate internal-channel privacy protections and enforce privacy controls on inter-agent communication.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AgentLeak**，这是首个专门针对多智能体大语言模型（LLM）系统内部隐私泄露问题的全栈基准测试。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着多智能体系统（MAS）在医疗、金融等敏感领域的应用，智能体之间需要频繁交换数据。
*   **核心问题**：现有的隐私评估基准（如 AgentDojo）主要关注**最终输出（外部渠道）**，而忽略了智能体之间的**内部通信渠道**（如智能体间消息、共享内存、工具参数等）。
*   **整体含义**：论文指出，即使最终输出看起来是安全的，敏感数据也可能在内部协调过程中发生泄露。这种“隐形泄露”绕过了传统的边界审计，构成了严重的合规和安全风险。

### 2. 核心方法论
*   **AgentLeak 基准设计**：构建了一个包含 1,000 个场景的测试集，涵盖医疗、金融、法律和企业四大领域。
*   **七大泄露渠道（C1-C7）**：
    *   **外部渠道**：C1（最终输出）、C3（工具输入）、C4（工具输出）、C6（系统日志）、C7（持久化制品）。
    *   **内部渠道**：C2（智能体间消息）、C5（共享内存）。
*   **32 类攻击分类法**：将攻击分为 6 大族群，包括提示词攻击（F1）、间接攻击（F2）、内存攻击（F3）、多智能体协调攻击（F4）、推理攻击（F5）和规避攻击（F6）。
*   **三层检测流水线**：
    1.  **Tier 1 (Canary Matching)**：正则匹配预设的唯一标识符（金丝雀）。
    2.  **Tier 2 (Structured Field Audit)**：针对特定格式（如 SSN、信用卡号）的模式提取。
    3.  **Tier 3 (LLM-as-Judge)**：利用 LLM（如 Qwen-2.5-7B）进行语义分析，识别改写后的敏感信息泄露。

### 3. 实验设计
*   **数据集/场景**：1,000 个场景（250 个/领域），其中 40% 为单智能体，60% 为多智能体（协调者-执行者拓扑）。
*   **评估对象（模型）**：GPT-4o, GPT-4o-mini, Claude 3.5 Sonnet, Mistral Large, Llama 3.3 70B。
*   **评估框架**：LangChain, CrewAI, AutoGPT, MetaGPT。
*   **对比维度**：单智能体 vs. 多智能体；基准（Vanilla）vs. 8 种防御配置（如隐私提示词、输出过滤、LlamaGuard 等）。
*   **威胁模型**：A0（良性/配置错误）、A1（弱对手/外部注入）、A2（强对手/直接访问组件）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 训练时长，因为该研究侧重于**推理评估**而非模型训练。
*   **成本与环境**：实验在 Python 3.10 环境下运行，至少需要 16GB RAM。使用了 OpenRouter API 进行模型调用，完整复现约需 3,000 次 API 调用，成本在 400-600 美元之间。
*   **检测器**：语义检测默认使用 Qwen-2.5-7B，因其在保持高准确率的同时计算成本较低。

### 5. 实验数量与充分性
*   **实验规模**：共生成并分析了 **4,979 条执行轨迹**。
*   **充分性**：实验覆盖了 5 种主流模型、4 个关键行业领域、4 种主流智能体框架以及 8 种防御机制。
*   **客观性**：采用了配对实验设计（同一场景在单/多智能体下运行），并引入了人类专家进行三阶段验证（场景设计、检测校准、结果核实），确保了评估的客观性和公平性。

### 6. 主要结论与发现
*   **内部渠道是重灾区**：内部渠道（C2, C5）的平均泄露率（57.8%）是外部渠道（27.2%）的 **2.1 倍**。其中智能体间消息（C2）泄露率最高，达 68.8%。
*   **审计盲区**：仅审计最终输出会漏掉 **41.7%** 的隐私违规行为。
*   **多智能体的悖论**：多智能体配置虽然能降低单个渠道的输出泄露率（从 43.2% 降至 27.2%），但由于增加了内部通道，系统总暴露率反而上升至 **68.9%**。
*   **模型表现**：**Claude 3.5 Sonnet** 在内外渠道上的泄露率均为最低，表现出最强的安全性；而 GPT-4o 的内部泄露率是外部的 4.5 倍。
*   **防御失效**：现有的边界防御（如输出过滤器）对内部通道几乎无效（0% 保护）。

### 7. 优点与亮点
*   **全栈视角**：首次将多智能体系统的内部通信和内存纳入隐私审计范围，填补了研究空白。
*   **框架无关性**：设计的 SDK 适配多种主流框架，具有很强的通用性和可扩展性。
*   **语义检测能力**：通过三层检测流水线，能够识别 82% 的语义级泄露（非逐字复制），这比单纯的正则匹配更符合现实威胁。
*   **现实指导意义**：量化了隐私与效用之间的权衡（TSR 下降约 4.7%），为企业部署安全智能体提供了数据支持。

### 8. 不足与局限
*   **语言限制**：目前仅支持英语场景，多语言环境下的泄露特征和检测准确率尚待研究。
*   **拓扑局限**：主要评估了“协调者-执行者”拓扑，更复杂的层级或对等拓扑可能具有不同的泄露模式。
*   **检测误差**：语义检测层（Tier 3）仍存在约 7.4% 的漏报率，且对某些专业领域的改写识别可能存在偏差。
*   **防御探索有限**：虽然指出了现有防御的不足，但尚未提出一种能完美平衡隐私与任务成功率（Utility）的新型防御机制。

（完）
