---
title: "M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval"
title_zh: M$^2$：通过轨迹摘要与见解检索实现的面向长程网络智能体的双重记忆增强
authors: "Dawei Yan, Haokui Zhang, Guangda Huzhang, Yang Li, Yibo Wang, Qing-Guo Chen, Zhao Xu, Weihua Luo, Ying Li, Wei Dong, Chunhua Shen"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00503v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 长程智能体决策的双重记忆增强
tldr: 针对多模态大模型在长程网页任务中面临的计算开销大和推理能力不足的问题，本文提出了M^2框架。这是一种无需训练的双重记忆增强方法，通过动态轨迹总结（内部记忆）压缩冗余历史，并利用见解检索增强（外部记忆）提供离线指南。该方法在WebVoyager等基准测试中显著提升了成功率并大幅降低了Token消耗，为构建高效网页智能体提供了新思路。
selection_source: fresh_fetch
motivation: 现有的网页智能体在处理长程任务时，面临计算成本高昂、上下文冗余以及复杂场景下推理能力不足的挑战。
method: 提出双层记忆机制，结合动态轨迹总结压缩交互历史，并利用外部见解库检索可操作的指南来辅助决策。
result: "在WebVoyager和OnlineMind2Web上，该方法使Qwen3-VL-32B的成功率提升高达19.6%，同时减少了58.7%的Token消耗。"
conclusion: M^2框架证明了通过高效的记忆管理和外部知识检索，可以在无需额外训练的情况下显著提升网页智能体的长程任务处理能力。
---

## 摘要
基于多模态大语言模型（MLLMs）的智能体在自主网络导航中展现出巨大潜力。然而，处理长程任务仍然是一个关键瓶颈。现有的策略通常严重依赖大规模数据收集和模型训练，但在面对复杂、长程场景时，仍面临高昂的计算成本和推理能力不足的问题。为此，我们提出了 M$^2$，这是一个无需训练的记忆增强框架，旨在优化上下文效率和决策鲁棒性。我们的方法结合了双层记忆机制，协同利用动态轨迹摘要（内部记忆）将冗长的交互历史压缩为简洁的状态更新，以及见解检索增强（外部记忆）通过从离线见解库中检索可操作的指南来引导智能体。在 WebVoyager 和 OnlineMind2Web 上的广泛评估表明，M$^2$ 持续超越基准模型，使 Qwen3-VL-32B 的成功率提升高达 19.6%，Token 消耗减少 58.7%，而像 Claude 这样的闭源模型在显著降低计算开销的同时，准确率提升了高达 12.5%。

## Abstract
Multimodal Large Language Models (MLLMs) based agents have demonstrated remarkable potential in autonomous web navigation. However, handling long-horizon tasks remains a critical bottleneck. Prevailing strategies often rely heavily on extensive data collection and model training, yet still struggle with high computational costs and insufficient reasoning capabilities when facing complex, long-horizon scenarios. To address this, we propose M$^2$, a training-free, memory-augmented framework designed to optimize context efficiency and decision-making robustness. Our approach incorporates a dual-tier memory mechanism that synergizes Dynamic Trajectory Summarization (Internal Memory) to compress verbose interaction history into concise state updates, and Insight Retrieval Augmentation (External Memory) to guide the agent with actionable guidelines retrieved from an offline insight bank. Extensive evaluations across WebVoyager and OnlineMind2Web demonstrate that M$^2$ consistently surpasses baselines, yielding up to a 19.6% success rate increase and 58.7% token reduction for Qwen3-VL-32B, while proprietary models like Claude achieve accuracy gains up to 12.5% alongside significantly lower computational overhead.

---

## 论文详细总结（自动生成）

以下是对论文《M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在网页导航任务中表现出色，但面对**长程（Long-Horizon）任务**时存在两大瓶颈：
    1.  **计算开销巨大**：传统的“全上下文”策略会将所有历史截图和文本拼接，导致 Token 消耗随步骤呈指数级增长。
    2.  **性能退化**：冗长且充满噪声的历史信息会导致模型出现“迷失在中间”（Lost-in-the-middle）现象，干扰决策。
*   **核心问题**：如何在不进行高成本微调（SFT）或强化学习（RL）的前提下，提升智能体在复杂长程任务中的效率与鲁棒性？
*   **整体含义**：提出了一种名为 **M$^2$** 的无需训练（Training-free）的记忆增强框架，通过内部轨迹压缩和外部经验检索，实现了性能与成本的最优平衡。

### 2. 方法论
M$^2$ 框架由双层记忆机制组成，将决策上下文 $C_t$ 重新定义为：$C'_t = \{P_{sys}, Q, M_{int,t}, M_{ext}, O_t\}$。

*   **动态轨迹摘要（内部记忆 $M_{int}$）**：
    *   **核心思想**：模仿人类认知，只保留高层抽象而非视觉细节。
    *   **技术细节**：在每一步交互中，智能体被要求生成一个三元组（思考、动作、状态摘要 $s_t$）。$s_t$ 包含视觉反馈和动作执行的简洁描述。
    *   **流程**：系统丢弃冗余的历史截图和原始文本，仅保留一个递归更新的摘要链。这使得上下文长度随任务深度仅呈线性（且斜率极小）增长。
*   **见解检索增强（外部记忆 $M_{ext}$）**：
    *   **核心思想**：利用跨任务的成功经验指导当前决策。
    *   **技术细节**：
        1.  **离线构建**：从 5.5 万条成功轨迹中，利用高性能模型（如 Ovis）提取通用的“高杠杆交互规则”（如搜索策略、导航效率、状态验证）。
        2.  **在线检索**：使用 Sentence Transformer 计算当前查询 $Q$ 与见解库中历史查询的余弦相似度，检索 Top-$i$ 条相关见解。
        3.  **注入引导**：将检索到的见解作为“防御性提示”注入系统提示词，帮助智能体避开常见陷阱。

### 3. 实验设计
*   **数据集/场景**：
    *   **WebVoyager**：涵盖 12 个代表性网站（如 Amazon, Apple, GitHub 等）。
    *   **OnlineMind2Web**：包含 291 个真实世界的在线任务，分为简单、中等、困难三个等级。
*   **对比方法**：
    *   **Baseline**：使用原始全上下文（Full-history）策略的模型。
    *   **SOTA 模型**：包括经过大量 RL 训练的 UI-TARS-1.5 和 OpenAI CUA。
*   **实验模型**：Claude-3.7-Sonnet、Claude-Sonnet-4、开源模型 Qwen3-VL-32B。

### 4. 资源与算力
*   **算力设施**：实验在具有高并发能力的 **Kubernetes (K8s) 集群**上运行，部署了 **64 个并发节点**，每个节点托管一个虚拟计算机环境。
*   **训练时长**：该框架本身是 **无需训练（Training-free）** 的，因此没有模型训练时长。
*   **离线成本**：构建见解库（Insight Bank）时使用了 Ovis-inhouse 80A3B 模型进行知识提取，这部分涉及一定的离线推理算力，但属于一次性投入。
*   **推理延迟**：单次相似度检索的延迟约为 6 毫秒，在实时部署中几乎可以忽略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准测试上进行了完整测试。
    *   **消融实验**：针对历史窗口大小 $k$、相似度函数（TF-IDF vs. BM25 vs. ST）、检索见解数量 $i$、内部记忆组件（视觉 vs. 动作）进行了多组对比。
    *   **定性分析**：提供了 GitHub 导航和地理位置歧义处理等多个案例研究。
*   **充分性与客观性**：实验覆盖了开源与闭源模型，且在真实在线环境（OnlineMind2Web）中验证，证明了方法的泛化性。通过 GPT-4o 作为第三方评判器，保证了评估的客观性。

### 6. 主要结论与发现
*   **性能大幅提升**：在 WebVoyager 上，Qwen3-VL-32B 的成功率提升了 16.2%；在 OnlineMind2Web 的中等难度任务中，成功率提升了 22.3%。
*   **成本显著降低**：Qwen3-VL-32B 的平均 Token 消耗减少了 **58.7%**，Claude 系列模型也减少了 30%~55%。
*   **开源追赶闭源**：增强后的开源模型 Qwen3-VL-32B（74.0%）在性能上甚至超越了未增强的 Claude-3.7-Sonnet（72.0%）。
*   **长程稳定性**：内部记忆有效解决了上下文爆炸问题，而外部记忆显著增强了智能体在复杂 UI 下的决策实用性。

### 7. 优点
*   **高效节能**：通过摘要机制解决了 MLLM 处理长视频/多截图时的 Token 冗余问题。
*   **即插即用**：无需对模型进行微调，适用于任何具有推理能力的 MLLM。
*   **经验迁移**：外部见解库实现了“前人栽树后人乘凉”，将特定任务的成功经验转化为通用的 UI 导航逻辑。

### 8. 不足与局限
*   **见解库依赖**：外部记忆的有效性高度依赖于离线见解库的质量和覆盖范围。如果任务极其冷门，检索到的见解可能关联度不高。
*   **静态库限制**：论文未讨论见解库的动态更新机制（即智能体在运行过程中如何自动将新学到的经验存入库中）。
*   **视觉信息丢失风险**：虽然摘要压缩了文本，但完全丢弃历史截图可能在某些极度依赖视觉时序对比的微小 UI 变化场景中导致信息丢失。

（完）
