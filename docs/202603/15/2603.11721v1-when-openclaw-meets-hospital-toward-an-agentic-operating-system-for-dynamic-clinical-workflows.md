---
title: "When OpenClaw Meets Hospital: Toward an Agentic Operating System for Dynamic Clinical Workflows"
title_zh: 当 OpenClaw 走进医院：构建面向动态临床工作流的智能体操作系统
authors: "Wenxian Yang, Hanzheng Qiu, Bangqun Zhang, Chengquan Li, Zhiyong Huang, Xiaobin Feng, Rongshan Yu, Jiahong Dong"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11721v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 临床工作流中 LLM 智能体长期记忆机制不足
tldr: 本研究针对医疗环境中LLM智能体在可靠性、安全性和长期记忆方面的挑战，提出了一个面向医院的智能体操作系统架构。该架构基于OpenClaw框架，引入了受限执行环境、以文档为中心的交互范式、分页索引记忆及医疗技能库。通过预定义技能接口和资源隔离，该系统在实现临床工作流自动化的同时，确保了医疗过程的安全、透明和可审计性，为构建动态临床工作流提供了关键的底层计算支撑。
selection_source: fresh_fetch
motivation: 现有的LLM智能体在医疗部署中面临可靠性不足、安全风险高以及缺乏有效的长期记忆机制等核心挑战。
method: 提出一种医院智能体操作系统架构，结合受限执行环境、分页索引记忆和医疗技能库，通过OpenClaw框架实现临床任务的受控执行。
result: 该设计通过技能接口和资源隔离机制，实现了对智能体行为的精确约束，并支持复杂临床上下文的长期管理。
conclusion: 该架构为在保障安全性和透明度的前提下，利用智能体协调动态临床工作流提供了一种可行的系统级解决方案。
---

## 摘要
大语言模型（LLM）智能体通过集成推理、工具调用和持久化记忆，扩展了传统的生成式模型。近期研究表明，此类智能体可通过自动化文档记录、协调护理流程以及辅助医疗决策，显著改善临床工作流。然而，尽管进展迅速，由于可靠性限制、安全风险以及长期记忆机制不足，在医疗环境中部署自主智能体仍然面临困难。本研究提出了一种使 LLM 智能体适配医院环境的架构。该设计引入了四个核心组件：受 Linux 多用户系统启发的受限执行环境；连接患者与临床医生智能体的以文档为中心的交互范式；旨在管理长期临床上下文的页索引记忆架构；以及支持临床任务序列即时组合的精选医疗技能库。该架构并非授予智能体不受限制的系统访问权限，而是通过预定义的技能接口和资源隔离来约束其行为。我们认为，此类系统构成了“医院智能体操作系统”（Agentic Operating System for Hospital）的基础，这是一个能够在保持安全性、透明度和可审计性的同时，协调临床工作流的计算层。本研究基于 OpenClaw（一个将智能体能力构建为精选离散技能库的开源自主智能体框架）进行设计，并针对安全临床部署所需的底层基础设施约束对其进行了扩展。

## Abstract
Large language model (LLM) agents extend conventional generative models by integrating reasoning, tool invocation, and persistent memory. Recent studies suggest that such agents may significantly improve clinical workflows by automating documentation, coordinating care processes, and assisting medical decision making. However, despite rapid progress, deploying autonomous agents in healthcare environments remains difficult due to reliability limitations, security risks, and insufficient long-term memory mechanisms. This work proposes an architecture that adapts LLM agents for hospital environments. The design introduces four core components: a restricted execution environment inspired by Linux multi-user systems, a document-centric interaction paradigm connecting patient and clinician agents, a page-indexed memory architecture designed for long-term clinical context management, and a curated medical skills library enabling ad-hoc composition of clinical task sequences. Rather than granting agents unrestricted system access, the architecture constrains actions through predefined skill interfaces and resource isolation. We argue that such a system forms the basis of an Agentic Operating System for Hospital, a computing layer capable of coordinating clinical workflows while maintaining safety, transparency, and auditability. This work grounds the design in OpenClaw, an open-source autonomous agent framework that structures agent capabilities as a curated library of discrete skills, and extends it with the infrastructure-level constraints required for safe clinical deployment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“医院智能体操作系统”（Agentic Operating System for Hospital, AOS-H）的架构，旨在解决大语言模型（LLM）智能体在医疗环境部署中面临的安全、记忆和协作挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体框架在医疗场景中难以落地，主要受限于：
    *   **安全性风险**：通用框架通常需要过高的系统权限（如文件访问、网络调用），不符合医疗隐私和审计要求。
    *   **记忆机制缺陷**：传统的 RAG（检索增强生成）依赖向量相似度，会打碎病历的层级和时间结构，导致临床推理上下文丢失。
    *   **系统僵化**：现有的医院信息系统（HIS）是预设的静态工作流，无法处理临床中复杂的“长尾”需求（如罕见并发症的个性化处理）。
*   **整体含义**：论文认为医疗智能体的部署本质上是**基础设施设计问题**，而非单纯的模型能力问题。

### 2. 方法论：核心思想与技术细节
该架构基于开源框架 **OpenClaw** 进行了扩展，包含四个核心组件：
*   **受限执行环境（Restricted Execution Environment）**：
    *   借鉴 Linux 多用户隔离机制，为每个角色（患者、医生、护士）分配独立的智能体进程和命名空间。
    *   采用“最小权限原则”，通过内核级的 `seccomp`、`AppArmor` 等工具限制智能体只能进行文件读写，禁止直接网络访问和代码执行。
*   **页索引记忆架构（Page-Indexed Memory Architecture）**：
    *   **核心思想**：完全抛弃向量嵌入（Embedding），改用基于文档层级的“清单（Manifest）”管理。
    *   **算法流程**：将病历组织为树状结构，每个节点包含一个 `manifest.md`（描述子节点内容）。检索时，智能体通过“渐进式披露（Progressive Disclosure）”策略，逐层阅读清单并决定进入哪个分支，直到找到目标页面。
*   **以文档为中心的交互范式**：
    *   智能体之间不直接通信，而是通过对共享文档的“追加写（Append-only Mutation）”进行协作。
    *   引入事件订阅机制，当文档发生变动时，触发相关智能体的推理循环。
*   **医疗技能库（Medical Skills Library）**：
    *   预定义一系列经过审计的离散技能（如生命体征聚合、药物核对），智能体根据临床目标动态组合这些技能。

### 3. 实验设计
*   **场景模拟**：本论文属于**架构设计类论文**，未提供基于标准数据集的定量实验。
*   **定性分析场景**：论文详细描述了六个临床应用场景来验证架构的可行性：
    1.  持续监测与常规随访。
    2.  急诊分诊与初步评估。
    3.  紧急情况升级（Emergency Escalation）。
    4.  药物管理与依从性跟踪。
    5.  主动风险识别。
    6.  多学科协作（针对多病共存患者）。
*   **对比对象**：主要在理论层面对比了传统 RAG（如 MemGPT, GraphRAG）和固定功能的 HIS 系统。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：系统基于 Linux 内核特性构建，主要依赖 LLM 的推理能力（如调用 LLM-Select 进行清单导航），而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：论文没有进行大规模的 Benchmark 测试或消融实验。
*   **充分性评价**：作为一篇提出新架构的论文，其逻辑推导和场景覆盖较为全面，但在**实证数据**方面存在明显缺失。它更多地展示了“如何做”以及“逻辑上为何安全”，而非“实际运行效果比某方法提升了多少百分比”。

### 6. 主要结论与发现
*   **基础设施优先**：通过约束环境而非仅仅约束模型指令，可以实现更高级别的医疗安全性。
*   **抛弃向量检索的可行性**：在结构化程度高的临床文档中，基于语言推理的层级导航比向量相似度检索更具解释性和准确性。
*   **动态组合能力**：智能体能够通过组合原子技能，解决传统 HIS 系统无法覆盖的非标临床任务。

### 7. 优点
*   **安全性极高**：将安全边界下沉到 OS 内核层，使“智能体作恶”在物理上变得不可能。
*   **记忆机制创新**：页索引记忆解决了医疗记录的时间序列和上下文依赖问题，且无需维护复杂的向量数据库。
*   **高可审计性**：所有的智能体行为都转化为文件系统的追加记录，天然符合医疗合规审计。

### 8. 不足与局限
*   **缺乏定量验证**：没有在真实或模拟的临床数据集上进行准确率、召回率或时延的测试。
*   **维护成本**：频繁的文档更新会导致大量的清单（Manifest）重写，可能带来较高的 LLM API 调用成本和延迟。
*   **并发控制**：论文未详细说明多个智能体同时写入同一文档时的冲突解决机制。
*   **遗留系统集成**：如何与现有的 Epic、Cerner 等主流 EHR 系统进行双向数据同步，在工程实现上仍有巨大挑战。

（完）
