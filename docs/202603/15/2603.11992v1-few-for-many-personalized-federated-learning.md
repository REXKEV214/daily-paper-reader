---
title: Few-for-Many Personalized Federated Learning
title_zh: 少对多个性化联邦学习
authors: "Ping Guo, Tiantian Zhang, Xi Lin, Xiang Li, Zhi-Ri Tang, Qingfu Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11992v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 针对异构客户端的个性化联邦学习
tldr: 针对个性化联邦学习中大量异构客户端带来的扩展性难题，本文提出了一种“以少胜多”的优化框架。该框架不再为每个客户端维护独立模型，而是通过维护极少量（K远小于M）的共享服务器模型来服务所有客户端。研究提出了FedFew算法，通过梯度优化自动发现最佳模型多样性。理论证明该方法具有近乎最优的个性化效果，且在视觉、NLP及医学影像等多个领域的实验中，仅用3个模型便超越了现有最先进技术。
selection_source: fresh_fetch
motivation: 解决个性化联邦学习中为大量异构客户端维护独立模型所导致的计算与存储扩展性瓶颈。
method: 将PFL重新表述为“以少胜多”优化问题，利用K个共享模型服务M个客户端，并开发了自动优化模型多样性的FedFew算法。
result: 在视觉、自然语言处理和真实医学影像数据集上，FedFew仅需3个模型即可持续优于现有的SOTA个性化联邦学习方法。
conclusion: 该研究证明了通过少量共享模型实现高效个性化联邦学习的可行性，为大规模异构环境下的模型部署提供了理论支撑和实践方案。
---

## 摘要
个性化联邦学习 (PFL) 旨在为具有高度异构数据分布的客户端训练定制化模型，同时保护数据隐私。现有方法通常依赖于聚类或模型插值等启发式方法，缺乏平衡异构客户端目标的原则性机制。为具有不同数据分布的 $M$ 个客户端提供服务本质上是一个多目标优化问题，理想情况下，实现最优个性化需要在帕累托前沿 (Pareto front) 上建立 $M$ 个不同的模型。然而，在拥有数百或数千个客户端的联邦设置中，维护 $M$ 个独立的模型会带来显著的可扩展性挑战。为了应对这一挑战，我们将 PFL 重新表述为一个“少对多” (few-for-many) 优化问题，仅维护 $K$ 个共享服务器模型 ($K \ll M$) 来共同为所有 $M$ 个客户端提供服务。我们证明了该框架实现了近乎最优的个性化：近似误差随着 $K$ 的增加而减小，且随着数据的增长，每个客户端的模型都会收敛到各自的最优解。基于这一重新表述，我们提出了 FedFew，这是一种通过高效的基于梯度的更新来联合优化 $K$ 个服务器模型的实用算法。与需要手动划分客户端的聚类方法或需要仔细调整超参数的插值方法不同，FedFew 通过其优化过程自动发现最优的模型多样性。在视觉、自然语言处理 (NLP) 和真实世界医学影像数据集上的实验表明，仅需 3 个模型，FedFew 就能持续优于其他最先进的方法。代码可在 https://github.com/pgg3/FedFew 获取。

## Abstract
Personalized Federated Learning (PFL) aims to train customized models for clients with highly heterogeneous data distributions while preserving data privacy. Existing approaches often rely on heuristics like clustering or model interpolation, which lack principled mechanisms for balancing heterogeneous client objectives. Serving $M$ clients with distinct data distributions is inherently a multi-objective optimization problem, where achieving optimal personalization ideally requires $M$ distinct models on the Pareto front. However, maintaining $M$ separate models poses significant scalability challenges in federated settings with hundreds or thousands of clients. To address this challenge, we reformulate PFL as a few-for-many optimization problem that maintains only $K$ shared server models ($K \ll M$) to collectively serve all $M$ clients. We prove that this framework achieves near-optimal personalization: the approximation error diminishes as $K$ increases and each client's model converges to each client's optimum as data grows. Building on this reformulation, we propose FedFew, a practical algorithm that jointly optimizes the $K$ server models through efficient gradient-based updates. Unlike clustering-based approaches that require manual client partitioning or interpolation-based methods that demand careful hyperparameter tuning, FedFew automatically discovers the optimal model diversity through its optimization process. Experiments across vision, NLP, and real-world medical imaging datasets demonstrate that FedFew, with just 3 models, consistently outperforms other state-of-the-art approaches. Code is available at https://github.com/pgg3/FedFew.

---

## 论文详细总结（自动生成）

### 论文总结：少对多个性化联邦学习 (FedFew)

#### 1. 核心问题与整体含义（研究动机和背景）
个性化联邦学习 (PFL) 的核心矛盾在于**个性化质量**与**系统可扩展性**之间的权衡。
*   **背景**：在非独立同分布 (Non-IID) 场景下，单一全局模型难以满足所有客户端的需求。
*   **痛点**：现有的 PFL 方法要么为每个客户端维护独立模型（$M$ 个模型，难以扩展到成千上万个客户端），要么依赖于启发式的聚类或插值方法（缺乏理论保证且超参数敏感）。
*   **核心问题**：如何用极少量的共享模型（$K$ 个）有效地服务大量的异构客户端（$M$ 个），并保证优化过程的原则性和收敛性？

#### 2. 方法论：核心思想与关键技术
论文提出了 **FedFew** 算法，将 PFL 重新定义为一个“少对多”的优化问题：
*   **核心思想**：服务器仅维护 $K$ 个共享模型（$K \ll M$），每个客户端从中选择最适合其本地分布的模型。这本质上是一个多目标优化 (MOO) 问题。
*   **关键技术细节**：
    *   **切比雪夫集合标量化 (TCH-Set)**：将 $M$ 个目标的优化问题转化为单一标量目标，确保解的帕累托最优性 (Pareto Optimality)。
    *   **双层平滑技术**：由于原始目标包含非连续的 `max` 和 `min` 算子，论文采用 Log-Sum-Exp 函数进行平滑处理，使其变为全可微函数，从而支持基于梯度的优化。
    *   **分解梯度计算**：
        *   **外部权重 ($\alpha_i$)**：实现“难样本挖掘”效果，给予表现较差的客户端更高权重。
        *   **内部权重 ($w_{ik}$)**：实现“软模型选择”，自动将客户端关联到最匹配的模型。
    *   **自动多样性发现**：无需手动划分聚类，算法通过梯度下降自动演化出 $K$ 个具有差异化的模型以覆盖客户端需求。

#### 3. 实验设计
*   **数据集**：
    *   **视觉**：CIFAR-10, CIFAR-100 (病理性与 Dirichlet 分布), TinyImageNet, FEMNIST (自然用户划分)。
    *   **NLP**：AG News 文本分类。
    *   **医疗影像**：Kvasir (胃肠道疾病), FedISIC2019 (皮肤病变)，模拟真实的跨机构异构性。
*   **Benchmark 与对比方法**：
    *   **中心化方法**：FedAvg, FedProx, FedMTL。
    *   **单客户端单模型方法**：APFL, Ditto, FedRep, FedAMP。
    *   **多服务器模型方法**：IFCA (硬聚类基准)。
    *   **医疗特有**：Local-only (仅本地训练)。

#### 4. 资源与算力
*   **模型架构**：使用了 4 层 CNN (用于 CIFAR/FEMNIST)、TextCNN (用于 AG News) 和 ResNet-18 (用于医疗数据集)。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到实验设置了 1000 到 2000 个通信轮次，且每个客户端在每轮进行 1 次本地迭代 (Local Epoch)。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个数据集，对比了 9 种最先进的基准方法，实验组数非常丰富。
*   **充分性评价**：
    *   **多样性**：覆盖了视觉、文本和真实医疗场景，验证了通用性。
    *   **消融与敏感性分析**：详细探讨了模型数量 $K$、平滑参数 $\mu$、本地迭代次数以及通信效率的影响。
    *   **客观性**：通过 Jain's Fairness Index 评估了公平性，并提供了最差客户端的准确率 (Min Accuracy)，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **性能卓越**：FedFew 仅需 3 个模型即可在几乎所有数据集上达到最高或次高的平均准确率，显著优于 IFCA 等硬聚类方法。
*   **理论验证**：证明了“少对多”框架的近似误差随 $K$ 增加而减小，且能收敛到帕累托平稳点。
*   **通信效率**：通过增加本地迭代次数（如 LE=16），FedFew 可以在减少 16 倍通信频率的情况下保持性能稳定。
*   **医疗场景优势**：在医疗数据上，FedFew 显著提升了表现最差机构的准确率，体现了多目标优化在平衡异构需求方面的优势。

#### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了算法，还给出了严谨的帕累托覆盖间隙和统计误差分解证明。
*   **无需手动调优**：相比于需要预设聚类或精调插值权重的传统方法，FedFew 实现了模型多样性的自动发现。
*   **高扩展性**：$K$ 是固定的常数（如 3 或 5），不随客户端数量 $M$ 增长，极大地节省了服务器存储和管理成本。

#### 8. 不足与局限
*   **本地计算开销**：客户端需要为 $K$ 个模型计算梯度，本地计算量随 $K$ 线性增加（虽然 $K$ 很小，但在资源极度受限的设备上仍有压力）。
*   **$K$ 的选择**：虽然 $K=3$ 表现良好，但对于极度复杂的分布，如何自适应地确定最优 $K$ 值仍是一个开放问题。
*   **优化复杂度**：实验观察到 $K$ 越大，优化景观越复杂，收敛速度会变慢。

（完）
