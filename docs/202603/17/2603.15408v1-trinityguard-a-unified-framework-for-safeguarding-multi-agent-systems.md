---
title: "TrinityGuard: A Unified Framework for Safeguarding Multi-Agent Systems"
title_zh: TrinityGuard：一个用于保护多智能体系统的统一框架
authors: "Kai Wang, Biaojie Zeng, Zeming Wei, Chang Jin, Hefeng Zhou, Xiangtian Li, Chao Yang, Jingjing Qu, Xingcheng Xu, Xia Hu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15408v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 多智能体系统的安全评估与风险评估
tldr: 随着大模型多智能体系统（MAS）的快速发展，其特有的通信威胁和系统级涌现风险日益凸显。本文提出TrinityGuard，一个基于OWASP标准的统一安全评估与监控框架。该框架包含涵盖20种风险类型的细粒度分类体系，通过抽象层适配不同MAS结构，利用评估层进行攻击探测并生成报告，同时结合运行时监控代理实现实时预警，为MAS安全提供了全面的保障方案。
selection_source: fresh_fetch
motivation: 现有的安全研究多集中于单智能体，缺乏针对多智能体系统特有的通信风险和系统级涌现危害的统一安全框架。
method: 提出包含风险分类、评估层和监控层的三位一体框架，利用攻击探测和LLM评判工厂实现全生命周期的安全管理。
result: 通过对多种代表性MAS的案例研究，证明了该框架在识别细粒度漏洞和实时监控风险方面的通用性与可靠性。
conclusion: TrinityGuard为多智能体系统的安全评估与监控奠定了基础，是提升MAS系统鲁棒性的重要工具。
---

## 摘要
随着基于大语言模型（LLM）的多智能体系统（MAS）的快速发展，其显著的安全与保障问题日益凸显，引入了超越单智能体或大语言模型的新型风险。尽管已有研究尝试解决这些问题，但现有文献仍缺乏专门针对 MAS 风险的内聚性保护系统。在这项工作中，我们推出了 TrinityGuard，这是一个基于 OWASP 标准的、针对基于 LLM 的 MAS 的全面安全评估与监控框架。具体而言，TrinityGuard 包含一个三层细粒度风险分类体系，识别了 20 种风险类型，涵盖了单智能体漏洞、智能体间通信威胁以及系统级涌现危害。TrinityGuard 旨在跨各种 MAS 结构和平台实现可扩展性，并以三位一体的方式组织，包括可适配任何 MAS 结构的 MAS 抽象层、包含特定风险测试模块的评估层，以及由统一 LLM 评判工厂（LLM Judge Factory）协调的运行时监控智能体。在评估过程中，TrinityGuard 执行精心设计的攻击探测，为每种风险类型生成详细的漏洞报告，其中监控智能体分析结构化的执行轨迹并发出实时警报，从而实现开发前评估和运行时监控。我们进一步将这些安全指标形式化，并展示了针对各种代表性 MAS 示例的详细案例研究，彰显了 TrinityGuard 的通用性和可靠性。总的来说，TrinityGuard 作为一个评估和监控 MAS 中各种风险的综合框架，为进一步研究其安全与保障铺平了道路。

## Abstract
With the rapid development of LLM-based multi-agent systems (MAS), their significant safety and security concerns have emerged, which introduce novel risks going beyond single agents or LLMs. Despite attempts to address these issues, the existing literature lacks a cohesive safeguarding system specialized for MAS risks. In this work, we introduce TrinityGuard, a comprehensive safety evaluation and monitoring framework for LLM-based MAS, grounded in the OWASP standards. Specifically, TrinityGuard encompasses a three-tier fine-grained risk taxonomy that identifies 20 risk types, covering single-agent vulnerabilities, inter-agent communication threats, and system-level emergent hazards. Designed for scalability across various MAS structures and platforms, TrinityGuard is organized in a trinity manner, involving an MAS abstraction layer that can be adapted to any MAS structures, an evaluation layer containing risk-specific test modules, alongside runtime monitor agents coordinated by a unified LLM Judge Factory. During Evaluation, TrinityGuard executes curated attack probes to generate detailed vulnerability reports for each risk type, where monitor agents analyze structured execution traces and issue real-time alerts, enabling both pre-development evaluation and runtime monitoring. We further formalize these safety metrics and present detailed case studies across various representative MAS examples, showcasing the versatility and reliability of TrinityGuard. Overall, TrinityGuard acts as a comprehensive framework for evaluating and monitoring various risks in MAS, paving the way for further research into their safety and security.

---

## 论文详细总结（自动生成）

### TrinityGuard：多智能体系统安全保护统一框架论文总结

#### 1. 核心问题与研究动机
随着基于大语言模型（LLM）的多智能体系统（MAS）在软件工程、科学发现和金融分析等领域的广泛应用，其安全性挑战日益严峻。现有的安全研究大多集中在单智能体或 LLM 本身（如提示词注入、幻觉），而 MAS 引入了**层次化的新型风险**：
*   **个体层面**：单智能体继承的传统攻击面。
*   **通信层面**：恶意指令在智能体间传播、身份冒用及错误放大。
*   **系统层面**：群体幻觉、代理合谋等单体无法产生的涌现性危害。
目前行业缺乏一个系统化、可扩展且适配不同 MAS 框架的统一评估与监控工具。

#### 2. 方法论：三位一体架构
TrinityGuard 提出了一个三层解耦架构，旨在实现跨平台的通用性：
*   **MAS 抽象层 (Level 1)**：定义了统一的 `BaseMAS` 接口，屏蔽了不同框架（如 AutoGen/AG2, LangGraph, CrewAI）的差异，实现消息拦截和任务执行的标准化。
*   **中间层 (Level 2)**：提供框架无关的原语。
    *   **测试干预原语**：用于注入攻击刺激（如伪造消息、毒化内存）。
    *   **运行时观察原语**：记录消息交换、工具调用等结构化事件流。
*   **安全评估层 (Level 3)**：
    *   **风险测试模块**：结合静态语料库与 LLM 合成的动态探测器，针对 20 种风险类型生成攻击。
    *   **LLM 评判工厂 (LLM Judge Factory)**：利用 LLM 作为语义评判器，对执行轨迹进行违规判定。
*   **三层风险分类体系**：
    *   **RT1 (原子风险)**：8 种单体风险（如提示词注入、权限过度）。
    *   **RT2 (通信风险)**：6 种交互风险（如恶意传播、目标漂移）。
    *   **RT3 (系统风险)**：6 种涌现风险（如级联失效、沙箱逃逸）。

#### 3. 实验设计
*   **合成测试集**：使用 EvoAgentX 自动合成了 **300 个唯一的 MAS 工作流**，涵盖 QA、编程、数据库、研究、物流、路由和调度 7 个领域。
*   **案例研究**：选取了 4 个代表性的官方 AG2 示例：
    1.  金融分析代理（代码解释器模式）。
    2.  游戏设计团队（群聊协作模式）。
    3.  旅游规划代理（Swarm 架构 + GraphRAG）。
    4.  深度研究代理（递归搜索模式）。
*   **对比与指标**：主要衡量**通过率 (Pass Rate)**，即系统成功防御或避免特定风险的测试用例比例。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该框架主要作为评估和监控工具，其核心开销在于调用 LLM（作为 Judge 或生成探测器）的 API 成本或推理开销，而非大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：300 个合成工作流和 20 种细分风险类型的交叉评估，提供了较大的样本量。
*   **充分性**：实验覆盖了从简单双代理到复杂层次化结构的多种 MAS 模式，并对比了不同领域（如 Coding vs. Research）的风险分布，实验设计较为全面。
*   **客观性**：通过标准化的评判提示词和自动化的攻击生成，减少了人工干预的偏差。

#### 6. 主要结论与发现
*   **普遍脆弱性**：当前 MAS 的安全性极低，平均通过率仅为 **7.1%**。
*   **系统级风险最严重**：RT3（系统风险）的平均通过率仅为 **1.3%**，许多领域在级联失效和沙箱逃逸面前几乎毫无防备。
*   **结构影响安全**：具有“研究者-审查者”层次结构的系统（如深度研究代理）在抑制幻觉方面表现较好，但仍无法防御身份冒用等通信攻击。
*   **开箱即用不安全**：实验证明，目前主流框架的默认配置在面对恶意环境时存在严重安全隐患。

#### 7. 优点与亮点
*   **高度解耦**：三层架构使得支持新的 MAS 框架只需实现抽象层接口，具有极强的扩展性。
*   **双模保障**：同时支持**部署前评估**（渗透测试）和**运行时监控**（实时预警），覆盖了系统全生命周期。
*   **细粒度诊断**：20 种风险分类比传统的单一安全分数更能精准定位漏洞位置（是单体问题、通信问题还是系统涌现问题）。

#### 8. 不足与局限
*   **评判器依赖**：安全判定的准确性高度依赖于底层 LLM Judge 的能力，可能存在误报或漏报。
*   **缺乏自动修复**：目前框架主要侧重于“诊断”和“告警”，尚未实现自动化的防御干预（如自动过滤恶意消息或隔离受损代理）。
*   **因果溯源困难**：虽然能识别系统级失效，但将 RT3 的结果自动追溯到 RT1 的初始诱因（因果链分析）仍是未来挑战。
*   **算力成本**：在大规模运行时监控中，引入额外的监控代理和 LLM 评判会增加系统的延迟和计算成本。

（完）
