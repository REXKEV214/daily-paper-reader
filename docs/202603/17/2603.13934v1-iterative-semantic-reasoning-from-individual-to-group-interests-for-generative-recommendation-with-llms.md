---
title: Iterative Semantic Reasoning from Individual to Group Interests for Generative Recommendation with LLMs
title_zh: 基于大语言模型的从个人到群体兴趣的迭代语义推理生成式推荐
authors: "Xiaofei Zhu, Jinfei Chen, Feiyang Yuan, Zhou Yang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13934v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 推荐系统中的用户偏好学习与语义推理
tldr: 该研究针对生成式推荐系统，提出了一种迭代语义推理框架（ISRF），旨在利用大语言模型（LLM）弥补显式个人兴趣与隐式群体兴趣之间的鸿沟。ISRF通过多步双向推理提取项目语义特征，构建用户相似度图以挖掘群体兴趣，并采用迭代优化策略实现个人与群体建模的相互增强。在多个数据集上的实验证明，该方法能更精准地捕捉用户兴趣，显著提升了推荐性能。
selection_source: fresh_fetch
motivation: 现有的推荐系统主要关注语义集成，缺乏从显式个人兴趣到隐式群体兴趣的深度语义推理能力。
method: 提出ISRF框架，利用LLM进行项目属性推理、构建用户相似度图，并通过迭代批处理优化策略协同细化个人与群体兴趣建模。
result: 在Sports、Beauty和Toys等数据集上的广泛实验表明，ISRF在推荐准确性上优于目前最先进的基准模型。
conclusion: 通过LLM实现的迭代语义推理能有效整合个人与群体兴趣，为生成式推荐提供更全面且一致的用户画像。
---

## 摘要
推荐系统旨在从历史行为中学习用户兴趣并提供相关物品。最近的方法利用大语言模型（LLMs）构建并整合用户和物品的语义表示，以捕捉用户兴趣。然而，用户行为理论表明，真正理解用户兴趣不仅需要语义整合，还需要从显式个人兴趣到隐式群体兴趣的语义推理。为此，我们提出了一种用于生成式推荐的迭代语义推理框架（ISRF）。ISRF 利用 LLMs 通过三个步骤连接显式个人兴趣和隐式群体兴趣。首先，我们对物品属性进行多步双向推理，以推断语义物品特征，并构建捕捉用户显式兴趣的语义交互图。其次，我们基于语义物品特征生成语义用户特征，并构建基于相似度的用户图，以推断相似用户群体的隐式兴趣。第三，我们采用迭代批优化策略，其中个人显式兴趣直接指导群体隐式兴趣的细化，而群体隐式兴趣则间接增强个人建模。这一迭代过程确保了连贯且渐进的兴趣推理，从而实现更准确、更全面的用户兴趣学习。在 Sports、Beauty 和 Toys 数据集上的广泛实验表明，ISRF 优于最先进的基准模型。代码可在 https://github.com/htired/ISRF 获取。

## Abstract
Recommendation systems aim to learn user interests from historical behaviors and deliver relevant items. Recent methods leverage large language models (LLMs) to construct and integrate semantic representations of users and items for capturing user interests. However, user behavior theories suggest that truly understanding user interests requires not only semantic integration but also semantic reasoning from explicit individual interests to implicit group interests. To this end, we propose an Iterative Semantic Reasoning Framework (ISRF) for generative recommendation. ISRF leverages LLMs to bridge explicit individual interests and implicit group interests in three steps. First, we perform multi-step bidirectional reasoning over item attributes to infer semantic item features and build a semantic interaction graph capturing users' explicit interests. Second, we generate semantic user features based on the semantic item features and construct a similarity-based user graph to infer the implicit interests of similar user groups. Third, we adopt an iterative batch optimization strategy, where individual explicit interests directly guide the refinement of group implicit interests, while group implicit interests indirectly enhance individual modeling. This iterative process ensures consistent and progressive interest reasoning, enabling more accurate and comprehensive user interest learning. Extensive experiments on the Sports, Beauty, and Toys datasets demonstrate that ISRF outperforms state-of-the-art baselines. The code is available at https://github.com/htired/ISRF.

---

## 论文详细总结（自动生成）

以下是对论文《Iterative Semantic Reasoning from Individual to Group Interests for Generative Recommendation with LLMs》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的生成式推荐系统虽然利用大语言模型（LLM）增强了语义表示，但大多停留在“语义集成”层面（即简单融合物品属性），缺乏对用户兴趣从“显式个人偏好”到“隐式群体需求”的深度推理能力。
*   **研究动机**：根据用户行为理论，理解用户真实兴趣需要跨越语义鸿沟。例如，用户购买防滑篮球鞋（显式兴趣：硬地性能），其深层需求可能还包括透气和轻便（隐式兴趣：通过相似群体行为推断出的耐力需求）。论文旨在通过LLM的推理能力，构建一个从个体到群体的迭代语义推理框架。

### 2. 方法论：ISRF 框架
ISRF（Iterative Semantic Reasoning Framework）由三个协同模块组成：
*   **个体兴趣推理（IIR）**：
    *   利用思维链（CoT）引导LLM对物品属性进行多步双向推理，生成正面（适用人群）和负面（不适用人群）描述，形成丰富的物品语义特征。
    *   通过PCA降维和Adapter映射，将语义特征嵌入推荐空间。
    *   利用LightGCN在“用户-物品”交互图上捕捉用户的显式兴趣。
*   **群体兴趣推理（GIR）**：
    *   基于用户历史行为，利用LLM推断用户的语义偏好描述。
    *   构建基于语义相似度的“用户-用户”图，通过GNN聚合相似邻居的信息，挖掘群体层面的隐式兴趣。
*   **迭代细化（IR）**：
    *   **直接到序列对齐**：在直接推荐任务中，以群体隐式兴趣为“教师”，引导个体显式兴趣的优化。
    *   **序列表示对齐**：在序列推荐任务中，通过最大化互信息，使个体偏好表示与群体兴趣保持一致。
    *   **算法流程**：交替优化文本生成损失和对比学习对齐损失，确保两种兴趣表示的连贯性。

### 3. 实验设计
*   **数据集**：使用了 Amazon 的三个主流公开数据集：**Sports & Outdoors**（运动户外）、**Beauty**（美妆）、**Toys & Games**（玩具游戏）。
*   **任务场景**：涵盖了**序列推荐**（预测下一个物品）和**直接推荐**（从候选集中选出正样本）。
*   **Benchmark（基准方法）**：对比了 14 种方法，分为四类：
    1.  传统方法：SimpleX, Caser, GRU4Rec, HGN。
    2.  注意力机制方法：SASRec, BERT4Rec, FDSA。
    3.  图神经网络方法：LightGCN, NCL, XSimGCL。
    4.  LLM 推荐方法：P5, RSL, POD, ELMRec（SOTA）。

### 4. 资源与算力
*   **模型骨干**：
    *   推理与嵌入提取：**DeepSeek-R1-14B**。
    *   语义编码器：EasyRec (RoBERTa-large)。
    *   推荐生成骨干：**T5-small**。
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 (24GB VRAM)**。
*   **训练时长**：根据数据集大小，每轮（epoch）训练约 5 到 15 分钟；推理时间在 7 到 24 分钟之间。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集上进行了主实验对比、消融实验、语义变体分析、超参数敏感性分析及案例研究。
*   **充分性评价**：实验设计较为**充分且客观**。通过消融实验验证了每个损失函数和模块的必要性；通过语义变体实验（仅用正面或负面推理结果）证明了双向推理的优越性；超参数实验覆盖了图层数 $L'$ 和相似邻居数 $K$ 的变化趋势。

### 6. 主要结论与发现
*   **性能领先**：ISRF 在所有数据集和任务上均显著优于现有 SOTA（如 ELMRec），在直接推荐任务上的提升尤为明显（最高达 24.57%）。
*   **推理价值**：证明了从“个体显式”到“群体隐式”的推理比单纯的语义嵌入融合更有效。
*   **对齐效应**：迭代细化机制能有效解决个体与群体表示不一致的问题，增强了模型的泛化能力。

### 7. 优点与亮点
*   **视角新颖**：首次将 LLM 的推理能力应用于从个体兴趣向群体兴趣的跨越，符合消费者行为学理论。
*   **双向语义建模**：通过正反两方面的 CoT 推理，捕捉了更全面的物品和用户画像。
*   **协同优化**：设计了精巧的对齐损失，使生成式推荐模型能够同时吸收图结构信息和深层语义信息。

### 8. 不足与局限
*   **计算开销**：虽然推荐阶段使用了 T5-small，但前期的 LLM 推理（DeepSeek-R1-14B）和语义图构建在大规模实时场景下可能存在延迟挑战。
*   **数据依赖**：模型高度依赖物品的文本属性质量，若属性缺失或存在噪声，推理效果可能受限。
*   **偏差风险**：相似用户图的构建基于语义相似度，可能引入流行度偏差或加剧“过滤气泡”效应。
*   **覆盖范围**：实验主要集中在 Amazon 电商数据集，在新闻、短视频等兴趣极其多变的领域尚需验证。

（完）
