---
title: "LMEB: Long-horizon Memory Embedding Benchmark"
title_zh: LMEB：长跨度记忆嵌入基准
authors: "Xinping Zhao, Xinshuo Hu, Jiaxin Xu, Danyu Tang, Xin Zhang, Mengjia Zhou, Yan Zhong, Yao Zhou, Zifei Shan, Meishan Zhang, Baotian Hu, Min Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12572v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 记忆增强系统中长程记忆检索的基准测试
tldr: 针对现有文本嵌入基准在长程记忆检索评估方面的不足，本文提出了LMEB基准。该基准涵盖22个数据集和193个零样本任务，分为情景、对话、语义和程序四类记忆类型。通过对15个主流模型的评估，发现现有模型在处理碎片化、上下文相关及长跨度记忆任务时仍面临挑战，且性能与传统检索任务不完全相关，为记忆增强系统提供了重要的评估框架。
selection_source: fresh_fetch
motivation: 现有基准侧重于传统段落检索，忽视了模型在处理碎片化、上下文依赖及长跨度记忆检索任务中的关键能力。
method: 构建了包含22个数据集和193个零样本任务的LMEB基准，涵盖四种记忆类型，并对15个从亿级到百亿级参数的嵌入模型进行了评估。
result: 实验发现LMEB难度适中，模型规模并不直接决定性能，且其评估结果与MTEB具有正交性，表明传统检索能力难以泛化至长程记忆任务。
conclusion: LMEB填补了记忆嵌入评估的空白，揭示了当前模型在长程记忆处理上的局限，为推动更通用的文本嵌入技术发展提供了标准。
---

## 摘要
记忆嵌入对于 OpenClaw 等记忆增强系统至关重要，但在目前的文本嵌入基准测试中，对其评估的研究尚不充分。现有的基准测试主要局限于传统的段落检索，未能评估模型处理涉及碎片化、上下文相关且时间跨度较长的信息的长跨度记忆检索任务的能力。为了解决这一问题，我们推出了长跨度记忆嵌入基准（LMEB），这是一个评估嵌入模型处理复杂长跨度记忆检索任务能力的综合框架。LMEB 涵盖了 22 个数据集和 193 个零样本检索任务，涉及四种记忆类型：情景记忆、对话记忆、语义记忆和程序记忆，包含 AI 生成和人工标注的数据。这些记忆类型在抽象程度和时间依赖性方面各不相同，捕捉了记忆检索的不同维度，反映了现实世界中的多样化挑战。我们评估了 15 个广泛使用的嵌入模型，参数规模从亿级到百亿级不等。结果显示：(1) LMEB 提供了合理的难度水平；(2) 更大的模型并不总是表现更好；(3) LMEB 与 MTEB 表现出正交性。这表明该领域尚未收敛出一个能够在所有记忆检索任务中表现出色的通用模型，且传统段落检索的性能可能无法泛化到长跨度记忆检索。总之，通过提供标准化且可复现的评估框架，LMEB 填补了记忆嵌入评估中的关键空白，推动了文本嵌入在处理长期、上下文相关的记忆检索方面的进一步发展。LMEB 已在 https://github.com/KaLM-Embedding/LMEB 开源。

## Abstract
Memory embeddings are crucial for memory-augmented systems, such as OpenClaw, but their evaluation is underexplored in current text embedding benchmarks, which narrowly focus on traditional passage retrieval and fail to assess models' ability to handle long-horizon memory retrieval tasks involving fragmented, context-dependent, and temporally distant information. To address this, we introduce the Long-horizon Memory Embedding Benchmark (LMEB), a comprehensive framework that evaluates embedding models' capabilities in handling complex, long-horizon memory retrieval tasks. LMEB spans 22 datasets and 193 zero-shot retrieval tasks across 4 memory types: episodic, dialogue, semantic, and procedural, with both AI-generated and human-annotated data. These memory types differ in terms of level of abstraction and temporal dependency, capturing distinct aspects of memory retrieval that reflect the diverse challenges of the real world. We evaluate 15 widely used embedding models, ranging from hundreds of millions to ten billion parameters. The results reveal that (1) LMEB provides a reasonable level of difficulty; (2) Larger models do not always perform better; (3) LMEB and MTEB exhibit orthogonality. This suggests that the field has yet to converge on a universal model capable of excelling across all memory retrieval tasks, and that performance in traditional passage retrieval may not generalize to long-horizon memory retrieval. In summary, by providing a standardized and reproducible evaluation framework, LMEB fills a crucial gap in memory embedding evaluation, driving further advancements in text embedding for handling long-term, context-dependent memory retrieval. LMEB is available at https://github.com/KaLM-Embedding/LMEB.

---

## 论文详细总结（自动生成）

以下是对论文《LMEB: Long-horizon Memory Embedding Benchmark》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的文本嵌入基准（如 MTEB）主要关注传统的段落检索（信息组织良好、上下文独立），忽视了**长跨度记忆检索（Long-horizon Memory Retrieval）**。
*   **研究背景**：在智能体（Agent）和记忆增强系统中，模型需要处理碎片化、高度依赖上下文且时间跨度极长的信息。目前的基准测试无法有效评估模型在这些复杂、真实场景下的记忆提取能力。
*   **整体含义**：论文提出了 LMEB 基准，旨在填补这一空白，为开发能够处理长期、上下文相关记忆的嵌入模型提供标准化的评估框架。

### 2. 论文提出的方法论
*   **核心思想**：基于“抽象程度”和“时间依赖性”两个维度，将记忆划分为四种类型，构建多维度的评估体系。
*   **记忆分类法（Taxonomy）**：
    1.  **情景记忆（Episodic）**：低抽象、高时间依赖（如：特定时间发生的事件）。
    2.  **对话记忆（Dialogue）**：高抽象、高时间依赖（如：多轮对话中的用户偏好）。
    3.  **语义记忆（Semantic）**：低抽象、低时间依赖（如：稳定的事实知识）。
    4.  **程序记忆（Procedural）**：高抽象、低时间依赖（如：操作序列、工具使用技能）。
*   **技术细节**：
    *   **统一格式**：将所有数据集转换为标准的 IR 风格（queries, corpus, qrels）。
    *   **时间锚点**：针对含相对时间描述的查询，在 Query 中添加显式的时间戳（如“2天前”对应具体的日期）。
    *   **候选池限制**：引入 `candidates.jsonl`，模拟真实的记忆访问限制（如仅在当前对话历史中检索）。

### 3. 实验设计
*   **数据集规模**：涵盖 **22 个数据集**，共计 **193 个零样本检索任务**。数据来源包括 AI 生成和人工标注。
*   **评估指标**：主要使用 **NDCG@10**，辅助使用 **Recall@10**（采用 Capped Recall 以处理相关文档数超过 K 的情况）。
*   **对比方法**：评估了 **15 个主流嵌入模型**，参数量从 239M 到 12B 不等，包括：
    *   **大型模型**：KaLM-Embedding-Gemma3 (12B), bge-multilingual-gemma2 (9B), Qwen3-Embedding (8B), NV-Embed-v2 (7B) 等。
    *   **中小型模型**：jina-v5-text, bge-m3, EmbeddingGemma-300M, e5-large 等。
*   **实验设置**：分为“无指令（w/o inst.）”和“有指令（w/ inst.）”两种模式，评估模型对任务指令的遵循能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号、数量或时长），因为该论文主要关注基准测试的构建和对现有预训练模型的评估。
*   **推理工具**：提到了使用 Transformers、Sentence-Transformers 和 vLLM 等框架进行模型推理。

### 5. 实验数量与充分性
*   **实验数量**：论文进行了大规模的横向对比，涉及 15 个模型在 193 个任务上的表现，并计算了 LMEB 与 MTEB 之间的 Pearson 和 Spearman 相关系数。
*   **充分性与公平性**：
    *   **充分性**：覆盖了四种截然不同的记忆类型，实验规模巨大，足以反映当前嵌入模型的现状。
    *   **公平性**：采用零样本（Zero-shot）评估，确保模型在未见过的任务上公平竞争；统一了输入长度限制（1024 tokens）。

### 6. 论文的主要结论与发现
*   **难度适中**：最高分模型在 NDCG@10 上仅达到 61.41，说明 LMEB 具有挑战性，且仍有提升空间。
*   **规模非唯一因素**：**更大的模型并不总是表现更好**。例如，300M 的 EmbeddingGemma 在某些任务上优于 12B 的模型，说明架构和训练数据质量更关键。
*   **正交性发现**：LMEB 与 MTEB 的相关性极低（接近 0），甚至在对话记忆上呈现负相关。这证明**传统检索能力的强弱无法代表长程记忆检索能力**。
*   **指令敏感性**：不同模型对指令的偏好不同，部分模型（如 NV-Embed-v2）对指令不敏感，而部分模型在加入指令后性能显著提升。

### 7. 优点（亮点）
*   **填补空白**：首次系统性地针对“长程记忆”这一 Agent 核心能力构建嵌入基准。
*   **分类科学**：通过抽象度和时间依赖性两个维度对记忆进行分类，逻辑严密且符合认知心理学。
*   **易扩展性**：提供了开源工具包，支持通过简单的配置文件添加新模型和新数据集。
*   **多样性高**：结合了合成数据与真实人类标注数据，涵盖了从代码、学术论文到日常对话的广泛领域。

### 8. 不足与局限
*   **语言局限**：目前 LMEB 主要集中在**英语**数据集，缺乏对多语言（如中文）长程记忆能力的深度评估。
*   **AI 生成数据偏差**：部分数据集（如 EPBench）由 AI 生成，可能存在与真实人类记忆模式不完全一致的风险。
*   **长文本截断**：实验中统一截断为 1024 tokens，对于极长上下文的记忆检索，可能未能完全测试出模型处理超长输入的能力。

（完）
