---
title: Financial Transaction Retrieval and Contextual Evidence for Knowledge-Grounded Reasoning
title_zh: 金融交易检索与知识增强推理的上下文证据
authors: "Artem Sakhno, Daniil Tomilov, Yuliana Shakhvalieva, Inessa Fedorova, Daria Ruzanova, Omar Zoloev, Andrey Savchenko, Maksim Makarenko"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15459v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 利用金融交易轨迹改进用户建模流水线
tldr: 针对通用大语言模型难以处理时间分布表格交易数据的问题，本文提出 FinTRACE 架构。该方法采用检索优先策略，将原始交易转化为特征表示并存入行为知识库，通过指令微调使 LLM 能够基于检索到的行为模式进行推理。实验表明，FinTRACE 在低监督环境下显著提升了交易分析性能，为金融用户建模提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 通用大语言模型难以有效处理具有时间分布特性的表格交易数据，且现有专用模型对标注数据依赖性强。
method: 提出 FinTRACE 架构，通过将交易转化为特征并构建行为知识库，结合检索增强技术和指令微调来辅助 LLM 推理。
result: 在流失预测等任务中，FinTRACE 将零样本 MCC 性能翻倍，并在多项交易分析指标上取得当前最优结果。
conclusion: 该研究证明了结合检索增强与知识库的方法能显著提升 LLM 在复杂金融交易分析中的推理能力和泛化性能。
---

## 摘要
如今，金融机构的成功在很大程度上取决于其处理客户生成的数字足迹（如从多方来源收集的交易历史）以优化用户建模流程的能力。由于通用大语言模型（LLMs）在处理随时间分布的表格数据方面表现不佳，生产环境仍依赖于迁移性有限且需要大量标注数据的专用表格和序列模型。为解决这一问题，我们提出了 FinTRACE，这是一种检索优先的架构，它将原始交易转换为可重用的特征表示，应用基于规则的检测器，并将生成的信号存储在行为知识库中，该知识库与下游任务目标具有分级关联。在公开和工业基准测试中，FinTRACE 显著提升了低监督交易分析的性能，将流失预测任务中的零样本（zero-shot）MCC 从 0.19 翻倍至 0.38，并将 16 样本（16-shot）MCC 从 0.25 提升至 0.40。我们进一步利用 FinTRACE，通过对检索到的行为模式进行指令微调来增强 LLMs 的推理能力，在交易分析问题上取得了最先进的 LLM 性能。

## Abstract
Nowadays, success of financial organizations heavily depends on their ability to process digital traces generated by their clients, e.g., transaction histories, gathered from various sources to improve user modeling pipelines. As general-purpose LLMs struggle with time-distributed tabular data, production stacks still depend on specialized tabular and sequence models with limited transferability and need for labeled data. To address this, we introduce FinTRACE, a retrieval-first architecture that converts raw transactions into reusable feature representations, applies rule-based detectors, and stores the resulting signals in a behavioral knowledge base with graded associations to the objectives of downstream tasks. Across public and industrial benchmarks, FinTRACE substantially improves low-supervision transaction analytics, doubling zero-shot MCC on churn prediction performance from 0.19 to 0.38 and improving 16-shot MCC from 0.25 to 0.40. We further use FinTRACE to ground LLMs via instruction tuning on retrieved behavioral patterns, achieving state-of-the-art LLM results on transaction analytics problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Financial Transaction Retrieval and Contextual Evidence for Knowledge-Grounded Reasoning》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
金融机构拥有海量的客户交易数据，但如何有效利用这些数据进行用户建模（如流失预测、风险评估）仍面临挑战。
*   **现有瓶颈**：传统的专用模型（如 CatBoost、CoLES）虽然在全监督下表现优异，但极度依赖标注数据，且模型间难以迁移。通用大语言模型（LLM）虽然具备推理能力，但在处理长序列、不规则且缺乏语义的表格交易数据时表现糟糕，存在明显的“泛化鸿沟”。
*   **核心目标**：提出一种名为 **FinTRACE** 的框架，通过构建“行为知识库”将原始交易数据转化为结构化的证据，从而弥合语言模型与结构化金融数据之间的鸿沟，实现在低监督（零样本或少样本）环境下的高效推理。

### 2. 论文提出的方法论
FinTRACE 采用“检索优先”的架构，核心思想是将原始日志转化为可解释、可检索的知识层。
*   **三层知识库（KB）构建**：
    1.  **特征本质（Feature Essences）**：从交易序列中提取任务无关的数值描述符（如交易频率、金额波动）。
    2.  **行为模式（Behavioral Patterns）**：利用 LLM 引导，将特征组合成高层概念（如“财务稳定性”、“行为忠诚度”）。
    3.  **目标关联（Targets）**：通过白盒预测器（AutoWoE）学习特征与目标之间的显式规则（如：若活跃天数 ≤ 70.5，则流失信号强）。
*   **推理与微调流程**：
    *   **检索增强推理**：在推理时，不直接输入原始交易，而是检索与任务相关的行为事实和白盒规则，构建结构化 Prompt。
    *   **知识增强指令微调**：将知识库中的三元组（指令、上下文、响应）转化为训练数据，使 LLM 内部化金融推理模式。
    *   **自反思机制**：在少样本场景下，通过存储先前的预测成败记录，让模型在推理时进行自我修正。

### 3. 实验设计
*   **数据集**：
    1.  **Gender Prediction**：性别分类基准（8.4K 用户）。
    2.  **Rosbank**：流失预测（10K 用户）。
    3.  **DataFusion**：大规模流失预测（1.96万用户子集）。
    4.  **私有工业数据集**：某大型银行的 10K 用户采样，用于分类任务。
*   **Benchmark 与对比方法**：
    *   **LLM 基准**：GPT-OSS (120B), Llama-3-8B, GPT-4o。
    *   **专用表格/序列模型**：TabPFN, CatBoost, CoLES, LLM4ES。
    *   **少样本学习方法**：TabLLM, FeatLLM, KNN+Embedding。

### 4. 资源与算力
*   **硬件环境**：实验使用了由 **4 张 NVIDIA A100 GPU** 组成的集群。
*   **模型使用**：零样本和少样本评估主要基于 `gpt-oss-120b`，微调实验则基于 `Llama-3-8B-Instruct`。
*   **训练细节**：文中未明确给出具体的训练总时长，但提到了使用 AutoWoE 提取规则和标准的指令微调流程。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个公开数据集和 1 个私有工业数据集，测试了从 0-shot 到 16-shot 以及全监督的不同配置。
*   **消融实验**：作者进行了多组消融实验，包括：
    *   行为模式选择策略（随机 vs LLM 引导）。
    *   上下文构建策略（简单统计量 vs 白盒规则）。
    *   反思机制（Reflection Tuning）在不同样本量下的增益。
*   **客观性**：通过与多种 SOTA（最先进）专用模型和 LLM 适配方法对比，实验设计较为全面，能够客观反映 FinTRACE 在处理非语义表格数据时的优势。

### 6. 主要结论与发现
*   **性能翻倍**：在 Rosbank 流失预测中，FinTRACE 将零样本 MCC 从 0.19 提升至 0.38。
*   **弥合差距**：经过知识增强指令微调的 LLM（FinTRACE Instruct）在 Rosbank 上达到了 0.48 MCC，与最顶尖的专用序列模型（CoLES）持平。
*   **保留通用能力**：在提升金融任务表现的同时，模型在 MMLU 等通用能力测试上未出现灾难性遗忘。
*   **规则的重要性**：消融实验证明，显式的“白盒规则”比简单的统计摘要（如分位数）更能有效辅助 LLM 推理。

### 7. 优点
*   **可解释性强**：通过白盒规则将数值特征转化为人类可读的逻辑，使 LLM 的输出具有可追溯的证据链。
*   **低监督优势**：在标注数据极度匮乏的情况下，利用预定义的行为模式和检索机制实现了极强的泛化能力。
*   **架构灵活**：知识库可以跨任务重用，无需为每个新任务重新训练整个流水线。

### 8. 不足与局限
*   **依赖预定义特征**：框架仍需依赖 LATTE 等工具进行初步的特征工程，并非完全的端到端原始数据处理。
*   **规则提取开销**：虽然推理是轻量级的，但构建知识库时使用 AutoWoE 提取规则可能在大规模动态数据下存在计算延迟。
*   **LLM 引导的偏差**：行为模式的定义部分依赖于 LLM 的建议，可能存在模型固有的偏见或对特定金融场景理解不深的问题。

（完）
