---
title: Relationship-Aware Safety Unlearning for Multimodal LLMs
title_zh: 针对多模态大语言模型的关系感知安全遗忘学习
authors: "Vishnu Narayanan Anilkumar, Abhijith Sreesylesh Babu, Trieu Hai Vo, Mohankrishna Kolla, Alexander Cuneo"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14185v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 多模态大语言模型的关系感知安全遗忘
tldr: 本研究针对多模态大模型中因特定关系导致的安全性问题（如“儿童饮酒”），提出了关系感知安全遗忘框架。该方法通过显式表示“对象-关系-对象”（O-R-O）三元组，并利用LoRA进行针对性的参数微调，旨在抑制不安全的关系组合，同时保留对象本身的良性用途及其他安全关系。实验证明该方法在抑制不安全内容的同时，对各种攻击具有鲁棒性，且减少了对正常功能的损害。
selection_source: fresh_fetch
motivation: 现有的安全遗忘方法通常针对孤立概念或图文对，容易误伤同一对象的良性用途，无法精准处理由特定关系引发的安全风险。
method: 提出一种基于O-R-O三元组的框架，通过LoRA参数高效微调来抑制不安全关系，并确保对象边缘分布和安全邻近关系的完整性。
result: 在CLIP实验和多种攻击场景（如改写、上下文、分布外图像）下，该方法成功抑制了不安全元组并表现出强劲的鲁棒性。
conclusion: 关系感知遗忘为多模态模型提供了一种更精准的安全治理方案，在消除特定风险与保持模型通用能力之间取得了平衡。
---

## 摘要
生成式多模态模型可能会表现出本质上属于关系性的安全故障：当两个良性概念通过特定的动作或关系（例如，儿童-饮用-葡萄酒）连接时，可能会变得不安全。现有的遗忘学习（unlearning）和概念擦除方法通常针对孤立的概念或图像-文本对，这可能会对相同对象和关系的良性使用造成附带损害。我们提出了关系感知安全遗忘学习：这是一个显式表示不安全的对象-关系-对象（O-R-O）三元组，并应用针对性的参数高效编辑（LoRA）来抑制不安全三元组，同时保留对象边缘分布和安全邻近关系的框架。我们进行了基于 CLIP 的实验，并在改写、上下文和分布外图像攻击下进行了鲁棒性评估。

## Abstract
Generative multimodal models can exhibit safety failures that are inherently relational: two benign concepts can become unsafe when linked by a specific action or relation (e.g., child-drinking-wine). Existing unlearning and concept-erasure approaches often target isolated concepts or image-text pairs, which can cause collateral damage to benign uses of the same objects and relations. We propose relationship-aware safety unlearning: a framework that explicitly represents unsafe object-relation-object (O-R-O) tuples and applies targeted parameter-efficient edits (LoRA) to suppress unsafe tuples while preserving object marginals and safe neighboring relations. We include CLIP-based experiments and robustness evaluation under paraphrase, contextual, and out-of-distribution image attacks.

---

## 论文详细总结（自动生成）

以下是对论文《Relationship-Aware Safety Unlearning for Multimodal LLMs》（针对多模态大语言模型的关系感知安全遗忘学习）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大模型（如 CLIP、生成式模型）中存在“关系性安全故障”。即两个单独看是安全的概念（如“儿童”和“葡萄酒”），在特定关系下（如“儿童饮用葡萄酒”）会组合成不安全的内容。
*   **研究动机**：现有的“遗忘学习”（Unlearning）方法通常针对孤立的对象（如“武器”）或特定的图文对。这种粗放的方法容易产生“附带损害”，导致模型在处理该对象的良性场景（如“成人饮酒”或“儿童喝水”）时能力受损。论文旨在实现精准的“手术式”遗忘：仅删除不安全的关系，而保留对象本身及其在安全上下文中的用途。

### 2. 方法论
论文提出了一种**关系感知安全遗忘框架**，核心思想是将安全约束建模为图结构，并通过多目标优化进行参数微调。
*   **关系图谱表示**：将不安全场景表示为“对象-关系-对象”（O-R-O）三元组。例如，为了遗忘“儿童-饮用-葡萄酒”，框架会构建一个图，包含需要抑制的边（不安全关系）和需要保留的节点（对象本身）以及邻近的良性边（如“成人-饮用-葡萄酒”）。
*   **参数高效编辑（LoRA）**：使用低秩自适应（LoRA）技术对模型的注意力层和投影层进行微调。这种方式不仅计算高效，还能有效隔离修改，减少对模型基础能力的干扰。
*   **复合损失函数**：
    *   **$L_3$ (Unsafe Push)**：推开不安全关系的嵌入向量，降低其余弦相似度。
    *   **$L_1, L_2, L_4$ (Pull Losses)**：拉近并保留安全对象、安全关系及随机中性概念的表示。
    *   **$Lc$ (Consistency)**：确保遗忘后的模型与原模型在安全任务上保持一致。
    *   **$L_{adv}$ (Adversarial)**：针对改写或模糊化的提示词进行防御，增强鲁棒性。

### 3. 实验设计
*   **实验对象**：以 CLIP 模型为实验目标。
*   **实验场景**：设定“小孩吃汉堡”（Kid eating a hamburger）为需要遗忘的“目标关系”（注：实验中将其作为不安全关系的替代进行验证）。
*   **数据集**：使用合成数据。图像由 `gemini-2.0-flash-preview-image-generation` 生成，对应的文本描述由 `gemini-2.5-flash` 生成。
*   **评估基准（Benchmark）**：
    *   **遗忘效果**：测量目标关系的余弦相似度变化（$\Delta cos$）。
    *   **鲁棒性测试**：包括改写攻击（Paraphrase）、上下文攻击（Contextual）和分布外图像攻击（OOD Image）。
    *   **效用保留**：评估单节点保留、新安全边、新安全节点和中性边的性能漂移。
*   **对比方法**：基准方法（Baseline）仅使用 $L_3$（仅推开不安全关系，不考虑保留）。

### 4. 资源与算力
*   **硬件环境**：使用了 Ruby 服务器，配备 **NVIDIA A100 GPU (40GB RAM)**。
*   **算力需求**：由于需要同时激活文本和图像编码器，并进行多次前向传播以计算复合损失，40GB 显存是确保大批量操作和 LoRA 更新稳定的关键。
*   **训练参数**：使用 AdamW 优化器，学习率 $1 \times 10^{-3}$，Batch Size 为 32，训练 3 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文针对三种攻击场景（易、中、难）进行了详细评估，并进行了**消融实验**（Ablation Study）来验证各个损失组件（如一致性损失、对抗损失）的必要性。
*   **充分性评价**：
    *   **优点**：实验涵盖了从文本改写到视觉风格变化的多种攻击手段，证明了方法的鲁棒性。消融实验清晰地展示了单一损失函数会导致严重的“效用坍塌”。
    *   **局限**：实验主要集中在 CLIP 模型和一个特定的关系案例上，缺乏在更大规模生成模型（如 Stable Diffusion 或大型多模态 LLM）上的多案例并行验证。

### 6. 主要结论与发现
*   **精准遗忘**：全优化模型（FULL OPTIMAL）在三种攻击下的余弦相似度显著下降（$\Delta cos$ 达 0.48-0.70），成功实现了遗忘。
*   **效用保持**：相比基准方法，该框架对安全概念的“漂移”极小（绝对漂移量仅在 0.01-0.06 之间），证明了保留项的有效性。
*   **LoRA 的价值**：LoRA 不仅节省内存，且其低秩特性有助于在不破坏模型通用表征的前提下，对特定嵌入空间进行微调。

### 7. 优点
*   **细粒度控制**：首次明确提出了针对“关系”而非“孤立对象”的遗忘学习，解决了多模态安全中的上下文敏感问题。
*   **鲁棒性强**：通过引入对抗损失，能够抵御同义词替换和复杂的视觉干扰（如梵高风格的 OOD 图像）。
*   **自动化数据生成**：利用先进的生成模型构建微调数据集，解决了遗忘学习中特定负样本缺失的问题。

### 8. 不足与局限
*   **案例单一**：实验仅展示了“小孩吃汉堡”这一案例，虽然作为原理验证足够，但在处理更复杂、更隐蔽的不安全关系时（如政治敏感或深层偏见）的效果尚待验证。
*   **模型局限**：主要在判别式模型（CLIP）上验证，尚未在纯生成式多模态模型（如直接修改 U-Net 权重）中进行大规模部署测试。
*   **依赖合成数据**：微调效果高度依赖于合成数据的质量，若生成模型本身存在偏见，可能会影响遗忘学习的效果。

（完）
