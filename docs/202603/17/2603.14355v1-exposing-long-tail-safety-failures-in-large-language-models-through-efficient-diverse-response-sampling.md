---
title: Exposing Long-Tail Safety Failures in Large Language Models through Efficient Diverse Response Sampling
title_zh: 通过高效的多样化响应采样揭示大语言模型中的长尾安全失效
authors: "Suvadeep Hajra, Palash Nandi, Tanmoy Chakraborty"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14355v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 揭示大语言模型中的长尾安全故障和越狱风险
tldr: 本研究发现安全微调仅抑制了不安全行为，使其潜藏在输出分布的长尾中。为此，本文提出渐进式多样化种群采样（PDPS），通过在输出空间进行高效的多样化响应采样来揭示安全漏洞。PDPS 结合随机采样与多样性感知选择，能以极低计算成本显著提升攻击成功率并发现更多样化的违规输出，为评估模型安全性提供了新视角。
selection_source: fresh_fetch
motivation: 现有安全微调仅抑制而非消除不安全行为，且传统红队测试忽视了固定提示词下输出空间的多样性风险。
method: 提出渐进式多样化种群采样（PDPS），通过令牌级采样与多样性感知选择，从候选池中高效提取语义多样的响应子集。
result: "PDPS 以 8%-29% 的计算成本实现了与大规模采样相当的攻击成功率，并在受限设置下比传统方法提升了 26%-40%。"
conclusion: 该方法证明了通过高效探索输出空间可以系统地暴露大模型的长尾安全失效，对提升模型鲁棒性具有重要意义。
---

## 摘要
通过监督微调和来自人类反馈的强化学习进行的安全微调显著提高了大语言模型（LLMs）的鲁棒性。然而，它通常只是抑制而非消除不安全行为，使得罕见但关键的失效隐藏在输出分布的长尾中。虽然大多数红队测试工作强调对抗性提示词搜索（输入空间优化），但我们证明，对于固定的安全关键提示词，通过多样化响应生成（输出空间探索）也可以系统地揭示安全失效，其中增加采样响应的数量和多样性可以将越狱成功率推向接近 1。为了高效地发现此类失效，我们提出了渐进式多样化种群采样（PDPS），它结合了随机词元级采样与多样性感知选择，以探索庞大的候选响应池并保留一个紧凑且语义多样的子集。在多个越狱基准测试和开源大语言模型中，PDPS 实现了与大规模独立同分布（IID）采样相当的攻击成功率，而计算成本仅为后者的 8% 到 29%。在有限响应设置下，其成功率比 IID 采样和多样化束搜索提高了 26% 到 40%。此外，PDPS 生成的响应表现出更高数量和更大多样性的不安全输出，证明了其在揭示更广泛失效方面的有效性。

## Abstract
Safety tuning through supervised fine-tuning and reinforcement learning from human feedback has substantially improved the robustness of large language models (LLMs). However, it often suppresses rather than eliminates unsafe behaviors, leaving rare but critical failures hidden in the long tail of the output distribution. While most red-teaming work emphasizes adversarial prompt search (input-space optimization), we show that safety failures can also be systematically exposed through diverse response generation (output-space exploration) for a fixed safety-critical prompt, where increasing the number and diversity of sampled responses can drive jailbreak success rates close to unity. To efficiently uncover such failures, we propose Progressive Diverse Population Sampling (PDPS), which combines stochastic token-level sampling with diversity-aware selection to explore a large candidate pool of responses and retain a compact, semantically diverse subset. Across multiple jailbreak benchmarks and open-source LLMs, PDPS achieves attack success rates comparable to large-scale IID sampling while using only 8% to 29% of the computational cost. Under limited-response settings, it improves success rates by 26% to 40% over IID sampling and Diverse Beam Search. Furthermore, responses generated by PDPS exhibit both a higher number and greater diversity of unsafe outputs, demonstrating its effectiveness in uncovering a broader range of failures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Exposing Long-Tail Safety Failures in Large Language Models through Efficient Diverse Response Sampling》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 安全对齐（如 SFT 和 RLHF）虽然能显著降低有害输出的概率，但往往只是**抑制**而非完全**消除**了不安全行为。这些不安全响应潜伏在模型输出分布的“长尾”区域。
*   **研究动机**：
    *   **输入空间 vs. 输出空间**：传统的红队测试（Red-Teaming）主要集中在“输入空间优化”（即寻找能绕过过滤器的对抗性提示词）。本文则开辟了“输出空间探索”的新视角——即针对**固定**的安全关键提示词，通过增加采样响应的数量和多样性，系统地诱导模型暴露出潜藏的违规行为。
    *   **效率挑战**：虽然大规模随机采样（IID Sampling）能发现漏洞，但计算成本极高且会产生大量重复的拒绝响应（如“对不起，我不能...”），效率低下。

### 2. 方法论：PDPS 框架
*   **核心思想**：利用不安全响应与标准拒绝响应在语义空间（Embedding Space）通常具有显著差异的特性，通过“扩展-选择”的迭代策略，以极小的计算代价覆盖尽可能广的语义区域。
*   **关键技术细节**：
    1.  **初始化**：针对一个提示词，初始化 $n$ 个短响应候选。
    2.  **迭代扩展（Expansion）**：使用词元级多样化采样（如高温度采样或 Nucleus Sampling）为每个候选生成一小段后续文本（Block）。
    3.  **多样性感知选择（Diversity-aware Selection）**：这是核心步骤。通过解决一个**质量-多样性优化问题（Max-Avg Diversification）**，从候选池中筛选出既有高质量（高似然概率）又具有语义差异（Embedding 距离远）的子集。
    4.  **算法流程**：PDPS 采用渐进式剪枝，初始池很大（如 1024 个），随着生成长度增加，逐步筛选并保留最具有代表性的少数响应（如 16 或 64 个），直到生成完整响应。
*   **理论保证**：该选择问题属于 NP-hard，但论文采用了具有理论保证的贪心算法，确保结果至少能达到全局最优解的 1/2 近似。

### 3. 实验设计
*   **目标模型**：Llama-2-7B-Chat, Llama-2-13B-Chat, Qwen2.5-7B-Instruct, Qwen2.5-14B-Instruct。
*   **数据集（Benchmarks）**：HarmBench, JailbreakBench, AdvBench, MaliciousInstruct（每个数据集随机抽取 100 个样本）。
*   **对比方法**：
    *   **IID Sampling**：独立同分布随机采样（包括 16 次、64 次及作为上限的 1024 次采样）。
    *   **Diverse Beam Search (DBS)**：传统的多样化束搜索。
*   **评估指标**：攻击成功率（ASR）、响应多样性指标（Dist-n, Self-BLEU, 语义余弦距离等）。

### 4. 资源与算力
*   **硬件设备**：论文提到在 **NVIDIA A100 GPU** 上进行了实验。
*   **算力消耗**：
    *   论文强调了 PDPS 的高效性：在达到与 1024 次 IID 采样相当的 ASR 时，PDPS 的计算时间仅为前者的 **8% 到 29%**。
    *   具体训练时长未明确给出，但提供了采样时间随生成长度变化的百分比曲线。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流开源模型和 4 个权威安全基准，共计 16 组模型-数据集组合。
*   **消融与敏感性分析**：
    *   研究了采样温度 ($\tau$)、Nucleus 阈值 ($p$) 以及多样性权重 ($\lambda$) 对结果的影响。
    *   对比了不同生成长度下的计算效率。
*   **充分性评价**：实验设计较为全面，不仅关注 ASR，还深入分析了不安全响应的语义分布和多样性。通过与 1024 次大规模采样的对比，客观地展示了该方法的性能上限。

### 6. 主要结论与发现
1.  **长尾风险真实存在**：即使是经过严格安全对齐的模型，只要采样次数足够多或多样性足够高，越狱成功率（ASR）会单调上升并趋近于 1。
2.  **PDPS 的高效性**：在受限响应预算下（如仅允许生成 16 或 64 条响应），PDPS 的 ASR 比 IID 采样和 DBS 提高了 **26%–40%**。
3.  **语义分离特性**：实验证实，不安全响应在 Embedding 空间中往往形成与拒绝响应分离的簇，这验证了 PDPS 基于语义多样性进行筛选的合理性。
4.  **失效模式更丰富**：PDPS 不仅能提高攻击成功率，还能发现更多样化、更独特的安全失效模式，而 DBS 往往只能产生表面措辞略有不同的重复内容。

### 7. 优点（亮点）
*   **视角独特**：跳出了“卷提示词工程”的怪圈，证明了单纯通过优化解码策略也能系统性地挖掘模型漏洞。
*   **计算友好**：通过渐进式剪枝，解决了大规模采样带来的算力浪费问题，非常适合集成到 RLHF 等资源密集型流水线中。
*   **理论与实践结合**：引入了子集选择理论中的 Max-Avg 优化，并给出了近似保证，使方法更具科学性。

### 8. 不足与局限
*   **Embedding 依赖**：PDPS 的效果高度依赖于所使用的 Embedding（论文使用的是模型最后一层隐藏状态）。如果 Embedding 无法很好地捕捉语义差异，筛选效果会打折扣。
*   **模型覆盖**：主要在开源模型上测试，尚未在闭源模型（如 GPT-4, Claude）上验证其在 API 受限环境下的表现。
*   **判别器偏差**：安全评估依赖于辅助的分类器模型（如 HarmBench 分类器），若分类器本身存在误报或漏报，会影响最终 ASR 的准确性。
*   **极端长尾**：对于极低概率（如 $10^{-6}$ 以下）的失效，PDPS 的初始池规模可能仍需进一步扩大。

（完）
