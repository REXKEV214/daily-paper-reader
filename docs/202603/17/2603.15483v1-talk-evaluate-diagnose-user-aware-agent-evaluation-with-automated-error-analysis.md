---
title: "Talk, Evaluate, Diagnose: User-aware Agent Evaluation with Automated Error Analysis"
title_zh: 交流、评估、诊断：结合自动化错误分析的用户感知型智能体评估
authors: "Penny Chong, Harshavardhan Abichandani, Jiyuan Shen, Atin Ghosh, Min Pyae Moe, Yifan Mai, Daniel Dahlmeier"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15483v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 考虑用户角色与专业知识的智能体评估框架
tldr: "针对现有智能体评估方法碎片化且忽视用户角色的问题，本文提出TED（Talk, Evaluate, Diagnose）框架。该框架通过预设专家与非专家用户画像进行交互，利用LLM对自然语言化的子目标进行自动评分，并引入自动化错误分析工具。研究不仅提供了多维度的性能指标，还通过诊断反馈实现了8-10%的性能提升，为构建更高效、用户感知的智能体评估体系提供了新路径。"
selection_source: fresh_fetch
motivation: 现有的智能体评估方法缺乏统一标准，且未能系统性地考虑用户角色和专业知识对交互质量及效率的影响。
method: 提出TED框架，包含模拟多层级用户画像、利用LLM进行自然语言子目标评分以及自动化错误诊断三个核心模块。
result: "实验显示该框架能深入揭示模型在不同用户背景下的表现差异，并利用诊断反馈使智能体性能提升了8-10%。"
conclusion: TED框架为智能体提供了一个全面、可扩展且具备诊断能力的评估方案，有效促进了智能体设计的针对性优化。
---

## 摘要
智能体应用正越来越多地被用于跨多样化任务的工作流自动化。然而，由于其运行领域具有异构性，创建一个可扩展的评估框架具有挑战性。先前的研究各自采用不同的方法来确定任务成功与否，例如数据库查询、正则表达式匹配等，这增加了开发统一智能体评估方法的复杂性。此外，它们没有系统地考虑交互中用户的角色或专业知识，导致对智能体性能的洞察不够全面。我们认为，有效的智能体评估不应仅局限于正确性，还应包含对话质量、效率以及对智能体错误的系统性诊断。为此，我们引入了 TED 框架（交流、评估、诊断）：(1) 交流（Talk）：我们利用可重用的通用专家和非专家用户画像模板进行用户与智能体的交互。(2) 评估（Evaluate）：我们通过将子目标（如工具签名和响应）表示为自然语言评分笔记来改编现有数据集，并使用 LLM-as-a-judge 进行自动评估。我们提出了新的指标，用于捕捉智能体的轮次效率和中间进展，从而补充用户感知设置。(3) 诊断（Diagnose）：我们引入了一种自动化错误分析工具，用于分析评判者与智能体之间的一致性，揭示常见错误，并为改进智能体提供可操作的反馈。我们展示了 TED 框架揭示了关于不同模型和用户专业水平下智能体性能的新见解。我们还证明，在将识别出的错误补救措施整合到智能体设计中后，智能体性能在所提指标上最高可提升 8-10%。

## Abstract
Agent applications are increasingly adopted to automate workflows across diverse tasks. However, due to the heterogeneous domains they operate in, it is challenging to create a scalable evaluation framework. Prior works each employ their own methods to determine task success, such as database lookups, regex match, etc., adding complexity to the development of a unified agent evaluation approach. Moreover, they do not systematically account for the user's role nor expertise in the interaction, providing incomplete insights into the agent's performance. We argue that effective agent evaluation goes beyond correctness alone, incorporating conversation quality, efficiency and systematic diagnosis of agent errors. To address this, we introduce the TED framework (Talk, Evaluate, Diagnose). (1) Talk: We leverage reusable, generic expert and non-expert user persona templates for user-agent interaction. (2) Evaluate: We adapt existing datasets by representing subgoals-such as tool signatures, and responses-as natural language grading notes, evaluated automatically with LLM-as-a-judge. We propose new metrics that capture both turn efficiency and intermediate progress of the agent complementing the user-aware setup. (3) Diagnose: We introduce an automated error analysis tool that analyzes the inconsistencies of the judge and agents, uncovering common errors, and providing actionable feedback for agent improvement. We show that our TED framework reveals new insights regarding agent performance across models and user expertise levels. We also demonstrate potential gains in agent performance with peaks of 8-10% on our proposed metrics after incorporating the identified error remedies into the agent's design.

---

## 论文详细总结（自动生成）

以下是对论文《Talk, Evaluate, Diagnose: User-aware Agent Evaluation with Automated Error Analysis》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）智能体在自动化工作流中的广泛应用，评估其性能变得愈发困难。现有的评估方法（如数据库状态检查、正则表达式匹配等）通常针对特定领域，缺乏统一性。
*   **核心问题**：
    1.  **忽视用户角色**：现有评估往往忽略了用户专业知识（专家 vs. 菜鸟）对智能体表现的影响。
    2.  **评估维度单一**：大多仅关注最终成功率，忽视了对话质量、任务完成效率及错误的系统性诊断。
    3.  **扩展性差**：依赖复杂的环境访问或硬编码规则，难以跨领域迁移。
*   **整体含义**：本文提出了 **TED 框架**，旨在通过模拟不同用户画像、利用自然语言子目标进行自动评分，并结合自动化错误诊断，构建一个更全面、具备用户感知能力且可扩展的智能体评估体系。

### 2. 方法论
TED 框架包含三个核心阶段：
*   **Talk（交流 - 用户画像模拟）**：
    *   将任务指令与用户画像解耦。通过通用的“专家”和“非专家”模板生成动态用户代理（User Proxy）。
    *   专家用户提供精确信息；非专家用户则表现得含糊、零散，以此测试智能体的鲁棒性和澄清能力。
*   **Evaluate（评估 - 评分笔记与新指标）**：
    *   **评分笔记（Grading Notes）**：将复杂的子目标（如工具调用、特定响应）转化为自然语言检查清单。
    *   **LLM-as-a-judge**：利用 LLM 根据智能体的交互轨迹对评分笔记进行逐项打分，无需访问底层数据库。
    *   **新指标**：
        *   **MaxProgressRate@k**：在 $k$ 次尝试中达到的最大进度。
        *   **MaxAUC@k**：进度随对话轮次变化的曲线下面积，奖励那些能更早完成子目标的智能体。
        *   **MaxPPT@k**：每轮平均进度，衡量任务推进的效率。
*   **Diagnose（诊断 - 自动化错误分析）**：
    *   分析评判者（Judge）的不一致性和智能体在多次尝试中的失败点。
    *   **两步走策略**：首先识别底层错误原因，然后通过语义聚类将相似错误归类，生成可操作的改进建议。

### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau^2$-bench**：涵盖航空（Airline）和零售（Retail）领域，包含简单和困难任务。
    *   **ToolSandbox**：侧重于工具调用能力，涉及联系人更新、消息发送、提醒设置等。
*   **Benchmark 与对比方法**：
    *   对比了原始论文的评估方法（如基于状态的检查）。
    *   评估了多种主流模型作为智能体的表现：GPT-4、GPT-4o、GPT-4o-mini、GPT-5（预览版/占位符）、Mistral-Nemo、Mistral-Large。
*   **实验设置**：每个样本进行多次独立运行（$\tau^2$-bench 为 20 次，$n=k=20$；ToolSandbox 为 8 次，$n=k=8$）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该研究主要基于现有的闭源和开源模型进行推理评估（API 调用），而非从头训练模型，其算力消耗主要集中在推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型基准数据集上进行了广泛测试，涵盖了多个领域和 6 种不同的 LLM 智能体模型。
*   **充分性**：
    *   **多维度对比**：不仅对比了模型，还对比了不同用户画像（专家 vs. 非专家）的影响。
    *   **消融与验证**：进行了人类验证实验，证明了 LLM 评判者、用户代理和错误诊断结果的准确性（Cohen's Kappa 系数显示高度一致）。
    *   **闭环改进**：通过将诊断出的错误反馈回智能体指令中，观察到了明显的性能提升（8-10%），验证了诊断工具的实用性。
*   **客观性**：通过多次运行取平均值/最大值，并使用多数投票机制来缓解 LLM 评判者的随机性。

### 6. 主要结论与发现
1.  **用户画像影响显著**：非专家用户的加入显著降低了智能体的效率（AUC 指标下降），证明了用户感知评估的必要性。
2.  **传统指标的局限**：在简单任务中，成功率往往会饱和（均为 1.0），而 AUC 和 PPT 指标能有效区分不同模型的效率差异。
3.  **模型表现差异**：Mistral 模型在专家模式下表现强劲，但在应对非专家用户的模糊指令时，表现往往逊于 GPT 系列。
4.  **诊断反馈有效**：利用 TED 自动生成的错误分析来优化智能体提示词，可使任务进度和效率提升 8-10%。

### 7. 优点
*   **解耦设计**：将用户行为与任务逻辑分离，使评估更贴近真实世界中用户水平参差不齐的场景。
*   **无需环境依赖**：基于轨迹日志和自然语言评分笔记，极大降低了跨领域部署评估框架的门槛。
*   **闭环诊断**：不仅给出分数，还能自动总结错误模式并提供改进建议，具有极强的工程实践价值。

### 8. 不足与局限
*   **轨迹依赖性**：如果智能体产生了“静默失败”（即数据库状态改变了但未在对话或日志中体现），该框架无法捕捉。
*   **评判者偏差**：虽然引入了多数投票，但评估结果仍受限于作为评判者的 LLM 本身的能力和偏见。
*   **领域局限**：目前主要针对任务导向型对话，对于开放式对话或非工具调用类任务的适用性尚待验证。

（完）
