---
title: Learning Adaptive LLM Decoding
title_zh: 学习自适应大语言模型解码
authors: "Chloe H. Su, Zhe Ye, Samuel Tenka, Aidan Yang, Soonho Kong, Udaya Ghai"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09065v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 基于任务难度的LLM自适应解码策略
tldr: 针对大语言模型解码时固定采样参数无法适应不同任务难度的问题，本文提出了一种学习自适应解码策略的方法。通过引入轻量级解码适配器并利用强化学习训练，该方法能在推理时根据计算资源动态选择采样策略。研究涵盖了序列级和标记级两种粒度，在数学和编程基准测试中显著提升了准确率与计算预算之间的权衡效率，为高效推理提供了新思路。
selection_source: fresh_fetch
motivation: 传统的固定采样超参数无法应对不同提示词和解码步骤中任务难度及不确定性的巨大差异。
method: 引入轻量级解码适配器，通过强化学习在序列级和标记级动态调整采样策略（如贪婪、top-k、min-p）。
result: "在MATH基准测试中，标记级适配器在固定预算下将Pass@1准确率提升了高达10.2%，序列级适配器也带来了显著增益。"
conclusion: 学习自适应解码策略能有效优化LLM的推理效率，在有限的计算预算内实现更高的任务准确性。
---

## 摘要
大语言模型（LLM）的解码通常依赖于固定的采样超参数（如温度、top-p），尽管在不同的提示词和单个解码步骤中，任务难度和不确定性存在显著差异。我们提出学习自适应解码策略，在推理时根据可用的计算资源动态选择采样策略。我们没有微调语言模型本身，而是引入了通过强化学习和可验证的终端奖励（如数学和编程任务的正确性）训练的轻量级解码适配器。在序列层面，我们将解码建模为一个上下文老虎机（contextual bandit）问题：策略根据提示词嵌入和并行采样预算，为每个提示词选择一种解码策略（如贪婪搜索、top-k、min-p）。在标记（token）层面，我们将解码建模为一个部分可观测马尔可夫决策过程（POMDP），策略根据内部模型特征和剩余的标记预算，在每个标记步骤选择采样动作。在 MATH 和 CodeContests 基准测试上的实验表明，学习到的适配器改善了准确率与预算之间的权衡：在 MATH 数据集上，在固定标记预算下，标记级适配器比最佳静态基准线提高了高达 10.2% 的 Pass@1 准确率，而序列级适配器在固定并行采样下获得了 2-3% 的提升。消融分析支持了序列级和标记级自适应的贡献。

## Abstract
Decoding from large language models (LLMs) typically relies on fixed sampling hyperparameters (e.g., temperature, top-p), despite substantial variation in task difficulty and uncertainty across prompts and individual decoding steps. We propose to learn adaptive decoding policies that dynamically select sampling strategies at inference time, conditioned on available compute resources. Rather than fine-tuning the language model itself, we introduce lightweight decoding adapters trained with reinforcement learning and verifiable terminal rewards (e.g. correctness on math and coding tasks). At the sequence level, we frame decoding as a contextual bandit problem: a policy selects a decoding strategy (e.g. greedy, top-k, min-p) for each prompt, conditioned on the prompt embedding and a parallel sampling budget. At the token level, we model decoding as a partially observable Markov decision process (POMDP), where a policy selects sampling actions at each token step based on internal model features and the remaining token budget. Experiments on the MATH and CodeContests benchmarks show that the learned adapters improve the accuracy-budget tradeoff: on MATH, the token-level adapter improves Pass@1 accuracy by up to 10.2% over the best static baseline under a fixed token budget, while the sequence-level adapter yields 2-3% gains under fixed parallel sampling. Ablation analyses support the contribution of both sequence- and token-level adaptation.

---

## 论文详细总结（自动生成）

这篇论文《Learning Adaptive LLM Decoding》探讨了如何通过学习自适应的解码策略来优化大语言模型（LLM）的推理效率与准确性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
**核心问题：**
传统的 LLM 解码通常采用固定的采样超参数（如温度 $T$、Top-p、Top-k），但在实际应用中，不同的提示词（Prompt）难度不一，且同一序列中不同标记（Token）的不确定性也随步骤剧烈波动。固定参数无法在“生成多样性”与“结果准确性”之间取得最优平衡，导致计算资源的浪费或推理性能的下降。

**整体含义：**
本文提出了一种**自适应解码适配器（Decoding Adapters）**框架。其核心意义在于：不改变底层语言模型参数，仅通过训练轻量级的策略网络，在推理时根据**当前上下文特征**和**剩余计算预算**动态调整采样策略，从而在有限的计算资源下最大化任务成功率。

---

### 2. 方法论
论文提出了两种粒度的自适应控制方案，均采用强化学习（RL）进行训练：

*   **序列级适配（Sequence-Level）：上下文老虎机（Contextual Bandit）**
    *   **核心思想：** 为每个 Prompt 选择一个最优的固定采样配置。
    *   **技术细节：** 输入 Prompt 的嵌入向量和并行采样预算 $B$，策略网络 $\pi_\theta$ 预测一个离散动作（如“Greedy”或“Top-p=0.9”）。
    *   **动作空间构建：** 从 180 种候选配置中，利用贪婪子模最大化算法筛选出 4-6 种互补性最强的策略组合。

*   **标记级适配（Token-Level）：部分可观测马尔可夫决策过程（POMDP）**
    *   **核心思想：** 在生成的每一个 Token 步骤动态调整采样参数（主要是温度 $T$）。
    *   **技术细节：** 输入当前 Token 的隐藏层表示和剩余 Token 预算 $b_t$。策略网络在每一步选择一个温度动作。
    *   **训练稳定性：** 引入了“Token 掩码”技术，跳过那些预测概率极度集中（Max Prob > 0.95）的 Token，以减少梯度噪声。

*   **训练算法：**
    *   使用 **REINFORCE** 算法，配合熵正则化（Entropy Regularization）鼓励探索。
    *   **奖励函数：** 采用可验证的终端奖励（Verifiable Rewards），即数学题答案是否正确或代码是否通过测试用例。

---

### 3. 实验设计
*   **数据集：**
    *   **MATH：** 竞赛级数学推理。
    *   **CodeContests：** 算法编程竞赛。
    *   **AIME 2025：** 用于测试跨领域泛化能力的极难数学集。
*   **基准模型（Backbone）：** Qwen3-4B（主要）、Qwen3-8B、Qwen2.5-Math-1.5B。
*   **对比方法（Baselines）：**
    *   **Best Static：** 验证集上表现最好的单一固定采样策略。
    *   **Mixed Static：** 在候选动作空间中进行均匀随机采样的策略。
*   **评估指标：** Pass@1, Pass@8（在固定计算预算下的准确率）。

---

### 4. 资源与算力
*   **框架支持：** 使用 `vLLM` 引擎进行快速序列生成，使用 `Ray Train` 进行分布式数据并行（DDP）训练。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到适配器是轻量级的 MLP，且底层 LLM 处于冻结（Frozen）状态，这暗示了训练开销远小于全量微调或 LoRA 微调。

---

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了不同参数规模的模型（1.5B 到 8B），并在数学和代码两个异质领域进行了验证。
*   **消融实验：** 充分探讨了“预算感知（Budget-aware）”的重要性、仅使用熵（Entropy）作为特征的局限性，以及混合领域训练的效果。
*   **客观性：** 实验采用了 3 个独立随机种子的平均值及 95% 置信区间，并进行了跨数据集的泛化性测试（如 MATH 训练，AIME 测试），结果具有较强的说服力。

---

### 6. 主要结论与发现
*   **性能提升显著：** 在 MATH 任务上，标记级适配器在固定预算下将 Pass@1 提升了 **10.2%**；序列级适配器在并行采样下提升了 2-3%。
*   **预算感知至关重要：** 将“剩余预算”作为输入特征，能显著增强策略在不同推理约束下的鲁棒性。
*   **Token 级控制更优：** 相比于序列级的一次性决策，Token 级的细粒度调整能更好地捕捉推理过程中的“关键分叉点（Forking Tokens）”。
*   **泛化能力：** 在 MATH 上训练的适配器直接应用于 AIME'25 仍能取得超越官方报告的性能，证明了采样规律具有跨任务的通用性。

---

### 7. 优点
*   **非侵入性：** 无需修改 LLM 权重，适合闭源模型（若能获取 Logits）或计算资源受限的场景。
*   **端到端学习：** 避开了人工设计复杂的启发式采样规则，直接通过最终任务的正确性信号进行优化。
*   **计算效率：** 适配器仅为简单的 MLP，推理延迟增加极小，却能显著优化 Token 的分配效率。

---

### 8. 不足与局限
*   **领域限制：** 目前仅在有“客观正确答案”的领域（数学、代码）验证，对于开放式创作、对话等缺乏明确验证信号的任务，其奖励函数的设计将面临挑战。
*   **动作空间离散：** 采样参数仍被限制在预设的离散集合中，未能实现连续空间的参数优化。
*   **训练稳定性：** 论文提到 Token 级训练容易出现高方差，需要复杂的过滤和掩码策略才能收敛，这增加了复现难度。

---
（完）
