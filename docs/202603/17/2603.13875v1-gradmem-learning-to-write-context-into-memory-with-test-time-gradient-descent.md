---
title: "GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent"
title_zh: GradMem：通过测试时梯度下降学习将上下文写入记忆
authors: "Yuri Kuratov, Matvey Kairov, Aydar Bulatov, Ivan Rodkin, Mikhail Burtsev"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13875v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 大语言模型长上下文的压缩记忆
tldr: 针对大语言模型处理长上下文时KV缓存内存开销大的问题，本文提出GradMem。该方法通过在推理时对少量前缀记忆令牌进行梯度下降优化，将上下文信息压缩进固定大小的记忆状态中。相比传统的单向前向写入，GradMem利用自监督重构损失进行迭代纠错，显著提升了记忆容量和检索性能，在合成及自然语言任务上均表现优异。
selection_source: fresh_fetch
motivation: 传统的KV缓存机制内存开销巨大，而现有的压缩记忆方法在信息写入效率和容量扩展方面存在局限。
method: 提出GradMem方法，在模型权重冻结的情况下，通过对前缀记忆令牌执行数步梯度下降，以最小化上下文重构损失的方式写入记忆。
result: 在键值检索任务中，GradMem在相同内存占用下优于前向写入方法，且在bAbI和SQuAD等自然语言任务中展现出极具竞争力的性能。
conclusion: 通过测试时梯度优化实现损失驱动的记忆写入，是一种比纯前向传播更有效且可扩展的上下文压缩方案。
---

## 摘要
许多大语言模型应用需要基于长上下文进行条件生成。Transformer 通常通过存储过去激活的大型逐层 KV 缓存来支持这一点，但这会产生巨大的内存开销。一个理想的替代方案是压缩记忆：一次性读取上下文，将其存储在紧凑的状态中，并根据该状态回答多个查询。我们在上下文移除设置下对此进行研究，即模型在推理时必须在无法访问原始上下文的情况下生成答案。我们引入了 GradMem，它通过逐样本的测试时优化将上下文写入记忆。给定一个上下文，GradMem 在保持模型权重冻结的情况下，对一小组前缀记忆标记（prefix memory tokens）执行几步梯度下降。GradMem 显式地优化模型级的自监督上下文重建损失，从而实现一种具有迭代误差校正功能的损失驱动写入操作，这与仅前向传播的方法不同。在关联键值检索任务上，GradMem 在相同记忆容量下优于仅前向传播的记忆写入方法，且额外的梯度步骤比重复的前向写入能更有效地扩展容量。我们进一步证明 GradMem 可以迁移到合成基准之外：结合预训练语言模型，它在包括 bAbI 和 SQuAD 变体在内的自然语言任务上取得了具有竞争力的结果，且仅依赖于编码在记忆中的信息。

## Abstract
Many large language model applications require conditioning on long contexts. Transformers typically support this by storing a large per-layer KV-cache of past activations, which incurs substantial memory overhead. A desirable alternative is ompressive memory: read a context once, store it in a compact state, and answer many queries from that state. We study this in a context removal setting, where the model must generate an answer without access to the original context at inference time. We introduce GradMem, which writes context into memory via per-sample test-time optimization. Given a context, GradMem performs a few steps of gradient descent on a small set of prefix memory tokens while keeping model weights frozen. GradMem explicitly optimizes a model-level self-supervised context reconstruction loss, resulting in a loss-driven write operation with iterative error correction, unlike forward-only methods. On associative key--value retrieval, GradMem outperforms forward-only memory writers with the same memory size, and additional gradient steps scale capacity much more effectively than repeated forward writes. We further show that GradMem transfers beyond synthetic benchmarks: with pretrained language models, it attains competitive results on natural language tasks including bAbI and SQuAD variants, relying only on information encoded in memory.

---

## 论文详细总结（自动生成）

以下是对论文《GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在处理长上下文时，传统的 **KV-cache（键值缓存）** 机制会产生巨大的内存开销，且难以生成可移植、可复用的上下文表示。
*   **研究动机**：现有的“压缩记忆”方法（如 RMT）通常采用**仅前向（Forward-only）**的写入规则，缺乏对压缩质量的反馈和纠错能力。
*   **整体含义**：本文提出 **GradMem**，将记忆写入过程视为一个**测试时优化（Test-Time Optimization）**问题。通过在推理阶段对少量记忆令牌进行梯度下降，使模型能够迭代地修正写入错误，从而在极小的内存占用下实现更高的信息存储容量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将记忆表示为一组可学习的连续向量（Memory Tokens），在保持模型权重冻结的前提下，通过梯度下降优化这些向量，使其能够“重建”输入的上下文。
*   **关键阶段**：
    1.  **写入阶段 (WRITE)**：给定上下文 $C$，从元学习得到的初始值 $M_0$ 开始，执行 $K$ 步梯度下降，最小化自监督重构损失 $\mathcal{L}_{write}$（即模型预测上下文令牌的交叉熵）。
    2.  **读取阶段 (READ)**：将优化后的记忆 $M_K$ 与查询 $Q$ 拼接，模型在不接触原始上下文 $C$ 的情况下预测目标 $Y$。
*   **元学习 (Meta-learning)**：为了让模型在极少数步数（如 $K \le 5$）内完成高效写入，研究者使用类似 MAML 的算法，在训练阶段通过对写入过程进行二阶求导，联合优化模型参数 $\theta$ 和记忆初始值 $M_0$。
*   **损失驱动**：与前向方法不同，GradMem 的写入是由损失函数驱动的，这意味着它能自动关注那些难以预测（高熵）的信息。

### 3. 实验设计
*   **数据集与场景**：
    *   **关联 KV 检索 (Associative KV-retrieval)**：核心合成基准，测试在固定记忆大小下能存储多少对键值。
    *   **bAbI**：测试多步推理能力。
    *   **Short SQuAD**：测试自然语言问答的抽取能力。
    *   **WikiText-103**：测试语言建模和长程依赖。
*   **对比方法 (Baselines)**：
    *   **Full-Attention Transformer**：性能上限（不压缩）。
    *   **Mamba**：强力的状态空间模型（SSM）基准。
    *   **RMT (Recurrent Memory Transformer)**：代表仅前向写入的压缩记忆方法。
    *   **ARMT**：使用关联矩阵进行前向写入的方法。

### 4. 资源与算力
*   **硬件支持**：实验主要在 **NVIDIA A100 GPU** 上运行。
*   **算力优化**：由于元学习涉及**二阶导数（Double Backward）**，计算开销巨大。作者实现了一套自定义的注意力机制双向反向传播内核（如 Flash HVP），相比 PyTorch 原生实现，在 1024 令牌长度下将内存占用从 60GB 降至 30GB，并显著提升了速度。
*   **训练时长**：文中未明确给出总训练小时数，但强调了通过优化内核才使得这种计算密集型训练变得可行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到自然语言任务的多个维度，并对记忆步数 $K$、记忆令牌数量、上下文长度进行了详细的消融实验。
*   **充分性与客观性**：
    *   实验对比了相同内存大小下的不同写入规则，确保了公平性。
    *   通过在推理时增加步数（$K_{eval} > K_{train}$）展示了计算与精度的权衡，证明了方法的泛化能力。
    *   实验结果给出了均值和标准差，具有统计说服力。

### 6. 主要结论与发现
*   **梯度写入优于前向写入**：在相同记忆大小下，GradMem 的存储容量远超 RMT 等前向方法。
*   **计算可扩展性**：在推理时增加梯度步数能持续提升性能，而重复前向写入（多次阅读上下文）带来的收益微乎其微。
*   **自然语言迁移**：尽管重构损失是任务无关的，但它能有效保留自然语言任务中的关键信息，在 SQuAD 和 bAbI 上达到了与非压缩模型相当的水平。
*   **选择性记忆**：分析发现，GradMem 在优化过程中会优先降低“值（Value）”的损失，显示出一种自动的、任务导向的压缩偏好。

### 7. 优点（亮点）
*   **迭代纠错**：引入了反馈机制，解决了传统压缩记忆方法“写完就走、无法修正”的问题。
*   **计算-容量权衡**：用户可以通过增加推理时的梯度步数来换取更高的记忆精度，无需重新训练。
*   **架构通用性**：该方法不依赖特定的 Transformer 变体，可应用于现有的预训练模型。

### 8. 不足与局限
*   **训练成本高**：二阶导数的计算对显存和算力要求极高，限制了其在大规模模型（如 70B+）上的直接元训练。
*   **写入延迟**：在推理的“写入”阶段，梯度下降比单次前向传播慢得多。虽然可以通过多次查询来摊销成本，但在单次查询场景下缺乏优势。
*   **重构损失的局限**：简单的令牌重构可能不是所有任务的最优目标，对于需要高度抽象而非字面记忆的任务，可能需要更高级的自监督目标。

（完）
