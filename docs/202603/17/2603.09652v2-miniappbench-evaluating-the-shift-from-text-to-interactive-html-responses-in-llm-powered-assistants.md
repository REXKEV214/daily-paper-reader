---
title: "MiniAppBench: Evaluating the Shift from Text to Interactive HTML Responses in LLM-Powered Assistants"
title_zh: MiniAppBench：评估大语言模型驱动的助手中从文本到交互式 HTML 响应的转变
authors: "Zuhao Zhang, Chengyue Yu, Yuante Li, Chenyi Zhuang, Linjian Mo, Shuai Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09652v2"
tags: ["query:hci-ai"]
score: 9.0
evidence: 交互式HTML响应与人机交互
tldr: 随着大语言模型从文本输出转向生成交互式HTML应用（MiniApps），现有的静态评估基准已不再适用。本文提出了MiniAppBench，这是首个针对交互式应用生成的综合基准，包含来自真实场景的500个任务。同时，引入了基于智能体的自动化评估框架MiniAppEval，通过模拟人类探索性测试，从意图、静态和动态三个维度评估应用质量。实验表明当前模型在生成高质量MiniApp方面仍面临挑战。
selection_source: fresh_fetch
motivation: 现有的评估基准主要关注算法正确性或静态布局，无法有效衡量大语言模型生成具有复杂交互逻辑的HTML应用的能力。
method: 提出了包含500个跨领域任务的MiniAppBench基准，并开发了利用浏览器自动化进行探索性测试的智能体评估框架MiniAppEval。
result: 实验发现当前主流大语言模型在生成高质量交互式应用方面仍存在显著困难，而MiniAppEval框架表现出与人类判断的高度一致性。
conclusion: MiniAppBench为评估LLM生成交互式应用的能力建立了可靠标准，推动了人机交互从静态文本向动态应用的范式转变。
---

## 摘要
随着大语言模型（LLMs）在代码生成领域的飞速发展，人机交互正从静态文本响应演变为动态、交互式的 HTML 应用程序，我们将其称为 MiniApps。这些应用程序要求模型不仅能渲染视觉界面，还能构建符合现实世界原则的定制化交互逻辑。然而，现有的基准测试主要关注算法正确性或静态布局重建，未能捕捉到这一新范式所需的能力。为了填补这一空白，我们推出了 MiniAppBench，这是首个旨在评估原则驱动的交互式应用程序生成的综合基准。MiniAppBench 源自一个拥有超过 1000 万次生成的真实应用，从中提取了涵盖游戏、科学和工具等六个领域的 500 个任务。此外，为了应对在缺乏唯一标准答案（ground truth）的情况下评估开放式交互的挑战，我们提出了 MiniAppEval，一个基于智能体（agentic）的评估框架。利用浏览器自动化，它执行类人探索性测试，从意图（Intention）、静态（Static）和动态（Dynamic）三个维度系统地评估应用程序。我们的实验表明，目前的 LLM 在生成高质量 MiniApps 方面仍面临重大挑战，而 MiniAppEval 展示了与人类判断的高度一致性，为未来的研究建立了可靠的标准。我们的代码可在 github.com/MiniAppBench 获取。

## Abstract
With the rapid advancement of Large Language Models (LLMs) in code generation, human-AI interaction is evolving from static text responses to dynamic, interactive HTML-based applications, which we term MiniApps. These applications require models to not only render visual interfaces but also construct customized interaction logic that adheres to real-world principles. However, existing benchmarks primarily focus on algorithmic correctness or static layout reconstruction, failing to capture the capabilities required for this new paradigm. To address this gap, we introduce MiniAppBench, the first comprehensive benchmark designed to evaluate principle-driven, interactive application generation. Sourced from a real-world application with 10M+ generations, MiniAppBench distills 500 tasks across six domains (e.g., Games, Science, and Tools). Furthermore, to tackle the challenge of evaluating open-ended interactions where no single ground truth exists, we propose MiniAppEval, an agentic evaluation framework. Leveraging browser automation, it performs human-like exploratory testing to systematically assess applications across three dimensions: Intention, Static, and Dynamic. Our experiments reveal that current LLMs still face significant challenges in generating high-quality MiniApps, while MiniAppEval demonstrates high alignment with human judgment, establishing a reliable standard for future research. Our code is available in github.com/MiniAppBench.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：MiniAppBench

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）代码生成能力的提升，人机交互范式正从**静态文本响应**转向**动态交互式 HTML 应用**（论文称之为 **MiniApps**）。
*   **研究动机**：现有的代码评估基准（如 HumanEval, MBPP）侧重于算法逻辑，而 Web 生成基准（如 WebGenBench）侧重于静态布局或视觉还原。目前缺乏一个能够评估模型是否能构建“符合现实世界原则”（如物理定律、常识约束）且具备“定制化交互逻辑”的基准。
*   **核心问题**：如何系统地评估 LLM 生成具备复杂交互功能、遵循现实世界原则且满足用户开放式意图的微型应用的能力？

#### 2. 方法论：核心思想与关键技术
论文提出了两个核心组件：**MiniAppBench**（基准数据集）和 **MiniAppEval**（评估框架）。
*   **MiniAppBench 构建（四阶段流水线）**：
    1.  **识别原则驱动的查询**：从千万级真实用户查询中筛选出具有交互潜力和现实原则约束的种子查询。
    2.  **扩展覆盖范围**：通过 LLM 进化增强技术，在保持核心意图的同时扩展任务多样性，并进行标准化改写。
    3.  **构建可验证引用（Eval-Ref）**：由专家引导 LLM 生成针对每个任务的评估参考，涵盖意图、静态和动态约束。
    4.  **平衡难度与领域**：根据基准模型表现进行分层抽样，最终形成 500 个任务，涵盖科学、游戏、工具、人文、生活方式、可视化六大领域。
*   **MiniAppEval 评估框架（智能体化评估）**：
    *   **核心思想**：利用浏览器自动化工具（Playwright）驱动一个 LLM 智能体，像人类测试员一样对生成的 MiniApp 进行探索性测试。
    *   **三个评估维度**：
        *   **意图（Intention）**：应用是否正确理解并实现了用户的高层目标。
        *   **静态（Static）**：代码结构、UI 元素覆盖率及视觉布局的合理性。
        *   **动态（Dynamic）**：通过多步交互验证状态转换的逻辑一致性、鲁棒性及对现实原则的遵循。

#### 3. 实验设计
*   **数据集/场景**：500 个任务，分为 Easy (30%)、Mid (40%)、Hard (30%) 三个难度等级。
*   **对比方法（基准模型）**：共对比了 16 个主流 LLM，包括：
    *   **闭源模型**：GPT-5.2/5.1、Claude-Opus/Sonnet-4.5、Gemini-3-Pro/Flash、MiniMax-M2.1、Grok-4.1 等。
    *   **开源模型**：Qwen3 系列（32B, 235B, Coder-480B）、GLM-4.7/4.5-Air、Kimi-K2 等。
*   **评估指标**：通过 MiniAppEval 给出 0-1 分值，设定阈值（0.8）计算通过率（Pass Rate），并分析 Token 消耗和推理时间。

#### 4. 资源与算力
*   **训练算力**：论文未提及模型训练过程，因此**未说明训练所用的 GPU 型号或时长**。
*   **推理/评估开销**：论文详细分析了评估阶段的成本。MiniAppEval 运行了 44,981 次有效测试，分析了 Token 消耗与步骤数的关系。评估模型主要使用 Gemini-3-Pro-Preview 作为 Judge。

#### 5. 实验数量与充分性
*   **实验规模**：对 16 个模型在 500 个任务上进行了全面测试，并进行了 183 个样本的消融实验。
*   **消融实验**：验证了“代码检查”、“智能体交互”和“评估引用（Eval-Ref）”三个组件的必要性，结果显示缺少任何一项都会显著降低评估准确率。
*   **客观性与公平性**：
    *   引入了**双盲评审（Double Blind Judge）**机制以减少视觉任务中的确认偏差。
    *   进行了**人类一致性研究**：4 名专家对 549 个输出进行了 2196 次标注，Fleiss' Kappa 达到 0.89，证明了自动化评估框架与人类判断的高度一致性（Cohen's Kappa 0.81-0.89）。

#### 6. 主要结论与发现
*   **任务极具挑战性**：所有模型的平均通过率仅为 17.05%，即使是最强的 GPT-5.2 也仅达到 45.46%，说明生成高质量 MiniApp 对当前 LLM 仍是巨大挑战。
*   **闭源 vs 开源**：闭源模型在处理复杂逻辑和现实原则方面显著优于开源模型，且差距比传统代码基准更明显。
*   **领域差异**：模型在“可视化”和“生活方式”领域表现较好，但在需要深厚领域知识和复杂工程细节的“科学”和“工具”领域表现较差。
*   **成本相关性**：性能与 Token 消耗（相关系数 0.84）和推理时间（0.74）呈强正相关。

#### 7. 优点（亮点）
*   **范式领先**：率先关注从文本到“可执行世界模型”（HTML/JS）的交互范式转变。
*   **真实性高**：任务源自千万级真实用户查询，而非人工合成，具有极高的实际应用参考价值。
*   **评估创新**：MiniAppEval 结合了静态代码分析与动态 Playwright 交互，解决了开放式生成任务缺乏标准答案（Ground Truth）的评估难题。

#### 8. 不足与局限
*   **单文件限制**：目前主要评估单文件 HTML 构件，对于需要多文件协作或复杂后端支持的大型 Web 应用覆盖不足。
*   **Judge 模型依赖**：评估结果高度依赖于作为 Judge 的 LLM 的能力，可能存在潜在的模型偏见。
*   **视觉评估局限**：虽然引入了双盲机制，但对于极其复杂的图形渲染和审美判断，纯文本/DOM 驱动的智能体可能仍存在感知盲区。

（完）
