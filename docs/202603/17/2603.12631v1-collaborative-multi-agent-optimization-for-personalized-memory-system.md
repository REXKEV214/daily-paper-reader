---
title: Collaborative Multi-Agent Optimization for Personalized Memory System
title_zh: 个性化记忆系统的协同多智能体优化
authors: "Wenyu Mao, Haoyang Liu, Zhao Liu, Haosong Tan, Yaorui Shi, Jiancan Wu, An Zhang, Xiang Wang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12631v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 用于长期用户-大模型对话的个性化记忆系统
tldr: 针对个性化大模型在长程对话中的记忆管理挑战，本文提出CoMAM框架。传统方法往往独立优化多智能体，忽略了其间的协作效应。CoMAM通过强化学习将智能体执行建模为顺序马尔可夫决策过程，整合局部任务奖励与全局QA准确率，并利用自适应权重分配贡献度，实现了多智能体的联合优化，显著提升了记忆系统的整体性能。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆系统侧重于独立优化单个智能体，缺乏对跨智能体协作及全局系统性能的有效整合。
method: 提出CoMAM框架，将智能体交互建模为顺序MDP，并基于局部与全局奖励的一致性动态分配权重进行联合强化学习。
result: 实验证明CoMAM在多项指标上优于现有的主流记忆系统，验证了协同优化在提升记忆构建与检索准确性方面的有效性。
conclusion: 通过协同强化学习对多智能体进行联合优化，可以有效对齐局部任务与全局目标，从而显著增强个性化LLM的长程记忆能力。
---

## 摘要
记忆系统通过缓解捕获长期用户与大语言模型（LLM）对话时的上下文窗口限制，对个性化 LLM 至关重要。通常，此类系统利用多个智能体来处理多粒度的记忆构建和个性化记忆检索任务。为了优化系统，现有方法侧重于通过提示工程或微调，独立地使智能体专门化于其局部任务。然而，它们忽视了跨智能体协作，而局部智能体的独立优化难以保证全局系统性能。为解决这一问题，我们提出了一种用于多智能体记忆系统的协同强化学习框架（CoMAM），通过联合优化局部智能体来促进协作。具体而言，我们将智能体的执行正则化为序列马尔可夫决策过程（MDP），以将智能体间的依赖关系嵌入到状态转移中，从而产生局部任务奖励（例如记忆构建的信息覆盖率）和全局奖励（即问答准确率）。然后，我们通过局部奖励与全局奖励之间的群体级排名一致性来量化每个智能体的贡献，并将其作为自适应权重来分配全局信用并整合局部-全局奖励。每个智能体都通过这些整合后的奖励进行优化，使局部改进与全局性能保持一致。实验表明，CoMAM 优于领先的记忆系统，验证了我们提出的用于联合优化的协同强化学习的有效性。

## Abstract
Memory systems are crucial to personalized LLMs by mitigating the context window limitation in capturing long-term user-LLM conversations. Typically, such systems leverage multiple agents to handle multi-granular memory construction and personalized memory retrieval tasks. To optimize the system, existing methods focus on specializing agents on their local tasks independently via prompt engineering or fine-tuning. However, they overlook cross-agent collaboration, where independent optimization on local agents hardly guarantees the global system performance. To address this issue, we propose a Collaborative Reinforcement Learning Framework for Multi-Agent Memory Systems (CoMAM), jointly optimizing local agents to facilitate collaboration. Specifically, we regularize agents' execution as a sequential Markov decision process (MDP) to embed inter-agent dependencies into the state transition, yielding both local task rewards (e.g., information coverage for memory construction) and global rewards (i.e., query-answer accuracy). Then, we quantify each agent's contribution via group-level ranking consistency between local and global rewards, treating them as adaptive weights to assign global credit and integrate local-global rewards. Each agent is optimized by these integrated rewards, aligning local improvements with the global performance. Experiments show CoMAM outperforms leading memory systems, validating the efficacy of our proposed collaborative reinforcement learning for joint optimization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Collaborative Multi-Agent Optimization for Personalized Memory System》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何优化用于个性化大语言模型（LLM）的多智能体记忆系统，以解决长程对话中的上下文限制。
*   **研究背景**：现有的记忆系统通常由多个专门的智能体组成（如负责记忆构建的智能体和负责检索的智能体）。
*   **研究动机**：
    *   **独立优化的局限性**：现有方法倾向于独立优化单个智能体（通过提示工程或微调），但这无法保证全局系统性能。例如，构建智能体可能存储了冗余信息，虽然其局部指标良好，却干扰了检索智能体的效率。
    *   **协作缺失**：系统被视为“局部专家的集合”而非“协同进化的团队”，导致智能体间策略冲突。

### 2. 方法论：CoMAM 框架
CoMAM（Collaborative Multi-Agent Memory system）的核心思想是通过协同强化学习实现多智能体的联合优化。
*   **智能体定义**：
    1.  **提取智能体（Extraction Agent）**：从原始对话中提取细粒度事实。
    2.  **画像智能体（Profile Agent）**：从细粒度记忆中抽象出粗粒度的用户偏好。
    3.  **检索智能体（Retrieval Agent）**：检索相关记忆并生成最终回答。
*   **关键技术细节**：
    *   **执行轨迹正则化（MDP）**：将异步、异构的智能体执行过程建模为一个顺序马尔可夫决策过程（MDP）。这使得智能体间的依赖关系被嵌入到状态转移中，支持端到端的联合训练。
    *   **自适应信用分配（Adaptive Credit Assignment）**：
        *   **局部奖励**：针对每个任务设计（如提取的覆盖率、画像的合理性、检索的精准度）。
        *   **全局奖励**：最终问答的准确率。
        *   **对齐机制**：利用 **NDCG（归一化折损累计增益）** 衡量局部奖励排名与全局奖励排名的一致性，以此作为权重动态分配全局奖励给各个智能体。
    *   **优化算法**：采用 **GRPO（群体相对策略优化）**，通过组内相对奖励计算优势函数，无需额外的 Critic 网络，提高了训练效率。

### 3. 实验设计
*   **数据集**：使用 **PersonaMem** 基准测试，包含 180 多个长期对话历史和约 6,000 个多项选择题。
*   **实验场景**：设置了三种不同的历史长度：**32K、128K 和 1M tokens**，涵盖 7 种个性化查询类型（如事实召回、偏好演变跟踪等）。
*   **对比方法（Baselines）**：
    *   **无记忆方法**：Base（直接输入）、RAG（原始检索）。
    *   **基于提示的记忆系统**：CAM、MemoryBank、A-Mem。
    *   **基于强化学习的记忆系统**：Mem1、Memory-R1（独立优化智能体的代表）。
*   **模型骨干**：采用了 Qwen2.5 (3B/7B) 和 Llama-3.1/3.2 (3B/8B) 系列模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到了智能体分别使用 3B 和 7B/8B 参数规模的模型，上下文窗口配置为 32K。
*   **训练配置**：Batch size 为 128，Group size 为 8，训练轮数为 5 轮。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种上下文长度、2 种模型家族、7 种查询类型下进行了全面测试。
*   **消融实验**：
    *   **智能体贡献分析**：验证了提取、画像、检索三个智能体的必要性。
    *   **MDP 正则化分析**：对比了独立 RL 与 MDP 联合 RL 的效果。
    *   **信用分配分析**：对比了仅局部奖励、仅全局奖励、平均分配及自适应分配的效果。
*   **客观性**：所有结果均为至少三次独立运行的平均值，并提供了收敛曲线和效率对比，实验设计较为充分且客观。

### 6. 主要结论与发现
*   **性能领先**：CoMAM 在所有设置下均显著优于现有基准，相比最强的独立优化 RL 方法（Memory-R1），准确率提升了 **8.5% 至 16.7%**。
*   **联合优化优于独立优化**：实验证明，即使局部智能体训练到收敛，如果缺乏联合优化，全局性能仍会受限。
*   **自适应分配的有效性**：通过 NDCG 量化贡献度，能有效解决多智能体强化学习中的信用分配模糊问题，同时提升局部任务能力和全局协作水平。
*   **训练效率**：虽然联合训练单步更复杂，但由于并行更新，总收敛步数往往少于依次独立优化各智能体的时间。

### 7. 优点与亮点
*   **系统性视角**：首次在个性化记忆系统领域深入探讨了“局部最优不等于全局最优”的问题，并给出了联合优化的解决方案。
*   **数学建模严谨**：将复杂的异步智能体交互转化为顺序 MDP，并引入 NDCG 进行信用分配，方法论具有创新性。
*   **泛化性强**：在不同规模（3B 到 8B）和不同家族（Qwen, Llama）的模型上均表现出一致的增益。

### 8. 不足与局限
*   **静态 MDP 限制**：目前的 MDP 模型是静态顺序的，而现实中记忆系统的调用频率和更新可能是动态触发的。
*   **智能体任务范围有限**：目前仅关注提取、画像和检索，未涉及记忆冲突解决、冗余删除等更复杂的管理任务。
*   **局部-局部对齐缺失**：虽然实现了局部与全局的对齐，但智能体之间（局部与局部）的显式对齐尚未在奖励函数中直接体现。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
