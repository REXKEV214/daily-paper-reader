---
title: "Fusian: Multi-LoRA Fusion for Fine-Grained Continuous MBTI Personality Control in Large Language Models"
title_zh: Fusian：大语言模型中细粒度连续 MBTI 人格控制的多 LoRA 融合框架
authors: "Zehao Chen, Rong Pan"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15405v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 大模型细粒度连续人格控制
tldr: 针对大语言模型在人格模拟中缺乏连续强度控制的问题，本文提出Fusian框架。该框架通过在微调过程中收集LoRA适配器轨迹来映射人格特征流形，并利用强化学习训练策略网络动态融合多个适配器。实验证明，Fusian能在Qwen3-14B模型上实现高精度的MBTI人格强度连续控制，显著优于传统的离散分类方法，为实现更细腻的人机交互奠定了基础。
selection_source: fresh_fetch
motivation: 现有的人格控制方法通常将人格视为离散类别，无法在连续维度上精确控制人格特征的强度。
method: 通过收集微调过程中的LoRA适配器序列捕捉特征演变，并利用强化学习训练策略网络动态计算适配器融合权重。
result: 在Qwen3-14B模型上的实验表明，Fusian在对齐用户指定的数值人格强度方面具有极高的精度，显著优于基准方法。
conclusion: Fusian为大语言模型提供了一种细粒度、连续的人格控制方案，有效提升了模型模拟复杂人类行为的能力。
---

## 摘要
大语言模型（LLMs）在模拟多样化的人类行为和人格方面展现出了令人印象深刻的能力。然而，现有的性格控制方法（包括提示工程和标准有监督微调 SFT）通常将人格特质视为离散类别（例如“外向”与“内向”），缺乏在连续光谱上精确控制特质强度的能力。在本文中，我们提出了 Fusian，这是一个用于大语言模型细粒度、连续人格控制的新型框架。Fusian 分两个阶段运行：(1) 轨迹采集（Trajectory Collection），通过保存一系列 LoRA 适配器来捕捉 SFT 过程中人格采纳的动态演变，从而有效地映射特质的连续流形；(2) 基于强化学习（RL）的动态融合（RL-based Dynamic Fusion），利用强化学习训练策略网络，为这些冻结的适配器动态计算混合权重。通过从由策略网络参数化的狄利克雷分布（Dirichlet distribution）中进行采样，Fusian 融合了多个适配器，使模型的输出与特定的数值目标强度保持一致。在 Qwen3-14B 模型上的实验表明，Fusian 在人格控制方面实现了高精度，在与用户指定的特质强度对齐方面显著优于基线方法。

## Abstract
Large Language Models (LLMs) have demonstrated impressive capabilities in simulating diverse human behaviors and personalities. However, existing methods for personality control, which include prompt engineering and standard Supervised Fine-Tuning (SFT), typically treat personality traits as discrete categories (e.g., "Extroverted" vs. "Introverted"), lacking the ability to precisely control the intensity of a trait on a continuous spectrum. In this paper, we introduce Fusian, a novel framework for fine-grained, continuous personality control in LLMs. Fusian operates in two stages: (1) Trajectory Collection, where we capture the dynamic evolution of personality adoption during SFT by saving a sequence of LoRA adapters, effectively mapping the continuous manifold of a trait; and (2) RL-based Dynamic Fusion, where we train a policy network using Reinforcement Learning to dynamically compute mixing weights for these frozen adapters. By sampling from a Dirichlet distribution parameterized by the policy network, Fusian fuses multiple adapters to align the model's output with a specific numerical target intensity. Experiments on the Qwen3-14B model demonstrate that Fusian achieves high precision in personality control, significantly outperforming baseline methods in aligning with user-specified trait intensities.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Fusian**，一个旨在实现大语言模型（LLM）细粒度、连续人格特质控制的创新框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 在模拟人类人格方面表现出色，但现有方法（如提示工程或标准 SFT）通常将人格视为**离散类别**（如“内向”或“外向”）。
*   **核心问题**：人类人格本质上是连续且有强度的（如“78% 的外向度”）。现有方法难以在连续光谱上精确控制人格特质的强度，数值提示词往往被模型忽略或解释不一致，而 SFT 往往收敛于单一的极端特质。
*   **研究意义**：通过实现连续强度控制，可以使 AI 助手在心理咨询、教育支持等需要细腻情感调节的场景中表现得更自然、更具共情力。

### 2. 方法论
Fusian 框架分为两个核心阶段：
*   **阶段 1：轨迹采集（Trajectory Collection）**
    *   **核心思想**：假设 SFT 的训练过程实际上是在遍历一个连续的人格流形。
    *   **技术细节**：在对特定人格特质进行微调时，以高频率保存 LoRA 适配器（Checkpoints）。通过标准 MBTI 测试对每个适配器进行评分，计算其特质百分比（Trait Percentage）。
    *   **稳定基底选择**：利用滑动窗口过滤掉不稳定的适配器，并使用均匀值采样选择 $N$ 个适配器作为“基底”，覆盖从低到高的完整强度范围。
*   **阶段 2：基于强化学习的动态融合（RL-based Dynamic Fusion）**
    *   **核心思想**：训练一个策略网络，学习如何动态混合这些基底适配器，以匹配目标强度。
    *   **算法流程**：
        1.  策略网络（MLP）接收目标强度作为输入，输出狄利克雷分布（Dirichlet distribution）的参数。
        2.  从该分布中采样得到混合权重 $w$。
        3.  将 $N$ 个 LoRA 适配器按权重线性融合。
        4.  **奖励机制**：使用 REINFORCE 算法优化，奖励函数结合了指数衰减惩罚（针对偏差）和高精度奖励（针对极小误差），迫使模型实现精确对齐。

### 3. 实验设计
*   **模型与数据**：使用 **Qwen3-14B** 作为基座模型。实验基于 **Personality Control Datasets**，涵盖 MBTI 的四个维度（E/I, S/N, T/F, P/J）。
*   **评估指标**：
    *   **平均绝对误差 (MAE)**：衡量控制的精确度。
    *   **皮尔逊相关系数 ($r$)**：衡量控制的线性度和单调性。
*   **对比基准 (Benchmarks)**：
    *   **提示工程**：包括 Qwen3-14B 和 GPT-5-mini（注：论文设定在 2026 年背景）。
    *   **标准 LoRA**：选择最接近目标的单个 Checkpoint。
    *   **PISF**：一种现有的 LLM 人格控制方法。
    *   **人格向量 (P-Vector)**：通过缩放差异向量进行模型合并的方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了 LoRA 的秩（rank=8），策略网络为 128 维隐藏层的 MLP，训练了 20 个 Epoch，使用了 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 MBTI 的 4 个维度（共 8 个特质方向）上进行了全面测试。
*   **消融实验**：针对“动态融合”、“稳定基底选择”和“奖励函数设计”进行了三组消融实验，验证了各组件的必要性。
*   **案例研究**：通过“情感支持”场景的定性分析，展示了模型随强度增加从“逻辑分析”向“情感共鸣”的平滑过渡。
*   **评价**：实验设计较为充分，涵盖了定量指标、定性分析和内部组件验证，对比基准也包含了当前最先进的参数空间操作方法，具有较高的客观性。

### 6. 主要结论与发现
*   **高精度控制**：Fusian 在所有维度上的 MAE 显著低于基线，整体 MAE 仅为 6.79，而 GPT-5-mini 提示词方法为 14.97。
*   **线性相关性强**：皮尔逊系数达到 0.88，证明了 Fusian 能够提供平滑、单调的“人格调节旋钮”。
*   **非线性映射**：实验证明参数空间与语义强度之间是非线性关系，简单的线性插值效果远不如经过 RL 优化的动态融合。
*   **维度差异**：发现“直觉 (N)”维度最难控制，但 Fusian 依然在该维度上保持了 0.79 的高相关性，远超其他方法。

### 7. 优点
*   **创新视角**：将 SFT 训练轨迹视为连续流形，巧妙地利用了微调过程中的中间状态。
*   **控制粒度**：突破了离散分类的限制，实现了真正的数值化连续控制。
*   **鲁棒性**：通过稳定基底选择和狄利克雷采样，解决了参数融合中的噪声和非线性对齐问题。

### 8. 不足与局限
*   **多维度耦合**：目前框架一次只能控制一个 MBTI 维度，如何同时精确控制多个相互作用的维度（如同时调整外向度和理性度）尚待研究。
*   **计算开销**：虽然推理时只需加载融合后的 LoRA，但在 RL 训练阶段需要频繁调用模型进行 MBTI 测试评分，离线训练成本较高。
*   **语言限制**：实验主要集中在特定数据集上，对于跨语言或更广泛的人格框架（如大五人格）的泛化能力有待验证。

（完）
