---
title: "Deactivating Refusal Triggers: Understanding and Mitigating Overrefusal in Safety Alignment"
title_zh: 停用拒绝触发器：理解与缓解安全对齐中的过度拒绝问题
authors: "Zhiyu Xue, Zimo Qi, Guangliang Liu, Bocheng Chen, Ramtin Pedarsani"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11388v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 缓解大语言模型安全对齐中的过度拒绝问题
tldr: 本研究探讨了大语言模型在安全对齐后出现的过度拒绝问题。作者发现，安全训练使模型将特定的语言线索（拒绝触发词）与拒绝响应关联，但这些线索往往包含非有害成分，导致误伤良性查询。为此，本文提出了一种在微调中显式考虑拒绝触发词的缓解策略。实验证明，该方法在防御攻击与保持良性响应之间取得了更优的平衡，有效提升了模型的实用性。
selection_source: fresh_fetch
motivation: 安全对齐虽然能防御有害请求，但常导致模型过度拒绝良性查询，降低了实际应用中的可用性。
method: 通过分析发现拒绝触发词包含非有害线索，并据此提出在安全微调中显式处理这些触发词的优化方法。
result: 实验结果显示，该方法在抵御越狱攻击的同时，显著提高了对良性查询的响应率，优于现有基准。
conclusion: 深入理解并区分拒绝触发词中的有害与非有害成分，是解决安全对齐中过度拒绝问题的关键。
---

## 摘要
安全对齐旨在通过在有害查询与拒绝回答配对的数据上进行后训练，确保大语言模型（LLMs）拒绝有害请求。尽管安全对齐在工业界被广泛采用，但过度拒绝问题（即对齐后的 LLMs 在安全对齐后训练后也会拒绝良性查询）仍未得到充分研究。这一问题降低了安全对齐在实际应用中的可用性。在本文中，我们研究了安全对齐下过度拒绝是如何产生的，并根据我们的发现提出了一种缓解策略。我们将拒绝触发器定义为训练数据中诱发拒绝回答的语言线索；安全对齐鼓励 LLMs 将训练样本中的拒绝触发器与拒绝回答关联起来，从而引导对齐后的 LLMs 拒绝有害查询。然而，拒绝触发器不仅包含有害的语言线索，还包含非有害线索，从而导致对良性查询的过度拒绝。基于这一机理分析，我们提出了一种在安全对齐微调中显式考虑拒绝触发器的方法。实验结果表明，我们的方法在防御越狱攻击与对良性查询的响应性之间实现了更好的权衡，优于现有方法。警告：本文包含有害且带有偏见的句子。

## Abstract
Safety alignment aims to ensure that large language models (LLMs) refuse harmful requests by post-training on harmful queries paired with refusal answers. Although safety alignment is widely adopted in industry, the overrefusal problem where aligned LLMs also reject benign queries after safety alignment post-training, remains insufficiently studied. Such an issue degrades the usability of safety alignment in real-world applications. In this paper, we examine how overrefusal arises under safety alignment, and propose a mitigation strategy inspired by our findings. We define refusal triggers as linguistic cues in the training data that elicit refusal responses, safety alignment encourages LLMs to associate refusal triggers within a training sample with refusal responses, leading aligned LLMs to refuse harmful queries. However, the refusal triggers include not only harmful linguistic cues but also non-harmful cues, therefore causing overrefusal to benign queries. Building on this mechanistic analysis, we propose a method that explicitly considers refusal triggers in the safety alignment fine-tuning. Empirical results demonstrate that our approach achieves a more favorable trade-off between defense against jailbreak attacks and responsiveness to benign queries, outperforming prior methods. Warning: this paper contains harmful and biased sentences.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）在安全对齐过程中产生的“过度拒绝”（Overrefusal）现象，并提出了一种基于“拒绝触发器”分析的缓解方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：安全对齐（Safety Alignment）虽然能有效防御有害请求和越狱攻击，但往往会导致模型“草木皆兵”，即对大量无害的良性查询也表现出拒绝态度（过度拒绝）。
*   **研究动机**：现有的缓解方案（如加入通用良性数据进行正则化）效果有限，且缺乏对过度拒绝产生机制的深入理解。作者试图从机械论（Mechanistic）角度解释为什么安全训练会导致模型误伤良性指令。
*   **核心概念**：**拒绝触发器（Refusal Triggers）**。作者认为，训练数据中不仅有害意图会诱发拒绝，一些中性的语言线索（如“帮我”、“制作视频”、“SQL注入”中的“注入”等）也会因为在安全训练集中频繁与拒绝回答配对，而成为诱发拒绝的“触发器”。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：识别并提取有害训练样本中的非有害语言线索（拒绝触发器），然后显式地教导模型对这些触发器给出肯定回答，从而打破无害线索与拒绝行为之间的错误关联。
*   **关键技术步骤**：
    1.  **触发器提取**：利用 GPT-4o 从有害训练集 $D_h$ 中提取“脱敏”后的查询。通过指令要求模型保留原查询的语境、事件和话语结构，但移除有害意图。
    2.  **语义层级重写**：为了测试敏感度，作者将触发器重写为三个层级（Level 1-3），语义相似度依次递减。
    3.  **触发器感知微调**：在安全对齐的损失函数中，将提取出的拒绝触发器作为良性数据集 $D_b$。
*   **公式说明**：
    采用加权损失函数：$L = \alpha \cdot L_{harmful}(D_h) + (1-\alpha) \cdot L_{benign}(D_b)$。
    其中 $D_b$ 不再仅仅是通用的 Alpaca 数据，而是与有害数据分布高度相关的“拒绝触发器”数据。

### 3. 实验设计
*   **模型选择**：Llama2-7b-chat、Llama3-8B-Uncensored、Qwen2.5-7B-Instruct-Uncensored。
*   **对齐方法**：涵盖了三种主流技术：监督微调（SFT）、预填式监督微调（P-SFT）和基于规则奖励的强化学习（RLVR）。
*   **数据集与 Benchmark**：
    *   **训练数据**：Llama2 Safety Data（有害）、Alpaca（通用良性）、作者生成的触发器匹配数据（针对性良性）。
    *   **安全评估（ASR ↓）**：Sorry-Bench、JBench-H、HEx-PHI。
    *   **过度拒绝评估（RR ↓）**：Koala、JBench-B、GSM-8K（数学）、SQL-1K（代码）、OrBench-H（高难度过度拒绝基准）。
*   **对比方法**：基准模型（未对齐）、使用通用良性数据（Alpaca）对齐的模型、使用作者提出的触发器数据对齐的模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量、显存占用或具体的训练时长。这在一定程度上限制了对该方法计算效率的直接评估，但考虑到其微调的数据量较小（仅 248 条触发器样本），推测其算力需求并不高。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 个不同家族的模型上进行了实验，涵盖了从 SFT 到 RL 的多种对齐框架。
*   **分析深度**：
    *   **行为分析**：验证了不同重写层级的触发器对拒绝率的影响。
    *   **表征分析**：通过计算隐藏状态（Hidden States）的余弦相似度，证明了被拒绝的良性查询在向量空间中确实更接近训练集中的拒绝触发器。
*   **客观性与公平性**：实验对比了多种 Benchmark，不仅关注通用对话，还涵盖了数学和代码等特定领域，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **机制发现**：过度拒绝是由良性查询与安全训练中习得的“拒绝触发器”在语义空间上的接近性驱动的。
*   **有效性验证**：使用极少量的（约 248 条）针对性触发器数据，其缓解过度拒绝的效果远好于使用数万条通用的 Alpaca 数据。
*   **领域差异**：在 SQL 和数学领域，过度拒绝尤为严重（因为术语重叠，如 SQL 的 "drop"），而该方法在这些领域的提升最为显著。
*   **安全权衡**：虽然该方法显著降低了过度拒绝率，但在某些情况下会导致攻击成功率（ASR）轻微上升，体现了安全与实用性之间的固有张力。

### 7. 优点（亮点）
*   **视角新颖**：从“拒绝触发器”这一细粒度视角切入，比传统的“增加良性数据”更具解释力。
*   **高效性**：证明了“数据质量胜过数量”，仅需少量精准匹配的脱敏数据即可大幅改善模型表现。
*   **理论支撑**：结合了分布语义学理论，并通过隐藏状态分析提供了实证支持，而非单纯的经验性调参。

### 8. 不足与局限
*   **依赖外部模型**：拒绝触发器的提取高度依赖 GPT-4o，如果外部模型提取不彻底（残留有害信息）或过度脱敏，会影响训练效果。
*   **评估局限**：主要依赖基于规则和关键词的自动检测（ASR/RR），可能无法完全捕捉到复杂的语义拒绝或微妙的安全边界。
*   **算力细节缺失**：缺乏训练开销的具体数据，不便于复现者评估成本。
*   **规模化挑战**：对于不断演进的越狱攻击，如何动态且自动化地更新拒绝触发器库仍是一个挑战。

（完）
