---
title: "From Storage to Steering: Memory Control Flow Attacks on LLM Agents"
title_zh: 从存储到操控：针对大语言模型智能体的内存控制流攻击
authors: "Zhenlin Xu, Xiaogang Zhu, Yu Yao, Minhui Xue, Yiliao Song"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15125v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 记忆检索对LLM智能体控制流和持久行为的影响
tldr: "本研究揭示了针对大语言模型（LLM）智能体的新型安全威胁——内存控制流攻击（MCFA）。研究发现，内存检索可以主导智能体的控制流，使其违背用户指令并产生持久的行为偏差。为此，作者开发了自动化评估框架 MEMFLOW，并在 LangChain 和 LlamaIndex 平台上对 GPT-5 mini 等主流模型进行了测试。实验结果显示，即使在严格的安全约束下，超过 90% 的测试仍易受攻击，凸显了智能体内存机制中存在的重大安全风险。"
selection_source: fresh_fetch
motivation: 现有的安全分析忽略了内存检索对智能体控制流的持久影响，未能识别出内存可能被用来操纵工具调用的风险。
method: 提出了 MEMFLOW 自动化评估框架，通过模拟长周期交互来系统地量化和识别内存控制流攻击。
result: "在主流 LLM 和开发框架上的测试显示，超过 90% 的尝试成功实施了 MCFA 攻击，证明了该漏洞的普遍性。"
conclusion: 内存检索机制的安全缺陷可能导致智能体行为失控，必须重新审视并加强智能体内存系统的安全防护。
---

## 摘要
现代智能体系统允许大语言模型（LLM）智能体通过广泛使用工具来处理复杂任务，从而形成工具选择与执行的结构化控制流。现有的安全分析通常将这些控制流视为短暂的一次性会话，忽略了内存的持久影响。本文识别出一种来自内存控制流攻击（MCFA）的新威胁，即内存检索可以主导控制流，甚至在违背用户明确指令的情况下强制执行非预期的工具使用，并导致跨任务的持久行为偏差。为了理解该漏洞的影响，我们进一步设计了 MEMFLOW，这是一个自动化评估框架，能够系统地识别和量化跨异构任务和长交互周期的 MCFA。为了评估 MEMFLOW，我们针对来自两个主要 LLM 智能体开发框架（LangChain 和 LlamaIndex）的真实工具，对包括 GPT-5 mini、Claude Sonnet 4.5 和 Gemini 2.5 Flash 在内的最先进 LLM 进行了攻击。结果显示，即使在严格的安全约束下，通常也有超过 90% 的测试容易受到 MCFA 的攻击，这凸显了需要立即关注的关键安全风险。

## Abstract
Modern agentic systems allow Large Language Model (LLM) agents to tackle complex tasks through extensive tool usage, forming structured control flows of tool selection and execution. Existing security analyses often treat these control flows as ephemeral, one-off sessions, overlooking the persistent influence of memory. This paper identifies a new threat from Memory Control Flow Attacks (MCFA) that memory retrieval can dominate the control flow, forcing unintended tool usage even against explicit user instructions and inducing persistent behavioral deviations across tasks. To understand the impact of this vulnerability, we further design MEMFLOW, an automated evaluation framework that systematically identifies and quantifies MCFA across heterogeneous tasks and long interaction horizons. To evaluate MEMFLOW, we attack state-of-the-art LLMs, including GPT-5 mini, Claude Sonnet 4.5 and Gemini 2.5 Flash on real-world tools from two major LLM agent development frameworks, LangChain and LlamaIndex. The results show that in general over 90% trials are vulnerable to MCFA even under strict safety constraints, highlighting critical security risks that demand immediate attention.

---

## 论文详细总结（自动生成）

### 论文总结：从存储到操控——针对大语言模型智能体的内存控制流攻击（MCFA）

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体（Agent）能力的增强，它们通过调用外部工具来处理复杂任务，形成了结构化的“控制流”（即工具选择与执行顺序）。为了实现个性化和跨任务的一致性，智能体通常配备了**长期内存（Long-term Memory）**。
*   **研究动机**：现有的安全研究（如提示词注入）大多将智能体交互视为孤立、短暂的会话，忽略了内存的持久性影响。
*   **核心问题**：论文识别出一种新型威胁——**内存控制流攻击（MCFA）**。攻击者通过标准交互将恶意指令植入智能体内存，利用内存检索机制在后续任务中劫持智能体的工具调用逻辑，使其违背用户明确指令或绕过安全约束。

#### 2. 论文提出的方法论
论文通过形式化定义和自动化框架系统地研究了 MCFA。
*   **核心思想**：将内存视为一种“写一次，读多次”的控制信号。通过污染内存，攻击者可以诱导智能体形成持久的行为偏差。
*   **关键技术细节**：
    *   **形式化定义**：将内存增强型智能体建模为受内存检索驱动的状态机，定义了攻击的两阶段过程：注入（写入恶意策略）和触发（检索并执行偏差逻辑）。
    *   **因果验证定理（Memory-Causal Verification）**：提出通过“上下文重置（Context Reset）”策略，清除短期对话历史，从而证明观察到的行为偏差严格归因于长期内存的检索。
    *   **MEMFLOW 框架**：一个自动化评估框架，包含：
        1.  **场景生成层**：动态生成模拟真实用户偏好的注入指令。
        2.  **AgentRunner**：适配 LangChain 和 LlamaIndex 的统一交互接口。
        3.  **追踪审计流水线（Trace-audited Pipeline）**：通过分析工具调用轨迹（Tool-call Trace）而非文本输出来判定攻击是否成功。
*   **攻击族分类**：定义了五种攻击类型：**Override**（工具覆盖）、**Order**（顺序重排）、**M-Scope**（跨任务扩展）、**Persistence**（长周期持久性）和 **Resistance**（抗修复性）。

#### 3. 实验设计
*   **模型（Backbones）**：GPT-5 mini、Claude Sonnet 4.5、Gemini 2.5 Flash，以及开源模型 Qwen 3.5。
*   **框架与工具**：基于 LangChain 和 LlamaIndex 框架，从其官方工具库（LlamaHub 等）中采样了 36 个真实工具。
*   **Benchmark 设置**：
    *   **对比维度**：检索强度（强、弱、关闭）、工具标签（有害、中性）、交互模式（隔离模式 vs. 在线模式）。
    *   **防御对比**：测试了生产级别的**角色基内存隔离（RBMS）**防御机制，即将系统规则与用户偏好存储在不同通道并设定优先级。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练时长。
*   **原因分析**：由于该研究主要侧重于对现成商业模型和开源模型的推理测试及 API 调用，不涉及大规模的模型预训练或微调，因此算力消耗主要集中在推理成本上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个最先进的闭源模型和 1 个主流开源模型。
    *   在两个主流智能体开发框架上进行了跨平台验证。
    *   针对 5 种攻击族进行了详尽的测试，每种配置下均包含多轮试验。
*   **充分性与客观性**：实验设计了严谨的消融实验（如关闭检索以排除模型固有幻觉的影响），并引入了防御性实验。通过“隔离模式”确保了结果的客观性，证明了偏差确实源于内存污染。整体实验设计非常充分且具有统计学意义（ASR 普遍超过 90%）。

#### 6. 主要结论与发现
*   **内存主导性**：一旦开启内存检索，内存中的恶意指令往往会覆盖系统提示词中的安全约束，攻击成功率（ASR）接近 100%。
*   **跨任务传播**：一次内存注入即可导致智能体在完全不同的任务领域中产生行为偏差（M-Scope 成功率极高）。
*   **持久性与抗修复性**：攻击效果不会随时间自然衰减，且简单的文本纠正（如告诉智能体“不要这样做”）无法根除污染，系统极易复发（Relapse）。
*   **防御局限性**：即使采用 RBMS 隔离机制，智能体在面对强语气、行动导向的恶意内存时，仍可能违反优先级指令，防御并非万无一失。

#### 7. 优点
*   **视角新颖**：首次将安全分析从单次会话扩展到持久化的内存控制流。
*   **理论严谨**：提供了形式化的安全定义和因果验证定理，为智能体安全审计提供了数学基础。
*   **工具实用**：开发的 MEMFLOW 框架具有高度的自动化和通用性，可直接用于评估不同架构的智能体安全性。
*   **现实意义强**：直接针对 LangChain 等主流框架和 GPT-5 等前沿模型，揭示了当前智能体部署中迫切需要解决的架构缺陷。

#### 8. 不足与局限
*   **防御手段单一**：虽然测试了 RBMS，但尚未探索更深层的内存清理或加密验证机制。
*   **环境复杂性有限**：实验主要在受控的工具调用环境中进行，未充分考虑多模态输入（如图像注入）或动态变化的外部环境对内存检索的影响。
*   **模型依赖性**：虽然测试了多个模型，但攻击效果仍受限于底层 LLM 的指令遵循能力和检索算法的实现细节。

（完）
