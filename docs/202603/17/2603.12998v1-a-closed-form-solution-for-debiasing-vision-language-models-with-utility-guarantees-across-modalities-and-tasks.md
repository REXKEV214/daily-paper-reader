---
title: A Closed-Form Solution for Debiasing Vision-Language Models with Utility Guarantees Across Modalities and Tasks
title_zh: 一种具有跨模态和跨任务效用保证的视觉-语言模型去偏闭式解
authors: "Tangzheng Lian, Guanyu Hu, Yijing Ren, Dimitrios Kollias, Oya Celiktutan"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12998v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 具有效用保证的视觉语言模型去偏方法
tldr: 视觉语言模型（VLM）常携带社会偏见，且现有去偏方法缺乏效用保留的理论保证。本文提出一种无需训练且无需标注数据的去偏方法，通过在跨模态空间中推导闭式解，实现了帕累托最优公平性并提供了理论上的效用损失边界。该方法能同时处理视觉和文本模态，在零样本分类、检索和生成等任务中，在保持模型性能的同时显著提升了公平性。
selection_source: fresh_fetch
motivation: 现有的VLM去偏方法往往无法在提升公平性的同时，从理论上保证模型原有任务性能不被过度损害。
method: 提出一种在跨模态空间中具有闭式解的去偏方法，通过数学优化实现帕累托最优公平性并严格限制效用损失。
result: 实验证明该方法在零样本分类、图文检索和图像生成等任务中，在多种公平性指标上均优于现有方法，且有效保留了模型效用。
conclusion: 该研究为VLM提供了一种无需训练、具有理论保障且适用于多模态任务的高效去偏解决方案。
---

## 摘要
尽管视觉-语言模型（VLM）在各种下游任务中取得了卓越的性能，但最近的研究表明，它们可能会继承训练数据中的社会偏见，并将其进一步传播到下游应用中。为了解决这一问题，研究者提出了各种去偏方法，但其中大多数旨在提高公平性，却缺乏模型效用得以保留的理论保证。在本文中，我们介绍了一种在跨模态空间中产生闭式解的去偏方法，在有界效用损失的情况下实现了帕累托最优公平性。我们的方法是无需训练的，不需要标注数据，并且可以跨下游任务共同对视觉和文本模态进行去偏。大量实验表明，在零样本图像分类、文本到图像检索和文本到图像生成等下游任务中，我们的方法在多种公平性指标和数据集上，针对群体公平性和交叉性公平性的 VLM 去偏效果均优于现有方法，同时保留了任务性能。

## Abstract
While Vision-Language Models (VLMs) have achieved remarkable performance across diverse downstream tasks, recent studies have shown that they can inherit social biases from the training data and further propagate them into downstream applications. To address this issue, various debiasing approaches have been proposed, yet most of them aim to improve fairness without having a theoretical guarantee that the utility of the model is preserved. In this paper, we introduce a debiasing method that yields a \textbf{closed-form} solution in the cross-modal space, achieving Pareto-optimal fairness with \textbf{bounded utility losses}. Our method is \textbf{training-free}, requires \textbf{no annotated data}, and can jointly debias both visual and textual modalities across downstream tasks. Extensive experiments show that our method outperforms existing methods in debiasing VLMs across diverse fairness metrics and datasets for both group and \textbf{intersectional} fairness in downstream tasks such as zero-shot image classification, text-to-image retrieval, and text-to-image generation while preserving task performance.

---

## 论文详细总结（自动生成）

以下是对论文《A Closed-Form Solution for Debiasing Vision-Language Models with Utility Guarantees Across Modalities and Tasks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言模型（VLM，如 CLIP）在海量互联网数据上预训练时，不可避免地继承了社会偏见（如性别、种族、年龄等）。现有的去偏方法存在以下痛点：
    1.  **效用损失**：提高公平性往往以牺牲模型性能（效用）为代价，且缺乏理论上的效用保留保证。
    2.  **成本高昂**：许多方法需要重新训练网络或微调，计算开销大。
    3.  **数据依赖**：通常需要带有敏感属性标注的数据集，这涉及隐私和伦理风险。
    4.  **任务局限**：多数方法仅针对单一任务（如分类或检索），难以跨模态、跨任务通用。
*   **研究背景**：本文旨在提出一种**无需训练、无需标注数据**的通用去偏框架，通过数学推导给出**闭式解（Closed-form Solution）**，在保证公平性的同时，从理论上限定效用损失的边界。

### 2. 论文提出的方法论
该方法主要分为两个核心步骤：

*   **LLM 引导的群体原型构建（Group Prototype Construction）**：
    *   利用大语言模型（如 GPT-5）的推理能力，根据输入提示词（如“医生的照片”）生成不同敏感群体（如男性、女性）的变体提示词。
    *   通过 LLM 生成多种表达方式（如“男医生的照片”、“男性形象医生的照片”），计算这些文本嵌入的球面平均值，构建代表特定属性特征的“群体原型”。
*   **跨模态空间中的去偏嵌入搜索**：
    *   **核心思想**：将去偏问题转化为在单位超球面上寻找一个最优嵌入向量 $\vec{u}$，使其与属性子空间（由群体原型之差张成）正交，同时与原始嵌入 $\vec{e}$ 保持最大相似度。
    *   **数学建模**：构建了一个加权优化问题，平衡“属性泄漏（偏见）”和“自效用损失（语义保留）”。
    *   **闭式解推导**：通过正交分解定理，将高维搜索简化为 2D 单位圆上的标量优化，最终推导出 $\alpha^*$（属性泄漏程度）的闭式解。该解被证明是**帕累托最优（Pareto-optimal）**的，即在给定公平性水平下，效用损失最小。

### 3. 实验设计
*   **下游任务**：
    1.  **零样本图像分类**：预测发色等属性，评估公平性指标（EO 违规）。
    2.  **文本到图像检索**：在候选集中检索匹配图像，评估 MaxSkew 和 Recall@K。
    3.  **文本到图像生成**：使用 Stable Diffusion v2.1 生成图像，评估统计平价（SP）和 CLIP 分数。
*   **数据集**：
    *   分类：**CelebA**（人脸）、**FACET**（多样化职业/场景）。
    *   检索：**Flickr30K**、**COCO2017**。
    *   生成：使用 34 种职业提示词进行生成。
*   **对比方法（Benchmarks）**：
    *   包括 SFID、DeAR、CLIP-clip、FairerCLIP、PRISM、RoboShot、PromptArray、Orth-Proj 等近几年主流的 VLM 去偏算法。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或推理总时长。
*   **计算特性**：由于该方法是 **Training-free（无需训练）** 且具有闭式解，其核心开销仅在于：
    1.  调用 LLM 生成提示词变体（一次性操作）。
    2.  特征提取（VLM 编码器推理）。
    3.  矩阵运算（计算投影和闭式解公式）。
*   相比需要训练辅助网络的方法，该方法的算力需求极低，属于轻量级插件式方案。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三大主流 VLM 任务。
    *   使用了 4 个大型公开数据集。
    *   针对**群体公平性**和更复杂的**交叉性公平性**（如性别×种族、性别×年龄）均进行了验证。
    *   进行了消融实验，验证了 LLM 引导原型的有效性以及双模态去偏的必要性。
*   **充分性评价**：实验设计非常充分。通过自助法（Bootstrapping）报告均值和方差，确保了结果的统计可靠性。对比的方法涵盖了从“需标注+需训练”到“无需标注+无需训练”的各类代表性工作，对比公平且客观。

### 6. 论文的主要结论与发现
*   **性能领先**：在所有任务中，该方法在提升公平性指标的同时，对任务性能（如 F1、Recall、CLIP Score）的保留效果显著优于现有方法。
*   **理论验证**：证明了跨模态效用损失（Cross-Utility Loss）受限于自效用损失（Self-Utility Loss），且闭式解能有效控制这一边界。
*   **双模态必要性**：实验发现，仅对文本或仅对图像去偏是不够的，同时处理两种模态能获得最佳的公平性增益。
*   **LLM 鲁棒性**：敏感性分析显示，使用不同的 LLM（如 DeepSeek v3, Gemini 2.5）对最终去偏效果影响较小，说明该框架对 LLM 的选择具有鲁棒性。

### 7. 优点
*   **理论严谨**：是首个为 VLM 去偏提供闭式解并给出效用损失理论边界的工作。
*   **高效便捷**：无需训练、无需额外标注数据，易于集成到现有的预训练模型流水线中。
*   **通用性强**：不仅支持单一属性去偏，还能处理复杂的交叉属性偏见，且适用于生成式和判别式多种任务。
*   **帕累托最优**：在数学上保证了公平与效用权衡的最优性。

### 8. 不足与局限
*   **几何空间局限**：效用保证是在嵌入向量的几何空间（余弦相似度）内定义的，虽然实验证明了其有效性，但理论上并未直接保证下游任务特定指标（如准确率）的绝对不下降。
*   **解码器未覆盖**：该方法主要作用于 VLM 的编码器（特征空间），对于生成任务中的图像/文本解码器内部可能存在的偏见未做直接干预。
*   **属性覆盖**：受限于现有公平性基准数据集，实验主要集中在性别、种族、年龄等常见属性，对于更隐蔽或长尾的社会偏见覆盖有限。

（完）
