---
title: Are Dilemmas and Conflicts in LLM Alignment Solvable? A View from Priority Graph
title_zh: 大语言模型对齐中的困境与冲突是否可解？基于优先级图的视角
authors: "Zhenheng Tang, Xiang Liu, Qian Wang, Eunsol Choi, Bo Li, Xiaowen Chu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15527v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: LLM 对齐冲突与安全对齐
tldr: 本研究探讨了大语言模型在对齐过程中面临的冲突与困境。作者通过构建“优先级图”模型，将指令和价值观建模为节点，分析其在不同上下文中的优先级动态。研究揭示了对齐的非稳定性及“优先级劫持”漏洞，即攻击者可通过伪造上下文操纵模型选择。为此，提出了一种运行时验证机制以增强鲁棒性。最后指出，部分伦理困境具有哲学上的不可还原性，是AI对齐的长期挑战。
selection_source: fresh_fetch
motivation: 随着大语言模型能力的提升，模型在复杂场景中频繁遭遇指令与价值观之间的冲突，亟需系统化的建模与解决框架。
method: 提出优先级图模型，将指令和价值观作为节点，通过模型输出分布来刻画特定上下文下的优先级关系。
result: 揭示了对齐的不稳定性及潜在的优先级劫持风险，并开发了通过外部源校验上下文的运行时验证机制。
conclusion: 统一且稳定的对齐极具挑战，技术手段虽能提升安全性，但深层伦理困境仍是AI对齐领域长期存在的开放性问题。
---

## 摘要
随着大语言模型（LLMs）变得日益强大且自主，它们在许多场景中面临着越来越多的冲突与困境。我们首先对这些多样的冲突进行了总结和分类。随后，我们将 LLM 在做出不同选择时的偏好建模为优先级图，其中指令和价值观作为节点，边则代表由模型输出分布决定的特定上下文优先级。该图揭示了实现统一且稳定的 LLM 对齐极具挑战性，因为该图在不同上下文中既非静态，也不一定保持一致。此外，它还揭示了一个潜在的漏洞：优先级劫持（priority hacking），即攻击者可以构建欺骗性的上下文来操纵该图，从而绕过安全对齐。为应对这一问题，我们提出了一种运行时验证机制，使 LLM 能够查询外部资源以锚定其上下文并抵御操纵。尽管这种方法增强了鲁棒性，但我们也承认，许多伦理和价值观困境在哲学上是不可还原的，这为 AI 对齐的未来提出了一个长期的、开放性的挑战。

## Abstract
As Large Language Models (LLMs) become more powerful and autonomous, they increasingly face conflicts and dilemmas in many scenarios. We first summarize and taxonomize these diverse conflicts. Then, we model the LLM's preferences to make different choices as a priority graph, where instructions and values are nodes, and the edges represent context-specific priorities determined by the model's output distribution. This graph reveals that a unified stable LLM alignment is very challenging, because the graph is neither static nor necessarily consistent in different contexts. Besides, it also reveals a potential vulnerability: priority hacking, where adversaries can craft deceptive contexts to manipulate the graph and bypass safety alignments. To counter this, we propose a runtime verification mechanism, enabling LLMs to query external sources to ground their context and resist manipulation. While this approach enhances robustness, we also acknowledge that many ethical and value dilemmas are philosophically irreducible, posing a long-term, open challenge for the future of AI alignment.

---

## 论文详细总结（自动生成）

这篇论文由香港科技大学、纽约大学和新加坡国立大学的研究人员合作完成，探讨了大语言模型（LLM）在对齐过程中面临的深层冲突与困境。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 LLM 变得更加自主，它们经常面临相互冲突的指令、价值观和知识。论文探讨了这些冲突是否可以通过技术手段彻底解决，还是在哲学上具有不可还原性。
*   **研究动机**：现有的对齐研究多关注简单的指令遵循（如 Helpful, Honest, Harmless），但在复杂现实中，不同价值观（如隐私 vs. 帮助、安全 vs. 正义）往往会发生碰撞。作者试图通过系统化的分类和建模，揭示对齐的脆弱性并寻找应对方案。

### 2. 核心方法论：优先级图（Priority Graph）
*   **冲突分类学**：论文将冲突分为五类：
    1.  **指令冲突**：多轮对话或长提示词中相互矛盾的命令。
    2.  **信息冲突**：模型内部参数化知识与外部检索信息（RAG）的冲突。
    3.  **伦理困境**：经典的道德难题（如电车难题）。
    4.  **价值困境**：两个正面价值观之间的权衡（如诚实 vs. 保护他人免受伤害）。
    5.  **偏好困境**：不同用户之间主观且多样化的偏好。
*   **优先级图模型**：
    *   定义图 $G_C = (V, E_C)$，其中 $V$ 是指令或价值观节点，$E_C$ 是在特定上下文 $C$ 下的优先级边。
    *   **数学表达**：若模型在上下文 $C$ 中面对 $A_1$ 和 $A_2$ 时，其输出分布 $p_\theta$ 使得测量函数 $M(D, A_1, C) > M(D, A_2, C)$，则存在边 $A_1 \succ A_2$。
    *   **动态性与一致性**：该图随上下文 $C$ 动态变化，且可能存在环（如 $A \succ B \succ C \succ A$），导致逻辑上的不一致。
*   **优先级劫持（Priority Hacking）**：攻击者通过伪造高优先级的上下文（如“为了正义”或“为了公共卫生”），诱导模型将恶意请求的优先级置于安全对齐之上，从而实现绕过。
*   **运行时验证机制**：提出 LLM 应具备主动连接现实世界的能力，通过查询外部可信源来验证用户提供的上下文 $C$ 是否属实，从而抵御欺骗性操纵。

### 3. 实验设计与场景分析
*   **研究性质**：本文属于**概念性分析与框架论文**，侧重于理论建模和案例分析，而非大规模的基准测试对比。
*   **分析场景**：
    *   **电车难题**：分析模型在功利主义与义务论之间的选择。
    *   **网络钓鱼案例**：展示攻击者如何利用“调查记者揭露腐败（正义感）”的虚假背景，诱导模型生成钓鱼邮件。
    *   **医疗研究案例**：探讨“诚实”与“保护病童”之间的价值权衡。
*   **引用基准**：论文引用了 `DailyDilemmas`（日常困境）、`IHEval`（指令层级评估）、`PRISM`（偏好对齐）等数据集来支持其分类和论点。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于本文的核心贡献在于提出“优先级图”这一理论框架和分类体系，不涉及大规模的模型预训练或微调实验，因此对算力的依赖较低。

### 5. 实验数量与充分性
*   **实验规模**：论文通过多个具体的、具有代表性的定性案例（Case Studies）来验证其分类学和优先级劫持理论。
*   **充分性评价**：
    *   对于**理论构建**而言，其逻辑推导和案例选择较为充分，涵盖了从逻辑冲突到深层哲学冲突的各个维度。
    *   对于**防御机制**（运行时验证），论文仅提出了概念性方案，缺乏大规模的定量实验来证明该机制在不同攻击场景下的召回率和误报率。

### 6. 主要结论与发现
*   **对齐的挑战性**：由于优先级图是动态且可能不一致的，实现一个统一、静态且完美的对齐几乎是不可能的。
*   **安全漏洞**：揭示了“优先级劫持”这一新型攻击路径，证明了仅靠内部对齐无法完全防御复杂的上下文操纵。
*   **哲学不可还原性**：技术手段（如运行时验证）可以解决事实层面的欺骗，但无法解决深层的伦理冲突（如价值多元化），这些冲突在哲学上是长期存在的。

### 7. 优点
*   **系统化的视角**：首次将 LLM 的各种冲突统一在“优先级图”这一数学框架下，为理解对齐问题提供了清晰的逻辑工具。
*   **敏锐的漏洞洞察**：提出的“优先级劫持”概念非常具有前瞻性，揭示了安全对齐中被忽视的上下文操纵风险。
*   **跨学科结合**：成功地将计算机科学的对齐问题与伦理学、哲学中的经典命题结合起来。

### 8. 不足与局限
*   **缺乏定量验证**：作为一篇观点/框架类论文，缺乏对所提防御机制的实证数据支持。
*   **实施难度**：提出的“运行时验证”依赖于外部可信知识库的完备性，在处理虚构文学创作或高度主观的讨论时可能失效。
*   **未给出终极方案**：论文承认部分冲突不可解，虽然诚实地反映了现实，但未能为开发者提供在面对不可解困境时的具体操作指南（如模型应如何拒绝或回应）。

（完）
