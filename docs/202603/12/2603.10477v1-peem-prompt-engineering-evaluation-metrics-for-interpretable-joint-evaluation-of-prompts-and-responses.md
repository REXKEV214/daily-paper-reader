---
title: "PEEM: Prompt Engineering Evaluation Metrics for Interpretable Joint Evaluation of Prompts and Responses"
title_zh: PEEM：用于提示词与响应可解释联合评估的提示工程评估指标
authors: "Minki Hong, Eunsoo Lee, Sohyun Park, Jihie Kim"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10477v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 提示词公平性与语言质量的评估指标
tldr: 针对大语言模型提示词评估仅关注结果准确性而缺乏可解释性的问题，本文提出PEEM框架。该框架通过9个维度（3个提示词维度和6个响应维度）对提示词和模型回复进行联合评估，利用LLM生成1-5分评分及解释性理由。实验表明PEEM与传统指标高度一致，且能有效指导提示词优化，在零样本重写任务中显著提升了下游任务的准确率，为LLM交互提供了系统化的诊断工具。
selection_source: fresh_fetch
motivation: 现有的提示词评估方法主要关注答案正确性，忽略了提示词本身质量对结果的影响，且缺乏可操作的改进建议。
method: 提出PEEM框架，通过定义提示词和响应的9个评估维度，利用大模型生成量化评分及基于准则的自然语言解释。
result: PEEM与传统准确率指标高度相关（相关系数约0.97），且基于其反馈的提示词优化效果优于监督学习和强化学习基准。
conclusion: PEEM建立了一种将提示词表述与响应行为联系起来的可解释评估协议，能够有效支持大模型交互的系统化诊断与自动优化。
---

## 摘要
提示词设计是大语言模型（LLMs）的主要控制接口，然而标准的评估方法很大程度上将性能简化为答案的正确性，掩盖了提示词成功或失败的原因，且几乎无法提供可操作的指导。我们提出了 PEEM（提示工程评估指标），这是一个用于对提示词和响应进行联合且可解释评估的统一框架。PEEM 定义了一个包含 9 个维度的结构化评分标准：3 个提示词准则（清晰度/结构、语言质量、公平性）和 6 个响应准则（准确性、连贯性、相关性、客观性、清晰度、简洁性），并使用基于 LLM 的评估器输出 (i) 1-5 分李克特量表（Likert scale）的标量分数，以及 (ii) 基于评分标准的特定准则自然语言理由。在 7 个基准测试和 5 个任务模型中，PEEM 的准确性维度与传统准确率高度一致，同时保留了模型排名（综合 Spearman rho 约为 0.97，Pearson r 约为 0.94，p < 0.001）。一项涉及四个模型的多评估器研究显示了一致的相对判断（两两 rho = 0.68-0.85），支持了与评估器无关的部署。除了对齐性之外，PEEM 还能捕捉互补的语言失败模式，并在提示词扰动下保持信息量：提示词质量趋势在迭代重写下追踪下游准确率，语义对抗操纵导致明显的分数下降，而保持原意的改写则表现出高度稳定性（鲁棒率约为 76.7-80.6%）。最后，仅使用 PEEM 分数和理由作为反馈，零样本提示词重写循环将下游准确率提高了多达 11.7 个百分点，优于有监督和基于强化学习（RL）的提示词优化基准。总的来说，PEEM 提供了一个可重复的、准则驱动的协议，将提示词表述与响应行为联系起来，并实现了对 LLM 交互的系统化诊断和优化。

## Abstract
Prompt design is a primary control interface for large language models (LLMs), yet standard evaluations largely reduce performance to answer correctness, obscuring why a prompt succeeds or fails and providing little actionable guidance. We propose PEEM (Prompt Engineering Evaluation Metrics), a unified framework for joint and interpretable evaluation of both prompts and responses. PEEM defines a structured rubric with 9 axes: 3 prompt criteria (clarity/structure, linguistic quality, fairness) and 6 response criteria (accuracy, coherence, relevance, objectivity, clarity, conciseness), and uses an LLM-based evaluator to output (i) scalar scores on a 1-5 Likert scale and (ii) criterion-specific natural-language rationales grounded in the rubric. Across 7 benchmarks and 5 task models, PEEM's accuracy axis strongly aligns with conventional accuracy while preserving model rankings (aggregate Spearman rho about 0.97, Pearson r about 0.94, p < 0.001). A multi-evaluator study with four models shows consistent relative judgments (pairwise rho = 0.68-0.85), supporting evaluator-agnostic deployment. Beyond alignment, PEEM captures complementary linguistic failure modes and remains informative under prompt perturbations: prompt-quality trends track downstream accuracy under iterative rewrites, semantic adversarial manipulations induce clear score degradation, and meaning-preserving paraphrases yield high stability (robustness rate about 76.7-80.6%). Finally, using only PEEM scores and rationales as feedback, a zero-shot prompt rewriting loop improves downstream accuracy by up to 11.7 points, outperforming supervised and RL-based prompt-optimization baselines. Overall, PEEM provides a reproducible, criterion-driven protocol that links prompt formulation to response behavior and enables systematic diagnosis and optimization of LLM interactions.

---

## 论文详细总结（自动生成）

### PEEM: 提示工程评估指标——提示词与响应的可解释联合评估

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的性能高度依赖于提示词（Prompt）的设计，但现有的评估实践主要集中在输出结果的**准确性（Accuracy）**或**精确匹配（Exact Match）**上。这种“以输出为中心”的评估方式存在三大缺陷：
*   **黑盒化**：无法解释为什么某个提示词有效或失效。
*   **维度单一**：忽略了提示词本身的语言质量、结构和公平性，也忽略了响应的连贯性、客观性等非准确性维度。
*   **缺乏指导性**：无法为提示词的迭代优化提供具体、可操作的反馈。
**PEEM** 旨在建立一个统一的、可解释的框架，通过对提示词和响应进行联合评估，提供量化分数和定性理由，从而弥补这一空白。

#### 2. 方法论：核心思想与算法流程
PEEM 的核心思想是利用高性能 LLM 作为评估器，基于结构化的评分标准（Rubric）进行**零样本（Zero-shot）评估**。
*   **评估维度（9个轴）**：
    *   **提示词准则（3个）**：清晰度与结构（Clarity/Structure）、语言质量（Linguistic Quality）、公平性（Fairness）。
    *   **响应准则（6个）**：准确性（Accuracy）、连贯性（Coherence）、相关性（Relevance）、客观性（Objectivity）、清晰度（Clarity）、简洁性（Conciseness）。
*   **输出形式**：对于每个维度，评估器输出 1-5 分的李克特量表分数，并附带一段**自然语言理由（Rationale）**，解释评分依据。
*   **算法流程**：
    1.  将提示词 $P$（指令+查询）和模型生成的响应 $a$ 输入评估器。
    2.  评估器根据预定义的系统提示模板（包含各维度的详细定义），逐项打分并生成理由。
    3.  （可选）反馈循环：将 PEEM 生成的分数和理由作为上下文，输入重写模型（Rewrite Model）自动优化提示词。

#### 3. 实验设计
*   **数据集**：涵盖分类、推理、常识和情感分析，包括 AG News, ARC-Challenge, ARC-Easy, BigBench-Hard (BBH), GSM8K, MMLU, SST-2。
*   **任务模型**：Gemma-2-9B-IT, LLaMA-3.1-8B-IT, Qwen-2.5-7B-IT, GPT-4o-mini, Gemini-2.5-Flash。
*   **评估器模型**：默认使用 GPT-4o-mini，并使用 Gemini-2.5-Flash 等其他 4 个模型进行跨评估器一致性测试。
*   **对比基准（Benchmarks）**：
    *   **对齐性对比**：传统 Accuracy。
    *   **评估方法对比**：GPTScore, G-EVAL, ChatGPT-as-a-Judge。
    *   **优化方法对比**：AutoPrompt, RLPrompt, TEMPERA, PRewrite。
*   **鲁棒性测试**：包括语义保持的改写（Paraphrase）和语义对抗扰动（误导、矛盾、信息不足、越狱提示词）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本与效率**：文中提到使用 GPT-4o-mini 作为评估器时，平均每个样本的评估成本约为 **$0.002**，处理每个“提示词-响应对”的平均时间为 **1.2 秒**。这表明该方法主要依赖 API 调用，而非本地大规模训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个基准数据集上测试了 5 个任务模型，并使用了 4-5 个不同的评估器模型。在鲁棒性实验中，每类任务抽取了 210 个样本。
*   **充分性**：实验设计非常全面，不仅验证了与传统指标的相关性（Spearman $\rho \approx 0.97$），还通过人工评估（3名研究生，210个样本）验证了自动评分的可靠性。
*   **客观性**：通过跨评估器一致性研究（Cross-evaluator analysis）证明了 PEEM 不依赖于特定的评估模型，具有较好的通用性和客观性。

#### 6. 主要结论与发现
*   **高度对齐**：PEEM 的准确性轴与传统准确率指标表现出极强的相关性，能够准确反映模型排名。
*   **反馈有效性**：仅依靠 PEEM 提供的自然语言理由作为反馈，零样本重写后的提示词在下游任务中准确率最高提升了 **11.7%**，效果优于需要大量计算的强化学习方法。
*   **鲁棒性与敏感性**：PEEM 对意思不变的改写保持稳定（鲁棒率 >76%），但对语义上的对抗攻击（如矛盾指令）非常敏感，分数会显著下降。
*   **越狱检测**：发现“越狱”提示词虽然在提示词质量分上可能较高（因其指令明确），但会导致响应质量分骤降，证明了联合评估的必要性。

#### 7. 优点
*   **可解释性**：通过自然语言理由打破了评分的“黑盒”，为开发者提供了改进提示词的具体方向。
*   **联合评估**：首次将提示词本身的质量纳入评估框架，揭示了输入质量与输出质量之间的因果联系。
*   **低成本高效率**：无需梯度访问或额外训练，通过零样本提示即可实现高性能的评估与优化。
*   **框架通用**：评估器无关的设计使其易于迁移到不同的 LLM 后端。

#### 8. 不足与局限
*   **语言局限**：实验仅限于**英语**基准，在多语言环境下的适用性有待验证。
*   **人工评估规模**：虽然进行了人工对齐实验，但样本量（210个）相对较小，且主要由 AI 专业学生完成，可能存在领域偏差。
*   **极端情况误判**：错误分析显示，PEEM 在处理“逻辑看似合理但事实错误”的响应时可能给出高分（1.1% 假阳性），在处理“正确但表述过于简略”的响应时可能给出低分（0.7% 假阴性）。
*   **长文本敏感度**：未系统分析提示词长度对评分的独立影响。

（完）
