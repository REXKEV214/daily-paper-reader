---
title: "Robust Post-Training for Generative Recommenders: Why Exponential Reward-Weighted SFT Outperforms RLHF"
title_zh: 生成式推荐系统的鲁棒后训练：为什么指数奖励加权 SFT 优于 RLHF
authors: "Keertana Chidambaram, Sanath Kumar Krishnamurthy, Qiuling Xu, Ko-Jen Hsiao, Moumita Bhattacharya"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10279v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 将生成式推荐系统与用户偏好对齐
tldr: 本研究针对生成式推荐系统在后训练阶段面临的RLHF奖励欺骗、离线RL倾向得分缺失及在线交互受限等挑战，提出了一种基于指数奖励加权的有监督微调（SFT）方法。该方法直接利用观测奖励进行优化，无需学习奖励模型或倾向得分，具有全离线且抗干扰的特性。理论证明了其在噪声奖励下的策略改进保证，并通过温度参数平衡鲁棒性与性能，在多个数据集上显著优于RLHF等基线。
selection_source: fresh_fetch
motivation: 现有的RLHF和离线RL方法在生产级生成式推荐系统中因奖励欺骗、噪声反馈和倾向得分缺失而难以有效应用。
method: 提出使用指数奖励加权的SFT方法，通过权重 $w = \exp(r/\lambda)$ 直接在观测奖励上进行全离线优化。
result: 在三个开源和一个私有数据集上的实验表明，该方法在性能和可扩展性上均优于RLHF等四种主流基线。
conclusion: 指数奖励加权SFT为生成式推荐系统提供了一种简单、鲁棒且具有理论改进保证的对齐方案。
---

## 摘要
通过后训练将生成式推荐系统与用户偏好对齐，对于缩小下一项预测与实际推荐质量之间的差距至关重要。现有的后训练方法不适用于生产规模的系统：RLHF 方法由于噪声用户反馈和不可靠的奖励模型而产生奖励作弊（reward hacking），离线强化学习（offline RL）替代方案需要无法获取的倾向得分（propensity scores），而在线交互则是不可行的。我们发现权重为 $w = \exp(r/λ)$ 的指数奖励加权 SFT 特别适合这种场景，并提供了阐明其原因的理论和实证基础。通过直接对观察到的奖励进行优化，而无需查询学习到的奖励模型，该方法对奖励作弊具有免疫力，不需要倾向得分，且完全是离线的。我们证明了在噪声奖励下该场景的首个策略改进保证，表明差距仅随目录大小呈对数级增长，并且即使对于大型项目目录也保持信息量。至关重要的是，我们展示了温度参数 $λ$ 显式且可量化地控制了鲁棒性与改进之间的权衡，为从业者提供了一个具有理论依据的单一可解释正则化超参数。在三个开源数据集和一个私有数据集上针对四个基准进行的实验证实，指数奖励加权简单、可扩展，且始终优于基于 RLHF 的替代方案。

## Abstract
Aligning generative recommender systems to user preferences via post-training is critical for closing the gap between next-item prediction and actual recommendation quality. Existing post-training methods are ill-suited for production-scale systems: RLHF methods reward hack due to noisy user feedback and unreliable reward models, offline RL alternatives require propensity scores that are unavailable, and online interaction is infeasible. We identify exponential reward-weighted SFT with weights $w = \exp(r/λ)$ as uniquely suited to this setting, and provide the theoretical and empirical foundations that explain why. By optimizing directly on observed rewards without querying a learned reward model, the method is immune to reward hacking, requires no propensity scores, and is fully offline. We prove the first policy improvement guarantees for this setting under noisy rewards, showing that the gap scales only logarithmically with catalog size and remains informative even for large item catalogs. Crucially, we show that temperature $λ$ explicitly and quantifiably controls the robustness-improvement tradeoff, providing practitioners with a single interpretable regularization hyperparameter with theoretical grounding. Experiments on three open-source and one proprietary dataset against four baselines confirm that exponential reward weighting is simple, scalable, and consistently outperforms RLHF-based alternatives.

---

## 论文详细总结（自动生成）

这篇论文针对生成式推荐系统（Generative Recommenders）的后训练对齐问题进行了深入研究，探讨了为什么简单的指数奖励加权有监督微调（Exp-RSFT）在实际生产环境下优于复杂的 RLHF 方法。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：生成式推荐系统（如 SASRec, HSTU）通常采用行为克隆（Behavior Cloning, BC）进行预训练，但这会导致模型无差别地模仿用户行为（包括误触、标题党点击等低质量交互）。
*   **研究动机**：虽然 LLM 领域通过 RLHF 取得了成功，但在推荐系统中直接应用 RLHF 面临三大挑战：
    1.  **奖励模型不可靠**：推荐系统的项目空间巨大且极其稀疏，奖励模型在外推时极易出错，导致策略出现“奖励欺骗”（Reward Hacking）。
    2.  **离线学习限制**：生产环境通常只有静态数据集，无法进行在线交互。
    3.  **倾向得分缺失**：传统的离线 RL 需要倾向得分（Propensity Scores）来纠正选择偏差，但在复杂系统中这些得分往往不可知或估计极不准确。

### 2. 论文提出的方法论
*   **核心思想**：提出 **指数奖励加权 SFT（Exp-RSFT）**。该方法不依赖于学习到的奖励模型，而是直接利用离线数据中观测到的真实奖励（如观看时长、评分）作为权重进行微调。
*   **关键技术细节**：
    *   **权重机制**：对训练样本 $(s, a, r)$ 赋予权重 $w = \exp(r/\lambda)$，其中 $r$ 是观测奖励，$\lambda$ 是温度参数。
    *   **优化目标**：通过加权的极大似然估计（Weighted MLE）更新策略参数 $\theta$：
        $$\theta_{k+1} = \arg \max_{\theta} \mathbb{E}_{(s,a,r) \sim \mathcal{D}} \left[ \exp\left(\frac{r}{\lambda}\right) \log \pi_\theta(a|s) \right]$$
    *   **理论保证**：论文首次证明了在**噪声奖励**下该方法的策略改进保证。证明显示，性能差距仅随项目目录大小呈对数级增长（$O(\sigma \sqrt{\log |A|})$），这意味着该方法在超大规模项目库中依然稳健。
    *   **温度参数 $\lambda$**：作为唯一的超参数，$\lambda$ 显式地控制了“改进程度”与“鲁棒性”之间的权衡。较小的 $\lambda$ 鼓励模型更激进地向高奖励项目偏移，但会放大噪声影响。

### 3. 实验设计
*   **数据集**：
    *   开源数据集：MovieLens 1M, MovieLens 20M, Amazon Books。
    *   工业级数据集：Netflix 内部大规模私有数据集（规模达数百万用户、数万项目）。
*   **对比基准（Baselines）**：
    1.  **BC (Behavior Cloning)**：基础 HSTU 模型，无奖励优化。
    2.  **Reward-SFT**：线性奖励加权（权重直接等于 $r$）。
    3.  **DPO (Direct Preference Optimization)**：针对推荐场景修改的在线变体。
    4.  **PPO (Proximal Policy Optimization)**：标准的 RLHF 流程，依赖学习到的奖励模型。
*   **评估指标**：NDCG@10/50, Hit Rate (HR)@10/50, MRR，以及奖励模型预测的平均奖励。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 HSTU 作为基础架构，并在开源数据集上分别运行了 10 到 30 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（ML-1M）到超大型（Netflix）的不同规模数据集，实验结果具有跨域一致性。
*   **消融与分析**：
    *   对温度参数 $\lambda$ 进行了详尽的扫参实验（Sweep），验证了理论预言的“倒 U 型”性能曲线。
    *   对比了奖励模型与简单基准（如 Item Mean）的误差，证明了在推荐场景下奖励模型本身往往不如简单统计量准确。
*   **充分性评价**：实验设计非常充分且客观。通过在私有工业数据集上的验证，增强了结论在实际生产环境中的说服力。

### 6. 论文的主要结论与发现
1.  **Exp-RSFT 表现最优**：在所有数据集和指标上，Exp-RSFT 均显著优于 PPO、DPO 和线性加权 SFT。
2.  **RLHF 的失效**：PPO 和 DPO 在推荐任务中出现了“灾难性崩溃”。原因是它们过度优化了一个泛化能力极差的奖励模型，导致推荐出的项目虽然奖励模型评分高，但实际用户指标（HR/NDCG）极低。
3.  **$\lambda$ 的关键作用**：指数加权比线性加权更有效，因为它能更好地解耦项目质量与项目流行度，且 $\lambda$ 提供了必要的正则化。
4.  **奖励模型瓶颈**：在推荐系统中，由于用户交互的稀疏性，学习一个能覆盖全目录的奖励模型是非常困难的，这限制了所有依赖奖励模型的方法。

### 7. 优点
*   **简单且鲁棒**：不需要复杂的强化学习训练流程（如 Actor-Critic 架构），实现成本极低。
*   **理论与实证结合**：不仅给出了算法，还提供了严谨的收敛保证和噪声鲁棒性证明。
*   **解决痛点**：直接针对工业界无法获取倾向得分、奖励模型易被欺骗的痛点提出方案。

### 8. 不足与局限
*   **上下文强度的假设**：该研究主要基于 Contextual Bandit（上下文多臂老虎机）视角，虽然扩展到了 MDP，但对于极长路径的序列决策优化讨论较少。
*   **依赖观测奖励**：该方法要求离线数据中必须包含高质量的标量奖励信号。如果数据中只有隐式反馈（如点击/未点击）且噪声极大，该方法的提升空间可能会受限。
*   **评估偏差**：实验主要在评分 $\ge 4.5$ 的测试集上评估，虽然这符合优化高价值目标的逻辑，但也可能忽略了模型在处理一般偏好时的表现。

（完）
