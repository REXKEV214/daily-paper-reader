---
title: Social Knowledge for Cross-Domain User Preference Modeling
title_zh: 跨领域用户偏好建模中的社交知识
authors: "Nir Lotan, Adir Solomon, Ido Guy, Einat Minkov"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10148v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 利用社交嵌入进行跨域用户偏好建模
tldr: 本研究提出一种利用大规模社交建模进行跨领域用户偏好预测的方法。通过将用户和热门实体投影到基于Twitter网络学习到的联合社交嵌入空间中，利用余弦相似度评估实体相关性。该方法在零样本设置下表现优异，显著超越了基于流行度的基准模型，并揭示了社交嵌入中编码的人口统计学因素与跨领域偏好的关联，同时展示了其在增强大语言模型社交建模方面的潜力。
selection_source: fresh_fetch
motivation: 旨在解决跨领域推荐中用户偏好建模的难题，特别是在目标领域缺乏用户反馈的零样本场景下。
method: 通过在大规模Twitter社交网络上学习用户与实体的联合社交嵌入空间，并利用余弦相似度进行偏好预测。
result: 实验证明该方法在零样本个性化任务中显著优于强流行度基准，且社交嵌入能有效捕捉影响偏好的社会人口统计特征。
conclusion: 社交知识是跨领域偏好建模的有效手段，且能与大语言模型结合以提升对终端用户的社交建模能力。
---

## 摘要
我们证明，利用大规模社交建模可以跨主题领域对用户偏好进行表示和预测。给定用户喜爱的热门实体信息，我们将用户投影到一个从 Twitter（现为 X）网络的大规模样本中学习到的社交嵌入空间中。通过在联合社交空间中表示用户和热门实体，我们可以利用该嵌入空间内的余弦相似度来评估候选实体（例如音乐艺术家）的相关性。使用链路预测实验进行的全面评估表明，该方法在零样本（zero-shot）设置下（即目标领域实体没有用户反馈时）实现了有效的个性化，相比于强大的基于流行度的基准模型有显著提升。深入分析进一步说明，社交嵌入中编码的社会人口统计因素与跨领域的用户偏好相关。最后，我们论证并证明了所提方法可以促进利用大语言模型（LLM）对终端用户进行社交建模。

## Abstract
We demonstrate that user preferences can be represented and predicted across topical domains using large-scale social modeling. Given information about popular entities favored by a user, we project the user into a social embedding space learned from a large-scale sample of the Twitter (now X) network. By representing both users and popular entities in a joint social space, we can assess the relevance of candidate entities (e.g., music artists) using cosine similarity within this embedding space. A comprehensive evaluation using link prediction experiments shows that this method achieves effective personalization in zero-shot setting, when no user feedback is available for entities in the target domain, yielding substantial improvements over a strong popularity-based baseline. In-depth analysis further illustrates that socio-demographic factors encoded in the social embeddings are correlated with user preferences across domains. Finally, we argue and demonstrate that the proposed approach can facilitate social modeling of end users using large language models (LLMs).

---

## 论文详细总结（自动生成）

这篇论文《Social Knowledge for Cross-Domain User Preference Modeling》探讨了如何利用社交媒体中的“社交知识”来解决推荐系统中的跨领域偏好预测和冷启动问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的推荐系统高度依赖特定领域的历史反馈（如电影评分），在面对新用户（冷启动）或跨领域（如从电影偏好推测汽车偏好）时表现不佳。
*   **研究动机**：用户的偏好通常与其社会人口统计学特征（年龄、性别、教育、政治倾向等）相关，而这些特征在不同领域间具有一致性。社交网络（如 Twitter/X）提供了丰富的实体关注信息，这些信息构成了“社交语义”，可以作为跨领域建模的桥梁。

### 2. 方法论
*   **核心思想**：将用户和热门实体（Entity）投影到同一个预训练的社交嵌入空间中，通过计算向量相似度来预测用户对新实体的偏好。
*   **关键技术细节**：
    *   **社交实体嵌入（SocialVec）**：利用类似 Word2Vec 的 Skip-gram 算法，在 Twitter 大规模网络上学习 20 万个热门账号（实体）的嵌入向量。如果两个实体经常被同一组用户关注，它们在空间中距离更近。
    *   **用户表示（Inductive Modeling）**：采用**归纳式（Inductive）**方法。给定一个用户关注的实体集合 $\{e_i\}$，通过对这些实体的预训练向量取平均值（Mean Pooling），生成该用户的社交嵌入向量 $u_i$。
    *   **偏好预测**：计算用户向量 $u_i$ 与目标领域候选实体向量之间的**余弦相似度**，按相似度得分进行排序推荐。
    *   **LLM 集成**：将用户喜爱的实体列表作为 Prompt 输入给大语言模型（如 GPT-4o），利用 LLM 内部蕴含的社交知识进行个性化排序。

### 3. 实验设计
*   **数据集**：自建数据集，包含约 1.2 万至 1.4 万名 Twitter 用户，涵盖 14 个主题领域（如音乐、新闻、政治、体育、时尚、汽车等），共 280 个核心实体。
*   **实验场景**：链路预测（Link Prediction）。即预测用户是否会关注某个特定领域的实体。
*   **基准模型（Benchmark）**：
    *   **Popularity-based Ranking**：基于实体粉丝总数的非个性化流行度排名（推荐系统中最强的非个性化基准）。
    *   **Closed-world Evaluation**：限制用户表示仅由非目标领域的实体组成，测试纯粹的跨领域迁移能力。
    *   **LLM Baseline**：GPT-4o 在无个性化信息下的默认排序。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **资源消耗分析**：由于该方法主要基于预训练的 SocialVec 嵌入（已由前人训练好）和向量相似度计算，其推理过程的计算开销极低。LLM 实验部分使用了 OpenAI 的 API（GPT-4o-2025-01-01-preview），属于调用性质。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 14 个不同领域分别进行了独立测试。
    *   进行了**消融实验**：改变用户画像中包含的实体数量 $k$（从 10 到 500），观察性能收敛情况。
    *   进行了**跨领域限制性实验**（Sampled 模式），确保模型不是通过目标领域的已知信息进行预测。
    *   **LLM 验证**：通过热力图分析了不同类别数量和实体数量对 LLM 个性化效果的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从传统向量检索到前沿 LLM 应用的对比，且通过跨领域设置证明了方法的泛化性，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：社交嵌入方法在 13/14 个领域中优于流行度基准，平均 MAP（平均精度均值）提升了 **22%**。
*   **跨领域有效性**：即使在完全屏蔽目标领域信息的情况下，性能仍比流行度基准高出 **12%**。
*   **数据效率**：该方法对稀疏数据非常鲁棒，用户只需提供 **10-20 个**感兴趣的实体，即可实现高质量的个性化建模。
*   **社会人口学关联**：分析证实，社交嵌入捕捉到了深层的社会特征（如政治倾向、教育程度），这些特征是跨领域偏好一致性的根本原因。
*   **LLM 兼容性**：LLM 能够理解实体列表背后的社交含义，加入 12 个实体信息即可使 LLM 的推荐准确度提升 13%。

### 7. 优点与亮点
*   **零样本/冷启动能力**：无需用户在目标领域有任何历史行为，仅凭其在社交媒体上的“品味”即可预测。
*   **轻量级与归纳式**：不需要为每个新用户重新训练模型，只需简单的向量平均，易于在大规模系统中部署。
*   **可解释性**：通过社会人口统计学分析，揭示了推荐背后的社会学逻辑，而非纯粹的黑盒计算。

### 8. 不足与局限
*   **社交媒体偏差**：数据源于 Twitter，可能存在特定的人口统计学偏差（如政治活跃度高、特定年龄段集中），无法完全代表所有互联网用户。
*   **实体覆盖范围**：该方法依赖于“热门实体”，对于长尾、小众或本地化实体的建模能力可能受限。
*   **隐私与伦理风险**：论文也指出，这种建模方式可能会强化社会偏见或刻板印象（Stereotypes），且利用社交追踪数据进行用户画像存在隐私争议。
*   **动态性不足**：社交嵌入通常是静态预训练的，可能无法实时捕捉用户兴趣的快速漂移。

（完）
