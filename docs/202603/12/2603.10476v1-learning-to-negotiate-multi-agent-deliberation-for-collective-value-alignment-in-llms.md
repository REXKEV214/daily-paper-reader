---
title: "Learning to Negotiate: Multi-Agent Deliberation for Collective Value Alignment in LLMs"
title_zh: 学习协商：大语言模型中集体价值对齐的多智能体审议
authors: "Panatchakorn Anantaprayoon, Nataliia Babina, Nima Asgharbeygi, Jad Tarifi"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10476v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 集体价值对齐的多智能体商议
tldr: 针对大语言模型在多利益相关者冲突场景下的对齐难题，本文提出一种基于多智能体协商的对齐框架。该方法通过让同一模型的两个对立人格进行自我博弈和结构化对话，并利用GRPO算法结合集体代理（CA）目标进行强化学习优化。实验证明，该方法在不损害通用语言能力的前提下，显著增强了模型的冲突解决能力和集体决策支持水平，为复杂价值对齐提供了新路径。
selection_source: fresh_fetch
motivation: 现有的单智能体对齐方法难以处理多利益相关者之间的价值冲突，缺乏必要的审议式协商能力。
method: 采用对立人格自我博弈的结构化对话机制，并利用GRPO算法根据最终方案的集体代理评分来优化对话交互动态。
result: 该模型在显著提升冲突解决性能和集体代理对齐水平的同时，成功保持了通用的语言处理能力。
conclusion: 协商驱动的审议训练是提升LLM在价值冲突场景中支持集体决策和价值对齐的有效路径。
---

## 摘要
大语言模型（LLMs）的对齐在单智能体设置中通过 RLHF 和宪法 AI（Constitutional AI）等范式取得了实质性进展，近期研究还探索了 RLAIF 等可扩展的替代方案以及不断演进的对齐目标。然而，这些方法在多利益相关者场景中仍然受限，因为此类场景会出现价值观冲突，且需要审议式协商能力。本研究提出了一种基于多智能体协商的对齐框架，该框架将 LLMs 与集体代理（Collective Agency, CA）进行对齐——这是一种旨在促进代理能力持续扩展的现有对齐目标——同时提升冲突解决能力。为了实现可扩展训练，同一 LLM 的两个自我博弈（self-play）实例被赋予对立的人格，通过结构化的轮次对话来合成互利的解决方案。我们生成了合成的道德困境提示词和冲突的人格对，并利用外部 LLM 奖励模型，通过 GRPO 算法以 RLAIF 方式优化策略。虽然奖励是根据最终生成内容的 CA 评分计算的，但梯度被应用于对话标记（tokens），以直接改善审议式交互动态。实验表明，所得模型在实现与单智能体基准相当的 CA 对齐的同时，显著提升了冲突解决性能，且未降低通用语言能力。这些结果表明，协商驱动的审议训练为开发能更好地支持价值冲突场景下集体决策的 LLMs 提供了一条切实可行的路径。

## Abstract
The alignment of large language models (LLMs) has progressed substantially in single-agent settings through paradigms such as RLHF and Constitutional AI, with recent work exploring scalable alternatives such as RLAIF and evolving alignment objectives. However, these approaches remain limited in multi-stakeholder settings, where conflicting values arise and deliberative negotiation capabilities are required. This work proposes a multi-agent negotiation-based alignment framework that aligns LLMs to Collective Agency (CA)-an existing alignment objective introduced to promote the continual expansion of agency-while simultaneously improving conflict-resolution capability. To enable scalable training, two self-play instances of the same LLM, assigned opposing personas, engage in structured turn-based dialogue to synthesize mutually beneficial solutions. We generate synthetic moral-dilemma prompts and conflicting persona pairs, and optimize the policy via RLAIF using GRPO with an external LLM reward model. While rewards are computed from CA scores assigned to the final completion, gradients are applied to dialogue tokens to directly improve deliberative interaction dynamics. Experiments show that the resulting model achieves CA alignment comparable to a single-agent baseline while substantially improving conflict-resolution performance without degrading general language capabilities. These results suggest that negotiation-driven deliberation training provides a practical path toward LLMs that better support collective decision-making in value-conflict scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Negotiate: Multi-Agent Deliberation for Collective Value Alignment in LLMs》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 对齐方法（如 RLHF、RLAIF）主要集中在单智能体设置下优化静态行为目标（如有用性、诚实性、无害性）。然而，在现实世界的多利益相关者场景中，不同主体之间存在价值观冲突，单智能体对齐往往导致模型在面对分歧时给出过于抽象、不切实际或无法收敛的回答。
*   **研究动机**：作者试图探索如何让 LLM 具备“审议式协商”能力，通过多智能体交互来解决价值冲突，并将其对齐到一种动态目标——**集体代理（Collective Agency, CA）**。CA 强调知识、仁慈、力量和生命力的持续扩展，而非固定的行为准则。

### 2. 方法论
*   **核心思想**：将结构化协商嵌入到强化学习循环中，通过自我博弈（Self-play）让模型学习如何在冲突的人格（Personas）之间达成互利的共识。
*   **关键技术细节**：
    *   **两阶段任务设计**：
        1.  **协商阶段**：两个具有对立人格的模型实例（一个可训练，一个冻结）针对某一困境进行多轮对话（最多 7 轮）。
        2.  **最终生成阶段**：在达成一致或达到轮次上限后，生成一份总结性的最终方案。
    *   **对立人格库**：构建了 25 对对立的人格（如“数据驱动 vs 情感中心”、“成本最小化 vs 质量最大化”），在训练时随机采样分配给智能体。
    *   **算法流程（GRPO 优化）**：
        *   使用 **组相对策略优化（GRPO）** 算法。
        *   **奖励机制**：由外部 LLM（GPT-4o-mini）根据 CA 评分标准对最终方案打分（0-5 分）。如果协商未达成一致，奖励直接设为 0。
        *   **梯度回传**：关键创新在于梯度是应用在**对话 tokens** 上，而非仅仅是最终的总结 tokens。这意味着模型被直接训练去优化其交互和协商的动态过程。
        *   **Token 归一化**：采用 DAPO 方法对 token 长度进行归一化，以缓解长对话带来的长度偏差。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：合成的 1,100 个道德与实际困境提示词，涵盖高风险专业决策、人际冲突和日常微观伦理。
    *   **人格库**：50 个独立人格组成的 25 对冲突对。
*   **Benchmark（基准测试）**：
    *   **冲突解决测试集**：100 个留出的冲突任务（配有未见过的对立人格）。
    *   **通用 CA 测试集**：100 个无需协商的开放式问题。
    *   **通用能力测试**：IFEval（指令遵循）、GPQA Diamond（科学问答）、AIME 2024/2025（数学竞赛）。
*   **对比方法**：
    *   **Base 模型**：Qwen3-14B-Instruct。
    *   **单智能体对齐模型**：仅针对 CA 目标进行单智能体强化学习训练的基准。

### 4. 资源与算力
*   **算力设备**：使用了 **1 台 NVIDIA RTX PRO 6000 GPU (96GB VRAM)**。
*   **训练时长**：训练至奖励收敛，耗时约 **110 小时**。
*   **模型配置**：基于 Qwen3-14B-Instruct，使用 **4-bit QLoRA** 进行微调。

### 5. 实验数量与充分性
*   **实验规模**：训练步数约 1,900 步，每步包含多个采样轨迹（组大小 G=8）。
*   **评估客观性**：
    *   使用了比训练奖励模型更强大的 **GPT-5.2** 作为最终评估裁判，以减少自评偏差。
    *   进行了双向位置交换测试以消除位置偏差。
    *   对比了贪婪解码（Greedy）和随机采样（Sampling）两种推理策略。
*   **充分性评价**：实验设计较为全面，涵盖了对齐质量、协商效率和通用能力保留。但作者也承认，目前仅限于双人协商，且缺乏对各组件贡献的详细消融实验（Ablation Study）。

### 6. 论文的主要结论与发现
*   **冲突解决能力显著提升**：多智能体对齐模型在冲突解决任务上的胜率显著高于 Base 模型和单智能体对齐模型，且达成一致所需的轮数减少了约 20-25%。
*   **CA 对齐效果良好**：在保持或提升 CA 评分的同时，模型生成的方案更加具体、具有可操作性，而非空洞的价值陈述。
*   **通用能力未受损**：在 IFEval、GPQA 和 AIME 等标准基准上，对齐后的模型表现与 Base 模型相当，证明了该框架在提升特定能力时不会导致灾难性遗忘。
*   **质量下限提升**：训练动态显示，模型主要通过提高“最差表现”的质量（提升 CA 评分的下限）来优化整体性能。

### 7. 优点
*   **创新的训练目标**：将梯度直接作用于对话交互过程，而非仅仅是结果，这让模型真正“学会了如何协商”。
*   **可扩展性**：通过自我博弈和 RLAIF（AI 反馈强化学习），无需人工标注即可生成大规模训练数据。
*   **动态对齐视角**：采用了 Collective Agency 这一动态目标，比传统的静态 HHH 目标更适应复杂多变的现实环境。

### 8. 不足与局限
*   **消融实验缺失**：尚未明确对立人格、困境提示词、协商交互和 GRPO 算法各自对最终效果的贡献比例。
*   **评估维度单一**：主要依赖 LLM 胜率和协商轮数，缺乏对协商质量（如是否真正实现了深度合成而非简单妥协）的细粒度分析。
*   **场景局限性**：目前仅限于双人（1 vs 1）协商，未涉及多人（N > 2）场景下的联盟形成或复杂信用分配问题。
*   **合成数据偏差**：训练数据完全由 LLM 生成，可能带有生成器模型（Gemini-3-Pro）的固有偏见。

（完）
