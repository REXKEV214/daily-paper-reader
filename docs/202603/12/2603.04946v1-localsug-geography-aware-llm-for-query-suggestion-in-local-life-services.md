---
title: "LocalSUG: Geography-Aware LLM for Query Suggestion in Local-Life Services"
title_zh: LocalSUG：面向本地生活服务搜索建议的地理感知大语言模型
authors: "Jinwen Chen, Shuai Gong, Shiwen Zhang, Zheng Zhang, Yachao Zhao, Lingxiang Wang, Haibo Zhou, Yuan Zhan, Wei Lin, Hainan Zhang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04946v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: LLM查询建议中的偏好优化与用户体验
tldr: 本研究针对本地生活服务平台中的搜索建议任务，提出了LocalSUG框架。为解决传统系统对长尾需求处理不足及大模型在地理感知、训练偏差和推理延迟方面的挑战，该框架引入了城市感知候选挖掘策略、基于束搜索驱动的GRPO对齐算法，并结合词表裁剪等加速技术。LocalSUG在提升地理相关性的同时，显著优化了业务指标，为大模型在实时本地搜索场景的落地提供了有效方案。
selection_source: fresh_fetch
motivation: 传统搜索建议系统难以应对长尾需求，且大模型在本地生活场景中存在地理感知缺失、暴露偏差及推理延迟高的问题。
method: 提出LocalSUG框架，采用城市感知候选挖掘、束搜索驱动的GRPO算法进行偏好优化，并利用质量感知加速和词表裁剪降低延迟。
result: "在线A/B测试表明，LocalSUG使点击率（CTR）提升了0.35%，同时将低/无结果率降低了2.56%。"
conclusion: LocalSUG证明了结合地理感知与优化算法的大模型能有效提升本地生活服务的搜索体验，并满足工业级实时推理需求。
---

## 摘要
在本地生活服务平台中，搜索建议模块通过根据用户输入的前缀生成候选查询词，在提升用户体验、减少用户工作量和加速搜索方面发挥着至关重要的作用。传统的多阶段级联系统严重依赖历史高频查询词，限制了其应对长尾需求的能力。虽然大语言模型（LLMs）具有强大的语义泛化能力，但在本地生活服务中部署它们面临三个关键挑战：缺乏地理基础（geographic grounding）、偏好优化中的曝光偏差以及在线推理延迟。为了解决这些问题，我们提出了 LocalSUG，这是一个专为本地生活服务平台定制的基于 LLM 的搜索建议框架。首先，我们引入了一种基于词项共现的城市感知候选词挖掘策略，以在生成中注入地理基础。其次，我们提出了一种由束搜索（beam-search）驱动的 GRPO 算法，使训练与推理时的解码对齐，从而减少自回归生成中的曝光偏差。多目标奖励机制进一步优化了相关性和业务导向的指标。最后，我们开发了质量感知的束加速和词表剪枝技术，在保持生成质量的同时显著降低了在线延迟。广泛的线下评估和大规模线上 A/B 测试表明，LocalSUG 将点击率（CTR）提升了 0.35%，并将低/无结果率降低了 2.56%，验证了其在实际部署中的有效性。

## Abstract
In local-life service platforms, the query suggestion module plays a crucial role in enhancing user experience by generating candidate queries based on user input prefixes, thus reducing user effort and accelerating search. Traditional multi-stage cascading systems rely heavily on historical top queries, limiting their ability to address long-tail demand. While LLMs offer strong semantic generalization, deploying them in local-life services introduces three key challenges: lack of geographic grounding, exposure bias in preference optimization, and online inference latency. To address these issues, we propose LocalSUG, an LLM-based query suggestion framework tailored for local-life service platforms. First, we introduce a city-aware candidate mining strategy based on term co-occurrence to inject geographic grounding into generation. Second, we propose a beam-search-driven GRPO algorithm that aligns training with inference-time decoding, reducing exposure bias in autoregressive generation. A multi-objective reward mechanism further optimizes both relevance and business-oriented metrics. Finally, we develop quality-aware beam acceleration and vocabulary pruning techniques that significantly reduce online latency while preserving generation quality. Extensive offline evaluations and large-scale online A/B testing demonstrate that LocalSUG improves click-through rate (CTR) by +0.35% and reduces the low/no-result rate by 2.56%, validating its effectiveness in real-world deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《LocalSUG: Geography-Aware LLM for Query Suggestion in Local-Life Services》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：在本地生活服务（如美团、饿了么等平台）中，搜索建议（Query Suggestion）是引导用户意图、提升搜索效率的关键。
*   **核心痛点**：
    1.  **长尾需求处理难**：传统基于检索和排序的级联系统依赖历史高频日志，难以处理冷门或新兴意图。
    2.  **地理感知缺失**：LLM 生成的建议往往是全局性的，忽略了不同城市间商户可用性和用户偏好的差异（例如：北京用户搜“披萨”可能偏好达美乐，而澳门用户可能偏好必胜客）。
    3.  **训练-推理不一致（曝光偏差）**：现有模型多采用序列级优化，而实际推理使用束搜索（Beam Search）生成列表，导致性能损失。
    4.  **推理延迟高**：LLM 的自回归特性难以满足搜索场景极高的实时性要求。

### 2. 方法论
LocalSUG 提出了一个端到端的生成式框架，核心技术包括：
*   **城市感知候选挖掘（City-aware Candidate Mining）**：
    *   基于词项共现统计，为每个前缀维护“城市特定”和“全局”两套候选列表。
    *   在 Prompt 构造时优先注入城市相关候选词，确保生成的建议具备地理基础。
*   **束搜索驱动的 GRPO 算法（Beam-Search-Driven GRPO）**：
    *   **对齐训练与推理**：在训练阶段直接从模型中通过束搜索采样一组候选结果，利用组相对策略优化（GRPO）进行强化学习。
    *   **多目标奖励函数**：设计了包含间隔塑造（Gap Shaping）、目标命中（Target Hit）、排名奖励（Rank Bonus）、格式惩罚（Format Penalty）和缺失惩罚（Miss Penalty）的综合奖励机制，平衡相关性与业务指标。
*   **推理加速技术**：
    *   **质量感知加速束搜索（QA-BS）**：引入累积对数概率阈值剪枝和自适应终止机制，当有效候选词达到饱和或质量下降时提前停止。
    *   **词表剪枝（Vocabulary Pruning）**：根据 3800 万条日志统计，将 LLM 的输出层剪枝至最频繁的 30,000 个词元，大幅减少计算开销。

### 3. 实验设计
*   **数据集**：采集自真实本地生活平台的 8 天连续日志。前 7 天为训练集（350 万样本），第 8 天为测试集（5 万样本）。
*   **评估场景**：分为混合（MIX）、点击（CLICK）和下单（ORDER）三个子集。
*   **Benchmark 与对比方法**：
    *   **MCA**：工业界标准的级联架构（检索+排序）。
    *   **SFT**：标准监督微调模型。
    *   **OneSug**：SOTA 的电商生成式搜索建议框架。
    *   **LocalSUG 变体**：包括使用 DPO 优化、更换 Llama 骨干网络等。
*   **评估指标**：HitRate@K（命中率）、MRR（平均倒数排名）、Diversity（多样性）、Quality（生成质量/无重复率）。

### 4. 资源与算力
*   **硬件环境**：使用 H20-141G GPU 集群。
*   **训练细节**：
    *   骨干网络：Qwen3-0.6B。
    *   SFT 阶段：使用 4 张 H20 GPU，全局 Batch Size 为 2048。
    *   GRPO 阶段：每张卡 Batch Size 为 8，组大小 $G=16$。
*   **更新效率**：全自动流水线完成一次每日增量更新（含训练、评估、部署）约需 9 小时（使用 2 张 H20）。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的线下评估和为期一周的线上 A/B 测试（覆盖 10% 流量）。
*   **消融实验**：针对奖励函数中的五个组件分别进行了剔除实验，验证了各部分的必要性。
*   **鲁棒性分析**：测试了不同组大小 $G$ 对性能的影响，证明了方法对超参数的稳健性。
*   **人工评估**：邀请专业标注员对 2000 条结果进行盲测评分。
*   **评价**：实验设计全面，涵盖了从算法性能到工业部署效率的各个维度，对比基线具有代表性，结果客观可信。

### 6. 主要结论与发现
*   **业务提升**：线上 A/B 测试显示，LocalSUG 使 PV CTR 提升了 **0.35%**，Session CTR 提升了 **0.25%**。
*   **长尾挖掘**：成功将“低/无结果率”降低了 **2.56%**，显著提升了系统对长尾需求的覆盖。
*   **用户体验**：平均搜索建议输入长度缩短了 **0.75%**，说明用户能更快找到目标。
*   **多样性**：唯一商品曝光数（Unique Item Exposure）大幅增加 **7.50%**。

### 7. 优点
*   **工业落地性极强**：不仅关注模型精度，还通过词表剪枝和 QA-BS 解决了 LLM 在高并发环境下的延迟瓶颈。
*   **地理感知创新**：通过简单的共现规则有效地解决了 LLM 难以实时感知地理位置变化的难题。
*   **训练推理对齐**：提出的束搜索驱动 GRPO 较好地解决了自回归模型在列表生成任务中的偏差问题。

### 8. 不足与局限
*   **冷启动依赖**：候选挖掘仍部分依赖历史日志，对于完全没有历史数据的“零样本”新兴查询词，其引导能力可能受限。
*   **奖励权重敏感**：多目标奖励函数中的系数（如 $\lambda_{rank}, \lambda_{f mt}$）目前仍需人工调优，缺乏自动化的帕累托最优平衡机制。
*   **模型规模限制**：为了满足延迟要求，目前仅部署了 0.6B 参数的模型，更大规模模型的潜力尚未在实时系统中完全释放。

（完）
