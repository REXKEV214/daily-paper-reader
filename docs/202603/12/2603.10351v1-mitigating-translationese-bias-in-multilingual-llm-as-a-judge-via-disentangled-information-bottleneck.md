---
title: Mitigating Translationese Bias in Multilingual LLM-as-a-Judge via Disentangled Information Bottleneck
title_zh: 通过解耦信息瓶颈缓解多语言 LLM-as-a-Judge 中的翻译腔偏见
authors: "Hongbin Zhang, Kehai Chen, Xuefen Bai, Youcheng Pan, Yang Xiang, Jinpeng Wang, Min Zhang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10351v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 缓解多语言LLM中的翻译腔偏见
tldr: 针对多语言大模型作为裁判时存在的“翻译腔偏好”问题（即倾向于机器翻译而非人工文本），本文提出了DIBJudge框架。该框架利用解耦信息瓶颈技术，通过变分信息压缩提取关键评判特征，并结合互协方差惩罚项显式隔离与英语对齐相关的偏置因素。实验证明，DIBJudge在多语言奖励建模和偏置评估中表现优异，有效缓解了翻译腔偏见，提升了评估的公正性。
selection_source: fresh_fetch
motivation: 多语言大模型在评估时系统性地偏好机器翻译文本而非人工参考文本，这种翻译腔偏见在低资源语言中尤为严重。
method: 提出DIBJudge框架，通过变分信息压缩学习最小充分的评判表示，并利用互协方差惩罚显式解耦鲁棒特征与偏置因子。
result: 在多语言奖励建模基准和专门的翻译腔偏置评估集上，DIBJudge一致优于强基准模型并显著降低了偏见。
conclusion: 通过解耦信息瓶颈显式隔离虚假相关性，是提升多语言大模型评判公正性和鲁棒性的有效途径。
---

## 摘要
大语言模型（LLMs）已成为多语言评估的标准，但它们表现出严重的系统性翻译腔偏见。在本文中，翻译腔偏见被刻画为 LLMs 系统性地偏好机器翻译文本而非人工撰写的参考文本，尤其是在低资源语言中。我们将这种偏见归因于与以下因素的伪相关性：(i) 与英语的潜在流形对齐，以及 (ii) 跨语言可预测性。为了缓解这种偏见，我们提出了 DIBJudge，这是一个鲁棒的微调框架，它通过变分信息压缩学习最小充分且对评判至关重要的表示，同时显式地将伪因素隔离到专门的偏见分支中。此外，我们引入了互协方差惩罚，显式抑制鲁棒表示与偏见表示之间的统计依赖性，从而促进有效的解耦。在多语言奖励建模基准和专门的翻译腔偏见评估套件上的广泛评估表明，所提出的 DIBJudge 一贯优于强基准模型，并显著缓解了翻译腔偏见。

## Abstract
Large language models (LLMs) have become a standard for multilingual evaluation, yet they exhibit a severe systematic translationese bias. In this paper, translationese bias is characterized as LLMs systematically favoring machine-translated text over human-authored references, particularly in low-resource languages. We attribute this bias to spurious correlations with (i) latent manifold alignment with English and (ii) cross-lingual predictability. To mitigate this bias, we propose DIBJudge, a robust fine-tuning framework that learns a minimally sufficient, judgment-critical representation via variational information compression, while explicitly isolating spurious factors into the dedicated bias branch. Furthermore, we incorporate a cross-covariance penalty that explicitly suppresses statistical dependence between robust and bias representations, thereby encouraging effective disentanglement. Extensive evaluations on multilingual reward modeling benchmarks and a dedicated translationese bias evaluation suite demonstrate that the proposed DIBJudge consistently outperforms strong baselines and substantially mitigates translationese bias.

---

## 论文详细总结（自动生成）

# 论文总结：通过解耦信息瓶颈缓解多语言 LLM-as-a-Judge 中的翻译腔偏见

## 1. 核心问题与研究动机
*   **核心问题**：论文指出多语言大模型在作为“裁判”（LLM-as-a-Judge）评估文本质量时，存在严重的**“翻译腔偏见”（Translationese Bias）**。即模型会系统性地偏好机器翻译生成的文本，即使这些文本在语义上存在缺陷或不如人工撰写的参考文本自然。
*   **研究动机**：
    *   这种偏见在**低资源语言**中表现得尤为剧烈，严重损害了自动化评估的公正性。
    *   作者通过分析发现，这种偏见源于两种虚假相关性：一是模型在预训练阶段形成的**潜在流形与英语的过度对齐**；二是模型对机器翻译文本特有的**跨语言高可预测性**（低困惑度）的过度依赖。
    *   现有的微调方法（如标准 SFT）难以从根本上消除这些深层特征层面的偏见。

## 2. 方法论：DIB-Judge 框架
*   **核心思想**：基于**解耦信息瓶颈（Disentangled Information Bottleneck, DIB）**理论，将模型的潜在表示显式地分解为两个独立部分：**鲁棒表示（Robust Representation）**和**偏见表示（Bias Representation）**。
*   **关键技术细节**：
    *   **双分支编码结构**：使用独立的编码器分支分别提取评判关键特征（$Z_r$）和偏置特征（$Z_b$）。
    *   **变分信息压缩**：对鲁棒分支 $Z_r$ 施加变分信息瓶颈约束，通过最小化 $Z_r$ 与输入 $X$ 之间的互信息（利用 KL 散度实现），迫使模型只保留对评判任务最核心的语义信息，过滤掉冗余的风格噪声。
    *   **互协方差惩罚（Cross-Covariance Penalty）**：为了确保两个分支真正解耦，引入互协方差矩阵的 Frobenius 范数作为惩罚项，显式抑制 $Z_r$ 与 $Z_b$ 之间的统计依赖性。
    *   **偏置捕获代理任务**：在训练期间，强迫偏置分支 $Z_b$ 去预测翻译腔相关的特征（如跨语言对齐得分和对数概率分箱），从而将偏见因素“吸纳”并隔离到该分支中。
*   **目标函数**：最终损失函数由任务预测损失、信息压缩损失、偏置捕获损失和解耦损失加权组成。

## 3. 实验设计
*   **数据集与场景**：
    *   **多语言奖励建模基准**：m-RewardBench（23 种语言）、MM-Eval（18 种语言）。
    *   **英文基准**：RewardBench（用于验证是否损害核心英文能力）。
    *   **偏置评估套件**：基于 BELEBELE、AYA 和 XL-SUM 构建的专门用于测试翻译腔偏见的成对偏好数据集。
*   **对比方法（Baselines）**：
    *   **闭源模型**：GPT-4o, Gemini-2.5-Flash。
    *   **开源通用模型**：Qwen2.5/3 系列。
    *   **多语言专家模型**：Nemotron-49B, M-Prometheus, mR3, Think-as-Locals。

## 4. 资源与算力
*   **算力设备**：使用了一个节点，配备 **8 张 NVIDIA H20 (96GB) GPU**。
*   **训练细节**：
    *   采用了 **DeepSpeed ZeRO Stage 3** 和 **FlashAttention-2** 技术。
    *   训练时长：每个模型训练 3 个 Epoch。
    *   参数配置：学习率 1e-4，最大序列长度 16384，采用 LoRA 轻量化微调。

## 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从高资源（如中文、英文）到极低资源（如约鲁巴语、祖鲁语）的数十种语言，样本量充足。
*   **消融实验**：非常详尽，分别验证了：
    1.  目标函数中各个损失项（压缩、解耦、偏置捕获）的作用。
    2.  不同代理任务（CLA, LPBC）对偏见缓解的贡献。
    3.  不同瓶颈机制（VQ, Low-Rank）的优劣对比。
    4.  不同解耦算法（HSIC, MINE, 互协方差）的效率与效果平衡。
*   **客观性**：通过位置交换协议消除了位置偏见，并使用线性探测（Linear Probing）客观地量化了信息泄露程度，实验设计严谨。

## 6. 主要结论与发现
*   **性能提升**：DIB-Judge 在多语言奖励建模任务上显著优于所有开源基准，甚至在某些指标上超过了参数量大得多的模型。
*   **偏见缓解**：在低资源语言中，翻译腔偏见被降低了 **56% 至 80%**，有效提升了评估的公平性。
*   **帕累托前沿**：实验证明 DIB-Judge 在“偏见缓解”与“评判准确率”之间取得了比传统方法更好的平衡（Pareto Frontier）。
*   **零样本泛化**：该方法学习到的鲁棒特征对未见过的偏见（如长度偏见、自我偏好偏见）也表现出了一定的抑制作用。

## 7. 优点
*   **理论创新**：将信息瓶颈理论与解耦学习结合，为解决 LLM 偏见问题提供了坚实的数学基础。
*   **针对性强**：精准捕捉到了多语言评估中长期被忽视的“翻译腔”这一特定偏见来源。
*   **高效性**：提出的互协方差惩罚项比传统的互信息估计方法（如 MINE）计算开销更小，更适合大模型微调。

## 8. 不足与局限
*   **负样本构造**：偏置分支的训练依赖于反向翻译生成的负样本，若翻译工具生成的文本特征与实际应用中的翻译腔特征不符，可能会影响效果。
*   **计算复杂度**：虽然优化了算法，但双分支结构和额外的损失计算仍比标准 SFT 增加了约 18% 的训练延迟。
*   **应用限制**：目前主要针对“裁判”场景（奖励建模），在更广泛的生成任务中是否能保持同样的解耦效果尚需进一步验证。

（完）
