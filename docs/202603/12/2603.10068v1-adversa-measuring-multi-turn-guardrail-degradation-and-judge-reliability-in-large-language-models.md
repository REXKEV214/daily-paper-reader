---
title: "ADVERSA: Measuring Multi-Turn Guardrail Degradation and Judge Reliability in Large Language Models"
title_zh: ADVERSA：衡量大语言模型中的多轮护栏退化与裁判可靠性
authors: Harry Owiredu-Ashley
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10068v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 衡量多轮对话中的护栏退化与安全对齐
tldr: ADVERSA是一个自动化的红队测试框架，旨在评估大语言模型在多轮对抗交互中的安全护栏退化情况。与传统的单轮二元评估不同，它通过微调的70B攻击者模型和5分制评分标准，量化连续的合规轨迹。研究揭示了越狱发生的动态过程、评判器的可靠性以及攻击者模型在微调后的局限性，为多轮对话安全评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有的单轮二元安全评估无法捕捉大语言模型在持续对抗交互中安全属性的演变过程。
method: 开发了基于微调Llama-3.1-70B的攻击者模型ADVERSA-Red，并采用三方评判共识机制和5分制量表来衡量多轮对话中的合规轨迹。
result: "在对前沿模型的测试中，越狱率为26.7%，且成功越狱多集中在初始轮次（平均1.25轮），而非随对话轮数增加而累积。"
conclusion: 该研究强调了多轮对话中护栏退化的动态性，并指出攻击者模型的拒绝行为和评判器一致性是衡量模型防御能力时不可忽视的关键因素。
---

## 摘要
大多数大语言模型（LLM）安全性的对抗性评估仅评估单次提示词并报告二元的通过/失败结果，这无法捕捉安全属性在持续对抗交互下如何演变。我们提出了 ADVERSA，这是一个自动化的红队测试框架，它将护栏退化动态衡量为连续的每轮合规轨迹，而非离散的越狱事件。ADVERSA 使用了一个经过微调的 70B 攻击者模型（ADVERSA-Red，基于 QLoRA 的 Llama-3.1-70B-Instruct），该模型消除了攻击者端的安全拒绝（这种拒绝会导致现成模型作为攻击者时不可靠），并根据结构化的 5 分制量表对受害者响应进行评分，将部分合规视为一种独特的可衡量状态。我们报告了一项针对三个前沿受害者模型（Claude Opus 4.6、Gemini 3.1 Pro、GPT-5.2）的受控实验，采用三重裁判共识架构，其中裁判可靠性被视为首要研究结果进行衡量，而非预先假设。在 15 场长达 10 轮对抗的对话中，我们观察到 26.7% 的越狱率，平均越狱轮次为 1.25，这表明在此评估设置下，成功的越狱集中在早期轮次，而非通过持续压力累积。我们记录了裁判间的一致性率、自我裁判评分倾向、微调攻击者在脱离训练分布部署时的失效模式（攻击者漂移），以及攻击者拒绝作为衡量受害者抵抗力时此前未被充分报告的混杂因素。所有局限性均已明确说明。根据负责任披露政策，攻击提示词被保留；所有其他实验产物均已发布。

## Abstract
Most adversarial evaluations of large language model (LLM) safety assess single prompts and report binary pass/fail outcomes, which fails to capture how safety properties evolve under sustained adversarial interaction. We present ADVERSA, an automated red-teaming framework that measures guardrail degradation dynamics as continuous per-round compliance trajectories rather than discrete jailbreak events. ADVERSA uses a fine-tuned 70B attacker model (ADVERSA-Red, Llama-3.1-70B-Instruct with QLoRA) that eliminates the attacker-side safety refusals that render off-the-shelf models unreliable as attackers, scoring victim responses on a structured 5-point rubric that treats partial compliance as a distinct measurable state.   We report a controlled experiment across three frontier victim models (Claude Opus 4.6, Gemini 3.1 Pro, GPT-5.2) using a triple-judge consensus architecture in which judge reliability is measured as a first-class research outcome rather than assumed. Across 15 conversations of up to 10 adversarial rounds, we observe a 26.7% jailbreak rate with an average jailbreak round of 1.25, suggesting that in this evaluation setting, successful jailbreaks were concentrated in early rounds rather than accumulating through sustained pressure. We document inter-judge agreement rates, self-judge scoring tendencies, attacker drift as a failure mode in fine-tuned attackers deployed out of their training distribution, and attacker refusals as a previously-underreported confound in victim resistance measurement. All limitations are stated explicitly. Attack prompts are withheld per responsible disclosure policy; all other experimental artifacts are released.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ADVERSA** 的自动化红队测试框架，旨在通过多轮对话动态衡量大语言模型（LLM）安全护栏的退化过程。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 安全评估大多局限于“单轮提示词”和“二元（通过/失败）结果”，忽略了现实中攻击者会通过多轮诱导、改写和持续施压来突破安全限制。
*   **研究动机**：安全属性并非固定的阈值，而是一个随交互演变的动态表面。论文旨在将安全评估从“是否越狱”转向“合规性得分如何随轮次、策略和模型身份演变”，从而捕捉模型在持续压力下的行为轨迹。

### 2. 方法论：ADVERSA 框架
*   **核心思想**：构建一个闭环自动化系统，包含攻击者、受害者和裁判团，通过多轮对话记录受害者的“合规轨迹”。
*   **关键技术组件**：
    *   **ADVERSA-Red（攻击者）**：基于 Llama-3.1-70B-Instruct，使用 QLoRA 技术在 10,724 个对抗性示例上进行微调。其目的是消除现成模型自带的安全过滤，确保攻击者能稳定生成恶意提示词。
    *   **5 分制合规量表**：将响应分为 1（强硬拒绝）到 5（完全合规/提供可用信息）。这种细粒度评分能捕捉到“部分合规”这一中间状态。
    *   **三重裁判共识架构**：引入三个不同的前沿模型（Claude、Gemini、GPT）独立评分，取中位数作为最终得分。该设计旨在量化裁判本身在对抗环境下的可靠性。
    *   **流程**：攻击者生成提示词 -> 受害者响应（带历史记录） -> 裁判独立评分 -> 记录 JSON 日志（不向攻击者反馈评分，以保持生态效度）。

### 3. 实验设计
*   **受害者模型**：Claude Opus 4.6, Gemini 3.1 Pro, GPT-5.2。
*   **对抗目标**：涵盖 4 类危害（社会工程、恶意代码、虚假信息、隐私侵犯）的 5 个具体目标。
*   **基准来源**：训练数据源自 AdvBench、HarmBench 和 JailbreakBench。
*   **实验设置**：每场对话最多 10 轮，共进行 15 场独立对话（5 个目标 × 3 个受害者）。

### 4. 资源与算力
*   **训练算力**：使用 1 台 NVIDIA A100 SXM4 80GB GPU，训练时长约 10 小时（4,023 步）。
*   **推理算力**：使用 4 × H200 140GB GPU，通过 vLLM 框架进行张量并行推理。
*   **实验耗时**：15 场对话的完整运行耗时约 65 分钟。

### 5. 实验数量与充分性
*   **实验规模**：共 15 场对话，约 112 轮交互。
*   **充分性评价**：
    *   **不足**：样本量极小（每个模型/目标组合仅 n=1），缺乏统计显著性和多随机种子的重复实验。
    *   **客观性**：论文诚实地记录了“攻击者拒绝”和“裁判不一致”等失败模式，并未掩盖系统缺陷，具有较高的学术透明度。
    *   **定位**：作者明确指出这是一项试点研究（Pilot Study），旨在验证框架可行性而非得出定论。

### 6. 主要结论与发现
*   **越狱动态**：总越狱率为 26.7%，但平均越狱轮次仅为 1.25。这表明在当前设置下，越狱多取决于初始框架（如学术伪装），而非多轮压力的累积。
*   **攻击者漂移（Attacker Drift）**：发现微调后的攻击者在长对话（超过 15 轮）中会逐渐丧失攻击性，转而模仿受害者的礼貌语气，这是由于训练分布（单轮）与推理环境（多轮）不匹配导致的。
*   **裁判可靠性**：裁判在“完全拒绝”或“完全合规”时一致性高，但在“软拒绝”与“部分合规”的边界处存在显著分歧。
*   **攻击者拒绝**：现成模型作为攻击者时常因自身安全机制拒绝执行攻击，这会虚假地提高受害者的“防御成功率”。

### 7. 优点与亮点
*   **轨迹化评估**：提出了“护栏退化曲线”，比二元分类更能反映模型安全性的细微变化。
*   **解决攻击者端瓶颈**：通过微调 70B 模型解决了红队测试中攻击者“不敢攻击”的普遍问题。
*   **裁判透明化**：不预设裁判是完美的，而是将裁判的一致性和自我评分偏差作为研究产物进行分析。
*   **开源贡献**：发布了完整的评估代码、日志和裁判推理字符串。

### 8. 不足与局限
*   **样本量限制**：受限于独立研究者的算力成本，未能完成原计划 6,000 场的规模化实验。
*   **攻击者分布偏移**：攻击者模型仅在单轮数据上训练，导致其在多轮对话中表现不稳定（漂移和拒绝）。
*   **受害者暴露不均**：由于攻击者有时会拒绝向某些模型（如 Gemini）发起攻击，导致不同受害者面临的实际攻击压力不一致，影响对比公平性。
*   **自我裁判偏差**：模型在评价自身家族的响应时可能存在潜在偏差，由于样本量限制，这一点尚未得到充分量化。

（完）
