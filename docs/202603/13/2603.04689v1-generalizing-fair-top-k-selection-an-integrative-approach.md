---
title: "Generalizing Fair Top-$k$ Selection: An Integrative Approach"
title_zh: 泛化公平 Top-$k$ 选择：一种综合方法
authors: Guangya Cai
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04689v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 公平Top-k选择与算法公平性
tldr: 本文研究了多保护群体下的公平 top-k 选择问题，旨在寻找既满足公平性约束又与参考评分函数差异最小的线性评分函数。研究指出了多群体设置下计算效率的潜在挑战，并证明了该问题在某些情况下是计算困难的。为此，作者提出了一种综合方法，引入“效用损失”作为新的差异度量以提升稳定性，并开发了针对小规模参数的高效算法。实验表明，该双管齐下的解决方案在真实数据集上具有出色的性能和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的公平 top-k 选择研究多局限于单保护群体，且缺乏对评分函数偏离参考标准时的差异最小化考量。
method: 提出一种综合框架，在多群体约束下优化线性评分函数，并引入“效用损失”作为衡量差异的新指标以增强扰动下的稳定性。
result: 硬度分析显示问题在一般情况下计算困难，但在保护群体较少时可实现高效求解，且实验验证了算法在真实数据上的优越性。
conclusion: 该研究通过平衡复杂性与性能，为实现多群体公平性提供了一个兼具理论保障和实践鲁棒性的通用解决方案。
---

## 摘要
公平 Top-$k$ 选择旨在确保在选定的前 $k$ 个候选人中，来自少数群体或历史弱势群体的成员具有适当的比例代表性，这一问题已引起广泛关注。我们研究了在存在多个受保护群体的情况下，寻找一个公平（线性）评分函数的问题，同时最小化其与参考评分函数之间的差异。这泛化了之前的设定，即仅限于单一群体且不考虑差异最小化的情形。先前的研究暗示受保护群体的数量对运行效率的影响可能有限。然而，在实验探索的需求驱动下，我们发现这一暗示忽略了一个可能影响结果公平性的关键问题。一旦妥善考虑该问题，我们的复杂度分析表明，即使对于二维数据集和较小的 $k$ 值，该问题在计算上可能也是难解的。然而，我们的分析也揭示了硬度壁垒中的一个缺口，使我们能够在受保护群体数量足够少的情况下，恢复小 $k$ 值的计算效率。此外，除了将差异衡量为公平评分函数与参考评分函数之间的“距离”外，我们还引入了一种替代的差异度量——效用损失（utility loss），它在权重微扰下可能产生更稳定的评分函数。通过在实现复杂度、鲁棒性和性能之间进行细致的工程权衡，我们增强的双管齐下解决方案在真实数据集上展示了强大的实证性能，实验观察结果也为算法设计和实现决策提供了参考。

## Abstract
Fair top-$k$ selection, which ensures appropriate proportional representation of members from minority or historically disadvantaged groups among the top-$k$ selected candidates, has drawn significant attention. We study the problem of finding a fair (linear) scoring function with multiple protected groups while also minimizing the disparity from a reference scoring function. This generalizes the prior setup, which was restricted to the single-group setting without disparity minimization. Previous studies imply that the number of protected groups may have a limited impact on the runtime efficiency. However, driven by the need for experimental exploration, we find that this implication overlooks a critical issue that may affect the fairness of the outcome. Once this issue is properly considered, our hardness analysis shows that the problem may become computationally intractable even for a two-dimensional dataset and small values of $k$. However, our analysis also reveals a gap in the hardness barrier, enabling us to recover the efficiency for the case of small $k$ when the number of protected groups is sufficiently small. Furthermore, beyond measuring disparity as the "distance" between the fair and the reference scoring functions, we introduce an alternative disparity measure$\unicode{x2014}$utility loss$\unicode{x2014}$that may yield a more stable scoring function under small weight perturbations. Through careful engineering trade-offs that balance implementation complexity, robustness, and performance, our augmented two-pronged solution demonstrates strong empirical performance on real-world datasets, with experimental observations also informing algorithm design and implementation decisions.

---

## 论文详细总结（自动生成）

这篇论文《Generalizing Fair Top-$k$ Selection: An Integrative Approach》对公平 Top-$k$ 选择问题进行了深入的理论与实证研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在存在**多个受保护群体**（如性别、种族及其交叉项）的情况下，找到一个既满足公平性约束（比例代表性），又与初始参考评分函数（专家权重）**差异最小**的线性评分函数。
*   **研究动机**：
    *   自动化决策（如招聘、录取）中，简单的 Top-$k$ 选择常导致偏见。
    *   现有研究多局限于单一受保护群体，且在处理评分“平局（Ties）”问题上存在疏忽。
    *   需要一种既能保证公平，又能保持评分函数可解释性（即权重接近原始设定）的高效算法。

### 2. 方法论：核心思想与关键技术
*   **硬度分析（Hardness Analysis）**：
    *   证明了当受保护群体数量较多时，即使在二维数据集中，验证一个权重向量是否公平也是 **NP-hard** 的。
    *   基于细粒度复杂度理论（$t$-OV 假设），证明了对于常数 $k$，该问题存在 $\Omega(n^{k-\delta})$ 的计算下界。
*   **差异度量（Disparity Measures）**：
    *   **权重差异（$w$-difference）**：最小化公平权重与参考权重之间的 $L_1$ 距离。
    *   **效用损失（Utility Loss）**：最小化选定集合在参考权重下的总分损失。相比权重差异，该指标能产生更稳定的评分函数。
*   **双管齐下的解决方案（Two-pronged Solution）**：
    *   **基于 $k$-level 的算法**：适用于 $k$ 较小的情况。通过遍历对偶空间中的排列单元（Cells），利用回溯算法处理平局问题，并结合线性规划（LP）优化目标函数。
    *   **基于 MILP 的算法**：适用于 $k$ 较大的情况。将公平性约束和评分逻辑建模为混合整数线性规划问题，利用高性能求解器求解。

### 3. 实验设计
*   **数据集**：
    *   **COMPAS**：犯罪风险评估数据集（6维属性，涉及种族、性别及其交叉群体）。
    *   **IIT-JEE**：印度高考数据集（3维属性，涉及性别和预留类别）。
*   **基准方法（Benchmark）**：对比了 `2draysweep` 和 `ATC+`（来自 Asudeh 等人 2019 年的工作），并对其进行了针对多群体的优化改进。
*   **对比维度**：运行时间、权重偏差、效用损失，以及在不同 $k$ 值、维度 $d$、数据规模 $n$ 和允许偏差 $\epsilon$ 下的性能表现。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **64核 AMD EPYC 7763 处理器** (2.45 GHz) 和 **512GB RAM** 的服务器。
*   **软件环境**：C++ 实现，使用 Gurobi 作为 MILP 求解器，运行在 Apptainer 容器（Rocky Linux 8.10）中。
*   **算力说明**：论文未涉及深度学习训练，因此未提及 GPU。实验设置了 20 小时的运行时间上限。

### 5. 实验数量与充分性
*   **实验规模**：针对两个真实数据集进行了多组实验，涵盖了从 2 维到 6 维的不同属性组合。
*   **充分性**：实验考虑了参数敏感性分析（改变 $k, \epsilon, n, d$），并进行了验证性实验（50 组随机采样）以确保算法找到的是全局最优解。
*   **客观性**：通过在真实数据集上对比基准算法，证明了所提方法在运行速度上提升了数个数量级，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **计算复杂性**：多群体公平性显著增加了问题难度，但在受保护群体数量较少时，可以通过优化的回溯算法恢复计算效率。
*   **算法选择**：对于小 $k$，基于 $k$-level 的几何算法占优；对于大 $k$，MILP 更加稳健。
*   **稳定性**：引入“效用损失”作为优化目标，可以有效避免权重微小扰动导致的公平性失效，提升了评分函数的鲁棒性。
*   **性能**：增强后的双管齐下方案在处理多群体约束时，依然保持了极高的运行效率。

### 7. 优点
*   **理论与工程结合**：不仅提供了严谨的硬度证明，还针对实际工程中的“平局”和“数值稳定性”给出了解决方案。
*   **通用性强**：支持交叉公平性（Intersectional Fairness）约束。
*   **效率极高**：通过几何算法优化，解决了以往方法在多维数据下无法扩展的问题。

### 8. 不足与局限
*   **维度灾难**：尽管有所优化，但基于 $k$-level 的算法在维度 $d \ge 4$ 时运行时间仍呈指数级增长。
*   **线性限制**：研究仅限于线性评分函数，未探讨非线性评分模型在公平 Top-$k$ 选择中的应用。
*   **数据集覆盖**：虽然使用了真实数据，但未在更大规模（如千万级）的工业数据集上验证极端情况下的性能。

（完）
