---
title: "Beyond Interleaving: Causal Attention Reformulations for Generative Recommender Systems"
title_zh: 超越交错：生成式推荐系统的因果注意力重构
authors: Hailing Cheng
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10369v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 将用户行为建模为推荐的序列生成任务
tldr: "针对生成式推荐系统中交替排列物品和动作Token导致的序列长度翻倍、计算开销大及因果关系模糊等问题，本文提出了一种基于因果注意力重构的新范式。通过引入AttnLFA和AttnMVP两种架构，将序列复杂度降低了50%，显式建模了物品与动作间的因果依赖。在大规模社交网络数据上的实验表明，该方法在提升推荐准确性的同时，显著缩短了训练时间，为高效生成式排名提供了新思路。"
selection_source: fresh_fetch
motivation: 传统的交替式Token排列方式导致计算效率低下，且难以有效捕捉物品与其对应动作之间的显式因果关系。
method: 提出了AttnLFA和AttnMVP两种新架构，通过注意力机制融合动作信息，在不增加序列长度的前提下显式编码因果依赖。
result: "实验显示新架构在评估损失和归一化熵上均优于基准模型，且训练时间分别减少了23%和12%。"
conclusion: 显式建模物品-动作因果关系是构建可扩展且高效的生成式推荐系统的一种更优设计范式。
---

## 摘要
生成式推荐系统（GR）越来越多地通过交错物品（item）和行为（action）标记，将用户行为建模为序列生成任务。尽管这种方式有效，但该公式引入了显著的结构和计算效率低下问题：它使序列长度翻倍，产生了二次方开销，并依赖隐式注意力来恢复物品与其相关行为之间的因果关系。此外，交错异构标记迫使 Transformer 解耦语义不兼容的信号，导致注意力噪声增加并降低了表示效率。在这项工作中，我们提出了一种原则性的生成式推荐重构方法，使序列建模与底层因果结构和注意力理论保持一致。我们证明了当前的交错机制实际上是相似度加权行为池化（similarity-weighted action pooling）的一种低效代理。为解决这一问题，我们引入了两种消除交错依赖的新型架构，将序列复杂度降低了 50%：基于注意力的行为后期融合（AttnLFA）和基于注意力的混合值池化（AttnMVP）。这些模型显式地编码了 $i_n \rightarrow a_n$ 的因果依赖，同时保留了基于 Transformer 的序列建模的表达能力。我们在来自某大型社交网络的规模化产品推荐数据上评估了我们的框架。实验结果表明，AttnLFA 和 AttnMVP 一致优于交错基准模型，在评估损失上分别实现了 0.29% 和 0.80% 的改进，并在归一化熵（NE）方面取得了显著提升。至关重要的是，这些性能提升分别伴随着 23% 和 12% 的训练时间减少。我们的研究结果表明，显式建模物品-行为因果关系为可扩展且高效的生成式排序提供了一种更优的设计范式。

## Abstract
Generative Recommender Systems (GR) increasingly model user behavior as a sequence generation task by interleaving item and action tokens. While effective, this formulation introduces significant structural and computational inefficiencies: it doubles sequence length, incurs quadratic overhead, and relies on implicit attention to recover the causal relationship between an item and its associated action. Furthermore, interleaving heterogeneous tokens forces the Transformer to disentangle semantically incompatible signals, leading to increased attention noise and reduced representation efficiency.In this work, we propose a principled reformulation of generative recommendation that aligns sequence modeling with underlying causal structures and attention theory. We demonstrate that current interleaving mechanisms act as inefficient proxies for similarity-weighted action pooling. To address this, we introduce two novel architectures that eliminate interleaved dependencies to reduce sequence complexity by 50%: Attention-based Late Fusion for Actions (AttnLFA) and Attention-based Mixed Value Pooling (AttnMVP). These models explicitly encode the $i_n \rightarrow a_n$ causal dependency while preserving the expressive power of Transformer-based sequence modeling.We evaluate our framework on large-scale product recommendation data from a major social network. Experimental results show that AttnLFA and AttnMVP consistently outperform interleaved baselines, achieving evaluation loss improvements of 0.29% and 0.80%, and significant gains in Normalized Entropy (NE). Crucially, these performance gains are accompanied by training time reductions of 23% and 12%, respectively. Our findings suggest that explicitly modeling item-action causality provides a superior design paradigm for scalable and efficient generative ranking.

---

## 论文详细总结（自动生成）

### 论文总结：超越交错——生成式推荐系统的因果注意力重构

#### 1. 论文的核心问题与研究动机
*   **核心问题**：当前的生成式推荐系统（如 Meta 的 HSTU）通常采用“物品-动作交错”（Interleaving）的 Token 序列建模方式（即 $[i_0, a_0, i_1, a_1, \dots]$）。这种方式存在三大缺陷：
    *   **计算低效**：序列长度翻倍导致 Transformer 的二次方计算开销增加 4 倍。
    *   **语义噪声**：物品（内容语义）与动作（用户反馈）属于异构空间，强制交错会引入注意力噪声，干扰表示学习。
    *   **因果模糊**：依赖隐式的注意力机制来捕捉“物品 $i_n$ 触发动作 $a_n$”的显式因果关系，缺乏结构化的归纳偏置。
*   **研究动机**：通过重构注意力机制，显式建模物品与动作间的因果依赖，在降低计算复杂度的同时提升推荐的准确性。

#### 2. 核心方法论
论文提出了两种改进架构，旨在将序列长度缩减 50%，并强化因果建模：

*   **AttnLFA（基于注意力的动作后期融合）**：
    *   **核心思想**：将推荐建模为“以物品为条件的动作池化”任务。
    *   **技术细节**：物品序列单独通过 Transformer 提取特征。最后一层物品表示作为 **Query** 和 **Key**，而动作嵌入作为 **Value**。
    *   **因果约束**：引入“查询偏移”（Query-shifting）机制，确保物品 $i_n$ 只能观察到 $n$ 之前的动作，严格防止标签泄露，同时兼容 FlashAttention 等高性能算子。
*   **AttnMVP（基于注意力的混合值池化）**：
    *   **核心思想**：在 Transformer 的每一层中进行早期融合。
    *   **技术细节**：在每一层注意力计算中，Value 向量由当前层的物品表示与原始动作嵌入加权融合而成（$V = H + \lambda a$）。
    *   **演化过程**：物品表示在堆叠的层中从纯内容语义演化为“用户偏好感知”的语义（例如从“狗”演化为“用户喜欢的狗”）。
*   **AttnDHN（双螺旋网络，探索性方案）**：
    *   提出对称的双流架构，同时更新物品和动作表示，形成类似双螺旋的耦合交互。

#### 3. 实验设计
*   **数据集与场景**：使用某大型专业社交网络（LinkedIn）的真实产品推荐日志，涵盖过去 12 个月的数据。
*   **序列设置**：用户行为序列长度最高达 1024。
*   **任务目标**：多任务学习，包括长停留（Long Dwell）、贡献（Contribution，如点赞/评论/分享）和点赞（Like）等 12 个以上的监督信号。
*   **Benchmark（基准模型）**：标准的交错式生成推荐架构（Interleaved Ranker）。
*   **评估指标**：评估损失（Eval Loss）、归一化熵（NE）以及训练耗时。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）或具体的 GPU 数量。
*   **效率提升**：论文强调了计算效率的显著提升。相比基准模型，AttnLFA 的训练时间减少了 **22.8%**，AttnMVP 减少了 **12.3%**。

#### 5. 实验数量与充分性
*   **实验规模**：实验基于工业级大规模数据集，具有较高的统计显著性。
*   **消融实验**：针对 AttnMVP 进行了消融研究（AttnMVP-LFA），验证了早期融合是性能提升的主要驱动力。
*   **公平性**：所有模型使用相同的特征集（包括内容特征、元数据和计数统计特征）、相同的 Transformer 层数（12层）和位置编码（RoPE），确保了架构对比的客观性。
*   **充分性**：虽然主要展示了三个核心任务的结果，但提到其他九个任务趋势一致，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能超越**：AttnMVP 在评估损失上比基准模型降低了 **0.80%**，AttnLFA 降低了 **0.29%**。
*   **效率双赢**：新架构在提升预测精度的同时，通过缩短序列长度显著降低了训练和推理的计算开销。
*   **因果建模的重要性**：显式建模 $i_n \rightarrow a_n$ 的因果关系比让模型通过交错 Token 自行学习隐式关联更有效。
*   **早期融合优势**：AttnMVP 优于 AttnLFA，说明在表示学习的早期阶段引入动作信号有助于捕捉更深层的用户偏好。

#### 7. 优点与亮点
*   **理论与实践结合**：从因果结构和信息论角度重新审视了 Token 交错的弊端，给出了合理的理论解释。
*   **工程友好**：提出的“查询偏移”机制巧妙地解决了严格因果掩码与高性能注意力算子（FlashAttention）之间的兼容性问题。
*   **显著的效率增益**：在不损失精度前提下大幅减少序列长度，对长序列推荐场景具有极大的工业应用价值。

#### 8. 不足与局限
*   **双流架构的不稳定性**：AttnDHN 虽然理论上更对称，但在实验中表现出训练不稳定，需要更小的学习率，且在异构空间（物品空间巨大 vs 动作空间极小）下的表现未达预期。
*   **硬件细节缺失**：未提供具体的算力资源消耗数据，难以精确评估单位算力的产出比。
*   **应用范围限制**：实验主要集中在社交网络推荐，对于其他领域（如电商、短视频）的泛化性尚需更多验证。
*   **超参数敏感性**：虽然初步分析显示 $\lambda$ 较稳定，但在不同任务中动作信号的融合权重可能需要更精细的调优。

（完）
