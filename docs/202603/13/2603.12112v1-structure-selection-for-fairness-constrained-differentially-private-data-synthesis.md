---
title: Structure Selection for Fairness-Constrained Differentially Private Data Synthesis
title_zh: 公平性约束下的差分隐私数据合成的结构选择
authors: "Naeim Ghahramanpour, Mostafa Milani"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12112v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 数据合成中的算法公平性与偏见缓解
tldr: 针对差分隐私（DP）合成数据中存在的偏见问题，本文提出了PrivCI框架。该框架通过在数据测量阶段引入条件独立性（CI）约束，利用改进的贪婪最小生成树（MST）算法和指数机制，在保证隐私的同时消除敏感属性与结果间的关联。实验证明，PrivCI在满足公平性约束的前提下，比现有方法具有更高的保真度和预测准确性，为公平性敏感的数据发布提供了有效方案。
selection_source: fresh_fetch
motivation: 传统的差分隐私数据合成方法会保留数据中的虚假关联，导致在公平性敏感场景下产生偏见。
method: 提出PrivCI算法，在最小生成树构建过程中集成条件独立性检查，通过指数机制选择满足约束的边。
result: 在标准公平性基准测试中，PrivCI在满足CI约束的同时，实现了比基线方法更强的保真度和预测精度。
conclusion: 通过在结构选择阶段显式引入公平性约束，可以有效提升差分隐私合成数据的公平性与实用性。
---

## 摘要
差分隐私（DP）实现了安全的数据发布，合成数据生成已成为近年来的常用方法。然而，标准的合成器保留了数据中的所有依赖关系，包括敏感属性与结果之间的伪相关。在对公平性要求极高的场景中，这会重现不必要的偏见。一种原则性的补救措施是强制执行条件独立性（CI）约束，这些约束编码了领域知识或法律要求，即在考虑了允许因素后，结果应独立于敏感属性。DP 合成通常分为两个阶段：（i）测量步骤，对选定的边缘分布进行隐私化处理，通常通过最小生成树（MST）进行结构化；（ii）重建步骤，拟合一个与含噪边缘分布一致的概率模型。我们提出了 PrivCI，它在测量步骤中通过一种感知 CI 的贪婪 MST 算法来强制执行 CI，该算法在指数机制下将可行性检查集成到 Kruskal 构造中，从而提高了优于竞争方法的准确性。在标准公平性基准上的实验表明，PrivCI 在满足指定 CI 约束的同时，比之前的基准方法实现了更强的保真度和预测准确性。

## Abstract
Differential privacy (DP) enables safe data release, with synthetic data generation emerging as a common approach in recent years. Yet standard synthesizers preserve all dependencies in the data, including spurious correlations between sensitive attributes and outcomes. In fairness-critical settings, this reproduces unwanted bias. A principled remedy is to enforce conditional independence (CI) constraints, which encode domain knowledge or legal requirements that outcomes be independent of sensitive attributes once admissible factors are accounted for. DP synthesis typically proceeds in two phases: (i) a measure- ment step that privatizes selected marginals, often structured via minimum spanning trees (MSTs), and (ii) a reconstruction step that fits a probabilistic model consistent with the noisy marginals. We propose PrivCI, which enforces CI during the measurement step via a CI-aware greedy MST algorithm that integrates feasibility checks into Kruskal's construction under the exponential mechanism, improving accuracy over competing methods. Experiments on standard fairness benchmarks show that PrivCI achieves stronger fidelity and predictive accuracy than prior baselines while satisfying the specified CI constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Structure Selection for Fairness-Constrained Differentially Private Data Synthesis》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：差分隐私（DP）合成数据旨在保留原始数据的统计结构，但这会导致数据中存在的**偏见和虚假关联**（如敏感属性与结果之间的关联）也被一同保留。
*   **研究动机**：在公平性敏感的场景（如招聘、信贷）中，简单的隐私保护不足以消除歧视。现有的公平性修复方法通常是事后处理，这会浪费隐私预算并降低数据实用性。本文旨在将**条件独立性（CI）约束**（如：在给定教育程度的前提下，性别应与录用结果独立）直接集成到 DP 合成流程的结构选择阶段，以实现隐私与公平性的双重保障。

### 2. 方法论：PrivCI 框架
*   **核心思想**：在基于边缘分布（Marginals）的合成框架中，通过改进最小生成树（MST）的构建过程，在选择属性间的依赖关系时，预先过滤掉违反公平性约束的边。
*   **关键技术细节**：
    *   **属性划分**：将属性分为受保护属性 ($S$)、结果属性 ($O$)、允许的因素 ($A$) 和不允许的因素 ($I$)。公平性目标是实现 $O \perp S | A$。
    *   **CI 感知贪婪 MST 算法**：在构建依赖树时，使用 **Kruskal 算法** 的变体。在每一步选择边时，不仅检查是否成环，还检查该边是否会在 $V \setminus A$ 的图中引入连接 $S$ 和 $O$ 的路径。
    *   **指数机制（Exponential Mechanism）**：在满足上述可行性约束的候选边集合中，利用指数机制根据依赖得分（如互信息代理值）私密地选择边。
    *   **两阶段流程**：
        1.  **测量阶段**：私密测量 1 维边缘分布，计算边得分，通过上述算法选择树结构，再测量选定边的 2 维边缘分布。
        2.  **重建阶段**：使用 **PrivatePGM** 算法根据含噪边缘分布拟合图形模型，并采样生成合成数据。

### 3. 实验设计
*   **数据集**：使用了 5 个标准公平性基准数据集：**Adult**（收入预测）、**COMPAS**（累犯预测）、**Dutch Census**（职业预测）、**German Credit**（信用风险）、**Law School**（法学院通过率）。
*   **对比基准（Benchmark）**：
    *   **MST**：标准的 DP 合成方法，不考虑公平性约束（作为实用性上限）。
    *   **PreFair**：现有的唯一支持 CI 约束的 DP 合成方法（主要竞争对手）。
*   **评估指标**：互信息代理得分 ($q$)、KL 散度、全变分距离（TV）、下游分类任务的 AUC、条件互信息（CMI，衡量 CI 违规程度）以及均等赔率（Equalized Odds, EO）。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该方法基于表格数据的边缘分布和图形模型（PrivatePGM），其计算开销通常远低于深度学习模型，主要集中在边缘分布的计算和 PrivatePGM 的优化迭代上。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的数据集上进行了测试，涵盖了从 0.1 到 10 的多个隐私预算 ($\epsilon$) 水平。
*   **充分性**：采用了 **5 折交叉验证**，并使用了 **Wilcoxon 符号秩检验** 进行统计显著性分析。
*   **客观性**：实验不仅对比了数据保真度，还对比了下游机器学习任务的性能和多种公平性指标，评估维度全面且客观。

### 6. 主要结论与发现
*   **实用性提升**：PrivCI 在大多数数据集上（除 Adult 外）生成的合成数据在保真度（KL/TV）和预测准确性（AUC）上显著优于 PreFair。
*   **公平性达标**：PrivCI 能够有效降低 CMI，证明其在分布层面上成功强制执行了 CI 约束，且效果与 PreFair 相当。
*   **结构选择的重要性**：通过在测量阶段进行贪婪的结构过滤，比 PreFair 预先删除边的启发式方法能保留更多有用的统计依赖。
*   **局限性发现**：虽然消除了分布层面的依赖，但下游模型的公平性指标（如 EO）改善程度因数据集而异，说明数据层面的 CI 修复并不等同于模型层面的完全公平。

### 7. 优点
*   **结构化集成**：将公平性约束作为硬性结构约束集成到 DP 流程中，避免了隐私预算的浪费。
*   **统计严谨性**：利用指数机制和 zCDP 组合分析，保证了严格的隐私证明。
*   **性能优越**：通过更精细的边选择策略，在保持公平性的同时显著提升了数据的实用性。

### 8. 不足与局限
*   **模型限制**：目前仅限于**树状结构**（Tree-structured models），对于具有复杂高阶依赖的数据集，表达能力可能不足。
*   **数据类型**：主要针对离散/分类属性，连续属性需要预先离散化，这可能引入额外的信息损失。
*   **约束复杂度**：目前主要处理单一的 CI 约束，对于多个重叠或复杂的因果约束，算法的扩展性有待验证。

（完）
