---
title: Entropy Guided Diversification and Preference Elicitation in Agentic Recommendation Systems
title_zh: 代理型推荐系统中的熵引导多样化与偏好获取
authors: "Dat Tran, Yongce Li, Hannah Clay, Negin Golrezaei, Sajjad Beygi, Amin Saberi"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11399v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 偏好引导与智能体推荐
tldr: 针对电商平台中用户搜索意图模糊导致交互疲劳或推荐过早收敛的问题，本文提出了一种基于熵引导的交互式决策支持系统（IDSS）。该系统利用熵来量化商品属性的不确定性，通过最大化信息增益来引导自适应偏好获取，并在偏好不完整时通过不确定性感知排序和多样化展示来提供推荐。实验表明，该方法在减少冗余交互的同时，显著提升了推荐的质量、多样性和透明度。
selection_source: fresh_fetch
motivation: 现有的推荐系统在处理模糊查询时，容易导致过多的交互疲劳或因过度自信而过早缩小搜索空间。
method: 提出IDSS系统，利用熵量化属性不确定性，通过最大化信息增益进行提问，并结合不确定性感知排序实现多样化推荐。
result: 实验结果显示，熵引导的偏好获取减少了不必要的追问，而不确定性感知排序提升了推荐结果的信息量和多样性。
conclusion: 熵引导的推理机制为处理不确定性环境下的智能体推荐系统提供了一个有效且稳健的基础框架。
---

## 摘要
电子商务平台的用户在搜索初期往往对其偏好并不确定。提交给推荐系统的查询通常是模糊、不完整或定义不明确的。代理型系统（Agentic systems）被期望能够主动推理、提出澄清性问题并代表用户采取行动，这使得处理此类模糊性变得日益重要。在现有平台中，模糊性会导致过度的交互和问题疲劳，或者导致过度自信的推荐过早地压缩了搜索空间。我们提出了一种交互式决策支持系统（IDSS），该系统利用熵作为统一信号来处理模糊的用户查询。IDSS 维护一个动态过滤的候选产品集，并利用熵来量化物品属性的不确定性。这种不确定性通过选择能够最大化预期信息增益的后续问题，来引导自适应的偏好获取。当偏好仍不完整时，IDSS 通过不确定性感知排序和基于熵的多样化，将残余不确定性显式地纳入下游推荐中，而不是强行进行过早的决策。我们使用基于真实用户评论驱动的模拟用户对 IDSS 进行评估，从而实现了对多样化购物行为的受控研究。我们的评估衡量了交互效率和推荐质量。结果表明，熵引导的获取减少了不必要的后续问题，而不确定性感知排序和展示在模糊意图下产生了更具信息量、多样化且透明的推荐集。这些发现表明，熵引导推理为在不确定性下运行的代理型推荐系统提供了有效的基础。

## Abstract
Users on e-commerce platforms can be uncertain about their preferences early in their search. Queries to recommendation systems are frequently ambiguous, incomplete, or weakly specified. Agentic systems are expected to proactively reason, ask clarifying questions, and act on the user's behalf, which makes handling such ambiguity increasingly important. In existing platforms, ambiguity led to excessive interactions and question fatigue or overconfident recommendations prematurely collapsing the search space. We present an Interactive Decision Support System (IDSS) that addresses ambiguous user queries using entropy as a unifying signal. IDSS maintains a dynamically filtered candidate product set and quantifies uncertainty over item attributes using entropy. This uncertainty guides adaptive preference elicitation by selecting follow-up questions that maximize expected information gain. When preferences remain incomplete, IDSS explicitly incorporates residual uncertainty into downstream recommendations through uncertainty-aware ranking and entropy-based diversification, rather than forcing premature resolution. We evaluate IDSS using review-driven simulated users grounded in real user reviews, enabling a controlled study of diverse shopping behaviors. Our evaluation measures both interaction efficiency and recommendation quality. Results show that entropy-guided elicitation reduces unnecessary follow-up questions, while uncertainty-aware ranking and presentation yield more informative, diverse, and transparent recommendation sets under ambiguous intent. These findings demonstrate that entropy-guided reasoning provides an effective foundation for agentic recommendation systems operating under uncertainty.

---

## 论文详细总结（自动生成）

### 代理型推荐系统中的熵引导多样化与偏好获取：论文总结

#### 1. 核心问题与研究动机
在电子商务搜索初期，用户往往无法准确表达其偏好，导致查询具有模糊性、不完整性或弱规范性。现有的推荐系统在处理这种不确定性时存在两个极端：一是通过过多的追问导致用户“交互疲劳”；二是过度自信地推荐窄范围商品，导致搜索空间过早收敛（即“信息茧房”风险）。本文旨在构建一个**代理型推荐系统（Agentic RS）**，通过信息论中的“熵”来统一量化不确定性，从而在减少交互负担的同时，提供更具多样性和透明度的推荐结果。

#### 2. 核心方法论：IDSS 系统
论文提出了**交互式决策支持系统（IDSS）**，其核心思想是将熵作为贯穿偏好获取、排序和展示的统一信号。

*   **语义解析（Semantic Parsing）：** 利用大语言模型（LLM，文中提到使用 GPT-5）将用户自然语言输入解析为结构化状态，包括硬约束（F）、软偏好（P+、P-）和用户耐心程度（s）。
*   **熵引导的提问选择（Entropy-Guided Elicitation）：** 
    *   系统计算当前候选集 $C$ 中各属性维度 $d$ 的香农熵 $H(d)$。
    *   选择熵值最大的维度进行提问（即该维度下商品分布最分散，提问获得的信息增益最大）。
    *   设置熵阈值 $\tau_H$，若所有维度熵值均较低，则停止提问直接推荐，以避免低价值交互。
*   **双重排序策略：**
    *   **嵌入相似度与 MMR：** 利用语义向量匹配意图，并通过最大边际相关性（MMR）算法在重排序阶段抑制冗余。
    *   **覆盖-风险优化（Coverage-Risk Optimization）：** 针对长查询，通过贪心算法最大化对用户“喜爱特征”的覆盖，同时惩罚包含“厌恶特征”的风险项。
*   **基于熵的结果多样化展示：** 系统不提供单一列表，而是选择一个高熵的未指定维度（如能源类型），将结果组织成网格（Grid），让用户通过对比（如混合动力 vs 纯电）来发现潜在偏好。

#### 3. 实验设计
*   **场景与数据集：** 主要在**汽车推荐**领域进行评估（具有高维属性和强权衡特性），并简要扩展至电子产品领域。
*   **模拟用户：** 采用“评论驱动的模拟”方法。利用真实的汽车评论生成 150 个具有特定性格、背景和意图的 Persona（画像），并模拟其与系统的多轮对话。
*   **对比基准（Benchmark）：** 采用消融实验设计，对比了移除 MMR 多样化、移除熵引导提问（EntropyQ）以及不同排序方法（ES vs CR）的表现。
*   **评估指标：** 
    *   **交互质量：** 相关性（Relevance）、新颖性（Newness）。
    *   **推荐质量：** Precision@9、nDCG@9、满意计数（Sat@9）、列表内多样性（ILD）。

#### 4. 资源与算力
*   **模型使用：** 文中明确提到使用 **GPT-5** 进行语义解析和模拟用户生成（注：考虑到论文发布时间标注为 2026 年，这可能指代当时的最先进模型）。
*   **算力细节：** 论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于该系统主要基于 LLM 的 Agentic 工作流和向量检索，其核心开销在于 API 调用和推理，而非从头训练大规模模型。

#### 5. 实验数量与充分性
*   **实验规模：** 构建了 150 个独立的 Persona，涵盖了短查询（模糊意图）和长查询（详细意图）两种设置。
*   **消融实验：** 针对排序算法、多样化组件和提问逻辑进行了详尽的消融对比，每组实验运行三次并报告了均值和标准差。
*   **主观验证：** 补充了 12 名真实用户的试点调查，验证了模拟实验结果与人类感知的一致性。
*   **评价：** 实验设计较为客观，通过引入“LLM 裁判”并结合置信度过滤和多次投票机制，降低了评估过程中的随机噪声，实验充分性较高。

#### 6. 主要结论与发现
*   **提问效率：** 熵引导机制在用户初始输入较少（短查询）时显著提升了问题的“新颖性”，有效锁定了最具区分度的属性。
*   **推荐质量：** 
    *   在短查询下，嵌入相似度（ES）表现更稳健；在长查询下，覆盖-风险优化（CR）能更好地处理复杂约束。
    *   MMR 虽然会轻微降低 Top-K 的相关性得分，但能极大地提升结果的多样性（ILD），缓解选项重复问题。
*   **用户体验：** 结构化的网格展示比扁平列表更受用户欢迎，因为它显式地展示了未指定属性之间的权衡。

#### 7. 优点与亮点
*   **理论统一性：** 将“熵”这一信息论概念贯穿于交互和推荐的全生命周期，解决了 Agent 提问“盲目性”的问题。
*   **动态自适应：** 系统能根据候选集的变化动态调整策略，并能通过“耐心检测”主动终止冗余交互，体现了较强的智能体属性。
*   **评估创新：** 采用基于真实评论生成的模拟用户，既保证了行为的真实感，又实现了大规模、可重复的受控实验。

#### 8. 不足与局限
*   **领域局限：** 实验主要集中在汽车领域，虽然在电子产品上有初步尝试，但在属性较少或感性偏好占主导（如时尚、艺术）的领域，熵引导的效果可能受限。
*   **模拟偏差：** 尽管使用了 LLM 模拟用户，但模拟器可能无法完全捕捉真实人类在面对复杂决策时的犹豫、矛盾或策略性行为。
*   **实时性挑战：** 每一轮交互都需要进行多次 LLM 调用（解析、提问生成、排序解释），在实际生产环境中的响应延迟（Latency）可能是一个挑战，文中未对此进行深入讨论。

（完）
