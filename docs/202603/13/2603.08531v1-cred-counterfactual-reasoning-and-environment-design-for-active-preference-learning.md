---
title: "CRED: Counterfactual Reasoning and Environment Design for Active Preference Learning"
title_zh: CRED：用于主动偏好学习的反事实推理与环境设计
authors: "Yi-Shiuan Tung, Gyanig Kumar, Wei Jiang, Bradley Hayes, Alessandro Roncone"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08531v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 用于人类对齐的主动偏好学习
tldr: 针对复杂机器人任务中奖励函数难以显式定义的问题，本文提出CRED框架。该方法通过联合优化环境设计与轨迹选择，利用反事实推理生成具有高度区分性的轨迹对。CRED能够“想象”新场景并对比不同潜在偏好下的行为差异，从而更高效地从人类反馈中学习奖励函数，显著提升了主动偏好学习的样本效率和准确性，为机器人行为对齐提供了新思路。
selection_source: fresh_fetch
motivation: 现有的主动偏好学习方法受限于固定的轨迹集，难以生成足够多样且具有信息量的对比样本来准确推断人类偏好。
method: 提出CRED方法，通过环境设计生成新场景，并结合反事实推理采样潜在奖励，产生能暴露不同偏好差异的轨迹对进行查询。
result: 实验和用户研究证明，CRED在奖励推断准确性、样本效率以及用户主观评分方面均显著优于现有的最先进方法。
conclusion: 结合环境设计与反事实推理的主动偏好学习能更有效地对齐机器人行为与人类意图，是解决复杂任务奖励建模的有效途径。
---

## 摘要
随着机器人运行环境及其执行任务的复杂性不断增加，为了实现理想的行为特征而显式指定并平衡优化目标变得越来越难以实现。这些系统通过使其行为与人类偏好保持一致并响应修正而获益匪浅，但手动编码此类反馈是不可行的。主动偏好学习 (APL) 通过展示轨迹供人类排序来学习人类的奖励函数。然而，现有方法从固定的轨迹集或回放缓冲区中采样，这限制了查询的多样性，且往往无法识别出具有信息量的比较。我们提出了 CRED，一种用于 APL 的新型轨迹生成方法，该方法通过联合优化环境设计和轨迹选择来改进奖励推理，从而高效地查询并提取用户偏好。CRED 通过环境设计“想象”新场景，并利用反事实推理——通过从其当前信念中采样可能的奖励并询问“如果这是真实的偏好会怎样？”——来生成能够揭示竞争奖励函数之间差异的轨迹对。全面的实验和用户研究表明，CRED 在奖励准确性和样本效率方面显著优于现有最先进的方法，并获得了更高的用户评分。

## Abstract
As a robot's operational environment and tasks to perform within it grow in complexity, the explicit specification and balancing of optimization objectives to achieve a preferred behavior profile moves increasingly farther out of reach. These systems benefit strongly by being able to align their behavior to reflect human preferences and respond to corrections, but manually encoding this feedback is infeasible. Active preference learning (APL) learns human reward functions by presenting trajectories for ranking. However, existing methods sample from fixed trajectory sets or replay buffers that limit query diversity and often fail to identify informative comparisons. We propose CRED, a novel trajectory generation method for APL that improves reward inference by jointly optimizing environment design and trajectory selection to efficiently query and extract preferences from users. CRED "imagines" new scenarios through environment design and leverages counterfactual reasoning -- by sampling possible rewards from its current belief and asking "What if this were the true preference?" -- to generate trajectory pairs that expose differences between competing reward functions. Comprehensive experiments and a user study show that CRED significantly outperforms state-of-the-art methods in reward accuracy and sample efficiency and receives higher user ratings.

---

## 论文详细总结（自动生成）

以下是对论文《CRED: Counterfactual Reasoning and Environment Design for Active Preference Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**机器人如何通过人类反馈高效学习奖励函数**。
*   **背景**：在复杂任务中，手动编写奖励函数（Reward Function）既困难又容易出错。主动偏好学习（APL）通过让用户在两段机器人轨迹中进行二选一（偏好排序）来推断奖励函数。
*   **核心问题**：现有的 APL 方法通常在**固定环境**中从**预生成的轨迹集**或回放缓冲区中采样。这导致查询（Query）缺乏多样性，难以覆盖罕见但具有高度信息量的场景，从而导致学习效率低下，且学到的奖励函数泛化性差。
*   **目标**：提出一种能够“主动设计环境”并“想象不同偏好后果”的方法，以最少的交互次数准确捕捉人类意图。

### 2. 方法论：核心思想与关键技术
CRED 框架将 APL 建模为一个**双层优化问题**，结合了两个核心技术：

*   **反事实推理 (Counterfactual Reasoning, CR)**：
    *   **核心思想**：从当前的奖励权重信念分布 $p(w)$ 中采样多个可能的权重 $w_i, w_j$。
    *   **操作流程**：假设 $w_i$ 或 $w_j$ 是真实偏好，生成对应的最优轨迹 $\xi_i, \xi_j$。通过对比这些“如果偏好是 X 会怎样”的轨迹，系统能更有效地暴露不同潜在奖励函数之间的差异。
*   **环境设计 (Environment Design, ED)**：
    *   **核心思想**：环境参数 $\theta$（如障碍物位置、地形分布）会限制可观察到的特征。CRED 主动寻找能最大化信息增益的环境。
    *   **技术细节**：使用**贝叶斯优化 (Bayesian Optimization)** 来搜索环境参数空间。外层优化寻找最优环境 $\theta^*$，内层优化在该环境下寻找最具区分性的轨迹对 $(\xi_A, \xi_B)$。
*   **算法流程**：
    1.  使用自适应 Metropolis 算法维护奖励权重的信念分布。
    2.  通过贝叶斯优化提议环境参数 $\theta$。
    3.  在 $\theta$ 下，利用 CR 生成候选轨迹对。
    4.  计算互信息（Mutual Information）作为信息增益指标。
    5.  将最优质的查询展示给人类，获取反馈并更新信念。

### 3. 实验设计
论文在三个具有代表性的仿真场景中进行了评估，并辅以用户研究：
*   **场景设置**：
    1.  **Lunar Lander (月球着陆器)**：学习着陆速度与位置的偏好，环境参数为风力大小。
    2.  **Tabletop Manipulation (桌面操作)**：学习避开特定电子产品的偏好，环境参数为物体摆放位置（通过 VAE 压缩空间）。
    3.  **Navigation (导航)**：学习对不同地形（草地、沥青等）的偏好，环境参数为道路网络的地形分配。
*   **Benchmark 与对比方法**：
    *   **RR (Random Rollouts)**：从随机生成的轨迹中选择。
    *   **MBP (Mean Belief Policy)**：基于当前平均信念生成的策略。
    *   **消融变体**：仅 CR（无环境设计）、MBP-ED（无反事实推理）、DR（领域随机化/随机环境）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **算法实现细节**：提到了使用 `Stable Baselines` 实现 PPO 算法（用于 Lunar Lander），使用 `CHOMP` 进行轨迹优化（用于桌面操作），以及使用 `BayesianOptimization` Python 库。这些算法通常在单张消费级 GPU 或高性能 CPU 上即可运行。

### 5. 实验数量与充分性
*   **仿真实验**：针对每个场景模拟了 10 个具有不同地面真值（Ground-truth）奖励权重的虚拟用户。
*   **消融实验**：设计了多组消融实验（如 CR vs. CR-DR），验证了环境设计和反事实推理的协同作用。
*   **用户研究**：招募了 **25 名真实参与者**（20-39 岁），在桌面操作和导航任务中进行实测。
*   **评价**：实验设计较为充分，涵盖了从底层控制到高层规划的不同维度，且通过用户研究验证了主观体验（NASA-TLX 负荷量表），结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **准确性与效率**：CRED 在所有任务中均达到了最高的奖励推断准确率（相关系数 $r$ 接近 0.9-1.0），且收敛速度显著快于基准方法。
*   **环境设计的重要性**：单纯增加环境多样性（领域随机化 DR）不如有针对性的环境设计（ED）有效。
*   **用户体验**：在用户研究中，CRED 生成的查询被认为“更容易做出选择”，且用户的**心理负荷（Mental Workload）显著降低**。
*   **泛化性**：通过在不同环境中进行查询，学到的奖励函数在未见过的场景中表现出更好的鲁棒性。

### 7. 优点
*   **创新性结合**：首次将环境设计与反事实推理结合用于 APL，打破了“固定环境”的局限。
*   **双层优化框架**：该框架具有通用性，可以适配不同的底层策略优化器（如 PPO、Value Iteration 或 CHOMP）。
*   **以人为本**：不仅关注算法指标，还通过 NASA-TLX 量表证明了该方法能减轻人类在提供反馈时的认知负担。

### 8. 不足与局限
*   **计算开销**：双层优化结构（外层 BO，内层轨迹生成）计算成本较高，可能难以实现极高频率的实时交互。
*   **环境参数化依赖**：环境设计依赖于对环境参数 $\theta$ 的定义。在极其复杂的开放世界中，如何定义和搜索 $\theta$ 仍是一个挑战（文中使用了 VAE 降维作为缓解方案）。
*   **线性奖励假设**：实验基于线性奖励模型 $R = w^T \Phi$，虽然这是 APL 的常见假设，但在处理极度非线性的复杂偏好时可能存在局限。

（完）
