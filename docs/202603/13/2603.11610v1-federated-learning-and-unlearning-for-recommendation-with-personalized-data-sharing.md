---
title: Federated Learning and Unlearning for Recommendation with Personalized Data Sharing
title_zh: 具有个性化数据共享的推荐联邦学习与遗忘学习
authors: "Liang Qu, Jianxin Li, Wei Yuan, Shangfei Zheng, Lu Chen, Chengfei Liu, Hongzhi Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11610v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 个性化用户偏好学习与数据共享
tldr: 针对联邦推荐系统中用户隐私偏好差异及数据撤回需求，本文提出FedShare框架。该框架允许用户自主选择数据共享比例，利用共享数据在服务端构建高阶用户-项目图，并结合对比学习对齐局部与全局表征。同时，针对数据撤回场景，设计了基于嵌入快照的对比遗忘学习机制，能高效移除撤回数据的影响并大幅降低存储开销。
selection_source: fresh_fetch
motivation: 现有联邦推荐系统通常假设所有用户均不共享数据，忽视了用户个性化的共享意愿以及撤回已共享数据的动态需求。
method: 通过服务端图对比学习增强推荐性能，并利用少量历史嵌入快照实现无需存储大量梯度的对比遗忘学习。
result: 在三个公开数据集上的实验表明，FedShare在学习和遗忘阶段均取得优异性能，且显著降低了存储成本。
conclusion: FedShare成功实现了支持个性化数据共享与高效模型遗忘的联邦推荐，为隐私保护下的动态数据管理提供了新思路。
---

## 摘要
联邦推荐系统 (FedRS) 已成为一种保护用户隐私的范式，它通过将交互数据保留在本地设备上，同时通过中央服务器协调模型训练。然而，大多数现有的联邦推荐系统对用户隐私采用“一刀切”的假设，即要求所有用户严格将数据保留在本地。这种设置忽略了那些愿意与服务器共享数据以换取更好推荐性能的用户。尽管最近的一些研究探索了 FedRS 中的个性化用户数据共享，但它们假设用户隐私偏好是静态的，无法处理用户删除先前共享数据及其对已训练模型相应影响的请求。为了解决这一局限性，我们提出了 FedShare，这是一个针对具有个性化用户数据共享的推荐系统的联邦学习-遗忘学习框架。FedShare 不仅允许用户控制与服务器共享多少交互数据，还支持通过从已训练模型中消除取消共享数据的影响来响应数据取消共享请求。具体而言，FedShare 利用共享数据构建服务器端的高阶用户-项目图，并使用对比学习来联合对齐本地和全局表示。在遗忘学习阶段，我们设计了一种对比遗忘学习机制，利用少量历史嵌入快照选择性地移除由取消共享数据诱导的表示，从而避免了现有联邦推荐遗忘学习方法所需的存储大量历史梯度信息的需求。在三个公开数据集上的广泛实验表明，FedShare 在学习和遗忘学习阶段均实现了强大的推荐性能，同时与最先进的基准方法相比，显著降低了遗忘学习阶段的存储开销。

## Abstract
Federated recommender systems (FedRS) have emerged as a paradigm for protecting user privacy by keeping interaction data on local devices while coordinating model training through a central server. However, most existing federated recommender systems adopt a one-size-fits-all assumption on user privacy, where all users are required to keep their data strictly local. This setting overlooks users who are willing to share their data with the server in exchange for better recommendation performance. Although several recent studies have explored personalized user data sharing in FedRS, they assume static user privacy preferences and cannot handle user requests to remove previously shared data and its corresponding influence on the trained model. To address this limitation, we propose FedShare, a federated learn-unlearn framework for recommender systems with personalized user data sharing. FedShare not only allows users to control how much interaction data is shared with the server, but also supports data unsharing requests by removing the influence of the unshared data from the trained model. Specifically, FedShare leverages shared data to construct a server-side high-order user-item graph and uses contrastive learning to jointly align local and global representations. In the unlearning phase, we design a contrastive unlearning mechanism that selectively removes representations induced by the unshared data using a small number of historical embedding snapshots, avoiding the need to store large amounts of historical gradient information as required by existing federated recommendation unlearning methods. Extensive experiments on three public datasets demonstrate that FedShare achieves strong recommendation performance in both the learning and unlearning phases, while significantly reducing storage overhead in the unlearning phase compared with state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 论文总结：具有个性化数据共享的推荐联邦学习与遗忘学习 (FedShare)

#### 1. 核心问题与研究动机
*   **研究背景**：联邦推荐系统（FedRS）通过将数据保留在本地来保护隐私，但现有系统通常采用“一刀切”的隐私假设，即强制所有用户不共享任何数据。
*   **核心问题**：
    1.  **忽视共享意愿**：部分用户愿意共享部分数据以换取更精准的推荐，现有系统难以利用这种异构的隐私偏好。
    2.  **动态隐私需求**：用户可能在共享后反悔（受 GDPR“被遗忘权”驱动），需要系统能高效移除特定数据及其对模型的影响。
    3.  **存储瓶颈**：现有的联邦遗忘学习方法（如 FRU, CUFRU）依赖存储大量历史梯度信息，对资源受限的客户端或服务器压力巨大。
*   **整体含义**：本文提出了 **FedShare** 框架，旨在统一处理“个性化数据共享”与“高效模型遗忘”，在提升推荐性能的同时，大幅降低遗忘阶段的存储开销。

#### 2. 方法论
FedShare 包含两个核心阶段：
*   **学习阶段（Learning Phase）**：
    *   **个性化共享**：用户自主决定共享全部、部分或不共享数据。服务器利用共享数据构建“高阶用户-项目二部图”。
    *   **双视图对比学习**：
        *   **本地视图**：通过标准的联邦聚合（FedAvg）获取。
        *   **全局视图**：在服务器端的高阶图上通过轻量级图卷积（LGC）传播获取。
    *   **对齐机制**：利用 InfoNCE 损失函数对齐本地与全局表示，使模型能同时捕获本地的一阶协同信号和服务器端的全局高阶信号。
*   **遗忘学习阶段（Unlearning Phase）**：
    *   **对比遗忘机制**：当用户请求撤回数据时，服务器更新共享图并移除对应边。
    *   **内存高效设计**：不存储梯度，仅保留少量历史嵌入快照（Embedding Snapshots）。
    *   **三视图协同**：
        1.  **新本地视图**：剩余数据在客户端训练后的聚合结果。
        2.  **新全局视图**：在更新后的服务器图中推导。
        3.  **遗忘视图**：利用历史快照在“被遗忘数据”构成的子图上推导。
    *   **优化目标**：通过对比损失，拉近新本地视图与新全局视图（保持性能），同时推开新本地视图与遗忘视图（消除影响）。

#### 3. 实验设计
*   **数据集**：使用了三个公开基准数据集：**MovieLens-1M**（电影）、**Fashion**（服装）、**Video Games**（游戏）。
*   **数据划分**：按 8:1:1 比例划分训练、验证和测试集。
*   **对比方法 (Baselines)**：
    *   **学习阶段**：FedAvg（基础聚合）、CDCGNNFed（图补全对比学习）、PDC-FRS（差分隐私共享）。
    *   **遗忘阶段**：Retrain（从头重练，作为金标准）、FRU（基于梯度的遗忘）、CUFRU（基于梯度转移站的遗忘）。
*   **评价指标**：Hit Ratio (HR@20) 和 NDCG@20。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及具体的训练时长。
*   **效率分析**：文中通过实验对比了遗忘阶段的“执行时间”和“存储开销（MB）”，结果显示 FedShare 的存储需求远低于基于梯度的 SOTA 方法。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个不同领域的数据集上进行了主实验。
    *   **消融实验**：分别验证了对比学习（CL）、BPR 损失、遗忘图、剩余数据联邦训练等组件的有效性。
    *   **敏感性分析**：涵盖了用户共享比例、对比损失权重、历史快照数量（M）、遗忘比例（$\gamma$）等多个维度。
*   **充分性评价**：实验设计较为全面，不仅关注推荐精度，还深入探讨了遗忘效率和存储成本，对比了从头训练的“理想状态”，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：FedShare 在学习阶段优于所有基准，证明了结合服务器端高阶图与对比学习的有效性。
*   **遗忘效果**：在遗忘阶段，FedShare 的推荐精度接近“从头重练”（Retrain），且优于 FRU 和 CUFRU。
*   **存储优势**：FedShare 显著降低了存储开销，因为它只需存储极少量的嵌入快照，而非每一轮的梯度信息。
*   **鲁棒性**：随着共享数据比例增加，性能稳步提升；在不同遗忘比例下均保持了稳健的表现。

#### 7. 优点与亮点
*   **机制创新**：首次在联邦推荐中统一考虑了“个性化共享”与“动态遗忘”的闭环。
*   **存储友好**：通过“对比遗忘”替代“梯度校准”，解决了联邦遗忘学习中最核心的存储瓶颈问题。
*   **视图对齐**：巧妙利用服务器端共享数据构建全局视图，弥补了联邦学习中本地数据稀疏、缺乏高阶信息的短板。

#### 8. 不足与局限
*   **客户端依赖**：遗忘过程仍需剩余用户参与辅助训练，这会增加客户端的通信和计算负担。
*   **覆盖范围有限**：目前的遗忘机制主要针对“已共享数据”的撤回，对于从未共享数据但参与过联邦训练的用户的遗忘支持尚不明确。
*   **算力细节缺失**：缺乏具体的硬件环境和训练耗时细节，不便于其他研究者进行精确的复现对比。

（完）
