---
title: "Theory Discovery in Social Networks: Automating ERGM Specification with Large Language Models"
title_zh: 社交网络中的理论发现：利用大语言模型实现 ERGM 规范化的自动化
authors: "Yidan Sun, Mayank Kejriwal"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04306v1"
tags: ["query:social-computing"]
score: 9.0
evidence: 利用大语言模型自动发现社交网络形成理论
tldr: 本研究针对社会网络分析中指数随机图模型（ERGM）建模门槛高的问题，提出了 Forge 框架。该框架利用大语言模型将定性社会假设自动转化为统计规范，并通过统计约束和拟合优度诊断进行迭代优化。在多个基准网络上的实验证明，Forge 能有效降低人工建模负担，在保证模型稳定性的同时显著提升拟合精度，为自动化社会网络理论发现提供了新路径。
selection_source: fresh_fetch
motivation: 将定性的社会学假设转化为稳定的 ERGM 统计模型需要深厚的领域知识和繁琐的手动调优。
method: 开发了 Forge 框架，利用 LLM 提出形成机制建议，并结合统计守卫进行可行性验证与迭代优化。
result: "在 12 个基准网络中，Forge 在 10 个案例中成功收敛，且在收敛情况下有 90% 达到了最佳的似然拟合效果。"
conclusion: Forge 证明了结合 LLM 推理与统计约束可以有效自动化 ERGM 规范过程，降低了计算社会科学的研究门槛。
---

## 摘要
理解社交网络如何形成（无论是通过互惠性、共同属性还是三元闭包）是计算社会科学的核心。指数随机图模型（ERGMs）为测试此类形成理论提供了一个原则性框架，但将定性的社会假设转化为稳定的统计规范仍然是一个重大障碍，需要具备网络理论和模型估计方面的专业知识。我们提出了 Forge（面向形成的带保护 ERGM 推理），这是一个利用大语言模型自动完成这种转化的框架。给定一个网络和社交背景的非正式描述，Forge 会提出候选的形成机制，根据可行性和稳定性约束对其进行验证，并利用拟合优度诊断迭代地改进规范。对涵盖学校、组织和在线通信的 12 个基准网络进行的评估表明，Forge 在 12 个案例中有 10 个实现了收敛；在收敛的情况下，它在 10 个案例中有 9 个达到了基于似然的最佳拟合，同时满足了充分性阈值。通过将基于 LLM 的提议与统计护栏相结合，Forge 减少了 ERGM 规范化所需的人工工作。

## Abstract
Understanding how social networks form, whether through reciprocity, shared attributes, or triadic closure, is central to computational social science. Exponential Random Graph Models (ERGMs) offer a principled framework for testing such formation theories, but translating qualitative social hypotheses into stable statistical specifications remains a significant barrier, requiring expertise in both network theory and model estimation. We present Forge (Formation-Oriented Reasoning with Guarded ERGMs), a framework that uses large language models to automate this translation. Given a network and an informal description of the social context, Forge proposes candidate formation mechanisms, validates them against feasibility and stability constraints, and iteratively refines specifications using goodness-of-fit diagnostics. Evaluation across twelve benchmark networks spanning schools, organizations, and online communication shows that Forge converges in 10 of 12 cases, and conditional on convergence it achieves the best likelihood-based fit in 9 of 10 while meeting adequacy thresholds. By combining LLM-based proposals with statistical guardrails, Forge reduces the manual effort required for ERGM specification.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FORGE**（Formation-Oriented Reasoning with Guarded ERGMs）的框架，旨在利用大语言模型（LLM）自动化社交网络分析中复杂的指数随机图模型（ERGM）规范化过程。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在计算社会科学中，ERGM 是测试社交网络形成理论（如互惠性、同质性、三元闭包）的标准统计工具。然而，将定性的社会学假设转化为稳定的统计模型（即选择合适的模型项）极具挑战性，需要深厚的领域知识和统计专业知识，且容易出现模型退化（Degeneracy）或不收敛。
*   **核心问题**：如何降低 ERGM 建模的门槛，实现从非正式的社交背景描述到科学、稳定且可解释的统计规范的自动转化？

### 2. 方法论：FORGE 框架
FORGE 结合了 LLM 的推理能力与严格的统计约束，分为四个阶段：
1.  **候选规范生成（Stage I）**：提取网络诊断指标（如密度、聚类系数）和元数据，提示 LLM 根据社交背景提议候选的 ERGM 项。通过预定义的“可行性与稳定性约束”过滤掉不合规的项，形成候选池。
2.  **筛选与模型选择（Stage II）**：利用快速的极大伪似然估计（MPLE）对候选模型进行初步拟合，根据 BIC（贝叶斯信息准则）和初步稳定性检查筛选出最佳初始模型。
3.  **迭代规范优化（Stage III）**：使用全似然估计（MCMLE）拟合模型。若拟合优度（GOF）不达标，则将残差诊断信息反馈给 LLM，由其建议单项修改（增加、删除或替换），直到模型在统计上达标且 BIC 最小。
4.  **事后理论解释（Stage IV）**：将最终拟合的统计项翻译回人类可读的社会学机制描述，确保模型不仅拟合好，而且可理解。

### 3. 实验设计
*   **数据集**：使用了 12 个经典的社交网络基准，涵盖学校友谊（Faux Mesa 等）、组织建议（Lazega Lawyers）、工作场所（Kapferer Tailor Shop）、电子邮件通信（Enron）和恐怖分子网络（Noordin Top）等。
*   **对比方法（Baselines）**：
    *   **M1 (Random-K)**：随机选择模型项。
    *   **M2 (Null)**：仅包含边数的空模型。
    *   **M3-M5**：不同提示策略下的 LLM 直接生成（One-shot, Few-shot, Unconstrained）。
*   **LLM 后端**：测试了 GPT-4o, GPT-4o-mini, Claude 3.5 Sonnet, Gemini 2.5 Pro 和 Llama 3.1-70B。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。由于该方法主要依赖于调用 LLM API（推理阶段）和 R 语言的 `statnet` 包进行 MCMC 采样（估计阶段），其计算开销主要集中在 MCMLE 的迭代模拟上。
*   **配置**：论文提到 MCMC 采样参数根据网络规模进行了调整，并在不同模型间保持一致以确保公平。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同领域、不同规模的网络上进行了测试，并横向对比了 5 种主流 LLM 后端。
*   **充分性评价**：实验设计较为充分。不仅评估了模型的统计拟合度（BIC、GOF），还通过消融实验验证了 LLM 提议的准确率、召回率，以及事后解释的忠实度。实验过程考虑了随机性，使用了确定性解码（Temperature=0）来增强可重复性。

### 6. 主要结论与发现
*   **收敛性**：FORGE 在 12 个网络中的 10 个成功收敛（在大规模或极度异构的网络如 Caltech36 上失败）。
*   **拟合精度**：在收敛的情况下，FORGE 在 90% 的案例中取得了比所有基准方法更好的 BIC 拟合效果，且均通过了拟合优度（GOF）检验。
*   **LLM 表现**：Gemini 2.5 Pro 在术语提议的准确率和召回率上表现最均衡；Claude 3.5 和 GPT-4o 在理论合成解释上最为精准。
*   **自动化价值**：证明了 LLM 可以作为“推理引擎”，在统计护栏的保护下，有效地模仿人类专家的建模迭代过程。

### 7. 优点与亮点
*   **统计守卫（Guarded Interface）**：并非盲目信任 LLM，而是引入了硬性的统计约束和诊断反馈循环，解决了 LLM 容易产生幻觉或生成不可估计模型的问题。
*   **端到端流程**：从非正式描述到最终的理论解释，实现了全流程自动化，极大地降低了非专家用户使用 ERGM 的难度。
*   **可解释性**：Stage IV 的设计确保了自动化发现的理论具有社会学意义，而非纯粹的曲线拟合。

### 8. 不足与局限
*   **复杂网络挑战**：在处理超大规模网络或具有极强依赖结构的社交系统时，MCMLE 估计仍可能失败，LLM 无法解决底层统计估计的固有困难。
*   **硬性拒绝策略**：目前对于不收敛的情况采取直接拒绝，缺乏更灵活的参数调整机制（如自动调整 MCMC 步长）。
*   **依赖库限制**：模型的表达能力受限于底层 `statnet` 库已实现的统计项，无法自主创造全新的数学统计量。

（完）
