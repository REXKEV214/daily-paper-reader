---
title: Causally Robust Reward Learning from Reason-Augmented Preference Feedback
title_zh: 基于理由增强偏好反馈的因果鲁棒奖励学习
authors: "Minjune Hwang, Yigit Korkmaz, Daniel Seita, Erdem Bıyık"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04861v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 基于自然语言解释的偏好奖励学习
tldr: 针对基于偏好的奖励学习中容易出现的因果混淆问题，本文提出了ReCouPLe框架。该框架利用自然语言理由作为因果信号，将理由视为嵌入空间中的投影轴，引导模型根据与理由相关的特征对轨迹进行评分。ReCouPLe能有效抑制虚假相关性，并支持跨任务的偏好知识迁移。实验表明，该方法在分布偏移下的奖励准确度和下游策略性能上均显著优于基准模型，展现了极强的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的基于偏好的奖励学习因反馈稀疏，容易使模型捕捉到与用户意图无关的虚假相关特征，导致在环境变化时失效。
method: ReCouPLe框架将自然语言理由作为嵌入空间中的投影轴，训练模型仅根据与理由语义对齐的特征进行评分，从而排除无关背景干扰。
result: 实验显示该方法在分布偏移下的奖励准确度提升达1.5倍，且在处理新任务时的下游策略性能比基准模型高出2倍。
conclusion: 引入自然语言理由作为因果引导，能有效解决奖励学习中的因果混淆问题，提升模型在复杂和多任务场景下的泛化能力。
---

## 摘要
基于偏好的奖励学习被广泛用于塑造智能体行为以匹配用户偏好，但其稀疏的二元反馈使其特别容易受到因果混淆的影响。学习到的奖励往往会锁定在训练期间仅与偏好轨迹共同出现的伪特征上，当这些相关性在测试时消失或反转时，模型性能就会崩溃。我们引入了 ReCouPLe，这是一个轻量级框架，利用自然语言理由来提供缺失的因果信号。每个理由都被视为嵌入空间中的引导投影轴，训练模型根据与该轴对齐的特征对轨迹进行评分，同时削弱与所述理由无关的上下文。由于相同的理由（例如“避免碰撞”、“更快完成任务”）可以出现在多个任务中，每当任务共享语义时，ReCouPLe 就会自然地重用相同的因果方向，并在无需额外数据或语言模型微调的情况下，将偏好知识迁移到新任务中。我们学习到的奖励模型可以将偏好建立在明确阐述的理由之上，从而更好地与用户意图对齐，并泛化到伪特征之外。在分布偏移下，ReCouPLe 的奖励准确率比基准模型高出多达 1.5 倍，在新任务的下游策略性能上高出 2 倍。我们已在 https://github.com/mj-hwang/ReCouPLe 发布了代码。

## Abstract
Preference-based reward learning is widely used for shaping agent behavior to match a user's preference, yet its sparse binary feedback makes it especially vulnerable to causal confusion. The learned reward often latches onto spurious features that merely co-occur with preferred trajectories during training, collapsing when those correlations disappear or reverse at test time. We introduce ReCouPLe, a lightweight framework that uses natural language rationales to provide the missing causal signal. Each rationale is treated as a guiding projection axis in an embedding space, training the model to score trajectories based on features aligned with that axis while de-emphasizing context that is unrelated to the stated reason. Because the same rationales (e.g., "avoids collisions", "completes the task faster") can appear across multiple tasks, ReCouPLe naturally reuses the same causal direction whenever tasks share semantics, and transfers preference knowledge to novel tasks without extra data or language-model fine-tuning. Our learned reward model can ground preferences on the articulated reason, aligning better with user intent and generalizing beyond spurious features. ReCouPLe outperforms baselines by up to 1.5x in reward accuracy under distribution shifts, and 2x in downstream policy performance in novel tasks. We have released our code at https://github.com/mj-hwang/ReCouPLe

---

## 论文详细总结（自动生成）

### 论文总结：基于理由增强偏好反馈的因果鲁棒奖励学习 (ReCouPLe)

#### 1. 核心问题与整体含义（研究动机和背景）
在基于偏好的强化学习（PbRL）中，用户通常通过二元比较（如“轨迹 A 优于 B”）来训练奖励模型。然而，这种反馈信息量极低（仅 1 bit），导致模型容易产生**因果混淆（Causal Confusion）**：模型可能会锁定在与偏好标签偶然相关的虚假特征（Spurious Features）上，而非真正的因果特征。例如，如果训练中大盒子总是红色的，模型可能错误地认为“红色”是奖励来源。当测试环境下大盒子变为蓝色时，模型就会失效。本文旨在通过引入**自然语言理由（Rationales）**作为额外的因果信号，引导模型识别真正的偏好原因。

#### 2. 方法论：核心思想与技术细节
论文提出了 **ReCouPLe** 框架，其核心思想是将自然语言理由作为嵌入空间中的**投影轴**，将轨迹表示分解为与理由相关的部分和无关的部分。

*   **核心流程**：
    1.  **表示分解**：使用预训练语言模型（如 T5）将任务描述和理由编码为向量 $\theta$ 和 $\psi$。将轨迹嵌入 $\phi(\tau)$ 正交分解为：
        *   **理由对齐部分 ($\phi_{\parallel}$)**：平行于理由向量 $\psi$ 的分量，代表由理由解释的特征。
        *   **理由正交部分 ($\phi_{\perp}$)**：与理由垂直的分量，代表其他任务相关但未被理由提及的特征。
    2.  **奖励建模**：总奖励定义为轨迹嵌入与任务嵌入的内积。通过分解，奖励也分为 $r_{\parallel}$（理由解释部分）和 $r_{\perp}$（残余信号）。
*   **关键损失函数**：
    *   **Reason Loss ($L_{reason}$)**：强制偏好仅由 $r_{\parallel}$ 决定。
    *   **Consistency Loss**：确保 $r_{\perp}$ 不包含偏好信号。分为 **EC (等式约束)**（强制 A 和 B 的正交部分奖励相等）和 **IC (不等式约束)**（允许差异但需小于理由部分的差异）。
    *   **Ratio Regularizer**：防止奖励完全塌陷到理由子空间，保持表示的丰富性。

#### 3. 实验设计
论文在两个主要的机器人操纵基准上进行了评估：
*   **ManiSkill 场景 (针对 RQ1: 因果鲁棒性)**：设计了 4 个任务（抓取、放置等），故意将物体“尺寸”（因果特征）与“颜色”（虚假特征）在训练集中强耦合。测试时交换颜色，验证模型是否仍能识别“尺寸”才是偏好原因。
*   **Meta-World 场景 (针对 RQ2: 任务迁移)**：在 3 个训练任务上学习，然后在未见过的第 4 个任务（Pick-Place）上进行零样本（Zero-shot）奖励迁移。
*   **对比方法 (Benchmarks)**：
    *   **BT-Multi**：标准的多任务 Bradley-Terry 偏好学习模型（无理由输入）。
    *   **RFP (Reason-Feature Preference)**：将理由作为辅助任务的基准方法。
    *   **BC (Behavior Cloning)**：作为下游策略性能的参考。

#### 4. 资源与算力
*   论文提到使用了南加州大学（USC）的高性能计算中心（CARC）资源。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量、单次实验的训练时长或总计算开销。

#### 5. 实验数量与充分性
实验设计非常全面且具有说服力：
*   **多维度评估**：涵盖了基于状态（State-based）和基于图像（Image-based）的控制任务。
*   **鲁棒性测试**：进行了语言多样性实验（使用 16 种不同的同义词/句式表达同一理由），证明模型并非死记硬背关键词。
*   **标签效率实验**：测试了在仅有 25% 或 50% 的偏好对附带理由时的性能，证明了该方法的扩展性。
*   **消融实验**：验证了一致性损失和比例正则化项的必要性。
*   **统计显著性**：所有实验均基于 3 个随机种子取平均值，并提供了标准差。

#### 6. 主要结论与发现
*   **因果鲁棒性**：在分布偏移（颜色交换）的情况下，ReCouPLe 的奖励预测准确率比基准模型高出 1.5 倍。
*   **任务迁移**：通过共享的理由语义空间，ReCouPLe 在新任务上的下游策略成功率比基准模型高出 2 倍。
*   **视觉抗干扰**：在复杂的视觉背景干扰下，ReCouPLe 能有效分离背景颜色与操作目标的因果关系。
*   **标签效率**：即使只有少量偏好对附带理由，其产生的因果信号也能通过共享编码器传播到整个数据集。

#### 7. 优点
*   **原理简洁且有效**：利用正交投影这一线性代数工具，巧妙地实现了语言语义与物理特征的解耦。
*   **无需微调 LLM**：使用冻结的预训练语言模型，计算开销低，易于集成到现有 PbRL 流程中。
*   **跨任务组合性**：理由（如“避免碰撞”）在不同任务间具有通用性，天然支持知识迁移。

#### 8. 不足与局限
*   **理由获取成本**：虽然实验证明了稀疏理由也有效，但在现实大规模应用中，获取高质量、准确的自然语言理由仍比单纯的二元点击成本更高。
*   **理由准确性假设**：模型假设用户提供的理由是正确且与偏好一致的，未讨论如果用户提供错误或误导性理由（Noisy Rationales）时的鲁棒性。
*   **模拟环境局限**：目前所有实验均在模拟器（ManiSkill, Meta-World）中完成，尚未在真实的物理机器人或更复杂的开放世界场景中验证。

（完）
