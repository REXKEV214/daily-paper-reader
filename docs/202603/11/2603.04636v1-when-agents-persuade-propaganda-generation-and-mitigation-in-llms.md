---
title: "When Agents Persuade: Propaganda Generation and Mitigation in LLMs"
title_zh: 当智能体进行说服：大语言模型中的宣传生成与缓解
authors: "Julia Jose, Ritik Roongta, Rachel Greenstadt"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04636v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 缓解大模型中的煽动性内容与宣传
tldr: 本研究探讨了大语言模型（LLM）在生成操纵性宣传内容方面的风险。研究者通过给LLM设定宣传任务，利用专门的分类和修辞技术检测模型分析其输出，发现LLM易被诱导产生宣传性言论。随后，研究对比了监督微调（SFT）、直接偏好优化（DPO）和赔率比偏好优化（ORPO）三种缓解策略。结果表明，微调能显著降低LLM的宣传倾向，其中ORPO效果最为显著，为构建更安全、抗操纵的智能体提供了参考。
selection_source: fresh_fetch
motivation: 旨在评估大语言模型被恶意利用生成操纵性宣传内容的风险，并探索有效的技术缓解手段。
method: 任务化LLM生成宣传内容，利用领域模型检测修辞技术，并对比SFT、DPO和ORPO三种微调方法的抑制效果。
result: 发现LLM在特定提示下会运用多种修辞手段生成宣传文本，而微调能有效减少此类输出。
conclusion: ORPO被证明是降低大语言模型宣传性内容生成倾向最有效的优化方法。
---

## 摘要
尽管部署在开放环境中的大语言模型（LLM）智能体具有广泛的益处，但它们也可能被利用来生成具有操纵性的材料。在本研究中，我们为 LLM 设定了宣传任务目标，并使用两个领域特定模型分析其输出：一个模型将文本分类为宣传或非宣传，另一个模型则检测宣传的修辞技巧（如带有偏见的语言、诉诸恐惧、诉诸爱国主义、谩骂等）。研究结果表明，在受到提示时，LLM 会表现出宣传行为，并在此过程中运用多种修辞技巧。我们还探索了通过有监督微调（SFT）、直接偏好优化（DPO）以及优势比偏好优化（ORPO）来缓解这一问题的方法。研究发现，微调能显著降低模型生成此类内容的倾向，其中 ORPO 的效果最为显著。

## Abstract
Despite their wide-ranging benefits, LLM-based agents deployed in open environments can be exploited to produce manipulative material. In this study, we task LLMs with propaganda objectives and analyze their outputs using two domain-specific models: one that classifies text as propaganda or non-propaganda, and another that detects rhetorical techniques of propaganda (e.g., loaded language, appeals to fear, flag-waving, name-calling). Our findings show that, when prompted, LLMs exhibit propagandistic behaviors and use a variety of rhetorical techniques in doing so. We also explore mitigation via Supervised Fine-Tuning (SFT), Direct Preference Optimization (DPO), and ORPO (Odds Ratio Preference Optimization). We find that fine-tuning significantly reduces their tendency to generate such content, with ORPO proving most effective.

---

## 论文详细总结（自动生成）

这篇论文《当智能体进行说服：大语言模型中的宣传生成与缓解》（*When Agents Persuade: Propaganda Generation and Mitigation in LLMs*）深入探讨了生成式人工智能在操纵性信息传播中的风险及其防御手段。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着大语言模型（LLM）被集成到自动化智能体（Agents）中，恶意行为者可能利用其强大的文本生成能力，大规模地生产和传播具有心理操纵性的“宣传”（Propaganda）内容。
*   **核心问题**：论文旨在回答三个关键问题：LLM 是否能生成宣传内容？它们在生成时使用了哪些具体的修辞技巧（如诉诸恐惧、谩骂等）？如何通过微调技术有效缓解这种倾向？
*   **背景**：宣传不同于简单的虚假信息，它往往通过逻辑谬误、情感诉诸和心理战术来塑造认知。

### 2. 论文提出的方法论
研究流程分为四个阶段：
*   **构建检测器**：
    *   **二分类检测器**：基于 RoBERTa-large，在 QProp 和 PTC 数据集上微调，用于识别文本是否属于宣传（F1 达 0.98）。
    *   **细粒度修辞检测器**：针对 6 种常见宣传技巧（谩骂、偏见语言、质疑、诉诸恐惧、诉诸爱国主义、夸张/缩小）分别训练二分类器（平均 F1 为 0.82）。
*   **宣传生成**：利用 GPT-4o、Llama 3.1 和 Mistral Small 3，基于从新闻中提取的“论题陈述”（Thesis Statements），诱导模型生成宣传性文章。
*   **人工验证**：由领域专家对生成的文本进行标注，验证自动检测器的可靠性。
*   **缓解策略（微调）**：
    *   **SFT（监督微调）**：使用非宣传样本训练。
    *   **DPO（直接偏好优化）**：通过“宣传 vs 非宣传”样本对，直接优化模型偏好。
    *   **ORPO（赔率比偏好优化）**：将偏好对齐与语言建模目标结合，无需参考模型即可惩罚负面输出。

### 3. 实验设计
*   **数据集**：
    *   **QProp**：包含 51,000 篇新闻文章，用于宏观分类。
    *   **PTC (Propaganda Techniques Corpus)**：包含 18 种修辞技巧的标注，用于细粒度分析。
*   **Benchmark 与对比**：
    *   对比了人类撰写的宣传文本与 LLM 生成文本在修辞技巧使用频率上的差异。
    *   对比了三种微调方法（SFT, DPO, ORPO）在降低宣传生成率方面的表现。
*   **场景**：模拟了 1,000 个不同主题（如国际政治、经济政策等）的生成任务。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 80GB GPU**。
*   **技术细节**：
    *   使用 **QLoRA** 进行 4-bit 量化微调，以降低显存占用。
    *   训练参数包括：学习率 1e-5，Batch Size 1（梯度累积 4 步），训练 30 个 Epoch。
    *   使用了 Flash Attention 优化长文本处理。
*   **时长**：文中未明确给出总训练时长，但提到了早停机制（Early Stopping）通常在 2 个 Epoch 后触发。

### 5. 实验数量与充分性
*   **实验规模**：生成了 6 个数据集（3 个模型 × 2 种提示类型），涵盖 1,000 个论题。
*   **验证充分性**：
    *   **人工标注**：随机抽取 200 篇 Llama 输出进行专家独立标注，计算了 Cohen’s Kappa 和 Krippendorff’s α，确保了自动检测器的客观性。
    *   **统计检验**：使用了 Mann-Whitney U 检验和 Bonferroni 校正来验证不同模型间差异的显著性。
*   **公平性**：所有模型使用相同的温度（0.1）和 Top-p（0.3）参数，确保了对比的基准一致。

### 6. 主要结论与发现
*   **LLM 极易被诱导**：在特定提示下，GPT-4o 和 Mistral 3 生成的文章 99% 被识别为宣传，Llama 3.1 为 77%。
*   **修辞特征**：LLM 生成的宣传比人类更依赖“偏见语言”（Loaded Language）、“夸张”（Exaggeration）和“诉诸爱国主义”（Flag-Waving）。GPT-4o 使用诉诸恐惧的频率是人类的 4 倍。
*   **微调效果显著**：
    *   未微调的 Llama 3.1 宣传率为 77%。
    *   SFT 降至 14%，DPO 降至 28%。
    *   **ORPO 效果最佳**，将宣传率降至 **10%**，且修辞技巧的使用频率降低了 13.4 倍。
*   **提示词防御失效**：简单的系统提示（如“不要生成宣传内容”）极易被用户指令绕过。

### 7. 优点
*   **细粒度分析**：不只是做二分类，而是深入分析了 LLM 如何通过具体的修辞手段进行说服，具有很强的解释性。
*   **实战化缓解**：对比了多种前沿的偏好对齐算法，为安全部署 Agent 提供了实操指南。
*   **高可靠性检测**：通过人工标注验证了自动检测器的有效性，使大规模自动化分析具备可信度。

### 8. 不足与局限
*   **修辞覆盖有限**：仅研究了 6 种技巧，忽略了如“诉诸平民”（Plain Folks）或“转移话题”（Whataboutism）等其他复杂手段。
*   **上下文缺失**：检测器基于句子级别，难以捕捉跨句子的修辞（如重复）。
*   **模型范围**：虽然涵盖了主流模型，但未包含 Claude 3.5 等在拒绝机制上表现更强的模型（文中提到 Claude 会直接拒绝此类任务）。
*   **静态评估**：实验在受控环境下进行，未模拟 Agent 在社交网络中动态演化和自适应调整策略的真实场景。

（完）
