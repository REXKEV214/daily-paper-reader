---
title: "RecThinker: An Agentic Framework for Tool-Augmented Reasoning in Recommendation"
title_zh: RecThinker：一种用于推荐中工具增强推理的智能体框架
authors: "Haobo Zhang, Yutao Zhu, Kelong Mao, Tianhao Li, Zhicheng Dou"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09843v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 处理碎片化用户画像的代理推理框架
tldr: 针对现有推荐系统大模型代理在处理碎片化信息时表现出的被动性与局限性，本文提出RecThinker框架。该框架采用“分析-计划-执行”范式，使代理能自主识别信息缺口并动态调用工具获取用户、物品及协同信息。通过结合监督微调与强化学习的自增强训练，RecThinker实现了从被动处理到主动调查的转变，显著提升了推荐的推理能力与准确性。
selection_source: fresh_fetch
motivation: 现有推荐代理多依赖静态工作流或受限信息，难以在用户画像碎片化或元数据稀疏时做出最优决策。
method: 提出一种基于“分析-计划-执行”范式的代理框架，通过自主工具调用获取多维信息，并利用SFT和强化学习优化推理路径。
result: 在多个基准数据集上的实验证明，RecThinker在推荐性能和工具使用效率方面均优于现有的强基准模型。
conclusion: 该研究表明，将推荐过程从被动信息处理转变为主动的工具增强型自主调查，能有效增强大模型的推理与匹配能力。
---

## 摘要
大语言模型（LLMs）通过提供卓越的推理和灵活的决策能力，彻底改变了推荐智能体。然而，现有方法主要遵循被动的信息获取范式，智能体要么依赖静态的预定义工作流，要么在受限的信息下进行推理。这限制了智能体识别信息充分性的能力，在面对碎片化的用户画像或稀疏的物品元数据时，往往会导致次优的推荐结果。为了解决这些局限性，我们提出了 RecThinker，这是一种用于推荐中工具增强推理的智能体框架，它通过动态规划推理路径并通过自主工具调用主动获取关键信息，将推荐从被动处理转变为自主调查。具体而言，RecThinker 采用了“分析-规划-执行”（Analyze-Plan-Act）范式，首先分析用户-物品信息的充分性，并自主调用工具序列，以弥合现有知识与推理需求之间的信息鸿沟。我们为 RecThinker 开发了一套专门的工具，使模型能够获取用户侧、物品侧和协同信息，从而实现更好的推理和用户-物品匹配。此外，我们引入了一个自增强训练流水线，包括用于内化高质量推理轨迹的有监督微调（SFT）阶段，以及用于优化决策准确性和工具使用效率的强化学习（RL）阶段。在多个基准数据集上的广泛实验表明，RecThinker 在推荐场景中始终优于强基准模型。

## Abstract
Large Language Models (LLMs) have revolutionized recommendation agents by providing superior reasoning and flexible decision-making capabilities. However, existing methods mainly follow a passive information acquisition paradigm, where agents either rely on static pre-defined workflows or perform reasoning with constrained information. It limits the agent's ability to identify information sufficiency, often leading to suboptimal recommendations when faced with fragmented user profiles or sparse item metadata. To address these limitations, we propose RecThinker, an agentic framework for tool-augmented reasoning in recommendation, which shifts recommendation from passive processing to autonomous investigation by dynamically planning reasoning paths and proactively acquiring essential information via autonomous tool-use. Specifically, RecThinker adopts an Analyze-Plan-Act paradigm, which first analyzes the sufficiency of user-item information and autonomously invokes tool-calling sequences to bridge information gaps between available knowledge and reasoning requirements. We develop a suite of specialized tools for RecThinker, enabling the model to acquire user-side, item-side, and collaborative information for better reasoning and user-item matching. Furthermore, we introduce a self-augmented training pipeline, comprising a Supervised Fine-Tuning (SFT) stage to internalize high-quality reasoning trajectories and a Reinforcement Learning (RL) stage to optimize for decision accuracy and tool-use efficiency. Extensive experiments on multiple benchmark datasets demonstrate that RecThinker consistently outperforms strong baselines in the recommendation scenario.

---

## 论文详细总结（自动生成）

### 论文总结：RecThinker - 推荐系统中的工具增强型推理智能体框架

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 推荐智能体大多处于“被动信息处理”模式，即依赖预定义的工作流或受限的静态信息进行推理。这种模式无法识别信息的“充分性”，在面对碎片化的用户画像或稀疏的物品元数据时，容易产生次优推荐。
*   **研究动机**：作者希望将推荐智能体从“被动处理者”转变为“主动调查者”（Agent-as-Investigator）。智能体应能自主分析当前信息与决策需求之间的差距（Information Gap），并通过主动调用外部工具来弥合这一差距，从而实现更精准的推理和匹配。

#### 2. 方法论
RecThinker 采用 **Analyze-Plan-Act（分析-计划-执行）** 范式，核心架构包括：
*   **推理工作流**：
    1.  **分析（Analyze）**：评估当前用户侧（$K_u$）和物品侧（$K_{ci}$）信息的充分性，识别信息缺口 $\Delta_t$。
    2.  **计划（Plan）**：根据缺口决定是直接进行排名（RANK），还是调用工具获取证据。
    3.  **执行（Act）**：调用工具并获取观察结果（Observation），更新推理状态。
*   **专用工具集**：
    *   **用户侧**：用户画像搜索（长期偏好）、历史记录搜索（支持回溯）。
    *   **物品侧**：物品信息搜索（元数据及关联图谱）。
    *   **协同侧**：相似用户搜索（混合信号）、知识图谱搜索（多步路径检索）。
*   **两阶段训练策略**：
    1.  **自增强 SFT**：利用基础模型生成推理轨迹，通过准确性和格式过滤出高质量样本进行微调，稳定推理模式。
    2.  **强化学习（RL）优化**：采用 **GRPO** 算法，针对困难样本进行策略改进。设计了复合奖励函数，包括准确性奖励（NDCG）、格式奖励和工具使用效率奖励（防止过度调用或调用不足）。

#### 3. 实验设计
*   **数据集**：使用了两个经典数据集的四个变体：Amazon CD & Vinyl（稀疏/稠密版）、MovieLens-1M（稀疏/稠密版）。
*   **Benchmark 任务**：在给定 10 个候选物品（1 个正样本 + 9 个负样本）的情况下进行排名任务。
*   **对比方法**：
    *   传统模型：Pop, BPR-MF, SASRec。
    *   LLM 基础方法：LLMSeqSim, LLMRank。
    *   推理/智能体方法：R2Rec, AgentCF, PersonaX。

#### 4. 资源与算力
*   **模型骨干**：主要使用 **QWQ-32B** 作为智能体核心，并在泛化性实验中测试了 **Qwen2.5-7B**。
*   **训练细节**：采用 **LoRA** 技术进行微调。SFT 阶段使用 8,000 个样本（3 epochs），RL 阶段使用 1,000 个困难样本（5 epochs）。
*   **算力说明**：文中未明确给出具体的 GPU 型号、数量或总训练时长，但提到 RL 阶段每组采样 8 条轨迹（G=8），这在 32B 模型规模下通常需要多张 A100/H100 级别的显卡支持。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比、消融实验（针对训练阶段、奖励函数、工具模块）、骨干模型泛化性实验、以及序列长度影响分析。
*   **充分性评价**：实验设计较为全面。通过在不同密度（Sparse/Dense）的数据集上测试，验证了模型在处理信息缺失时的鲁棒性。消融实验逐一拆解了 SFT、RL 及 5 种工具的作用，证明了各组件的必要性。对比基准涵盖了从传统 CF 到最新 Agent 框架的多种路径，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：RecThinker 在所有数据集上均显著优于强基准模型，NDCG@10 提升幅度达 7.61% 至 11.79%。
*   **主动调查的价值**：实验证明，主动获取用户历史和物品细节工具对性能贡献最大，验证了“弥合信息缺口”逻辑的有效性。
*   **RL 的作用**：RL 阶段不仅提升了准确性，还显著优化了工具调用的效率，使智能体学会“适时收手”，避免冗余查询。
*   **泛化能力**：即使将骨干模型缩小至 7B，RecThinker 依然能保持较强的竞争力，说明该框架具有良好的可移植性。

#### 7. 优点
*   **范式创新**：从被动接收信息转向主动调查，更符合人类专家进行推荐时的思维逻辑。
*   **工具设计专业**：针对推荐场景定制了协同过滤和知识图谱工具，而非通用的搜索引擎。
*   **训练方案完备**：结合了轨迹过滤的 SFT 和基于 GRPO 的 RL，解决了智能体推理中常见的格式崩溃和效率低下问题。

#### 8. 不足与局限
*   **推理延迟与成本**：由于涉及多步推理和多次工具调用，其推理延迟（Latency）和 API/算力成本显著高于传统模型，难以直接应用于超大规模实时推荐。
*   **工具依赖性**：推荐效果高度依赖于底层工具（如检索器、图谱）的质量，若底层数据源存在偏差，智能体推理可能被误导。
*   **任务局限**：目前实验主要集中在重排序（Reranking）阶段，尚未探讨在全库检索（Retrieval）阶段的应用表现。

（完）
