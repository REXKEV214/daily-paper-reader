---
title: "ActiveUltraFeedback: Efficient Preference Data Generation using Active Learning"
title_zh: ActiveUltraFeedback：利用主动学习的高效偏好数据生成
authors: "Davit Melikidze, Marian Schneider, Jessica Lam, Martin Wertich, Ido Hakimi, Barna Pásztor, Andreas Krause"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09692v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 用于大模型对齐的高效偏好数据生成
tldr: 针对RLHF中偏好数据获取成本高的问题，本文提出ActiveUltraFeedback，这是一个基于主动学习的模块化框架。该框架利用不确定性估计动态识别最具信息量的响应进行标注，并引入DRTS和DeltaUCB等新方法。实验证明，该方法仅需六分之一的标注数据即可达到或超过静态基准的性能，显著提升了偏好数据生成的效率。
selection_source: fresh_fetch
motivation: 强化学习对齐LLM的效果受限于获取高质量偏好数据的高昂成本，尤其是在低资源和专家领域。
method: 提出ActiveUltraFeedback主动学习流水线，通过不确定性估计和优先选择质量差异大的响应对来优化标注过程。
result: 实验表明，该方法仅使用六分之一的标注数据即可实现与全量静态基准相当或更优的下游任务性能。
conclusion: 主动学习能显著提高偏好数据生成的效率，为低成本、高质量的LLM对齐提供了有效的解决方案。
---

## 摘要
来自人类反馈的强化学习（RLHF）已成为对齐大语言模型（LLMs）的标准，但其有效性受限于获取偏好数据的高昂成本，特别是在低资源和专家领域。为了解决这一问题，我们引入了 ACTIVEULTRAFEEDBACK，这是一个模块化的主动学习流水线，它利用不确定性估计来动态识别最具信息量的待标注响应。我们的流水线促进了对标准响应选择方法以及双重反向汤普森采样（DRTS）和 DELTAUCB 的系统评估，后两者是优先考虑具有较大预测质量差距的响应对的新方法，利用了近期研究结果，即此类响应对能为微调提供良好的信号。我们的实验表明，ACTIVEULTRAFEEDBACK 产生了高质量的数据集，从而显著提升了下游性能，特别是相对于静态基准，仅需六分之一的标注数据即可达到相当或更优的结果。我们的流水线可在 https://github.com/lasgroup/ActiveUltraFeedback 获取，偏好数据集可在 https://huggingface.co/ActiveUltraFeedback 获取。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) has become the standard for aligning Large Language Models (LLMs), yet its efficacy is bottlenecked by the high cost of acquiring preference data, especially in low-resource and expert domains. To address this, we introduce ACTIVEULTRAFEEDBACK, a modular active learning pipeline that leverages uncertainty estimates to dynamically identify the most informative responses for annotation. Our pipeline facilitates the systematic evaluation of standard response selection methods alongside DOUBLE REVERSE THOMPSON SAMPLING (DRTS) and DELTAUCB, two novel methods prioritizing response pairs with large predicted quality gaps, leveraging recent results showing that such pairs provide good signals for fine-tuning. Our experiments demonstrate that ACTIVEULTRAFEEDBACK yields high-quality datasets that lead to significant improvements in downstream performance, notably achieving comparable or superior results with as little as one-sixth of the annotated data relative to static baselines. Our pipeline is available at https://github.com/lasgroup/ActiveUltraFeedback and our preference datasets at https://huggingface.co/ActiveUltraFeedback.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ActiveUltraFeedback** 的框架，旨在通过主动学习（Active Learning）显著降低大语言模型（LLM）对齐过程中偏好数据的标注成本。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：RLHF（基于人类反馈的强化学习）是模型对齐的关键，但获取高质量的偏好数据（Pairwise Preferences）成本极高，且在专家领域或低资源领域难以大规模获取。
*   **研究动机**：现有的偏好数据集（如 UltraFeedback）多采用静态启发式方法（如随机采样或 Best-of-N），这种方式不考虑样本的信息量，导致标注效率低下。论文旨在探索如何利用主动学习动态识别“最具信息量”的响应对，以极少的数据量达到甚至超过全量数据的对齐效果。

### 2. 方法论：ActiveUltraFeedback 流水线
该框架将偏好数据收集建模为**上下文决斗多臂老虎机（Contextual Dueling Bandit）**问题，包含五个模块化步骤：
*   **响应生成**：使用包含 30 个不同规模和家族的开源 LLM 组成模型池，针对每个提示词（Prompt）生成多样化的候选响应。
*   **奖励预测与不确定性估计**：采用**认识神经网路（ENN）**架构，通过在冻结的骨干模型上训练 MLP 集合（Ensemble），估计每个响应的奖励均值和认识不确定性（$\sigma$）。
*   **响应对选择（核心创新）**：
    *   **DRTS（双重反向汤普森采样）**：从奖励后验分布中采样，选择一个预测质量最高和一个预测质量最低的响应，旨在最大化质量差距（Delta）。
    *   **DeltaUCB**：利用置信区间上界（UCB），选择在乐观情况下偏好概率差距最大的响应对。
*   **偏好标注**：使用 LLM-as-a-Judge（如 Qwen 3 235B）根据真实性、指令遵循、诚实和助人四个维度进行打分，并采用概率化评分机制以消除离散评分的“饱和”问题。
*   **模型更新**：利用新收集的标注数据迭代训练 ENN 奖励模型。

### 3. 实验设计
*   **数据集**：使用了 UltraFeedback (60k)、Skywork (80k)、Tulu 3 (272k) 等多个规模的提示词集。
*   **Benchmark**：
    *   **奖励模型评估**：RewardBench 2。
    *   **下游任务评估**：GSM8K（数学）、IFEval（指令遵循）、TruthfulQA（真实性）、AlpacaEval 2（人类偏好）。
*   **对比方法**：
    *   **静态基准**：Random（随机）、UltraFeedback 原始方法、MaxMin（全局最优 vs 最差）、DeltaQwen（固定大小模型对比）。
    *   **标准老虎机算法**：InfoMax、DTS、MaxMinLCB。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA GH200 Grace Hopper Superchips**。
*   **算力消耗**：总计约 **200,000 GPU 小时**。
    *   响应生成：约 600 GPU 小时。
    *   标注过程：约 600 GPU 小时。
    *   主动学习循环：约 32 GPU 小时。
    *   其余算力用于大规模消融实验、不同算法的微调训练及稳定性测试。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。论文不仅在 4 个不同规模的数据集上验证了泛化性，还针对 DPO、IPO、SimPO 三种主流对齐算法进行了交叉验证。
*   **消融实验**：涵盖了评判模型（Judge）的选择、评分机制（离散 vs 概率）、训练稳定性（5 个随机种子）等。
*   **客观性**：通过报告相对于基座模型的 Delta 分数，并进行显著性分析（如定义了 0.008 的显著性阈值），确保了结果的公平性。

### 6. 主要结论与发现
*   **极高的样本效率**：使用 DRTS 或 DeltaUCB 方法，仅需 **1/6 的标注数据**（约 10k 样本）即可达到或超过在 60k 全量数据上训练的性能。
*   **质量差距的重要性**：实验证明，选择质量差距大的响应对（Delta Learning）比单纯减少不确定性或最小化悔值（Regret）对模型微调更有利。
*   **算法鲁棒性**：ActiveUltraFeedback 生成的数据集在不同的偏好优化算法（DPO/IPO/SimPO）下均表现优异，而传统的启发式方法（如 DeltaQwen）在算法切换时表现不稳定。

### 7. 论文亮点
*   **模块化设计**：框架允许自由更换模型池、奖励模型架构和评判模型，具有极强的扩展性。
*   **解决评分饱和**：通过提取 Logits 计算期望值而非直接取整数分，捕捉到了评判模型细微的偏好倾向。
*   **开源贡献**：开源了完整的流水线代码、生成的偏好数据集和训练好的模型，对社区贡献巨大。

### 8. 不足与局限
*   **推理成本高昂**：虽然减少了标注成本，但在主动学习循环中，为了选择最优的一对响应，需要先让 30 个模型生成响应并让 ENN 进行预测，这带来了巨大的推理计算开销。
*   **评判模型偏差**：尽管使用了强大的 Qwen 3 作为评判者，但仍可能存在 LLM 固有的偏好偏差（如长度偏见、自我复制偏见）。
*   **领域覆盖**：虽然在通用指令上表现良好，但在极度专业的垂直领域（如医疗、法律）是否能保持同样的效果仍需进一步验证。

（完）
