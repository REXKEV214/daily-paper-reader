---
title: "Evoking User Memory: Personalizing LLM via Recollection-Familiarity Adaptive Retrieval"
title_zh: 唤起用户记忆：通过回忆-熟悉度自适应检索实现大语言模型个性化
authors: "Yingyi Zhang, Junyi Li, Wenlin Zhang, Penyue Jia, Xianneng Li, Yichao Wang, Derong Xu, Yi Wen, Huifeng Guo, Yong Liu, Xiangyu Zhao"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09250v1"
tags: ["query:llm-memory"]
score: 10.0
evidence: 通过包含回忆和熟悉度的双重过程记忆检索实现大语言模型个性化
tldr: 针对个性化大模型在记忆检索中存在的全上下文开销大或单次检索精度不足的问题，本文受认知科学启发提出RF-Mem。该方法通过评估熟悉度信号自适应切换检索路径：高熟悉度执行快速Top-K检索，低熟悉度则通过聚类与alpha-mix模拟人类的“回想”过程进行迭代重构。实验证明该方法在固定预算下显著提升了检索质量与个性化表现。
selection_source: fresh_fetch
motivation: 现有的LLM记忆检索方法在处理复杂用户历史时，往往在计算成本与检索深度之间难以平衡，缺乏类似人类的深度回想机制。
method: 提出RF-Mem框架，利用熟悉度不确定性引导双路径检索，结合快速相似度匹配与基于嵌入空间迭代重构的深度回想路径。
result: 在三个基准测试中，RF-Mem在固定延迟和预算约束下，表现均优于传统的单次检索和全上下文推理方法。
conclusion: 将人类双重过程认知模型引入检索系统，为实现高效、可扩展且自适应的个性化大模型提供了新思路。
---

## 摘要
个性化大语言模型（LLMs）依赖记忆检索来整合用户特定的历史、偏好和上下文。现有方法要么通过将用户所有的过去记忆输入提示词来使 LLM 过载，这既昂贵且不可扩展；要么将检索简化为单次相似度搜索，这仅能捕捉表面匹配。然而，认知科学表明，人类记忆通过双重过程运作：熟悉度（Familiarity），提供快速但粗略的识别；以及回忆（Recollection），能够进行深思熟虑的链式重构，以深度恢复情境内容。当前系统既缺乏执行回忆检索的能力，也缺乏在双重检索路径之间自适应切换的机制，导致召回不足或引入噪声。为解决这一问题，我们提出了 RF-Mem（回忆-熟悉度记忆检索），这是一种由熟悉度不确定性引导的双路径记忆检索器。RF-Mem 通过平均分和熵来衡量熟悉度信号。高熟悉度会引导至直接的 Top-K 熟悉度检索路径，而低熟悉度则会激活回忆路径。在回忆路径中，系统对候选记忆进行聚类，并对查询应用 alpha-mix，在嵌入空间中迭代扩展证据，模拟深思熟虑的上下文重构。这种设计将类人的双重过程识别嵌入到检索器中，避免了全上下文开销，并实现了可扩展的自适应个性化。在三个基准测试和语料库规模上的实验表明，在固定的预算和延迟约束下，RF-Mem 的性能始终优于单次检索和全上下文推理。我们的代码可以在可复现性声明中找到。

## Abstract
Personalized large language models (LLMs) rely on memory retrieval to incorporate user-specific histories, preferences, and contexts. Existing approaches either overload the LLM by feeding all the user's past memory into the prompt, which is costly and unscalable, or simplify retrieval into a one-shot similarity search, which captures only surface matches. Cognitive science, however, shows that human memory operates through a dual process: Familiarity, offering fast but coarse recognition, and Recollection, enabling deliberate, chain-like reconstruction for deeply recovering episodic content. Current systems lack both the ability to perform recollection retrieval and mechanisms to adaptively switch between the dual retrieval paths, leading to either insufficient recall or the inclusion of noise. To address this, we propose RF-Mem (Recollection-Familiarity Memory Retrieval), a familiarity uncertainty-guided dual-path memory retriever. RF-Mem measures the familiarity signal through the mean score and entropy. High familiarity leads to the direct top-K Familiarity retrieval path, while low familiarity activates the Recollection path. In the Recollection path, the system clusters candidate memories and applies alpha-mix with the query to iteratively expand evidence in embedding space, simulating deliberate contextual reconstruction. This design embeds human-like dual-process recognition into the retriever, avoiding full-context overhead and enabling scalable, adaptive personalization. Experiments across three benchmarks and corpus scales demonstrate that RF-Mem consistently outperforms both one-shot retrieval and full-context reasoning under fixed budget and latency constraints. Our code can be found in the Reproducibility Statement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evoking User Memory: Personalizing LLM via Recollection-Familiarity Adaptive Retrieval》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
个性化大语言模型（LLM）依赖记忆检索来整合用户的历史、偏好和上下文。然而，现有方法存在两极分化：
*   **全上下文输入（Full Context）：** 将所有历史喂给模型，成本极高且难以扩展到长文本。
*   **单次检索（One-shot Retrieval）：** 仅依赖简单的向量相似度搜索，往往只能捕捉表面匹配，无法处理复杂、模糊或跨时段的个性化需求。
*   **研究动机：** 受认知科学中人类记忆的“双重过程理论”启发——即**熟悉度（Familiarity，快速直觉识别）**和**回忆（Recollection，慢速逻辑重构）**。论文旨在构建一个能够像人一样在快速识别和深度回想之间自适应切换的检索框架。

### 2. 提出方法论：RF-Mem
RF-Mem（Recollection-Familiarity Memory Retrieval）是一个由不确定性引导的双路径检索框架：
*   **熟悉度不确定性引导的选择机制：** 
    *   首先进行初步探测检索，计算候选列表的**平均相似度得分（$\bar{s}$）**和**信息熵（$H$）**。
    *   **决策逻辑：** 如果得分高或熵值低（确定性强），走“熟悉度路径”；如果得分低或熵值高（不确定性强），激活“回忆路径”。
*   **熟悉度路径（Familiarity Path）：** 执行标准的单次 Top-K 向量检索，确保高效率。
*   **回忆路径（Recollection Path）：** 模拟人类的联想过程。
    *   **聚类（Clustering）：** 对初步检索到的记忆片段进行 KMeans 聚类，提取语义质心。
    *   **$\alpha$-混合（$\alpha$-mix）：** 将原始查询向量与聚类质心按比例混合，生成新的“回想查询”。
    *   **迭代重构：** 在嵌入空间中多次执行“检索-聚类-混合”循环，逐步扩展证据链，挖掘深层关联。

### 3. 实验设计
*   **数据集：**
    *   **PersonaMem：** 包含 32K、128K、1M 不同规模的语料，涵盖 7 类真实任务（如偏好追踪、事实共享等）。
    *   **PersonaBench：** 侧重于私有用户文档和个人信息查询。
    *   **LongMemEval：** 针对长短期交互记忆的基准测试。
*   **对比方法：** 无记忆（Zero Memory）、全上下文（Full Context）、密集检索（Dense Retrieval/Familiarity）、纯回忆模式（Recollection-only）。
*   **评估指标：** 生成任务使用准确率（Accuracy），检索任务使用 Recall@K。

### 4. 资源与算力
*   **硬件：** 实验在单台 **NVIDIA A100 GPU** 上运行（Ubuntu 系统）。
*   **算力细节：** 论文详细记录了检索延迟（Latency）。例如，在 32K 语料下，RF-Mem 的检索时间约为 5.09ms，远低于全上下文处理时间，且仅比标准密集检索略高。由于该方法主要涉及向量运算和轻量级聚类，不需要大规模的重新训练。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 32K 到 1M 标记的不同记忆规模，验证了扩展性。
*   **消融与参数分析：** 对混合比例 $\alpha$、熵阈值 $\tau$、束宽 $B$ 和扇出 $F$ 进行了详尽的敏感性分析。
*   **自适应研究：** 验证了 RF-Mem 与其他索引方法（如 MemoryBank）、查询扩展（如 HyDE）以及迭代 RAG（如 Search-o1）的兼容性。
*   **客观性：** 实验在多个基准测试和多种嵌入模型（MiniLM, MPNet, BGE）上进行，结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能领先：** RF-Mem 在所有规模的语料库上均优于单次检索，且在长文本情况下避免了全上下文输入的性能崩溃。
*   **效率平衡：** 通过自适应切换，它在保持接近单次检索延迟的同时，达到了接近甚至超过复杂重构方法的精度。
*   **任务适应性：** “熟悉度路径”擅长处理事实性查询，而“回忆路径”在处理需要跨会话推理的复杂个性化任务（如社会关系、深度偏好）时表现更佳。

### 7. 优点
*   **理论支撑：** 巧妙地将认知科学理论引入检索算法，具有良好的可解释性。
*   **自适应性：** 能够根据查询的难度动态分配计算资源，避免了“一刀切”的检索策略。
*   **轻量级：** 所有的“回想”过程都在嵌入空间完成，无需调用 LLM 进行多次查询重写，极大地节省了 API 成本和时间。

### 8. 不足与局限
*   **模态限制：** 目前仅限于文本记忆，未涉及多模态（如图像、视频）历史。
*   **静态索引：** 实验基于静态嵌入索引，未深入探讨记忆随时间演变（如记忆遗忘、冲突更新）的动态管理。
*   **不确定性度量：** 仅依赖得分和熵作为不确定性指标，可能无法完全捕捉复杂的语义歧义。
*   **隐私风险：** 增强的记忆检索能力可能在无意中提升了敏感信息的暴露风险，论文虽有提及但未深入探讨防御机制。

（完）
