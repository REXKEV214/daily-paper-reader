---
title: "Dynamic Multimodal Expression Generation for LLM-Driven Pedagogical Agents: From User Experience Perspective"
title_zh: 大语言模型驱动的教学代理动态多模态表达生成：基于用户体验视角
authors: "Ninghao Wan, Jiarun Song, Fuzheng Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09536v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 从用户体验角度研究大模型驱动的教学智能体
tldr: 本研究针对虚拟现实教学代理（PA）行为僵硬、缺乏语义适应性的问题，提出了一种由大语言模型（LLM）驱动的多模态表达生成方法。该方法通过构建语义敏感提示词，生成协调的语音与手势指令，实现了教学语义与表现行为的动态对齐。实验表明，该方法显著提升了学习者的参与度、感知效果及社会存在感，为构建更自然、沉浸的智能教学代理提供了设计准则。
selection_source: fresh_fetch
motivation: 现有的虚拟教学代理多依赖静态语音和简单手势，难以根据教学内容的语义语境进行动态调整，导致交互缺乏自然性。
method: 提出一种基于大语言模型的多模态表达生成方法，利用语义敏感提示词实现语音与手势的动态协调对齐。
result: 实验证明动态生成的多模态表达显著增强了学习者的参与度、学习效果感知及社会存在感，并有效缓解了学习疲劳。
conclusion: 动态语音与手势的结合是提升智能教学代理拟人化和沉浸感的关键，为未来教育代理的设计提供了新思路。
---

## 摘要
在虚拟现实（VR）教育场景中，教学代理（PAs）通过逼真的外观和交互行为增强沉浸式学习体验。然而，大多数现有的教学代理依赖于静态语音和简单的手势。这种局限性降低了它们动态适应教学内容语义语境的能力，导致教学过程中的交互往往缺乏自然性和有效性。为了应对这一挑战，本研究提出了一种大语言模型（LLM）驱动的多模态表达生成方法，该方法通过构建语义敏感的提示词来生成协调的语音和手势指令，从而实现教学语义与多模态表达行为之间的动态对齐。研究开发了一个基于VR的教学代理原型，并通过面向用户体验的主观实验进行了评估。结果表明，动态生成的多模态表达显著增强了学习者的感知学习效果、参与度和使用意愿，同时有效缓解了学习过程中的疲劳感和枯燥感。此外，语音和手势的组合动态表达显著提升了学习者对拟人化和社交临场感的感知。研究结果为构建更具沉浸感和自然表达能力的智能教学代理提供了新的见解和设计指南。

## Abstract
In virtual reality (VR) educational scenarios, Pedagogical agents (PAs) enhance immersive learning through realistic appearances and interactive behaviors. However, most existing PAs rely on static speech and simple gestures. This limitation reduces their ability to dynamically adapt to the semantic context of instructional content. As a result, interactions often lack naturalness and effectiveness in the teaching process. To address this challenge, this study proposes a large language model (LLM)-driven multimodal expression generation method that constructs semantically sensitive prompts to generate coordinated speech and gesture instructions, enabling dynamic alignment between instructional semantics and multimodal expressive behaviors. A VR-based PA prototype was developed and evaluated through user experience-oriented subjective experiments. Results indicate that dynamically generated multimodal expressions significantly enhance learners' perceived learning effectiveness, engagement, and intention to use, while effectively alleviating feelings of fatigue and boredom during the learning process. Furthermore, the combined dynamic expression of speech and gestures notably enhances learners' perceptions of human-likeness and social presence. The findings provide new insights and design guidelines for building more immersive and naturally expressive intelligent PAs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dynamic Multimodal Expression Generation for LLM-Driven Pedagogical Agents: From User Experience Perspective》的结构化深入分析总结：

### 1. 核心问题与研究背景
*   **核心问题**：在虚拟现实（VR）教育场景中，现有的教学代理（Pedagogical Agents, PAs）通常表现得过于机械。其语音输出往往语调单一，手势动作与教学内容的语义脱节，缺乏人类教师那种根据内容难易程度自动调整语速、停顿或强调手势的能力。
*   **研究动机**：为了提升VR学习的沉浸感和有效性，需要赋予教学代理“语义感知”的动态表达能力，使非语言行为（手势）与语言行为（语音节奏）能够根据教学语境自动对齐。

### 2. 核心方法论
论文提出了一种由大语言模型（LLM）驱动的动态多模态表达生成框架，核心思想是将“说什么”与“怎么说”在语义层面统一起来。
*   **语义敏感提示词构建（Prompt Construction, PC）**：这是核心技术细节。提示词包含三部分：
    *   **背景信息**：定义角色属性和场景。
    *   **表达信息（知识+标注）**：向LLM提供一套“表达规则库”。例如，定义语音标签（如`<break>`停顿、`<prosody>`语调）和手势标签（如“思考”、“强调”、“总结”）。通过示例（Annotation）引导模型在生成文本的同时，在合适的位置插入这些标签。
    *   **用户问题**：实时的语音转文字输入。
*   **文本解析模块（Text Parsing, TP）**：LLM生成的带有标签的文本被解析。语音标签转换为SSML格式驱动Azure TTS生成抑扬顿挫的语音；手势标签则映射到Unity动画控制器，触发相应的动作。
*   **端到端流程**：STT（语音转文字）→ 提示词构建 → LLM（GPT-4o）生成带标签文本 → 文本解析 → TTS（语音合成）与手势动画同步执行。

### 3. 实验设计
*   **实验场景**：开发了一个VR教室原型，参与者扮演学生，就“多媒体通信”课程内容与虚拟教师进行问答交互。
*   **实验设计**：采用 **2×2 被试内实验设计**（Within-subjects design），包含四个对比条件：
    1.  **条件 A (SS+SG)**：静态语音 + 静态手势（基准对照组）。
    2.  **条件 B (DS+SG)**：动态语音 + 静态手势。
    3.  **条件 C (SS+DG)**：静态语音 + 动态手势。
    4.  **条件 D (DS+DG)**：动态语音 + 动态手势（完整方案）。
*   **评估指标**：感知有用性（PU）、学习参与度（EN）、使用意愿（IU）、拟人化（HL）、社会临场感（SP）和不适感（DC）。

### 4. 资源与算力
*   **模型与API**：使用了 OpenAI 的 **GPT-4o** 作为核心推理引擎，**Whisper API** 用于语音识别，**Microsoft Azure API** 用于语音合成。
*   **硬件设备**：VR头显使用 **Oculus Quest 2**，开发平台为 Unity 2022。
*   **算力说明**：文中**未明确说明**具体的本地 GPU 训练时长或型号，因为该系统主要依赖于云端 API 调用实现推理，而非从头训练大模型。

### 5. 实验数量与充分性
*   **样本量**：招募了 **36名** 大学生参与者（22男14女），年龄在20-26岁之间。
*   **实验充分性**：
    *   采用了**平衡拉丁方设计**（Balanced Latin Square Design）来消除实验顺序效应，确保公平性。
    *   使用了 **ART ANOVA**（对齐秩变换方差分析）进行非参数统计检验，并配合 Holm-Bonferroni 校正进行事后比较，统计方法严谨。
    *   结合了定量问卷和定性的半结构化访谈，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **多模态协同效应**：动态语音和手势的结合（条件D）在所有正面指标（有用性、参与度、意愿）上均显著优于其他条件。
*   **缓解负面情绪**：动态表达能显著降低学习者的疲劳感和枯燥感（DC指标显著下降）。
*   **拟人化瓶颈**：虽然动态表达提升了拟人化和社会临场感，但这两个维度的绝对得分仍然相对较低，说明仅靠语音和手势的初步动态化还不足以完全消除“恐怖谷”效应。
*   **独立贡献**：语音动态化对提升理解力的贡献略大于手势动态化，但两者在减少不适感方面贡献相当。

### 7. 优点与亮点
*   **语义-行为对齐**：通过提示词工程巧妙地让LLM同时处理内容生成和行为标注，解决了传统方法中行为与语义脱节的问题。
*   **低成本可扩展性**：基于现有的成熟API（GPT-4o, Azure）构建，无需海量动作捕捉数据即可生成相对自然的交互行为。
*   **用户体验视角**：研究不仅关注技术实现，还深入探讨了多模态表达如何通过影响认知负荷来提升学习效果。

### 8. 不足与局限
*   **手势库有限**：访谈显示参与者认为手势重复率较高，且动作过渡（Transition）不够平滑，存在僵硬感。
*   **交互模式单一**：目前仍是“你问我答”的单向回合制，缺乏人类教学中常见的“插话/中断”机制。
*   **语义一致性风险**：虽然LLM能生成标签，但有时语音节奏与手势的精确时间对齐（同步性）仍有优化空间。
*   **认知负荷挑战**：长篇大论的解释即使有动态表达，仍可能导致学习者注意力下降，需要更智能的内容长度自适应机制。

（完）
