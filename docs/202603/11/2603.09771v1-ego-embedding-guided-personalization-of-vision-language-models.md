---
title: "Ego: Embedding-Guided Personalization of Vision-Language Models"
title_zh: Ego：嵌入引导的视觉语言模型个性化
authors: "Soroush Seifi, Simon Gardier, Vaggelis Dorovatas, Daniel Olmeda Reino, Rahaf Aljundi"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09771v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 视觉语言模型的嵌入引导个性化
tldr: 本研究针对多模态语言模型在个性化体验方面的局限性，提出了Ego方法。传统方法依赖额外训练或复杂外部模块，导致效率和通用性较低。Ego利用模型内部注意力机制提取代表特定概念的视觉Token作为“记忆”，使模型能在后续测试中准确召回并描述这些概念。该方法在单概念、多概念及视频个性化任务中均表现优异，且具有极低的个性化开销。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型个性化方法通常面临训练成本高、部署效率低或难以扩展到多概念场景的挑战。
method: 利用模型内部的注意力机制提取并存储代表特定概念的视觉Token，将其作为引导模型识别该概念的记忆向量。
result: 在单概念、多概念和视频等多种个性化评估设置中，该方法均实现了显著的性能提升，且计算开销极小。
conclusion: Ego证明了无需额外训练即可通过模型固有能力实现高效的个性化，为构建通用的个性化AI助手提供了新思路。
---

## 摘要
随着多模态语言模型的飞速发展，在日常生活中辅助人类的 AI 助手正变得日益可行。一个关键挑战在于如何克服这些模型的通用性，以提供个性化的体验。现有的视觉语言大模型个性化方法通常依赖于额外的训练阶段，这限制了通用性和可扩展性；或者依赖于带有外部预训练模块的工程化流水线，这阻碍了部署效率。在这项工作中，我们提出了一种高效的个性化方法，利用模型捕获个性化概念的固有能力。具体而言，我们利用模型的内部注意力机制，提取主要代表目标概念的视觉 token。这些 token 作为该特定概念的记忆，使模型能够在测试图像中出现该概念时对其进行召回和描述。我们在包括单概念、多概念和视频个性化在内的各种个性化设置下，对我们的方法和 SOTA 方法进行了全面且统一的评估，结果表明我们的方法在极小的个性化开销下实现了显著的性能提升。

## Abstract
AI assistants that support humans in daily life are becoming increasingly feasible, driven by the rapid advancements in multimodal language models. A key challenge lies in overcoming the generic nature of these models to deliver personalized experiences. Existing approaches to personalizing large vision language models often rely on additional training stages, which limit generality and scalability, or on engineered pipelines with external pre-trained modules, which hinder deployment efficiency. In this work, we propose an efficient personalization method that leverages the model's inherent ability to capture personalized concepts. Specifically, we extract visual tokens that predominantly represent the target concept by utilizing the model's internal attention mechanisms. These tokens serve as a memory of that specific concept, enabling the model to recall and describe it when it appears in test images. We conduct a comprehensive and unified evaluation of our approach and SOTA methods across various personalization settings including single-concept, multi-concept, and video personalization, demonstrating strong performance gains with minimal personalization overhead.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Ego**（Embedding-Guided Personalization），一种旨在实现视觉语言模型（LVLM）高效个性化的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何让通用的视觉语言大模型（LVLM）识别并理解用户特定的私有概念（如特定的宠物、个人物品或人物），同时避免高昂的训练成本和复杂的系统架构。
*   **研究动机**：
    *   **现有方法局限**：测试时微调（Test-time fine-tuning）扩展性差；基于训练的方法（Training-based）依赖大规模合成数据且难以处理多概念场景；免训练方法（Training-free）通常依赖外部视觉模块（如检测器、分割模型），增加了推理延迟和系统复杂度。
    *   **核心假设**：现代 LVLM 内部已经具备了跨图像关联对象的能力，可以通过提取模型内部的判别性嵌入（Embeddings）来构建个性化“记忆”。

### 2. 方法论（Ego）
Ego 的核心思想是利用模型自身的注意力机制来提取最具代表性的视觉 Token，将其作为个性化概念的“软提示（Soft Prompts）”。
*   **概念引入（Concept Introduction）**：
    1.  输入参考图像，让模型生成描述该主体的关键词（Keywords）。
    2.  **注意力引导提取**：分析模型内部 LLM 层的交叉模态注意力图，计算关键词 Token 对视觉 Token 的注意力得分。
    3.  **重要性评分 ($I_j$)**：通过跨层和跨头的注意力分数加权平均，识别出与主体最相关的视觉 Token。
*   **关键技术细节**：
    1.  **动态记忆大小 ($K_c$)**：模型先估计主体在图像中的面积占比，据此动态决定提取多少个视觉 Token（避免背景噪声）。
    2.  **自动层选择**：提出一种校准策略，自动识别模型中视觉与文本交互最强的中间层（通常是中后部层）。
*   **推理阶段**：将提取的视觉 Token 存储在 LLM 的嵌入空间中。推理时，直接将这些“视觉记忆”作为上下文注入，无需重新运行视觉编码器。

### 3. 实验设计
*   **数据集与场景**：
    *   **单概念**：MyVLM, Yo’LLaVA, This-is-my-img。
    *   **多概念**：This-is-my-img (Multi), RAP (Multi)。
    *   **视频**：This-is-my 视频问答数据集。
*   **评估任务**：识别（Recognition）、视觉问答（VQA）、图像描述（Captioning）。
*   **对比方法**：
    *   **基于训练**：RAP（SOTA 检索增强个性化方法）。
    *   **免训练**：R2P（基于检索）、PeKit（基于外部存储和分割模型）。
*   **Benchmark**：统一了评估协议，使用 Precision、Recall、F1-score 以及 GPT-3.5 辅助的语义评分。

### 4. 资源与算力
*   **硬件使用**：
    *   **推理/评估**：使用 2 张 NVIDIA A100 GPU。
    *   **基准训练**：为了复现 RAP 方法，使用了 8 张 A100 GPU 进行 LoRA 微调。
*   **耗时**：Ego 引入一个新概念仅需约 1.25 - 1.40 秒（远快于 PeKit 的 21 秒或 RAP 的 24 小时训练）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单图像到多图像参考（1-view vs 5-views）、从单概念到多实体共存、从静态图像到动态视频的全方位测试。
*   **消融实验**：非常充分。作者对比了关键词提取 vs 全文描述、动态 $K_c$ vs 固定 $K$、不同层选择策略、以及与使用完整参考图像的对比。
*   **客观性**：作者指出以往研究在数据集分割和指标上存在不一致，因此在相同硬件和模型底座（InternVL3-14B, Qwen2.5-VL-7B）下重新运行了所有基准方法，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：Ego 在多概念识别和视频 VQA 任务中显著优于现有方法。例如，在多概念 Captioning 任务中，比 RAP 提升了近 30%。
*   **效率极高**：由于只存储精简后的视觉 Token，推理时的上下文负担极小，且无需外部模块。
*   **模型通用性**：在不同规模的模型（7B 和 14B）上均表现稳健，证明了该方法的普适性。

### 7. 优点（亮点）
*   **完全免训练**：无需微调模型参数，也无需外部预训练模块（如 DINOv2 或 SAM），实现了真正的“即插即用”。
*   **模型不可知（Model-agnostic）**：适用于任何具有交叉注意力机制的现代 LVLM。
*   **多概念支持**：通过精简 Token 数量，有效解决了多概念共存时上下文窗口溢出的问题。
*   **动态适应**：能够根据物体大小自动调整记忆容量，有效过滤背景干扰。

### 8. 不足与局限
*   **依赖底座模型能力**：Ego 依赖于模型本身的视觉理解和上下文学习能力。在较弱或较旧的模型（如参数量小于 4B 的模型）上效果受限。
*   **提示词敏感性**：作为一种基于 Prompt 的方法，其性能可能受到关键词生成质量的影响。
*   **极端场景挑战**：虽然在多概念上表现良好，但如果场景中存在大量极小物体，提取的 Token 密度可能不足以区分细微特征。

（完）
