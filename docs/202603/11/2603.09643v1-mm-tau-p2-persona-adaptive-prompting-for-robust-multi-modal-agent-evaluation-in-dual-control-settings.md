---
title: "MM-tau-p$^2$: Persona-Adaptive Prompting for Robust Multi-Modal Agent Evaluation in Dual-Control Settings"
title_zh: MM-tau-p$^2$：双重控制设置下鲁棒多模态智能体评估的人格自适应提示
authors: "Anupam Purwar, Aditya Choudhary"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09643v1"
tags: ["query:llm-personalization"]
score: 10.0
evidence: 用于多模态智能体评估的人格自适应提示
tldr: 本研究针对现有LLM智能体评估框架忽略用户人格且仅限于文本交互的问题，提出了MM-tau-p$^2$基准。该基准专门用于在双控制设置下评估多模态智能体的人格适配能力及鲁棒性，并引入了包括多模态鲁棒性和轮次开销在内的12项新指标。通过在电信和零售领域的实验，利用LLM-as-judge方法揭示了前沿模型在处理多模态输入和个性化交互时的性能瓶颈，为构建更智能的客户体验管理系统提供了自动化评估方案。
selection_source: fresh_fetch
motivation: 现有LLM智能体评估框架多为文本驱动且忽略用户人格特征，无法满足多模态及个性化客户体验管理的需求。
method: 提出MM-tau-p$^2$基准，通过双控制设置和12项新指标，利用LLM-as-judge方法评估多模态智能体在人格适配下的鲁棒性。
result: 实验表明即使是GPT-4等前沿模型，在引入多模态后仍面临多模态鲁棒性和对话轮次开销等方面的显著挑战。
conclusion: MM-tau-p$^2$为自动化评估多模态智能体提供了一套全面的衡量标准，特别是在需要考虑用户人格和实时交互的复杂场景下。
---

## 摘要
当前针对大语言模型（LLM）驱动的智能体的评估框架和基准主要集中在文本对话驱动的智能体上，这些框架并未向智能体展示用户的人格（persona），因此是在一种与用户无关的环境中运行。重要的是，在客户体验管理领域，智能体的行为会随着其对用户性格的了解而演变。随着实时语音合成（TTS）和多模态语言模型的普及，基于 LLM 的智能体正逐渐向多模态化发展。为此，我们提出了 MM-tau-p$^2$ 基准，其中包含用于评估多模态智能体在双重控制设置下（无论是否进行用户人格自适应）的鲁棒性的指标，同时在规划过程中考虑用户输入以解决用户查询。特别是，我们的研究表明，即使是像 GPT-5、GPT 4.1 这样的最先进前沿 LLM，在将多模态引入基于 LLM 的智能体时，仍需通过多模态鲁棒性、轮次开销（turn overhead）等指标来衡量额外的考量因素。总的来说，MM-tau-p$^2$ 基于我们之前的工作 FOCAL，通过引入 12 个新颖指标，提供了一种自动评估多模态智能体的全面方法。我们还通过 LLM-as-judge 方法，利用精心设计的提示和定义明确的评分标准（rubrics）来评估每场对话，从而提供了这些指标在电信和零售领域的估算值。

## Abstract
Current evaluation frameworks and benchmarks for LLM powered agents focus on text chat driven agents, these frameworks do not expose the persona of user to the agent, thus operating in a user agnostic environment. Importantly, in customer experience management domain, the agent's behaviour evolves as the agent learns about user personality. With proliferation of real time TTS and multi-modal language models, LLM based agents are gradually going to become multi-modal. Towards this, we propose the MM-tau-p$^2$ benchmark with metrics for evaluating the robustness of multi-modal agents in dual control setting with and without persona adaption of user, while also taking user inputs in the planning process to resolve a user query. In particular, our work shows that even with state of-the-art frontier LLMs like GPT-5, GPT 4.1, there are additional considerations measured using metrics viz. multi-modal robustness, turn overhead while introducing multi-modality into LLM based agents. Overall, MM-tau-p$^2$ builds on our prior work FOCAL and provides a holistic way of evaluating multi-modal agents in an automated way by introducing 12 novel metrics. We also provide estimates of these metrics on the telecom and retail domains by using the LLM-as-judge approach using carefully crafted prompts with well defined rubrics for evaluating each conversation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MM-tau-p$^2$**，一个专门用于在双重控制（Dual-Control）设置下评估多模态（语音与文本）大语言模型（LLM）智能体人格自适应能力的自动化基准测试框架。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的智能体评估基准（如 tau-bench）大多局限于纯文本交互，且将用户视为静态的信息提供者（User-Agnostic）。但在真实的客户体验管理（CX）场景中，用户的人格特征（如专业程度、情绪、表达能力）会显著影响交互流程。
*   **核心问题**：如何系统性地评估多模态智能体在面对不同人格特征的用户时，其在任务达成、交互效率、错误恢复及安全性方面的表现？
*   **整体含义**：论文提出了 MM-tau-p$^2$ 基准，通过引入人格自适应提示（Persona-Adaptive Prompting）和 12 项新颖指标，填补了多模态、双向控制环境下智能体评估的空白。

### 2. 方法论
*   **核心思想**：通过“p$^2$”两个杠杆提升评估稳定性：第一个 **p** 代表优化的智能体提示词（含护栏）；第二个 **p** 代表详细的用户人格注入（控制专业度、模糊性等）。
*   **关键技术细节**：
    *   **双重控制协议**：模拟用户与智能体共同影响对话走向的真实场景，用户会提供不完整信息或中途改变主意。
    *   **多模态流水线**：构建了“用户语音 $\rightarrow$ ASR（语音识别） $\rightarrow$ LLM智能体 $\rightarrow$ TTS（语音合成）”的完整闭环。
    *   **人格建模与注入**：
        *   **静态注入**：直接在系统提示词中告知智能体用户的人格。
        *   **动态上下文注入**：每 3 轮对话利用 LLM 提取用户当前的年龄、技术水平、挫败感等特征，动态更新智能体的系统提示词。
    *   **LLM-as-Judge**：利用 GPT-4.1 和 GPT-5 作为裁判，基于预设的评分标准（Rubrics）对对话进行自动化打分。
*   **综合评分公式 (mm-tap)**：
    $$S = \alpha(CFA \cdot pass@1) + \beta \frac{MRS + ARGA + ERR}{3} + \gamma \frac{TE + \frac{1}{1+UES} + \frac{1}{1+TO}}{3} + \delta IAS$$
    （涵盖了关键字段准确率、鲁棒性、效率和安全性等多个维度）。

### 3. 实验设计
*   **场景与数据集**：选择了**电信（Telecom）**和**零售（Retail）**两个领域。电信任务涉及复杂的故障排除（如 SIM 卡锁定），零售任务侧重于订单处理。
*   **Benchmark 设置**：基于 tau-bench 扩展，增加了语音模态和人格变量。
*   **对比方法**：
    *   **模态对比**：纯文本 vs. 语音。
    *   **人格配置对比**：无（None）、简单人格注入（Persona Injection）、动态上下文注入（Context Injection）。
    *   **模型对比**：使用 GPT-4.1 和 GPT-5 分别作为智能体骨干和裁判模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **外部服务**：实验高度依赖闭源 API，包括 OpenAI/Microsoft 的 GPT-4.1 和 GPT-5 接口，以及 ElevenLabs 的 TTS 服务。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个领域、三种人格模式、两种模态以及两个前沿模型的交叉实验。
*   **充分性评价**：
    *   **多维度分析**：不仅关注成功率，还深入分析了 ASR 错误下的恢复率（ARGA）和轮次开销（TO）。
    *   **消融/对比实验**：通过对比不同 p 值（人格注入深度）的影响，验证了动态上下文注入的有效性。
    *   **客观性**：通过对比 GPT-4.1 和 GPT-5 两个不同版本的法官表现，揭示了 LLM 裁判在处理“人工升级”场景时的一致性问题，体现了研究的批判性。

### 6. 论文的主要结论与发现
*   **多模态性能下降**：从文本转向语音时，由于 ASR 噪声和确认环节增加，智能体的轮次开销显著增加，鲁棒性普遍下降。
*   **人格适配的权衡**：动态上下文注入能显著提升电信领域的关键字段准确率（CFA），但往往会**降低安全性指标**（智能体在追求效率时可能忽略必要的安全确认）。
*   **法官模型差异**：GPT-5 作为裁判比 GPT-4.1 更“乐观”，尤其在判定“转接人工”是否属于任务成功时，两者存在显著分歧。
*   **领域敏感性**：电信任务比零售任务更脆弱，对人格适配的需求更高，且更容易受到语音模态负面影响。

### 7. 优点
*   **指标体系全面**：12 项指标覆盖了从任务达成到用户体验、安全性的全方位评估。
*   **动态性强**：引入了动态人格推理，比传统的静态 Persona 注入更贴近真实的人机交互。
*   **现实指导意义**：针对 CX 领域的特定痛点（如 SIM 锁定、身份验证）设计了专门的评估逻辑。

### 8. 不足与局限
*   **语音交互特征缺失**：目前的基准尚未模拟语音交互中至关重要的**中断（Interruption）**、**重叠说话（Overtalk）**和**响应延迟**。
*   **法官一致性风险**：实验显示 LLM 裁判在处理“转接人工”这一模糊边界时存在逻辑矛盾，可能导致评估结果存在偏差。
*   **成本与闭源依赖**：评估过程依赖昂贵的商业 API，对于开源社区的复现和低成本测试不够友好。

（完）
