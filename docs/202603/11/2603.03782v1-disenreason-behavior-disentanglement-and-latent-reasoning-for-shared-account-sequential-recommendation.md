---
title: "DisenReason: Behavior Disentanglement and Latent Reasoning for Shared-Account Sequential Recommendation"
title_zh: DisenReason：共享账号序列推荐中的行为解耦与潜在推理
authors: "Jiawei Cheng, Min Gao, Zongwei Wang, Xiaofei Zhu, Zhiyi Liu, Wentao Li, Wei Li, Huan Wu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03782v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 在共享账号序列推荐中推断用户偏好
tldr: 针对共享账号序列推荐（SSR）中固定潜在用户数假设的局限性，本文提出 DisenReason。该方法通过频域行为解耦构建统一的账号表征，并利用潜在推理技术动态推断账号背后的用户数量。实验证明，该方法在四个基准数据集上均显著优于现有模型，有效提升了推荐准确性。
selection_source: fresh_fetch
motivation: 现有共享账号推荐方法通常假设固定的潜在用户数，且难以通过单一最近行为推断整个账号的复杂用户构成。
method: 提出两阶段推理方法，结合频域行为解耦生成统一账号表征，并以此为基础进行潜在用户推理以识别用户数量。
result: "在四个基准数据集上，DisenReason 在 MRR@5 和 Recall@20 指标上分别实现了最高 12.56% 和 6.06% 的相对提升。"
conclusion: 通过解耦账号行为并动态推理潜在用户，DisenReason 能够更灵活地适应多样的共享模式，显著增强了共享账号场景下的推荐性能。
---

## 摘要
共享账号的使用在流媒体和电子商务平台中非常普遍，即多个用户共享同一个账号。现有的共享账号序列推荐（SSR）方法通常假设每个账号拥有固定数量的潜在用户，这限制了它们适应多样化共享模式的能力，并降低了推荐准确性。最近应用于序列推荐（SR）的潜在推理技术通过用户嵌入（例如，最后一个物品的嵌入）生成中间嵌入，以揭示用户的潜在兴趣。这启发我们将推断潜在用户数量的问题视为生成一系列中间嵌入的过程，从而将研究重点从推断用户背后的偏好转向推断账号背后的用户。然而，在 SSR 中，最后一个物品不能直接用于推理，因为它只能代表最近一个潜在用户的行为，而不能代表整个账号的集体行为。为此，我们提出了 DisenReason，一种专为 SSR 设计的两阶段推理方法。DisenReason 结合了从频域视角出发的行为解耦阶段，以创建集体且统一的账号行为表示，并将其作为潜在用户推理阶段的枢纽，从而推断账号背后的用户数量。在四个基准数据集上的实验表明，DisenReason 在所有数据集上均一致优于现有的最先进基准模型，在 MRR@5 和 Recall@20 指标上分别实现了高达 12.56% 和 6.06% 的相对提升。

## Abstract
Shared-account usage is common on streaming and e-commerce platforms, where multiple users share one account. Existing shared-account sequential recommendation (SSR) methods often assume a fixed number of latent users per account, limiting their ability to adapt to diverse sharing patterns and reducing recommendation accuracy. Recent latent reasoning technique applied in sequential recommendation (SR) generate intermediate embeddings from the user embedding (e.g, last item embedding) to uncover users' potential interests, which inspires us to treat the problem of inferring the number of latent users as generating a series of intermediate embeddings, shifting from inferring preferences behind user to inferring the users behind account. However, the last item cannot be directly used for reasoning in SSR, as it can only represent the behavior of the most recent latent user, rather than the collective behavior of the entire account. To address this, we propose DisenReason, a two-stage reasoning method tailored to SSR. DisenReason combines behavior disentanglement stage from frequency-domain perspective to create a collective and unified account behavior representation, which serves as a pivot for latent user reasoning stage to infer the number of users behind the account. Experiments on four benchmark datasets show that DisenReason consistently outperforms all state-of-the-art baselines across four benchmark datasets, achieving relative improvements of up to 12.56\% in MRR@5 and 6.06\% in Recall@20.

---

## 论文详细总结（自动生成）

这是一份关于论文《DisenReason: Behavior Disentanglement and Latent Reasoning for Shared-Account Sequential Recommendation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在流媒体和电商平台中，多个用户共享一个账号（如家庭成员共用视频会员）是普遍现象。现有的共享账号序列推荐（SSR）方法通常假设每个账号背后的潜在用户数量是**固定**的，这无法适应现实中多变的共享模式。
*   **研究动机**：
    *   传统的序列推荐（SR）假设一号一人，不适用于混合行为序列。
    *   现有的潜在推理技术（如“先思考再推荐”）通常以序列最后一个物品为推理枢纽，但在共享账号场景下，最后一个物品仅代表最近一个用户的行为，无法代表整个账号的集体特征。
    *   **目标**：如何准确推断并表征共享账号背后动态变化的潜在用户数量，从而实现高质量推荐。

### 2. 方法论：核心思想与技术细节
论文提出了 **DisenReason**，一个两阶段推理框架：
*   **第一阶段：行为解耦构建推理枢纽（Pivot）**
    *   **全局信号增强**：利用 LightGCN 捕捉账号与物品间的全局协作信号。
    *   **频域解耦**：通过快速傅里叶变换（FFT）将混合行为序列转换到频域。基于信号处理理论，将序列分解为多个等宽频带（子带），其中低频代表稳定长期偏好，高频代表突发短期交互。
    *   **自适应融合**：使用类似专家混合（MoE）的机制，对不同频率的行为模式进行加权融合，生成一个统一的账号级表征，作为后续推理的“枢纽”。
*   **第二阶段：潜在用户的渐进式残差推理**
    *   **初始化**：在枢纽表征上加入可学习的推理位置嵌入。
    *   **循环推理**：利用推理函数 $\phi$ 提取当前状态下最显著的潜在用户表征 $u^{(t)}$。
    *   **残差更新**：从当前状态中减去已识别的用户信息（$r^{(t+1)} = r^{(t)} - u^{(t)}$），以避免重复推理。
    *   **自适应终止**：当连续两次推理出的用户表征余弦相似度超过阈值 $\alpha$ 时，自动停止推理，从而动态确定用户数量。
*   **损失函数**：结合了主推荐损失（交叉熵）和辅助推荐损失（针对推理过程中的每个中间用户表征进行监督）。

### 3. 实验设计
*   **数据集**：使用了 4 个基准数据集，包括真实的共享账号数据（HvideoE, HvideoV）和合成的共享账号数据（HamazonM, HamazonB）。
*   **评估指标**：Recall@5/20, MRR@5/20。
*   **对比方法（Baselines）**：
    *   **传统序列推荐（SR）**：SASRec, GRURec, FMLP-Rec, FEARec, BSARec, GLINT-RU。
    *   **共享账号序列推荐（SSR）**：$\pi$-Net, PSJNet, DA-GCN, TiDA-GCN, 以及最先进的 LightGC2N。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA RTX 4090 (24 GB)** 上运行。
*   **软件环境**：PyTorch, Ubuntu 20.04, VS Code。
*   **训练细节**：使用 Adam 优化器，应用了早停（Early Stopping）策略。文中未明确给出具体的训练总时长，但提到了最大序列长度设为 50。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个数据集上进行了全面的性能对比。
    *   **消融实验**：验证了 LightGCN、行为解耦、自适应融合和残差操作的有效性。
    *   **参数敏感性分析**：探讨了频带宽度 $B$、GCN 层数 $L$、终止阈值 $\alpha$ 和损失权重 $\beta$ 的影响。
    *   **鲁棒性测试**：测试了不同序列长度和训练数据比例下的表现。
    *   **深入分析**：可视化了不同账号 ID 推断出的潜在用户数量。
*   **评价**：实验设计全面，涵盖了从宏观性能到微观机制的验证，对比了多种 SOTA 模型，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：DisenReason 在所有数据集上均显著优于现有 SSR 和 SR 模型，MRR@5 最高提升达 12.56%。
*   **动态推断有效**：模型能够根据账号行为的复杂程度，自动推断出不同数量的潜在用户（如某些账号推断出 2 人，某些 4 人），这比固定用户数的假设更符合实际。
*   **频域视角优势**：通过频域解耦能更有效地从混合序列中分离出不同的行为节奏。
*   **鲁棒性强**：在数据稀疏（仅 20% 训练集）或长序列情况下，表现依然稳定且优于基准。

### 7. 优点（亮点）
*   **创新视角**：首次将潜在推理（Latent Reasoning）引入 SSR 任务，并巧妙地将“推断用户偏好”转变为“推断账号背后的用户”。
*   **枢纽设计**：解决了 SSR 中无法直接使用最后一个物品进行推理的难题，通过频域解耦构建了全局账号枢纽。
*   **残差推理机制**：引入残差操作和自适应终止条件，使模型具备了处理变长用户群体的灵活性。

### 8. 不足与局限
*   **频带划分固定**：目前采用等宽频带划分，可能无法完美捕捉某些细粒度的行为边界。
*   **参数耦合**：两阶段推理中的参数存在一定程度的耦合，可能模糊了全局聚合与个性化推理的任务目标。
*   **合成数据偏差**：在合成数据集（Amazon）上，SSR 专用模型有时并不比强力的 SR 模型有绝对优势，反映出合成数据可能无法完全模拟真实的账号共享复杂性。

（完）
