---
title: Why Is RLHF Alignment Shallow? A Gradient Analysis
title_zh: 为什么 RLHF 对齐是浅层的？一项梯度分析
authors: Robin Young
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04851v1"
tags: ["query:llm-bias"]
score: 9.0
evidence: 大语言模型安全对齐的梯度分析
tldr: 本研究探讨了大型语言模型安全对齐为何往往集中在序列早期。通过对梯度进行数学分析，发现基于梯度的对齐本质上集中在决定有害性的关键位置，而在有害性确定后的后续位置梯度会消失。研究提出了“有害信息”概念来量化各位置的影响力，并证明了标准对齐目标无法实现深度对齐。最后，提出了一种基于恢复惩罚的新目标函数，为实现全序列的深度对齐提供了理论支撑。
selection_source: fresh_fetch
motivation: 旨在解释为什么LLM的安全对齐梯度往往集中在序列开头的少数Token，导致对齐效果不够深入。
method: 利用序列级有害性的鞅分解对对齐梯度进行精确的数学表征，分析梯度在不同Token位置的分布规律。
result: 证明了梯度大小取决于该位置对有害性的贡献，一旦有害性在序列早期确定，后续位置的梯度信号将完全消失。
conclusion: 标准对齐目标在理论上无法产生深度对齐，需要通过引入恢复惩罚等新机制来确保全序列都能获得有效的对齐信号。
---

## 摘要
为什么大语言模型（LLMs）的安全对齐是浅层的？我们证明了基于梯度的对齐本质上集中在决定有害性的位置，而在这些位置之后梯度会消失。利用序列级有害性的鞅分解（martingale decomposition），我们推导出了对齐梯度的精确表征。位置 $t$ 处的梯度等于条件期望有害性与评分函数（score function）之间的协方差。这意味着在有害性界限（harm horizon）之外、即输出的有害性已经确定的位置，在训练期间接收到的梯度信号为零。这解释了对齐模型与基础模型之间的 KL 散度集中在早期 token 的实证观察结果。因此，无论优化质量如何，标准的对齐目标都无法产生深层对齐。我们引入了有害信息 $I_t$ 的概念，用于量化每个位置对有害性的影响，并证明了均衡 KL 散度追踪这一指标。最后，我们推导出一个基于恢复惩罚（recovery penalties）的目标函数，该函数在所有位置都能产生梯度信号，为实证中成功的数据增强技术提供了理论基础。

## Abstract
Why is safety alignment in LLMs shallow? We prove that gradient-based alignment inherently concentrates on positions where harm is decided and vanishes beyond. Using a martingale decomposition of sequence-level harm, we derive an exact characterization of alignment gradients. The gradient at position $t$ equals the covariance between the conditional expected harm and the score function. This implies that positions beyond the harm horizon where the output's harmfulness is already determined receive zero gradient signal during training. This explains empirical observations that KL divergence between aligned and base models concentrates on early tokens. Consequently, standard alignment objectives cannot produce deep alignment, regardless of optimization quality. We introduce the concept of harm information $I_t$, which quantifies each position's influence on harm, and prove that equilibrium KL divergence tracks this quantity. Finally, we derive an objective based on recovery penalties that creates gradient signal at all positions, providing theoretical grounding for empirically successful data augmentation techniques.

---

## 论文详细总结（自动生成）

这是一家资深学术论文助手为您提供的关于论文《Why Is RLHF Alignment Shallow? A Gradient Analysis》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）经过了安全对齐（如 RLHF 或 DPO），但这种对齐被证明是非常“浅层”的——即对齐产生的行为变化主要集中在序列开头的几个 token。这导致模型极易受到“预填充攻击”（prefilling attacks），攻击者只需提供有害回答的开头，模型就会绕过安全机制继续生成有害内容。
*   **核心问题**：为什么对齐总是集中在开头？这究竟是训练数据不足、优化算法的缺陷，还是某种更深层的数学必然？
*   **整体含义**：本文通过梯度分析证明，**浅层对齐是标准对齐目标下的数学必然结果**。一旦序列的有害性在早期被确定，后续位置的梯度信号就会消失，导致模型在序列后期失去对齐压力。

### 2. 论文提出的方法论
论文通过严谨的数学推导构建了一个分析框架：
*   **有害性的鞅分解（Martingale Decomposition）**：将序列级的有害性函数分解为每个位置的“创新”（Innovations）。定义 $h_t$ 为给定前 $t$ 个 token 时有害性的条件期望。
*   **梯度精确表征（Theorem 8）**：证明了在位置 $t$ 处的对齐梯度等于“条件期望有害性”与“评分函数（score function）”之间的**协方差**。
    *   公式含义：$\nabla_\theta E[Harm(y)] = \sum Cov(h_t, \nabla \log P_\theta)$。
*   **有害信息（Harm Information, $I_t$）**：引入 $I_t$ 来量化每个位置对最终有害性判定的贡献度（即方差的减少量）。
*   **零梯度定理（Zero-Gradient Theorem）**：证明如果有害性在位置 $k$ 之后已经确定（即进入“有害性界限”之外），那么对于所有 $t > k$，梯度信号恒为零。
*   **深度对齐目标（Deep Alignment Objective）**：为了解决这一问题，提出引入“恢复惩罚”（Recovery Penalties），强制模型在有害序列的任何位置都尝试转向安全行为（如说“对不起”），从而在全序列产生梯度。

### 3. 实验设计
*   **研究性质**：本文是一篇**纯理论证明论文**，侧重于数学分析和定理推导，而非传统的实证对比实验。
*   **对比对象**：论文主要针对标准的 RLHF（基于强化学习的人类反馈）和 DPO（直接偏好优化）目标函数进行理论剖析。
*   **验证场景**：论文引用并解释了 Qi et al. (2025) 的实证观察结果（即 KL 散度在早期 token 后迅速衰减），将其作为理论框架的现实支撑。
*   **Benchmark**：论文通过数学推导给出了“鲁棒性保证”（Robustness Guarantee），证明了在提出的新目标函数下，模型抵御预填充攻击的概率下界。

### 4. 资源与算力
*   **算力说明**：由于本文属于理论分析和数学证明，**文中未提及任何具体的 GPU 型号、数量或训练时长**。其核心贡献在于推导出的定理和闭式解（Closed-form solutions）。

### 5. 实验数量与充分性
*   **理论充分性**：论文提供了 29 个定义、命题和定理，涵盖了从梯度分解、方差分析到均衡状态表征的完整逻辑链条。
*   **客观性**：推导过程严谨，考虑了指数族分布和共享参数（Transformer 架构）等不同假设下的情况，具有较高的理论普适性。
*   **实证缺失**：虽然理论完备，但论文未在真实的大规模模型上进行消融实验来验证“恢复惩罚”在实际训练中的具体数值表现。

### 6. 论文的主要结论与发现
*   **浅层对齐是“最优解”**：在标准目标函数下，模型只需改变开头几个 token 就能最大化降低期望有害性，因此在数学上没有动力去对齐后续 token。
*   **梯度与信息量挂钩**：梯度的大小与该位置的“有害信息量” $I_t$ 的平方根成正比。
*   **KL 散度追踪有害性**：对齐模型与基础模型之间的 KL 散度本质上是在追踪有害性的确定过程。
*   **深度对齐需要目标重构**：要实现真正的深度对齐，必须修改目标函数，在有害性确定后的位置人为创造梯度信号。

### 7. 优点
*   **理论深度极高**：首次利用鞅论（Martingale Theory）对 LLM 对齐梯度进行了精确的解析表征。
*   **解释力强**：完美解释了为什么现有的安全对齐在面对预填充攻击时如此脆弱，将一个经验观察上升到了理论高度。
*   **建设性方案**：不仅指出了问题，还推导出了具有数学保证的防御目标函数（恢复惩罚），为后续算法改进指明了方向。

### 8. 不足与局限
*   **缺乏大规模实证**：论文提出的“深度对齐目标”尚未在超大规模参数模型上进行实际训练验证，其对模型通用能力（如流畅度、知识量）的潜在负面影响仅在理论上讨论。
*   **有害性函数的简化**：假设有害性函数 $Harm(y)$ 是已知且固定的，但在现实中，奖励模型（Reward Model）本身可能存在偏差或分布偏移。
*   **Token 级与语义级的差异**：分析主要在 token 级别展开，而有害性往往是语义级别的，两者之间的映射关系在复杂长文本中可能更加微妙。
*   **忽略了内部表示**：主要关注输出层的分布对齐，未直接涉及表征工程（Representation Engineering）等内部激活层面的干预机制。

（完）
