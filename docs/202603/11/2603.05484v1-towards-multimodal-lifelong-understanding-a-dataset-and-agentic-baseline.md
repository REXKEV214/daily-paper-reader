---
title: "Towards Multimodal Lifelong Understanding: A Dataset and Agentic Baseline"
title_zh: 迈向多模态终身理解：一个数据集与智能体基线
authors: "Guo Chen, Lidong Lu, Yicheng Liu, Liangrui Dong, Lidong Zou, Jixin Lv, Zhenquan Li, Xinyi Mao, Baoqi Pei, Shihao Wang, Zhiqi Li, Karan Sapra, Fuxiao Liu, Yin-Dong Zheng, Yifei Huang, Limin Wang, Zhiding Yu, Andrew Tao, Guilin Liu, Tong Lu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05484v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 多模态终身理解与工作记忆瓶颈
tldr: 本研究针对现有视频理解数据集缺乏真实生活长时序特性的问题，推出了MM-Lifelong数据集，涵盖181.1小时跨越日、周、月尺度的视频。研究揭示了现有大模型在处理超长视频时的“工作记忆瓶颈”和“全局定位崩溃”现象。为此，作者提出了递归多模态智能体（ReMA），通过动态内存管理和递归信念状态更新，在长时序理解任务上显著优于现有方法，为多模态终身学习提供了新的基准和解决方案。
selection_source: fresh_fetch
motivation: 现有视频理解数据集多为密集剪辑，无法模拟真实生活中稀疏且跨度极长的多模态终身理解场景。
method: 构建了181.1小时的多尺度数据集MM-Lifelong，并提出一种基于动态内存管理和递归信念状态更新的智能体ReMA。
result: 实验表明ReMA有效解决了端到端模型在长上下文下的饱和问题及智能体在稀疏时间线上的定位失效问题。
conclusion: 该工作通过新数据集和智能体架构，为多模态终身理解研究提供了严谨的评估基础和高效的基准模型。
---

## 摘要
尽管视频理解数据集的规模已扩展至数小时，但它们通常由密集拼接的片段组成，与自然、无脚本的日常生活有所不同。为了弥补这一差距，我们推出了 MM-Lifelong，这是一个专为多模态终身理解设计的数据集。该数据集包含 181.1 小时的素材，按天、周和月三个尺度进行组织，以捕捉不同的时间密度。广泛的评估揭示了当前范式中的两种关键失效模式：端到端多模态大语言模型（MLLMs）因上下文饱和而面临工作记忆瓶颈（Working Memory Bottleneck），而代表性的智能体基线在处理稀疏的月度时间线时会出现全局定位崩溃（Global Localization Collapse）。为解决这些问题，我们提出了递归多模态智能体（ReMA），它采用动态记忆管理来迭代更新递归信念状态，性能显著优于现有方法。最后，我们建立了旨在隔离时间与领域偏差的数据集划分方式，为监督学习和分布外泛化的未来研究提供了严谨的基础。

## Abstract
While datasets for video understanding have scaled to hour-long durations, they typically consist of densely concatenated clips that differ from natural, unscripted daily life. To bridge this gap, we introduce MM-Lifelong, a dataset designed for Multimodal Lifelong Understanding. Comprising 181.1 hours of footage, it is structured across Day, Week, and Month scales to capture varying temporal densities. Extensive evaluations reveal two critical failure modes in current paradigms: end-to-end MLLMs suffer from a Working Memory Bottleneck due to context saturation, while representative agentic baselines experience Global Localization Collapse when navigating sparse, month-long timelines. To address this, we propose the Recursive Multimodal Agent (ReMA), which employs dynamic memory management to iteratively update a recursive belief state, significantly outperforming existing methods. Finally, we establish dataset splits designed to isolate temporal and domain biases, providing a rigorous foundation for future research in supervised learning and out-of-distribution generalization.

---

## 论文详细总结（自动生成）

### 论文总结：迈向多模态终身理解（MM-Lifelong）

#### 1. 核心问题与整体含义（研究动机和背景）
当前多模态大模型（MLLM）在视频理解上虽已能处理数小时的素材，但仍局限于“密集剪辑”的短时任务。真实世界的“终身理解”具有**高时间稀疏性**（物理跨度 $T_{span}$ 远大于观察时长 $T_{dur}$），即模型需要跨越数天甚至数月的“未观察间隙”来关联事件。
*   **研究动机**：现有的端到端模型在处理超长上下文时会遇到“工作记忆瓶颈”（性能随上下文增加而饱和甚至下降）；而现有的智能体方法在面对月度跨度的稀疏时间线时，容易出现“全局定位崩溃”。
*   **核心目标**：定义“终身理解”标准，构建真实、长跨度的数据集，并提出能有效管理长期记忆的智能体架构。

#### 2. 论文提出的方法论：递归多模态智能体（ReMA）
ReMA 的核心思想是将连续的多模态流转化为一个**动态更新的、以语言为媒介的信念状态（Belief State）**。
*   **感知阶段（Perception Phase）**：
    *   将视频切分为固定长度（如5分钟）的片段。
    *   利用多模态工具（MMInspect）提取通用摘要，并通过内存管理模块（MemManage）将新信息增量式地整合进全局记忆库（Memory Bank），同时合并冗余信息。
*   **控制阶段（Control Phase）**：
    *   LLM 控制器根据用户查询，在记忆库基础上进行迭代推理。
    *   **动作原语**：包括 `Answer`（输出答案）、`MMInspect`（针对特定时间段进行精细化视觉检查）和 `MemSearch`（检索并总结相关记忆条目）。
    *   **递归机制**：每一轮推理的结果都会反馈回记忆库，实现信念状态的逐步精炼。

#### 3. 实验设计
*   **数据集（MM-Lifelong）**：
    *   **规模**：181.1 小时视频，211 GB 数据，涵盖 1289 个问题。
    *   **多尺度设计**：分为“天”（游戏过程）、“周”（第一视角日常生活）、“月”（无脚本直播流）三个尺度。
    *   **任务类型**：包括“大海捞针”（寻找特定瞬间）和“多跳推理”（跨越数天关联信息）。
*   **Benchmark 与对比方法**：
    *   **端到端 MLLMs**：GPT-5、Qwen3-VL (235B/30B)、Video-XL-2、Eagle-2.5、Nemotron-v2。
    *   **智能体基线**：VideoMind、LongVT、DeepVideoDiscovery (DVD)。
*   **评估指标**：答案准确率（LLM-as-a-Judge）和参考定位精度（Ref@N，基于量化时间交并比）。

#### 4. 资源与算力
*   **模型使用**：实验主要采用了 GPT-5 和 Qwen3-VL 系列作为控制器和视觉感知后端。
*   **算力说明**：论文**未明确列出**具体的硬件集群配置（如 GPU 型号、数量）或总训练/推理时长。但提到了标注成本极高：月尺度数据的单个样本验证平均耗时 51 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同时间尺度的测试集上进行了全面对比。
    *   **消融实验**：分析了递归深度（Round 0-8）、感知粒度（2min 到 Full Video）、不同骨干模型（MLLM vs. 纯文本模型）对性能的影响。
    *   **一致性验证**：通过人工评估验证了 GPT-5 作为裁判的可靠性（F1 分数达 99.39）。
*   **客观性与公平性**：采用了严格的时间划分（按时间顺序 3:7 划分训练/验证集），避免了随机采样导致的环境信息泄露，确保了评估的严谨性。

#### 6. 主要结论与发现
*   **工作记忆瓶颈**：端到端模型在增加输入帧数时，性能最初提升但很快会因噪声积累和上下文饱和而下降。
*   **智能体优势**：ReMA 通过将视觉信息转化为语言空间并进行动态管理，有效突破了上下文天花板，在准确率和定位精度上均显著优于端到端模型。
*   **语言中心化记忆的重要性**：实验证明，即使是文本空间的推理，使用 MLLM 作为控制器也比纯文本模型更稳定，因为多模态对齐增强了指令遵循能力。

#### 7. 优点
*   **数据集质量高**：MM-Lifelong 填补了真实生活长时序、稀疏性数据集的空白，且提供了基于线索（Clue-Grounded）的手工标注。
*   **任务定义严谨**：明确区分了“观察时长”与“物理跨度”，为终身学习研究提供了清晰的数学定义。
*   **ReMA 架构的扩展性**：递归推理机制允许模型通过增加推理轮次来提升复杂问题的处理能力，具有良好的 Scaling 潜力。

#### 8. 不足与局限
*   **主体多样性有限**：每个尺度的数据主要跟踪单一主体（如一个游戏角色或一个主播），缺乏大规模的人群多样性。
*   **未观察间隙的建模**：虽然测试了跨间隙推理，但尚未深入探讨“未记录时间”内发生的隐性状态演变对后续观察的复杂影响。
*   **实时性挑战**：ReMA 依赖多次调用 LLM 和视觉模型，在处理超大规模视频流时的推理成本和延迟可能较高。

（完）
