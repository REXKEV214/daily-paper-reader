---
title: "MEMO: Memory-Augmented Model Context Optimization for Robust Multi-Turn Multi-Agent LLM Games"
title_zh: MEMO：用于鲁棒多轮多智能体大语言模型博弈的记忆增强模型上下文优化
authors: "Yunfei Xie, Kevin Wang, Bobby Cheng, Jianzhu Yao, Zhizhou Sha, Alexander Duffy, Yihan Xi, Hongyuan Mei, Cheston Tan, Chen Wei, Pramod Viswanath, Zhangyang Wang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.09022v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: 多轮 LLM 交互的持久化存储库
tldr: 针对多轮多智能体大模型游戏评估中存在的高方差和性能不稳定问题，本文提出了MEMO框架。该框架通过自博弈优化推理时上下文，结合了持久化记忆库（保留结构化见解）与基于TrueSkill的不确定性感知提示词演化（探索）。实验表明，MEMO在多项文本游戏中显著提升了胜率并降低了运行波动，尤其在谈判和非完全信息博弈中表现优异。
selection_source: fresh_fetch
motivation: 多轮多智能体交互中的微小偏差会随时间累积并被耦合效应放大，导致评估结果不稳定且排名不可靠。
method: 提出MEMO框架，通过记忆保留机制存储自博弈见解，并利用基于TrueSkill的提示词演化和优先级回放进行探索。
result: 在五种游戏中，MEMO将GPT-4o-mini和Qwen-2.5的平均胜率分别提升了约24个百分点，并显著降低了运行方差。
conclusion: 上下文优化能有效提升多智能体游戏的性能与鲁棒性，MEMO在复杂博弈场景下比传统强化学习更具优势。
---

## 摘要
多轮、多智能体大语言模型（LLM）博弈评估通常表现出显著的运行间方差。在长时程交互中，早期的微小偏差会随轮次累积，并被多智能体耦合放大。这会导致胜率估计产生偏差，并使重复锦标赛中的排名变得不可靠。提示词的选择通过产生不同的有效策略进一步加剧了这一问题。我们通过 MEMO（记忆增强模型上下文优化）来解决不稳定性与性能不足的问题。这是一个自博弈框架，通过耦合保留（retention）与探索（exploration）来优化推理时的上下文。保留机制维护一个持久的记忆库，存储来自自博弈轨迹的结构化见解，并在随后的博弈中将其作为先验知识注入。探索机制通过 TrueSkill 进行具有不确定性感知选择的锦标赛式提示词演化，并使用优先回放来重新访问稀有且关键的状态。在五种基于文本的游戏中，MEMO 将 GPT-4o-mini 的平均胜率从 25.1% 提高到 49.5%，将 Qwen-2.5-7B-Instruct 的平均胜率从 20.9% 提高到 44.3%（每个任务使用 2,000 场自博弈）。运行间方差也有所下降，使得在不同提示词变化下的排名更加稳定。这些结果表明，通过上下文优化，多智能体 LLM 博弈的性能和鲁棒性仍有巨大的提升空间。MEMO 在谈判和非完全信息博弈中取得了最大的收益，而强化学习（RL）在完全信息设定下仍然更有效。

## Abstract
Multi-turn, multi-agent LLM game evaluations often exhibit substantial run-to-run variance. In long-horizon interactions, small early deviations compound across turns and are amplified by multi-agent coupling. This biases win rate estimates and makes rankings unreliable across repeated tournaments. Prompt choice worsens this further by producing different effective policies. We address both instability and underperformance with MEMO (Memory-augmented MOdel context optimization), a self-play framework that optimizes inference-time context by coupling retention and exploration. Retention maintains a persistent memory bank that stores structured insights from self-play trajectories and injects them as priors during later play. Exploration runs tournament-style prompt evolution with uncertainty-aware selection via TrueSkill, and uses prioritized replay to revisit rare and decisive states. Across five text-based games, MEMO raises mean win rate from 25.1% to 49.5% for GPT-4o-mini and from 20.9% to 44.3% for Qwen-2.5-7B-Instruct, using $2,000$ self-play games per task. Run-to-run variance also drops, giving more stable rankings across prompt variations. These results suggest that multi-agent LLM game performance and robustness have substantial room for improvement through context optimization. MEMO achieves the largest gains in negotiation and imperfect-information games, while RL remains more effective in perfect-information settings.

---

## 论文详细总结（自动生成）

### 论文总结：MEMO —— 用于鲁棒多轮多智能体 LLM 博弈的记忆增强上下文优化

#### 1. 核心问题与整体含义（研究动机和背景）
在多轮、多智能体的 LLM 博弈评估中，存在显著的**运行间方差（Run-to-run variance）**。由于多轮交互的路径依赖性，早期的微小偏差会随轮次累积，并因智能体间的耦合效应而放大，导致胜率估计偏差和模型排名不稳定。此外，LLM 对提示词（Prompt）高度敏感，微小的措辞变化可能诱发完全不同的策略。
**研究动机**：现有的提示词优化方法多针对静态任务，缺乏在动态博弈中保留长期策略见解的能力。本文旨在通过优化“推理时上下文”（Inference-time context）而非更新模型权重，提升 LLM 在复杂博弈中的性能与鲁棒性。

#### 2. 方法论：核心思想与关键技术
MEMO 框架结合了**保留（Retention）**与**探索（Exploration）**，其核心流程如下：

*   **锦标赛式提示词演化（Tournament-Based Optimization）**：
    *   维护一个候选上下文种群，通过自博弈（Self-play）进行评估。
    *   **TrueSkill 选择机制**：使用贝叶斯技能评分系统评估每个提示词的水平。公式为 $S(c) = \mu_c - \kappa \sigma_c$，其中 $\mu_c$ 是平均技能，$\sigma_c$ 是不确定性，$\kappa$ 是惩罚系数。这种“保守估计”倾向于选择表现稳定且不确定性低的提示词。
*   **持久化记忆库（Persistent Memory Bank）**：
    *   **轨迹反思**：从完成的博弈轨迹中提取结构化见解（如规则澄清、策略先验）。
    *   **CRUD 操作**：对记忆进行“增（Add）、删（Remove）、改（Edit）”。如果新见解与旧见解冲突则删除，相似则合并，独特则添加。
    *   **先验注入**：在后续博弈中，将这些见解作为推理时的先验知识注入上下文。
*   **优先级回放（Prioritized Replay）**：
    *   维护一个回放缓冲区，存储轨迹前缀。
    *   使用**逆频率评分**（Inverse-frequency score）对稀有状态进行优先级排序，确保模型能重新访问并学习那些决定性的、罕见的失败或成功模式。

#### 3. 实验设计
*   **场景与数据集**：涵盖三类共 5 种文本博弈：
    *   **谈判类**：SimpleNegotiation（资源交换）、TwoDollar（2美元分配）。
    *   **非完全信息类**：KuhnPoker（库恩扑克）、Briscola（意大利纸牌）。
    *   **完全信息类**：SimpleTak（连线游戏）。
*   **Benchmark**：使用 TextArena 和 SPIN-Bench。
*   **对比方法**：
    *   **静态提示词**：Baseline, CoT（思维链）, ToT（思维树）。
    *   **提示词优化**：TextGrad, MIPRO, GEPA。
    *   **强化学习（RL）**：UnstableBaselines, SPIRAL（通过自博弈更新权重）。
*   **评估指标**：平均胜率和相对标准误差（RSE）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **样本规模**：每个任务进行 2,000 场自博弈。
*   **效率对比**：在库恩扑克中，MEMO 达到 60% 胜率仅需 2,000 场游戏，比 RL 基线方法（需 38,000 场）节省了 **19 倍** 的交互量。
*   **Token 消耗**：MEMO 的平均输出 Token 消耗约为 91K，显著低于 MIPRO（354K）和 GEPA（113K）。

#### 5. 实验数量与充分性
*   **实验规模**：在 2 个基础模型（GPT-4o-mini, Qwen-2.5-7B）上进行了跨 5 种游戏的测试。
*   **消融实验**：详细测试了记忆库、回放机制、锦标赛选择各自的贡献，验证了“保留”与“探索”耦合的必要性。
*   **泛化性测试**：进行了跨游戏（Zero-shot 迁移）和跨模型（从 GPT 迁移到 Gemini/Grok）的实验。
*   **客观性**：通过 3 次独立运行取平均值，并引入 RSE 指标衡量稳定性，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **性能提升**：MEMO 将 GPT-4o-mini 的平均胜率从 25.1% 提升至 49.5%，Qwen-2.5 从 20.9% 提升至 44.3%。
*   **稳定性增强**：运行间方差大幅下降，RSE 从基线的 40% 以上降至 6.4% 左右，提供了更可靠的模型排名。
*   **博弈类型差异**：MEMO 在**谈判和非完全信息博弈**中表现最强；但在**完全信息博弈**（如 SimpleTak）中，传统的强化学习（RL）由于能进行更深层的权重优化，依然更具优势。
*   **知识迁移**：学习到的上下文（策略见解）具有跨游戏迁移的能力，但在跨模型迁移时，如果目标模型本身很强，可能会出现负迁移（干扰原生策略）。

#### 7. 优点
*   **无需训练**：通过纯上下文优化实现性能飞跃，无需昂贵的模型微调或权重更新。
*   **高样本效率**：相比 RL 方法，极大地减少了所需的博弈场次。
*   **鲁棒性高**：引入 TrueSkill 处理评估中的不确定性，有效解决了 LLM 博弈评估不稳定的痛点。
*   **可解释性**：持久化记忆库存储的是自然语言见解，人类可直观理解模型学到了哪些策略。

#### 8. 不足与局限
*   **任务局限性**：在需要极高逻辑严密性的完全信息博弈中，性能上限仍受限于推理侧优化，不如直接更新权重的 RL。
*   **模型依赖**：虽然是权重无关的，但优化过程仍依赖于一个强大的“优化器 LLM”来生成提示词和反思轨迹。
*   **迁移风险**：跨模型迁移并不总是正向的，特定模型的优化策略可能不适用于另一种架构。
*   **文本限制**：目前仅在文本博弈中验证，尚未扩展到多模态或更复杂的实时博弈环境。

（完）
