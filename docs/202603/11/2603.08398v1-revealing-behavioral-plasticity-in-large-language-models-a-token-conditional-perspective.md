---
title: "Revealing Behavioral Plasticity in Large Language Models: A Token-Conditional Perspective"
title_zh: 揭示大语言模型中的行为可塑性：一种 Token 条件视角
authors: "Liyuan Mao, Le Yu, Jing Zhou, Chujie Zheng, Bowen Yu, Chang Gao, Shixuan Liu, An Yang, Weinan Zhang, JunYang Lin"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08398v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 通过行为可塑性实现的自适应语言模型
tldr: 本研究揭示了大语言模型（LLM）具有类似变色龙的行为可塑性，可通过特定Token前缀在推理时无缝切换行为模式（如从思维链转向直接回答）。基于此，作者提出了Token条件强化学习（ToCoRL）框架，利用强化学习将这种瞬时的推理适应转化为稳定的行为模式。实验证明，ToCoRL能在不损害模型能力的前提下实现精确的行为控制，显著提升了推理模型在事实问答等任务中的表现。
selection_source: fresh_fetch
motivation: 旨在探索LLM在推理阶段通过Token引导展现出的内在行为可塑性，并解决如何将其转化为稳定可控行为的问题。
method: 提出ToCoRL框架，通过Token条件生成引导探索，并利用强化学习内化并稳定特定的行为模式。
result: 实验表明ToCoRL实现了精确的行为控制且无性能退化，成功让推理模型在保持数学能力的同时，通过改变模式在事实问答中表现优异。
conclusion: LLM的行为模式可以通过Token条件化进行有效调节和强化，为实现灵活且受控的模型行为提供了新路径。
---

## 摘要
在这项工作中，我们揭示了大语言模型（LLMs）具有内在的行为可塑性——类似于变色龙根据环境线索调整体色——这种特性可以通过 Token 条件生成（token-conditional generation）来展现，并通过强化学习（reinforcement learning）进行稳定。具体而言，通过将生成过程以从展现目标行为的响应中采样出的、经过精心选择的 Token 前缀为条件，LLMs 可以在推理阶段无缝调整其行为模式（例如，从逐步推理切换到直接回答），而无需重新训练。基于这一洞察，我们提出了 Token 条件强化学习（ToCoRL），这是一个利用强化学习将这种变色龙般的可塑性内化的原则性框架，将瞬时的推理阶段适应转化为稳定且可学习的行为模式。ToCoRL 利用 Token 条件生成引导探索，并持续增强利用（exploitation），从而使适当的行为得以涌现。大量实验表明，ToCoRL 能够在不降低能力的情况下实现精确的行为控制。值得注意的是，我们展示了大型推理模型虽然在复杂数学方面表现强劲，但可以被有效地调整以擅长事实性问答，而这一能力此前曾受到其逐步推理模式的阻碍。

## Abstract
In this work, we reveal that Large Language Models (LLMs) possess intrinsic behavioral plasticity-akin to chameleons adapting their coloration to environmental cues-that can be exposed through token-conditional generation and stabilized via reinforcement learning. Specifically, by conditioning generation on carefully selected token prefixes sampled from responses exhibiting desired behaviors, LLMs seamlessly adapt their behavioral modes at inference time (e.g., switching from step-by-step reasoning to direct answering) without retraining. Based on this insight, we propose Token-Conditioned Reinforcement Learning (ToCoRL), a principled framework that leverages RL to internalize this chameleon-like plasticity, transforming transient inference-time adaptations into stable and learnable behavioral patterns. ToCoRL guides exploration with token-conditional generation and keep enhancing exploitation, enabling emergence of appropriate behaviors. Extensive experiments show that ToCoRL enables precise behavioral control without capability degradation. Notably, we show that large reasoning models, while performing strongly on complex mathematics, can be effectively adapted to excel at factual question answering, which was a capability previously hindered by their step-by-step reasoning patterns.

---

## 论文详细总结（自动生成）

### 论文总结：揭示大语言模型中的行为可塑性：一种 Token 条件视角

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大型语言模型（LLM）的**行为可塑性**（Behavioral Plasticity），即模型像变色龙一样根据环境线索调整自身行为模式的能力。
*   **研究背景**：当前的推理模型（LRM，如 Qwen-Thinking 或 OpenAI o1 类模型）虽然擅长复杂数学推理，但在处理简单事实问答时，往往会陷入冗长且不必要的逐步推理（CoT），这不仅浪费算力，还可能因过度联想导致幻觉。
*   **核心问题**：如何挖掘并稳定 LLM 内在的行为调节能力，使其能根据任务需求在“深度推理”与“直接回答”之间灵活切换，而无需针对每种行为单独微调。

#### 2. 方法论：ToCoRL 框架
论文提出了 **ToCoRL（Token-Conditioned Reinforcement Learning，Token 条件强化学习）**，旨在将推理时的瞬时行为引导转化为模型持久的内在能力。
*   **核心思想**：利用“Token 条件生成”来引导强化学习的探索过程。通过给模型提供一小段展现目标行为的 Token 前缀（例如由 Instruct 模型生成的直接回答的前缀），诱导模型进入特定的行为轨道。
*   **关键技术细节**：
    *   **Token 条件引导**：在 RL 的 Rollout 阶段，强制模型以特定前缀（如 `<think>\n` 加上几个直接回答的 Token）开始生成。
    *   **KL 散度约束优化**：设计了一个定制的 KL 散度项，将当前策略 $\pi_\theta$ 与由 Token 条件诱导的参考策略进行对齐。
    *   **混合策略（Mixed Policy）**：为了降低优势估计（Advantage Estimation）的方差，ToCoRL 在实践中采用混合采样策略，结合了正常生成和 Token 条件生成的样本。
    *   **重新校准式推理（Recalibrative Reasoning）**：通过 ToCoRL 训练，模型涌现出一种新行为——先给出直接答案，随后进行简短的自我验证和校准，而非盲目的长链推理。

#### 3. 实验设计
*   **实验对象**：主要基于 Qwen3-30B-A3B-2507-Thinking（推理模型）和其 Base 版本。
*   **数据集 / Benchmark**：
    *   **事实问答**：SimpleQA、AA-Omniscience（评估知识检索的准确性）。
    *   **数学推理**：AIME’24、AIME’25（评估复杂逻辑推理能力是否退化）。
*   **对比方法**：
    *   **GRPO**：标准的群体相对策略优化。
    *   **Adaptive-Thinking**：通过长度惩罚强制模型简洁的 RL 方法。
    *   **Instruct-Tuning GRPO**：在 Prompt 中加入行为引导指令的 RL 方法。

#### 4. 资源与算力
*   **框架支持**：使用 **Megatron** 进行参数更新，使用 **vLLM** 进行高效的 Rollout 采样。
*   **超参数**：学习率 2e-6，Batch Size 为 128，Rollout Group Size 为 16。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）及具体数量，也未提及总训练时长，仅提到最大训练步数为 1000 步且支持早期停止（Early Stopping）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   核心实验涵盖了从 Thinking 模型和 Base 模型开始的两种路径。
    *   进行了**消融实验**，测试了 KL 系数 $\lambda$ 和前缀长度 $k$ 的影响。
    *   测试了**鲁棒性**，更换了不同的前缀提供者（如 Qwen2.5-14B, Qwen3-235B）。
    *   验证了**行为迁移性**，证明 ToCoRL 发现的行为模式可以通过 SFT 蒸馏给其他模型。
*   **充分性评价**：实验设计较为全面，不仅关注了目标任务（事实问答）的提升，还严格监控了原始强项（数学推理）的稳定性。通过多维度对比，客观地证明了 ToCoRL 在行为控制上的精确度优于简单的长度惩罚或指令引导。

#### 6. 主要结论与发现
*   **可塑性证实**：LLM 内部存在未被充分利用的行为模式，仅需极短的 Token 前缀即可激活。
*   **性能飞跃**：ToCoRL 使 Qwen3-30B 推理模型在 SimpleQA 上的准确率从 18.9% 提升至 28.3%，同时在 AIME 数学竞赛上的表现稳中有升（80.5 -> 81.5）。
*   **行为涌现**：模型学会了“按需推理”，即简单问题直接回答，复杂问题进行“重新校准式”的针对性思考，而非无效的思维发散。
*   **兼容性**：证明了多样化的行为模式（数学推理 vs. 事实检索）可以在同一个统一模型中和谐共存，互不干扰。

#### 7. 优点
*   **视角独特**：从 Token 级别的微观视角切入，揭示了自回归模型行为生成的动态本质。
*   **理论与实践结合**：提供了完备的数学证明（定理 4.1-4.4）来支撑 RL 目标的合理性，并给出了可操作的算法流程。
*   **解决实际痛点**：有效解决了 LRM 在简单任务上“过度思考”导致的效率低下和幻觉问题。

#### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在数学和事实问答两个极端场景，对于创意写作、多轮对话等更通用的行为模式探讨较少。
*   **前缀依赖**：训练初期仍依赖于另一个高性能 Instruct 模型提供 Token 前缀，虽然实验证明对前缀提供者的要求不高，但仍存在一定的外部依赖。
*   **算力成本**：作为一种在线 RL 方法，尽管比 PPO 简化，但大规模 Rollout 带来的计算开销依然是实际部署中的挑战。

（完）
