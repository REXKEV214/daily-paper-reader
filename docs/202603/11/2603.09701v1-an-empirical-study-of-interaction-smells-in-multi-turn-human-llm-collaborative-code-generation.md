---
title: An Empirical Study of Interaction Smells in Multi-Turn Human-LLM Collaborative Code Generation
title_zh: 多轮人机（LLM）协作代码生成中交互异味的实证研究
authors: "Binquan Zhang, Li Zhang, Lin Shi, Song Wang, Yuwei Qian, Linhui Zhao, Fang Liu, An Fu, Yida Ye"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09701v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 人机协作代码生成的交互异味
tldr: 本研究针对大模型在多轮协作编程中存在的上下文一致性问题，提出了“交互异味（Interaction Smells）”概念。通过对真实交互数据的实证研究，建立了包含用户意图质量、历史指令遵循和历史响应违背三大类的分类体系。随后评估了GPT-4o等主流模型在交互异味上的表现，并提出InCE多智能体框架，通过提取全局不变式和预生成审计来优化交互质量，显著提升了任务成功率并减少了异味发生。
selection_source: fresh_fetch
motivation: 现有基准测试侧重于代码最终的功能正确性，忽视了多轮交互过程中影响协作效率和质量的潜在问题。
method: 通过对真实交互日志进行开放式卡片分类建立分类学，并提出基于全局不变式提取与预生成质量审计的InCE多智能体框架。
result: 实验表明，InCE框架在WildBench基准上显著提高了任务成功率，并有效抑制了各类交互异味的出现。
conclusion: 识别并缓解交互异味对于提升人机协作编程的连贯性与效率至关重要，InCE为优化多轮对话质量提供了有效方案。
---

## 摘要
大语言模型（LLMs）彻底改变了代码生成，使其从静态工具演变为能够促进复杂多轮协作编程的动态对话接口。虽然 LLMs 在生成独立代码片段方面表现出卓越的能力，但在长时间交互过程中往往难以保持上下文一致性，从而在协作过程中造成了重大障碍。现有的基准测试主要强调最终输出的功能正确性，忽视了交互过程本身潜在的质量问题，我们将其称为“交互异味”（Interaction Smells）。在本文中，我们对来自 WildChat 和 LMSYS-Chat-1M 数据集的真实用户与 LLM 交互样本进行了实证研究，从现象、分布和缓解的角度系统地调查了人机协作代码生成任务中的交互异味。首先，我们通过对真实交互日志进行人工开放式卡片分类，建立了首个交互异味分类法。该分类法将交互异味分为三个主要类别，即用户意图质量、历史指令遵循和历史响应违背，共包含九个具体的子类。接下来，我们定量评估了六种主流 LLMs（即 GPT-4o、DeepSeek-Chat、Gemini 2.5、Qwen2.5-32B、Qwen2.5-72B 和 Qwen3-235B-a22b），以分析交互异味在不同模型中的分布情况。最后，我们提出了不变性感知约束演化（InCE），这是一个多智能体框架，旨在通过显式提取全局不变性和生成前质量审计来提高多轮交互质量。在扩展的 WildBench 基准测试上的实验结果表明，这种轻量级的缓解方法显著提高了任务成功率，并有效抑制了交互异味的发生。

## Abstract
Large Language Models (LLMs) have revolutionized code generation, evolving from static tools into dynamic conversational interfaces that facilitate complex, multi-turn collaborative programming. While LLMs exhibit remarkable proficiency in generating standalone code snippets, they often struggle to maintain contextual consistency during extended interactions, creating significant obstacles in the collaboration process. Existing benchmarks primarily emphasize the functional correctness of the final output, overlooking latent quality issues within the interaction process itself, which we term Interaction Smells. In this paper, we conduct an empirical study on sampled real-word user-LLM interactions from WildChat and LMSYS-Chat-1M datasets to systematically investigate Interaction Smells in human-LLM code generation tasks from the perspectives of phenomena, distribution, and mitigation. First, we establish the first taxonomy of Interaction Smells by manually performing open card sorting on real-world interaction logs. This taxonomy categorizes Interaction Smells into three primary categories, i.e., User Intent Quality, Historical Instruction Compliance, and Historical Response Violation, comprising nine specific subcategories. Next, we quantitatively evaluate six mainstream LLMs (i.e., GPT-4o, DeepSeek-Chat, Gemini 2.5, Qwen2.5-32B, Qwen2.5-72B, and Qwen3-235B-a22b) to analyze the distribution of Interaction Smells across different models. Finally, we propose Invariant-aware Constraint Evolution (InCE), a multi-agent framework designed to improve multi-turn interaction quality through explicit extraction of global invariants and pre-generation quality audits. Experimental results on the extended WildBench benchmark demonstrate that this lightweight mitigation approach significantly improves the Task Success Rate and effectively suppresses the occurrence of Interaction Smells.

---

## 论文详细总结（自动生成）

这篇论文对多轮人机协作代码生成中的“交互异味”进行了深入的实证研究，并提出了相应的缓解框架。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的发展，代码生成已从单次生成演变为多轮对话协作。然而，LLM 在长对话中常出现上下文丢失、指令偏离等问题，导致协作效率低下。
*   **研究动机**：现有的评估基准（如 Pass@k）主要关注最终代码的功能正确性，而忽视了交互过程中的质量问题。
*   **核心概念**：论文首次提出了**“交互异味”（Interaction Smells）**的概念，指代那些阻碍交互收敛、破坏开发者心流的潜在障碍。研究旨在系统地识别这些异味、分析其分布并提出缓解方案。

### 2. 方法论：核心思想与关键技术
论文提出了 **InCE（Invariant-aware Constraint Evolution，不变性感知约束演化）** 框架，采用多智能体协作模式来优化交互质量：
*   **核心思想**：将约束处理与代码生成解耦，通过显式管理全局不变式（Invariants）来对抗长对话中的注意力稀释。
*   **关键模块**：
    1.  **不变式提取模块（IEM）**：利用 LLM 从历史对话中识别并提炼出全局约束（如格式要求、禁用库等），剔除过时或冗余指令，遵循“最新指令优先”原则。
    2.  **主动异味检测器（PSD）**：在生成代码前进行质量审计。它将当前用户意图与 IEM 维护的不变式池进行交叉比对，检测是否存在歧义或冲突，并输出结构化的“约束清单”指导生成。
*   **算法流程**：用户输入 -> PSD 检测异味 -> IEM 更新/提取约束 -> 生成器参考约束清单生成代码 -> 循环迭代。

### 3. 实验设计
*   **数据集**：
    *   **分类学构建**：从 **WildChat** 和 **LMSYS-Chat-1M** 中提取了 66,371 条编程相关对话，并人工标注了 378 个多轮样本。
    *   **模型评估**：使用 **WildBench**（WildChat 的高质量子集）作为测试基准。
*   **对比方法**：评估了 6 种主流 LLM 的原生表现（Vanilla）与应用 InCE 框架后的表现，包括 **GPT-4o、DeepSeek-Chat (V3)、Gemini 2.5 Flash、Qwen2.5 (32B/72B) 和 Qwen3-235B**。
*   **评估设置**：引入 **GPT-4 作为用户模拟器**进行闭环反馈，模拟真实的人机交互过程。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力资源（如 GPU 型号、数量或总训练时长）。
*   实验主要基于 API 调用（如 GPT-4o, Gemini）和开源模型推理，侧重于框架的逻辑设计而非大规模模型预训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **人工标注**：由 4 名专家和 4 名研究生通过“开放式卡片分类法”进行标注，Cohen’s Kappa 系数达到 0.82，显示了较高的一致性。
    *   **定量评估**：每个模型在 WildBench 上进行了 170 个任务的测试，涵盖了 9 种异味的分布统计。
*   **充分性与公平性**：实验覆盖了从闭源顶尖模型到开源 MoE 模型的多种架构，使用了统一的自动化评估 Oracle（基于 WildBench Checklist），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **分类学建立**：建立了包含 3 大类（用户意图质量、历史指令遵循、历史响应违背）和 **9 种细分异味**（如：歧义指令、必须项遗漏、功能局部崩溃、代码回滚等）的体系。
*   **异味分布**：**“必须项遗漏”（Must-Do Omit）**和**“功能局部崩溃”（Partial Functionality Breakdown）**是最普遍的异味，即使是 GPT-4o 也有超过 50% 的概率出现必须项遗漏。
*   **框架效果**：InCE 框架显著提升了任务成功率（TSR），其中 GPT-4o 提升了 5.00%，Gemini 提升了 6.67%，并有效减少了约 13.5% 的重复响应和指令遗漏。

### 7. 优点与亮点
*   **视角新颖**：从“过程质量”而非仅仅“结果正确性”切入，填补了人机协作编程研究的空白。
*   **实证扎实**：基于百万量级的真实世界交互数据进行分析，而非人工合成的简单任务。
*   **即插即用**：InCE 框架是一种轻量级的增强层，不需要重新训练模型，具有很强的实用性。

### 8. 不足与局限
*   **主观性风险**：尽管有专家参与，但异味的定义和标注仍存在一定的主观性。
*   **评估指标局限**：主要依赖 TSR（成功率）和 ATS（平均回合数），未能充分衡量用户的心理感受（如挫败感、认知负荷）。
*   **应用范围**：目前主要针对通用编程任务，在特定领域（如嵌入式、低代码平台）的适用性有待验证。
*   **生存偏差**：在计算 ATS 时仅统计成功样本，可能掩盖了某些极难任务的交互成本。

（完）
