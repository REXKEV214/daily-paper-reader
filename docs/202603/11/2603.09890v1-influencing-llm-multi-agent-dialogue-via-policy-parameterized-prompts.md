---
title: Influencing LLM Multi-Agent Dialogue via Policy-Parameterized Prompts
title_zh: 通过策略参数化提示词影响大语言模型多智能体对话
authors: "Hongbo Bo, Jingyu Hu, Weiru Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09890v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 通过参数化提示影响多智能体对话
tldr: 本研究针对大语言模型（LLM）多智能体对话中缺乏系统性策略控制的问题，提出了一种基于策略参数化提示词的方法。该方法将提示词视为可参数化的动作，通过五个核心组件根据当前状态动态构建提示词，无需额外训练即可引导对话行为。实验在公共讨论场景下通过响应性、反驳性等五个指标验证了其有效性，为多智能体社会模拟提供了简单高效的对话干预机制。
selection_source: fresh_fetch
motivation: 现有的LLM多智能体系统依赖随机提示词，缺乏从策略角度对对话行为进行系统化和原则性控制的研究。
method: 提出一种将提示词视为动作的框架，通过五个组件根据智能体当前状态动态构建参数化提示词策略。
result: 实验表明，该参数化控制方法能显著影响对话的响应性、证据使用和立场转变等动态特征。
conclusion: 策略参数化提示词为影响多智能体对话过程提供了一种无需训练且简单有效的机制，有助于社会模拟研究。
---

## 摘要
大语言模型（LLMs）已成为多智能体系统的一种新范式。然而，现有关于基于 LLM 的多智能体行为的研究依赖于即时（ad hoc）提示词，缺乏系统性的策略视角。与强化学习不同，我们研究了“提示词即动作”（prompt-as-action）是否可以被参数化，从而构建一种由一系列状态-动作对组成的轻量级策略，在无需训练的情况下影响对话行为。我们的框架将提示词视为 LLM 执行的动作，并根据智能体的当前状态，通过五个组件动态构建提示词。为了测试参数化控制的有效性，我们基于五个指标评估了对话流：响应性、反驳性、证据使用、非重复性和立场转变。我们在两个与公众相关的讨论场景中使用不同的 LLM 驱动智能体进行了实验，结果表明提示词参数化可以影响对话动态。这一结果表明，策略参数化提示词为影响对话过程提供了一种简单且有效的机制，这将有助于多智能体系统在社会模拟方向的研究。

## Abstract
Large Language Models (LLMs) have emerged as a new paradigm for multi-agent systems. However, existing research on the behaviour of LLM-based multi-agents relies on ad hoc prompts and lacks a principled policy perspective. Different from reinforcement learning, we investigate whether prompt-as-action can be parameterized so as to construct a lightweight policy which consists of a sequence of state-action pairs to influence conversational behaviours without training. Our framework regards prompts as actions executed by LLMs, and dynamically constructs prompts through five components based on the current state of the agent. To test the effectiveness of parameterized control, we evaluated the dialogue flow based on five indicators: responsiveness, rebuttal, evidence usage, non-repetition, and stance shift. We conduct experiments using different LLM-driven agents in two discussion scenarios related to the general public and show that prompt parameterization can influence the dialogue dynamics. This result shows that policy-parameterised prompts offer a simple and effective mechanism to influence the dialogue process, which will help the research of multi-agent systems in the direction of social simulation.

---

## 论文详细总结（自动生成）

### 论文总结：通过策略参数化提示词影响大语言模型多智能体对话

#### 1. 核心问题与整体含义（研究动机和背景）
在多智能体系统（MAS）中，基于大语言模型（LLM）的智能体通常依赖于即兴（ad hoc）的提示词进行交流。这种方式缺乏系统性的策略框架，导致智能体的行为难以预测、难以优化，且在不同任务间缺乏可迁移性。
本研究的核心动机是探索一种**原则性的方法**，将智能体的通信策略形式化。研究者提出将“提示词”视为一种“动作”（Prompt-as-Action），并通过参数化的方式构建轻量级策略，旨在无需额外微调或强化学习训练的情况下，系统性地引导和控制多智能体对话的行为动态。

#### 2. 方法论：核心思想与关键技术
论文提出了一个将提示词参数化的轻量级策略框架，其核心思想是将智能体的状态映射为动态构建的提示词。

*   **核心组件（T-M-D-R-W）**：
    *   **T (Task & Persona)**：任务描述与角色设定。
    *   **M (Memory)**：对话历史记忆，采用共享消息池模式。
    *   **D (External Knowledge)**：通过检索增强生成（RAG）获取的角色相关外部知识。
    *   **R (Rule Template)**：规则模板，分为三级：`None`（无约束）、`Light`（基本结构约束）、`Struct`（严格的推理结构约束）。
    *   **W (Weights)**：权重向量，控制智能体对 T、M、D 组件的依赖程度。
*   **策略参数化机制**：
    *   **权重三级信号系统**：将权重（0-2范围）映射为“低、中、高”三个层级，每个层级对应不同的微指令（例如，高权重 D 要求必须列出至少2条证据）。
    *   **自适应权重调度器**：
        1.  **时间趋势更新**：随对话轮数增加，自动调高记忆（M）权重，调低知识（D）权重，以促进从“陈述观点”向“深入辩论”转变。
        2.  **行为纠偏**：若智能体在前一轮未能有效使用知识或响应对话，则在下一轮自动增加相应组件的权重。

#### 3. 实验设计
*   **实验场景**：设计了两个涉及公共利益的讨论场景：
    1.  **土地资源利用（Land）**：涉及农民、环保人士、社区代表。
    2.  **教育资源分配（Education）**：涉及农村教师、城市家长、政策制定者。
*   **模型配置**：采用异质骨干模型以增加多样性，包括 Qwen3-8B、Llama3-8B 和 Mistral-7B。
*   **评估指标（5项）**：
    *   **响应性 (Responsiveness)**：是否回应了最近的发言。
    *   **反驳性 (Rebuttal)**：是否表达了反对意见。
    *   **非重复性 (Non-repetition)**：与自身前序发言的差异度。
    *   **证据使用 (Evidence usage)**：是否引用了检索到的外部知识。
    *   **立场转变 (Stance shift)**：发言与初始角色设定的语义一致性。
*   **对比基准**：主要对比了不同规则模板（None vs Light vs Struct）以及不同权重配置下的表现。

#### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号、数量）及推理总时长。由于该方法基于 Zero-shot 推理和动态提示词构建，不涉及模型训练或微调，因此其算力消耗主要集中在 LLM 的 API 调用或本地推理上。

#### 5. 实验数量与充分性
*   **实验规模**：每个场景包含 5 个具体的主题查询（Query），每个查询进行 10 轮对话，每组实验独立运行 5 次以获取均值和标准差。
*   **消融实验**：系统地移除了 T、M、D 组件，验证了各部分对最终对话质量的贡献。
*   **敏感性分析**：对权重参数 $W$ 和自适应步长 $\alpha$ 进行了多组对比实验。
*   **多样性测试**：对比了同质模型（全 Qwen3）与异质模型的效果。
*   **充分性评价**：实验设计较为全面，涵盖了从宏观规则到微观权重的多维度分析，且通过跨场景验证增强了结论的普遍性。

#### 6. 主要结论与发现
*   **提示词控制有效性**：通过参数化提示词可以显著调节智能体的对话行为（RQ1 得到肯定回答）。
*   **规则模板的影响**：`Light` 规则最能促进证据的使用；`Struct` 规则在减少重复方面表现最好，但过强的结构约束可能会抑制证据的自然融入。
*   **权重的调节作用**：增加角色（T）权重会显著提升对话的反驳性和立场稳定性；自适应权重能有效引导对话从初始的“知识陈述”转向后期的“互动辩论”。
*   **模型多样性**：异质模型组合比单一模型组合能产生更丰富、更具互动性的对话动态。

#### 7. 优点
*   **轻量且无需训练**：提供了一种无需昂贵训练成本即可实现智能体行为控制的新路径。
*   **高可解释性**：通过 T-M-D-R-W 组件，将复杂的对话行为拆解为可理解、可调节的参数。
*   **动态自适应**：引入了基于时间和行为的反馈机制，使对话过程更接近真实的社会互动演变。

#### 8. 不足与局限
*   **场景覆盖有限**：虽然涵盖了两个场景，但均属于公共政策讨论类，对于协作开发、创意写作等其他类型的 MAS 任务验证不足。
*   **评判者偏差**：评估高度依赖 LLM 作为 Judge Model，可能存在潜在的偏见或评分不一致风险。
*   **参数敏感性**：权重阈值（如 0.85, 1.25）和自适应步长 $\alpha$ 的设定具有一定的人为经验成分，缺乏自动化的最优参数搜索。
*   **应用限制**：该框架主要针对文本对话，对于需要复杂环境交互（如具身智能）的 MAS 场景尚需进一步扩展。

（完）
