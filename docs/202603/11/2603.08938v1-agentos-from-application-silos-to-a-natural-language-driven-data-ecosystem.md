---
title: "AgentOS: From Application Silos to a Natural Language-Driven Data Ecosystem"
title_zh: AgentOS：从应用孤岛到自然语言驱动的数据生态系统
authors: "Rui Liu, Tao Zhe, Dongjie Wang, Zijun Yao, Kunpeng Liu, Yanjie Fu, Huan Liu, Jian Pei"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08938v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 用于个性化人机交互的自然语言驱动数据生态系统
tldr: 本文针对当前大模型智能体在传统操作系统上运行导致的交互碎片化和权限管理混乱等问题，提出了AgentOS——一种以自然语言驱动的个人智能体操作系统。该系统通过Agent内核解析意图并协调模块化技能，将传统应用转化为可组合的插件。研究强调实现AgentOS本质上是一个知识发现与数据挖掘（KDD）问题，涉及意图挖掘、推荐系统和动态知识图谱，为下一代智能计算系统定义了新的研究议程。
selection_source: fresh_fetch
motivation: 传统操作系统架构与LLM智能体不匹配，导致交互碎片化、上下文断裂及安全管理难题。
method: 提出以Agent内核为核心的架构，采用自然语言界面取代桌面，并将传统应用重构为模块化技能。
result: 构建了一个实时意图挖掘和知识发现引擎，实现了通过自然语言规则自主编排工作流和检索技能。
conclusion: AgentOS将操作系统转变为持续的数据挖掘流水线，为KDD社区在智能系统构建方面指明了新方向。
---

## 摘要
开源、本地托管的智能体（intelligent agents）的迅速崛起，标志着人机交互的一个关键转折点。诸如 OpenClaw 之类的系统表明，基于大语言模型（LLM）的智能体可以自主操作本地计算环境、编排工作流并集成外部工具。然而，在当前的范式下，这些智能体仍然是运行在最初为图形用户界面（GUI）或命令行界面（CLI）设计的传统操作系统上的常规应用程序。这种架构上的不匹配导致了交互模型的碎片化、权限管理结构不良（通常被称为“影子 AI”）以及严重的上下文断裂。本文提出了一种新范式：个人智能体操作系统（AgentOS）。在 AgentOS 中，传统的 GUI 桌面被以统一自然语言或语音入口为中心的自然用户界面（NUI）所取代。系统核心演变为智能体内核（Agent Kernel），负责解释用户意图、分解任务并协调多个智能体，而传统应用程序则演变为模块化的“技能即模块”（Skills-as-Modules），使用户能够通过自然语言规则组合软件。我们认为，实现 AgentOS 从根本上变成了一个知识发现与数据挖掘（KDD）问题。智能体内核必须作为一个用于意图挖掘和知识发现的实时引擎运行。从这个视角来看，操作系统变成了一个持续的数据挖掘流水线，涉及用于工作流自动化的序列模式挖掘、用于技能检索的推荐系统以及动态演化的个人知识图谱。这些挑战为 KDD 社区在构建下一代智能计算系统方面定义了新的研究议程。

## Abstract
The rapid emergence of open-source, locally hosted intelligent agents marks a critical inflection point in human-computer interaction. Systems such as OpenClaw demonstrate that Large Language Model (LLM)-based agents can autonomously operate local computing environments, orchestrate workflows, and integrate external tools. However, within the current paradigm, these agents remain conventional applications running on legacy operating systems originally designed for Graphical User Interfaces (GUIs) or Command Line Interfaces (CLIs). This architectural mismatch leads to fragmented interaction models, poorly structured permission management (often described as "Shadow AI"), and severe context fragmentation. This paper proposes a new paradigm: a Personal Agent Operating System (AgentOS). In AgentOS, traditional GUI desktops are replaced by a Natural User Interface (NUI) centered on a unified natural language or voice portal. The system core becomes an Agent Kernel that interprets user intent, decomposes tasks, and coordinates multiple agents, while traditional applications evolve into modular Skills-as-Modules enabling users to compose software through natural language rules. We argue that realizing AgentOS fundamentally becomes a Knowledge Discovery and Data Mining (KDD) problem. The Agent Kernel must operate as a real-time engine for intent mining and knowledge discovery. Viewed through this lens, the operating system becomes a continuous data mining pipeline involving sequential pattern mining for workflow automation, recommender systems for skill retrieval, and dynamically evolving personal knowledge graphs. These challenges define a new research agenda for the KDD community in building the next generation of intelligent computing systems.

---

## 论文详细总结（自动生成）

这篇论文由堪萨斯大学、克莱姆森大学、亚利桑那州立大学和杜克大学的研究人员共同发表，提出了一种名为 **AgentOS** 的新型操作系统范式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的智能体（如 OpenClaw）虽然具备自主操作能力，但仍作为普通应用程序运行在为图形用户界面（GUI）设计的传统操作系统（如 Windows、macOS）之上。
*   **研究动机**：这种“架构不匹配”导致了三大问题：
    1.  **“影子 AI”危机**：系统缺乏对智能体行为的语义理解，导致权限管理混乱。
    2.  **“屏幕即接口”瓶颈**：智能体依赖视觉抓取（像素转文本）来操作 GUI，这种方式脆弱且缺乏语义信息。
    3.  **上下文碎片化**：应用之间相互隔离，智能体难以跨应用维持长期的用户意图。
*   **整体含义**：论文主张从底层重构操作系统，将计算机从“应用孤岛”转变为由自然语言驱动、以智能体为核心的数据生态系统。

### 2. 论文提出的方法论：核心思想与关键技术
AgentOS 的核心是将操作系统内核转变为一个**智能体内核（Agent Kernel）**，其架构分为三个关键层：
*   **单一入口（Single Port）**：取消传统的桌面、图标和任务栏，代之以统一的自然用户界面（NUI），支持语音、文本和多模态输入。
*   **智能体内核（Agent Kernel）**：
    *   **北向接口（意图解析）**：将模糊的人类指令转化为结构化的机器意图。
    *   **南向接口（多智能体编排）**：通过模型上下文协议（MCP）协调多个专门智能体，操作底层的硬件和文件系统。
    *   **资源调度**：类似于传统内核调度 CPU，Agent Kernel 负责调度 LLM 的上下文窗口、Token 预算和 API 速率限制。
*   **技能即模块（Skills-as-Modules）**：传统应用被拆解为模块化的“技能”。用户可以通过自然语言定义规则（如“收到发票后自动核对预算并草拟支付”），系统将其转化为可组合的微服务。
*   **KDD 视角下的实现**：论文强调 AgentOS 的实现本质上是**知识发现与数据挖掘（KDD）**问题，包括：
    *   **意图挖掘**：构建动态的**个人知识图谱（PKG）**以消除歧义。
    *   **技能推荐**：采用“双塔推荐架构”（用户塔 vs. 技能塔）匹配最合适的自动化逻辑。
    *   **序列模式挖掘（SPM）**：分析用户操作日志，自动优化和合成高效的工作流宏。

### 3. 实验设计
*   **性质**：本文属于**愿景/立场论文（Vision/Position Paper）**，旨在提出架构设想和研究议程，而非展示具体的实验数据。
*   **评估框架（Benchmark）**：论文对比了传统 OS 与 AgentOS 的评估维度，提出应使用 **意图对齐（Intent Alignment）**、任务完成率和工具调用准确率作为 KPI。
*   **提及的基准测试**：引用了 *AndroidArena*（模拟环境）、*Agentic Benchmark Checklist (ABC)* 和 *Tri-Agent* 框架作为未来评估此类系统的标准。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：由于是架构设计论文，重点在于系统逻辑和 KDD 挑战的论述，不涉及特定大模型的训练过程。

### 5. 实验数量与充分性
*   **实验数量**：文中没有进行大规模的实证实验或消融实验。
*   **充分性评价**：作为一篇愿景论文，其充分性体现在对现有技术瓶颈（如 OpenClaw 的局限性）的深度剖析，以及对 KDD 技术如何解决系统问题的逻辑推演。它为 KDD 社区提供了一个清晰的研究路线图，而非最终的实验结论。

### 6. 主要结论与发现
*   **范式转移**：操作系统必须从“进程调度”转向“意图编排”。
*   **KDD 的核心地位**：未来的操作系统将是一个持续的数据挖掘流水线，能够从非结构化交互中提取可执行的意图。
*   **安全重构**：需要引入**语义防火墙（Semantic Firewall）**，根据请求的“语义意图”而非简单的 ACL 权限来拦截恶意行为（如间接提示词注入）。
*   **容错机制**：系统应具备基于文件系统快照（如 ZFS/Btrfs）的“状态回滚”功能，以应对 LLM 的幻觉导致的错误操作。

### 7. 优点
*   **前瞻性强**：敏锐地捕捉到了本地 AI 智能体爆发带来的系统级挑战。
*   **架构清晰**：提出了从单一入口到智能体内核的完整层级结构，具有很强的理论指导意义。
*   **跨学科融合**：成功地将传统的操作系统研究与现代的数据挖掘、推荐系统技术结合在一起。

### 8. 不足与局限
*   **缺乏实证**：目前仅停留在架构设计阶段，缺乏原型系统的性能表现数据（如延迟、功耗）。
*   **生态迁移难度**：将现有的数百万个 GUI 应用转化为“技能模块”是一个巨大的生态工程挑战。
*   **隐私风险**：高度集成的个人知识图谱（PKG）虽然提升了智能化，但也成为了极具吸引力的隐私攻击目标。
*   **计算开销**：实时进行意图挖掘和多智能体协调对本地算力提出了极高要求，文中未讨论如何在端侧设备上平衡性能与功耗。

（完）
