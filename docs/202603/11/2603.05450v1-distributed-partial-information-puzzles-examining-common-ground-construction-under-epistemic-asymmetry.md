---
title: "Distributed Partial Information Puzzles: Examining Common Ground Construction Under Epistemic Asymmetry"
title_zh: 分布式部分信息谜题：考察认知不对称下的共同基础构建
authors: "Yifan Zhu, Mariah Bradford, Kenneth Lai, Timothy Obiso, Videep Venkatesha, James Pustejovsky, Nikhil Krishnaswamy"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05450v1"
tags: ["query:hci-ai"]
score: 9.0
evidence: 多模态人机协作中的共识构建
tldr: 本研究探讨了AI在信息不对称环境下的协作能力，提出了“分布式部分信息谜题”（DPIP）任务及相应的多模态数据集。该数据集涵盖语音、手势和动作，用于分析共同认知的构建过程。研究对比了大型语言模型（LLM）与基于动态认识逻辑（DEL）的推理方法，发现LLM在追踪任务进度和信念状态方面存在显著短板。这一工作为提升AI在复杂多方协作中的认知建模能力提供了重要基准和见解。
selection_source: fresh_fetch
motivation: 建立共同认知是协作的基础，但当前AI在处理信息不对称的多模态协作任务时仍面临巨大挑战。
method: 提出了DPIP协作任务并构建了多模态数据集，通过LLM提示工程和动态认识逻辑（DEL）两种范式评估共同认知的建模能力。
result: 实验表明，现代大语言模型在追踪任务进度和信念状态方面表现欠佳，难以有效处理复杂的认知不对称问题。
conclusion: DPIP任务为评估AI的协作认知能力提供了新基准，强调了在多模态交互中显式建模信念动态的必要性。
---

## 摘要
建立共同基础（Common Ground，即一组共享的信念和相互认可的事实）是协作的基础，但对于当前的 AI 系统来说仍然是一个挑战，特别是在多模态、多方参与且协作方持有不同信息的场景中。我们引入了分布式部分信息谜题（DPIP），这是一种协作构建任务，旨在诱发认知不对称下的丰富多模态交流。我们提出了一个关于这些交互的多模态数据集，该数据集在语音、手势和动作模态上进行了标注和时间对齐，以支持对命题内容和信念动态的推理。随后，我们评估了两种建模共同基础（CG）的范式：(1) 最先进的大型语言模型（LLMs），通过提示从多模态更新中推断共享信念；(2) 基于动态认知逻辑（DEL）的公理化流水线，以增量方式执行相同的任务。在标注的 DPIP 数据上的结果表明，该任务对现代 LLMs 追踪任务进度和信念状态的能力提出了挑战。

## Abstract
Establishing common ground, a shared set of beliefs and mutually recognized facts, is fundamental to collaboration, yet remains a challenge for current AI systems, especially in multimodal, multiparty settings, where the collaborators bring different information to the table. We introduce the Distributed Partial Information Puzzle (DPIP), a collaborative construction task that elicits rich multimodal communication under epistemic asymmetry. We present a multimodal dataset of these interactions, annotated and temporally aligned across speech, gesture, and action modalities to support reasoning over propositional content and belief dynamics. We then evaluate two paradigms for modeling common ground (CG): (1) state-of-the-art large language models (LLMs), prompted to infer shared beliefs from multimodal updates, and (2) an axiomatic pipeline grounded in Dynamic Epistemic Logic (DEL) that incrementally performs the same task. Results on the annotated DPIP data indicate that it poses a challenge to modern LLMs' abilities to track both task progression and belief state.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed Partial Information Puzzles: Examining Common Ground Construction Under Epistemic Asymmetry》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：有效的协作依赖于“共同基础”（Common Ground, CG），即参与者之间共享的信念和事实。然而，在现实协作中，参与者往往拥有不同的背景和信息（认知不对称）。
*   **核心问题**：当前的 AI 系统（尤其是大语言模型）在多模态（语音、手势、动作）、多方参与且信息不对称的复杂环境下，能否有效地追踪任务进度并构建/推断参与者之间的共同信念？
*   **背景**：现有的对话数据集大多缺乏“认知不对称”或“具身动作”的结合，导致难以评估 AI 在处理复杂人类协作逻辑时的真实能力。

### 2. 方法论
论文提出了一种全新的协作范式和配套的推理框架：
*   **DPIP 任务设计**：
    *   **角色分配**：3名“导演”（Directors）和1名“建筑师”（Builder）。
    *   **信息分布**：每位导演仅能看到目标乐高结构的一个侧面视图（如前、左、右），且不知道自己看的是哪一面；建筑师负责动手搭建，但看不到任何视图。
    *   **协作目标**：导演们必须通过交流，引导建筑师搭建出符合所有侧面视图的唯一正确结构。
*   **多模态标注流水线**：
    *   对语音（Whisper 转录+人工校对）、动作（通过 3D 标注工具记录块的位置变化）和手势（使用 GAMR 框架标注含义）进行时间对齐。
    *   将所有模态转化为命题逻辑形式（如 `on(block_A, block_B)`）。
*   **共同基础计算（CGC）框架**：
    *   基于**动态认知逻辑（DEL）**，设定三大公理：**眼见为实**（感知更新信念）、**动作为实**（具身动作揭示意图）、**言说为实**（语言交流状态）。
    *   通过公理化流水线增量式地更新参与者的信念集，当至少两名成员达成一致且无异议时，将其存入“共同基础”池。

### 3. 实验设计
*   **数据集**：DPIP Lego Dataset，包含 10 组四人协作的录像及详尽标注（其中 1 组为任务失败的离群组）。
*   **Benchmark 任务**：
    1.  **Action → Structure**：仅根据动作序列预测最终结构（基准测试）。
    2.  **Aligned → Structure**：根据对齐的多模态标注预测结构。
    3.  **CGC → Structure**：利用公理化推理得出的共同基础来预测结构。
    4.  **Aligned → CGC**：让模型直接预测参与者的“共享信念集”，并与公理化计算结果对比。
*   **对比方法**：
    *   **LLMs**：Llama-3.2-3B-Instruct, Qwen3-4B-Instruct, GPT-5-mini / GPT-5。
    *   **逻辑基准**：基于 DEL 的公理化推理流水线。
*   **评价指标**：Dice 相似系数（DSC），用于衡量预测结构/信念与真实状态的重合度。

### 4. 资源与算力
*   **算力说明**：论文提到在“有限的计算预算”下进行了实验。
*   **具体使用**：使用了本地部署的开源模型（Qwen3-4B, Llama-3.2-3B）以及 OpenAI 的 API（GPT-5 系列）。
*   **训练/推理时长**：文中未明确记录具体的训练时长或 GPU 硬件型号，实验重点在于对预训练模型的 Prompting（提示工程）评估而非从头训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 组真实的协作对话，总时长约 20 小时（论文分析了其中已完全标注的 10 组）。
*   **充分性评价**：
    *   **多维度验证**：设计了四个不同的子任务，从物理结构预测到抽象信念追踪，逻辑链条完整。
    *   **包含失败案例**：专门分析了任务失败组（Group 7），验证了模型在“缺乏共识”环境下的表现，增强了实验的客观性。
    *   **统计显著性**：使用了 Wilcoxon 符号秩检验来验证模型间差异的显著性。
    *   **局限性**：样本量（10 组对话）对于深度学习评估来说偏小，但考虑到多模态标注的极高复杂度，这在同类研究中属于合理范围。

### 6. 主要结论与发现
*   **LLM 的短板**：即使是最先进的模型（如 GPT-5），在处理长程、多回合的动作追踪时表现也会下降。
*   **多模态的“双刃剑”效应**：增加手势和语音模态在全局范围内有助于理解，但在局部回合中可能引入噪声，导致 LLM 的步进式预测准确率反而下降。
*   **逻辑推理的优势**：基于公理的 CGC 框架在预测物理结构方面有时优于 LLM，说明显式的逻辑建模在处理认知不对称时更稳健。
*   **共识检测能力**：LLM 擅长检测“共识缺失”（如在失败组中表现完美），但在推断“复杂共识的具体内容”时与人类/逻辑基准存在显著偏差。

### 7. 优点
*   **任务创新**：DPIP 任务极好地模拟了现实中专家团队协作时的信息差问题。
*   **高质量数据**：提供了极其精细的时间对齐多模态标注，为后续研究提供了宝贵资源。
*   **跨学科结合**：将传统的动态认知逻辑（DEL）与现代 LLM 评估相结合，提供了更深入的认知科学视角。

### 8. 不足与局限
*   **视角受限**：标注仅基于单一摄像机视角，可能存在物理遮挡导致的标注误差。
*   **模型规模限制**：受限于预算，未在大规模参数模型上进行全量微调。
*   **泛化性风险**：乐高任务虽然具有代表性，但其空间关系相对标准，在更开放、非结构化的协作场景下，公理化方法的有效性尚待验证。
*   **样本量**：10 组数据虽然标注深入，但难以覆盖所有可能的人际交互异常情况。

（完）
