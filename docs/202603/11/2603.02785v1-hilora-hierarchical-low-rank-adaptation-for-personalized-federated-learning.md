---
title: "HiLoRA: Hierarchical Low-Rank Adaptation for Personalized Federated Learning"
title_zh: HiLoRA：面向个性化联邦学习的分层低秩自适应
authors: "Zihao Peng, Nan Zou, Jiandian Zeng, Guo Li, Ke Chen, Boyuan Li, Tian Wang"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02785v1"
tags: ["query:llm-personalization"]
score: 9.0
evidence: 用于个性化联邦学习的分层LoRA，旨在捕获特定客户端的知识
tldr: 针对联邦学习中视觉Transformer微调通信开销大且现有LoRA方法忽略客户端潜在结构的问题，本文提出HiLoRA框架。该框架通过在根、簇、叶三个层级部署适配器，分别捕获全局、子组和客户端特定知识。结合跨层正交性约束、级联优化及子空间自适应聚类机制，HiLoRA在降低通信成本的同时，显著提升了模型的个性化能力和对未知客户端的泛化性能。
selection_source: fresh_fetch
motivation: 现有的联邦LoRA微调方法未能充分利用客户端间的潜在结构，限制了共享表示的学习效果及对新客户端的适应力。
method: 设计了包含根、簇、叶三级适配器的分层架构，并引入子空间自适应聚类与级联优化机制来实现多尺度知识的解耦与共享。
result: 在CIFAR-100和DomainNet等数据集上的实验表明，HiLoRA在个性化准确率和泛化性能方面均取得了显著提升。
conclusion: HiLoRA通过分层适配和结构化知识挖掘，为大规模预训练模型在个性化联邦学习场景下的高效微调提供了有效路径。
---

## 摘要
Vision Transformer (ViT) 因其强大的迁移能力在视觉任务中被广泛采用。在联邦学习 (FL) 中，全量微调会带来沉重的通信负担，而低秩自适应 (LoRA) 提供了一种高效且通信友好的方式来适配 ViT。然而，现有的基于 LoRA 的联邦微调方法忽略了现实场景中潜在的客户端结构，限制了共享表示学习，并阻碍了对未知客户端的有效适配。为此，我们提出了 HiLoRA，这是一个分层 LoRA 框架，在根节点 (root)、聚类 (cluster) 和叶节点 (leaf) 三个层级部署自适应器，分别旨在捕获全局、子组和客户端特定的知识。通过跨层正交性和级联优化，HiLoRA 分离了更新子空间，并使每一层与其残差个性化目标保持一致。特别地，我们开发了一种 LoRA 子空间自适应聚类机制，通过子空间相似性分析推断潜在的客户端分组，从而促进结构对齐的客户端之间的知识共享。在理论上，我们建立了支持 HiLoRA 设计的分层泛化分析。在 CIFAR-100 和 DomainNet 上使用 ViT 骨干网络的实验表明，该方法在个性化和泛化性能方面均有持续提升。

## Abstract
Vision Transformers (ViTs) have been widely adopted in vision tasks due to their strong transferability. In Federated Learning (FL), where full fine-tuning is communication heavy, Low-Rank Adaptation (LoRA) provides an efficient and communication-friendly way to adapt ViTs. However, existing LoRA-based federated tuning methods overlook latent client structures in real-world settings, limiting shared representation learning and hindering effective adaptation to unseen clients. To address this, we propose HiLoRA, a hierarchical LoRA framework that places adapters at three levels: root, cluster, and leaf, each designed to capture global, subgroup, and client-specific knowledge, respectively. Through cross-tier orthogonality and cascaded optimization, HiLoRA separates update subspaces and aligns each tier with its residual personalized objective. In particular, we develop a LoRA-Subspace Adaptive Clustering mechanism that infers latent client groups via subspace similarity analysis, thereby facilitating knowledge sharing across structurally aligned clients. Theoretically, we establish a tier-wise generalization analysis that supports HiLoRA's design. Experiments on ViT backbones with CIFAR-100 and DomainNet demonstrate consistent improvements in both personalization and generalization.

---

## 论文详细总结（自动生成）

### HiLoRA：面向个性化联邦学习的分层低秩自适应论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大规模预训练模型（如 Vision Transformers, ViTs）的普及，如何在联邦学习（FL）环境下高效微调这些模型成为关键。现有的低秩自适应（LoRA）方法虽然降低了通信开销，但在个性化联邦学习（pFL）中面临以下挑战：
*   **全局漂移与局部过拟合**：单一全局 LoRA 难以适配异构数据，而纯局部 LoRA 在数据稀疏时易过拟合。
*   **忽略潜在结构**：现有的“双适配器”（全局+局部）设计忽略了客户端之间天然存在的子组（Cluster）结构，导致子组间的共享知识被稀释或浪费。
*   **泛化性不足**：难以有效处理未见过的（Unseen）新客户端。

#### 2. 论文提出的方法论
HiLoRA 提出了一个**三层分层 LoRA 架构**，旨在解耦并捕获不同粒度的知识：
*   **核心思想**：将权重更新 $\Delta W$ 分解为三个正交层级：
    1.  **Root-LoRA（根层）**：捕获所有客户端共有的全局模式。
    2.  **Cluster-LoRA（簇层）**：捕获具有相似分布的客户端子组间的共享知识。
    3.  **Leaf-LoRA（叶层）**：捕获每个客户端特有的残差模式。
*   **关键技术细节**：
    *   **LoRA 子空间自适应聚类**：利用 SVD 提取客户端 LoRA 更新的左奇异向量（子空间基数），通过计算主角（Principal Angles）的相似度来衡量客户端间的距离，并使用谱聚类自动划分客户端。
    *   **跨层正交性约束**：在损失函数中加入正则项（如 $\|B_r^\top B_c\|_F^2$），强制不同层级的适配器在正交的子空间中更新，防止知识冗余和层间干扰。
    *   **级联优化流程**：采用“先根、后簇、再叶”的训练顺序。训练下一层时冻结上一层，确保每一层只学习上一层无法解释的残差信息。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CIFAR-100**：设置了三种 Non-IID 划分：标签倾斜（GL-Dir）、超类倾斜（SC-Dir）和病理划分（Patho）。
    *   **DomainNet**：包含 6 个不同领域（如草图、照片等）的跨域异构场景。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流方法，包括单分支方法（FedIT, FlexLoRA）、双分支个性化方法（FedSA-LoRA, FDLoRA, FedDPA, PF2LoRA）以及自适应门控方法（FedALT）。
*   **评价指标**：个性化准确率（均值及 10% 分位数以衡量尾部鲁棒性）和对未知客户端的泛化准确率。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练总时长。
*   **训练配置**：提到使用了在 ImageNet-21K 上预训练的 ViT-Base 作为骨干网络，总训练预算设定为 50 个全局轮次，以确保与基线方法在通信和计算成本上的公平比较。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了多维度测试，涵盖了 100 个客户端的规模。
*   **消融实验**：详细分析了分层结构、子空间聚类和正交性约束各自的贡献。
*   **客观性与公平性**：
    *   通过控制总训练轮次（$T_{root} + T_{cluster} + T_{leaf} = 50$）来保证对比的公平性。
    *   引入了理论泛化界限分析（Theorem 1），为实验结果提供了数学支撑。
    *   使用了 t-SNE 可视化和主角分布分析，增强了实验结果的可解释性。

#### 6. 主要结论与发现
*   **性能领先**：HiLoRA 在所有 Non-IID 设置下的个性化准确率均优于 SOTA 方法，尤其在 SC-Dir 划分下提升显著。
*   **泛化能力强**：对于新客户端，通过简单的子空间探测将其归类到现有簇，即可获得极高的初始性能，且微调收敛极快。
*   **层级增益**：实验证实了从 Root 到 Cluster 再到 Leaf，模型性能稳步提升，且客户端间的性能方差逐渐减小。

#### 7. 优点
*   **结构化知识挖掘**：通过三层架构巧妙地解决了全局一致性与局部特异性之间的矛盾。
*   **聚类机制创新**：基于 LoRA 子空间相似度的聚类比传统的基于参数权重或梯度的聚类更具鲁棒性，且不暴露原始数据。
*   **理论与实践结合**：提供了严谨的泛化误差界限证明，解释了正交性和聚类如何收紧泛化界。

#### 8. 不足与局限
*   **级联优化的潜在风险**：级联训练意味着如果 Root 层训练不足或偏差过大，可能会影响后续 Cluster 和 Leaf 层的学习效果。
*   **计算开销**：虽然通信高效，但服务器端进行 SVD 分解和谱聚类会增加额外的计算负担。
*   **超参数敏感性**：引入了多个正则化系数（$\gamma_c, \gamma_l$）和聚类参数，可能需要针对不同任务进行精细调优。
*   **Rank 分配**：目前各层使用相同的 Rank，未探讨在不同层级动态分配秩（Rank）以进一步优化参数效率的可能性。

（完）
