---
title: "The Missing Memory Hierarchy: Demand Paging for LLM Context Windows"
title_zh: 缺失的存储层次结构：大语言模型上下文窗口的请求分页
authors: Tony Mason
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.09023v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: LLM上下文窗口的需求分页与内存管理
tldr: "本研究指出当前大语言模型将上下文窗口视为唯一的L1缓存，缺乏多级存储体系，导致约21.8%的结构性浪费。为此，作者提出了Pichay系统，通过在客户端与API间建立透明代理，实现请求分页机制。该系统能自动驱逐陈旧内容、检测缺页中断并固定活跃工作集。实验表明，Pichay在生产环境中最高可减少93%的上下文消耗，证明了借鉴虚拟内存理论解决LLM上下文限制和成本问题的可行性。"
selection_source: fresh_fetch
motivation: 当前LLM缺乏类似操作系统的虚拟内存管理，导致大量陈旧信息占据昂贵的上下文空间并造成严重的资源浪费。
method: 提出Pichay系统，通过透明代理实现请求分页，包括陈旧内容驱逐、缺页检测及基于故障历史的工作集固定。
result: "在生产部署中，该系统将上下文消耗最高降低了93%，且离线回放中的缺页率仅为0.0254%。"
conclusion: LLM的上下文限制和成本问题本质上是虚拟内存管理问题，可通过引入多级存储层次结构和请求分页机制有效解决。
---

## 摘要
大语言模型的上下文窗口并非内存。它是 L1 缓存：一种小容量、高速且昂贵的资源，而该领域却将其视为整个存储系统。这里没有 L2 缓存，没有虚拟内存，也没有分页机制。每一个工具定义、每一条系统提示以及每一个过时的工具结果，在整个会话生命周期内都会占用上下文。结果是可衡量的：在 857 个生产会话和 445 万个有效输入 token 中，结构性浪费占比达 21.8%。我们提出了 Pichay，一个针对大语言模型上下文窗口的请求分页系统。Pichay 作为客户端与推理 API 之间的透明代理实现，它介入消息流以逐出陈旧内容，在模型重新请求已逐出材料时检测缺页异常，并根据缺页历史记录固定工作集页面。在 140 万次模拟逐出的离线回放中，缺页率为 0.0254%。在超过 681 轮的生产环境部署中，该系统将上下文消耗降低了高达 93%（从 5,038KB 降至 339KB）；在极端持续压力下，系统仍能保持运行，但表现出预期的抖动病态，即已逐出内容的重复调入。关键观察在于，该领域面临的问题（如上下文限制、注意力退化、成本扩展、跨会话状态丢失）本质上是披着不同外衣的虚拟内存问题。解决方案已经存在：工作集理论（Denning, 1968）、请求分页、缺页驱动的替换策略，以及具有多个逐出管理层级的存储层次结构。我们描述了 LLM 系统完整存储层次结构（从 L1 到持久化存储）的架构，报告了在生产中部署的前三个层级（L1 逐出、L2 缺页驱动固定、L3 模型发起的对话压缩），并将跨会话内存确定为尚待探索的前沿领域。

## Abstract
The context window of a large language model is not memory. It is L1 cache: a small, fast, expensive resource that the field treats as the entire memory system. There is no L2, no virtual memory, no paging. Every tool definition, every system prompt, and every stale tool result occupies context for the lifetime of the session. The result is measurable: across 857 production sessions and 4.45 million effective input tokens, 21.8% is structural waste.   We present Pichay, a demand paging system for LLM context windows. Implemented as a transparent proxy between client and inference API, Pichay interposes on the message stream to evict stale content, detect page faults when the model re-requests evicted material, and pin working-set pages identified by fault history. In offline replay across 1.4 million simulated evictions, the fault rate is 0.0254%. In live production deployment over 681turns, the system reduces context consumption by up to 93% (5,038KB to 339KB); under extreme sustained pressure, the system remains operational but exhibits the expected thrashing pathology, with repeated fault-in of evicted content.   The key observation is that the problems the field faces, such as context limits, attention degradation, cost scaling, lost state across sessions, are virtual memory problems wearing different clothes. The solutions exist: working set theory (Denning, 1968), demand paging, fault-driven replacement policies, and memory hierarchies with multiple eviction-managed levels. We describe the architecture of a full memory hierarchy for LLM systems (L1 through persistent storage), report on the first three levels deployed in production use (L1 eviction, L2 fault-driven pinning, L3 model-initiated conversation compaction), and identify cross-session memory as the remaining frontier.

---

## 论文详细总结（自动生成）

这篇论文由 Tony Mason 撰写，探讨了如何将操作系统中的**虚拟内存（Virtual Memory）**和**存储层次结构（Memory Hierarchy）**理论引入大语言模型（LLM）的上下文窗口管理中，以解决当前 AI Agent 存在的资源浪费和成本爆炸问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：论文指出，当前的 LLM 领域将“上下文窗口”误视为内存，而它本质上是 **L1 缓存**（极快、极贵、容量极小）。
*   **现状弊端**：目前的 AI Agent（如 Claude Code, Cursor）采用“手动覆盖（Overlays）”模式，即每次 API 调用都重新发送完整的系统提示、工具定义和历史记录。这种“全量堆积”导致了严重的**结构性浪费**。
*   **研究动机**：通过对 857 个生产会话（44.5 亿 token）的分析，发现 21.8% 的 token 是无意义的浪费（如未使用的工具架构、重复内容、陈旧的工具结果）。作者旨在通过构建类似操作系统的存储管理系统来消除这种浪费。

### 2. 方法论：Pichay 系统
作者开发了名为 **Pichay** 的系统，其核心思想是在客户端和推理 API 之间建立一个**透明代理（Transparent Proxy）**，实现“请求分页（Demand Paging）”。

*   **存储层次结构**：
    *   **L1 (生成窗口)**：当前的活跃上下文。
    *   **L2 (工作集)**：通过缺页历史识别并固定的活跃页面。
    *   **L3 (会话历史)**：由模型自主发起的对话压缩/摘要。
    *   **L4 (跨会话内存)**：持久化的索引和检索存储。
*   **关键技术细节**：
    *   **垃圾回收 vs. 分页**：区分临时输出（如 Bash 结果，不可重请求）和可寻址内容（如文件读取，可重新调入）。
    *   **缺页检测（Page Fault）**：当模型请求已被驱逐的内容时，代理层捕获该事件并重新调入。
    *   **故障驱动固定（Fault-Driven Pinning）**：如果某个页面被驱逐后立即触发缺页，系统将其永久固定在 L2 中，防止重复驱逐导致的“抖动”。
    *   **协作式内存管理**：引入“幻影工具（Phantom Tools）”和“清理标签（Cleanup Tags）”，允许模型主动释放内存或请求压缩历史。
*   **反向成本模型（Inverted Cost Model）**：论文提出一个重要公式，指出在 LLM 中“保留 token”比“重新读取（缺页）”更贵。因此，最优策略是**激进驱逐**：只要一个页面在下一轮不被使用，就应该驱逐。

### 3. 实验设计
*   **数据集/场景**：基于作者 4 个月内使用 Claude Code 的真实生产数据，包含 857 个会话、54,170 次 API 调用。
*   **实验阶段**：
    1.  **离线分析**：对 140 万次模拟驱逐进行回放，测量缺页率。
    2.  **在线部署**：在真实开发流中运行 Pichay，观察长达 681 轮的极端会话。
    3.  **等效性检查**：使用 LLM 评审团（Sonnet 4, Haiku 4.5）对“原始上下文”和“Pichay 压缩后上下文”生成的回答进行盲测。
*   **对比基准（Benchmark）**：Baseline（无管理）、Trimmed（仅精简工具定义）、Compact+Trim（分页+精简）。

### 4. 资源与算力
*   **模型使用**：主要使用 Claude 3.5 Opus, Sonnet 4 和 Haiku 4.5。
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为 Pichay 是一个**推理侧的代理系统**，不涉及模型训练。实验运行在本地 WSL 环境和 Ubuntu 虚拟机上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 **44.5 亿**个有效输入 token，样本量极大。
    *   离线回放模拟了 **139.3 万**次驱逐，缺页率仅为 **0.0254%**，具有极强的统计说服力。
    *   在线部署包含 681 轮的超长会话，成功观察到了操作系统理论中的“抖动（Thrashing）”病态。
*   **充分性评价**：实验设计非常全面，涵盖了从宏观统计到微观个案分析，并引入了 LLM 评审团进行主观质量评估，实验结果客观且公平。

### 6. 主要结论与发现
*   **浪费严重**：生产环境中 21.8% 的 token 是结构性浪费。
*   **分页有效**：Pichay 在生产中将上下文消耗最高降低了 **93%**（5MB 降至 339KB）。
*   **模型理解力**：模型能够理解“内容已分页，如需请重读”的占位符，无需额外指令即可自主触发缺页处理。
*   **注意力集中效应**：移除无关的陈旧信息不仅降低了成本，还通过减少注意力稀释提高了模型的回答质量。

### 7. 优点
*   **理论跨界**：完美地将 60 年前的操作系统虚拟内存理论映射到现代 LLM 架构上。
*   **架构优雅**：作为透明代理实现，无需修改模型、客户端或 API，具有极强的实用性。
*   **成本洞察**：提出了“反向成本模型”，为 LLM 经济学提供了新的理论支撑。

### 8. 不足与局限
*   **样本偏差**：数据来源于单一的高级用户（作者本人），虽然是生产数据，但可能无法完全代表所有用户的行为模式。
*   **任务局限**：主要集中在“Agentic Coding（智能编程）”领域，在创意写作或多轮闲聊等其他场景下的表现尚未验证。
*   **L3/L4 验证不足**：论文详细评估了 L1/L2 层级，但 L3（对话压缩）和 L4（跨会话持久化）的评估尚处于初步阶段。
*   **抖动风险**：在工作集超过物理上下文限制时，系统会陷入频繁的缺页循环，导致成本反而上升。

（完）
